# Expo/Metro resolve modules through a flat node_modules tree. pnpm's default
# isolated linker hides transitive Expo and react-native-web internals from
# Metro, so the mobile app fails to bundle without hoisted linking.
# See https://docs.expo.dev/guides/monorepo/
node-linker=hoisted
