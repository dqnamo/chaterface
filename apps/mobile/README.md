# Chaterface Mobile

Expo mobile app for the Chaterface workspace.

Required environment:

- `EXPO_PUBLIC_INSTANT_APP_ID` must match the Instant app used by the web app.
- `EXPO_PUBLIC_API_URL` should point at the web app/API origin for GitHub credential saving and diff loading.

Run locally:

```sh
pnpm --filter mobile start
```
