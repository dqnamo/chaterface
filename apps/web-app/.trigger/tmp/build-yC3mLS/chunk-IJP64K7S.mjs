import {
  __commonJS,
  __esm,
  __export,
  __name,
  __require,
  __toCommonJS,
  init_esm
} from "./chunk-5VFD3YHA.mjs";

// ../../node_modules/.pnpm/openapi-fetch@0.14.1/node_modules/openapi-fetch/dist/index.cjs
var require_dist = __commonJS({
  "../../node_modules/.pnpm/openapi-fetch@0.14.1/node_modules/openapi-fetch/dist/index.cjs"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    var PATH_PARAM_RE = /\{[^{}]+\}/g;
    var supportsRequestInitExt = /* @__PURE__ */ __name(() => {
      return typeof process === "object" && Number.parseInt(process?.versions?.node?.substring(0, 2)) >= 18 && process.versions.undici;
    }, "supportsRequestInitExt");
    function randomID() {
      return Math.random().toString(36).slice(2, 11);
    }
    __name(randomID, "randomID");
    function createClient(clientOptions) {
      let {
        baseUrl = "",
        Request: CustomRequest = globalThis.Request,
        fetch: baseFetch = globalThis.fetch,
        querySerializer: globalQuerySerializer,
        bodySerializer: globalBodySerializer,
        headers: baseHeaders,
        requestInitExt = void 0,
        ...baseOptions
      } = { ...clientOptions };
      requestInitExt = supportsRequestInitExt() ? requestInitExt : void 0;
      baseUrl = removeTrailingSlash(baseUrl);
      const middlewares = [];
      async function coreFetch(schemaPath, fetchOptions) {
        const {
          baseUrl: localBaseUrl,
          fetch: fetch2 = baseFetch,
          Request: Request2 = CustomRequest,
          headers,
          params = {},
          parseAs = "json",
          querySerializer: requestQuerySerializer,
          bodySerializer = globalBodySerializer ?? defaultBodySerializer,
          body,
          ...init
        } = fetchOptions || {};
        let finalBaseUrl = baseUrl;
        if (localBaseUrl) {
          finalBaseUrl = removeTrailingSlash(localBaseUrl) ?? baseUrl;
        }
        let querySerializer = typeof globalQuerySerializer === "function" ? globalQuerySerializer : createQuerySerializer(globalQuerySerializer);
        if (requestQuerySerializer) {
          querySerializer = typeof requestQuerySerializer === "function" ? requestQuerySerializer : createQuerySerializer({
            ...typeof globalQuerySerializer === "object" ? globalQuerySerializer : {},
            ...requestQuerySerializer
          });
        }
        const serializedBody = body === void 0 ? void 0 : bodySerializer(
          body,
          // Note: we declare mergeHeaders() both here and below because it’s a bit of a chicken-or-egg situation:
          // bodySerializer() needs all headers so we aren’t dropping ones set by the user, however,
          // the result of this ALSO sets the lowest-priority content-type header. So we re-merge below,
          // setting the content-type at the very beginning to be overwritten.
          // Lastly, based on the way headers work, it’s not a simple “present-or-not” check becauase null intentionally un-sets headers.
          mergeHeaders(baseHeaders, headers, params.header)
        );
        const finalHeaders = mergeHeaders(
          // with no body, we should not to set Content-Type
          serializedBody === void 0 || // if serialized body is FormData; browser will correctly set Content-Type & boundary expression
          serializedBody instanceof FormData ? {} : {
            "Content-Type": "application/json"
          },
          baseHeaders,
          headers,
          params.header
        );
        const requestInit = {
          redirect: "follow",
          ...baseOptions,
          ...init,
          body: serializedBody,
          headers: finalHeaders
        };
        let id;
        let options;
        let request = new Request2(
          createFinalURL(schemaPath, { baseUrl: finalBaseUrl, params, querySerializer }),
          requestInit
        );
        let response;
        for (const key in init) {
          if (!(key in request)) {
            request[key] = init[key];
          }
        }
        if (middlewares.length) {
          id = randomID();
          options = Object.freeze({
            baseUrl: finalBaseUrl,
            fetch: fetch2,
            parseAs,
            querySerializer,
            bodySerializer
          });
          for (const m of middlewares) {
            if (m && typeof m === "object" && typeof m.onRequest === "function") {
              const result = await m.onRequest({
                request,
                schemaPath,
                params,
                options,
                id
              });
              if (result) {
                if (result instanceof Request2) {
                  request = result;
                } else if (result instanceof Response) {
                  response = result;
                  break;
                } else {
                  throw new Error("onRequest: must return new Request() or Response() when modifying the request");
                }
              }
            }
          }
        }
        if (!response) {
          try {
            response = await fetch2(request, requestInitExt);
          } catch (error2) {
            let errorAfterMiddleware = error2;
            if (middlewares.length) {
              for (let i = middlewares.length - 1; i >= 0; i--) {
                const m = middlewares[i];
                if (m && typeof m === "object" && typeof m.onError === "function") {
                  const result = await m.onError({
                    request,
                    error: errorAfterMiddleware,
                    schemaPath,
                    params,
                    options,
                    id
                  });
                  if (result) {
                    if (result instanceof Response) {
                      errorAfterMiddleware = void 0;
                      response = result;
                      break;
                    }
                    if (result instanceof Error) {
                      errorAfterMiddleware = result;
                      continue;
                    }
                    throw new Error("onError: must return new Response() or instance of Error");
                  }
                }
              }
            }
            if (errorAfterMiddleware) {
              throw errorAfterMiddleware;
            }
          }
          if (middlewares.length) {
            for (let i = middlewares.length - 1; i >= 0; i--) {
              const m = middlewares[i];
              if (m && typeof m === "object" && typeof m.onResponse === "function") {
                const result = await m.onResponse({
                  request,
                  response,
                  schemaPath,
                  params,
                  options,
                  id
                });
                if (result) {
                  if (!(result instanceof Response)) {
                    throw new Error("onResponse: must return new Response() when modifying the response");
                  }
                  response = result;
                }
              }
            }
          }
        }
        if (response.status === 204 || request.method === "HEAD" || response.headers.get("Content-Length") === "0") {
          return response.ok ? { data: void 0, response } : { error: void 0, response };
        }
        if (response.ok) {
          if (parseAs === "stream") {
            return { data: response.body, response };
          }
          return { data: await response[parseAs](), response };
        }
        let error = await response.text();
        try {
          error = JSON.parse(error);
        } catch {
        }
        return { error, response };
      }
      __name(coreFetch, "coreFetch");
      return {
        request(method, url, init) {
          return coreFetch(url, { ...init, method: method.toUpperCase() });
        },
        /** Call a GET endpoint */
        GET(url, init) {
          return coreFetch(url, { ...init, method: "GET" });
        },
        /** Call a PUT endpoint */
        PUT(url, init) {
          return coreFetch(url, { ...init, method: "PUT" });
        },
        /** Call a POST endpoint */
        POST(url, init) {
          return coreFetch(url, { ...init, method: "POST" });
        },
        /** Call a DELETE endpoint */
        DELETE(url, init) {
          return coreFetch(url, { ...init, method: "DELETE" });
        },
        /** Call a OPTIONS endpoint */
        OPTIONS(url, init) {
          return coreFetch(url, { ...init, method: "OPTIONS" });
        },
        /** Call a HEAD endpoint */
        HEAD(url, init) {
          return coreFetch(url, { ...init, method: "HEAD" });
        },
        /** Call a PATCH endpoint */
        PATCH(url, init) {
          return coreFetch(url, { ...init, method: "PATCH" });
        },
        /** Call a TRACE endpoint */
        TRACE(url, init) {
          return coreFetch(url, { ...init, method: "TRACE" });
        },
        /** Register middleware */
        use(...middleware) {
          for (const m of middleware) {
            if (!m) {
              continue;
            }
            if (typeof m !== "object" || !("onRequest" in m || "onResponse" in m || "onError" in m)) {
              throw new Error("Middleware must be an object with one of `onRequest()`, `onResponse() or `onError()`");
            }
            middlewares.push(m);
          }
        },
        /** Unregister middleware */
        eject(...middleware) {
          for (const m of middleware) {
            const i = middlewares.indexOf(m);
            if (i !== -1) {
              middlewares.splice(i, 1);
            }
          }
        }
      };
    }
    __name(createClient, "createClient");
    var PathCallForwarder = class {
      static {
        __name(this, "PathCallForwarder");
      }
      constructor(client, url) {
        this.client = client;
        this.url = url;
      }
      GET = /* @__PURE__ */ __name((init) => {
        return this.client.GET(this.url, init);
      }, "GET");
      PUT = /* @__PURE__ */ __name((init) => {
        return this.client.PUT(this.url, init);
      }, "PUT");
      POST = /* @__PURE__ */ __name((init) => {
        return this.client.POST(this.url, init);
      }, "POST");
      DELETE = /* @__PURE__ */ __name((init) => {
        return this.client.DELETE(this.url, init);
      }, "DELETE");
      OPTIONS = /* @__PURE__ */ __name((init) => {
        return this.client.OPTIONS(this.url, init);
      }, "OPTIONS");
      HEAD = /* @__PURE__ */ __name((init) => {
        return this.client.HEAD(this.url, init);
      }, "HEAD");
      PATCH = /* @__PURE__ */ __name((init) => {
        return this.client.PATCH(this.url, init);
      }, "PATCH");
      TRACE = /* @__PURE__ */ __name((init) => {
        return this.client.TRACE(this.url, init);
      }, "TRACE");
    };
    var PathClientProxyHandler = class {
      static {
        __name(this, "PathClientProxyHandler");
      }
      constructor() {
        this.client = null;
      }
      // Assume the property is an URL.
      get(coreClient, url) {
        const forwarder = new PathCallForwarder(coreClient, url);
        this.client[url] = forwarder;
        return forwarder;
      }
    };
    function wrapAsPathBasedClient(coreClient) {
      const handler = new PathClientProxyHandler();
      const proxy = new Proxy(coreClient, handler);
      function Client() {
      }
      __name(Client, "Client");
      Client.prototype = proxy;
      const client = new Client();
      handler.client = client;
      return client;
    }
    __name(wrapAsPathBasedClient, "wrapAsPathBasedClient");
    function createPathBasedClient(clientOptions) {
      return wrapAsPathBasedClient(createClient(clientOptions));
    }
    __name(createPathBasedClient, "createPathBasedClient");
    function serializePrimitiveParam(name, value, options) {
      if (value === void 0 || value === null) {
        return "";
      }
      if (typeof value === "object") {
        throw new Error(
          "Deeply-nested arrays/objects aren’t supported. Provide your own `querySerializer()` to handle these."
        );
      }
      return `${name}=${options?.allowReserved === true ? value : encodeURIComponent(value)}`;
    }
    __name(serializePrimitiveParam, "serializePrimitiveParam");
    function serializeObjectParam(name, value, options) {
      if (!value || typeof value !== "object") {
        return "";
      }
      const values = [];
      const joiner = {
        simple: ",",
        label: ".",
        matrix: ";"
      }[options.style] || "&";
      if (options.style !== "deepObject" && options.explode === false) {
        for (const k in value) {
          values.push(k, options.allowReserved === true ? value[k] : encodeURIComponent(value[k]));
        }
        const final2 = values.join(",");
        switch (options.style) {
          case "form": {
            return `${name}=${final2}`;
          }
          case "label": {
            return `.${final2}`;
          }
          case "matrix": {
            return `;${name}=${final2}`;
          }
          default: {
            return final2;
          }
        }
      }
      for (const k in value) {
        const finalName = options.style === "deepObject" ? `${name}[${k}]` : k;
        values.push(serializePrimitiveParam(finalName, value[k], options));
      }
      const final = values.join(joiner);
      return options.style === "label" || options.style === "matrix" ? `${joiner}${final}` : final;
    }
    __name(serializeObjectParam, "serializeObjectParam");
    function serializeArrayParam(name, value, options) {
      if (!Array.isArray(value)) {
        return "";
      }
      if (options.explode === false) {
        const joiner2 = { form: ",", spaceDelimited: "%20", pipeDelimited: "|" }[options.style] || ",";
        const final = (options.allowReserved === true ? value : value.map((v) => encodeURIComponent(v))).join(joiner2);
        switch (options.style) {
          case "simple": {
            return final;
          }
          case "label": {
            return `.${final}`;
          }
          case "matrix": {
            return `;${name}=${final}`;
          }
          // case "spaceDelimited":
          // case "pipeDelimited":
          default: {
            return `${name}=${final}`;
          }
        }
      }
      const joiner = { simple: ",", label: ".", matrix: ";" }[options.style] || "&";
      const values = [];
      for (const v of value) {
        if (options.style === "simple" || options.style === "label") {
          values.push(options.allowReserved === true ? v : encodeURIComponent(v));
        } else {
          values.push(serializePrimitiveParam(name, v, options));
        }
      }
      return options.style === "label" || options.style === "matrix" ? `${joiner}${values.join(joiner)}` : values.join(joiner);
    }
    __name(serializeArrayParam, "serializeArrayParam");
    function createQuerySerializer(options) {
      return /* @__PURE__ */ __name(function querySerializer(queryParams) {
        const search = [];
        if (queryParams && typeof queryParams === "object") {
          for (const name in queryParams) {
            const value = queryParams[name];
            if (value === void 0 || value === null) {
              continue;
            }
            if (Array.isArray(value)) {
              if (value.length === 0) {
                continue;
              }
              search.push(
                serializeArrayParam(name, value, {
                  style: "form",
                  explode: true,
                  ...options?.array,
                  allowReserved: options?.allowReserved || false
                })
              );
              continue;
            }
            if (typeof value === "object") {
              search.push(
                serializeObjectParam(name, value, {
                  style: "deepObject",
                  explode: true,
                  ...options?.object,
                  allowReserved: options?.allowReserved || false
                })
              );
              continue;
            }
            search.push(serializePrimitiveParam(name, value, options));
          }
        }
        return search.join("&");
      }, "querySerializer");
    }
    __name(createQuerySerializer, "createQuerySerializer");
    function defaultPathSerializer(pathname, pathParams) {
      let nextURL = pathname;
      for (const match of pathname.match(PATH_PARAM_RE) ?? []) {
        let name = match.substring(1, match.length - 1);
        let explode = false;
        let style = "simple";
        if (name.endsWith("*")) {
          explode = true;
          name = name.substring(0, name.length - 1);
        }
        if (name.startsWith(".")) {
          style = "label";
          name = name.substring(1);
        } else if (name.startsWith(";")) {
          style = "matrix";
          name = name.substring(1);
        }
        if (!pathParams || pathParams[name] === void 0 || pathParams[name] === null) {
          continue;
        }
        const value = pathParams[name];
        if (Array.isArray(value)) {
          nextURL = nextURL.replace(match, serializeArrayParam(name, value, { style, explode }));
          continue;
        }
        if (typeof value === "object") {
          nextURL = nextURL.replace(match, serializeObjectParam(name, value, { style, explode }));
          continue;
        }
        if (style === "matrix") {
          nextURL = nextURL.replace(match, `;${serializePrimitiveParam(name, value)}`);
          continue;
        }
        nextURL = nextURL.replace(match, style === "label" ? `.${encodeURIComponent(value)}` : encodeURIComponent(value));
      }
      return nextURL;
    }
    __name(defaultPathSerializer, "defaultPathSerializer");
    function defaultBodySerializer(body, headers) {
      if (body instanceof FormData) {
        return body;
      }
      if (headers) {
        const contentType = headers.get instanceof Function ? headers.get("Content-Type") ?? headers.get("content-type") : headers["Content-Type"] ?? headers["content-type"];
        if (contentType === "application/x-www-form-urlencoded") {
          return new URLSearchParams(body).toString();
        }
      }
      return JSON.stringify(body);
    }
    __name(defaultBodySerializer, "defaultBodySerializer");
    function createFinalURL(pathname, options) {
      let finalURL = `${options.baseUrl}${pathname}`;
      if (options.params?.path) {
        finalURL = defaultPathSerializer(finalURL, options.params.path);
      }
      let search = options.querySerializer(options.params.query ?? {});
      if (search.startsWith("?")) {
        search = search.substring(1);
      }
      if (search) {
        finalURL += `?${search}`;
      }
      return finalURL;
    }
    __name(createFinalURL, "createFinalURL");
    function mergeHeaders(...allHeaders) {
      const finalHeaders = new Headers();
      for (const h of allHeaders) {
        if (!h || typeof h !== "object") {
          continue;
        }
        const iterator = h instanceof Headers ? h.entries() : Object.entries(h);
        for (const [k, v] of iterator) {
          if (v === null) {
            finalHeaders.delete(k);
          } else if (Array.isArray(v)) {
            for (const v2 of v) {
              finalHeaders.append(k, v2);
            }
          } else if (v !== void 0) {
            finalHeaders.set(k, v);
          }
        }
      }
      return finalHeaders;
    }
    __name(mergeHeaders, "mergeHeaders");
    function removeTrailingSlash(url) {
      if (url.endsWith("/")) {
        return url.substring(0, url.length - 1);
      }
      return url;
    }
    __name(removeTrailingSlash, "removeTrailingSlash");
    exports.createFinalURL = createFinalURL;
    exports.createPathBasedClient = createPathBasedClient;
    exports.createQuerySerializer = createQuerySerializer;
    exports.default = createClient;
    exports.defaultBodySerializer = defaultBodySerializer;
    exports.defaultPathSerializer = defaultPathSerializer;
    exports.mergeHeaders = mergeHeaders;
    exports.randomID = randomID;
    exports.removeTrailingSlash = removeTrailingSlash;
    exports.serializeArrayParam = serializeArrayParam;
    exports.serializeObjectParam = serializeObjectParam;
    exports.serializePrimitiveParam = serializePrimitiveParam;
    exports.wrapAsPathBasedClient = wrapAsPathBasedClient;
  }
});

// ../../node_modules/.pnpm/platform@1.3.6/node_modules/platform/platform.js
var require_platform = __commonJS({
  "../../node_modules/.pnpm/platform@1.3.6/node_modules/platform/platform.js"(exports, module) {
    init_esm();
    (function() {
      "use strict";
      var objectTypes = {
        "function": true,
        "object": true
      };
      var root = objectTypes[typeof window] && window || this;
      var oldRoot = root;
      var freeExports = objectTypes[typeof exports] && exports;
      var freeModule = objectTypes[typeof module] && module && !module.nodeType && module;
      var freeGlobal = freeExports && freeModule && typeof global == "object" && global;
      if (freeGlobal && (freeGlobal.global === freeGlobal || freeGlobal.window === freeGlobal || freeGlobal.self === freeGlobal)) {
        root = freeGlobal;
      }
      var maxSafeInteger = Math.pow(2, 53) - 1;
      var reOpera = /\bOpera/;
      var thisBinding = this;
      var objectProto = Object.prototype;
      var hasOwnProperty = objectProto.hasOwnProperty;
      var toString = objectProto.toString;
      function capitalize(string) {
        string = String(string);
        return string.charAt(0).toUpperCase() + string.slice(1);
      }
      __name(capitalize, "capitalize");
      function cleanupOS(os2, pattern, label) {
        var data = {
          "10.0": "10",
          "6.4": "10 Technical Preview",
          "6.3": "8.1",
          "6.2": "8",
          "6.1": "Server 2008 R2 / 7",
          "6.0": "Server 2008 / Vista",
          "5.2": "Server 2003 / XP 64-bit",
          "5.1": "XP",
          "5.01": "2000 SP1",
          "5.0": "2000",
          "4.0": "NT",
          "4.90": "ME"
        };
        if (pattern && label && /^Win/i.test(os2) && !/^Windows Phone /i.test(os2) && (data = data[/[\d.]+$/.exec(os2)])) {
          os2 = "Windows " + data;
        }
        os2 = String(os2);
        if (pattern && label) {
          os2 = os2.replace(RegExp(pattern, "i"), label);
        }
        os2 = format(
          os2.replace(/ ce$/i, " CE").replace(/\bhpw/i, "web").replace(/\bMacintosh\b/, "Mac OS").replace(/_PowerPC\b/i, " OS").replace(/\b(OS X) [^ \d]+/i, "$1").replace(/\bMac (OS X)\b/, "$1").replace(/\/(\d)/, " $1").replace(/_/g, ".").replace(/(?: BePC|[ .]*fc[ \d.]+)$/i, "").replace(/\bx86\.64\b/gi, "x86_64").replace(/\b(Windows Phone) OS\b/, "$1").replace(/\b(Chrome OS \w+) [\d.]+\b/, "$1").split(" on ")[0]
        );
        return os2;
      }
      __name(cleanupOS, "cleanupOS");
      function each(object, callback) {
        var index = -1, length = object ? object.length : 0;
        if (typeof length == "number" && length > -1 && length <= maxSafeInteger) {
          while (++index < length) {
            callback(object[index], index, object);
          }
        } else {
          forOwn(object, callback);
        }
      }
      __name(each, "each");
      function format(string) {
        string = trim(string);
        return /^(?:webOS|i(?:OS|P))/.test(string) ? string : capitalize(string);
      }
      __name(format, "format");
      function forOwn(object, callback) {
        for (var key in object) {
          if (hasOwnProperty.call(object, key)) {
            callback(object[key], key, object);
          }
        }
      }
      __name(forOwn, "forOwn");
      function getClassOf(value) {
        return value == null ? capitalize(value) : toString.call(value).slice(8, -1);
      }
      __name(getClassOf, "getClassOf");
      function isHostType(object, property) {
        var type = object != null ? typeof object[property] : "number";
        return !/^(?:boolean|number|string|undefined)$/.test(type) && (type == "object" ? !!object[property] : true);
      }
      __name(isHostType, "isHostType");
      function qualify(string) {
        return String(string).replace(/([ -])(?!$)/g, "$1?");
      }
      __name(qualify, "qualify");
      function reduce(array, callback) {
        var accumulator = null;
        each(array, function(value, index) {
          accumulator = callback(accumulator, value, index, array);
        });
        return accumulator;
      }
      __name(reduce, "reduce");
      function trim(string) {
        return String(string).replace(/^ +| +$/g, "");
      }
      __name(trim, "trim");
      function parse(ua) {
        var context = root;
        var isCustomContext = ua && typeof ua == "object" && getClassOf(ua) != "String";
        if (isCustomContext) {
          context = ua;
          ua = null;
        }
        var nav = context.navigator || {};
        var userAgent = nav.userAgent || "";
        ua || (ua = userAgent);
        var isModuleScope = isCustomContext || thisBinding == oldRoot;
        var likeChrome = isCustomContext ? !!nav.likeChrome : /\bChrome\b/.test(ua) && !/internal|\n/i.test(toString.toString());
        var objectClass = "Object", airRuntimeClass = isCustomContext ? objectClass : "ScriptBridgingProxyObject", enviroClass = isCustomContext ? objectClass : "Environment", javaClass = isCustomContext && context.java ? "JavaPackage" : getClassOf(context.java), phantomClass = isCustomContext ? objectClass : "RuntimeObject";
        var java = /\bJava/.test(javaClass) && context.java;
        var rhino = java && getClassOf(context.environment) == enviroClass;
        var alpha = java ? "a" : "α";
        var beta = java ? "b" : "β";
        var doc = context.document || {};
        var opera = context.operamini || context.opera;
        var operaClass = reOpera.test(operaClass = isCustomContext && opera ? opera["[[Class]]"] : getClassOf(opera)) ? operaClass : opera = null;
        var data;
        var arch = ua;
        var description = [];
        var prerelease = null;
        var useFeatures = ua == userAgent;
        var version = useFeatures && opera && typeof opera.version == "function" && opera.version();
        var isSpecialCasedOS;
        var layout = getLayout([
          { "label": "EdgeHTML", "pattern": "Edge" },
          "Trident",
          { "label": "WebKit", "pattern": "AppleWebKit" },
          "iCab",
          "Presto",
          "NetFront",
          "Tasman",
          "KHTML",
          "Gecko"
        ]);
        var name = getName([
          "Adobe AIR",
          "Arora",
          "Avant Browser",
          "Breach",
          "Camino",
          "Electron",
          "Epiphany",
          "Fennec",
          "Flock",
          "Galeon",
          "GreenBrowser",
          "iCab",
          "Iceweasel",
          "K-Meleon",
          "Konqueror",
          "Lunascape",
          "Maxthon",
          { "label": "Microsoft Edge", "pattern": "(?:Edge|Edg|EdgA|EdgiOS)" },
          "Midori",
          "Nook Browser",
          "PaleMoon",
          "PhantomJS",
          "Raven",
          "Rekonq",
          "RockMelt",
          { "label": "Samsung Internet", "pattern": "SamsungBrowser" },
          "SeaMonkey",
          { "label": "Silk", "pattern": "(?:Cloud9|Silk-Accelerated)" },
          "Sleipnir",
          "SlimBrowser",
          { "label": "SRWare Iron", "pattern": "Iron" },
          "Sunrise",
          "Swiftfox",
          "Vivaldi",
          "Waterfox",
          "WebPositive",
          { "label": "Yandex Browser", "pattern": "YaBrowser" },
          { "label": "UC Browser", "pattern": "UCBrowser" },
          "Opera Mini",
          { "label": "Opera Mini", "pattern": "OPiOS" },
          "Opera",
          { "label": "Opera", "pattern": "OPR" },
          "Chromium",
          "Chrome",
          { "label": "Chrome", "pattern": "(?:HeadlessChrome)" },
          { "label": "Chrome Mobile", "pattern": "(?:CriOS|CrMo)" },
          { "label": "Firefox", "pattern": "(?:Firefox|Minefield)" },
          { "label": "Firefox for iOS", "pattern": "FxiOS" },
          { "label": "IE", "pattern": "IEMobile" },
          { "label": "IE", "pattern": "MSIE" },
          "Safari"
        ]);
        var product = getProduct([
          { "label": "BlackBerry", "pattern": "BB10" },
          "BlackBerry",
          { "label": "Galaxy S", "pattern": "GT-I9000" },
          { "label": "Galaxy S2", "pattern": "GT-I9100" },
          { "label": "Galaxy S3", "pattern": "GT-I9300" },
          { "label": "Galaxy S4", "pattern": "GT-I9500" },
          { "label": "Galaxy S5", "pattern": "SM-G900" },
          { "label": "Galaxy S6", "pattern": "SM-G920" },
          { "label": "Galaxy S6 Edge", "pattern": "SM-G925" },
          { "label": "Galaxy S7", "pattern": "SM-G930" },
          { "label": "Galaxy S7 Edge", "pattern": "SM-G935" },
          "Google TV",
          "Lumia",
          "iPad",
          "iPod",
          "iPhone",
          "Kindle",
          { "label": "Kindle Fire", "pattern": "(?:Cloud9|Silk-Accelerated)" },
          "Nexus",
          "Nook",
          "PlayBook",
          "PlayStation Vita",
          "PlayStation",
          "TouchPad",
          "Transformer",
          { "label": "Wii U", "pattern": "WiiU" },
          "Wii",
          "Xbox One",
          { "label": "Xbox 360", "pattern": "Xbox" },
          "Xoom"
        ]);
        var manufacturer = getManufacturer({
          "Apple": { "iPad": 1, "iPhone": 1, "iPod": 1 },
          "Alcatel": {},
          "Archos": {},
          "Amazon": { "Kindle": 1, "Kindle Fire": 1 },
          "Asus": { "Transformer": 1 },
          "Barnes & Noble": { "Nook": 1 },
          "BlackBerry": { "PlayBook": 1 },
          "Google": { "Google TV": 1, "Nexus": 1 },
          "HP": { "TouchPad": 1 },
          "HTC": {},
          "Huawei": {},
          "Lenovo": {},
          "LG": {},
          "Microsoft": { "Xbox": 1, "Xbox One": 1 },
          "Motorola": { "Xoom": 1 },
          "Nintendo": { "Wii U": 1, "Wii": 1 },
          "Nokia": { "Lumia": 1 },
          "Oppo": {},
          "Samsung": { "Galaxy S": 1, "Galaxy S2": 1, "Galaxy S3": 1, "Galaxy S4": 1 },
          "Sony": { "PlayStation": 1, "PlayStation Vita": 1 },
          "Xiaomi": { "Mi": 1, "Redmi": 1 }
        });
        var os2 = getOS([
          "Windows Phone",
          "KaiOS",
          "Android",
          "CentOS",
          { "label": "Chrome OS", "pattern": "CrOS" },
          "Debian",
          { "label": "DragonFly BSD", "pattern": "DragonFly" },
          "Fedora",
          "FreeBSD",
          "Gentoo",
          "Haiku",
          "Kubuntu",
          "Linux Mint",
          "OpenBSD",
          "Red Hat",
          "SuSE",
          "Ubuntu",
          "Xubuntu",
          "Cygwin",
          "Symbian OS",
          "hpwOS",
          "webOS ",
          "webOS",
          "Tablet OS",
          "Tizen",
          "Linux",
          "Mac OS X",
          "Macintosh",
          "Mac",
          "Windows 98;",
          "Windows "
        ]);
        function getLayout(guesses) {
          return reduce(guesses, function(result, guess) {
            return result || RegExp("\\b" + (guess.pattern || qualify(guess)) + "\\b", "i").exec(ua) && (guess.label || guess);
          });
        }
        __name(getLayout, "getLayout");
        function getManufacturer(guesses) {
          return reduce(guesses, function(result, value, key) {
            return result || (value[product] || value[/^[a-z]+(?: +[a-z]+\b)*/i.exec(product)] || RegExp("\\b" + qualify(key) + "(?:\\b|\\w*\\d)", "i").exec(ua)) && key;
          });
        }
        __name(getManufacturer, "getManufacturer");
        function getName(guesses) {
          return reduce(guesses, function(result, guess) {
            return result || RegExp("\\b" + (guess.pattern || qualify(guess)) + "\\b", "i").exec(ua) && (guess.label || guess);
          });
        }
        __name(getName, "getName");
        function getOS(guesses) {
          return reduce(guesses, function(result, guess) {
            var pattern = guess.pattern || qualify(guess);
            if (!result && (result = RegExp("\\b" + pattern + "(?:/[\\d.]+|[ \\w.]*)", "i").exec(ua))) {
              result = cleanupOS(result, pattern, guess.label || guess);
            }
            return result;
          });
        }
        __name(getOS, "getOS");
        function getProduct(guesses) {
          return reduce(guesses, function(result, guess) {
            var pattern = guess.pattern || qualify(guess);
            if (!result && (result = RegExp("\\b" + pattern + " *\\d+[.\\w_]*", "i").exec(ua) || RegExp("\\b" + pattern + " *\\w+-[\\w]*", "i").exec(ua) || RegExp("\\b" + pattern + "(?:; *(?:[a-z]+[_-])?[a-z]+\\d+|[^ ();-]*)", "i").exec(ua))) {
              if ((result = String(guess.label && !RegExp(pattern, "i").test(guess.label) ? guess.label : result).split("/"))[1] && !/[\d.]+/.test(result[0])) {
                result[0] += " " + result[1];
              }
              guess = guess.label || guess;
              result = format(result[0].replace(RegExp(pattern, "i"), guess).replace(RegExp("; *(?:" + guess + "[_-])?", "i"), " ").replace(RegExp("(" + guess + ")[-_.]?(\\w)", "i"), "$1 $2"));
            }
            return result;
          });
        }
        __name(getProduct, "getProduct");
        function getVersion(patterns) {
          return reduce(patterns, function(result, pattern) {
            return result || (RegExp(pattern + "(?:-[\\d.]+/|(?: for [\\w-]+)?[ /-])([\\d.]+[^ ();/_-]*)", "i").exec(ua) || 0)[1] || null;
          });
        }
        __name(getVersion, "getVersion");
        function toStringPlatform() {
          return this.description || "";
        }
        __name(toStringPlatform, "toStringPlatform");
        layout && (layout = [layout]);
        if (/\bAndroid\b/.test(os2) && !product && (data = /\bAndroid[^;]*;(.*?)(?:Build|\) AppleWebKit)\b/i.exec(ua))) {
          product = trim(data[1]).replace(/^[a-z]{2}-[a-z]{2};\s*/i, "") || null;
        }
        if (manufacturer && !product) {
          product = getProduct([manufacturer]);
        } else if (manufacturer && product) {
          product = product.replace(RegExp("^(" + qualify(manufacturer) + ")[-_.\\s]", "i"), manufacturer + " ").replace(RegExp("^(" + qualify(manufacturer) + ")[-_.]?(\\w)", "i"), manufacturer + " $2");
        }
        if (data = /\bGoogle TV\b/.exec(product)) {
          product = data[0];
        }
        if (/\bSimulator\b/i.test(ua)) {
          product = (product ? product + " " : "") + "Simulator";
        }
        if (name == "Opera Mini" && /\bOPiOS\b/.test(ua)) {
          description.push("running in Turbo/Uncompressed mode");
        }
        if (name == "IE" && /\blike iPhone OS\b/.test(ua)) {
          data = parse(ua.replace(/like iPhone OS/, ""));
          manufacturer = data.manufacturer;
          product = data.product;
        } else if (/^iP/.test(product)) {
          name || (name = "Safari");
          os2 = "iOS" + ((data = / OS ([\d_]+)/i.exec(ua)) ? " " + data[1].replace(/_/g, ".") : "");
        } else if (name == "Konqueror" && /^Linux\b/i.test(os2)) {
          os2 = "Kubuntu";
        } else if (manufacturer && manufacturer != "Google" && (/Chrome/.test(name) && !/\bMobile Safari\b/i.test(ua) || /\bVita\b/.test(product)) || /\bAndroid\b/.test(os2) && /^Chrome/.test(name) && /\bVersion\//i.test(ua)) {
          name = "Android Browser";
          os2 = /\bAndroid\b/.test(os2) ? os2 : "Android";
        } else if (name == "Silk") {
          if (!/\bMobi/i.test(ua)) {
            os2 = "Android";
            description.unshift("desktop mode");
          }
          if (/Accelerated *= *true/i.test(ua)) {
            description.unshift("accelerated");
          }
        } else if (name == "UC Browser" && /\bUCWEB\b/.test(ua)) {
          description.push("speed mode");
        } else if (name == "PaleMoon" && (data = /\bFirefox\/([\d.]+)\b/.exec(ua))) {
          description.push("identifying as Firefox " + data[1]);
        } else if (name == "Firefox" && (data = /\b(Mobile|Tablet|TV)\b/i.exec(ua))) {
          os2 || (os2 = "Firefox OS");
          product || (product = data[1]);
        } else if (!name || (data = !/\bMinefield\b/i.test(ua) && /\b(?:Firefox|Safari)\b/.exec(name))) {
          if (name && !product && /[\/,]|^[^(]+?\)/.test(ua.slice(ua.indexOf(data + "/") + 8))) {
            name = null;
          }
          if ((data = product || manufacturer || os2) && (product || manufacturer || /\b(?:Android|Symbian OS|Tablet OS|webOS)\b/.test(os2))) {
            name = /[a-z]+(?: Hat)?/i.exec(/\bAndroid\b/.test(os2) ? os2 : data) + " Browser";
          }
        } else if (name == "Electron" && (data = (/\bChrome\/([\d.]+)\b/.exec(ua) || 0)[1])) {
          description.push("Chromium " + data);
        }
        if (!version) {
          version = getVersion([
            "(?:Cloud9|CriOS|CrMo|Edge|Edg|EdgA|EdgiOS|FxiOS|HeadlessChrome|IEMobile|Iron|Opera ?Mini|OPiOS|OPR|Raven|SamsungBrowser|Silk(?!/[\\d.]+$)|UCBrowser|YaBrowser)",
            "Version",
            qualify(name),
            "(?:Firefox|Minefield|NetFront)"
          ]);
        }
        if (data = layout == "iCab" && parseFloat(version) > 3 && "WebKit" || /\bOpera\b/.test(name) && (/\bOPR\b/.test(ua) ? "Blink" : "Presto") || /\b(?:Midori|Nook|Safari)\b/i.test(ua) && !/^(?:Trident|EdgeHTML)$/.test(layout) && "WebKit" || !layout && /\bMSIE\b/i.test(ua) && (os2 == "Mac OS" ? "Tasman" : "Trident") || layout == "WebKit" && /\bPlayStation\b(?! Vita\b)/i.test(name) && "NetFront") {
          layout = [data];
        }
        if (name == "IE" && (data = (/; *(?:XBLWP|ZuneWP)(\d+)/i.exec(ua) || 0)[1])) {
          name += " Mobile";
          os2 = "Windows Phone " + (/\+$/.test(data) ? data : data + ".x");
          description.unshift("desktop mode");
        } else if (/\bWPDesktop\b/i.test(ua)) {
          name = "IE Mobile";
          os2 = "Windows Phone 8.x";
          description.unshift("desktop mode");
          version || (version = (/\brv:([\d.]+)/.exec(ua) || 0)[1]);
        } else if (name != "IE" && layout == "Trident" && (data = /\brv:([\d.]+)/.exec(ua))) {
          if (name) {
            description.push("identifying as " + name + (version ? " " + version : ""));
          }
          name = "IE";
          version = data[1];
        }
        if (useFeatures) {
          if (isHostType(context, "global")) {
            if (java) {
              data = java.lang.System;
              arch = data.getProperty("os.arch");
              os2 = os2 || data.getProperty("os.name") + " " + data.getProperty("os.version");
            }
            if (rhino) {
              try {
                version = context.require("ringo/engine").version.join(".");
                name = "RingoJS";
              } catch (e) {
                if ((data = context.system) && data.global.system == context.system) {
                  name = "Narwhal";
                  os2 || (os2 = data[0].os || null);
                }
              }
              if (!name) {
                name = "Rhino";
              }
            } else if (typeof context.process == "object" && !context.process.browser && (data = context.process)) {
              if (typeof data.versions == "object") {
                if (typeof data.versions.electron == "string") {
                  description.push("Node " + data.versions.node);
                  name = "Electron";
                  version = data.versions.electron;
                } else if (typeof data.versions.nw == "string") {
                  description.push("Chromium " + version, "Node " + data.versions.node);
                  name = "NW.js";
                  version = data.versions.nw;
                }
              }
              if (!name) {
                name = "Node.js";
                arch = data.arch;
                os2 = data.platform;
                version = /[\d.]+/.exec(data.version);
                version = version ? version[0] : null;
              }
            }
          } else if (getClassOf(data = context.runtime) == airRuntimeClass) {
            name = "Adobe AIR";
            os2 = data.flash.system.Capabilities.os;
          } else if (getClassOf(data = context.phantom) == phantomClass) {
            name = "PhantomJS";
            version = (data = data.version || null) && data.major + "." + data.minor + "." + data.patch;
          } else if (typeof doc.documentMode == "number" && (data = /\bTrident\/(\d+)/i.exec(ua))) {
            version = [version, doc.documentMode];
            if ((data = +data[1] + 4) != version[1]) {
              description.push("IE " + version[1] + " mode");
              layout && (layout[1] = "");
              version[1] = data;
            }
            version = name == "IE" ? String(version[1].toFixed(1)) : version[0];
          } else if (typeof doc.documentMode == "number" && /^(?:Chrome|Firefox)\b/.test(name)) {
            description.push("masking as " + name + " " + version);
            name = "IE";
            version = "11.0";
            layout = ["Trident"];
            os2 = "Windows";
          }
          os2 = os2 && format(os2);
        }
        if (version && (data = /(?:[ab]|dp|pre|[ab]\d+pre)(?:\d+\+?)?$/i.exec(version) || /(?:alpha|beta)(?: ?\d)?/i.exec(ua + ";" + (useFeatures && nav.appMinorVersion)) || /\bMinefield\b/i.test(ua) && "a")) {
          prerelease = /b/i.test(data) ? "beta" : "alpha";
          version = version.replace(RegExp(data + "\\+?$"), "") + (prerelease == "beta" ? beta : alpha) + (/\d+\+?/.exec(data) || "");
        }
        if (name == "Fennec" || name == "Firefox" && /\b(?:Android|Firefox OS|KaiOS)\b/.test(os2)) {
          name = "Firefox Mobile";
        } else if (name == "Maxthon" && version) {
          version = version.replace(/\.[\d.]+/, ".x");
        } else if (/\bXbox\b/i.test(product)) {
          if (product == "Xbox 360") {
            os2 = null;
          }
          if (product == "Xbox 360" && /\bIEMobile\b/.test(ua)) {
            description.unshift("mobile mode");
          }
        } else if ((/^(?:Chrome|IE|Opera)$/.test(name) || name && !product && !/Browser|Mobi/.test(name)) && (os2 == "Windows CE" || /Mobi/i.test(ua))) {
          name += " Mobile";
        } else if (name == "IE" && useFeatures) {
          try {
            if (context.external === null) {
              description.unshift("platform preview");
            }
          } catch (e) {
            description.unshift("embedded");
          }
        } else if ((/\bBlackBerry\b/.test(product) || /\bBB10\b/.test(ua)) && (data = (RegExp(product.replace(/ +/g, " *") + "/([.\\d]+)", "i").exec(ua) || 0)[1] || version)) {
          data = [data, /BB10/.test(ua)];
          os2 = (data[1] ? (product = null, manufacturer = "BlackBerry") : "Device Software") + " " + data[0];
          version = null;
        } else if (this != forOwn && product != "Wii" && (useFeatures && opera || /Opera/.test(name) && /\b(?:MSIE|Firefox)\b/i.test(ua) || name == "Firefox" && /\bOS X (?:\d+\.){2,}/.test(os2) || name == "IE" && (os2 && !/^Win/.test(os2) && version > 5.5 || /\bWindows XP\b/.test(os2) && version > 8 || version == 8 && !/\bTrident\b/.test(ua))) && !reOpera.test(data = parse.call(forOwn, ua.replace(reOpera, "") + ";")) && data.name) {
          data = "ing as " + data.name + ((data = data.version) ? " " + data : "");
          if (reOpera.test(name)) {
            if (/\bIE\b/.test(data) && os2 == "Mac OS") {
              os2 = null;
            }
            data = "identify" + data;
          } else {
            data = "mask" + data;
            if (operaClass) {
              name = format(operaClass.replace(/([a-z])([A-Z])/g, "$1 $2"));
            } else {
              name = "Opera";
            }
            if (/\bIE\b/.test(data)) {
              os2 = null;
            }
            if (!useFeatures) {
              version = null;
            }
          }
          layout = ["Presto"];
          description.push(data);
        }
        if (data = (/\bAppleWebKit\/([\d.]+\+?)/i.exec(ua) || 0)[1]) {
          data = [parseFloat(data.replace(/\.(\d)$/, ".0$1")), data];
          if (name == "Safari" && data[1].slice(-1) == "+") {
            name = "WebKit Nightly";
            prerelease = "alpha";
            version = data[1].slice(0, -1);
          } else if (version == data[1] || version == (data[2] = (/\bSafari\/([\d.]+\+?)/i.exec(ua) || 0)[1])) {
            version = null;
          }
          data[1] = (/\b(?:Headless)?Chrome\/([\d.]+)/i.exec(ua) || 0)[1];
          if (data[0] == 537.36 && data[2] == 537.36 && parseFloat(data[1]) >= 28 && layout == "WebKit") {
            layout = ["Blink"];
          }
          if (!useFeatures || !likeChrome && !data[1]) {
            layout && (layout[1] = "like Safari");
            data = (data = data[0], data < 400 ? 1 : data < 500 ? 2 : data < 526 ? 3 : data < 533 ? 4 : data < 534 ? "4+" : data < 535 ? 5 : data < 537 ? 6 : data < 538 ? 7 : data < 601 ? 8 : data < 602 ? 9 : data < 604 ? 10 : data < 606 ? 11 : data < 608 ? 12 : "12");
          } else {
            layout && (layout[1] = "like Chrome");
            data = data[1] || (data = data[0], data < 530 ? 1 : data < 532 ? 2 : data < 532.05 ? 3 : data < 533 ? 4 : data < 534.03 ? 5 : data < 534.07 ? 6 : data < 534.1 ? 7 : data < 534.13 ? 8 : data < 534.16 ? 9 : data < 534.24 ? 10 : data < 534.3 ? 11 : data < 535.01 ? 12 : data < 535.02 ? "13+" : data < 535.07 ? 15 : data < 535.11 ? 16 : data < 535.19 ? 17 : data < 536.05 ? 18 : data < 536.1 ? 19 : data < 537.01 ? 20 : data < 537.11 ? "21+" : data < 537.13 ? 23 : data < 537.18 ? 24 : data < 537.24 ? 25 : data < 537.36 ? 26 : layout != "Blink" ? "27" : "28");
          }
          layout && (layout[1] += " " + (data += typeof data == "number" ? ".x" : /[.+]/.test(data) ? "" : "+"));
          if (name == "Safari" && (!version || parseInt(version) > 45)) {
            version = data;
          } else if (name == "Chrome" && /\bHeadlessChrome/i.test(ua)) {
            description.unshift("headless");
          }
        }
        if (name == "Opera" && (data = /\bzbov|zvav$/.exec(os2))) {
          name += " ";
          description.unshift("desktop mode");
          if (data == "zvav") {
            name += "Mini";
            version = null;
          } else {
            name += "Mobile";
          }
          os2 = os2.replace(RegExp(" *" + data + "$"), "");
        } else if (name == "Safari" && /\bChrome\b/.exec(layout && layout[1])) {
          description.unshift("desktop mode");
          name = "Chrome Mobile";
          version = null;
          if (/\bOS X\b/.test(os2)) {
            manufacturer = "Apple";
            os2 = "iOS 4.3+";
          } else {
            os2 = null;
          }
        } else if (/\bSRWare Iron\b/.test(name) && !version) {
          version = getVersion("Chrome");
        }
        if (version && version.indexOf(data = /[\d.]+$/.exec(os2)) == 0 && ua.indexOf("/" + data + "-") > -1) {
          os2 = trim(os2.replace(data, ""));
        }
        if (os2 && os2.indexOf(name) != -1 && !RegExp(name + " OS").test(os2)) {
          os2 = os2.replace(RegExp(" *" + qualify(name) + " *"), "");
        }
        if (layout && !/\b(?:Avant|Nook)\b/.test(name) && (/Browser|Lunascape|Maxthon/.test(name) || name != "Safari" && /^iOS/.test(os2) && /\bSafari\b/.test(layout[1]) || /^(?:Adobe|Arora|Breach|Midori|Opera|Phantom|Rekonq|Rock|Samsung Internet|Sleipnir|SRWare Iron|Vivaldi|Web)/.test(name) && layout[1])) {
          (data = layout[layout.length - 1]) && description.push(data);
        }
        if (description.length) {
          description = ["(" + description.join("; ") + ")"];
        }
        if (manufacturer && product && product.indexOf(manufacturer) < 0) {
          description.push("on " + manufacturer);
        }
        if (product) {
          description.push((/^on /.test(description[description.length - 1]) ? "" : "on ") + product);
        }
        if (os2) {
          data = / ([\d.+]+)$/.exec(os2);
          isSpecialCasedOS = data && os2.charAt(os2.length - data[0].length - 1) == "/";
          os2 = {
            "architecture": 32,
            "family": data && !isSpecialCasedOS ? os2.replace(data[0], "") : os2,
            "version": data ? data[1] : null,
            "toString": /* @__PURE__ */ __name(function() {
              var version2 = this.version;
              return this.family + (version2 && !isSpecialCasedOS ? " " + version2 : "") + (this.architecture == 64 ? " 64-bit" : "");
            }, "toString")
          };
        }
        if ((data = /\b(?:AMD|IA|Win|WOW|x86_|x)64\b/i.exec(arch)) && !/\bi686\b/i.test(arch)) {
          if (os2) {
            os2.architecture = 64;
            os2.family = os2.family.replace(RegExp(" *" + data), "");
          }
          if (name && (/\bWOW64\b/i.test(ua) || useFeatures && /\w(?:86|32)$/.test(nav.cpuClass || nav.platform) && !/\bWin64; x64\b/i.test(ua))) {
            description.unshift("32-bit");
          }
        } else if (os2 && /^OS X/.test(os2.family) && name == "Chrome" && parseFloat(version) >= 39) {
          os2.architecture = 64;
        }
        ua || (ua = null);
        var platform2 = {};
        platform2.description = ua;
        platform2.layout = layout && layout[0];
        platform2.manufacturer = manufacturer;
        platform2.name = name;
        platform2.prerelease = prerelease;
        platform2.product = product;
        platform2.ua = ua;
        platform2.version = name && version;
        platform2.os = os2 || {
          /**
           * The CPU architecture the OS is built for.
           *
           * @memberOf platform.os
           * @type number|null
           */
          "architecture": null,
          /**
           * The family of the OS.
           *
           * Common values include:
           * "Windows", "Windows Server 2008 R2 / 7", "Windows Server 2008 / Vista",
           * "Windows XP", "OS X", "Linux", "Ubuntu", "Debian", "Fedora", "Red Hat",
           * "SuSE", "Android", "iOS" and "Windows Phone"
           *
           * @memberOf platform.os
           * @type string|null
           */
          "family": null,
          /**
           * The version of the OS.
           *
           * @memberOf platform.os
           * @type string|null
           */
          "version": null,
          /**
           * Returns the OS string.
           *
           * @memberOf platform.os
           * @returns {string} The OS string.
           */
          "toString": /* @__PURE__ */ __name(function() {
            return "null";
          }, "toString")
        };
        platform2.parse = parse;
        platform2.toString = toStringPlatform;
        if (platform2.version) {
          description.unshift(version);
        }
        if (platform2.name) {
          description.unshift(name);
        }
        if (os2 && name && !(os2 == String(os2).split(" ")[0] && (os2 == name.split(" ")[0] || product))) {
          description.push(product ? "(" + os2 + ")" : "on " + os2);
        }
        if (description.length) {
          platform2.description = description.join(" ");
        }
        return platform2;
      }
      __name(parse, "parse");
      var platform = parse();
      if (typeof define == "function" && typeof define.amd == "object" && define.amd) {
        root.platform = platform;
        define(function() {
          return platform;
        });
      } else if (freeExports && freeModule) {
        forOwn(platform, function(value, key) {
          freeExports[key] = value;
        });
      } else {
        root.platform = platform;
      }
    }).call(exports);
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/code.js
var require_code = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/code.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Code = void 0;
    var Code;
    (function(Code2) {
      Code2[Code2["Canceled"] = 1] = "Canceled";
      Code2[Code2["Unknown"] = 2] = "Unknown";
      Code2[Code2["InvalidArgument"] = 3] = "InvalidArgument";
      Code2[Code2["DeadlineExceeded"] = 4] = "DeadlineExceeded";
      Code2[Code2["NotFound"] = 5] = "NotFound";
      Code2[Code2["AlreadyExists"] = 6] = "AlreadyExists";
      Code2[Code2["PermissionDenied"] = 7] = "PermissionDenied";
      Code2[Code2["ResourceExhausted"] = 8] = "ResourceExhausted";
      Code2[Code2["FailedPrecondition"] = 9] = "FailedPrecondition";
      Code2[Code2["Aborted"] = 10] = "Aborted";
      Code2[Code2["OutOfRange"] = 11] = "OutOfRange";
      Code2[Code2["Unimplemented"] = 12] = "Unimplemented";
      Code2[Code2["Internal"] = 13] = "Internal";
      Code2[Code2["Unavailable"] = 14] = "Unavailable";
      Code2[Code2["DataLoss"] = 15] = "DataLoss";
      Code2[Code2["Unauthenticated"] = 16] = "Unauthenticated";
    })(Code || (exports.Code = Code = {}));
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/types.js
var require_types = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/types.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/is-message.js
var require_is_message = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/is-message.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.isMessage = isMessage;
    function isMessage(arg, schema) {
      const isMessage2 = arg !== null && typeof arg == "object" && "$typeName" in arg && typeof arg.$typeName == "string";
      if (!isMessage2) {
        return false;
      }
      if (schema === void 0) {
        return true;
      }
      return schema.typeName === arg.$typeName;
    }
    __name(isMessage, "isMessage");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/descriptors.js
var require_descriptors = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/descriptors.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ScalarType = void 0;
    var ScalarType;
    (function(ScalarType2) {
      ScalarType2[ScalarType2["DOUBLE"] = 1] = "DOUBLE";
      ScalarType2[ScalarType2["FLOAT"] = 2] = "FLOAT";
      ScalarType2[ScalarType2["INT64"] = 3] = "INT64";
      ScalarType2[ScalarType2["UINT64"] = 4] = "UINT64";
      ScalarType2[ScalarType2["INT32"] = 5] = "INT32";
      ScalarType2[ScalarType2["FIXED64"] = 6] = "FIXED64";
      ScalarType2[ScalarType2["FIXED32"] = 7] = "FIXED32";
      ScalarType2[ScalarType2["BOOL"] = 8] = "BOOL";
      ScalarType2[ScalarType2["STRING"] = 9] = "STRING";
      ScalarType2[ScalarType2["BYTES"] = 12] = "BYTES";
      ScalarType2[ScalarType2["UINT32"] = 13] = "UINT32";
      ScalarType2[ScalarType2["SFIXED32"] = 15] = "SFIXED32";
      ScalarType2[ScalarType2["SFIXED64"] = 16] = "SFIXED64";
      ScalarType2[ScalarType2["SINT32"] = 17] = "SINT32";
      ScalarType2[ScalarType2["SINT64"] = 18] = "SINT64";
    })(ScalarType || (exports.ScalarType = ScalarType = {}));
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wire/varint.js
var require_varint = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wire/varint.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.varint64read = varint64read;
    exports.varint64write = varint64write;
    exports.int64FromString = int64FromString;
    exports.int64ToString = int64ToString;
    exports.uInt64ToString = uInt64ToString;
    exports.varint32write = varint32write;
    exports.varint32read = varint32read;
    function varint64read() {
      let lowBits = 0;
      let highBits = 0;
      for (let shift = 0; shift < 28; shift += 7) {
        let b = this.buf[this.pos++];
        lowBits |= (b & 127) << shift;
        if ((b & 128) == 0) {
          this.assertBounds();
          return [lowBits, highBits];
        }
      }
      let middleByte = this.buf[this.pos++];
      lowBits |= (middleByte & 15) << 28;
      highBits = (middleByte & 112) >> 4;
      if ((middleByte & 128) == 0) {
        this.assertBounds();
        return [lowBits, highBits];
      }
      for (let shift = 3; shift <= 31; shift += 7) {
        let b = this.buf[this.pos++];
        highBits |= (b & 127) << shift;
        if ((b & 128) == 0) {
          this.assertBounds();
          return [lowBits, highBits];
        }
      }
      throw new Error("invalid varint");
    }
    __name(varint64read, "varint64read");
    function varint64write(lo, hi, bytes) {
      for (let i = 0; i < 28; i = i + 7) {
        const shift = lo >>> i;
        const hasNext = !(shift >>> 7 == 0 && hi == 0);
        const byte = (hasNext ? shift | 128 : shift) & 255;
        bytes.push(byte);
        if (!hasNext) {
          return;
        }
      }
      const splitBits = lo >>> 28 & 15 | (hi & 7) << 4;
      const hasMoreBits = !(hi >> 3 == 0);
      bytes.push((hasMoreBits ? splitBits | 128 : splitBits) & 255);
      if (!hasMoreBits) {
        return;
      }
      for (let i = 3; i < 31; i = i + 7) {
        const shift = hi >>> i;
        const hasNext = !(shift >>> 7 == 0);
        const byte = (hasNext ? shift | 128 : shift) & 255;
        bytes.push(byte);
        if (!hasNext) {
          return;
        }
      }
      bytes.push(hi >>> 31 & 1);
    }
    __name(varint64write, "varint64write");
    var TWO_PWR_32_DBL = 4294967296;
    function int64FromString(dec) {
      const minus = dec[0] === "-";
      if (minus) {
        dec = dec.slice(1);
      }
      const base = 1e6;
      let lowBits = 0;
      let highBits = 0;
      function add1e6digit(begin, end) {
        const digit1e6 = Number(dec.slice(begin, end));
        highBits *= base;
        lowBits = lowBits * base + digit1e6;
        if (lowBits >= TWO_PWR_32_DBL) {
          highBits = highBits + (lowBits / TWO_PWR_32_DBL | 0);
          lowBits = lowBits % TWO_PWR_32_DBL;
        }
      }
      __name(add1e6digit, "add1e6digit");
      add1e6digit(-24, -18);
      add1e6digit(-18, -12);
      add1e6digit(-12, -6);
      add1e6digit(-6);
      return minus ? negate(lowBits, highBits) : newBits(lowBits, highBits);
    }
    __name(int64FromString, "int64FromString");
    function int64ToString(lo, hi) {
      let bits = newBits(lo, hi);
      const negative = bits.hi & 2147483648;
      if (negative) {
        bits = negate(bits.lo, bits.hi);
      }
      const result = uInt64ToString(bits.lo, bits.hi);
      return negative ? "-" + result : result;
    }
    __name(int64ToString, "int64ToString");
    function uInt64ToString(lo, hi) {
      ({ lo, hi } = toUnsigned(lo, hi));
      if (hi <= 2097151) {
        return String(TWO_PWR_32_DBL * hi + lo);
      }
      const low = lo & 16777215;
      const mid = (lo >>> 24 | hi << 8) & 16777215;
      const high = hi >> 16 & 65535;
      let digitA = low + mid * 6777216 + high * 6710656;
      let digitB = mid + high * 8147497;
      let digitC = high * 2;
      const base = 1e7;
      if (digitA >= base) {
        digitB += Math.floor(digitA / base);
        digitA %= base;
      }
      if (digitB >= base) {
        digitC += Math.floor(digitB / base);
        digitB %= base;
      }
      return digitC.toString() + decimalFrom1e7WithLeadingZeros(digitB) + decimalFrom1e7WithLeadingZeros(digitA);
    }
    __name(uInt64ToString, "uInt64ToString");
    function toUnsigned(lo, hi) {
      return { lo: lo >>> 0, hi: hi >>> 0 };
    }
    __name(toUnsigned, "toUnsigned");
    function newBits(lo, hi) {
      return { lo: lo | 0, hi: hi | 0 };
    }
    __name(newBits, "newBits");
    function negate(lowBits, highBits) {
      highBits = ~highBits;
      if (lowBits) {
        lowBits = ~lowBits + 1;
      } else {
        highBits += 1;
      }
      return newBits(lowBits, highBits);
    }
    __name(negate, "negate");
    var decimalFrom1e7WithLeadingZeros = /* @__PURE__ */ __name((digit1e7) => {
      const partial = String(digit1e7);
      return "0000000".slice(partial.length) + partial;
    }, "decimalFrom1e7WithLeadingZeros");
    function varint32write(value, bytes) {
      if (value >= 0) {
        while (value > 127) {
          bytes.push(value & 127 | 128);
          value = value >>> 7;
        }
        bytes.push(value);
      } else {
        for (let i = 0; i < 9; i++) {
          bytes.push(value & 127 | 128);
          value = value >> 7;
        }
        bytes.push(1);
      }
    }
    __name(varint32write, "varint32write");
    function varint32read() {
      let b = this.buf[this.pos++];
      let result = b & 127;
      if ((b & 128) == 0) {
        this.assertBounds();
        return result;
      }
      b = this.buf[this.pos++];
      result |= (b & 127) << 7;
      if ((b & 128) == 0) {
        this.assertBounds();
        return result;
      }
      b = this.buf[this.pos++];
      result |= (b & 127) << 14;
      if ((b & 128) == 0) {
        this.assertBounds();
        return result;
      }
      b = this.buf[this.pos++];
      result |= (b & 127) << 21;
      if ((b & 128) == 0) {
        this.assertBounds();
        return result;
      }
      b = this.buf[this.pos++];
      result |= (b & 15) << 28;
      for (let readBytes = 5; (b & 128) !== 0 && readBytes < 10; readBytes++)
        b = this.buf[this.pos++];
      if ((b & 128) != 0)
        throw new Error("invalid varint");
      this.assertBounds();
      return result >>> 0;
    }
    __name(varint32read, "varint32read");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/proto-int64.js
var require_proto_int64 = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/proto-int64.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.protoInt64 = void 0;
    var varint_js_1 = require_varint();
    exports.protoInt64 = makeInt64Support();
    function makeInt64Support() {
      const dv = new DataView(new ArrayBuffer(8));
      const ok = typeof BigInt === "function" && typeof dv.getBigInt64 === "function" && typeof dv.getBigUint64 === "function" && typeof dv.setBigInt64 === "function" && typeof dv.setBigUint64 === "function" && (!!globalThis.Deno || typeof process != "object" || typeof process.env != "object" || process.env.BUF_BIGINT_DISABLE !== "1");
      if (ok) {
        const MIN = BigInt("-9223372036854775808");
        const MAX = BigInt("9223372036854775807");
        const UMIN = BigInt("0");
        const UMAX = BigInt("18446744073709551615");
        return {
          zero: BigInt(0),
          supported: true,
          parse(value) {
            const bi = typeof value == "bigint" ? value : BigInt(value);
            if (bi > MAX || bi < MIN) {
              throw new Error(`invalid int64: ${value}`);
            }
            return bi;
          },
          uParse(value) {
            const bi = typeof value == "bigint" ? value : BigInt(value);
            if (bi > UMAX || bi < UMIN) {
              throw new Error(`invalid uint64: ${value}`);
            }
            return bi;
          },
          enc(value) {
            dv.setBigInt64(0, this.parse(value), true);
            return {
              lo: dv.getInt32(0, true),
              hi: dv.getInt32(4, true)
            };
          },
          uEnc(value) {
            dv.setBigInt64(0, this.uParse(value), true);
            return {
              lo: dv.getInt32(0, true),
              hi: dv.getInt32(4, true)
            };
          },
          dec(lo, hi) {
            dv.setInt32(0, lo, true);
            dv.setInt32(4, hi, true);
            return dv.getBigInt64(0, true);
          },
          uDec(lo, hi) {
            dv.setInt32(0, lo, true);
            dv.setInt32(4, hi, true);
            return dv.getBigUint64(0, true);
          }
        };
      }
      return {
        zero: "0",
        supported: false,
        parse(value) {
          if (typeof value != "string") {
            value = value.toString();
          }
          assertInt64String(value);
          return value;
        },
        uParse(value) {
          if (typeof value != "string") {
            value = value.toString();
          }
          assertUInt64String(value);
          return value;
        },
        enc(value) {
          if (typeof value != "string") {
            value = value.toString();
          }
          assertInt64String(value);
          return (0, varint_js_1.int64FromString)(value);
        },
        uEnc(value) {
          if (typeof value != "string") {
            value = value.toString();
          }
          assertUInt64String(value);
          return (0, varint_js_1.int64FromString)(value);
        },
        dec(lo, hi) {
          return (0, varint_js_1.int64ToString)(lo, hi);
        },
        uDec(lo, hi) {
          return (0, varint_js_1.uInt64ToString)(lo, hi);
        }
      };
    }
    __name(makeInt64Support, "makeInt64Support");
    function assertInt64String(value) {
      if (!/^-?[0-9]+$/.test(value)) {
        throw new Error("invalid int64: " + value);
      }
    }
    __name(assertInt64String, "assertInt64String");
    function assertUInt64String(value) {
      if (!/^[0-9]+$/.test(value)) {
        throw new Error("invalid uint64: " + value);
      }
    }
    __name(assertUInt64String, "assertUInt64String");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/reflect/scalar.js
var require_scalar = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/reflect/scalar.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.scalarEquals = scalarEquals;
    exports.scalarZeroValue = scalarZeroValue;
    exports.isScalarZeroValue = isScalarZeroValue;
    var proto_int64_js_1 = require_proto_int64();
    var descriptors_js_1 = require_descriptors();
    function scalarEquals(type, a, b) {
      if (a === b) {
        return true;
      }
      if (type == descriptors_js_1.ScalarType.BYTES) {
        if (!(a instanceof Uint8Array) || !(b instanceof Uint8Array)) {
          return false;
        }
        if (a.length !== b.length) {
          return false;
        }
        for (let i = 0; i < a.length; i++) {
          if (a[i] !== b[i]) {
            return false;
          }
        }
        return true;
      }
      switch (type) {
        case descriptors_js_1.ScalarType.UINT64:
        case descriptors_js_1.ScalarType.FIXED64:
        case descriptors_js_1.ScalarType.INT64:
        case descriptors_js_1.ScalarType.SFIXED64:
        case descriptors_js_1.ScalarType.SINT64:
          return a == b;
      }
      return false;
    }
    __name(scalarEquals, "scalarEquals");
    function scalarZeroValue(type, longAsString) {
      switch (type) {
        case descriptors_js_1.ScalarType.STRING:
          return "";
        case descriptors_js_1.ScalarType.BOOL:
          return false;
        case descriptors_js_1.ScalarType.DOUBLE:
        case descriptors_js_1.ScalarType.FLOAT:
          return 0;
        case descriptors_js_1.ScalarType.INT64:
        case descriptors_js_1.ScalarType.UINT64:
        case descriptors_js_1.ScalarType.SFIXED64:
        case descriptors_js_1.ScalarType.FIXED64:
        case descriptors_js_1.ScalarType.SINT64:
          return longAsString ? "0" : proto_int64_js_1.protoInt64.zero;
        case descriptors_js_1.ScalarType.BYTES:
          return new Uint8Array(0);
        default:
          return 0;
      }
    }
    __name(scalarZeroValue, "scalarZeroValue");
    function isScalarZeroValue(type, value) {
      switch (type) {
        case descriptors_js_1.ScalarType.BOOL:
          return value === false;
        case descriptors_js_1.ScalarType.STRING:
          return value === "";
        case descriptors_js_1.ScalarType.BYTES:
          return value instanceof Uint8Array && !value.byteLength;
        default:
          return value == 0;
      }
    }
    __name(isScalarZeroValue, "isScalarZeroValue");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/reflect/unsafe.js
var require_unsafe = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/reflect/unsafe.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.unsafeLocal = void 0;
    exports.unsafeOneofCase = unsafeOneofCase;
    exports.unsafeIsSet = unsafeIsSet;
    exports.unsafeIsSetExplicit = unsafeIsSetExplicit;
    exports.unsafeGet = unsafeGet;
    exports.unsafeSet = unsafeSet;
    exports.unsafeClear = unsafeClear;
    var scalar_js_1 = require_scalar();
    var IMPLICIT = 2;
    exports.unsafeLocal = Symbol.for("reflect unsafe local");
    function unsafeOneofCase(target, oneof) {
      const c = target[oneof.localName].case;
      if (c === void 0) {
        return c;
      }
      return oneof.fields.find((f) => f.localName === c);
    }
    __name(unsafeOneofCase, "unsafeOneofCase");
    function unsafeIsSet(target, field) {
      const name = field.localName;
      if (field.oneof) {
        return target[field.oneof.localName].case === name;
      }
      if (field.presence != IMPLICIT) {
        return target[name] !== void 0 && Object.prototype.hasOwnProperty.call(target, name);
      }
      switch (field.fieldKind) {
        case "list":
          return target[name].length > 0;
        case "map":
          return Object.keys(target[name]).length > 0;
        case "scalar":
          return !(0, scalar_js_1.isScalarZeroValue)(field.scalar, target[name]);
        case "enum":
          return target[name] !== field.enum.values[0].number;
      }
      throw new Error("message field with implicit presence");
    }
    __name(unsafeIsSet, "unsafeIsSet");
    function unsafeIsSetExplicit(target, localName) {
      return Object.prototype.hasOwnProperty.call(target, localName) && target[localName] !== void 0;
    }
    __name(unsafeIsSetExplicit, "unsafeIsSetExplicit");
    function unsafeGet(target, field) {
      if (field.oneof) {
        const oneof = target[field.oneof.localName];
        if (oneof.case === field.localName) {
          return oneof.value;
        }
        return void 0;
      }
      return target[field.localName];
    }
    __name(unsafeGet, "unsafeGet");
    function unsafeSet(target, field, value) {
      if (field.oneof) {
        target[field.oneof.localName] = {
          case: field.localName,
          value
        };
      } else {
        target[field.localName] = value;
      }
    }
    __name(unsafeSet, "unsafeSet");
    function unsafeClear(target, field) {
      const name = field.localName;
      if (field.oneof) {
        const oneofLocalName = field.oneof.localName;
        if (target[oneofLocalName].case === name) {
          target[oneofLocalName] = { case: void 0 };
        }
      } else if (field.presence != IMPLICIT) {
        delete target[name];
      } else {
        switch (field.fieldKind) {
          case "map":
            target[name] = {};
            break;
          case "list":
            target[name] = [];
            break;
          case "enum":
            target[name] = field.enum.values[0].number;
            break;
          case "scalar":
            target[name] = (0, scalar_js_1.scalarZeroValue)(field.scalar, field.longAsString);
            break;
        }
      }
    }
    __name(unsafeClear, "unsafeClear");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/reflect/guard.js
var require_guard = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/reflect/guard.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.isObject = isObject;
    exports.isOneofADT = isOneofADT;
    exports.isReflectList = isReflectList;
    exports.isReflectMap = isReflectMap;
    exports.isReflectMessage = isReflectMessage;
    var unsafe_js_1 = require_unsafe();
    function isObject(arg) {
      return arg !== null && typeof arg == "object" && !Array.isArray(arg);
    }
    __name(isObject, "isObject");
    function isOneofADT(arg) {
      return arg !== null && typeof arg == "object" && "case" in arg && (typeof arg.case == "string" && "value" in arg && arg.value != null || arg.case === void 0 && (!("value" in arg) || arg.value === void 0));
    }
    __name(isOneofADT, "isOneofADT");
    function isReflectList(arg, field) {
      var _a, _b, _c, _d;
      if (isObject(arg) && unsafe_js_1.unsafeLocal in arg && "add" in arg && "field" in arg && typeof arg.field == "function") {
        if (field !== void 0) {
          const a = field;
          const b = arg.field();
          return a.listKind == b.listKind && a.scalar === b.scalar && ((_a = a.message) === null || _a === void 0 ? void 0 : _a.typeName) === ((_b = b.message) === null || _b === void 0 ? void 0 : _b.typeName) && ((_c = a.enum) === null || _c === void 0 ? void 0 : _c.typeName) === ((_d = b.enum) === null || _d === void 0 ? void 0 : _d.typeName);
        }
        return true;
      }
      return false;
    }
    __name(isReflectList, "isReflectList");
    function isReflectMap(arg, field) {
      var _a, _b, _c, _d;
      if (isObject(arg) && unsafe_js_1.unsafeLocal in arg && "has" in arg && "field" in arg && typeof arg.field == "function") {
        if (field !== void 0) {
          const a = field, b = arg.field();
          return a.mapKey === b.mapKey && a.mapKind == b.mapKind && a.scalar === b.scalar && ((_a = a.message) === null || _a === void 0 ? void 0 : _a.typeName) === ((_b = b.message) === null || _b === void 0 ? void 0 : _b.typeName) && ((_c = a.enum) === null || _c === void 0 ? void 0 : _c.typeName) === ((_d = b.enum) === null || _d === void 0 ? void 0 : _d.typeName);
        }
        return true;
      }
      return false;
    }
    __name(isReflectMap, "isReflectMap");
    function isReflectMessage(arg, messageDesc) {
      return isObject(arg) && unsafe_js_1.unsafeLocal in arg && "desc" in arg && isObject(arg.desc) && arg.desc.kind === "message" && (messageDesc === void 0 || arg.desc.typeName == messageDesc.typeName);
    }
    __name(isReflectMessage, "isReflectMessage");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/wrappers.js
var require_wrappers = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/wrappers.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.isWrapper = isWrapper;
    exports.isWrapperDesc = isWrapperDesc;
    exports.hasCustomJsonRepresentation = hasCustomJsonRepresentation;
    function isWrapper(arg) {
      return isWrapperTypeName(arg.$typeName);
    }
    __name(isWrapper, "isWrapper");
    function isWrapperDesc(messageDesc) {
      const f = messageDesc.fields[0];
      return isWrapperTypeName(messageDesc.typeName) && f !== void 0 && f.fieldKind == "scalar" && f.name == "value" && f.number == 1;
    }
    __name(isWrapperDesc, "isWrapperDesc");
    function hasCustomJsonRepresentation(desc) {
      switch (desc.typeName) {
        case "google.protobuf.Any":
        case "google.protobuf.Timestamp":
        case "google.protobuf.Duration":
        case "google.protobuf.FieldMask":
        case "google.protobuf.Struct":
        case "google.protobuf.Value":
        case "google.protobuf.ListValue":
          return true;
        default:
          return isWrapperDesc(desc);
      }
    }
    __name(hasCustomJsonRepresentation, "hasCustomJsonRepresentation");
    function isWrapperTypeName(name) {
      return name.startsWith("google.protobuf.") && [
        "DoubleValue",
        "FloatValue",
        "Int64Value",
        "UInt64Value",
        "Int32Value",
        "UInt32Value",
        "BoolValue",
        "StringValue",
        "BytesValue"
      ].includes(name.substring(16));
    }
    __name(isWrapperTypeName, "isWrapperTypeName");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/create.js
var require_create = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/create.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.create = create;
    var is_message_js_1 = require_is_message();
    var descriptors_js_1 = require_descriptors();
    var scalar_js_1 = require_scalar();
    var guard_js_1 = require_guard();
    var unsafe_js_1 = require_unsafe();
    var wrappers_js_1 = require_wrappers();
    var EDITION_PROTO3 = 999;
    var EDITION_PROTO2 = 998;
    var IMPLICIT = 2;
    function create(schema, init) {
      if ((0, is_message_js_1.isMessage)(init, schema)) {
        return init;
      }
      const message = createZeroMessage(schema);
      if (init !== void 0) {
        initMessage(schema, message, init);
      }
      return message;
    }
    __name(create, "create");
    function initMessage(messageDesc, message, init) {
      for (const member of messageDesc.members) {
        let value = init[member.localName];
        if (value == null) {
          continue;
        }
        let field;
        if (member.kind == "oneof") {
          const oneofField = (0, unsafe_js_1.unsafeOneofCase)(init, member);
          if (!oneofField) {
            continue;
          }
          field = oneofField;
          value = (0, unsafe_js_1.unsafeGet)(init, oneofField);
        } else {
          field = member;
        }
        switch (field.fieldKind) {
          case "message":
            value = toMessage(field, value);
            break;
          case "scalar":
            value = initScalar(field, value);
            break;
          case "list":
            value = initList(field, value);
            break;
          case "map":
            value = initMap(field, value);
            break;
        }
        (0, unsafe_js_1.unsafeSet)(message, field, value);
      }
      return message;
    }
    __name(initMessage, "initMessage");
    function initScalar(field, value) {
      if (field.scalar == descriptors_js_1.ScalarType.BYTES) {
        return toU8Arr(value);
      }
      return value;
    }
    __name(initScalar, "initScalar");
    function initMap(field, value) {
      if ((0, guard_js_1.isObject)(value)) {
        if (field.scalar == descriptors_js_1.ScalarType.BYTES) {
          return convertObjectValues(value, toU8Arr);
        }
        if (field.mapKind == "message") {
          return convertObjectValues(value, (val) => toMessage(field, val));
        }
      }
      return value;
    }
    __name(initMap, "initMap");
    function initList(field, value) {
      if (Array.isArray(value)) {
        if (field.scalar == descriptors_js_1.ScalarType.BYTES) {
          return value.map(toU8Arr);
        }
        if (field.listKind == "message") {
          return value.map((item) => toMessage(field, item));
        }
      }
      return value;
    }
    __name(initList, "initList");
    function toMessage(field, value) {
      if (field.fieldKind == "message" && !field.oneof && (0, wrappers_js_1.isWrapperDesc)(field.message)) {
        return initScalar(field.message.fields[0], value);
      }
      if ((0, guard_js_1.isObject)(value)) {
        if (field.message.typeName == "google.protobuf.Struct" && field.parent.typeName !== "google.protobuf.Value") {
          return value;
        }
        if (!(0, is_message_js_1.isMessage)(value, field.message)) {
          return create(field.message, value);
        }
      }
      return value;
    }
    __name(toMessage, "toMessage");
    function toU8Arr(value) {
      return Array.isArray(value) ? new Uint8Array(value) : value;
    }
    __name(toU8Arr, "toU8Arr");
    function convertObjectValues(obj, fn) {
      const ret = {};
      for (const entry of Object.entries(obj)) {
        ret[entry[0]] = fn(entry[1]);
      }
      return ret;
    }
    __name(convertObjectValues, "convertObjectValues");
    var tokenZeroMessageField = Symbol();
    var messagePrototypes = /* @__PURE__ */ new WeakMap();
    function createZeroMessage(desc) {
      let msg;
      if (!needsPrototypeChain(desc)) {
        msg = {
          $typeName: desc.typeName
        };
        for (const member of desc.members) {
          if (member.kind == "oneof" || member.presence == IMPLICIT) {
            msg[member.localName] = createZeroField(member);
          }
        }
      } else {
        const cached = messagePrototypes.get(desc);
        let prototype;
        let members;
        if (cached) {
          ({ prototype, members } = cached);
        } else {
          prototype = {};
          members = /* @__PURE__ */ new Set();
          for (const member of desc.members) {
            if (member.kind == "oneof") {
              continue;
            }
            if (member.fieldKind != "scalar" && member.fieldKind != "enum") {
              continue;
            }
            if (member.presence == IMPLICIT) {
              continue;
            }
            members.add(member);
            prototype[member.localName] = createZeroField(member);
          }
          messagePrototypes.set(desc, { prototype, members });
        }
        msg = Object.create(prototype);
        msg.$typeName = desc.typeName;
        for (const member of desc.members) {
          if (members.has(member)) {
            continue;
          }
          if (member.kind == "field") {
            if (member.fieldKind == "message") {
              continue;
            }
            if (member.fieldKind == "scalar" || member.fieldKind == "enum") {
              if (member.presence != IMPLICIT) {
                continue;
              }
            }
          }
          msg[member.localName] = createZeroField(member);
        }
      }
      return msg;
    }
    __name(createZeroMessage, "createZeroMessage");
    function needsPrototypeChain(desc) {
      switch (desc.file.edition) {
        case EDITION_PROTO3:
          return false;
        case EDITION_PROTO2:
          return true;
        default:
          return desc.fields.some((f) => f.presence != IMPLICIT && f.fieldKind != "message" && !f.oneof);
      }
    }
    __name(needsPrototypeChain, "needsPrototypeChain");
    function createZeroField(field) {
      if (field.kind == "oneof") {
        return { case: void 0 };
      }
      if (field.fieldKind == "list") {
        return [];
      }
      if (field.fieldKind == "map") {
        return {};
      }
      if (field.fieldKind == "message") {
        return tokenZeroMessageField;
      }
      const defaultValue = field.getDefaultValue();
      if (defaultValue !== void 0) {
        return field.fieldKind == "scalar" && field.longAsString ? defaultValue.toString() : defaultValue;
      }
      return field.fieldKind == "scalar" ? (0, scalar_js_1.scalarZeroValue)(field.scalar, field.longAsString) : field.enum.values[0].number;
    }
    __name(createZeroField, "createZeroField");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/reflect/error.js
var require_error = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/reflect/error.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.FieldError = void 0;
    exports.isFieldError = isFieldError;
    var errorNames = [
      "FieldValueInvalidError",
      "FieldListRangeError",
      "ForeignFieldError"
    ];
    var FieldError = class extends Error {
      static {
        __name(this, "FieldError");
      }
      constructor(fieldOrOneof, message, name = "FieldValueInvalidError") {
        super(message);
        this.name = name;
        this.field = () => fieldOrOneof;
      }
    };
    exports.FieldError = FieldError;
    function isFieldError(arg) {
      return arg instanceof Error && errorNames.includes(arg.name) && "field" in arg && typeof arg.field == "function";
    }
    __name(isFieldError, "isFieldError");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wire/text-encoding.js
var require_text_encoding = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wire/text-encoding.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.configureTextEncoding = configureTextEncoding;
    exports.getTextEncoding = getTextEncoding;
    var symbol = Symbol.for("@bufbuild/protobuf/text-encoding");
    function configureTextEncoding(textEncoding) {
      globalThis[symbol] = textEncoding;
    }
    __name(configureTextEncoding, "configureTextEncoding");
    function getTextEncoding() {
      if (globalThis[symbol] == void 0) {
        const te = new globalThis.TextEncoder();
        const td = new globalThis.TextDecoder();
        let tdStrict;
        globalThis[symbol] = {
          encodeUtf8(text) {
            return te.encode(text);
          },
          decodeUtf8(bytes, strict) {
            if (strict) {
              if (tdStrict === void 0) {
                tdStrict = new globalThis.TextDecoder("utf-8", { fatal: true });
              }
              return tdStrict.decode(bytes);
            }
            return td.decode(bytes);
          },
          checkUtf8(text) {
            try {
              encodeURIComponent(text);
              return true;
            } catch (_) {
              return false;
            }
          }
        };
      }
      return globalThis[symbol];
    }
    __name(getTextEncoding, "getTextEncoding");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wire/binary-encoding.js
var require_binary_encoding = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wire/binary-encoding.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.BinaryReader = exports.BinaryWriter = exports.INT32_MIN = exports.INT32_MAX = exports.UINT32_MAX = exports.FLOAT32_MIN = exports.FLOAT32_MAX = exports.WireType = void 0;
    var varint_js_1 = require_varint();
    var proto_int64_js_1 = require_proto_int64();
    var text_encoding_js_1 = require_text_encoding();
    var WireType;
    (function(WireType2) {
      WireType2[WireType2["Varint"] = 0] = "Varint";
      WireType2[WireType2["Bit64"] = 1] = "Bit64";
      WireType2[WireType2["LengthDelimited"] = 2] = "LengthDelimited";
      WireType2[WireType2["StartGroup"] = 3] = "StartGroup";
      WireType2[WireType2["EndGroup"] = 4] = "EndGroup";
      WireType2[WireType2["Bit32"] = 5] = "Bit32";
    })(WireType || (exports.WireType = WireType = {}));
    exports.FLOAT32_MAX = 34028234663852886e22;
    exports.FLOAT32_MIN = -34028234663852886e22;
    exports.UINT32_MAX = 4294967295;
    exports.INT32_MAX = 2147483647;
    exports.INT32_MIN = -2147483648;
    var BinaryWriter = class {
      static {
        __name(this, "BinaryWriter");
      }
      constructor(encodeUtf8 = (0, text_encoding_js_1.getTextEncoding)().encodeUtf8) {
        this.encodeUtf8 = encodeUtf8;
        this.stack = [];
        this.chunks = [];
        this.buf = [];
      }
      /**
       * Return all bytes written and reset this writer.
       */
      finish() {
        if (this.buf.length) {
          this.chunks.push(new Uint8Array(this.buf));
          this.buf = [];
        }
        let len = 0;
        for (let i = 0; i < this.chunks.length; i++)
          len += this.chunks[i].length;
        let bytes = new Uint8Array(len);
        let offset = 0;
        for (let i = 0; i < this.chunks.length; i++) {
          bytes.set(this.chunks[i], offset);
          offset += this.chunks[i].length;
        }
        this.chunks = [];
        return bytes;
      }
      /**
       * Start a new fork for length-delimited data like a message
       * or a packed repeated field.
       *
       * Must be joined later with `join()`.
       */
      fork() {
        this.stack.push({ chunks: this.chunks, buf: this.buf });
        this.chunks = [];
        this.buf = [];
        return this;
      }
      /**
       * Join the last fork. Write its length and bytes, then
       * return to the previous state.
       */
      join() {
        let chunk = this.finish();
        let prev = this.stack.pop();
        if (!prev)
          throw new Error("invalid state, fork stack empty");
        this.chunks = prev.chunks;
        this.buf = prev.buf;
        this.uint32(chunk.byteLength);
        return this.raw(chunk);
      }
      /**
       * Writes a tag (field number and wire type).
       *
       * Equivalent to `uint32( (fieldNo << 3 | type) >>> 0 )`.
       *
       * Generated code should compute the tag ahead of time and call `uint32()`.
       */
      tag(fieldNo, type) {
        return this.uint32((fieldNo << 3 | type) >>> 0);
      }
      /**
       * Write a chunk of raw bytes.
       */
      raw(chunk) {
        if (this.buf.length) {
          this.chunks.push(new Uint8Array(this.buf));
          this.buf = [];
        }
        this.chunks.push(chunk);
        return this;
      }
      /**
       * Write a `uint32` value, an unsigned 32 bit varint.
       */
      uint32(value) {
        assertUInt32(value);
        while (value > 127) {
          this.buf.push(value & 127 | 128);
          value = value >>> 7;
        }
        this.buf.push(value);
        return this;
      }
      /**
       * Write a `int32` value, a signed 32 bit varint.
       */
      int32(value) {
        assertInt32(value);
        (0, varint_js_1.varint32write)(value, this.buf);
        return this;
      }
      /**
       * Write a `bool` value, a varint.
       */
      bool(value) {
        this.buf.push(value ? 1 : 0);
        return this;
      }
      /**
       * Write a `bytes` value, length-delimited arbitrary data.
       */
      bytes(value) {
        this.uint32(value.byteLength);
        return this.raw(value);
      }
      /**
       * Write a `string` value, length-delimited data converted to UTF-8 text.
       */
      string(value) {
        let chunk = this.encodeUtf8(value);
        this.uint32(chunk.byteLength);
        return this.raw(chunk);
      }
      /**
       * Write a `float` value, 32-bit floating point number.
       */
      float(value) {
        assertFloat32(value);
        let chunk = new Uint8Array(4);
        new DataView(chunk.buffer).setFloat32(0, value, true);
        return this.raw(chunk);
      }
      /**
       * Write a `double` value, a 64-bit floating point number.
       */
      double(value) {
        let chunk = new Uint8Array(8);
        new DataView(chunk.buffer).setFloat64(0, value, true);
        return this.raw(chunk);
      }
      /**
       * Write a `fixed32` value, an unsigned, fixed-length 32-bit integer.
       */
      fixed32(value) {
        assertUInt32(value);
        let chunk = new Uint8Array(4);
        new DataView(chunk.buffer).setUint32(0, value, true);
        return this.raw(chunk);
      }
      /**
       * Write a `sfixed32` value, a signed, fixed-length 32-bit integer.
       */
      sfixed32(value) {
        assertInt32(value);
        let chunk = new Uint8Array(4);
        new DataView(chunk.buffer).setInt32(0, value, true);
        return this.raw(chunk);
      }
      /**
       * Write a `sint32` value, a signed, zigzag-encoded 32-bit varint.
       */
      sint32(value) {
        assertInt32(value);
        value = (value << 1 ^ value >> 31) >>> 0;
        (0, varint_js_1.varint32write)(value, this.buf);
        return this;
      }
      /**
       * Write a `sfixed64` value, a signed, fixed-length 64-bit integer.
       */
      sfixed64(value) {
        let chunk = new Uint8Array(8), view = new DataView(chunk.buffer), tc = proto_int64_js_1.protoInt64.enc(value);
        view.setInt32(0, tc.lo, true);
        view.setInt32(4, tc.hi, true);
        return this.raw(chunk);
      }
      /**
       * Write a `fixed64` value, an unsigned, fixed-length 64 bit integer.
       */
      fixed64(value) {
        let chunk = new Uint8Array(8), view = new DataView(chunk.buffer), tc = proto_int64_js_1.protoInt64.uEnc(value);
        view.setInt32(0, tc.lo, true);
        view.setInt32(4, tc.hi, true);
        return this.raw(chunk);
      }
      /**
       * Write a `int64` value, a signed 64-bit varint.
       */
      int64(value) {
        let tc = proto_int64_js_1.protoInt64.enc(value);
        (0, varint_js_1.varint64write)(tc.lo, tc.hi, this.buf);
        return this;
      }
      /**
       * Write a `sint64` value, a signed, zig-zag-encoded 64-bit varint.
       */
      sint64(value) {
        const tc = proto_int64_js_1.protoInt64.enc(value), sign = tc.hi >> 31, lo = tc.lo << 1 ^ sign, hi = (tc.hi << 1 | tc.lo >>> 31) ^ sign;
        (0, varint_js_1.varint64write)(lo, hi, this.buf);
        return this;
      }
      /**
       * Write a `uint64` value, an unsigned 64-bit varint.
       */
      uint64(value) {
        const tc = proto_int64_js_1.protoInt64.uEnc(value);
        (0, varint_js_1.varint64write)(tc.lo, tc.hi, this.buf);
        return this;
      }
    };
    exports.BinaryWriter = BinaryWriter;
    var BinaryReader = class {
      static {
        __name(this, "BinaryReader");
      }
      constructor(buf, decodeUtf8 = (0, text_encoding_js_1.getTextEncoding)().decodeUtf8) {
        this.decodeUtf8 = decodeUtf8;
        this.varint64 = varint_js_1.varint64read;
        this.uint32 = varint_js_1.varint32read;
        this.buf = buf;
        this.len = buf.length;
        this.pos = 0;
        this.view = new DataView(buf.buffer, buf.byteOffset, buf.byteLength);
      }
      /**
       * Reads a tag - field number and wire type. Tags are uint32 varints; values
       * that do not fit in uint32 are rejected.
       */
      tag() {
        const start = this.pos;
        const tag = this.uint32();
        const bytesRead = this.pos - start;
        if (bytesRead > 5 || bytesRead == 5 && this.buf[this.pos - 1] > 15) {
          throw new Error("illegal tag: varint overflows uint32");
        }
        const fieldNo = tag >>> 3;
        const wireType = tag & 7;
        if (fieldNo <= 0 || wireType > 5) {
          throw new Error("illegal tag: field no " + fieldNo + " wire type " + wireType);
        }
        return [fieldNo, wireType];
      }
      /**
       * Skip one element and return the skipped data.
       *
       * When skipping StartGroup, provide the tags field number to check for
       * matching field number in the EndGroup tag.
       */
      skip(wireType, fieldNo) {
        let start = this.pos;
        switch (wireType) {
          case WireType.Varint:
            while (this.buf[this.pos++] & 128) {
            }
            break;
          // @ts-ignore TS7029: Fallthrough case in switch -- ignore instead of expect-error for compiler settings without noFallthroughCasesInSwitch: true
          case WireType.Bit64:
            this.pos += 4;
          case WireType.Bit32:
            this.pos += 4;
            break;
          case WireType.LengthDelimited:
            let len = this.uint32();
            this.pos += len;
            break;
          case WireType.StartGroup:
            for (; ; ) {
              const [fn, wt] = this.tag();
              if (wt === WireType.EndGroup) {
                if (fieldNo !== void 0 && fn !== fieldNo) {
                  throw new Error("invalid end group tag");
                }
                break;
              }
              this.skip(wt, fn);
            }
            break;
          default:
            throw new Error("cant skip wire type " + wireType);
        }
        this.assertBounds();
        return this.buf.subarray(start, this.pos);
      }
      /**
       * Throws error if position in byte array is out of range.
       */
      assertBounds() {
        if (this.pos > this.len)
          throw new RangeError("premature EOF");
      }
      /**
       * Read a `int32` field, a signed 32 bit varint.
       */
      int32() {
        return this.uint32() | 0;
      }
      /**
       * Read a `sint32` field, a signed, zigzag-encoded 32-bit varint.
       */
      sint32() {
        let zze = this.uint32();
        return zze >>> 1 ^ -(zze & 1);
      }
      /**
       * Read a `int64` field, a signed 64-bit varint.
       */
      int64() {
        return proto_int64_js_1.protoInt64.dec(...this.varint64());
      }
      /**
       * Read a `uint64` field, an unsigned 64-bit varint.
       */
      uint64() {
        return proto_int64_js_1.protoInt64.uDec(...this.varint64());
      }
      /**
       * Read a `sint64` field, a signed, zig-zag-encoded 64-bit varint.
       */
      sint64() {
        let [lo, hi] = this.varint64();
        let s = -(lo & 1);
        lo = (lo >>> 1 | (hi & 1) << 31) ^ s;
        hi = hi >>> 1 ^ s;
        return proto_int64_js_1.protoInt64.dec(lo, hi);
      }
      /**
       * Read a `bool` field, a variant.
       */
      bool() {
        let [lo, hi] = this.varint64();
        return lo !== 0 || hi !== 0;
      }
      /**
       * Read a `fixed32` field, an unsigned, fixed-length 32-bit integer.
       */
      fixed32() {
        return this.view.getUint32((this.pos += 4) - 4, true);
      }
      /**
       * Read a `sfixed32` field, a signed, fixed-length 32-bit integer.
       */
      sfixed32() {
        return this.view.getInt32((this.pos += 4) - 4, true);
      }
      /**
       * Read a `fixed64` field, an unsigned, fixed-length 64 bit integer.
       */
      fixed64() {
        return proto_int64_js_1.protoInt64.uDec(this.sfixed32(), this.sfixed32());
      }
      /**
       * Read a `fixed64` field, a signed, fixed-length 64-bit integer.
       */
      sfixed64() {
        return proto_int64_js_1.protoInt64.dec(this.sfixed32(), this.sfixed32());
      }
      /**
       * Read a `float` field, 32-bit floating point number.
       */
      float() {
        return this.view.getFloat32((this.pos += 4) - 4, true);
      }
      /**
       * Read a `double` field, a 64-bit floating point number.
       */
      double() {
        return this.view.getFloat64((this.pos += 8) - 8, true);
      }
      /**
       * Read a `bytes` field, length-delimited arbitrary data.
       */
      bytes() {
        let len = this.uint32(), start = this.pos;
        this.pos += len;
        this.assertBounds();
        return this.buf.subarray(start, start + len);
      }
      /**
       * Read a `string` field, length-delimited data converted to UTF-8 text. If
       * `strict` is true, throw on invalid UTF-8 instead of substituting U+FFFD.
       */
      string(strict) {
        return this.decodeUtf8(this.bytes(), strict);
      }
    };
    exports.BinaryReader = BinaryReader;
    function assertInt32(arg) {
      if (typeof arg == "string") {
        arg = Number(arg);
      } else if (typeof arg != "number") {
        throw new Error("invalid int32: " + typeof arg);
      }
      if (!Number.isInteger(arg) || arg > exports.INT32_MAX || arg < exports.INT32_MIN)
        throw new Error("invalid int32: " + arg);
    }
    __name(assertInt32, "assertInt32");
    function assertUInt32(arg) {
      if (typeof arg == "string") {
        arg = Number(arg);
      } else if (typeof arg != "number") {
        throw new Error("invalid uint32: " + typeof arg);
      }
      if (!Number.isInteger(arg) || arg > exports.UINT32_MAX || arg < 0)
        throw new Error("invalid uint32: " + arg);
    }
    __name(assertUInt32, "assertUInt32");
    function assertFloat32(arg) {
      if (typeof arg == "string") {
        const o = arg;
        arg = Number(arg);
        if (Number.isNaN(arg) && o !== "NaN") {
          throw new Error("invalid float32: " + o);
        }
      } else if (typeof arg != "number") {
        throw new Error("invalid float32: " + typeof arg);
      }
      if (Number.isFinite(arg) && (arg > exports.FLOAT32_MAX || arg < exports.FLOAT32_MIN))
        throw new Error("invalid float32: " + arg);
    }
    __name(assertFloat32, "assertFloat32");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/reflect/reflect-check.js
var require_reflect_check = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/reflect/reflect-check.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.checkField = checkField;
    exports.checkListItem = checkListItem;
    exports.checkMapEntry = checkMapEntry;
    exports.formatVal = formatVal;
    var descriptors_js_1 = require_descriptors();
    var is_message_js_1 = require_is_message();
    var error_js_1 = require_error();
    var guard_js_1 = require_guard();
    var binary_encoding_js_1 = require_binary_encoding();
    var text_encoding_js_1 = require_text_encoding();
    var proto_int64_js_1 = require_proto_int64();
    function checkField(field, value) {
      const check = field.fieldKind == "list" ? (0, guard_js_1.isReflectList)(value, field) : field.fieldKind == "map" ? (0, guard_js_1.isReflectMap)(value, field) : checkSingular(field, value);
      if (check === true) {
        return void 0;
      }
      let reason;
      switch (field.fieldKind) {
        case "list":
          reason = `expected ${formatReflectList(field)}, got ${formatVal(value)}`;
          break;
        case "map":
          reason = `expected ${formatReflectMap(field)}, got ${formatVal(value)}`;
          break;
        default: {
          reason = reasonSingular(field, value, check);
        }
      }
      return new error_js_1.FieldError(field, reason);
    }
    __name(checkField, "checkField");
    function checkListItem(field, index, value) {
      const check = checkSingular(field, value);
      if (check !== true) {
        return new error_js_1.FieldError(field, `list item #${index + 1}: ${reasonSingular(field, value, check)}`);
      }
      return void 0;
    }
    __name(checkListItem, "checkListItem");
    function checkMapEntry(field, key, value) {
      const checkKey = checkScalarValue(key, field.mapKey);
      if (checkKey !== true) {
        return new error_js_1.FieldError(field, `invalid map key: ${reasonSingular({ scalar: field.mapKey }, key, checkKey)}`);
      }
      const checkVal = checkSingular(field, value);
      if (checkVal !== true) {
        return new error_js_1.FieldError(field, `map entry ${formatVal(key)}: ${reasonSingular(field, value, checkVal)}`);
      }
      return void 0;
    }
    __name(checkMapEntry, "checkMapEntry");
    function checkSingular(field, value) {
      if (field.scalar !== void 0) {
        return checkScalarValue(value, field.scalar);
      }
      if (field.enum !== void 0) {
        if (field.enum.open) {
          return Number.isInteger(value);
        }
        return field.enum.values.some((v) => v.number === value);
      }
      return (0, guard_js_1.isReflectMessage)(value, field.message);
    }
    __name(checkSingular, "checkSingular");
    function checkScalarValue(value, scalar) {
      switch (scalar) {
        case descriptors_js_1.ScalarType.DOUBLE:
          return typeof value == "number";
        case descriptors_js_1.ScalarType.FLOAT:
          if (typeof value != "number") {
            return false;
          }
          if (Number.isNaN(value) || !Number.isFinite(value)) {
            return true;
          }
          if (value > binary_encoding_js_1.FLOAT32_MAX || value < binary_encoding_js_1.FLOAT32_MIN) {
            return `${value.toFixed()} out of range`;
          }
          return true;
        case descriptors_js_1.ScalarType.INT32:
        case descriptors_js_1.ScalarType.SFIXED32:
        case descriptors_js_1.ScalarType.SINT32:
          if (typeof value !== "number" || !Number.isInteger(value)) {
            return false;
          }
          if (value > binary_encoding_js_1.INT32_MAX || value < binary_encoding_js_1.INT32_MIN) {
            return `${value.toFixed()} out of range`;
          }
          return true;
        case descriptors_js_1.ScalarType.FIXED32:
        case descriptors_js_1.ScalarType.UINT32:
          if (typeof value !== "number" || !Number.isInteger(value)) {
            return false;
          }
          if (value > binary_encoding_js_1.UINT32_MAX || value < 0) {
            return `${value.toFixed()} out of range`;
          }
          return true;
        case descriptors_js_1.ScalarType.BOOL:
          return typeof value == "boolean";
        case descriptors_js_1.ScalarType.STRING:
          if (typeof value != "string") {
            return false;
          }
          return (0, text_encoding_js_1.getTextEncoding)().checkUtf8(value) || "invalid UTF8";
        case descriptors_js_1.ScalarType.BYTES:
          return value instanceof Uint8Array;
        case descriptors_js_1.ScalarType.INT64:
        case descriptors_js_1.ScalarType.SFIXED64:
        case descriptors_js_1.ScalarType.SINT64:
          if (typeof value == "bigint" || typeof value == "number" || typeof value == "string" && value.length > 0) {
            try {
              proto_int64_js_1.protoInt64.parse(value);
              return true;
            } catch (_) {
              return `${value} out of range`;
            }
          }
          return false;
        case descriptors_js_1.ScalarType.FIXED64:
        case descriptors_js_1.ScalarType.UINT64:
          if (typeof value == "bigint" || typeof value == "number" || typeof value == "string" && value.length > 0) {
            try {
              proto_int64_js_1.protoInt64.uParse(value);
              return true;
            } catch (_) {
              return `${value} out of range`;
            }
          }
          return false;
      }
    }
    __name(checkScalarValue, "checkScalarValue");
    function reasonSingular(field, val, details) {
      details = typeof details == "string" ? `: ${details}` : `, got ${formatVal(val)}`;
      if (field.scalar !== void 0) {
        return `expected ${scalarTypeDescription(field.scalar)}` + details;
      }
      if (field.enum !== void 0) {
        return `expected ${field.enum.toString()}` + details;
      }
      return `expected ${formatReflectMessage(field.message)}` + details;
    }
    __name(reasonSingular, "reasonSingular");
    function formatVal(val) {
      switch (typeof val) {
        case "object":
          if (val === null) {
            return "null";
          }
          if (val instanceof Uint8Array) {
            return `Uint8Array(${val.length})`;
          }
          if (Array.isArray(val)) {
            return `Array(${val.length})`;
          }
          if ((0, guard_js_1.isReflectList)(val)) {
            return formatReflectList(val.field());
          }
          if ((0, guard_js_1.isReflectMap)(val)) {
            return formatReflectMap(val.field());
          }
          if ((0, guard_js_1.isReflectMessage)(val)) {
            return formatReflectMessage(val.desc);
          }
          if ((0, is_message_js_1.isMessage)(val)) {
            return `message ${val.$typeName}`;
          }
          return "object";
        case "string":
          return val.length > 30 ? "string" : `"${val.split('"').join('\\"')}"`;
        case "boolean":
          return String(val);
        case "number":
          return String(val);
        case "bigint":
          return String(val) + "n";
        default:
          return typeof val;
      }
    }
    __name(formatVal, "formatVal");
    function formatReflectMessage(desc) {
      return `ReflectMessage (${desc.typeName})`;
    }
    __name(formatReflectMessage, "formatReflectMessage");
    function formatReflectList(field) {
      switch (field.listKind) {
        case "message":
          return `ReflectList (${field.message.toString()})`;
        case "enum":
          return `ReflectList (${field.enum.toString()})`;
        case "scalar":
          return `ReflectList (${descriptors_js_1.ScalarType[field.scalar]})`;
      }
    }
    __name(formatReflectList, "formatReflectList");
    function formatReflectMap(field) {
      switch (field.mapKind) {
        case "message":
          return `ReflectMap (${descriptors_js_1.ScalarType[field.mapKey]}, ${field.message.toString()})`;
        case "enum":
          return `ReflectMap (${descriptors_js_1.ScalarType[field.mapKey]}, ${field.enum.toString()})`;
        case "scalar":
          return `ReflectMap (${descriptors_js_1.ScalarType[field.mapKey]}, ${descriptors_js_1.ScalarType[field.scalar]})`;
      }
    }
    __name(formatReflectMap, "formatReflectMap");
    function scalarTypeDescription(scalar) {
      switch (scalar) {
        case descriptors_js_1.ScalarType.STRING:
          return "string";
        case descriptors_js_1.ScalarType.BOOL:
          return "boolean";
        case descriptors_js_1.ScalarType.INT64:
        case descriptors_js_1.ScalarType.SINT64:
        case descriptors_js_1.ScalarType.SFIXED64:
          return "bigint (int64)";
        case descriptors_js_1.ScalarType.UINT64:
        case descriptors_js_1.ScalarType.FIXED64:
          return "bigint (uint64)";
        case descriptors_js_1.ScalarType.BYTES:
          return "Uint8Array";
        case descriptors_js_1.ScalarType.DOUBLE:
          return "number (float64)";
        case descriptors_js_1.ScalarType.FLOAT:
          return "number (float32)";
        case descriptors_js_1.ScalarType.FIXED32:
        case descriptors_js_1.ScalarType.UINT32:
          return "number (uint32)";
        case descriptors_js_1.ScalarType.INT32:
        case descriptors_js_1.ScalarType.SFIXED32:
        case descriptors_js_1.ScalarType.SINT32:
          return "number (int32)";
      }
    }
    __name(scalarTypeDescription, "scalarTypeDescription");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/reflect/reflect.js
var require_reflect = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/reflect/reflect.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.reflect = reflect;
    exports.reflectList = reflectList;
    exports.reflectMap = reflectMap;
    var descriptors_js_1 = require_descriptors();
    var reflect_check_js_1 = require_reflect_check();
    var error_js_1 = require_error();
    var unsafe_js_1 = require_unsafe();
    var create_js_1 = require_create();
    var wrappers_js_1 = require_wrappers();
    var scalar_js_1 = require_scalar();
    var proto_int64_js_1 = require_proto_int64();
    var guard_js_1 = require_guard();
    function reflect(messageDesc, message, check = true) {
      return new ReflectMessageImpl(messageDesc, message, check);
    }
    __name(reflect, "reflect");
    var messageSortedFields = /* @__PURE__ */ new WeakMap();
    var ReflectMessageImpl = class {
      static {
        __name(this, "ReflectMessageImpl");
      }
      get sortedFields() {
        const cached = messageSortedFields.get(this.desc);
        if (cached) {
          return cached;
        }
        const sortedFields = this.desc.fields.concat().sort((a, b) => a.number - b.number);
        messageSortedFields.set(this.desc, sortedFields);
        return sortedFields;
      }
      constructor(messageDesc, message, check = true) {
        this.lists = /* @__PURE__ */ new Map();
        this.maps = /* @__PURE__ */ new Map();
        this.check = check;
        this.desc = messageDesc;
        this.message = this[unsafe_js_1.unsafeLocal] = message !== null && message !== void 0 ? message : (0, create_js_1.create)(messageDesc);
        this.fields = messageDesc.fields;
        this.oneofs = messageDesc.oneofs;
        this.members = messageDesc.members;
      }
      findNumber(number) {
        if (!this._fieldsByNumber) {
          this._fieldsByNumber = new Map(this.desc.fields.map((f) => [f.number, f]));
        }
        return this._fieldsByNumber.get(number);
      }
      oneofCase(oneof) {
        assertOwn(this.message, oneof);
        return (0, unsafe_js_1.unsafeOneofCase)(this.message, oneof);
      }
      isSet(field) {
        assertOwn(this.message, field);
        return (0, unsafe_js_1.unsafeIsSet)(this.message, field);
      }
      clear(field) {
        assertOwn(this.message, field);
        (0, unsafe_js_1.unsafeClear)(this.message, field);
      }
      get(field) {
        assertOwn(this.message, field);
        const value = (0, unsafe_js_1.unsafeGet)(this.message, field);
        switch (field.fieldKind) {
          case "list":
            let list = this.lists.get(field);
            if (!list || list[unsafe_js_1.unsafeLocal] !== value) {
              this.lists.set(
                field,
                // biome-ignore lint/suspicious/noAssignInExpressions: no
                list = new ReflectListImpl(field, value, this.check)
              );
            }
            return list;
          case "map":
            let map = this.maps.get(field);
            if (!map || map[unsafe_js_1.unsafeLocal] !== value) {
              this.maps.set(
                field,
                // biome-ignore lint/suspicious/noAssignInExpressions: no
                map = new ReflectMapImpl(field, value, this.check)
              );
            }
            return map;
          case "message":
            return messageToReflect(field, value, this.check);
          case "scalar":
            return value === void 0 ? (0, scalar_js_1.scalarZeroValue)(field.scalar, false) : longToReflect(field, value);
          case "enum":
            return value !== null && value !== void 0 ? value : field.enum.values[0].number;
        }
      }
      set(field, value) {
        assertOwn(this.message, field);
        if (this.check) {
          const err = (0, reflect_check_js_1.checkField)(field, value);
          if (err) {
            throw err;
          }
        }
        let local;
        if (field.fieldKind == "message") {
          local = messageToLocal(field, value);
        } else if ((0, guard_js_1.isReflectMap)(value) || (0, guard_js_1.isReflectList)(value)) {
          local = value[unsafe_js_1.unsafeLocal];
        } else {
          local = longToLocal(field, value);
        }
        (0, unsafe_js_1.unsafeSet)(this.message, field, local);
      }
      getUnknown() {
        return this.message.$unknown;
      }
      setUnknown(value) {
        this.message.$unknown = value;
      }
    };
    function assertOwn(owner, member) {
      if (member.parent.typeName !== owner.$typeName) {
        throw new error_js_1.FieldError(member, `cannot use ${member.toString()} with message ${owner.$typeName}`, "ForeignFieldError");
      }
    }
    __name(assertOwn, "assertOwn");
    function reflectList(field, unsafeInput, check = true) {
      return new ReflectListImpl(field, unsafeInput !== null && unsafeInput !== void 0 ? unsafeInput : [], check);
    }
    __name(reflectList, "reflectList");
    var ReflectListImpl = class {
      static {
        __name(this, "ReflectListImpl");
      }
      field() {
        return this._field;
      }
      get size() {
        return this._arr.length;
      }
      constructor(field, unsafeInput, check) {
        this._field = field;
        this._arr = this[unsafe_js_1.unsafeLocal] = unsafeInput;
        this.check = check;
      }
      get(index) {
        const item = this._arr[index];
        return item === void 0 ? void 0 : listItemToReflect(this._field, item, this.check);
      }
      set(index, item) {
        if (index < 0 || index >= this._arr.length) {
          throw new error_js_1.FieldError(this._field, `list item #${index + 1}: out of range`);
        }
        if (this.check) {
          const err = (0, reflect_check_js_1.checkListItem)(this._field, index, item);
          if (err) {
            throw err;
          }
        }
        this._arr[index] = listItemToLocal(this._field, item);
      }
      add(item) {
        if (this.check) {
          const err = (0, reflect_check_js_1.checkListItem)(this._field, this._arr.length, item);
          if (err) {
            throw err;
          }
        }
        this._arr.push(listItemToLocal(this._field, item));
        return void 0;
      }
      clear() {
        this._arr.splice(0, this._arr.length);
      }
      [Symbol.iterator]() {
        return this.values();
      }
      keys() {
        return this._arr.keys();
      }
      *values() {
        for (const item of this._arr) {
          yield listItemToReflect(this._field, item, this.check);
        }
      }
      *entries() {
        for (let i = 0; i < this._arr.length; i++) {
          yield [i, listItemToReflect(this._field, this._arr[i], this.check)];
        }
      }
    };
    function reflectMap(field, unsafeInput, check = true) {
      return new ReflectMapImpl(field, unsafeInput, check);
    }
    __name(reflectMap, "reflectMap");
    var ReflectMapImpl = class {
      static {
        __name(this, "ReflectMapImpl");
      }
      constructor(field, unsafeInput, check = true) {
        this.obj = this[unsafe_js_1.unsafeLocal] = unsafeInput !== null && unsafeInput !== void 0 ? unsafeInput : {};
        this.check = check;
        this._field = field;
      }
      field() {
        return this._field;
      }
      set(key, value) {
        if (this.check) {
          const err = (0, reflect_check_js_1.checkMapEntry)(this._field, key, value);
          if (err) {
            throw err;
          }
        }
        this.obj[mapKeyToLocal(key)] = mapValueToLocal(this._field, value);
        return this;
      }
      delete(key) {
        const k = mapKeyToLocal(key);
        const has = Object.prototype.hasOwnProperty.call(this.obj, k);
        if (has) {
          delete this.obj[k];
        }
        return has;
      }
      clear() {
        for (const key of Object.keys(this.obj)) {
          delete this.obj[key];
        }
      }
      get(key) {
        let val = this.obj[mapKeyToLocal(key)];
        if (val !== void 0) {
          val = mapValueToReflect(this._field, val, this.check);
        }
        return val;
      }
      has(key) {
        return Object.prototype.hasOwnProperty.call(this.obj, mapKeyToLocal(key));
      }
      *keys() {
        for (const objKey of Object.keys(this.obj)) {
          yield mapKeyToReflect(objKey, this._field.mapKey);
        }
      }
      *entries() {
        for (const objEntry of Object.entries(this.obj)) {
          yield [
            mapKeyToReflect(objEntry[0], this._field.mapKey),
            mapValueToReflect(this._field, objEntry[1], this.check)
          ];
        }
      }
      [Symbol.iterator]() {
        return this.entries();
      }
      get size() {
        return Object.keys(this.obj).length;
      }
      *values() {
        for (const val of Object.values(this.obj)) {
          yield mapValueToReflect(this._field, val, this.check);
        }
      }
      forEach(callbackfn, thisArg) {
        for (const mapEntry of this.entries()) {
          callbackfn.call(thisArg, mapEntry[1], mapEntry[0], this);
        }
      }
    };
    function messageToLocal(field, value) {
      if (!(0, guard_js_1.isReflectMessage)(value)) {
        return value;
      }
      if ((0, wrappers_js_1.isWrapper)(value.message) && !field.oneof && field.fieldKind == "message") {
        return value.message.value;
      }
      if (value.desc.typeName == "google.protobuf.Struct" && field.parent.typeName != "google.protobuf.Value") {
        return wktStructToLocal(value.message);
      }
      return value.message;
    }
    __name(messageToLocal, "messageToLocal");
    function messageToReflect(field, value, check) {
      if (value !== void 0) {
        if ((0, wrappers_js_1.isWrapperDesc)(field.message) && !field.oneof && field.fieldKind == "message") {
          value = {
            $typeName: field.message.typeName,
            value: longToReflect(field.message.fields[0], value)
          };
        } else if (field.message.typeName == "google.protobuf.Struct" && field.parent.typeName != "google.protobuf.Value" && (0, guard_js_1.isObject)(value)) {
          value = wktStructToReflect(value);
        }
      }
      return new ReflectMessageImpl(field.message, value, check);
    }
    __name(messageToReflect, "messageToReflect");
    function listItemToLocal(field, value) {
      if (field.listKind == "message") {
        return messageToLocal(field, value);
      }
      return longToLocal(field, value);
    }
    __name(listItemToLocal, "listItemToLocal");
    function listItemToReflect(field, value, check) {
      if (field.listKind == "message") {
        return messageToReflect(field, value, check);
      }
      return longToReflect(field, value);
    }
    __name(listItemToReflect, "listItemToReflect");
    function mapValueToLocal(field, value) {
      if (field.mapKind == "message") {
        return messageToLocal(field, value);
      }
      return longToLocal(field, value);
    }
    __name(mapValueToLocal, "mapValueToLocal");
    function mapValueToReflect(field, value, check) {
      if (field.mapKind == "message") {
        return messageToReflect(field, value, check);
      }
      return value;
    }
    __name(mapValueToReflect, "mapValueToReflect");
    function mapKeyToLocal(key) {
      return typeof key == "string" || typeof key == "number" ? key : String(key);
    }
    __name(mapKeyToLocal, "mapKeyToLocal");
    function mapKeyToReflect(key, type) {
      switch (type) {
        case descriptors_js_1.ScalarType.STRING:
          return key;
        case descriptors_js_1.ScalarType.INT32:
        case descriptors_js_1.ScalarType.FIXED32:
        case descriptors_js_1.ScalarType.UINT32:
        case descriptors_js_1.ScalarType.SFIXED32:
        case descriptors_js_1.ScalarType.SINT32: {
          const n = Number.parseInt(key);
          if (Number.isFinite(n)) {
            return n;
          }
          break;
        }
        case descriptors_js_1.ScalarType.BOOL:
          switch (key) {
            case "true":
              return true;
            case "false":
              return false;
          }
          break;
        case descriptors_js_1.ScalarType.UINT64:
        case descriptors_js_1.ScalarType.FIXED64:
          try {
            return proto_int64_js_1.protoInt64.uParse(key);
          } catch (_a) {
          }
          break;
        default:
          try {
            return proto_int64_js_1.protoInt64.parse(key);
          } catch (_b) {
          }
          break;
      }
      return key;
    }
    __name(mapKeyToReflect, "mapKeyToReflect");
    function longToReflect(field, value) {
      switch (field.scalar) {
        case descriptors_js_1.ScalarType.INT64:
        case descriptors_js_1.ScalarType.SFIXED64:
        case descriptors_js_1.ScalarType.SINT64:
          if ("longAsString" in field && field.longAsString && typeof value == "string") {
            value = proto_int64_js_1.protoInt64.parse(value);
          }
          break;
        case descriptors_js_1.ScalarType.FIXED64:
        case descriptors_js_1.ScalarType.UINT64:
          if ("longAsString" in field && field.longAsString && typeof value == "string") {
            value = proto_int64_js_1.protoInt64.uParse(value);
          }
          break;
      }
      return value;
    }
    __name(longToReflect, "longToReflect");
    function longToLocal(field, value) {
      switch (field.scalar) {
        case descriptors_js_1.ScalarType.INT64:
        case descriptors_js_1.ScalarType.SFIXED64:
        case descriptors_js_1.ScalarType.SINT64:
          if ("longAsString" in field && field.longAsString) {
            value = String(value);
          } else if (typeof value == "string" || typeof value == "number") {
            value = proto_int64_js_1.protoInt64.parse(value);
          }
          break;
        case descriptors_js_1.ScalarType.FIXED64:
        case descriptors_js_1.ScalarType.UINT64:
          if ("longAsString" in field && field.longAsString) {
            value = String(value);
          } else if (typeof value == "string" || typeof value == "number") {
            value = proto_int64_js_1.protoInt64.uParse(value);
          }
          break;
      }
      return value;
    }
    __name(longToLocal, "longToLocal");
    function wktStructToReflect(json) {
      const struct = {
        $typeName: "google.protobuf.Struct",
        fields: {}
      };
      if ((0, guard_js_1.isObject)(json)) {
        for (const [k, v] of Object.entries(json)) {
          struct.fields[k] = wktValueToReflect(v);
        }
      }
      return struct;
    }
    __name(wktStructToReflect, "wktStructToReflect");
    function wktStructToLocal(val) {
      const json = {};
      for (const [k, v] of Object.entries(val.fields)) {
        json[k] = wktValueToLocal(v);
      }
      return json;
    }
    __name(wktStructToLocal, "wktStructToLocal");
    function wktValueToLocal(val) {
      switch (val.kind.case) {
        case "structValue":
          return wktStructToLocal(val.kind.value);
        case "listValue":
          return val.kind.value.values.map(wktValueToLocal);
        case "nullValue":
        case void 0:
          return null;
        default:
          return val.kind.value;
      }
    }
    __name(wktValueToLocal, "wktValueToLocal");
    function wktValueToReflect(json) {
      const value = {
        $typeName: "google.protobuf.Value",
        kind: { case: void 0 }
      };
      switch (typeof json) {
        case "number":
          value.kind = { case: "numberValue", value: json };
          break;
        case "string":
          value.kind = { case: "stringValue", value: json };
          break;
        case "boolean":
          value.kind = { case: "boolValue", value: json };
          break;
        case "object":
          if (json === null) {
            const nullValue = 0;
            value.kind = { case: "nullValue", value: nullValue };
          } else if (Array.isArray(json)) {
            const listValue = {
              $typeName: "google.protobuf.ListValue",
              values: []
            };
            if (Array.isArray(json)) {
              for (const e of json) {
                listValue.values.push(wktValueToReflect(e));
              }
            }
            value.kind = {
              case: "listValue",
              value: listValue
            };
          } else {
            value.kind = {
              case: "structValue",
              value: wktStructToReflect(json)
            };
          }
          break;
      }
      return value;
    }
    __name(wktValueToReflect, "wktValueToReflect");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/clone.js
var require_clone = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/clone.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.clone = clone;
    var descriptors_js_1 = require_descriptors();
    var reflect_js_1 = require_reflect();
    var guard_js_1 = require_guard();
    function clone(schema, message) {
      return cloneReflect((0, reflect_js_1.reflect)(schema, message)).message;
    }
    __name(clone, "clone");
    function cloneReflect(i) {
      const o = (0, reflect_js_1.reflect)(i.desc);
      for (const f of i.fields) {
        if (!i.isSet(f)) {
          continue;
        }
        switch (f.fieldKind) {
          case "list":
            const list = o.get(f);
            for (const item of i.get(f)) {
              list.add(cloneSingular(f, item));
            }
            break;
          case "map":
            const map = o.get(f);
            for (const entry of i.get(f).entries()) {
              map.set(entry[0], cloneSingular(f, entry[1]));
            }
            break;
          default: {
            o.set(f, cloneSingular(f, i.get(f)));
            break;
          }
        }
      }
      const unknown = i.getUnknown();
      if (unknown && unknown.length > 0) {
        o.setUnknown([...unknown]);
      }
      return o;
    }
    __name(cloneReflect, "cloneReflect");
    function cloneSingular(field, value) {
      if (field.message !== void 0 && (0, guard_js_1.isReflectMessage)(value)) {
        return cloneReflect(value);
      }
      if (field.scalar == descriptors_js_1.ScalarType.BYTES && value instanceof Uint8Array) {
        return value.slice();
      }
      return value;
    }
    __name(cloneSingular, "cloneSingular");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wire/base64-encoding.js
var require_base64_encoding = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wire/base64-encoding.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.base64Decode = base64Decode;
    exports.base64Encode = base64Encode;
    function base64Decode(base64Str) {
      const table = getDecodeTable();
      let es = base64Str.length * 3 / 4;
      if (base64Str[base64Str.length - 2] == "=")
        es -= 2;
      else if (base64Str[base64Str.length - 1] == "=")
        es -= 1;
      let bytes = new Uint8Array(es), bytePos = 0, groupPos = 0, b, p = 0;
      for (let i = 0; i < base64Str.length; i++) {
        b = table[base64Str.charCodeAt(i)];
        if (b === void 0) {
          switch (base64Str[i]) {
            // @ts-ignore TS7029: Fallthrough case in switch -- ignore instead of expect-error for compiler settings without noFallthroughCasesInSwitch: true
            case "=":
              groupPos = 0;
            // reset state when padding found
            case "\n":
            case "\r":
            case "	":
            case " ":
              continue;
            // skip white-space, and padding
            default:
              throw Error("invalid base64 string");
          }
        }
        switch (groupPos) {
          case 0:
            p = b;
            groupPos = 1;
            break;
          case 1:
            bytes[bytePos++] = p << 2 | (b & 48) >> 4;
            p = b;
            groupPos = 2;
            break;
          case 2:
            bytes[bytePos++] = (p & 15) << 4 | (b & 60) >> 2;
            p = b;
            groupPos = 3;
            break;
          case 3:
            bytes[bytePos++] = (p & 3) << 6 | b;
            groupPos = 0;
            break;
        }
      }
      if (groupPos == 1)
        throw Error("invalid base64 string");
      return bytes.subarray(0, bytePos);
    }
    __name(base64Decode, "base64Decode");
    function base64Encode(bytes, encoding = "std") {
      const table = getEncodeTable(encoding);
      const pad = encoding == "std";
      let base64 = "", groupPos = 0, b, p = 0;
      for (let i = 0; i < bytes.length; i++) {
        b = bytes[i];
        switch (groupPos) {
          case 0:
            base64 += table[b >> 2];
            p = (b & 3) << 4;
            groupPos = 1;
            break;
          case 1:
            base64 += table[p | b >> 4];
            p = (b & 15) << 2;
            groupPos = 2;
            break;
          case 2:
            base64 += table[p | b >> 6];
            base64 += table[b & 63];
            groupPos = 0;
            break;
        }
      }
      if (groupPos) {
        base64 += table[p];
        if (pad) {
          base64 += "=";
          if (groupPos == 1)
            base64 += "=";
        }
      }
      return base64;
    }
    __name(base64Encode, "base64Encode");
    var encodeTableStd;
    var encodeTableUrl;
    var decodeTable;
    function getEncodeTable(encoding) {
      if (!encodeTableStd) {
        encodeTableStd = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/".split("");
        encodeTableUrl = encodeTableStd.slice(0, -2).concat("-", "_");
      }
      return encoding == "url" ? (
        // biome-ignore lint/style/noNonNullAssertion: TS fails to narrow down
        encodeTableUrl
      ) : encodeTableStd;
    }
    __name(getEncodeTable, "getEncodeTable");
    function getDecodeTable() {
      if (!decodeTable) {
        decodeTable = [];
        const encodeTable = getEncodeTable("std");
        for (let i = 0; i < encodeTable.length; i++)
          decodeTable[encodeTable[i].charCodeAt(0)] = i;
        decodeTable["-".charCodeAt(0)] = encodeTable.indexOf("+");
        decodeTable["_".charCodeAt(0)] = encodeTable.indexOf("/");
      }
      return decodeTable;
    }
    __name(getDecodeTable, "getDecodeTable");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/reflect/names.js
var require_names = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/reflect/names.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.qualifiedName = qualifiedName;
    exports.protoCamelCase = protoCamelCase;
    exports.protoSnakeCase = protoSnakeCase;
    exports.safeObjectProperty = safeObjectProperty;
    function qualifiedName(desc) {
      switch (desc.kind) {
        case "field":
        case "oneof":
        case "rpc":
          return desc.parent.typeName + "." + desc.name;
        case "enum_value": {
          const p = desc.parent.parent ? desc.parent.parent.typeName : desc.parent.file.proto.package;
          return p + (p.length > 0 ? "." : "") + desc.name;
        }
        case "service":
        case "message":
        case "enum":
        case "extension":
          return desc.typeName;
        case "file":
          return desc.proto.name;
      }
    }
    __name(qualifiedName, "qualifiedName");
    function protoCamelCase(snakeCase) {
      let capNext = false;
      const b = [];
      for (let i = 0; i < snakeCase.length; i++) {
        let c = snakeCase.charAt(i);
        switch (c) {
          case "_":
            capNext = true;
            break;
          case "0":
          case "1":
          case "2":
          case "3":
          case "4":
          case "5":
          case "6":
          case "7":
          case "8":
          case "9":
            b.push(c);
            capNext = false;
            break;
          default:
            if (capNext) {
              capNext = false;
              c = c.toUpperCase();
            }
            b.push(c);
            break;
        }
      }
      return b.join("");
    }
    __name(protoCamelCase, "protoCamelCase");
    function protoSnakeCase(lowerCamelCase) {
      return lowerCamelCase.replace(/[A-Z]/g, (letter) => "_" + letter.toLowerCase());
    }
    __name(protoSnakeCase, "protoSnakeCase");
    var reservedObjectProperties = /* @__PURE__ */ new Set([
      // names reserved by JavaScript
      "constructor",
      "toString",
      "toJSON",
      "valueOf"
    ]);
    function safeObjectProperty(name) {
      return reservedObjectProperties.has(name) ? name + "$" : name;
    }
    __name(safeObjectProperty, "safeObjectProperty");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/restore-json-names.js
var require_restore_json_names = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/restore-json-names.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.restoreJsonNames = restoreJsonNames;
    var names_js_1 = require_names();
    var unsafe_js_1 = require_unsafe();
    function restoreJsonNames(message) {
      for (const f of message.field) {
        if (!(0, unsafe_js_1.unsafeIsSetExplicit)(f, "jsonName")) {
          f.jsonName = (0, names_js_1.protoCamelCase)(f.name);
        }
      }
      message.nestedType.forEach(restoreJsonNames);
    }
    __name(restoreJsonNames, "restoreJsonNames");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wire/text-format.js
var require_text_format = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wire/text-format.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.parseTextFormatEnumValue = parseTextFormatEnumValue;
    exports.parseTextFormatScalarValue = parseTextFormatScalarValue;
    var descriptors_js_1 = require_descriptors();
    var proto_int64_js_1 = require_proto_int64();
    function parseTextFormatEnumValue(descEnum, value) {
      const enumValue = descEnum.values.find((v) => v.name === value);
      if (!enumValue) {
        throw new Error(`cannot parse ${descEnum} default value: ${value}`);
      }
      return enumValue.number;
    }
    __name(parseTextFormatEnumValue, "parseTextFormatEnumValue");
    function parseTextFormatScalarValue(type, value) {
      switch (type) {
        case descriptors_js_1.ScalarType.STRING:
          return value;
        case descriptors_js_1.ScalarType.BYTES: {
          const u = unescapeBytesDefaultValue(value);
          if (u === false) {
            throw new Error(`cannot parse ${descriptors_js_1.ScalarType[type]} default value: ${value}`);
          }
          return u;
        }
        case descriptors_js_1.ScalarType.INT64:
        case descriptors_js_1.ScalarType.SFIXED64:
        case descriptors_js_1.ScalarType.SINT64:
          return proto_int64_js_1.protoInt64.parse(value);
        case descriptors_js_1.ScalarType.UINT64:
        case descriptors_js_1.ScalarType.FIXED64:
          return proto_int64_js_1.protoInt64.uParse(value);
        case descriptors_js_1.ScalarType.DOUBLE:
        case descriptors_js_1.ScalarType.FLOAT:
          switch (value) {
            case "inf":
              return Number.POSITIVE_INFINITY;
            case "-inf":
              return Number.NEGATIVE_INFINITY;
            case "nan":
              return Number.NaN;
            default:
              return parseFloat(value);
          }
        case descriptors_js_1.ScalarType.BOOL:
          return value === "true";
        case descriptors_js_1.ScalarType.INT32:
        case descriptors_js_1.ScalarType.UINT32:
        case descriptors_js_1.ScalarType.SINT32:
        case descriptors_js_1.ScalarType.FIXED32:
        case descriptors_js_1.ScalarType.SFIXED32:
          return parseInt(value, 10);
      }
    }
    __name(parseTextFormatScalarValue, "parseTextFormatScalarValue");
    function unescapeBytesDefaultValue(str) {
      const b = [];
      const input = {
        tail: str,
        c: "",
        next() {
          if (this.tail.length == 0) {
            return false;
          }
          this.c = this.tail[0];
          this.tail = this.tail.substring(1);
          return true;
        },
        take(n) {
          if (this.tail.length >= n) {
            const r = this.tail.substring(0, n);
            this.tail = this.tail.substring(n);
            return r;
          }
          return false;
        }
      };
      while (input.next()) {
        switch (input.c) {
          case "\\":
            if (input.next()) {
              switch (input.c) {
                case "\\":
                  b.push(input.c.charCodeAt(0));
                  break;
                case "b":
                  b.push(8);
                  break;
                case "f":
                  b.push(12);
                  break;
                case "n":
                  b.push(10);
                  break;
                case "r":
                  b.push(13);
                  break;
                case "t":
                  b.push(9);
                  break;
                case "v":
                  b.push(11);
                  break;
                case "0":
                case "1":
                case "2":
                case "3":
                case "4":
                case "5":
                case "6":
                case "7": {
                  const s = input.c;
                  const t = input.take(2);
                  if (t === false) {
                    return false;
                  }
                  const n = parseInt(s + t, 8);
                  if (Number.isNaN(n)) {
                    return false;
                  }
                  b.push(n);
                  break;
                }
                case "x": {
                  const s = input.c;
                  const t = input.take(2);
                  if (t === false) {
                    return false;
                  }
                  const n = parseInt(s + t, 16);
                  if (Number.isNaN(n)) {
                    return false;
                  }
                  b.push(n);
                  break;
                }
                case "u": {
                  const s = input.c;
                  const t = input.take(4);
                  if (t === false) {
                    return false;
                  }
                  const n = parseInt(s + t, 16);
                  if (Number.isNaN(n)) {
                    return false;
                  }
                  const chunk = new Uint8Array(4);
                  const view = new DataView(chunk.buffer);
                  view.setInt32(0, n, true);
                  b.push(chunk[0], chunk[1], chunk[2], chunk[3]);
                  break;
                }
                case "U": {
                  const s = input.c;
                  const t = input.take(8);
                  if (t === false) {
                    return false;
                  }
                  const tc = proto_int64_js_1.protoInt64.uEnc(s + t);
                  const chunk = new Uint8Array(8);
                  const view = new DataView(chunk.buffer);
                  view.setInt32(0, tc.lo, true);
                  view.setInt32(4, tc.hi, true);
                  b.push(chunk[0], chunk[1], chunk[2], chunk[3], chunk[4], chunk[5], chunk[6], chunk[7]);
                  break;
                }
              }
            }
            break;
          default:
            b.push(input.c.charCodeAt(0));
        }
      }
      return new Uint8Array(b);
    }
    __name(unescapeBytesDefaultValue, "unescapeBytesDefaultValue");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/reflect/nested-types.js
var require_nested_types = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/reflect/nested-types.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.nestedTypes = nestedTypes;
    exports.usedTypes = usedTypes;
    exports.parentTypes = parentTypes;
    function* nestedTypes(desc) {
      switch (desc.kind) {
        case "file":
          for (const message of desc.messages) {
            yield message;
            yield* nestedTypes(message);
          }
          yield* desc.enums;
          yield* desc.services;
          yield* desc.extensions;
          break;
        case "message":
          for (const message of desc.nestedMessages) {
            yield message;
            yield* nestedTypes(message);
          }
          yield* desc.nestedEnums;
          yield* desc.nestedExtensions;
          break;
      }
    }
    __name(nestedTypes, "nestedTypes");
    function usedTypes(descMessage) {
      return usedTypesInternal(descMessage, /* @__PURE__ */ new Set());
    }
    __name(usedTypes, "usedTypes");
    function* usedTypesInternal(descMessage, seen) {
      var _a, _b;
      for (const field of descMessage.fields) {
        const ref = (_b = (_a = field.enum) !== null && _a !== void 0 ? _a : field.message) !== null && _b !== void 0 ? _b : void 0;
        if (!ref || seen.has(ref.typeName)) {
          continue;
        }
        seen.add(ref.typeName);
        yield ref;
        if (ref.kind == "message") {
          yield* usedTypesInternal(ref, seen);
        }
      }
    }
    __name(usedTypesInternal, "usedTypesInternal");
    function parentTypes(desc) {
      const parents = [];
      while (desc.kind !== "file") {
        const p = parent(desc);
        desc = p;
        parents.push(p);
      }
      return parents;
    }
    __name(parentTypes, "parentTypes");
    function parent(desc) {
      var _a;
      switch (desc.kind) {
        case "enum_value":
        case "field":
        case "oneof":
        case "rpc":
          return desc.parent;
        case "service":
          return desc.file;
        case "extension":
        case "enum":
        case "message":
          return (_a = desc.parent) !== null && _a !== void 0 ? _a : desc.file;
      }
    }
    __name(parent, "parent");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/registry.js
var require_registry = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/registry.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.maximumEdition = exports.minimumEdition = void 0;
    exports.createRegistry = createRegistry;
    exports.createMutableRegistry = createMutableRegistry;
    exports.createFileRegistry = createFileRegistry;
    var descriptors_js_1 = require_descriptors();
    var text_format_js_1 = require_text_format();
    var nested_types_js_1 = require_nested_types();
    var unsafe_js_1 = require_unsafe();
    var names_js_1 = require_names();
    function createRegistry(...input) {
      return initBaseRegistry(input);
    }
    __name(createRegistry, "createRegistry");
    function createMutableRegistry(...input) {
      const reg = initBaseRegistry(input);
      return Object.assign(Object.assign({}, reg), { remove(desc) {
        var _a;
        if (desc.kind == "extension") {
          (_a = reg.extendees.get(desc.extendee.typeName)) === null || _a === void 0 ? void 0 : _a.delete(desc.number);
        }
        reg.types.delete(desc.typeName);
      } });
    }
    __name(createMutableRegistry, "createMutableRegistry");
    function createFileRegistry(...args) {
      const registry = createBaseRegistry();
      if (!args.length) {
        return registry;
      }
      if ("$typeName" in args[0] && args[0].$typeName == "google.protobuf.FileDescriptorSet") {
        for (const file of args[0].file) {
          addFile(file, registry);
        }
        return registry;
      }
      if ("$typeName" in args[0]) {
        let recurseDeps = function(file) {
          const deps = [];
          for (const protoFileName of file.dependency) {
            if (registry.getFile(protoFileName) != void 0) {
              continue;
            }
            if (seen.has(protoFileName)) {
              continue;
            }
            const dep = resolve(protoFileName);
            if (!dep) {
              throw new Error(`Unable to resolve ${protoFileName}, imported by ${file.name}`);
            }
            if ("kind" in dep) {
              registry.addFile(dep, false, true);
            } else {
              seen.add(dep.name);
              deps.push(dep);
            }
          }
          return deps.concat(...deps.map(recurseDeps));
        };
        __name(recurseDeps, "recurseDeps");
        const input = args[0];
        const resolve = args[1];
        const seen = /* @__PURE__ */ new Set();
        for (const file of [input, ...recurseDeps(input)].reverse()) {
          addFile(file, registry);
        }
      } else {
        for (const fileReg of args) {
          for (const file of fileReg.files) {
            registry.addFile(file);
          }
        }
      }
      return registry;
    }
    __name(createFileRegistry, "createFileRegistry");
    function createBaseRegistry() {
      const types = /* @__PURE__ */ new Map();
      const extendees = /* @__PURE__ */ new Map();
      const files = /* @__PURE__ */ new Map();
      return {
        kind: "registry",
        types,
        extendees,
        [Symbol.iterator]() {
          return types.values();
        },
        get files() {
          return files.values();
        },
        addFile(file, skipTypes, withDeps) {
          files.set(file.proto.name, file);
          if (!skipTypes) {
            for (const type of (0, nested_types_js_1.nestedTypes)(file)) {
              this.add(type);
            }
          }
          if (withDeps) {
            for (const f of file.dependencies) {
              this.addFile(f, skipTypes, withDeps);
            }
          }
        },
        add(desc) {
          if (desc.kind == "extension") {
            let numberToExt = extendees.get(desc.extendee.typeName);
            if (!numberToExt) {
              extendees.set(
                desc.extendee.typeName,
                // biome-ignore lint/suspicious/noAssignInExpressions: no
                numberToExt = /* @__PURE__ */ new Map()
              );
            }
            numberToExt.set(desc.number, desc);
          }
          types.set(desc.typeName, desc);
        },
        get(typeName) {
          return types.get(typeName);
        },
        getFile(fileName) {
          return files.get(fileName);
        },
        getMessage(typeName) {
          const t = types.get(typeName);
          return (t === null || t === void 0 ? void 0 : t.kind) == "message" ? t : void 0;
        },
        getEnum(typeName) {
          const t = types.get(typeName);
          return (t === null || t === void 0 ? void 0 : t.kind) == "enum" ? t : void 0;
        },
        getExtension(typeName) {
          const t = types.get(typeName);
          return (t === null || t === void 0 ? void 0 : t.kind) == "extension" ? t : void 0;
        },
        getExtensionFor(extendee, no) {
          var _a;
          return (_a = extendees.get(extendee.typeName)) === null || _a === void 0 ? void 0 : _a.get(no);
        },
        getService(typeName) {
          const t = types.get(typeName);
          return (t === null || t === void 0 ? void 0 : t.kind) == "service" ? t : void 0;
        }
      };
    }
    __name(createBaseRegistry, "createBaseRegistry");
    function initBaseRegistry(inputs) {
      const registry = createBaseRegistry();
      for (const input of inputs) {
        switch (input.kind) {
          case "registry":
            for (const n of input) {
              registry.add(n);
            }
            break;
          case "file":
            registry.addFile(input);
            break;
          default:
            registry.add(input);
            break;
        }
      }
      return registry;
    }
    __name(initBaseRegistry, "initBaseRegistry");
    var EDITION_PROTO2 = 998;
    var EDITION_PROTO3 = 999;
    var EDITION_UNSTABLE = 9999;
    var TYPE_STRING = 9;
    var TYPE_GROUP = 10;
    var TYPE_MESSAGE = 11;
    var TYPE_BYTES = 12;
    var TYPE_ENUM = 14;
    var LABEL_REPEATED = 3;
    var LABEL_REQUIRED = 2;
    var JS_STRING = 1;
    var IDEMPOTENCY_UNKNOWN = 0;
    var EXPLICIT = 1;
    var IMPLICIT = 2;
    var LEGACY_REQUIRED = 3;
    var PACKED = 1;
    var DELIMITED = 2;
    var OPEN = 1;
    var VERIFY = 2;
    exports.minimumEdition = 998, exports.maximumEdition = 1001;
    var featureDefaults = {
      // EDITION_PROTO2
      998: {
        fieldPresence: 1,
        // EXPLICIT,
        enumType: 2,
        // CLOSED,
        repeatedFieldEncoding: 2,
        // EXPANDED,
        utf8Validation: 3,
        // NONE,
        messageEncoding: 1,
        // LENGTH_PREFIXED,
        jsonFormat: 2,
        // LEGACY_BEST_EFFORT,
        enforceNamingStyle: 2,
        // STYLE_LEGACY,
        defaultSymbolVisibility: 1
        // EXPORT_ALL,
      },
      // EDITION_PROTO3
      999: {
        fieldPresence: 2,
        // IMPLICIT,
        enumType: 1,
        // OPEN,
        repeatedFieldEncoding: 1,
        // PACKED,
        utf8Validation: 2,
        // VERIFY,
        messageEncoding: 1,
        // LENGTH_PREFIXED,
        jsonFormat: 1,
        // ALLOW,
        enforceNamingStyle: 2,
        // STYLE_LEGACY,
        defaultSymbolVisibility: 1
        // EXPORT_ALL,
      },
      // EDITION_2023
      1e3: {
        fieldPresence: 1,
        // EXPLICIT,
        enumType: 1,
        // OPEN,
        repeatedFieldEncoding: 1,
        // PACKED,
        utf8Validation: 2,
        // VERIFY,
        messageEncoding: 1,
        // LENGTH_PREFIXED,
        jsonFormat: 1,
        // ALLOW,
        enforceNamingStyle: 2,
        // STYLE_LEGACY,
        defaultSymbolVisibility: 1
        // EXPORT_ALL,
      },
      // EDITION_2024
      1001: {
        fieldPresence: 1,
        // EXPLICIT,
        enumType: 1,
        // OPEN,
        repeatedFieldEncoding: 1,
        // PACKED,
        utf8Validation: 2,
        // VERIFY,
        messageEncoding: 1,
        // LENGTH_PREFIXED,
        jsonFormat: 1,
        // ALLOW,
        enforceNamingStyle: 1,
        // STYLE2024,
        defaultSymbolVisibility: 2
        // EXPORT_TOP_LEVEL,
      }
    };
    function addFile(proto2, reg) {
      var _a, _b;
      const file = {
        kind: "file",
        proto: proto2,
        deprecated: (_b = (_a = proto2.options) === null || _a === void 0 ? void 0 : _a.deprecated) !== null && _b !== void 0 ? _b : false,
        edition: getFileEdition(proto2),
        name: proto2.name.replace(/\.proto$/, ""),
        dependencies: findFileDependencies(proto2, reg),
        enums: [],
        messages: [],
        extensions: [],
        services: [],
        toString() {
          return `file ${proto2.name}`;
        }
      };
      const mapEntriesStore = /* @__PURE__ */ new Map();
      const mapEntries = {
        get(typeName) {
          return mapEntriesStore.get(typeName);
        },
        add(desc) {
          var _a2;
          assert(((_a2 = desc.proto.options) === null || _a2 === void 0 ? void 0 : _a2.mapEntry) === true);
          mapEntriesStore.set(desc.typeName, desc);
        }
      };
      for (const enumProto of proto2.enumType) {
        addEnum(enumProto, file, void 0, reg);
      }
      for (const messageProto of proto2.messageType) {
        addMessage(messageProto, file, void 0, reg, mapEntries);
      }
      for (const serviceProto of proto2.service) {
        addService(serviceProto, file, reg);
      }
      addExtensions(file, reg);
      for (const mapEntry of mapEntriesStore.values()) {
        addFields(mapEntry, reg, mapEntries);
      }
      for (const message of file.messages) {
        addFields(message, reg, mapEntries);
        addExtensions(message, reg);
      }
      reg.addFile(file, true);
    }
    __name(addFile, "addFile");
    function addExtensions(desc, reg) {
      switch (desc.kind) {
        case "file":
          for (const proto2 of desc.proto.extension) {
            const ext = newField(proto2, desc, reg);
            desc.extensions.push(ext);
            reg.add(ext);
          }
          break;
        case "message":
          for (const proto2 of desc.proto.extension) {
            const ext = newField(proto2, desc, reg);
            desc.nestedExtensions.push(ext);
            reg.add(ext);
          }
          for (const message of desc.nestedMessages) {
            addExtensions(message, reg);
          }
          break;
      }
    }
    __name(addExtensions, "addExtensions");
    function addFields(message, reg, mapEntries) {
      const allOneofs = message.proto.oneofDecl.map((proto2) => newOneof(proto2, message));
      const oneofsSeen = /* @__PURE__ */ new Set();
      for (const proto2 of message.proto.field) {
        const oneof = findOneof(proto2, allOneofs);
        const field = newField(proto2, message, reg, oneof, mapEntries);
        message.fields.push(field);
        message.field[field.localName] = field;
        if (oneof === void 0) {
          message.members.push(field);
        } else {
          oneof.fields.push(field);
          if (!oneofsSeen.has(oneof)) {
            oneofsSeen.add(oneof);
            message.members.push(oneof);
          }
        }
      }
      for (const oneof of allOneofs.filter((o) => oneofsSeen.has(o))) {
        message.oneofs.push(oneof);
      }
      for (const child of message.nestedMessages) {
        addFields(child, reg, mapEntries);
      }
    }
    __name(addFields, "addFields");
    function addEnum(proto2, file, parent, reg) {
      var _a, _b, _c, _d, _e;
      const sharedPrefix = findEnumSharedPrefix(proto2.name, proto2.value);
      const desc = {
        kind: "enum",
        proto: proto2,
        deprecated: (_b = (_a = proto2.options) === null || _a === void 0 ? void 0 : _a.deprecated) !== null && _b !== void 0 ? _b : false,
        file,
        parent,
        open: true,
        name: proto2.name,
        typeName: makeTypeName(proto2, parent, file),
        value: {},
        values: [],
        sharedPrefix,
        toString() {
          return `enum ${this.typeName}`;
        }
      };
      desc.open = isEnumOpen(desc);
      reg.add(desc);
      for (const p of proto2.value) {
        const name = p.name;
        desc.values.push(
          // biome-ignore lint/suspicious/noAssignInExpressions: no
          desc.value[p.number] = {
            kind: "enum_value",
            proto: p,
            deprecated: (_d = (_c = p.options) === null || _c === void 0 ? void 0 : _c.deprecated) !== null && _d !== void 0 ? _d : false,
            parent: desc,
            name,
            localName: (0, names_js_1.safeObjectProperty)(sharedPrefix == void 0 ? name : name.substring(sharedPrefix.length)),
            number: p.number,
            toString() {
              return `enum value ${desc.typeName}.${name}`;
            }
          }
        );
      }
      ((_e = parent === null || parent === void 0 ? void 0 : parent.nestedEnums) !== null && _e !== void 0 ? _e : file.enums).push(desc);
    }
    __name(addEnum, "addEnum");
    function addMessage(proto2, file, parent, reg, mapEntries) {
      var _a, _b, _c, _d;
      const desc = {
        kind: "message",
        proto: proto2,
        deprecated: (_b = (_a = proto2.options) === null || _a === void 0 ? void 0 : _a.deprecated) !== null && _b !== void 0 ? _b : false,
        file,
        parent,
        name: proto2.name,
        typeName: makeTypeName(proto2, parent, file),
        fields: [],
        field: {},
        oneofs: [],
        members: [],
        nestedEnums: [],
        nestedMessages: [],
        nestedExtensions: [],
        toString() {
          return `message ${this.typeName}`;
        }
      };
      if (((_c = proto2.options) === null || _c === void 0 ? void 0 : _c.mapEntry) === true) {
        mapEntries.add(desc);
      } else {
        ((_d = parent === null || parent === void 0 ? void 0 : parent.nestedMessages) !== null && _d !== void 0 ? _d : file.messages).push(desc);
        reg.add(desc);
      }
      for (const enumProto of proto2.enumType) {
        addEnum(enumProto, file, desc, reg);
      }
      for (const messageProto of proto2.nestedType) {
        addMessage(messageProto, file, desc, reg, mapEntries);
      }
    }
    __name(addMessage, "addMessage");
    function addService(proto2, file, reg) {
      var _a, _b;
      const desc = {
        kind: "service",
        proto: proto2,
        deprecated: (_b = (_a = proto2.options) === null || _a === void 0 ? void 0 : _a.deprecated) !== null && _b !== void 0 ? _b : false,
        file,
        name: proto2.name,
        typeName: makeTypeName(proto2, void 0, file),
        methods: [],
        method: {},
        toString() {
          return `service ${this.typeName}`;
        }
      };
      file.services.push(desc);
      reg.add(desc);
      for (const methodProto of proto2.method) {
        const method = newMethod(methodProto, desc, reg);
        desc.methods.push(method);
        desc.method[method.localName] = method;
      }
    }
    __name(addService, "addService");
    function newMethod(proto2, parent, reg) {
      var _a, _b, _c, _d;
      let methodKind;
      if (proto2.clientStreaming && proto2.serverStreaming) {
        methodKind = "bidi_streaming";
      } else if (proto2.clientStreaming) {
        methodKind = "client_streaming";
      } else if (proto2.serverStreaming) {
        methodKind = "server_streaming";
      } else {
        methodKind = "unary";
      }
      const input = reg.getMessage(trimLeadingDot(proto2.inputType));
      const output = reg.getMessage(trimLeadingDot(proto2.outputType));
      assert(input, `invalid MethodDescriptorProto: input_type ${proto2.inputType} not found`);
      assert(output, `invalid MethodDescriptorProto: output_type ${proto2.inputType} not found`);
      const name = proto2.name;
      return {
        kind: "rpc",
        proto: proto2,
        deprecated: (_b = (_a = proto2.options) === null || _a === void 0 ? void 0 : _a.deprecated) !== null && _b !== void 0 ? _b : false,
        parent,
        name,
        localName: (0, names_js_1.safeObjectProperty)(name.length ? (0, names_js_1.safeObjectProperty)(name[0].toLowerCase() + name.substring(1)) : name),
        methodKind,
        input,
        output,
        idempotency: (_d = (_c = proto2.options) === null || _c === void 0 ? void 0 : _c.idempotencyLevel) !== null && _d !== void 0 ? _d : IDEMPOTENCY_UNKNOWN,
        toString() {
          return `rpc ${parent.typeName}.${name}`;
        }
      };
    }
    __name(newMethod, "newMethod");
    function newOneof(proto2, parent) {
      return {
        kind: "oneof",
        proto: proto2,
        deprecated: false,
        parent,
        fields: [],
        name: proto2.name,
        localName: (0, names_js_1.safeObjectProperty)((0, names_js_1.protoCamelCase)(proto2.name)),
        toString() {
          return `oneof ${parent.typeName}.${this.name}`;
        }
      };
    }
    __name(newOneof, "newOneof");
    function newField(proto2, parentOrFile, reg, oneof, mapEntries) {
      var _a, _b, _c;
      const isExtension = mapEntries === void 0;
      const field = {
        kind: "field",
        proto: proto2,
        deprecated: (_b = (_a = proto2.options) === null || _a === void 0 ? void 0 : _a.deprecated) !== null && _b !== void 0 ? _b : false,
        name: proto2.name,
        number: proto2.number,
        scalar: void 0,
        message: void 0,
        enum: void 0,
        presence: getFieldPresence(proto2, oneof, isExtension, parentOrFile),
        utf8Validation: isUtf8Validated(proto2, parentOrFile),
        listKind: void 0,
        mapKind: void 0,
        mapKey: void 0,
        delimitedEncoding: void 0,
        packed: void 0,
        longAsString: false,
        getDefaultValue: void 0
      };
      if (isExtension) {
        const file = parentOrFile.kind == "file" ? parentOrFile : parentOrFile.file;
        const parent = parentOrFile.kind == "file" ? void 0 : parentOrFile;
        const typeName = makeTypeName(proto2, parent, file);
        field.kind = "extension";
        field.file = file;
        field.parent = parent;
        field.oneof = void 0;
        field.typeName = typeName;
        field.jsonName = `[${typeName}]`;
        field.toString = () => `extension ${typeName}`;
        const extendee = reg.getMessage(trimLeadingDot(proto2.extendee));
        assert(extendee, `invalid FieldDescriptorProto: extendee ${proto2.extendee} not found`);
        field.extendee = extendee;
      } else {
        const parent = parentOrFile;
        assert(parent.kind == "message");
        field.parent = parent;
        field.oneof = oneof;
        field.localName = oneof ? (0, names_js_1.protoCamelCase)(proto2.name) : (0, names_js_1.safeObjectProperty)((0, names_js_1.protoCamelCase)(proto2.name));
        field.jsonName = proto2.jsonName;
        field.toString = () => `field ${parent.typeName}.${proto2.name}`;
      }
      const label = proto2.label;
      const type = proto2.type;
      const jstype = (_c = proto2.options) === null || _c === void 0 ? void 0 : _c.jstype;
      if (label === LABEL_REPEATED) {
        const mapEntry = type == TYPE_MESSAGE ? mapEntries === null || mapEntries === void 0 ? void 0 : mapEntries.get(trimLeadingDot(proto2.typeName)) : void 0;
        if (mapEntry) {
          field.fieldKind = "map";
          const { key, value } = findMapEntryFields(mapEntry);
          field.mapKey = key.scalar;
          field.mapKind = value.fieldKind;
          field.message = value.message;
          field.delimitedEncoding = false;
          field.enum = value.enum;
          field.scalar = value.scalar;
          return field;
        }
        field.fieldKind = "list";
        switch (type) {
          case TYPE_MESSAGE:
          case TYPE_GROUP:
            field.listKind = "message";
            field.message = reg.getMessage(trimLeadingDot(proto2.typeName));
            assert(field.message);
            field.delimitedEncoding = isDelimitedEncoding(proto2, parentOrFile);
            break;
          case TYPE_ENUM:
            field.listKind = "enum";
            field.enum = reg.getEnum(trimLeadingDot(proto2.typeName));
            assert(field.enum);
            break;
          default:
            field.listKind = "scalar";
            field.scalar = type;
            field.longAsString = jstype == JS_STRING;
            break;
        }
        field.packed = isPackedField(proto2, parentOrFile);
        return field;
      }
      switch (type) {
        case TYPE_MESSAGE:
        case TYPE_GROUP:
          field.fieldKind = "message";
          field.message = reg.getMessage(trimLeadingDot(proto2.typeName));
          assert(field.message, `invalid FieldDescriptorProto: type_name ${proto2.typeName} not found`);
          field.delimitedEncoding = isDelimitedEncoding(proto2, parentOrFile);
          field.getDefaultValue = () => void 0;
          break;
        case TYPE_ENUM: {
          const enumeration = reg.getEnum(trimLeadingDot(proto2.typeName));
          assert(enumeration !== void 0, `invalid FieldDescriptorProto: type_name ${proto2.typeName} not found`);
          field.fieldKind = "enum";
          field.enum = reg.getEnum(trimLeadingDot(proto2.typeName));
          field.getDefaultValue = () => {
            return (0, unsafe_js_1.unsafeIsSetExplicit)(proto2, "defaultValue") ? (0, text_format_js_1.parseTextFormatEnumValue)(enumeration, proto2.defaultValue) : void 0;
          };
          break;
        }
        default: {
          field.fieldKind = "scalar";
          field.scalar = type;
          field.longAsString = jstype == JS_STRING;
          field.getDefaultValue = () => {
            return (0, unsafe_js_1.unsafeIsSetExplicit)(proto2, "defaultValue") ? (0, text_format_js_1.parseTextFormatScalarValue)(type, proto2.defaultValue) : void 0;
          };
          break;
        }
      }
      return field;
    }
    __name(newField, "newField");
    function getFileEdition(proto2) {
      switch (proto2.syntax) {
        case "":
        case "proto2":
          return EDITION_PROTO2;
        case "proto3":
          return EDITION_PROTO3;
        case "editions":
          if (proto2.edition === EDITION_UNSTABLE) {
            return exports.maximumEdition;
          }
          if (proto2.edition in featureDefaults) {
            return proto2.edition;
          }
          throw new Error(`${proto2.name}: unsupported edition`);
        default:
          throw new Error(`${proto2.name}: unsupported syntax "${proto2.syntax}"`);
      }
    }
    __name(getFileEdition, "getFileEdition");
    function findFileDependencies(proto2, reg) {
      return proto2.dependency.map((wantName) => {
        const dep = reg.getFile(wantName);
        if (!dep) {
          throw new Error(`Cannot find ${wantName}, imported by ${proto2.name}`);
        }
        return dep;
      });
    }
    __name(findFileDependencies, "findFileDependencies");
    function findEnumSharedPrefix(enumName, values) {
      const prefix = camelToSnakeCase(enumName) + "_";
      for (const value of values) {
        if (!value.name.toLowerCase().startsWith(prefix)) {
          return void 0;
        }
        const shortName = value.name.substring(prefix.length);
        if (shortName.length == 0) {
          return void 0;
        }
        if (/^\d/.test(shortName)) {
          return void 0;
        }
      }
      return prefix;
    }
    __name(findEnumSharedPrefix, "findEnumSharedPrefix");
    function camelToSnakeCase(camel) {
      return (camel.substring(0, 1) + camel.substring(1).replace(/[A-Z]/g, (c) => "_" + c)).toLowerCase();
    }
    __name(camelToSnakeCase, "camelToSnakeCase");
    function makeTypeName(proto2, parent, file) {
      let typeName;
      if (parent) {
        typeName = `${parent.typeName}.${proto2.name}`;
      } else if (file.proto.package.length > 0) {
        typeName = `${file.proto.package}.${proto2.name}`;
      } else {
        typeName = `${proto2.name}`;
      }
      return typeName;
    }
    __name(makeTypeName, "makeTypeName");
    function trimLeadingDot(typeName) {
      return typeName.startsWith(".") ? typeName.substring(1) : typeName;
    }
    __name(trimLeadingDot, "trimLeadingDot");
    function findOneof(proto2, allOneofs) {
      if (!(0, unsafe_js_1.unsafeIsSetExplicit)(proto2, "oneofIndex")) {
        return void 0;
      }
      if (proto2.proto3Optional) {
        return void 0;
      }
      const oneof = allOneofs[proto2.oneofIndex];
      assert(oneof, `invalid FieldDescriptorProto: oneof #${proto2.oneofIndex} for field #${proto2.number} not found`);
      return oneof;
    }
    __name(findOneof, "findOneof");
    function getFieldPresence(proto2, oneof, isExtension, parent) {
      if (proto2.label == LABEL_REQUIRED) {
        return LEGACY_REQUIRED;
      }
      if (proto2.label == LABEL_REPEATED) {
        return IMPLICIT;
      }
      if (!!oneof || proto2.proto3Optional) {
        return EXPLICIT;
      }
      if (isExtension) {
        return EXPLICIT;
      }
      const resolved = resolveFeature("fieldPresence", { proto: proto2, parent });
      if (resolved == IMPLICIT && (proto2.type == TYPE_MESSAGE || proto2.type == TYPE_GROUP)) {
        return EXPLICIT;
      }
      return resolved;
    }
    __name(getFieldPresence, "getFieldPresence");
    function isPackedField(proto2, parent) {
      if (proto2.label != LABEL_REPEATED) {
        return false;
      }
      switch (proto2.type) {
        case TYPE_STRING:
        case TYPE_BYTES:
        case TYPE_GROUP:
        case TYPE_MESSAGE:
          return false;
      }
      const o = proto2.options;
      if (o && (0, unsafe_js_1.unsafeIsSetExplicit)(o, "packed")) {
        return o.packed;
      }
      return PACKED == resolveFeature("repeatedFieldEncoding", {
        proto: proto2,
        parent
      });
    }
    __name(isPackedField, "isPackedField");
    function findMapEntryFields(mapEntry) {
      const key = mapEntry.fields.find((f) => f.number === 1);
      const value = mapEntry.fields.find((f) => f.number === 2);
      assert(key && key.fieldKind == "scalar" && key.scalar != descriptors_js_1.ScalarType.BYTES && key.scalar != descriptors_js_1.ScalarType.FLOAT && key.scalar != descriptors_js_1.ScalarType.DOUBLE && value && value.fieldKind != "list" && value.fieldKind != "map");
      return { key, value };
    }
    __name(findMapEntryFields, "findMapEntryFields");
    function isEnumOpen(desc) {
      var _a;
      return OPEN == resolveFeature("enumType", {
        proto: desc.proto,
        parent: (_a = desc.parent) !== null && _a !== void 0 ? _a : desc.file
      });
    }
    __name(isEnumOpen, "isEnumOpen");
    function isDelimitedEncoding(proto2, parent) {
      if (proto2.type == TYPE_GROUP) {
        return true;
      }
      return DELIMITED == resolveFeature("messageEncoding", {
        proto: proto2,
        parent
      });
    }
    __name(isDelimitedEncoding, "isDelimitedEncoding");
    function isUtf8Validated(proto2, parent) {
      return VERIFY == resolveFeature("utf8Validation", {
        proto: proto2,
        parent
      });
    }
    __name(isUtf8Validated, "isUtf8Validated");
    function resolveFeature(name, ref) {
      var _a, _b;
      const featureSet = (_a = ref.proto.options) === null || _a === void 0 ? void 0 : _a.features;
      if (featureSet) {
        const val = featureSet[name];
        if (val != 0) {
          return val;
        }
      }
      if ("kind" in ref) {
        if (ref.kind == "message") {
          return resolveFeature(name, (_b = ref.parent) !== null && _b !== void 0 ? _b : ref.file);
        }
        const editionDefaults = featureDefaults[ref.edition];
        if (!editionDefaults) {
          throw new Error(`feature default for edition ${ref.edition} not found`);
        }
        return editionDefaults[name];
      }
      return resolveFeature(name, ref.parent);
    }
    __name(resolveFeature, "resolveFeature");
    function assert(condition, msg) {
      if (!condition) {
        throw new Error(msg);
      }
    }
    __name(assert, "assert");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/boot.js
var require_boot = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/boot.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.boot = boot;
    exports.bootFileDescriptorProto = bootFileDescriptorProto;
    var restore_json_names_js_1 = require_restore_json_names();
    var registry_js_1 = require_registry();
    function boot(boot2) {
      const root = bootFileDescriptorProto(boot2);
      root.messageType.forEach(restore_json_names_js_1.restoreJsonNames);
      const reg = (0, registry_js_1.createFileRegistry)(root, () => void 0);
      return reg.getFile(root.name);
    }
    __name(boot, "boot");
    function bootFileDescriptorProto(init) {
      const proto2 = /* @__PURE__ */ Object.create({
        syntax: "",
        edition: 0
      });
      return Object.assign(proto2, Object.assign(Object.assign({ $typeName: "google.protobuf.FileDescriptorProto", dependency: [], publicDependency: [], weakDependency: [], optionDependency: [], service: [], extension: [] }, init), { messageType: init.messageType.map(bootDescriptorProto), enumType: init.enumType.map(bootEnumDescriptorProto) }));
    }
    __name(bootFileDescriptorProto, "bootFileDescriptorProto");
    function bootDescriptorProto(init) {
      var _a, _b, _c, _d, _e, _f, _g, _h;
      const proto2 = /* @__PURE__ */ Object.create({
        visibility: 0
      });
      return Object.assign(proto2, {
        $typeName: "google.protobuf.DescriptorProto",
        name: init.name,
        field: (_b = (_a = init.field) === null || _a === void 0 ? void 0 : _a.map(bootFieldDescriptorProto)) !== null && _b !== void 0 ? _b : [],
        extension: [],
        nestedType: (_d = (_c = init.nestedType) === null || _c === void 0 ? void 0 : _c.map(bootDescriptorProto)) !== null && _d !== void 0 ? _d : [],
        enumType: (_f = (_e = init.enumType) === null || _e === void 0 ? void 0 : _e.map(bootEnumDescriptorProto)) !== null && _f !== void 0 ? _f : [],
        extensionRange: (_h = (_g = init.extensionRange) === null || _g === void 0 ? void 0 : _g.map((e) => Object.assign({ $typeName: "google.protobuf.DescriptorProto.ExtensionRange" }, e))) !== null && _h !== void 0 ? _h : [],
        oneofDecl: [],
        reservedRange: [],
        reservedName: []
      });
    }
    __name(bootDescriptorProto, "bootDescriptorProto");
    function bootFieldDescriptorProto(init) {
      const proto2 = /* @__PURE__ */ Object.create({
        label: 1,
        typeName: "",
        extendee: "",
        defaultValue: "",
        oneofIndex: 0,
        jsonName: "",
        proto3Optional: false
      });
      return Object.assign(proto2, Object.assign(Object.assign({ $typeName: "google.protobuf.FieldDescriptorProto" }, init), { options: init.options ? bootFieldOptions(init.options) : void 0 }));
    }
    __name(bootFieldDescriptorProto, "bootFieldDescriptorProto");
    function bootFieldOptions(init) {
      var _a, _b, _c;
      const proto2 = /* @__PURE__ */ Object.create({
        ctype: 0,
        packed: false,
        jstype: 0,
        lazy: false,
        unverifiedLazy: false,
        deprecated: false,
        weak: false,
        debugRedact: false,
        retention: 0
      });
      return Object.assign(proto2, Object.assign(Object.assign({ $typeName: "google.protobuf.FieldOptions" }, init), { targets: (_a = init.targets) !== null && _a !== void 0 ? _a : [], editionDefaults: (_c = (_b = init.editionDefaults) === null || _b === void 0 ? void 0 : _b.map((e) => Object.assign({ $typeName: "google.protobuf.FieldOptions.EditionDefault" }, e))) !== null && _c !== void 0 ? _c : [], uninterpretedOption: [] }));
    }
    __name(bootFieldOptions, "bootFieldOptions");
    function bootEnumDescriptorProto(init) {
      const proto2 = /* @__PURE__ */ Object.create({
        visibility: 0
      });
      return Object.assign(proto2, {
        $typeName: "google.protobuf.EnumDescriptorProto",
        name: init.name,
        reservedName: [],
        reservedRange: [],
        value: init.value.map((e) => Object.assign({ $typeName: "google.protobuf.EnumValueDescriptorProto" }, e))
      });
    }
    __name(bootEnumDescriptorProto, "bootEnumDescriptorProto");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/message.js
var require_message = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/message.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.messageDesc = messageDesc;
    function messageDesc(file, path, ...paths) {
      return paths.reduce((acc, cur) => acc.nestedMessages[cur], file.messages[path]);
    }
    __name(messageDesc, "messageDesc");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/enum.js
var require_enum = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/enum.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.enumDesc = enumDesc;
    exports.tsEnum = tsEnum;
    function enumDesc(file, path, ...paths) {
      if (paths.length == 0) {
        return file.enums[path];
      }
      const e = paths.pop();
      return paths.reduce((acc, cur) => acc.nestedMessages[cur], file.messages[path]).nestedEnums[e];
    }
    __name(enumDesc, "enumDesc");
    function tsEnum(desc) {
      const enumObject = {};
      for (const value of desc.values) {
        enumObject[value.localName] = value.number;
        enumObject[value.number] = value.localName;
      }
      return enumObject;
    }
    __name(tsEnum, "tsEnum");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/descriptor_pb.js
var require_descriptor_pb = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/descriptor_pb.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.FeatureSet_FieldPresence = exports.FeatureSet_VisibilityFeature_DefaultSymbolVisibilitySchema = exports.FeatureSet_VisibilityFeature_DefaultSymbolVisibility = exports.FeatureSet_VisibilityFeatureSchema = exports.FeatureSetSchema = exports.UninterpretedOption_NamePartSchema = exports.UninterpretedOptionSchema = exports.MethodOptions_IdempotencyLevelSchema = exports.MethodOptions_IdempotencyLevel = exports.MethodOptionsSchema = exports.ServiceOptionsSchema = exports.EnumValueOptionsSchema = exports.EnumOptionsSchema = exports.OneofOptionsSchema = exports.FieldOptions_OptionTargetTypeSchema = exports.FieldOptions_OptionTargetType = exports.FieldOptions_OptionRetentionSchema = exports.FieldOptions_OptionRetention = exports.FieldOptions_JSTypeSchema = exports.FieldOptions_JSType = exports.FieldOptions_CTypeSchema = exports.FieldOptions_CType = exports.FieldOptions_FeatureSupportSchema = exports.FieldOptions_EditionDefaultSchema = exports.FieldOptionsSchema = exports.MessageOptionsSchema = exports.FileOptions_OptimizeModeSchema = exports.FileOptions_OptimizeMode = exports.FileOptionsSchema = exports.MethodDescriptorProtoSchema = exports.ServiceDescriptorProtoSchema = exports.EnumValueDescriptorProtoSchema = exports.EnumDescriptorProto_EnumReservedRangeSchema = exports.EnumDescriptorProtoSchema = exports.OneofDescriptorProtoSchema = exports.FieldDescriptorProto_LabelSchema = exports.FieldDescriptorProto_Label = exports.FieldDescriptorProto_TypeSchema = exports.FieldDescriptorProto_Type = exports.FieldDescriptorProtoSchema = exports.ExtensionRangeOptions_VerificationStateSchema = exports.ExtensionRangeOptions_VerificationState = exports.ExtensionRangeOptions_DeclarationSchema = exports.ExtensionRangeOptionsSchema = exports.DescriptorProto_ReservedRangeSchema = exports.DescriptorProto_ExtensionRangeSchema = exports.DescriptorProtoSchema = exports.FileDescriptorProtoSchema = exports.FileDescriptorSetSchema = exports.file_google_protobuf_descriptor = void 0;
    exports.SymbolVisibilitySchema = exports.SymbolVisibility = exports.EditionSchema = exports.Edition = exports.GeneratedCodeInfo_Annotation_SemanticSchema = exports.GeneratedCodeInfo_Annotation_Semantic = exports.GeneratedCodeInfo_AnnotationSchema = exports.GeneratedCodeInfoSchema = exports.SourceCodeInfo_LocationSchema = exports.SourceCodeInfoSchema = exports.FeatureSetDefaults_FeatureSetEditionDefaultSchema = exports.FeatureSetDefaultsSchema = exports.FeatureSet_EnforceNamingStyleSchema = exports.FeatureSet_EnforceNamingStyle = exports.FeatureSet_JsonFormatSchema = exports.FeatureSet_JsonFormat = exports.FeatureSet_MessageEncodingSchema = exports.FeatureSet_MessageEncoding = exports.FeatureSet_Utf8ValidationSchema = exports.FeatureSet_Utf8Validation = exports.FeatureSet_RepeatedFieldEncodingSchema = exports.FeatureSet_RepeatedFieldEncoding = exports.FeatureSet_EnumTypeSchema = exports.FeatureSet_EnumType = exports.FeatureSet_FieldPresenceSchema = void 0;
    var boot_js_1 = require_boot();
    var message_js_1 = require_message();
    var enum_js_1 = require_enum();
    exports.file_google_protobuf_descriptor = (0, boot_js_1.boot)({ "name": "google/protobuf/descriptor.proto", "package": "google.protobuf", "messageType": [{ "name": "FileDescriptorSet", "field": [{ "name": "file", "number": 1, "type": 11, "label": 3, "typeName": ".google.protobuf.FileDescriptorProto" }], "extensionRange": [{ "start": 536e6, "end": 536000001 }] }, { "name": "FileDescriptorProto", "field": [{ "name": "name", "number": 1, "type": 9, "label": 1 }, { "name": "package", "number": 2, "type": 9, "label": 1 }, { "name": "dependency", "number": 3, "type": 9, "label": 3 }, { "name": "public_dependency", "number": 10, "type": 5, "label": 3 }, { "name": "weak_dependency", "number": 11, "type": 5, "label": 3 }, { "name": "option_dependency", "number": 15, "type": 9, "label": 3 }, { "name": "message_type", "number": 4, "type": 11, "label": 3, "typeName": ".google.protobuf.DescriptorProto" }, { "name": "enum_type", "number": 5, "type": 11, "label": 3, "typeName": ".google.protobuf.EnumDescriptorProto" }, { "name": "service", "number": 6, "type": 11, "label": 3, "typeName": ".google.protobuf.ServiceDescriptorProto" }, { "name": "extension", "number": 7, "type": 11, "label": 3, "typeName": ".google.protobuf.FieldDescriptorProto" }, { "name": "options", "number": 8, "type": 11, "label": 1, "typeName": ".google.protobuf.FileOptions" }, { "name": "source_code_info", "number": 9, "type": 11, "label": 1, "typeName": ".google.protobuf.SourceCodeInfo" }, { "name": "syntax", "number": 12, "type": 9, "label": 1 }, { "name": "edition", "number": 14, "type": 14, "label": 1, "typeName": ".google.protobuf.Edition" }] }, { "name": "DescriptorProto", "field": [{ "name": "name", "number": 1, "type": 9, "label": 1 }, { "name": "field", "number": 2, "type": 11, "label": 3, "typeName": ".google.protobuf.FieldDescriptorProto" }, { "name": "extension", "number": 6, "type": 11, "label": 3, "typeName": ".google.protobuf.FieldDescriptorProto" }, { "name": "nested_type", "number": 3, "type": 11, "label": 3, "typeName": ".google.protobuf.DescriptorProto" }, { "name": "enum_type", "number": 4, "type": 11, "label": 3, "typeName": ".google.protobuf.EnumDescriptorProto" }, { "name": "extension_range", "number": 5, "type": 11, "label": 3, "typeName": ".google.protobuf.DescriptorProto.ExtensionRange" }, { "name": "oneof_decl", "number": 8, "type": 11, "label": 3, "typeName": ".google.protobuf.OneofDescriptorProto" }, { "name": "options", "number": 7, "type": 11, "label": 1, "typeName": ".google.protobuf.MessageOptions" }, { "name": "reserved_range", "number": 9, "type": 11, "label": 3, "typeName": ".google.protobuf.DescriptorProto.ReservedRange" }, { "name": "reserved_name", "number": 10, "type": 9, "label": 3 }, { "name": "visibility", "number": 11, "type": 14, "label": 1, "typeName": ".google.protobuf.SymbolVisibility" }], "nestedType": [{ "name": "ExtensionRange", "field": [{ "name": "start", "number": 1, "type": 5, "label": 1 }, { "name": "end", "number": 2, "type": 5, "label": 1 }, { "name": "options", "number": 3, "type": 11, "label": 1, "typeName": ".google.protobuf.ExtensionRangeOptions" }] }, { "name": "ReservedRange", "field": [{ "name": "start", "number": 1, "type": 5, "label": 1 }, { "name": "end", "number": 2, "type": 5, "label": 1 }] }] }, { "name": "ExtensionRangeOptions", "field": [{ "name": "uninterpreted_option", "number": 999, "type": 11, "label": 3, "typeName": ".google.protobuf.UninterpretedOption" }, { "name": "declaration", "number": 2, "type": 11, "label": 3, "typeName": ".google.protobuf.ExtensionRangeOptions.Declaration", "options": { "retention": 2 } }, { "name": "features", "number": 50, "type": 11, "label": 1, "typeName": ".google.protobuf.FeatureSet" }, { "name": "verification", "number": 3, "type": 14, "label": 1, "typeName": ".google.protobuf.ExtensionRangeOptions.VerificationState", "defaultValue": "UNVERIFIED", "options": { "retention": 2 } }], "nestedType": [{ "name": "Declaration", "field": [{ "name": "number", "number": 1, "type": 5, "label": 1 }, { "name": "full_name", "number": 2, "type": 9, "label": 1 }, { "name": "type", "number": 3, "type": 9, "label": 1 }, { "name": "reserved", "number": 5, "type": 8, "label": 1 }, { "name": "repeated", "number": 6, "type": 8, "label": 1 }] }], "enumType": [{ "name": "VerificationState", "value": [{ "name": "DECLARATION", "number": 0 }, { "name": "UNVERIFIED", "number": 1 }] }], "extensionRange": [{ "start": 1e3, "end": 536870912 }] }, { "name": "FieldDescriptorProto", "field": [{ "name": "name", "number": 1, "type": 9, "label": 1 }, { "name": "number", "number": 3, "type": 5, "label": 1 }, { "name": "label", "number": 4, "type": 14, "label": 1, "typeName": ".google.protobuf.FieldDescriptorProto.Label" }, { "name": "type", "number": 5, "type": 14, "label": 1, "typeName": ".google.protobuf.FieldDescriptorProto.Type" }, { "name": "type_name", "number": 6, "type": 9, "label": 1 }, { "name": "extendee", "number": 2, "type": 9, "label": 1 }, { "name": "default_value", "number": 7, "type": 9, "label": 1 }, { "name": "oneof_index", "number": 9, "type": 5, "label": 1 }, { "name": "json_name", "number": 10, "type": 9, "label": 1 }, { "name": "options", "number": 8, "type": 11, "label": 1, "typeName": ".google.protobuf.FieldOptions" }, { "name": "proto3_optional", "number": 17, "type": 8, "label": 1 }], "enumType": [{ "name": "Type", "value": [{ "name": "TYPE_DOUBLE", "number": 1 }, { "name": "TYPE_FLOAT", "number": 2 }, { "name": "TYPE_INT64", "number": 3 }, { "name": "TYPE_UINT64", "number": 4 }, { "name": "TYPE_INT32", "number": 5 }, { "name": "TYPE_FIXED64", "number": 6 }, { "name": "TYPE_FIXED32", "number": 7 }, { "name": "TYPE_BOOL", "number": 8 }, { "name": "TYPE_STRING", "number": 9 }, { "name": "TYPE_GROUP", "number": 10 }, { "name": "TYPE_MESSAGE", "number": 11 }, { "name": "TYPE_BYTES", "number": 12 }, { "name": "TYPE_UINT32", "number": 13 }, { "name": "TYPE_ENUM", "number": 14 }, { "name": "TYPE_SFIXED32", "number": 15 }, { "name": "TYPE_SFIXED64", "number": 16 }, { "name": "TYPE_SINT32", "number": 17 }, { "name": "TYPE_SINT64", "number": 18 }] }, { "name": "Label", "value": [{ "name": "LABEL_OPTIONAL", "number": 1 }, { "name": "LABEL_REPEATED", "number": 3 }, { "name": "LABEL_REQUIRED", "number": 2 }] }] }, { "name": "OneofDescriptorProto", "field": [{ "name": "name", "number": 1, "type": 9, "label": 1 }, { "name": "options", "number": 2, "type": 11, "label": 1, "typeName": ".google.protobuf.OneofOptions" }] }, { "name": "EnumDescriptorProto", "field": [{ "name": "name", "number": 1, "type": 9, "label": 1 }, { "name": "value", "number": 2, "type": 11, "label": 3, "typeName": ".google.protobuf.EnumValueDescriptorProto" }, { "name": "options", "number": 3, "type": 11, "label": 1, "typeName": ".google.protobuf.EnumOptions" }, { "name": "reserved_range", "number": 4, "type": 11, "label": 3, "typeName": ".google.protobuf.EnumDescriptorProto.EnumReservedRange" }, { "name": "reserved_name", "number": 5, "type": 9, "label": 3 }, { "name": "visibility", "number": 6, "type": 14, "label": 1, "typeName": ".google.protobuf.SymbolVisibility" }], "nestedType": [{ "name": "EnumReservedRange", "field": [{ "name": "start", "number": 1, "type": 5, "label": 1 }, { "name": "end", "number": 2, "type": 5, "label": 1 }] }] }, { "name": "EnumValueDescriptorProto", "field": [{ "name": "name", "number": 1, "type": 9, "label": 1 }, { "name": "number", "number": 2, "type": 5, "label": 1 }, { "name": "options", "number": 3, "type": 11, "label": 1, "typeName": ".google.protobuf.EnumValueOptions" }] }, { "name": "ServiceDescriptorProto", "field": [{ "name": "name", "number": 1, "type": 9, "label": 1 }, { "name": "method", "number": 2, "type": 11, "label": 3, "typeName": ".google.protobuf.MethodDescriptorProto" }, { "name": "options", "number": 3, "type": 11, "label": 1, "typeName": ".google.protobuf.ServiceOptions" }] }, { "name": "MethodDescriptorProto", "field": [{ "name": "name", "number": 1, "type": 9, "label": 1 }, { "name": "input_type", "number": 2, "type": 9, "label": 1 }, { "name": "output_type", "number": 3, "type": 9, "label": 1 }, { "name": "options", "number": 4, "type": 11, "label": 1, "typeName": ".google.protobuf.MethodOptions" }, { "name": "client_streaming", "number": 5, "type": 8, "label": 1, "defaultValue": "false" }, { "name": "server_streaming", "number": 6, "type": 8, "label": 1, "defaultValue": "false" }] }, { "name": "FileOptions", "field": [{ "name": "java_package", "number": 1, "type": 9, "label": 1 }, { "name": "java_outer_classname", "number": 8, "type": 9, "label": 1 }, { "name": "java_multiple_files", "number": 10, "type": 8, "label": 1, "defaultValue": "false", "options": {} }, { "name": "java_generate_equals_and_hash", "number": 20, "type": 8, "label": 1, "options": { "deprecated": true } }, { "name": "java_string_check_utf8", "number": 27, "type": 8, "label": 1, "defaultValue": "false" }, { "name": "optimize_for", "number": 9, "type": 14, "label": 1, "typeName": ".google.protobuf.FileOptions.OptimizeMode", "defaultValue": "SPEED" }, { "name": "go_package", "number": 11, "type": 9, "label": 1 }, { "name": "cc_generic_services", "number": 16, "type": 8, "label": 1, "defaultValue": "false" }, { "name": "java_generic_services", "number": 17, "type": 8, "label": 1, "defaultValue": "false" }, { "name": "py_generic_services", "number": 18, "type": 8, "label": 1, "defaultValue": "false" }, { "name": "deprecated", "number": 23, "type": 8, "label": 1, "defaultValue": "false" }, { "name": "cc_enable_arenas", "number": 31, "type": 8, "label": 1, "defaultValue": "true" }, { "name": "objc_class_prefix", "number": 36, "type": 9, "label": 1 }, { "name": "csharp_namespace", "number": 37, "type": 9, "label": 1 }, { "name": "swift_prefix", "number": 39, "type": 9, "label": 1 }, { "name": "php_class_prefix", "number": 40, "type": 9, "label": 1 }, { "name": "php_namespace", "number": 41, "type": 9, "label": 1 }, { "name": "php_metadata_namespace", "number": 44, "type": 9, "label": 1 }, { "name": "ruby_package", "number": 45, "type": 9, "label": 1 }, { "name": "features", "number": 50, "type": 11, "label": 1, "typeName": ".google.protobuf.FeatureSet" }, { "name": "uninterpreted_option", "number": 999, "type": 11, "label": 3, "typeName": ".google.protobuf.UninterpretedOption" }], "enumType": [{ "name": "OptimizeMode", "value": [{ "name": "SPEED", "number": 1 }, { "name": "CODE_SIZE", "number": 2 }, { "name": "LITE_RUNTIME", "number": 3 }] }], "extensionRange": [{ "start": 1e3, "end": 536870912 }] }, { "name": "MessageOptions", "field": [{ "name": "message_set_wire_format", "number": 1, "type": 8, "label": 1, "defaultValue": "false" }, { "name": "no_standard_descriptor_accessor", "number": 2, "type": 8, "label": 1, "defaultValue": "false" }, { "name": "deprecated", "number": 3, "type": 8, "label": 1, "defaultValue": "false" }, { "name": "map_entry", "number": 7, "type": 8, "label": 1 }, { "name": "deprecated_legacy_json_field_conflicts", "number": 11, "type": 8, "label": 1, "options": { "deprecated": true } }, { "name": "features", "number": 12, "type": 11, "label": 1, "typeName": ".google.protobuf.FeatureSet" }, { "name": "uninterpreted_option", "number": 999, "type": 11, "label": 3, "typeName": ".google.protobuf.UninterpretedOption" }], "extensionRange": [{ "start": 1e3, "end": 536870912 }] }, { "name": "FieldOptions", "field": [{ "name": "ctype", "number": 1, "type": 14, "label": 1, "typeName": ".google.protobuf.FieldOptions.CType", "defaultValue": "STRING" }, { "name": "packed", "number": 2, "type": 8, "label": 1 }, { "name": "jstype", "number": 6, "type": 14, "label": 1, "typeName": ".google.protobuf.FieldOptions.JSType", "defaultValue": "JS_NORMAL" }, { "name": "lazy", "number": 5, "type": 8, "label": 1, "defaultValue": "false" }, { "name": "unverified_lazy", "number": 15, "type": 8, "label": 1, "defaultValue": "false" }, { "name": "deprecated", "number": 3, "type": 8, "label": 1, "defaultValue": "false" }, { "name": "weak", "number": 10, "type": 8, "label": 1, "defaultValue": "false", "options": { "deprecated": true } }, { "name": "debug_redact", "number": 16, "type": 8, "label": 1, "defaultValue": "false" }, { "name": "retention", "number": 17, "type": 14, "label": 1, "typeName": ".google.protobuf.FieldOptions.OptionRetention" }, { "name": "targets", "number": 19, "type": 14, "label": 3, "typeName": ".google.protobuf.FieldOptions.OptionTargetType" }, { "name": "edition_defaults", "number": 20, "type": 11, "label": 3, "typeName": ".google.protobuf.FieldOptions.EditionDefault" }, { "name": "features", "number": 21, "type": 11, "label": 1, "typeName": ".google.protobuf.FeatureSet" }, { "name": "feature_support", "number": 22, "type": 11, "label": 1, "typeName": ".google.protobuf.FieldOptions.FeatureSupport" }, { "name": "uninterpreted_option", "number": 999, "type": 11, "label": 3, "typeName": ".google.protobuf.UninterpretedOption" }], "nestedType": [{ "name": "EditionDefault", "field": [{ "name": "edition", "number": 3, "type": 14, "label": 1, "typeName": ".google.protobuf.Edition" }, { "name": "value", "number": 2, "type": 9, "label": 1 }] }, { "name": "FeatureSupport", "field": [{ "name": "edition_introduced", "number": 1, "type": 14, "label": 1, "typeName": ".google.protobuf.Edition" }, { "name": "edition_deprecated", "number": 2, "type": 14, "label": 1, "typeName": ".google.protobuf.Edition" }, { "name": "deprecation_warning", "number": 3, "type": 9, "label": 1 }, { "name": "edition_removed", "number": 4, "type": 14, "label": 1, "typeName": ".google.protobuf.Edition" }, { "name": "removal_error", "number": 5, "type": 9, "label": 1 }] }], "enumType": [{ "name": "CType", "value": [{ "name": "STRING", "number": 0 }, { "name": "CORD", "number": 1 }, { "name": "STRING_PIECE", "number": 2 }] }, { "name": "JSType", "value": [{ "name": "JS_NORMAL", "number": 0 }, { "name": "JS_STRING", "number": 1 }, { "name": "JS_NUMBER", "number": 2 }] }, { "name": "OptionRetention", "value": [{ "name": "RETENTION_UNKNOWN", "number": 0 }, { "name": "RETENTION_RUNTIME", "number": 1 }, { "name": "RETENTION_SOURCE", "number": 2 }] }, { "name": "OptionTargetType", "value": [{ "name": "TARGET_TYPE_UNKNOWN", "number": 0 }, { "name": "TARGET_TYPE_FILE", "number": 1 }, { "name": "TARGET_TYPE_EXTENSION_RANGE", "number": 2 }, { "name": "TARGET_TYPE_MESSAGE", "number": 3 }, { "name": "TARGET_TYPE_FIELD", "number": 4 }, { "name": "TARGET_TYPE_ONEOF", "number": 5 }, { "name": "TARGET_TYPE_ENUM", "number": 6 }, { "name": "TARGET_TYPE_ENUM_ENTRY", "number": 7 }, { "name": "TARGET_TYPE_SERVICE", "number": 8 }, { "name": "TARGET_TYPE_METHOD", "number": 9 }] }], "extensionRange": [{ "start": 1e3, "end": 536870912 }] }, { "name": "OneofOptions", "field": [{ "name": "features", "number": 1, "type": 11, "label": 1, "typeName": ".google.protobuf.FeatureSet" }, { "name": "uninterpreted_option", "number": 999, "type": 11, "label": 3, "typeName": ".google.protobuf.UninterpretedOption" }], "extensionRange": [{ "start": 1e3, "end": 536870912 }] }, { "name": "EnumOptions", "field": [{ "name": "allow_alias", "number": 2, "type": 8, "label": 1 }, { "name": "deprecated", "number": 3, "type": 8, "label": 1, "defaultValue": "false" }, { "name": "deprecated_legacy_json_field_conflicts", "number": 6, "type": 8, "label": 1, "options": { "deprecated": true } }, { "name": "features", "number": 7, "type": 11, "label": 1, "typeName": ".google.protobuf.FeatureSet" }, { "name": "uninterpreted_option", "number": 999, "type": 11, "label": 3, "typeName": ".google.protobuf.UninterpretedOption" }], "extensionRange": [{ "start": 1e3, "end": 536870912 }] }, { "name": "EnumValueOptions", "field": [{ "name": "deprecated", "number": 1, "type": 8, "label": 1, "defaultValue": "false" }, { "name": "features", "number": 2, "type": 11, "label": 1, "typeName": ".google.protobuf.FeatureSet" }, { "name": "debug_redact", "number": 3, "type": 8, "label": 1, "defaultValue": "false" }, { "name": "feature_support", "number": 4, "type": 11, "label": 1, "typeName": ".google.protobuf.FieldOptions.FeatureSupport" }, { "name": "uninterpreted_option", "number": 999, "type": 11, "label": 3, "typeName": ".google.protobuf.UninterpretedOption" }], "extensionRange": [{ "start": 1e3, "end": 536870912 }] }, { "name": "ServiceOptions", "field": [{ "name": "features", "number": 34, "type": 11, "label": 1, "typeName": ".google.protobuf.FeatureSet" }, { "name": "deprecated", "number": 33, "type": 8, "label": 1, "defaultValue": "false" }, { "name": "uninterpreted_option", "number": 999, "type": 11, "label": 3, "typeName": ".google.protobuf.UninterpretedOption" }], "extensionRange": [{ "start": 1e3, "end": 536870912 }] }, { "name": "MethodOptions", "field": [{ "name": "deprecated", "number": 33, "type": 8, "label": 1, "defaultValue": "false" }, { "name": "idempotency_level", "number": 34, "type": 14, "label": 1, "typeName": ".google.protobuf.MethodOptions.IdempotencyLevel", "defaultValue": "IDEMPOTENCY_UNKNOWN" }, { "name": "features", "number": 35, "type": 11, "label": 1, "typeName": ".google.protobuf.FeatureSet" }, { "name": "uninterpreted_option", "number": 999, "type": 11, "label": 3, "typeName": ".google.protobuf.UninterpretedOption" }], "enumType": [{ "name": "IdempotencyLevel", "value": [{ "name": "IDEMPOTENCY_UNKNOWN", "number": 0 }, { "name": "NO_SIDE_EFFECTS", "number": 1 }, { "name": "IDEMPOTENT", "number": 2 }] }], "extensionRange": [{ "start": 1e3, "end": 536870912 }] }, { "name": "UninterpretedOption", "field": [{ "name": "name", "number": 2, "type": 11, "label": 3, "typeName": ".google.protobuf.UninterpretedOption.NamePart" }, { "name": "identifier_value", "number": 3, "type": 9, "label": 1 }, { "name": "positive_int_value", "number": 4, "type": 4, "label": 1 }, { "name": "negative_int_value", "number": 5, "type": 3, "label": 1 }, { "name": "double_value", "number": 6, "type": 1, "label": 1 }, { "name": "string_value", "number": 7, "type": 12, "label": 1 }, { "name": "aggregate_value", "number": 8, "type": 9, "label": 1 }], "nestedType": [{ "name": "NamePart", "field": [{ "name": "name_part", "number": 1, "type": 9, "label": 2 }, { "name": "is_extension", "number": 2, "type": 8, "label": 2 }] }] }, { "name": "FeatureSet", "field": [{ "name": "field_presence", "number": 1, "type": 14, "label": 1, "typeName": ".google.protobuf.FeatureSet.FieldPresence", "options": { "retention": 1, "targets": [4, 1], "editionDefaults": [{ "value": "EXPLICIT", "edition": 900 }, { "value": "IMPLICIT", "edition": 999 }, { "value": "EXPLICIT", "edition": 1e3 }] } }, { "name": "enum_type", "number": 2, "type": 14, "label": 1, "typeName": ".google.protobuf.FeatureSet.EnumType", "options": { "retention": 1, "targets": [6, 1], "editionDefaults": [{ "value": "CLOSED", "edition": 900 }, { "value": "OPEN", "edition": 999 }] } }, { "name": "repeated_field_encoding", "number": 3, "type": 14, "label": 1, "typeName": ".google.protobuf.FeatureSet.RepeatedFieldEncoding", "options": { "retention": 1, "targets": [4, 1], "editionDefaults": [{ "value": "EXPANDED", "edition": 900 }, { "value": "PACKED", "edition": 999 }] } }, { "name": "utf8_validation", "number": 4, "type": 14, "label": 1, "typeName": ".google.protobuf.FeatureSet.Utf8Validation", "options": { "retention": 1, "targets": [4, 1], "editionDefaults": [{ "value": "NONE", "edition": 900 }, { "value": "VERIFY", "edition": 999 }] } }, { "name": "message_encoding", "number": 5, "type": 14, "label": 1, "typeName": ".google.protobuf.FeatureSet.MessageEncoding", "options": { "retention": 1, "targets": [4, 1], "editionDefaults": [{ "value": "LENGTH_PREFIXED", "edition": 900 }] } }, { "name": "json_format", "number": 6, "type": 14, "label": 1, "typeName": ".google.protobuf.FeatureSet.JsonFormat", "options": { "retention": 1, "targets": [3, 6, 1], "editionDefaults": [{ "value": "LEGACY_BEST_EFFORT", "edition": 900 }, { "value": "ALLOW", "edition": 999 }] } }, { "name": "enforce_naming_style", "number": 7, "type": 14, "label": 1, "typeName": ".google.protobuf.FeatureSet.EnforceNamingStyle", "options": { "retention": 2, "targets": [1, 2, 3, 4, 5, 6, 7, 8, 9], "editionDefaults": [{ "value": "STYLE_LEGACY", "edition": 900 }, { "value": "STYLE2024", "edition": 1001 }] } }, { "name": "default_symbol_visibility", "number": 8, "type": 14, "label": 1, "typeName": ".google.protobuf.FeatureSet.VisibilityFeature.DefaultSymbolVisibility", "options": { "retention": 2, "targets": [1], "editionDefaults": [{ "value": "EXPORT_ALL", "edition": 900 }, { "value": "EXPORT_TOP_LEVEL", "edition": 1001 }] } }], "nestedType": [{ "name": "VisibilityFeature", "enumType": [{ "name": "DefaultSymbolVisibility", "value": [{ "name": "DEFAULT_SYMBOL_VISIBILITY_UNKNOWN", "number": 0 }, { "name": "EXPORT_ALL", "number": 1 }, { "name": "EXPORT_TOP_LEVEL", "number": 2 }, { "name": "LOCAL_ALL", "number": 3 }, { "name": "STRICT", "number": 4 }] }] }], "enumType": [{ "name": "FieldPresence", "value": [{ "name": "FIELD_PRESENCE_UNKNOWN", "number": 0 }, { "name": "EXPLICIT", "number": 1 }, { "name": "IMPLICIT", "number": 2 }, { "name": "LEGACY_REQUIRED", "number": 3 }] }, { "name": "EnumType", "value": [{ "name": "ENUM_TYPE_UNKNOWN", "number": 0 }, { "name": "OPEN", "number": 1 }, { "name": "CLOSED", "number": 2 }] }, { "name": "RepeatedFieldEncoding", "value": [{ "name": "REPEATED_FIELD_ENCODING_UNKNOWN", "number": 0 }, { "name": "PACKED", "number": 1 }, { "name": "EXPANDED", "number": 2 }] }, { "name": "Utf8Validation", "value": [{ "name": "UTF8_VALIDATION_UNKNOWN", "number": 0 }, { "name": "VERIFY", "number": 2 }, { "name": "NONE", "number": 3 }] }, { "name": "MessageEncoding", "value": [{ "name": "MESSAGE_ENCODING_UNKNOWN", "number": 0 }, { "name": "LENGTH_PREFIXED", "number": 1 }, { "name": "DELIMITED", "number": 2 }] }, { "name": "JsonFormat", "value": [{ "name": "JSON_FORMAT_UNKNOWN", "number": 0 }, { "name": "ALLOW", "number": 1 }, { "name": "LEGACY_BEST_EFFORT", "number": 2 }] }, { "name": "EnforceNamingStyle", "value": [{ "name": "ENFORCE_NAMING_STYLE_UNKNOWN", "number": 0 }, { "name": "STYLE2024", "number": 1 }, { "name": "STYLE_LEGACY", "number": 2 }] }], "extensionRange": [{ "start": 1e3, "end": 9995 }, { "start": 9995, "end": 1e4 }, { "start": 1e4, "end": 10001 }] }, { "name": "FeatureSetDefaults", "field": [{ "name": "defaults", "number": 1, "type": 11, "label": 3, "typeName": ".google.protobuf.FeatureSetDefaults.FeatureSetEditionDefault" }, { "name": "minimum_edition", "number": 4, "type": 14, "label": 1, "typeName": ".google.protobuf.Edition" }, { "name": "maximum_edition", "number": 5, "type": 14, "label": 1, "typeName": ".google.protobuf.Edition" }], "nestedType": [{ "name": "FeatureSetEditionDefault", "field": [{ "name": "edition", "number": 3, "type": 14, "label": 1, "typeName": ".google.protobuf.Edition" }, { "name": "overridable_features", "number": 4, "type": 11, "label": 1, "typeName": ".google.protobuf.FeatureSet" }, { "name": "fixed_features", "number": 5, "type": 11, "label": 1, "typeName": ".google.protobuf.FeatureSet" }] }] }, { "name": "SourceCodeInfo", "field": [{ "name": "location", "number": 1, "type": 11, "label": 3, "typeName": ".google.protobuf.SourceCodeInfo.Location" }], "nestedType": [{ "name": "Location", "field": [{ "name": "path", "number": 1, "type": 5, "label": 3, "options": { "packed": true } }, { "name": "span", "number": 2, "type": 5, "label": 3, "options": { "packed": true } }, { "name": "leading_comments", "number": 3, "type": 9, "label": 1 }, { "name": "trailing_comments", "number": 4, "type": 9, "label": 1 }, { "name": "leading_detached_comments", "number": 6, "type": 9, "label": 3 }] }], "extensionRange": [{ "start": 536e6, "end": 536000001 }] }, { "name": "GeneratedCodeInfo", "field": [{ "name": "annotation", "number": 1, "type": 11, "label": 3, "typeName": ".google.protobuf.GeneratedCodeInfo.Annotation" }], "nestedType": [{ "name": "Annotation", "field": [{ "name": "path", "number": 1, "type": 5, "label": 3, "options": { "packed": true } }, { "name": "source_file", "number": 2, "type": 9, "label": 1 }, { "name": "begin", "number": 3, "type": 5, "label": 1 }, { "name": "end", "number": 4, "type": 5, "label": 1 }, { "name": "semantic", "number": 5, "type": 14, "label": 1, "typeName": ".google.protobuf.GeneratedCodeInfo.Annotation.Semantic" }], "enumType": [{ "name": "Semantic", "value": [{ "name": "NONE", "number": 0 }, { "name": "SET", "number": 1 }, { "name": "ALIAS", "number": 2 }] }] }] }], "enumType": [{ "name": "Edition", "value": [{ "name": "EDITION_UNKNOWN", "number": 0 }, { "name": "EDITION_LEGACY", "number": 900 }, { "name": "EDITION_PROTO2", "number": 998 }, { "name": "EDITION_PROTO3", "number": 999 }, { "name": "EDITION_2023", "number": 1e3 }, { "name": "EDITION_2024", "number": 1001 }, { "name": "EDITION_UNSTABLE", "number": 9999 }, { "name": "EDITION_1_TEST_ONLY", "number": 1 }, { "name": "EDITION_2_TEST_ONLY", "number": 2 }, { "name": "EDITION_99997_TEST_ONLY", "number": 99997 }, { "name": "EDITION_99998_TEST_ONLY", "number": 99998 }, { "name": "EDITION_99999_TEST_ONLY", "number": 99999 }, { "name": "EDITION_MAX", "number": 2147483647 }] }, { "name": "SymbolVisibility", "value": [{ "name": "VISIBILITY_UNSET", "number": 0 }, { "name": "VISIBILITY_LOCAL", "number": 1 }, { "name": "VISIBILITY_EXPORT", "number": 2 }] }] });
    exports.FileDescriptorSetSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 0);
    exports.FileDescriptorProtoSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 1);
    exports.DescriptorProtoSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 2);
    exports.DescriptorProto_ExtensionRangeSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 2, 0);
    exports.DescriptorProto_ReservedRangeSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 2, 1);
    exports.ExtensionRangeOptionsSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 3);
    exports.ExtensionRangeOptions_DeclarationSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 3, 0);
    var ExtensionRangeOptions_VerificationState;
    (function(ExtensionRangeOptions_VerificationState2) {
      ExtensionRangeOptions_VerificationState2[ExtensionRangeOptions_VerificationState2["DECLARATION"] = 0] = "DECLARATION";
      ExtensionRangeOptions_VerificationState2[ExtensionRangeOptions_VerificationState2["UNVERIFIED"] = 1] = "UNVERIFIED";
    })(ExtensionRangeOptions_VerificationState || (exports.ExtensionRangeOptions_VerificationState = ExtensionRangeOptions_VerificationState = {}));
    exports.ExtensionRangeOptions_VerificationStateSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_descriptor, 3, 0);
    exports.FieldDescriptorProtoSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 4);
    var FieldDescriptorProto_Type;
    (function(FieldDescriptorProto_Type2) {
      FieldDescriptorProto_Type2[FieldDescriptorProto_Type2["DOUBLE"] = 1] = "DOUBLE";
      FieldDescriptorProto_Type2[FieldDescriptorProto_Type2["FLOAT"] = 2] = "FLOAT";
      FieldDescriptorProto_Type2[FieldDescriptorProto_Type2["INT64"] = 3] = "INT64";
      FieldDescriptorProto_Type2[FieldDescriptorProto_Type2["UINT64"] = 4] = "UINT64";
      FieldDescriptorProto_Type2[FieldDescriptorProto_Type2["INT32"] = 5] = "INT32";
      FieldDescriptorProto_Type2[FieldDescriptorProto_Type2["FIXED64"] = 6] = "FIXED64";
      FieldDescriptorProto_Type2[FieldDescriptorProto_Type2["FIXED32"] = 7] = "FIXED32";
      FieldDescriptorProto_Type2[FieldDescriptorProto_Type2["BOOL"] = 8] = "BOOL";
      FieldDescriptorProto_Type2[FieldDescriptorProto_Type2["STRING"] = 9] = "STRING";
      FieldDescriptorProto_Type2[FieldDescriptorProto_Type2["GROUP"] = 10] = "GROUP";
      FieldDescriptorProto_Type2[FieldDescriptorProto_Type2["MESSAGE"] = 11] = "MESSAGE";
      FieldDescriptorProto_Type2[FieldDescriptorProto_Type2["BYTES"] = 12] = "BYTES";
      FieldDescriptorProto_Type2[FieldDescriptorProto_Type2["UINT32"] = 13] = "UINT32";
      FieldDescriptorProto_Type2[FieldDescriptorProto_Type2["ENUM"] = 14] = "ENUM";
      FieldDescriptorProto_Type2[FieldDescriptorProto_Type2["SFIXED32"] = 15] = "SFIXED32";
      FieldDescriptorProto_Type2[FieldDescriptorProto_Type2["SFIXED64"] = 16] = "SFIXED64";
      FieldDescriptorProto_Type2[FieldDescriptorProto_Type2["SINT32"] = 17] = "SINT32";
      FieldDescriptorProto_Type2[FieldDescriptorProto_Type2["SINT64"] = 18] = "SINT64";
    })(FieldDescriptorProto_Type || (exports.FieldDescriptorProto_Type = FieldDescriptorProto_Type = {}));
    exports.FieldDescriptorProto_TypeSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_descriptor, 4, 0);
    var FieldDescriptorProto_Label;
    (function(FieldDescriptorProto_Label2) {
      FieldDescriptorProto_Label2[FieldDescriptorProto_Label2["OPTIONAL"] = 1] = "OPTIONAL";
      FieldDescriptorProto_Label2[FieldDescriptorProto_Label2["REPEATED"] = 3] = "REPEATED";
      FieldDescriptorProto_Label2[FieldDescriptorProto_Label2["REQUIRED"] = 2] = "REQUIRED";
    })(FieldDescriptorProto_Label || (exports.FieldDescriptorProto_Label = FieldDescriptorProto_Label = {}));
    exports.FieldDescriptorProto_LabelSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_descriptor, 4, 1);
    exports.OneofDescriptorProtoSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 5);
    exports.EnumDescriptorProtoSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 6);
    exports.EnumDescriptorProto_EnumReservedRangeSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 6, 0);
    exports.EnumValueDescriptorProtoSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 7);
    exports.ServiceDescriptorProtoSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 8);
    exports.MethodDescriptorProtoSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 9);
    exports.FileOptionsSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 10);
    var FileOptions_OptimizeMode;
    (function(FileOptions_OptimizeMode2) {
      FileOptions_OptimizeMode2[FileOptions_OptimizeMode2["SPEED"] = 1] = "SPEED";
      FileOptions_OptimizeMode2[FileOptions_OptimizeMode2["CODE_SIZE"] = 2] = "CODE_SIZE";
      FileOptions_OptimizeMode2[FileOptions_OptimizeMode2["LITE_RUNTIME"] = 3] = "LITE_RUNTIME";
    })(FileOptions_OptimizeMode || (exports.FileOptions_OptimizeMode = FileOptions_OptimizeMode = {}));
    exports.FileOptions_OptimizeModeSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_descriptor, 10, 0);
    exports.MessageOptionsSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 11);
    exports.FieldOptionsSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 12);
    exports.FieldOptions_EditionDefaultSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 12, 0);
    exports.FieldOptions_FeatureSupportSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 12, 1);
    var FieldOptions_CType;
    (function(FieldOptions_CType2) {
      FieldOptions_CType2[FieldOptions_CType2["STRING"] = 0] = "STRING";
      FieldOptions_CType2[FieldOptions_CType2["CORD"] = 1] = "CORD";
      FieldOptions_CType2[FieldOptions_CType2["STRING_PIECE"] = 2] = "STRING_PIECE";
    })(FieldOptions_CType || (exports.FieldOptions_CType = FieldOptions_CType = {}));
    exports.FieldOptions_CTypeSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_descriptor, 12, 0);
    var FieldOptions_JSType;
    (function(FieldOptions_JSType2) {
      FieldOptions_JSType2[FieldOptions_JSType2["JS_NORMAL"] = 0] = "JS_NORMAL";
      FieldOptions_JSType2[FieldOptions_JSType2["JS_STRING"] = 1] = "JS_STRING";
      FieldOptions_JSType2[FieldOptions_JSType2["JS_NUMBER"] = 2] = "JS_NUMBER";
    })(FieldOptions_JSType || (exports.FieldOptions_JSType = FieldOptions_JSType = {}));
    exports.FieldOptions_JSTypeSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_descriptor, 12, 1);
    var FieldOptions_OptionRetention;
    (function(FieldOptions_OptionRetention2) {
      FieldOptions_OptionRetention2[FieldOptions_OptionRetention2["RETENTION_UNKNOWN"] = 0] = "RETENTION_UNKNOWN";
      FieldOptions_OptionRetention2[FieldOptions_OptionRetention2["RETENTION_RUNTIME"] = 1] = "RETENTION_RUNTIME";
      FieldOptions_OptionRetention2[FieldOptions_OptionRetention2["RETENTION_SOURCE"] = 2] = "RETENTION_SOURCE";
    })(FieldOptions_OptionRetention || (exports.FieldOptions_OptionRetention = FieldOptions_OptionRetention = {}));
    exports.FieldOptions_OptionRetentionSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_descriptor, 12, 2);
    var FieldOptions_OptionTargetType;
    (function(FieldOptions_OptionTargetType2) {
      FieldOptions_OptionTargetType2[FieldOptions_OptionTargetType2["TARGET_TYPE_UNKNOWN"] = 0] = "TARGET_TYPE_UNKNOWN";
      FieldOptions_OptionTargetType2[FieldOptions_OptionTargetType2["TARGET_TYPE_FILE"] = 1] = "TARGET_TYPE_FILE";
      FieldOptions_OptionTargetType2[FieldOptions_OptionTargetType2["TARGET_TYPE_EXTENSION_RANGE"] = 2] = "TARGET_TYPE_EXTENSION_RANGE";
      FieldOptions_OptionTargetType2[FieldOptions_OptionTargetType2["TARGET_TYPE_MESSAGE"] = 3] = "TARGET_TYPE_MESSAGE";
      FieldOptions_OptionTargetType2[FieldOptions_OptionTargetType2["TARGET_TYPE_FIELD"] = 4] = "TARGET_TYPE_FIELD";
      FieldOptions_OptionTargetType2[FieldOptions_OptionTargetType2["TARGET_TYPE_ONEOF"] = 5] = "TARGET_TYPE_ONEOF";
      FieldOptions_OptionTargetType2[FieldOptions_OptionTargetType2["TARGET_TYPE_ENUM"] = 6] = "TARGET_TYPE_ENUM";
      FieldOptions_OptionTargetType2[FieldOptions_OptionTargetType2["TARGET_TYPE_ENUM_ENTRY"] = 7] = "TARGET_TYPE_ENUM_ENTRY";
      FieldOptions_OptionTargetType2[FieldOptions_OptionTargetType2["TARGET_TYPE_SERVICE"] = 8] = "TARGET_TYPE_SERVICE";
      FieldOptions_OptionTargetType2[FieldOptions_OptionTargetType2["TARGET_TYPE_METHOD"] = 9] = "TARGET_TYPE_METHOD";
    })(FieldOptions_OptionTargetType || (exports.FieldOptions_OptionTargetType = FieldOptions_OptionTargetType = {}));
    exports.FieldOptions_OptionTargetTypeSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_descriptor, 12, 3);
    exports.OneofOptionsSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 13);
    exports.EnumOptionsSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 14);
    exports.EnumValueOptionsSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 15);
    exports.ServiceOptionsSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 16);
    exports.MethodOptionsSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 17);
    var MethodOptions_IdempotencyLevel;
    (function(MethodOptions_IdempotencyLevel2) {
      MethodOptions_IdempotencyLevel2[MethodOptions_IdempotencyLevel2["IDEMPOTENCY_UNKNOWN"] = 0] = "IDEMPOTENCY_UNKNOWN";
      MethodOptions_IdempotencyLevel2[MethodOptions_IdempotencyLevel2["NO_SIDE_EFFECTS"] = 1] = "NO_SIDE_EFFECTS";
      MethodOptions_IdempotencyLevel2[MethodOptions_IdempotencyLevel2["IDEMPOTENT"] = 2] = "IDEMPOTENT";
    })(MethodOptions_IdempotencyLevel || (exports.MethodOptions_IdempotencyLevel = MethodOptions_IdempotencyLevel = {}));
    exports.MethodOptions_IdempotencyLevelSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_descriptor, 17, 0);
    exports.UninterpretedOptionSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 18);
    exports.UninterpretedOption_NamePartSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 18, 0);
    exports.FeatureSetSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 19);
    exports.FeatureSet_VisibilityFeatureSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 19, 0);
    var FeatureSet_VisibilityFeature_DefaultSymbolVisibility;
    (function(FeatureSet_VisibilityFeature_DefaultSymbolVisibility2) {
      FeatureSet_VisibilityFeature_DefaultSymbolVisibility2[FeatureSet_VisibilityFeature_DefaultSymbolVisibility2["DEFAULT_SYMBOL_VISIBILITY_UNKNOWN"] = 0] = "DEFAULT_SYMBOL_VISIBILITY_UNKNOWN";
      FeatureSet_VisibilityFeature_DefaultSymbolVisibility2[FeatureSet_VisibilityFeature_DefaultSymbolVisibility2["EXPORT_ALL"] = 1] = "EXPORT_ALL";
      FeatureSet_VisibilityFeature_DefaultSymbolVisibility2[FeatureSet_VisibilityFeature_DefaultSymbolVisibility2["EXPORT_TOP_LEVEL"] = 2] = "EXPORT_TOP_LEVEL";
      FeatureSet_VisibilityFeature_DefaultSymbolVisibility2[FeatureSet_VisibilityFeature_DefaultSymbolVisibility2["LOCAL_ALL"] = 3] = "LOCAL_ALL";
      FeatureSet_VisibilityFeature_DefaultSymbolVisibility2[FeatureSet_VisibilityFeature_DefaultSymbolVisibility2["STRICT"] = 4] = "STRICT";
    })(FeatureSet_VisibilityFeature_DefaultSymbolVisibility || (exports.FeatureSet_VisibilityFeature_DefaultSymbolVisibility = FeatureSet_VisibilityFeature_DefaultSymbolVisibility = {}));
    exports.FeatureSet_VisibilityFeature_DefaultSymbolVisibilitySchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_descriptor, 19, 0, 0);
    var FeatureSet_FieldPresence;
    (function(FeatureSet_FieldPresence2) {
      FeatureSet_FieldPresence2[FeatureSet_FieldPresence2["FIELD_PRESENCE_UNKNOWN"] = 0] = "FIELD_PRESENCE_UNKNOWN";
      FeatureSet_FieldPresence2[FeatureSet_FieldPresence2["EXPLICIT"] = 1] = "EXPLICIT";
      FeatureSet_FieldPresence2[FeatureSet_FieldPresence2["IMPLICIT"] = 2] = "IMPLICIT";
      FeatureSet_FieldPresence2[FeatureSet_FieldPresence2["LEGACY_REQUIRED"] = 3] = "LEGACY_REQUIRED";
    })(FeatureSet_FieldPresence || (exports.FeatureSet_FieldPresence = FeatureSet_FieldPresence = {}));
    exports.FeatureSet_FieldPresenceSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_descriptor, 19, 0);
    var FeatureSet_EnumType;
    (function(FeatureSet_EnumType2) {
      FeatureSet_EnumType2[FeatureSet_EnumType2["ENUM_TYPE_UNKNOWN"] = 0] = "ENUM_TYPE_UNKNOWN";
      FeatureSet_EnumType2[FeatureSet_EnumType2["OPEN"] = 1] = "OPEN";
      FeatureSet_EnumType2[FeatureSet_EnumType2["CLOSED"] = 2] = "CLOSED";
    })(FeatureSet_EnumType || (exports.FeatureSet_EnumType = FeatureSet_EnumType = {}));
    exports.FeatureSet_EnumTypeSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_descriptor, 19, 1);
    var FeatureSet_RepeatedFieldEncoding;
    (function(FeatureSet_RepeatedFieldEncoding2) {
      FeatureSet_RepeatedFieldEncoding2[FeatureSet_RepeatedFieldEncoding2["REPEATED_FIELD_ENCODING_UNKNOWN"] = 0] = "REPEATED_FIELD_ENCODING_UNKNOWN";
      FeatureSet_RepeatedFieldEncoding2[FeatureSet_RepeatedFieldEncoding2["PACKED"] = 1] = "PACKED";
      FeatureSet_RepeatedFieldEncoding2[FeatureSet_RepeatedFieldEncoding2["EXPANDED"] = 2] = "EXPANDED";
    })(FeatureSet_RepeatedFieldEncoding || (exports.FeatureSet_RepeatedFieldEncoding = FeatureSet_RepeatedFieldEncoding = {}));
    exports.FeatureSet_RepeatedFieldEncodingSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_descriptor, 19, 2);
    var FeatureSet_Utf8Validation;
    (function(FeatureSet_Utf8Validation2) {
      FeatureSet_Utf8Validation2[FeatureSet_Utf8Validation2["UTF8_VALIDATION_UNKNOWN"] = 0] = "UTF8_VALIDATION_UNKNOWN";
      FeatureSet_Utf8Validation2[FeatureSet_Utf8Validation2["VERIFY"] = 2] = "VERIFY";
      FeatureSet_Utf8Validation2[FeatureSet_Utf8Validation2["NONE"] = 3] = "NONE";
    })(FeatureSet_Utf8Validation || (exports.FeatureSet_Utf8Validation = FeatureSet_Utf8Validation = {}));
    exports.FeatureSet_Utf8ValidationSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_descriptor, 19, 3);
    var FeatureSet_MessageEncoding;
    (function(FeatureSet_MessageEncoding2) {
      FeatureSet_MessageEncoding2[FeatureSet_MessageEncoding2["MESSAGE_ENCODING_UNKNOWN"] = 0] = "MESSAGE_ENCODING_UNKNOWN";
      FeatureSet_MessageEncoding2[FeatureSet_MessageEncoding2["LENGTH_PREFIXED"] = 1] = "LENGTH_PREFIXED";
      FeatureSet_MessageEncoding2[FeatureSet_MessageEncoding2["DELIMITED"] = 2] = "DELIMITED";
    })(FeatureSet_MessageEncoding || (exports.FeatureSet_MessageEncoding = FeatureSet_MessageEncoding = {}));
    exports.FeatureSet_MessageEncodingSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_descriptor, 19, 4);
    var FeatureSet_JsonFormat;
    (function(FeatureSet_JsonFormat2) {
      FeatureSet_JsonFormat2[FeatureSet_JsonFormat2["JSON_FORMAT_UNKNOWN"] = 0] = "JSON_FORMAT_UNKNOWN";
      FeatureSet_JsonFormat2[FeatureSet_JsonFormat2["ALLOW"] = 1] = "ALLOW";
      FeatureSet_JsonFormat2[FeatureSet_JsonFormat2["LEGACY_BEST_EFFORT"] = 2] = "LEGACY_BEST_EFFORT";
    })(FeatureSet_JsonFormat || (exports.FeatureSet_JsonFormat = FeatureSet_JsonFormat = {}));
    exports.FeatureSet_JsonFormatSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_descriptor, 19, 5);
    var FeatureSet_EnforceNamingStyle;
    (function(FeatureSet_EnforceNamingStyle2) {
      FeatureSet_EnforceNamingStyle2[FeatureSet_EnforceNamingStyle2["ENFORCE_NAMING_STYLE_UNKNOWN"] = 0] = "ENFORCE_NAMING_STYLE_UNKNOWN";
      FeatureSet_EnforceNamingStyle2[FeatureSet_EnforceNamingStyle2["STYLE2024"] = 1] = "STYLE2024";
      FeatureSet_EnforceNamingStyle2[FeatureSet_EnforceNamingStyle2["STYLE_LEGACY"] = 2] = "STYLE_LEGACY";
    })(FeatureSet_EnforceNamingStyle || (exports.FeatureSet_EnforceNamingStyle = FeatureSet_EnforceNamingStyle = {}));
    exports.FeatureSet_EnforceNamingStyleSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_descriptor, 19, 6);
    exports.FeatureSetDefaultsSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 20);
    exports.FeatureSetDefaults_FeatureSetEditionDefaultSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 20, 0);
    exports.SourceCodeInfoSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 21);
    exports.SourceCodeInfo_LocationSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 21, 0);
    exports.GeneratedCodeInfoSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 22);
    exports.GeneratedCodeInfo_AnnotationSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_descriptor, 22, 0);
    var GeneratedCodeInfo_Annotation_Semantic;
    (function(GeneratedCodeInfo_Annotation_Semantic2) {
      GeneratedCodeInfo_Annotation_Semantic2[GeneratedCodeInfo_Annotation_Semantic2["NONE"] = 0] = "NONE";
      GeneratedCodeInfo_Annotation_Semantic2[GeneratedCodeInfo_Annotation_Semantic2["SET"] = 1] = "SET";
      GeneratedCodeInfo_Annotation_Semantic2[GeneratedCodeInfo_Annotation_Semantic2["ALIAS"] = 2] = "ALIAS";
    })(GeneratedCodeInfo_Annotation_Semantic || (exports.GeneratedCodeInfo_Annotation_Semantic = GeneratedCodeInfo_Annotation_Semantic = {}));
    exports.GeneratedCodeInfo_Annotation_SemanticSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_descriptor, 22, 0, 0);
    var Edition;
    (function(Edition2) {
      Edition2[Edition2["EDITION_UNKNOWN"] = 0] = "EDITION_UNKNOWN";
      Edition2[Edition2["EDITION_LEGACY"] = 900] = "EDITION_LEGACY";
      Edition2[Edition2["EDITION_PROTO2"] = 998] = "EDITION_PROTO2";
      Edition2[Edition2["EDITION_PROTO3"] = 999] = "EDITION_PROTO3";
      Edition2[Edition2["EDITION_2023"] = 1e3] = "EDITION_2023";
      Edition2[Edition2["EDITION_2024"] = 1001] = "EDITION_2024";
      Edition2[Edition2["EDITION_UNSTABLE"] = 9999] = "EDITION_UNSTABLE";
      Edition2[Edition2["EDITION_1_TEST_ONLY"] = 1] = "EDITION_1_TEST_ONLY";
      Edition2[Edition2["EDITION_2_TEST_ONLY"] = 2] = "EDITION_2_TEST_ONLY";
      Edition2[Edition2["EDITION_99997_TEST_ONLY"] = 99997] = "EDITION_99997_TEST_ONLY";
      Edition2[Edition2["EDITION_99998_TEST_ONLY"] = 99998] = "EDITION_99998_TEST_ONLY";
      Edition2[Edition2["EDITION_99999_TEST_ONLY"] = 99999] = "EDITION_99999_TEST_ONLY";
      Edition2[Edition2["EDITION_MAX"] = 2147483647] = "EDITION_MAX";
    })(Edition || (exports.Edition = Edition = {}));
    exports.EditionSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_descriptor, 0);
    var SymbolVisibility;
    (function(SymbolVisibility2) {
      SymbolVisibility2[SymbolVisibility2["VISIBILITY_UNSET"] = 0] = "VISIBILITY_UNSET";
      SymbolVisibility2[SymbolVisibility2["VISIBILITY_LOCAL"] = 1] = "VISIBILITY_LOCAL";
      SymbolVisibility2[SymbolVisibility2["VISIBILITY_EXPORT"] = 2] = "VISIBILITY_EXPORT";
    })(SymbolVisibility || (exports.SymbolVisibility = SymbolVisibility = {}));
    exports.SymbolVisibilitySchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_descriptor, 1);
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/from-binary.js
var require_from_binary = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/from-binary.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.fromBinary = fromBinary;
    exports.mergeFromBinary = mergeFromBinary;
    exports.readField = readField;
    var descriptors_js_1 = require_descriptors();
    var scalar_js_1 = require_scalar();
    var reflect_js_1 = require_reflect();
    var binary_encoding_js_1 = require_binary_encoding();
    var varint_js_1 = require_varint();
    var readDefaults = {
      readUnknownFields: true
    };
    function makeReadOptions(options) {
      return options ? Object.assign(Object.assign({}, readDefaults), options) : readDefaults;
    }
    __name(makeReadOptions, "makeReadOptions");
    function fromBinary(schema, bytes, options) {
      const msg = (0, reflect_js_1.reflect)(schema, void 0, false);
      readMessage(msg, new binary_encoding_js_1.BinaryReader(bytes), makeReadOptions(options), false, bytes.byteLength);
      return msg.message;
    }
    __name(fromBinary, "fromBinary");
    function mergeFromBinary(schema, target, bytes, options) {
      readMessage((0, reflect_js_1.reflect)(schema, target, false), new binary_encoding_js_1.BinaryReader(bytes), makeReadOptions(options), false, bytes.byteLength);
      return target;
    }
    __name(mergeFromBinary, "mergeFromBinary");
    function readMessage(message, reader, options, delimited, lengthOrDelimitedFieldNo) {
      var _a;
      const end = delimited ? reader.len : reader.pos + lengthOrDelimitedFieldNo;
      let fieldNo;
      let wireType;
      const unknownFields = (_a = message.getUnknown()) !== null && _a !== void 0 ? _a : [];
      while (reader.pos < end) {
        [fieldNo, wireType] = reader.tag();
        if (delimited && wireType == binary_encoding_js_1.WireType.EndGroup) {
          break;
        }
        const field = message.findNumber(fieldNo);
        if (!field) {
          const data = reader.skip(wireType, fieldNo);
          if (options.readUnknownFields) {
            unknownFields.push({ no: fieldNo, wireType, data });
          }
          continue;
        }
        readField(message, reader, field, wireType, options);
      }
      if (delimited) {
        if (wireType != binary_encoding_js_1.WireType.EndGroup || fieldNo !== lengthOrDelimitedFieldNo) {
          throw new Error("invalid end group tag");
        }
      }
      if (unknownFields.length > 0) {
        message.setUnknown(unknownFields);
      }
    }
    __name(readMessage, "readMessage");
    function readField(message, reader, field, wireType, options) {
      var _a;
      switch (field.fieldKind) {
        case "scalar":
          message.set(field, readScalar(reader, field.scalar, field.utf8Validation));
          break;
        case "enum":
          const val = readScalar(reader, descriptors_js_1.ScalarType.INT32);
          if (field.enum.open) {
            message.set(field, val);
          } else {
            const ok = field.enum.values.some((v) => v.number === val);
            if (ok) {
              message.set(field, val);
            } else if (options.readUnknownFields) {
              const bytes = [];
              (0, varint_js_1.varint32write)(val, bytes);
              const unknownFields = (_a = message.getUnknown()) !== null && _a !== void 0 ? _a : [];
              unknownFields.push({
                no: field.number,
                wireType,
                data: new Uint8Array(bytes)
              });
              message.setUnknown(unknownFields);
            }
          }
          break;
        case "message":
          message.set(field, readMessageField(reader, options, field, message.get(field)));
          break;
        case "list":
          readListField(reader, wireType, message.get(field), options);
          break;
        case "map":
          readMapEntry(reader, message.get(field), options);
          break;
      }
    }
    __name(readField, "readField");
    function readMapEntry(reader, map, options) {
      const field = map.field();
      let key;
      let val;
      const len = reader.uint32();
      const end = reader.pos + len;
      while (reader.pos < end) {
        const [fieldNo] = reader.tag();
        switch (fieldNo) {
          case 1:
            key = readScalar(reader, field.mapKey, field.utf8Validation);
            break;
          case 2:
            switch (field.mapKind) {
              case "scalar":
                val = readScalar(reader, field.scalar, field.utf8Validation);
                break;
              case "enum":
                val = reader.int32();
                break;
              case "message":
                val = readMessageField(reader, options, field);
                break;
            }
            break;
        }
      }
      if (key === void 0) {
        key = (0, scalar_js_1.scalarZeroValue)(field.mapKey, false);
      }
      if (val === void 0) {
        switch (field.mapKind) {
          case "scalar":
            val = (0, scalar_js_1.scalarZeroValue)(field.scalar, false);
            break;
          case "enum":
            val = field.enum.values[0].number;
            break;
          case "message":
            val = (0, reflect_js_1.reflect)(field.message, void 0, false);
            break;
        }
      }
      map.set(key, val);
    }
    __name(readMapEntry, "readMapEntry");
    function readListField(reader, wireType, list, options) {
      var _a;
      const field = list.field();
      if (field.listKind === "message") {
        list.add(readMessageField(reader, options, field));
        return;
      }
      const scalarType = (_a = field.scalar) !== null && _a !== void 0 ? _a : descriptors_js_1.ScalarType.INT32;
      const packed = wireType == binary_encoding_js_1.WireType.LengthDelimited && scalarType != descriptors_js_1.ScalarType.STRING && scalarType != descriptors_js_1.ScalarType.BYTES;
      if (!packed) {
        list.add(readScalar(reader, scalarType, field.utf8Validation));
        return;
      }
      const e = reader.uint32() + reader.pos;
      while (reader.pos < e) {
        list.add(readScalar(reader, scalarType, field.utf8Validation));
      }
    }
    __name(readListField, "readListField");
    function readMessageField(reader, options, field, mergeMessage) {
      const delimited = field.delimitedEncoding;
      const message = mergeMessage !== null && mergeMessage !== void 0 ? mergeMessage : (0, reflect_js_1.reflect)(field.message, void 0, false);
      readMessage(message, reader, options, delimited, delimited ? field.number : reader.uint32());
      return message;
    }
    __name(readMessageField, "readMessageField");
    function readScalar(reader, type, validateUtf8 = false) {
      switch (type) {
        case descriptors_js_1.ScalarType.STRING:
          return reader.string(validateUtf8);
        case descriptors_js_1.ScalarType.BOOL:
          return reader.bool();
        case descriptors_js_1.ScalarType.DOUBLE:
          return reader.double();
        case descriptors_js_1.ScalarType.FLOAT:
          return reader.float();
        case descriptors_js_1.ScalarType.INT32:
          return reader.int32();
        case descriptors_js_1.ScalarType.INT64:
          return reader.int64();
        case descriptors_js_1.ScalarType.UINT64:
          return reader.uint64();
        case descriptors_js_1.ScalarType.FIXED64:
          return reader.fixed64();
        case descriptors_js_1.ScalarType.BYTES:
          return reader.bytes();
        case descriptors_js_1.ScalarType.FIXED32:
          return reader.fixed32();
        case descriptors_js_1.ScalarType.SFIXED32:
          return reader.sfixed32();
        case descriptors_js_1.ScalarType.SFIXED64:
          return reader.sfixed64();
        case descriptors_js_1.ScalarType.SINT64:
          return reader.sint64();
        case descriptors_js_1.ScalarType.UINT32:
          return reader.uint32();
        case descriptors_js_1.ScalarType.SINT32:
          return reader.sint32();
      }
    }
    __name(readScalar, "readScalar");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/file.js
var require_file = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/file.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.fileDesc = fileDesc;
    var base64_encoding_js_1 = require_base64_encoding();
    var descriptor_pb_js_1 = require_descriptor_pb();
    var registry_js_1 = require_registry();
    var restore_json_names_js_1 = require_restore_json_names();
    var from_binary_js_1 = require_from_binary();
    function fileDesc(b64, imports) {
      var _a;
      const root = (0, from_binary_js_1.fromBinary)(descriptor_pb_js_1.FileDescriptorProtoSchema, (0, base64_encoding_js_1.base64Decode)(b64));
      root.messageType.forEach(restore_json_names_js_1.restoreJsonNames);
      root.dependency = (_a = imports === null || imports === void 0 ? void 0 : imports.map((f) => f.proto.name)) !== null && _a !== void 0 ? _a : [];
      const reg = (0, registry_js_1.createFileRegistry)(root, (protoFileName) => imports === null || imports === void 0 ? void 0 : imports.find((f) => f.proto.name === protoFileName));
      return reg.getFile(root.name);
    }
    __name(fileDesc, "fileDesc");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/timestamp_pb.js
var require_timestamp_pb = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/timestamp_pb.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.TimestampSchema = exports.file_google_protobuf_timestamp = void 0;
    var file_js_1 = require_file();
    var message_js_1 = require_message();
    exports.file_google_protobuf_timestamp = (0, file_js_1.fileDesc)("");
    exports.TimestampSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_timestamp, 0);
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/timestamp.js
var require_timestamp = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/timestamp.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.timestampNow = timestampNow;
    exports.timestampFromDate = timestampFromDate;
    exports.timestampDate = timestampDate;
    exports.timestampFromMs = timestampFromMs;
    exports.timestampMs = timestampMs;
    var timestamp_pb_js_1 = require_timestamp_pb();
    var create_js_1 = require_create();
    var proto_int64_js_1 = require_proto_int64();
    function timestampNow() {
      return timestampFromDate(/* @__PURE__ */ new Date());
    }
    __name(timestampNow, "timestampNow");
    function timestampFromDate(date) {
      return timestampFromMs(date.getTime());
    }
    __name(timestampFromDate, "timestampFromDate");
    function timestampDate(timestamp) {
      return new Date(timestampMs(timestamp));
    }
    __name(timestampDate, "timestampDate");
    function timestampFromMs(timestampMs2) {
      const seconds = Math.floor(timestampMs2 / 1e3);
      return (0, create_js_1.create)(timestamp_pb_js_1.TimestampSchema, {
        seconds: proto_int64_js_1.protoInt64.parse(seconds),
        nanos: (timestampMs2 - seconds * 1e3) * 1e6
      });
    }
    __name(timestampFromMs, "timestampFromMs");
    function timestampMs(timestamp) {
      return Number(timestamp.seconds) * 1e3 + Math.round(timestamp.nanos / 1e6);
    }
    __name(timestampMs, "timestampMs");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/duration_pb.js
var require_duration_pb = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/duration_pb.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.DurationSchema = exports.file_google_protobuf_duration = void 0;
    var file_js_1 = require_file();
    var message_js_1 = require_message();
    exports.file_google_protobuf_duration = (0, file_js_1.fileDesc)("");
    exports.DurationSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_duration, 0);
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/duration.js
var require_duration = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/duration.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.durationFromMs = durationFromMs;
    exports.durationMs = durationMs;
    var duration_pb_js_1 = require_duration_pb();
    var create_js_1 = require_create();
    var proto_int64_js_1 = require_proto_int64();
    function durationFromMs(durationMs2) {
      const sign = durationMs2 < 0 ? -1 : 1;
      const absDurationMs = Math.abs(durationMs2);
      const absSeconds = Math.floor(absDurationMs / 1e3);
      const absNanos = (absDurationMs - absSeconds * 1e3) * 1e6;
      return (0, create_js_1.create)(duration_pb_js_1.DurationSchema, {
        seconds: proto_int64_js_1.protoInt64.parse(absSeconds * sign),
        nanos: absNanos === 0 ? 0 : absNanos * sign
        // deliberately avoid signed 0 - it does not serialize
      });
    }
    __name(durationFromMs, "durationFromMs");
    function durationMs(duration) {
      return Number(duration.seconds) * 1e3 + Math.round(duration.nanos / 1e6);
    }
    __name(durationMs, "durationMs");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/any_pb.js
var require_any_pb = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/any_pb.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AnySchema = exports.file_google_protobuf_any = void 0;
    var file_js_1 = require_file();
    var message_js_1 = require_message();
    exports.file_google_protobuf_any = (0, file_js_1.fileDesc)("");
    exports.AnySchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_any, 0);
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/to-binary.js
var require_to_binary = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/to-binary.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.toBinary = toBinary;
    exports.writeField = writeField;
    var reflect_js_1 = require_reflect();
    var binary_encoding_js_1 = require_binary_encoding();
    var descriptors_js_1 = require_descriptors();
    var LEGACY_REQUIRED = 3;
    var writeDefaults = {
      writeUnknownFields: true
    };
    function makeWriteOptions(options) {
      return options ? Object.assign(Object.assign({}, writeDefaults), options) : writeDefaults;
    }
    __name(makeWriteOptions, "makeWriteOptions");
    function toBinary(schema, message, options) {
      return writeFields(new binary_encoding_js_1.BinaryWriter(), makeWriteOptions(options), (0, reflect_js_1.reflect)(schema, message)).finish();
    }
    __name(toBinary, "toBinary");
    function writeFields(writer, opts, msg) {
      var _a;
      for (const f of msg.sortedFields) {
        if (!msg.isSet(f)) {
          if (f.presence == LEGACY_REQUIRED) {
            throw new Error(`cannot encode ${f} to binary: required field not set`);
          }
          continue;
        }
        writeField(writer, opts, msg, f);
      }
      if (opts.writeUnknownFields) {
        for (const { no, wireType, data } of (_a = msg.getUnknown()) !== null && _a !== void 0 ? _a : []) {
          writer.tag(no, wireType).raw(data);
        }
      }
      return writer;
    }
    __name(writeFields, "writeFields");
    function writeField(writer, opts, msg, field) {
      var _a;
      switch (field.fieldKind) {
        case "scalar":
        case "enum":
          writeScalar(writer, msg.desc.typeName, field.name, (_a = field.scalar) !== null && _a !== void 0 ? _a : descriptors_js_1.ScalarType.INT32, field.number, msg.get(field));
          break;
        case "list":
          writeListField(writer, opts, field, msg.get(field));
          break;
        case "message":
          writeMessageField(writer, opts, field, msg.get(field));
          break;
        case "map":
          for (const [key, val] of msg.get(field)) {
            writeMapEntry(writer, opts, field, key, val);
          }
          break;
      }
    }
    __name(writeField, "writeField");
    function writeScalar(writer, msgName, fieldName, scalarType, fieldNo, value) {
      writeScalarValue(writer.tag(fieldNo, writeTypeOfScalar(scalarType)), msgName, fieldName, scalarType, value);
    }
    __name(writeScalar, "writeScalar");
    function writeMessageField(writer, opts, field, message) {
      if (field.delimitedEncoding) {
        writeFields(writer.tag(field.number, binary_encoding_js_1.WireType.StartGroup), opts, message).tag(field.number, binary_encoding_js_1.WireType.EndGroup);
      } else {
        writeFields(writer.tag(field.number, binary_encoding_js_1.WireType.LengthDelimited).fork(), opts, message).join();
      }
    }
    __name(writeMessageField, "writeMessageField");
    function writeListField(writer, opts, field, list) {
      var _a;
      if (field.listKind == "message") {
        for (const item of list) {
          writeMessageField(writer, opts, field, item);
        }
        return;
      }
      const scalarType = (_a = field.scalar) !== null && _a !== void 0 ? _a : descriptors_js_1.ScalarType.INT32;
      if (field.packed) {
        if (!list.size) {
          return;
        }
        writer.tag(field.number, binary_encoding_js_1.WireType.LengthDelimited).fork();
        for (const item of list) {
          writeScalarValue(writer, field.parent.typeName, field.name, scalarType, item);
        }
        writer.join();
        return;
      }
      for (const item of list) {
        writeScalar(writer, field.parent.typeName, field.name, scalarType, field.number, item);
      }
    }
    __name(writeListField, "writeListField");
    function writeMapEntry(writer, opts, field, key, value) {
      var _a;
      writer.tag(field.number, binary_encoding_js_1.WireType.LengthDelimited).fork();
      writeScalar(writer, field.parent.typeName, field.name, field.mapKey, 1, key);
      switch (field.mapKind) {
        case "scalar":
        case "enum":
          writeScalar(writer, field.parent.typeName, field.name, (_a = field.scalar) !== null && _a !== void 0 ? _a : descriptors_js_1.ScalarType.INT32, 2, value);
          break;
        case "message":
          writeFields(writer.tag(2, binary_encoding_js_1.WireType.LengthDelimited).fork(), opts, value).join();
          break;
      }
      writer.join();
    }
    __name(writeMapEntry, "writeMapEntry");
    function writeScalarValue(writer, msgName, fieldName, type, value) {
      try {
        switch (type) {
          case descriptors_js_1.ScalarType.STRING:
            writer.string(value);
            break;
          case descriptors_js_1.ScalarType.BOOL:
            writer.bool(value);
            break;
          case descriptors_js_1.ScalarType.DOUBLE:
            writer.double(value);
            break;
          case descriptors_js_1.ScalarType.FLOAT:
            writer.float(value);
            break;
          case descriptors_js_1.ScalarType.INT32:
            writer.int32(value);
            break;
          case descriptors_js_1.ScalarType.INT64:
            writer.int64(value);
            break;
          case descriptors_js_1.ScalarType.UINT64:
            writer.uint64(value);
            break;
          case descriptors_js_1.ScalarType.FIXED64:
            writer.fixed64(value);
            break;
          case descriptors_js_1.ScalarType.BYTES:
            writer.bytes(value);
            break;
          case descriptors_js_1.ScalarType.FIXED32:
            writer.fixed32(value);
            break;
          case descriptors_js_1.ScalarType.SFIXED32:
            writer.sfixed32(value);
            break;
          case descriptors_js_1.ScalarType.SFIXED64:
            writer.sfixed64(value);
            break;
          case descriptors_js_1.ScalarType.SINT64:
            writer.sint64(value);
            break;
          case descriptors_js_1.ScalarType.UINT32:
            writer.uint32(value);
            break;
          case descriptors_js_1.ScalarType.SINT32:
            writer.sint32(value);
            break;
        }
      } catch (e) {
        if (e instanceof Error) {
          throw new Error(`cannot encode field ${msgName}.${fieldName} to binary: ${e.message}`);
        }
        throw e;
      }
    }
    __name(writeScalarValue, "writeScalarValue");
    function writeTypeOfScalar(type) {
      switch (type) {
        case descriptors_js_1.ScalarType.BYTES:
        case descriptors_js_1.ScalarType.STRING:
          return binary_encoding_js_1.WireType.LengthDelimited;
        case descriptors_js_1.ScalarType.DOUBLE:
        case descriptors_js_1.ScalarType.FIXED64:
        case descriptors_js_1.ScalarType.SFIXED64:
          return binary_encoding_js_1.WireType.Bit64;
        case descriptors_js_1.ScalarType.FIXED32:
        case descriptors_js_1.ScalarType.SFIXED32:
        case descriptors_js_1.ScalarType.FLOAT:
          return binary_encoding_js_1.WireType.Bit32;
        default:
          return binary_encoding_js_1.WireType.Varint;
      }
    }
    __name(writeTypeOfScalar, "writeTypeOfScalar");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/any.js
var require_any = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/any.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.anyPack = anyPack;
    exports.anyIs = anyIs;
    exports.anyUnpack = anyUnpack;
    exports.anyUnpackTo = anyUnpackTo;
    var any_pb_js_1 = require_any_pb();
    var create_js_1 = require_create();
    var to_binary_js_1 = require_to_binary();
    var from_binary_js_1 = require_from_binary();
    function anyPack(schema, message, into) {
      let ret = false;
      if (!into) {
        into = (0, create_js_1.create)(any_pb_js_1.AnySchema);
        ret = true;
      }
      into.value = (0, to_binary_js_1.toBinary)(schema, message);
      into.typeUrl = typeNameToUrl(message.$typeName);
      return ret ? into : void 0;
    }
    __name(anyPack, "anyPack");
    function anyIs(any, descOrTypeName) {
      if (any.typeUrl === "") {
        return false;
      }
      const want = typeof descOrTypeName == "string" ? descOrTypeName : descOrTypeName.typeName;
      const got = typeUrlToName(any.typeUrl);
      return want === got;
    }
    __name(anyIs, "anyIs");
    function anyUnpack(any, registryOrMessageDesc) {
      if (any.typeUrl === "") {
        return void 0;
      }
      const desc = registryOrMessageDesc.kind == "message" ? registryOrMessageDesc : registryOrMessageDesc.getMessage(typeUrlToName(any.typeUrl));
      if (!desc || !anyIs(any, desc)) {
        return void 0;
      }
      return (0, from_binary_js_1.fromBinary)(desc, any.value);
    }
    __name(anyUnpack, "anyUnpack");
    function anyUnpackTo(any, schema, message) {
      if (!anyIs(any, schema)) {
        return void 0;
      }
      return (0, from_binary_js_1.mergeFromBinary)(schema, message, any.value);
    }
    __name(anyUnpackTo, "anyUnpackTo");
    function typeNameToUrl(name) {
      return `type.googleapis.com/${name}`;
    }
    __name(typeNameToUrl, "typeNameToUrl");
    function typeUrlToName(url) {
      const slash = url.lastIndexOf("/");
      const name = slash >= 0 ? url.substring(slash + 1) : url;
      if (!name.length) {
        throw new Error(`invalid type url: ${url}`);
      }
      return name;
    }
    __name(typeUrlToName, "typeUrlToName");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/source_context_pb.js
var require_source_context_pb = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/source_context_pb.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SourceContextSchema = exports.file_google_protobuf_source_context = void 0;
    var file_js_1 = require_file();
    var message_js_1 = require_message();
    exports.file_google_protobuf_source_context = (0, file_js_1.fileDesc)("");
    exports.SourceContextSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_source_context, 0);
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/type_pb.js
var require_type_pb = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/type_pb.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SyntaxSchema = exports.Syntax = exports.OptionSchema = exports.EnumValueSchema = exports.EnumSchema = exports.Field_CardinalitySchema = exports.Field_Cardinality = exports.Field_KindSchema = exports.Field_Kind = exports.FieldSchema = exports.TypeSchema = exports.file_google_protobuf_type = void 0;
    var file_js_1 = require_file();
    var any_pb_js_1 = require_any_pb();
    var source_context_pb_js_1 = require_source_context_pb();
    var message_js_1 = require_message();
    var enum_js_1 = require_enum();
    exports.file_google_protobuf_type = (0, file_js_1.fileDesc)("", [any_pb_js_1.file_google_protobuf_any, source_context_pb_js_1.file_google_protobuf_source_context]);
    exports.TypeSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_type, 0);
    exports.FieldSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_type, 1);
    var Field_Kind;
    (function(Field_Kind2) {
      Field_Kind2[Field_Kind2["TYPE_UNKNOWN"] = 0] = "TYPE_UNKNOWN";
      Field_Kind2[Field_Kind2["TYPE_DOUBLE"] = 1] = "TYPE_DOUBLE";
      Field_Kind2[Field_Kind2["TYPE_FLOAT"] = 2] = "TYPE_FLOAT";
      Field_Kind2[Field_Kind2["TYPE_INT64"] = 3] = "TYPE_INT64";
      Field_Kind2[Field_Kind2["TYPE_UINT64"] = 4] = "TYPE_UINT64";
      Field_Kind2[Field_Kind2["TYPE_INT32"] = 5] = "TYPE_INT32";
      Field_Kind2[Field_Kind2["TYPE_FIXED64"] = 6] = "TYPE_FIXED64";
      Field_Kind2[Field_Kind2["TYPE_FIXED32"] = 7] = "TYPE_FIXED32";
      Field_Kind2[Field_Kind2["TYPE_BOOL"] = 8] = "TYPE_BOOL";
      Field_Kind2[Field_Kind2["TYPE_STRING"] = 9] = "TYPE_STRING";
      Field_Kind2[Field_Kind2["TYPE_GROUP"] = 10] = "TYPE_GROUP";
      Field_Kind2[Field_Kind2["TYPE_MESSAGE"] = 11] = "TYPE_MESSAGE";
      Field_Kind2[Field_Kind2["TYPE_BYTES"] = 12] = "TYPE_BYTES";
      Field_Kind2[Field_Kind2["TYPE_UINT32"] = 13] = "TYPE_UINT32";
      Field_Kind2[Field_Kind2["TYPE_ENUM"] = 14] = "TYPE_ENUM";
      Field_Kind2[Field_Kind2["TYPE_SFIXED32"] = 15] = "TYPE_SFIXED32";
      Field_Kind2[Field_Kind2["TYPE_SFIXED64"] = 16] = "TYPE_SFIXED64";
      Field_Kind2[Field_Kind2["TYPE_SINT32"] = 17] = "TYPE_SINT32";
      Field_Kind2[Field_Kind2["TYPE_SINT64"] = 18] = "TYPE_SINT64";
    })(Field_Kind || (exports.Field_Kind = Field_Kind = {}));
    exports.Field_KindSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_type, 1, 0);
    var Field_Cardinality;
    (function(Field_Cardinality2) {
      Field_Cardinality2[Field_Cardinality2["UNKNOWN"] = 0] = "UNKNOWN";
      Field_Cardinality2[Field_Cardinality2["OPTIONAL"] = 1] = "OPTIONAL";
      Field_Cardinality2[Field_Cardinality2["REQUIRED"] = 2] = "REQUIRED";
      Field_Cardinality2[Field_Cardinality2["REPEATED"] = 3] = "REPEATED";
    })(Field_Cardinality || (exports.Field_Cardinality = Field_Cardinality = {}));
    exports.Field_CardinalitySchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_type, 1, 1);
    exports.EnumSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_type, 2);
    exports.EnumValueSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_type, 3);
    exports.OptionSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_type, 4);
    var Syntax;
    (function(Syntax2) {
      Syntax2[Syntax2["PROTO2"] = 0] = "PROTO2";
      Syntax2[Syntax2["PROTO3"] = 1] = "PROTO3";
      Syntax2[Syntax2["EDITIONS"] = 2] = "EDITIONS";
    })(Syntax || (exports.Syntax = Syntax = {}));
    exports.SyntaxSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_type, 0);
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/api_pb.js
var require_api_pb = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/api_pb.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.MixinSchema = exports.MethodSchema = exports.ApiSchema = exports.file_google_protobuf_api = void 0;
    var file_js_1 = require_file();
    var source_context_pb_js_1 = require_source_context_pb();
    var type_pb_js_1 = require_type_pb();
    var message_js_1 = require_message();
    exports.file_google_protobuf_api = (0, file_js_1.fileDesc)("", [source_context_pb_js_1.file_google_protobuf_source_context, type_pb_js_1.file_google_protobuf_type]);
    exports.ApiSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_api, 0);
    exports.MethodSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_api, 1);
    exports.MixinSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_api, 2);
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/extension.js
var require_extension = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/extension.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.extDesc = extDesc;
    function extDesc(file, path, ...paths) {
      if (paths.length == 0) {
        return file.extensions[path];
      }
      const e = paths.pop();
      return paths.reduce((acc, cur) => acc.nestedMessages[cur], file.messages[path]).nestedExtensions[e];
    }
    __name(extDesc, "extDesc");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/cpp_features_pb.js
var require_cpp_features_pb = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/cpp_features_pb.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.cpp = exports.CppFeatures_StringTypeSchema = exports.CppFeatures_StringType = exports.CppFeaturesSchema = exports.file_google_protobuf_cpp_features = void 0;
    var file_js_1 = require_file();
    var descriptor_pb_js_1 = require_descriptor_pb();
    var message_js_1 = require_message();
    var enum_js_1 = require_enum();
    var extension_js_1 = require_extension();
    exports.file_google_protobuf_cpp_features = (0, file_js_1.fileDesc)("", [descriptor_pb_js_1.file_google_protobuf_descriptor]);
    exports.CppFeaturesSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_cpp_features, 0);
    var CppFeatures_StringType;
    (function(CppFeatures_StringType2) {
      CppFeatures_StringType2[CppFeatures_StringType2["STRING_TYPE_UNKNOWN"] = 0] = "STRING_TYPE_UNKNOWN";
      CppFeatures_StringType2[CppFeatures_StringType2["VIEW"] = 1] = "VIEW";
      CppFeatures_StringType2[CppFeatures_StringType2["CORD"] = 2] = "CORD";
      CppFeatures_StringType2[CppFeatures_StringType2["STRING"] = 3] = "STRING";
    })(CppFeatures_StringType || (exports.CppFeatures_StringType = CppFeatures_StringType = {}));
    exports.CppFeatures_StringTypeSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_cpp_features, 0, 0);
    exports.cpp = (0, extension_js_1.extDesc)(exports.file_google_protobuf_cpp_features, 0);
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/empty_pb.js
var require_empty_pb = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/empty_pb.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.EmptySchema = exports.file_google_protobuf_empty = void 0;
    var file_js_1 = require_file();
    var message_js_1 = require_message();
    exports.file_google_protobuf_empty = (0, file_js_1.fileDesc)("Chtnb29nbGUvcHJvdG9idWYvZW1wdHkucHJvdG8SD2dvb2dsZS5wcm90b2J1ZiIHCgVFbXB0eUJ9ChNjb20uZ29vZ2xlLnByb3RvYnVmQgpFbXB0eVByb3RvUAFaLmdvb2dsZS5nb2xhbmcub3JnL3Byb3RvYnVmL3R5cGVzL2tub3duL2VtcHR5cGL4AQGiAgNHUEKqAh5Hb29nbGUuUHJvdG9idWYuV2VsbEtub3duVHlwZXNiBnByb3RvMw");
    exports.EmptySchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_empty, 0);
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/field_mask_pb.js
var require_field_mask_pb = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/field_mask_pb.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.FieldMaskSchema = exports.file_google_protobuf_field_mask = void 0;
    var file_js_1 = require_file();
    var message_js_1 = require_message();
    exports.file_google_protobuf_field_mask = (0, file_js_1.fileDesc)("");
    exports.FieldMaskSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_field_mask, 0);
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/go_features_pb.js
var require_go_features_pb = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/go_features_pb.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.go = exports.GoFeatures_StripEnumPrefixSchema = exports.GoFeatures_StripEnumPrefix = exports.GoFeatures_APILevelSchema = exports.GoFeatures_APILevel = exports.GoFeatures_OptimizeModeFeature_OptimizeModeSchema = exports.GoFeatures_OptimizeModeFeature_OptimizeMode = exports.GoFeatures_OptimizeModeFeatureSchema = exports.GoFeaturesSchema = exports.file_google_protobuf_go_features = void 0;
    var file_js_1 = require_file();
    var descriptor_pb_js_1 = require_descriptor_pb();
    var message_js_1 = require_message();
    var enum_js_1 = require_enum();
    var extension_js_1 = require_extension();
    exports.file_google_protobuf_go_features = (0, file_js_1.fileDesc)("", [descriptor_pb_js_1.file_google_protobuf_descriptor]);
    exports.GoFeaturesSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_go_features, 0);
    exports.GoFeatures_OptimizeModeFeatureSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_go_features, 0, 0);
    var GoFeatures_OptimizeModeFeature_OptimizeMode;
    (function(GoFeatures_OptimizeModeFeature_OptimizeMode2) {
      GoFeatures_OptimizeModeFeature_OptimizeMode2[GoFeatures_OptimizeModeFeature_OptimizeMode2["OPTIMIZE_MODE_UNSPECIFIED"] = 0] = "OPTIMIZE_MODE_UNSPECIFIED";
      GoFeatures_OptimizeModeFeature_OptimizeMode2[GoFeatures_OptimizeModeFeature_OptimizeMode2["SPEED"] = 1] = "SPEED";
      GoFeatures_OptimizeModeFeature_OptimizeMode2[GoFeatures_OptimizeModeFeature_OptimizeMode2["CODE_SIZE"] = 2] = "CODE_SIZE";
    })(GoFeatures_OptimizeModeFeature_OptimizeMode || (exports.GoFeatures_OptimizeModeFeature_OptimizeMode = GoFeatures_OptimizeModeFeature_OptimizeMode = {}));
    exports.GoFeatures_OptimizeModeFeature_OptimizeModeSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_go_features, 0, 0, 0);
    var GoFeatures_APILevel;
    (function(GoFeatures_APILevel2) {
      GoFeatures_APILevel2[GoFeatures_APILevel2["API_LEVEL_UNSPECIFIED"] = 0] = "API_LEVEL_UNSPECIFIED";
      GoFeatures_APILevel2[GoFeatures_APILevel2["API_OPEN"] = 1] = "API_OPEN";
      GoFeatures_APILevel2[GoFeatures_APILevel2["API_HYBRID"] = 2] = "API_HYBRID";
      GoFeatures_APILevel2[GoFeatures_APILevel2["API_OPAQUE"] = 3] = "API_OPAQUE";
    })(GoFeatures_APILevel || (exports.GoFeatures_APILevel = GoFeatures_APILevel = {}));
    exports.GoFeatures_APILevelSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_go_features, 0, 0);
    var GoFeatures_StripEnumPrefix;
    (function(GoFeatures_StripEnumPrefix2) {
      GoFeatures_StripEnumPrefix2[GoFeatures_StripEnumPrefix2["UNSPECIFIED"] = 0] = "UNSPECIFIED";
      GoFeatures_StripEnumPrefix2[GoFeatures_StripEnumPrefix2["KEEP"] = 1] = "KEEP";
      GoFeatures_StripEnumPrefix2[GoFeatures_StripEnumPrefix2["GENERATE_BOTH"] = 2] = "GENERATE_BOTH";
      GoFeatures_StripEnumPrefix2[GoFeatures_StripEnumPrefix2["STRIP"] = 3] = "STRIP";
    })(GoFeatures_StripEnumPrefix || (exports.GoFeatures_StripEnumPrefix = GoFeatures_StripEnumPrefix = {}));
    exports.GoFeatures_StripEnumPrefixSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_go_features, 0, 1);
    exports.go = (0, extension_js_1.extDesc)(exports.file_google_protobuf_go_features, 0);
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/java_features_pb.js
var require_java_features_pb = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/java_features_pb.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.java = exports.JavaFeatures_Utf8ValidationSchema = exports.JavaFeatures_Utf8Validation = exports.JavaFeatures_NestInFileClassFeature_NestInFileClassSchema = exports.JavaFeatures_NestInFileClassFeature_NestInFileClass = exports.JavaFeatures_NestInFileClassFeatureSchema = exports.JavaFeaturesSchema = exports.file_google_protobuf_java_features = void 0;
    var file_js_1 = require_file();
    var descriptor_pb_js_1 = require_descriptor_pb();
    var message_js_1 = require_message();
    var enum_js_1 = require_enum();
    var extension_js_1 = require_extension();
    exports.file_google_protobuf_java_features = (0, file_js_1.fileDesc)("", [descriptor_pb_js_1.file_google_protobuf_descriptor]);
    exports.JavaFeaturesSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_java_features, 0);
    exports.JavaFeatures_NestInFileClassFeatureSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_java_features, 0, 0);
    var JavaFeatures_NestInFileClassFeature_NestInFileClass;
    (function(JavaFeatures_NestInFileClassFeature_NestInFileClass2) {
      JavaFeatures_NestInFileClassFeature_NestInFileClass2[JavaFeatures_NestInFileClassFeature_NestInFileClass2["NEST_IN_FILE_CLASS_UNKNOWN"] = 0] = "NEST_IN_FILE_CLASS_UNKNOWN";
      JavaFeatures_NestInFileClassFeature_NestInFileClass2[JavaFeatures_NestInFileClassFeature_NestInFileClass2["NO"] = 1] = "NO";
      JavaFeatures_NestInFileClassFeature_NestInFileClass2[JavaFeatures_NestInFileClassFeature_NestInFileClass2["YES"] = 2] = "YES";
      JavaFeatures_NestInFileClassFeature_NestInFileClass2[JavaFeatures_NestInFileClassFeature_NestInFileClass2["LEGACY"] = 3] = "LEGACY";
    })(JavaFeatures_NestInFileClassFeature_NestInFileClass || (exports.JavaFeatures_NestInFileClassFeature_NestInFileClass = JavaFeatures_NestInFileClassFeature_NestInFileClass = {}));
    exports.JavaFeatures_NestInFileClassFeature_NestInFileClassSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_java_features, 0, 0, 0);
    var JavaFeatures_Utf8Validation;
    (function(JavaFeatures_Utf8Validation2) {
      JavaFeatures_Utf8Validation2[JavaFeatures_Utf8Validation2["UTF8_VALIDATION_UNKNOWN"] = 0] = "UTF8_VALIDATION_UNKNOWN";
      JavaFeatures_Utf8Validation2[JavaFeatures_Utf8Validation2["DEFAULT"] = 1] = "DEFAULT";
      JavaFeatures_Utf8Validation2[JavaFeatures_Utf8Validation2["VERIFY"] = 2] = "VERIFY";
    })(JavaFeatures_Utf8Validation || (exports.JavaFeatures_Utf8Validation = JavaFeatures_Utf8Validation = {}));
    exports.JavaFeatures_Utf8ValidationSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_java_features, 0, 0);
    exports.java = (0, extension_js_1.extDesc)(exports.file_google_protobuf_java_features, 0);
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/struct_pb.js
var require_struct_pb = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/struct_pb.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.NullValueSchema = exports.NullValue = exports.ListValueSchema = exports.ValueSchema = exports.StructSchema = exports.file_google_protobuf_struct = void 0;
    var file_js_1 = require_file();
    var message_js_1 = require_message();
    var enum_js_1 = require_enum();
    exports.file_google_protobuf_struct = (0, file_js_1.fileDesc)("");
    exports.StructSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_struct, 0);
    exports.ValueSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_struct, 1);
    exports.ListValueSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_struct, 2);
    var NullValue;
    (function(NullValue2) {
      NullValue2[NullValue2["NULL_VALUE"] = 0] = "NULL_VALUE";
    })(NullValue || (exports.NullValue = NullValue = {}));
    exports.NullValueSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_struct, 0);
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/wrappers_pb.js
var require_wrappers_pb = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/wrappers_pb.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.BytesValueSchema = exports.StringValueSchema = exports.BoolValueSchema = exports.UInt32ValueSchema = exports.Int32ValueSchema = exports.UInt64ValueSchema = exports.Int64ValueSchema = exports.FloatValueSchema = exports.DoubleValueSchema = exports.file_google_protobuf_wrappers = void 0;
    var file_js_1 = require_file();
    var message_js_1 = require_message();
    exports.file_google_protobuf_wrappers = (0, file_js_1.fileDesc)("");
    exports.DoubleValueSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_wrappers, 0);
    exports.FloatValueSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_wrappers, 1);
    exports.Int64ValueSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_wrappers, 2);
    exports.UInt64ValueSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_wrappers, 3);
    exports.Int32ValueSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_wrappers, 4);
    exports.UInt32ValueSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_wrappers, 5);
    exports.BoolValueSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_wrappers, 6);
    exports.StringValueSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_wrappers, 7);
    exports.BytesValueSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_wrappers, 8);
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/compiler/plugin_pb.js
var require_plugin_pb = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/gen/google/protobuf/compiler/plugin_pb.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.CodeGeneratorResponse_FeatureSchema = exports.CodeGeneratorResponse_Feature = exports.CodeGeneratorResponse_FileSchema = exports.CodeGeneratorResponseSchema = exports.CodeGeneratorRequestSchema = exports.VersionSchema = exports.file_google_protobuf_compiler_plugin = void 0;
    var file_js_1 = require_file();
    var descriptor_pb_js_1 = require_descriptor_pb();
    var message_js_1 = require_message();
    var enum_js_1 = require_enum();
    exports.file_google_protobuf_compiler_plugin = (0, file_js_1.fileDesc)("", [descriptor_pb_js_1.file_google_protobuf_descriptor]);
    exports.VersionSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_compiler_plugin, 0);
    exports.CodeGeneratorRequestSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_compiler_plugin, 1);
    exports.CodeGeneratorResponseSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_compiler_plugin, 2);
    exports.CodeGeneratorResponse_FileSchema = (0, message_js_1.messageDesc)(exports.file_google_protobuf_compiler_plugin, 2, 0);
    var CodeGeneratorResponse_Feature;
    (function(CodeGeneratorResponse_Feature2) {
      CodeGeneratorResponse_Feature2[CodeGeneratorResponse_Feature2["NONE"] = 0] = "NONE";
      CodeGeneratorResponse_Feature2[CodeGeneratorResponse_Feature2["PROTO3_OPTIONAL"] = 1] = "PROTO3_OPTIONAL";
      CodeGeneratorResponse_Feature2[CodeGeneratorResponse_Feature2["SUPPORTS_EDITIONS"] = 2] = "SUPPORTS_EDITIONS";
    })(CodeGeneratorResponse_Feature || (exports.CodeGeneratorResponse_Feature = CodeGeneratorResponse_Feature = {}));
    exports.CodeGeneratorResponse_FeatureSchema = (0, enum_js_1.enumDesc)(exports.file_google_protobuf_compiler_plugin, 2, 0);
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/index.js
var require_wkt = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wkt/index.js"(exports) {
    "use strict";
    init_esm();
    var __createBinding = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: /* @__PURE__ */ __name(function() {
          return m[k];
        }, "get") };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    });
    var __exportStar = exports && exports.__exportStar || function(m, exports2) {
      for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p)) __createBinding(exports2, m, p);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    __exportStar(require_timestamp(), exports);
    __exportStar(require_duration(), exports);
    __exportStar(require_any(), exports);
    __exportStar(require_wrappers(), exports);
    __exportStar(require_any_pb(), exports);
    __exportStar(require_api_pb(), exports);
    __exportStar(require_cpp_features_pb(), exports);
    __exportStar(require_descriptor_pb(), exports);
    __exportStar(require_duration_pb(), exports);
    __exportStar(require_empty_pb(), exports);
    __exportStar(require_field_mask_pb(), exports);
    __exportStar(require_go_features_pb(), exports);
    __exportStar(require_java_features_pb(), exports);
    __exportStar(require_source_context_pb(), exports);
    __exportStar(require_struct_pb(), exports);
    __exportStar(require_timestamp_pb(), exports);
    __exportStar(require_type_pb(), exports);
    __exportStar(require_wrappers_pb(), exports);
    __exportStar(require_plugin_pb(), exports);
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/extensions.js
var require_extensions = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/extensions.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getExtension = getExtension;
    exports.setExtension = setExtension;
    exports.clearExtension = clearExtension;
    exports.hasExtension = hasExtension;
    exports.hasOption = hasOption;
    exports.getOption = getOption;
    exports.createExtensionContainer = createExtensionContainer;
    var create_js_1 = require_create();
    var from_binary_js_1 = require_from_binary();
    var reflect_js_1 = require_reflect();
    var scalar_js_1 = require_scalar();
    var to_binary_js_1 = require_to_binary();
    var binary_encoding_js_1 = require_binary_encoding();
    var wrappers_js_1 = require_wrappers();
    function getExtension(message, extension) {
      assertExtendee(extension, message);
      const ufs = filterUnknownFields(message.$unknown, extension);
      const [container, field, get] = createExtensionContainer(extension);
      for (const uf of ufs) {
        (0, from_binary_js_1.readField)(container, new binary_encoding_js_1.BinaryReader(uf.data), field, uf.wireType, {
          readUnknownFields: true
        });
      }
      return get();
    }
    __name(getExtension, "getExtension");
    function setExtension(message, extension, value) {
      var _a;
      assertExtendee(extension, message);
      const ufs = ((_a = message.$unknown) !== null && _a !== void 0 ? _a : []).filter((uf) => uf.no !== extension.number);
      const [container, field] = createExtensionContainer(extension, value);
      const writer = new binary_encoding_js_1.BinaryWriter();
      (0, to_binary_js_1.writeField)(writer, { writeUnknownFields: true }, container, field);
      const reader = new binary_encoding_js_1.BinaryReader(writer.finish());
      while (reader.pos < reader.len) {
        const [no, wireType] = reader.tag();
        const data = reader.skip(wireType, no);
        ufs.push({ no, wireType, data });
      }
      message.$unknown = ufs;
    }
    __name(setExtension, "setExtension");
    function clearExtension(message, extension) {
      assertExtendee(extension, message);
      if (message.$unknown === void 0) {
        return;
      }
      message.$unknown = message.$unknown.filter((uf) => uf.no !== extension.number);
    }
    __name(clearExtension, "clearExtension");
    function hasExtension(message, extension) {
      var _a;
      return extension.extendee.typeName === message.$typeName && !!((_a = message.$unknown) === null || _a === void 0 ? void 0 : _a.find((uf) => uf.no === extension.number));
    }
    __name(hasExtension, "hasExtension");
    function hasOption(element, option) {
      const message = element.proto.options;
      if (!message) {
        return false;
      }
      return hasExtension(message, option);
    }
    __name(hasOption, "hasOption");
    function getOption(element, option) {
      const message = element.proto.options;
      if (!message) {
        const [, , get] = createExtensionContainer(option);
        return get();
      }
      return getExtension(message, option);
    }
    __name(getOption, "getOption");
    function filterUnknownFields(unknownFields, extension) {
      if (unknownFields === void 0)
        return [];
      if (extension.fieldKind === "enum" || extension.fieldKind === "scalar") {
        for (let i = unknownFields.length - 1; i >= 0; --i) {
          if (unknownFields[i].no == extension.number) {
            return [unknownFields[i]];
          }
        }
        return [];
      }
      return unknownFields.filter((uf) => uf.no === extension.number);
    }
    __name(filterUnknownFields, "filterUnknownFields");
    function createExtensionContainer(extension, value) {
      const localName = extension.typeName;
      const field = Object.assign(Object.assign({}, extension), { kind: "field", parent: extension.extendee, localName });
      const desc = Object.assign(Object.assign({}, extension.extendee), { fields: [field], members: [field], oneofs: [] });
      const container = (0, create_js_1.create)(desc, value !== void 0 ? { [localName]: value } : void 0);
      return [
        (0, reflect_js_1.reflect)(desc, container),
        field,
        () => {
          const value2 = container[localName];
          if (value2 === void 0) {
            const desc2 = extension.message;
            if ((0, wrappers_js_1.isWrapperDesc)(desc2)) {
              return (0, scalar_js_1.scalarZeroValue)(desc2.fields[0].scalar, desc2.fields[0].longAsString);
            }
            return (0, create_js_1.create)(desc2);
          }
          return value2;
        }
      ];
    }
    __name(createExtensionContainer, "createExtensionContainer");
    function assertExtendee(extension, message) {
      if (extension.extendee.typeName != message.$typeName) {
        throw new Error(`extension ${extension.typeName} can only be applied to message ${extension.extendee.typeName}`);
      }
    }
    __name(assertExtendee, "assertExtendee");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/equals.js
var require_equals = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/equals.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.equals = equals;
    var scalar_js_1 = require_scalar();
    var reflect_js_1 = require_reflect();
    var descriptors_js_1 = require_descriptors();
    var index_js_1 = require_wkt();
    var extensions_js_1 = require_extensions();
    function equals(schema, a, b, options) {
      if (a.$typeName != schema.typeName || b.$typeName != schema.typeName) {
        return false;
      }
      if (a === b) {
        return true;
      }
      return reflectEquals((0, reflect_js_1.reflect)(schema, a), (0, reflect_js_1.reflect)(schema, b), options);
    }
    __name(equals, "equals");
    function reflectEquals(a, b, opts) {
      if (a.desc.typeName === "google.protobuf.Any" && (opts === null || opts === void 0 ? void 0 : opts.unpackAny) == true) {
        return anyUnpackedEquals(a.message, b.message, opts);
      }
      for (const f of a.fields) {
        if (!fieldEquals(f, a, b, opts)) {
          return false;
        }
      }
      if ((opts === null || opts === void 0 ? void 0 : opts.unknown) == true && !unknownEquals(a, b, opts.registry)) {
        return false;
      }
      if ((opts === null || opts === void 0 ? void 0 : opts.extensions) == true && !extensionsEquals(a, b, opts)) {
        return false;
      }
      return true;
    }
    __name(reflectEquals, "reflectEquals");
    function fieldEquals(f, a, b, opts) {
      if (!a.isSet(f) && !b.isSet(f)) {
        return true;
      }
      if (!a.isSet(f) || !b.isSet(f)) {
        return false;
      }
      switch (f.fieldKind) {
        case "scalar":
          return (0, scalar_js_1.scalarEquals)(f.scalar, a.get(f), b.get(f));
        case "enum":
          return a.get(f) === b.get(f);
        case "message":
          return reflectEquals(a.get(f), b.get(f), opts);
        case "map": {
          const mapA = a.get(f);
          const mapB = b.get(f);
          const keys = [];
          for (const k of mapA.keys()) {
            if (!mapB.has(k)) {
              return false;
            }
            keys.push(k);
          }
          for (const k of mapB.keys()) {
            if (!mapA.has(k)) {
              return false;
            }
          }
          for (const key of keys) {
            const va = mapA.get(key);
            const vb = mapB.get(key);
            if (va === vb) {
              continue;
            }
            switch (f.mapKind) {
              case "enum":
                return false;
              case "message":
                if (!reflectEquals(va, vb, opts)) {
                  return false;
                }
                break;
              case "scalar":
                if (!(0, scalar_js_1.scalarEquals)(f.scalar, va, vb)) {
                  return false;
                }
                break;
            }
          }
          break;
        }
        case "list": {
          const listA = a.get(f);
          const listB = b.get(f);
          if (listA.size != listB.size) {
            return false;
          }
          for (let i = 0; i < listA.size; i++) {
            const va = listA.get(i);
            const vb = listB.get(i);
            if (va === vb) {
              continue;
            }
            switch (f.listKind) {
              case "enum":
                return false;
              case "message":
                if (!reflectEquals(va, vb, opts)) {
                  return false;
                }
                break;
              case "scalar":
                if (!(0, scalar_js_1.scalarEquals)(f.scalar, va, vb)) {
                  return false;
                }
                break;
            }
          }
          break;
        }
      }
      return true;
    }
    __name(fieldEquals, "fieldEquals");
    function anyUnpackedEquals(a, b, opts) {
      if (a.typeUrl !== b.typeUrl) {
        return false;
      }
      const unpackedA = (0, index_js_1.anyUnpack)(a, opts.registry);
      const unpackedB = (0, index_js_1.anyUnpack)(b, opts.registry);
      if (unpackedA && unpackedB) {
        const schema = opts.registry.getMessage(unpackedA.$typeName);
        if (schema) {
          return equals(schema, unpackedA, unpackedB, opts);
        }
      }
      return (0, scalar_js_1.scalarEquals)(descriptors_js_1.ScalarType.BYTES, a.value, b.value);
    }
    __name(anyUnpackedEquals, "anyUnpackedEquals");
    function unknownEquals(a, b, registry) {
      function getTrulyUnknown(msg, registry2) {
        var _a;
        const u = (_a = msg.getUnknown()) !== null && _a !== void 0 ? _a : [];
        return registry2 ? u.filter((uf) => !registry2.getExtensionFor(msg.desc, uf.no)) : u;
      }
      __name(getTrulyUnknown, "getTrulyUnknown");
      const unknownA = getTrulyUnknown(a, registry);
      const unknownB = getTrulyUnknown(b, registry);
      if (unknownA.length != unknownB.length) {
        return false;
      }
      for (let i = 0; i < unknownA.length; i++) {
        const a2 = unknownA[i];
        const b2 = unknownB[i];
        if (a2.no != b2.no) {
          return false;
        }
        if (a2.wireType != b2.wireType) {
          return false;
        }
        if (!(0, scalar_js_1.scalarEquals)(descriptors_js_1.ScalarType.BYTES, a2.data, b2.data)) {
          return false;
        }
      }
      return true;
    }
    __name(unknownEquals, "unknownEquals");
    function extensionsEquals(a, b, opts) {
      function getSetExtensions(msg, registry) {
        var _a;
        return ((_a = msg.getUnknown()) !== null && _a !== void 0 ? _a : []).map((uf) => registry.getExtensionFor(msg.desc, uf.no)).filter((e) => e != void 0).filter((e, index, arr) => arr.indexOf(e) === index);
      }
      __name(getSetExtensions, "getSetExtensions");
      const extensionsA = getSetExtensions(a, opts.registry);
      const extensionsB = getSetExtensions(b, opts.registry);
      if (extensionsA.length != extensionsB.length || extensionsA.some((e) => !extensionsB.includes(e))) {
        return false;
      }
      for (const extension of extensionsA) {
        const [containerA, field] = (0, extensions_js_1.createExtensionContainer)(extension, (0, extensions_js_1.getExtension)(a.message, extension));
        const [containerB] = (0, extensions_js_1.createExtensionContainer)(extension, (0, extensions_js_1.getExtension)(b.message, extension));
        if (!fieldEquals(field, containerA, containerB, opts)) {
          return false;
        }
      }
      return true;
    }
    __name(extensionsEquals, "extensionsEquals");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/fields.js
var require_fields = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/fields.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.isFieldSet = isFieldSet;
    exports.clearField = clearField;
    var unsafe_js_1 = require_unsafe();
    function isFieldSet(message, field) {
      return field.parent.typeName == message.$typeName && (0, unsafe_js_1.unsafeIsSet)(message, field);
    }
    __name(isFieldSet, "isFieldSet");
    function clearField(message, field) {
      if (field.parent.typeName == message.$typeName) {
        (0, unsafe_js_1.unsafeClear)(message, field);
      }
    }
    __name(clearField, "clearField");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wire/size-delimited.js
var require_size_delimited = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wire/size-delimited.js"(exports) {
    "use strict";
    init_esm();
    var __asyncValues = exports && exports.__asyncValues || function(o) {
      if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
      var m = o[Symbol.asyncIterator], i;
      return m ? m.call(o) : (o = typeof __values === "function" ? __values(o) : o[Symbol.iterator](), i = {}, verb("next"), verb("throw"), verb("return"), i[Symbol.asyncIterator] = function() {
        return this;
      }, i);
      function verb(n) {
        i[n] = o[n] && function(v) {
          return new Promise(function(resolve, reject) {
            v = o[n](v), settle(resolve, reject, v.done, v.value);
          });
        };
      }
      __name(verb, "verb");
      function settle(resolve, reject, d, v) {
        Promise.resolve(v).then(function(v2) {
          resolve({ value: v2, done: d });
        }, reject);
      }
      __name(settle, "settle");
    };
    var __await = exports && exports.__await || function(v) {
      return this instanceof __await ? (this.v = v, this) : new __await(v);
    };
    var __asyncGenerator = exports && exports.__asyncGenerator || function(thisArg, _arguments, generator) {
      if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
      var g = generator.apply(thisArg, _arguments || []), i, q = [];
      return i = Object.create((typeof AsyncIterator === "function" ? AsyncIterator : Object).prototype), verb("next"), verb("throw"), verb("return", awaitReturn), i[Symbol.asyncIterator] = function() {
        return this;
      }, i;
      function awaitReturn(f) {
        return function(v) {
          return Promise.resolve(v).then(f, reject);
        };
      }
      __name(awaitReturn, "awaitReturn");
      function verb(n, f) {
        if (g[n]) {
          i[n] = function(v) {
            return new Promise(function(a, b) {
              q.push([n, v, a, b]) > 1 || resume(n, v);
            });
          };
          if (f) i[n] = f(i[n]);
        }
      }
      __name(verb, "verb");
      function resume(n, v) {
        try {
          step(g[n](v));
        } catch (e) {
          settle(q[0][3], e);
        }
      }
      __name(resume, "resume");
      function step(r) {
        r.value instanceof __await ? Promise.resolve(r.value.v).then(fulfill, reject) : settle(q[0][2], r);
      }
      __name(step, "step");
      function fulfill(value) {
        resume("next", value);
      }
      __name(fulfill, "fulfill");
      function reject(value) {
        resume("throw", value);
      }
      __name(reject, "reject");
      function settle(f, v) {
        if (f(v), q.shift(), q.length) resume(q[0][0], q[0][1]);
      }
      __name(settle, "settle");
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.sizeDelimitedEncode = sizeDelimitedEncode;
    exports.sizeDelimitedDecodeStream = sizeDelimitedDecodeStream;
    exports.sizeDelimitedPeek = sizeDelimitedPeek;
    var to_binary_js_1 = require_to_binary();
    var binary_encoding_js_1 = require_binary_encoding();
    var from_binary_js_1 = require_from_binary();
    function sizeDelimitedEncode(messageDesc, message, options) {
      const writer = new binary_encoding_js_1.BinaryWriter();
      writer.bytes((0, to_binary_js_1.toBinary)(messageDesc, message, options));
      return writer.finish();
    }
    __name(sizeDelimitedEncode, "sizeDelimitedEncode");
    function sizeDelimitedDecodeStream(messageDesc, iterable, options) {
      return __asyncGenerator(this, arguments, /* @__PURE__ */ __name(function* sizeDelimitedDecodeStream_1() {
        var _a, e_1, _b, _c;
        function append(buffer2, chunk) {
          const n = new Uint8Array(buffer2.byteLength + chunk.byteLength);
          n.set(buffer2);
          n.set(chunk, buffer2.length);
          return n;
        }
        __name(append, "append");
        let buffer = new Uint8Array(0);
        try {
          for (var _d = true, iterable_1 = __asyncValues(iterable), iterable_1_1; iterable_1_1 = yield __await(iterable_1.next()), _a = iterable_1_1.done, !_a; _d = true) {
            _c = iterable_1_1.value;
            _d = false;
            const chunk = _c;
            buffer = append(buffer, chunk);
            for (; ; ) {
              const size = sizeDelimitedPeek(buffer);
              if (size.eof) {
                break;
              }
              if (size.offset + size.size > buffer.byteLength) {
                break;
              }
              yield yield __await((0, from_binary_js_1.fromBinary)(messageDesc, buffer.subarray(size.offset, size.offset + size.size), options));
              buffer = buffer.subarray(size.offset + size.size);
            }
          }
        } catch (e_1_1) {
          e_1 = { error: e_1_1 };
        } finally {
          try {
            if (!_d && !_a && (_b = iterable_1.return)) yield __await(_b.call(iterable_1));
          } finally {
            if (e_1) throw e_1.error;
          }
        }
        if (buffer.byteLength > 0) {
          throw new Error("incomplete data");
        }
      }, "sizeDelimitedDecodeStream_1"));
    }
    __name(sizeDelimitedDecodeStream, "sizeDelimitedDecodeStream");
    function sizeDelimitedPeek(data) {
      const sizeEof = { eof: true, size: null, offset: null };
      for (let i = 0; i < 10; i++) {
        if (i > data.byteLength) {
          return sizeEof;
        }
        if ((data[i] & 128) == 0) {
          const reader = new binary_encoding_js_1.BinaryReader(data);
          let size;
          try {
            size = reader.uint32();
          } catch (e) {
            if (e instanceof RangeError) {
              return sizeEof;
            }
            throw e;
          }
          return {
            eof: false,
            size,
            offset: reader.pos
          };
        }
      }
      throw new Error("invalid varint");
    }
    __name(sizeDelimitedPeek, "sizeDelimitedPeek");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wire/index.js
var require_wire = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/wire/index.js"(exports) {
    "use strict";
    init_esm();
    var __createBinding = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: /* @__PURE__ */ __name(function() {
          return m[k];
        }, "get") };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    });
    var __exportStar = exports && exports.__exportStar || function(m, exports2) {
      for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p)) __createBinding(exports2, m, p);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    __exportStar(require_binary_encoding(), exports);
    __exportStar(require_base64_encoding(), exports);
    __exportStar(require_text_encoding(), exports);
    __exportStar(require_text_format(), exports);
    __exportStar(require_size_delimited(), exports);
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/to-json.js
var require_to_json = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/to-json.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.toJson = toJson;
    exports.toJsonString = toJsonString;
    exports.enumToJson = enumToJson;
    var descriptors_js_1 = require_descriptors();
    var names_js_1 = require_names();
    var reflect_js_1 = require_reflect();
    var index_js_1 = require_wkt();
    var wrappers_js_1 = require_wrappers();
    var index_js_2 = require_wire();
    var extensions_js_1 = require_extensions();
    var reflect_check_js_1 = require_reflect_check();
    var LEGACY_REQUIRED = 3;
    var IMPLICIT = 2;
    var jsonWriteDefaults = {
      alwaysEmitImplicit: false,
      enumAsInteger: false,
      useProtoFieldName: false
    };
    function makeWriteOptions(options) {
      return options ? Object.assign(Object.assign({}, jsonWriteDefaults), options) : jsonWriteDefaults;
    }
    __name(makeWriteOptions, "makeWriteOptions");
    function toJson(schema, message, options) {
      return reflectToJson((0, reflect_js_1.reflect)(schema, message), makeWriteOptions(options));
    }
    __name(toJson, "toJson");
    function toJsonString(schema, message, options) {
      var _a;
      const jsonValue = toJson(schema, message, options);
      return JSON.stringify(jsonValue, null, (_a = options === null || options === void 0 ? void 0 : options.prettySpaces) !== null && _a !== void 0 ? _a : 0);
    }
    __name(toJsonString, "toJsonString");
    function enumToJson(descEnum, value) {
      var _a;
      if (descEnum.typeName == "google.protobuf.NullValue") {
        return null;
      }
      const name = (_a = descEnum.value[value]) === null || _a === void 0 ? void 0 : _a.name;
      if (name === void 0) {
        throw new Error(`${value} is not a value in ${descEnum}`);
      }
      return name;
    }
    __name(enumToJson, "enumToJson");
    function reflectToJson(msg, opts) {
      var _a;
      const wktJson = tryWktToJson(msg, opts);
      if (wktJson !== void 0)
        return wktJson;
      const json = {};
      for (const f of msg.sortedFields) {
        if (!msg.isSet(f)) {
          if (f.presence == LEGACY_REQUIRED) {
            throw new Error(`cannot encode ${f} to JSON: required field not set`);
          }
          if (!opts.alwaysEmitImplicit || f.presence !== IMPLICIT) {
            continue;
          }
        }
        const jsonValue = fieldToJson(f, msg.get(f), opts);
        if (jsonValue !== void 0) {
          json[jsonName(f, opts)] = jsonValue;
        }
      }
      if (opts.registry) {
        const tagSeen = /* @__PURE__ */ new Set();
        for (const { no } of (_a = msg.getUnknown()) !== null && _a !== void 0 ? _a : []) {
          if (!tagSeen.has(no)) {
            tagSeen.add(no);
            const extension = opts.registry.getExtensionFor(msg.desc, no);
            if (!extension) {
              continue;
            }
            const value = (0, extensions_js_1.getExtension)(msg.message, extension);
            const [container, field] = (0, extensions_js_1.createExtensionContainer)(extension, value);
            const jsonValue = fieldToJson(field, container.get(field), opts);
            if (jsonValue !== void 0) {
              json[extension.jsonName] = jsonValue;
            }
          }
        }
      }
      return json;
    }
    __name(reflectToJson, "reflectToJson");
    function fieldToJson(f, val, opts) {
      switch (f.fieldKind) {
        case "scalar":
          return scalarToJson(f, val);
        case "message":
          return reflectToJson(val, opts);
        case "enum":
          return enumToJsonInternal(f.enum, val, opts.enumAsInteger);
        case "list":
          return listToJson(val, opts);
        case "map":
          return mapToJson(val, opts);
      }
    }
    __name(fieldToJson, "fieldToJson");
    function mapToJson(map, opts) {
      const f = map.field();
      const jsonObj = {};
      switch (f.mapKind) {
        case "scalar":
          for (const [entryKey, entryValue] of map) {
            jsonObj[entryKey] = scalarToJson(f, entryValue);
          }
          break;
        case "message":
          for (const [entryKey, entryValue] of map) {
            jsonObj[entryKey] = reflectToJson(entryValue, opts);
          }
          break;
        case "enum":
          for (const [entryKey, entryValue] of map) {
            jsonObj[entryKey] = enumToJsonInternal(f.enum, entryValue, opts.enumAsInteger);
          }
          break;
      }
      return opts.alwaysEmitImplicit || map.size > 0 ? jsonObj : void 0;
    }
    __name(mapToJson, "mapToJson");
    function listToJson(list, opts) {
      const f = list.field();
      const jsonArr = [];
      switch (f.listKind) {
        case "scalar":
          for (const item of list) {
            jsonArr.push(scalarToJson(f, item));
          }
          break;
        case "enum":
          for (const item of list) {
            jsonArr.push(enumToJsonInternal(f.enum, item, opts.enumAsInteger));
          }
          break;
        case "message":
          for (const item of list) {
            jsonArr.push(reflectToJson(item, opts));
          }
          break;
      }
      return opts.alwaysEmitImplicit || jsonArr.length > 0 ? jsonArr : void 0;
    }
    __name(listToJson, "listToJson");
    function enumToJsonInternal(desc, value, enumAsInteger) {
      var _a;
      if (typeof value != "number") {
        throw new Error(`cannot encode ${desc} to JSON: expected number, got ${(0, reflect_check_js_1.formatVal)(value)}`);
      }
      if (desc.typeName == "google.protobuf.NullValue") {
        return null;
      }
      if (enumAsInteger) {
        return value;
      }
      const val = desc.value[value];
      return (_a = val === null || val === void 0 ? void 0 : val.name) !== null && _a !== void 0 ? _a : value;
    }
    __name(enumToJsonInternal, "enumToJsonInternal");
    function scalarToJson(field, value) {
      var _a, _b, _c, _d, _e, _f;
      switch (field.scalar) {
        // int32, fixed32, uint32: JSON value will be a decimal number. Either numbers or strings are accepted.
        case descriptors_js_1.ScalarType.INT32:
        case descriptors_js_1.ScalarType.SFIXED32:
        case descriptors_js_1.ScalarType.SINT32:
        case descriptors_js_1.ScalarType.FIXED32:
        case descriptors_js_1.ScalarType.UINT32:
          if (typeof value != "number") {
            throw new Error(`cannot encode ${field} to JSON: ${(_a = (0, reflect_check_js_1.checkField)(field, value)) === null || _a === void 0 ? void 0 : _a.message}`);
          }
          return value;
        // float, double: JSON value will be a number or one of the special string values "NaN", "Infinity", and "-Infinity".
        // Either numbers or strings are accepted. Exponent notation is also accepted.
        case descriptors_js_1.ScalarType.FLOAT:
        case descriptors_js_1.ScalarType.DOUBLE:
          if (typeof value != "number") {
            throw new Error(`cannot encode ${field} to JSON: ${(_b = (0, reflect_check_js_1.checkField)(field, value)) === null || _b === void 0 ? void 0 : _b.message}`);
          }
          if (Number.isNaN(value))
            return "NaN";
          if (value === Number.POSITIVE_INFINITY)
            return "Infinity";
          if (value === Number.NEGATIVE_INFINITY)
            return "-Infinity";
          return value;
        // string:
        case descriptors_js_1.ScalarType.STRING:
          if (typeof value != "string") {
            throw new Error(`cannot encode ${field} to JSON: ${(_c = (0, reflect_check_js_1.checkField)(field, value)) === null || _c === void 0 ? void 0 : _c.message}`);
          }
          return value;
        // bool:
        case descriptors_js_1.ScalarType.BOOL:
          if (typeof value != "boolean") {
            throw new Error(`cannot encode ${field} to JSON: ${(_d = (0, reflect_check_js_1.checkField)(field, value)) === null || _d === void 0 ? void 0 : _d.message}`);
          }
          return value;
        // JSON value will be a decimal string. Either numbers or strings are accepted.
        case descriptors_js_1.ScalarType.UINT64:
        case descriptors_js_1.ScalarType.FIXED64:
        case descriptors_js_1.ScalarType.INT64:
        case descriptors_js_1.ScalarType.SFIXED64:
        case descriptors_js_1.ScalarType.SINT64:
          if (typeof value == "bigint" || typeof value == "string" || typeof value == "number" && Number.isInteger(value)) {
            return value.toString();
          }
          throw new Error(`cannot encode ${field} to JSON: ${(_e = (0, reflect_check_js_1.checkField)(field, value)) === null || _e === void 0 ? void 0 : _e.message}`);
        // bytes: JSON value will be the data encoded as a string using standard base64 encoding with paddings.
        // Either standard or URL-safe base64 encoding with/without paddings are accepted.
        case descriptors_js_1.ScalarType.BYTES:
          if (value instanceof Uint8Array) {
            return (0, index_js_2.base64Encode)(value);
          }
          throw new Error(`cannot encode ${field} to JSON: ${(_f = (0, reflect_check_js_1.checkField)(field, value)) === null || _f === void 0 ? void 0 : _f.message}`);
      }
    }
    __name(scalarToJson, "scalarToJson");
    function jsonName(f, opts) {
      return opts.useProtoFieldName ? f.name : f.jsonName;
    }
    __name(jsonName, "jsonName");
    function tryWktToJson(msg, opts) {
      if (!msg.desc.typeName.startsWith("google.protobuf.")) {
        return void 0;
      }
      switch (msg.desc.typeName) {
        case "google.protobuf.Any":
          return anyToJson(msg.message, opts);
        case "google.protobuf.Timestamp":
          return timestampToJson(msg.message);
        case "google.protobuf.Duration":
          return durationToJson(msg.message);
        case "google.protobuf.FieldMask":
          return fieldMaskToJson(msg.message);
        case "google.protobuf.Struct":
          return structToJson(msg.message);
        case "google.protobuf.Value":
          return valueToJson(msg.message);
        case "google.protobuf.ListValue":
          return listValueToJson(msg.message);
        default:
          if ((0, wrappers_js_1.isWrapperDesc)(msg.desc)) {
            const valueField = msg.desc.fields[0];
            return scalarToJson(valueField, msg.get(valueField));
          }
          return void 0;
      }
    }
    __name(tryWktToJson, "tryWktToJson");
    function anyToJson(val, opts) {
      if (val.typeUrl === "") {
        return {};
      }
      const { registry } = opts;
      let message;
      let desc;
      if (registry) {
        message = (0, index_js_1.anyUnpack)(val, registry);
        if (message) {
          desc = registry.getMessage(message.$typeName);
        }
      }
      if (!desc || !message) {
        throw new Error(`cannot encode message ${val.$typeName} to JSON: "${val.typeUrl}" is not in the type registry`);
      }
      const reflected = (0, reflect_js_1.reflect)(desc, message);
      const json = (0, wrappers_js_1.hasCustomJsonRepresentation)(desc) ? { value: tryWktToJson(reflected, opts) } : reflectToJson(reflected, opts);
      json["@type"] = val.typeUrl;
      return json;
    }
    __name(anyToJson, "anyToJson");
    function durationToJson(val) {
      const seconds = Number(val.seconds);
      const nanos = val.nanos;
      if (seconds > 315576e6 || seconds < -315576e6) {
        throw new Error(`cannot encode message ${val.$typeName} to JSON: value out of range`);
      }
      if (seconds > 0 && nanos < 0 || seconds < 0 && nanos > 0) {
        throw new Error(`cannot encode message ${val.$typeName} to JSON: nanos sign must match seconds sign`);
      }
      let text = val.seconds.toString();
      if (nanos !== 0) {
        let nanosStr = Math.abs(nanos).toString();
        nanosStr = "0".repeat(9 - nanosStr.length) + nanosStr;
        if (nanosStr.substring(3) === "000000") {
          nanosStr = nanosStr.substring(0, 3);
        } else if (nanosStr.substring(6) === "000") {
          nanosStr = nanosStr.substring(0, 6);
        }
        text += "." + nanosStr;
        if (nanos < 0 && seconds == 0) {
          text = "-" + text;
        }
      }
      return text + "s";
    }
    __name(durationToJson, "durationToJson");
    function fieldMaskToJson(val) {
      return val.paths.map((p) => {
        if ((0, names_js_1.protoSnakeCase)((0, names_js_1.protoCamelCase)(p)) !== p) {
          throw new Error(`cannot encode message ${val.$typeName} to JSON: lowerCamelCase of path name "${p}" is irreversible`);
        }
        return (0, names_js_1.protoCamelCase)(p);
      }).join(",");
    }
    __name(fieldMaskToJson, "fieldMaskToJson");
    function structToJson(val) {
      const json = {};
      for (const [k, v] of Object.entries(val.fields)) {
        json[k] = valueToJson(v);
      }
      return json;
    }
    __name(structToJson, "structToJson");
    function valueToJson(val) {
      switch (val.kind.case) {
        case "nullValue":
          return null;
        case "numberValue":
          if (!Number.isFinite(val.kind.value)) {
            throw new Error(`${val.$typeName} cannot be NaN or Infinity`);
          }
          return val.kind.value;
        case "boolValue":
          return val.kind.value;
        case "stringValue":
          return val.kind.value;
        case "structValue":
          return structToJson(val.kind.value);
        case "listValue":
          return listValueToJson(val.kind.value);
        default:
          throw new Error(`${val.$typeName} must have a value`);
      }
    }
    __name(valueToJson, "valueToJson");
    function listValueToJson(val) {
      return val.values.map(valueToJson);
    }
    __name(listValueToJson, "listValueToJson");
    function timestampToJson(val) {
      const ms = Number(val.seconds) * 1e3;
      if (ms < Date.parse("0001-01-01T00:00:00Z") || ms > Date.parse("9999-12-31T23:59:59Z")) {
        throw new Error(`cannot encode message ${val.$typeName} to JSON: must be from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59Z inclusive`);
      }
      if (val.nanos < 0) {
        throw new Error(`cannot encode message ${val.$typeName} to JSON: nanos must not be negative`);
      }
      if (val.nanos > 999999999) {
        throw new Error(`cannot encode message ${val.$typeName} to JSON: nanos must not be greater than 99999999`);
      }
      let z = "Z";
      if (val.nanos > 0) {
        const nanosStr = (val.nanos + 1e9).toString().substring(1);
        if (nanosStr.substring(3) === "000000") {
          z = "." + nanosStr.substring(0, 3) + "Z";
        } else if (nanosStr.substring(6) === "000") {
          z = "." + nanosStr.substring(0, 6) + "Z";
        } else {
          z = "." + nanosStr + "Z";
        }
      }
      return new Date(ms).toISOString().replace(".000Z", z);
    }
    __name(timestampToJson, "timestampToJson");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/from-json.js
var require_from_json = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/from-json.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.fromJsonString = fromJsonString;
    exports.mergeFromJsonString = mergeFromJsonString;
    exports.fromJson = fromJson;
    exports.mergeFromJson = mergeFromJson;
    exports.enumFromJson = enumFromJson;
    exports.isEnumJson = isEnumJson;
    var descriptors_js_1 = require_descriptors();
    var proto_int64_js_1 = require_proto_int64();
    var create_js_1 = require_create();
    var reflect_js_1 = require_reflect();
    var error_js_1 = require_error();
    var reflect_check_js_1 = require_reflect_check();
    var names_js_1 = require_names();
    var base64_encoding_js_1 = require_base64_encoding();
    var index_js_1 = require_wkt();
    var extensions_js_1 = require_extensions();
    var jsonReadDefaults = {
      ignoreUnknownFields: false
    };
    function makeReadOptions(options) {
      return options ? Object.assign(Object.assign({}, jsonReadDefaults), options) : jsonReadDefaults;
    }
    __name(makeReadOptions, "makeReadOptions");
    function fromJsonString(schema, json, options) {
      return fromJson(schema, parseJsonString(json, schema.typeName), options);
    }
    __name(fromJsonString, "fromJsonString");
    function mergeFromJsonString(schema, target, json, options) {
      return mergeFromJson(schema, target, parseJsonString(json, schema.typeName), options);
    }
    __name(mergeFromJsonString, "mergeFromJsonString");
    function fromJson(schema, json, options) {
      const msg = (0, reflect_js_1.reflect)(schema);
      try {
        readMessage(msg, json, makeReadOptions(options));
      } catch (e) {
        if ((0, error_js_1.isFieldError)(e)) {
          throw new Error(`cannot decode ${e.field()} from JSON: ${e.message}`, {
            cause: e
          });
        }
        throw e;
      }
      return msg.message;
    }
    __name(fromJson, "fromJson");
    function mergeFromJson(schema, target, json, options) {
      try {
        readMessage((0, reflect_js_1.reflect)(schema, target), json, makeReadOptions(options));
      } catch (e) {
        if ((0, error_js_1.isFieldError)(e)) {
          throw new Error(`cannot decode ${e.field()} from JSON: ${e.message}`, {
            cause: e
          });
        }
        throw e;
      }
      return target;
    }
    __name(mergeFromJson, "mergeFromJson");
    function enumFromJson(descEnum, json) {
      return readEnum(descEnum, json, false);
    }
    __name(enumFromJson, "enumFromJson");
    function isEnumJson(descEnum, value) {
      return void 0 !== descEnum.values.find((v) => v.name === value);
    }
    __name(isEnumJson, "isEnumJson");
    var messageJsonFields = /* @__PURE__ */ new WeakMap();
    function getJsonField(desc, jsonKey) {
      var _a;
      if (!messageJsonFields.has(desc)) {
        const jsonNames = /* @__PURE__ */ new Map();
        for (const field of desc.fields) {
          jsonNames.set(field.name, field).set(field.jsonName, field);
        }
        messageJsonFields.set(desc, jsonNames);
      }
      return (_a = messageJsonFields.get(desc)) === null || _a === void 0 ? void 0 : _a.get(jsonKey);
    }
    __name(getJsonField, "getJsonField");
    function readMessage(msg, json, opts) {
      var _a;
      if (tryWktFromJson(msg, json, opts)) {
        return;
      }
      if (json == null || Array.isArray(json) || typeof json != "object") {
        throw new Error(`cannot decode ${msg.desc} from JSON: ${(0, reflect_check_js_1.formatVal)(json)}`);
      }
      const oneofSeen = /* @__PURE__ */ new Map();
      for (const [jsonKey, jsonValue] of Object.entries(json)) {
        const field = getJsonField(msg.desc, jsonKey);
        if (field) {
          if (field.oneof) {
            if (jsonValue === null && field.fieldKind == "scalar") {
              continue;
            }
            const seen = oneofSeen.get(field.oneof);
            if (seen !== void 0) {
              throw new error_js_1.FieldError(field.oneof, `oneof set multiple times by ${seen.name} and ${field.name}`);
            }
            oneofSeen.set(field.oneof, field);
          }
          readField(msg, field, jsonValue, opts);
        } else {
          let extension = void 0;
          if (jsonKey.startsWith("[") && jsonKey.endsWith("]") && // biome-ignore lint/suspicious/noAssignInExpressions: no
          (extension = (_a = opts.registry) === null || _a === void 0 ? void 0 : _a.getExtension(jsonKey.substring(1, jsonKey.length - 1))) && extension.extendee.typeName === msg.desc.typeName) {
            const [container, field2, get] = (0, extensions_js_1.createExtensionContainer)(extension);
            readField(container, field2, jsonValue, opts);
            (0, extensions_js_1.setExtension)(msg.message, extension, get());
          }
          if (!extension && !opts.ignoreUnknownFields) {
            throw new Error(`cannot decode ${msg.desc} from JSON: key "${jsonKey}" is unknown`);
          }
        }
      }
    }
    __name(readMessage, "readMessage");
    function readField(msg, field, json, opts) {
      switch (field.fieldKind) {
        case "scalar":
          readScalarField(msg, field, json);
          break;
        case "enum":
          readEnumField(msg, field, json, opts);
          break;
        case "message":
          readMessageField(msg, field, json, opts);
          break;
        case "list":
          readListField(msg.get(field), json, opts);
          break;
        case "map":
          readMapField(msg.get(field), json, opts);
          break;
      }
    }
    __name(readField, "readField");
    function readListOrMapItem(field, json, opts) {
      if (field.scalar && json !== null) {
        return scalarFromJson(field, json);
      }
      if (field.message && !isResetSentinelNullValue(field, json)) {
        const msgValue = (0, reflect_js_1.reflect)(field.message);
        readMessage(msgValue, json, opts);
        return msgValue;
      }
      if (field.enum && !isResetSentinelNullValue(field, json)) {
        return readEnum(field.enum, json, opts.ignoreUnknownFields);
      }
      throw new error_js_1.FieldError(field, `${field.fieldKind === "list" ? "list item" : "map value"} must not be null`);
    }
    __name(readListOrMapItem, "readListOrMapItem");
    function readMapField(map, json, opts) {
      if (json === null) {
        return;
      }
      const field = map.field();
      if (typeof json != "object" || Array.isArray(json)) {
        throw new error_js_1.FieldError(field, "expected object, got " + (0, reflect_check_js_1.formatVal)(json));
      }
      for (const [jsonMapKey, jsonMapValue] of Object.entries(json)) {
        const key = mapKeyFromJson(field.mapKey, jsonMapKey);
        const value = readListOrMapItem(field, jsonMapValue, opts);
        if (value !== tokenIgnoredUnknownEnum) {
          map.set(key, value);
        }
      }
    }
    __name(readMapField, "readMapField");
    function readListField(list, json, opts) {
      if (json === null) {
        return;
      }
      const field = list.field();
      if (!Array.isArray(json)) {
        throw new error_js_1.FieldError(field, "expected Array, got " + (0, reflect_check_js_1.formatVal)(json));
      }
      for (const jsonItem of json) {
        const value = readListOrMapItem(field, jsonItem, opts);
        if (value !== tokenIgnoredUnknownEnum) {
          list.add(value);
        }
      }
    }
    __name(readListField, "readListField");
    function readMessageField(msg, field, json, opts) {
      if (isResetSentinelNullValue(field, json)) {
        msg.clear(field);
        return;
      }
      const msgValue = msg.isSet(field) ? msg.get(field) : (0, reflect_js_1.reflect)(field.message);
      readMessage(msgValue, json, opts);
      msg.set(field, msgValue);
    }
    __name(readMessageField, "readMessageField");
    function readEnumField(msg, field, json, opts) {
      if (isResetSentinelNullValue(field, json)) {
        msg.clear(field);
        return;
      }
      const enumValue = readEnum(field.enum, json, opts.ignoreUnknownFields);
      if (enumValue !== tokenIgnoredUnknownEnum) {
        msg.set(field, enumValue);
      }
    }
    __name(readEnumField, "readEnumField");
    function readScalarField(msg, field, json) {
      if (json === null) {
        msg.clear(field);
      } else {
        msg.set(field, scalarFromJson(field, json));
      }
    }
    __name(readScalarField, "readScalarField");
    function isResetSentinelNullValue(field, json) {
      var _a, _b;
      return json === null && ((_a = field.message) === null || _a === void 0 ? void 0 : _a.typeName) != "google.protobuf.Value" && ((_b = field.enum) === null || _b === void 0 ? void 0 : _b.typeName) != "google.protobuf.NullValue";
    }
    __name(isResetSentinelNullValue, "isResetSentinelNullValue");
    var tokenIgnoredUnknownEnum = Symbol();
    function readEnum(desc, json, ignoreUnknownFields) {
      if (json === null) {
        return desc.values[0].number;
      }
      switch (typeof json) {
        case "number":
          if (Number.isInteger(json)) {
            return json;
          }
          break;
        case "string":
          const value = desc.values.find((ev) => ev.name === json);
          if (value !== void 0) {
            return value.number;
          }
          if (ignoreUnknownFields) {
            return tokenIgnoredUnknownEnum;
          }
          break;
      }
      throw new Error(`cannot decode ${desc} from JSON: ${(0, reflect_check_js_1.formatVal)(json)}`);
    }
    __name(readEnum, "readEnum");
    function scalarFromJson(field, json) {
      switch (field.scalar) {
        // float, double: JSON value will be a number or one of the special string values "NaN", "Infinity", and "-Infinity".
        // Either numbers or strings are accepted. Exponent notation is also accepted.
        case descriptors_js_1.ScalarType.DOUBLE:
        case descriptors_js_1.ScalarType.FLOAT:
          if (json === "NaN")
            return NaN;
          if (json === "Infinity")
            return Number.POSITIVE_INFINITY;
          if (json === "-Infinity")
            return Number.NEGATIVE_INFINITY;
          if (typeof json == "number") {
            if (Number.isNaN(json)) {
              throw new error_js_1.FieldError(field, "unexpected NaN number");
            }
            if (!Number.isFinite(json)) {
              throw new error_js_1.FieldError(field, "unexpected infinite number");
            }
            break;
          }
          if (typeof json == "string") {
            if (json === "") {
              break;
            }
            if (json.trim().length !== json.length) {
              break;
            }
            const float = Number(json);
            if (!Number.isFinite(float)) {
              break;
            }
            return float;
          }
          break;
        // int32, fixed32, uint32: JSON value will be a decimal number. Either numbers or strings are accepted.
        case descriptors_js_1.ScalarType.INT32:
        case descriptors_js_1.ScalarType.FIXED32:
        case descriptors_js_1.ScalarType.SFIXED32:
        case descriptors_js_1.ScalarType.SINT32:
        case descriptors_js_1.ScalarType.UINT32:
          return int32FromJson(json);
        // bytes: JSON value will be the data encoded as a string using standard base64 encoding with paddings.
        // Either standard or URL-safe base64 encoding with/without paddings are accepted.
        case descriptors_js_1.ScalarType.BYTES:
          if (typeof json == "string") {
            if (json === "") {
              return new Uint8Array(0);
            }
            try {
              return (0, base64_encoding_js_1.base64Decode)(json);
            } catch (e) {
              const message = e instanceof Error ? e.message : String(e);
              throw new error_js_1.FieldError(field, message);
            }
          }
          break;
      }
      return json;
    }
    __name(scalarFromJson, "scalarFromJson");
    function mapKeyFromJson(type, jsonString) {
      switch (type) {
        case descriptors_js_1.ScalarType.BOOL:
          switch (jsonString) {
            case "true":
              return true;
            case "false":
              return false;
          }
          return jsonString;
        case descriptors_js_1.ScalarType.INT32:
        case descriptors_js_1.ScalarType.FIXED32:
        case descriptors_js_1.ScalarType.UINT32:
        case descriptors_js_1.ScalarType.SFIXED32:
        case descriptors_js_1.ScalarType.SINT32:
          return int32FromJson(jsonString);
        default:
          return jsonString;
      }
    }
    __name(mapKeyFromJson, "mapKeyFromJson");
    function int32FromJson(json) {
      if (typeof json == "string") {
        if (json === "") {
          return json;
        }
        if (json.trim().length !== json.length) {
          return json;
        }
        const num = Number(json);
        if (Number.isNaN(num)) {
          return json;
        }
        return num;
      }
      return json;
    }
    __name(int32FromJson, "int32FromJson");
    function parseJsonString(jsonString, typeName) {
      try {
        return JSON.parse(jsonString);
      } catch (e) {
        const message = e instanceof Error ? e.message : String(e);
        throw new Error(
          `cannot decode message ${typeName} from JSON: ${message}`,
          // @ts-expect-error we use the ES2022 error CTOR option "cause" for better stack traces
          { cause: e }
        );
      }
    }
    __name(parseJsonString, "parseJsonString");
    function tryWktFromJson(msg, jsonValue, opts) {
      if (!msg.desc.typeName.startsWith("google.protobuf.")) {
        return false;
      }
      switch (msg.desc.typeName) {
        case "google.protobuf.Any":
          anyFromJson(msg.message, jsonValue, opts);
          return true;
        case "google.protobuf.Timestamp":
          timestampFromJson(msg.message, jsonValue);
          return true;
        case "google.protobuf.Duration":
          durationFromJson(msg.message, jsonValue);
          return true;
        case "google.protobuf.FieldMask":
          fieldMaskFromJson(msg.message, jsonValue);
          return true;
        case "google.protobuf.Struct":
          structFromJson(msg.message, jsonValue);
          return true;
        case "google.protobuf.Value":
          valueFromJson(msg.message, jsonValue);
          return true;
        case "google.protobuf.ListValue":
          listValueFromJson(msg.message, jsonValue);
          return true;
        default:
          if ((0, index_js_1.isWrapperDesc)(msg.desc)) {
            const valueField = msg.desc.fields[0];
            if (jsonValue === null) {
              msg.clear(valueField);
            } else {
              msg.set(valueField, scalarFromJson(valueField, jsonValue));
            }
            return true;
          }
          return false;
      }
    }
    __name(tryWktFromJson, "tryWktFromJson");
    function anyFromJson(any, json, opts) {
      var _a;
      if (json === null || Array.isArray(json) || typeof json != "object") {
        throw new Error(`cannot decode message ${any.$typeName} from JSON: expected object but got ${(0, reflect_check_js_1.formatVal)(json)}`);
      }
      if (Object.keys(json).length == 0) {
        return;
      }
      const typeUrl = json["@type"];
      if (typeof typeUrl != "string" || typeUrl == "") {
        throw new Error(`cannot decode message ${any.$typeName} from JSON: "@type" is empty`);
      }
      const typeName = typeUrl.includes("/") ? typeUrl.substring(typeUrl.lastIndexOf("/") + 1) : typeUrl;
      if (!typeName.length) {
        throw new Error(`cannot decode message ${any.$typeName} from JSON: "@type" is invalid`);
      }
      const desc = (_a = opts.registry) === null || _a === void 0 ? void 0 : _a.getMessage(typeName);
      if (!desc) {
        throw new Error(`cannot decode message ${any.$typeName} from JSON: ${typeUrl} is not in the type registry`);
      }
      const msg = (0, reflect_js_1.reflect)(desc);
      if ((0, index_js_1.hasCustomJsonRepresentation)(desc) && Object.prototype.hasOwnProperty.call(json, "value")) {
        const value = json.value;
        readMessage(msg, value, opts);
      } else {
        const copy = Object.assign({}, json);
        delete copy["@type"];
        readMessage(msg, copy, opts);
      }
      (0, index_js_1.anyPack)(msg.desc, msg.message, any);
    }
    __name(anyFromJson, "anyFromJson");
    function timestampFromJson(timestamp, json) {
      if (typeof json !== "string") {
        throw new Error(`cannot decode message ${timestamp.$typeName} from JSON: ${(0, reflect_check_js_1.formatVal)(json)}`);
      }
      const matches = json.match(/^([0-9]{4})-([0-9]{2})-([0-9]{2})T([0-9]{2}):([0-9]{2}):([0-9]{2})(?:\.([0-9]{1,9}))?(?:Z|([+-][0-9][0-9]:[0-9][0-9]))$/);
      if (!matches) {
        throw new Error(`cannot decode message ${timestamp.$typeName} from JSON: invalid RFC 3339 string`);
      }
      const ms = Date.parse(
        // biome-ignore format: want this to read well
        matches[1] + "-" + matches[2] + "-" + matches[3] + "T" + matches[4] + ":" + matches[5] + ":" + matches[6] + (matches[8] ? matches[8] : "Z")
      );
      if (Number.isNaN(ms)) {
        throw new Error(`cannot decode message ${timestamp.$typeName} from JSON: invalid RFC 3339 string`);
      }
      if (ms < Date.parse("0001-01-01T00:00:00Z") || ms > Date.parse("9999-12-31T23:59:59Z")) {
        throw new Error(`cannot decode message ${timestamp.$typeName} from JSON: must be from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59Z inclusive`);
      }
      timestamp.seconds = proto_int64_js_1.protoInt64.parse(ms / 1e3);
      timestamp.nanos = 0;
      if (matches[7]) {
        timestamp.nanos = parseInt("1" + matches[7] + "0".repeat(9 - matches[7].length)) - 1e9;
      }
    }
    __name(timestampFromJson, "timestampFromJson");
    function durationFromJson(duration, json) {
      if (typeof json !== "string") {
        throw new Error(`cannot decode message ${duration.$typeName} from JSON: ${(0, reflect_check_js_1.formatVal)(json)}`);
      }
      const match = json.match(/^(-?[0-9]+)(?:\.([0-9]+))?s/);
      if (match === null) {
        throw new Error(`cannot decode message ${duration.$typeName} from JSON: ${(0, reflect_check_js_1.formatVal)(json)}`);
      }
      const longSeconds = Number(match[1]);
      if (longSeconds > 315576e6 || longSeconds < -315576e6) {
        throw new Error(`cannot decode message ${duration.$typeName} from JSON: ${(0, reflect_check_js_1.formatVal)(json)}`);
      }
      duration.seconds = proto_int64_js_1.protoInt64.parse(longSeconds);
      if (typeof match[2] !== "string") {
        return;
      }
      const nanosStr = match[2] + "0".repeat(9 - match[2].length);
      duration.nanos = parseInt(nanosStr);
      if (longSeconds < 0 || Object.is(longSeconds, -0)) {
        duration.nanos = -duration.nanos;
      }
    }
    __name(durationFromJson, "durationFromJson");
    function fieldMaskFromJson(fieldMask, json) {
      if (typeof json !== "string") {
        throw new Error(`cannot decode message ${fieldMask.$typeName} from JSON: ${(0, reflect_check_js_1.formatVal)(json)}`);
      }
      if (json === "") {
        return;
      }
      fieldMask.paths = json.split(",").map((path) => {
        if (path.includes("_")) {
          throw new Error(`cannot decode message ${fieldMask.$typeName} from JSON: path names must be lowerCamelCase`);
        }
        return (0, names_js_1.protoSnakeCase)(path);
      });
    }
    __name(fieldMaskFromJson, "fieldMaskFromJson");
    function structFromJson(struct, json) {
      if (typeof json != "object" || json == null || Array.isArray(json)) {
        throw new Error(`cannot decode message ${struct.$typeName} from JSON ${(0, reflect_check_js_1.formatVal)(json)}`);
      }
      for (const [k, v] of Object.entries(json)) {
        const parsedV = (0, create_js_1.create)(index_js_1.ValueSchema);
        valueFromJson(parsedV, v);
        struct.fields[k] = parsedV;
      }
    }
    __name(structFromJson, "structFromJson");
    function valueFromJson(value, json) {
      switch (typeof json) {
        case "number":
          value.kind = { case: "numberValue", value: json };
          break;
        case "string":
          value.kind = { case: "stringValue", value: json };
          break;
        case "boolean":
          value.kind = { case: "boolValue", value: json };
          break;
        case "object":
          if (json === null) {
            value.kind = { case: "nullValue", value: index_js_1.NullValue.NULL_VALUE };
          } else if (Array.isArray(json)) {
            const listValue = (0, create_js_1.create)(index_js_1.ListValueSchema);
            listValueFromJson(listValue, json);
            value.kind = { case: "listValue", value: listValue };
          } else {
            const struct = (0, create_js_1.create)(index_js_1.StructSchema);
            structFromJson(struct, json);
            value.kind = { case: "structValue", value: struct };
          }
          break;
        default:
          throw new Error(`cannot decode message ${value.$typeName} from JSON ${(0, reflect_check_js_1.formatVal)(json)}`);
      }
      return value;
    }
    __name(valueFromJson, "valueFromJson");
    function listValueFromJson(listValue, json) {
      if (!Array.isArray(json)) {
        throw new Error(`cannot decode message ${listValue.$typeName} from JSON ${(0, reflect_check_js_1.formatVal)(json)}`);
      }
      for (const e of json) {
        const value = (0, create_js_1.create)(index_js_1.ValueSchema);
        valueFromJson(value, e);
        listValue.values.push(value);
      }
    }
    __name(listValueFromJson, "listValueFromJson");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/merge.js
var require_merge = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/merge.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.merge = merge;
    var reflect_js_1 = require_reflect();
    function merge(schema, target, source) {
      reflectMerge((0, reflect_js_1.reflect)(schema, target), (0, reflect_js_1.reflect)(schema, source));
    }
    __name(merge, "merge");
    function reflectMerge(target, source) {
      var _a;
      var _b;
      const sourceUnknown = source.message.$unknown;
      if (sourceUnknown !== void 0 && sourceUnknown.length > 0) {
        (_a = (_b = target.message).$unknown) !== null && _a !== void 0 ? _a : _b.$unknown = [];
        target.message.$unknown.push(...sourceUnknown);
      }
      for (const f of target.fields) {
        if (!source.isSet(f)) {
          continue;
        }
        switch (f.fieldKind) {
          case "scalar":
          case "enum":
            target.set(f, source.get(f));
            break;
          case "message":
            if (target.isSet(f)) {
              reflectMerge(target.get(f), source.get(f));
            } else {
              target.set(f, source.get(f));
            }
            break;
          case "list":
            const list = target.get(f);
            for (const e of source.get(f)) {
              list.add(e);
            }
            break;
          case "map":
            const map = target.get(f);
            for (const [k, v] of source.get(f)) {
              map.set(k, v);
            }
            break;
        }
      }
    }
    __name(reflectMerge, "reflectMerge");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/index.js
var require_cjs = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/index.js"(exports) {
    "use strict";
    init_esm();
    var __createBinding = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: /* @__PURE__ */ __name(function() {
          return m[k];
        }, "get") };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    });
    var __exportStar = exports && exports.__exportStar || function(m, exports2) {
      for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p)) __createBinding(exports2, m, p);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getOption = exports.hasOption = exports.clearExtension = exports.setExtension = exports.getExtension = exports.hasExtension = exports.mergeFromBinary = exports.fromBinary = exports.toBinary = void 0;
    __exportStar(require_types(), exports);
    __exportStar(require_is_message(), exports);
    __exportStar(require_create(), exports);
    __exportStar(require_clone(), exports);
    __exportStar(require_descriptors(), exports);
    __exportStar(require_equals(), exports);
    __exportStar(require_fields(), exports);
    __exportStar(require_registry(), exports);
    var to_binary_js_1 = require_to_binary();
    Object.defineProperty(exports, "toBinary", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return to_binary_js_1.toBinary;
    }, "get") });
    var from_binary_js_1 = require_from_binary();
    Object.defineProperty(exports, "fromBinary", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return from_binary_js_1.fromBinary;
    }, "get") });
    Object.defineProperty(exports, "mergeFromBinary", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return from_binary_js_1.mergeFromBinary;
    }, "get") });
    __exportStar(require_to_json(), exports);
    __exportStar(require_from_json(), exports);
    __exportStar(require_merge(), exports);
    var extensions_js_1 = require_extensions();
    Object.defineProperty(exports, "hasExtension", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return extensions_js_1.hasExtension;
    }, "get") });
    Object.defineProperty(exports, "getExtension", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return extensions_js_1.getExtension;
    }, "get") });
    Object.defineProperty(exports, "setExtension", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return extensions_js_1.setExtension;
    }, "get") });
    Object.defineProperty(exports, "clearExtension", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return extensions_js_1.clearExtension;
    }, "get") });
    Object.defineProperty(exports, "hasOption", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return extensions_js_1.hasOption;
    }, "get") });
    Object.defineProperty(exports, "getOption", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return extensions_js_1.getOption;
    }, "get") });
    __exportStar(require_proto_int64(), exports);
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/code-string.js
var require_code_string = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/code-string.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.codeToString = codeToString;
    exports.codeFromString = codeFromString;
    var code_js_1 = require_code();
    function codeToString(value) {
      const name = code_js_1.Code[value];
      if (typeof name != "string") {
        return value.toString();
      }
      return name[0].toLowerCase() + name.substring(1).replace(/[A-Z]/g, (c) => "_" + c.toLowerCase());
    }
    __name(codeToString, "codeToString");
    var stringToCode;
    function codeFromString(value) {
      if (!stringToCode) {
        stringToCode = {};
        for (const value2 of Object.values(code_js_1.Code)) {
          if (typeof value2 == "string") {
            continue;
          }
          stringToCode[codeToString(value2)] = value2;
        }
      }
      return stringToCode[value];
    }
    __name(codeFromString, "codeFromString");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/connect-error.js
var require_connect_error = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/connect-error.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ConnectError = void 0;
    var code_js_1 = require_code();
    var protobuf_1 = require_cjs();
    var code_string_js_1 = require_code_string();
    var ConnectError = class _ConnectError extends Error {
      static {
        __name(this, "ConnectError");
      }
      /**
       * Create a new ConnectError.
       * If no code is provided, code "unknown" is used.
       * Outgoing details are only relevant for the server side - a service may
       * raise an error with details, and it is up to the protocol implementation
       * to encode and send the details along with error.
       */
      constructor(message, code = code_js_1.Code.Unknown, metadata, outgoingDetails, cause) {
        super(createMessage(message, code));
        this.name = "ConnectError";
        Object.setPrototypeOf(this, new.target.prototype);
        this.rawMessage = message;
        this.code = code;
        this.metadata = new Headers(metadata !== null && metadata !== void 0 ? metadata : {});
        this.details = outgoingDetails !== null && outgoingDetails !== void 0 ? outgoingDetails : [];
        this.cause = cause;
      }
      /**
       * Convert any value - typically a caught error into a ConnectError,
       * following these rules:
       * - If the value is already a ConnectError, return it as is.
       * - If the value is an AbortError from the fetch API, return the message
       *   of the AbortError with code Canceled.
       * - For other Errors, return the error message with code Unknown by default.
       * - For other values, return the values String representation as a message,
       *   with the code Unknown by default.
       * The original value will be used for the "cause" property for the new
       * ConnectError.
       */
      static from(reason, code = code_js_1.Code.Unknown) {
        if (reason instanceof _ConnectError) {
          return reason;
        }
        if (reason instanceof Error) {
          if (reason.name == "AbortError") {
            return new _ConnectError(reason.message, code_js_1.Code.Canceled);
          }
          return new _ConnectError(reason.message, code, void 0, void 0, reason);
        }
        return new _ConnectError(String(reason), code, void 0, void 0, reason);
      }
      static [Symbol.hasInstance](v) {
        if (!(v instanceof Error)) {
          return false;
        }
        if (Object.getPrototypeOf(v) === _ConnectError.prototype) {
          return true;
        }
        return v.name === "ConnectError" && "code" in v && typeof v.code === "number" && "metadata" in v && "details" in v && Array.isArray(v.details) && "rawMessage" in v && typeof v.rawMessage == "string" && "cause" in v;
      }
      findDetails(typeOrRegistry) {
        const registry = typeOrRegistry.kind === "message" ? {
          getMessage: /* @__PURE__ */ __name((typeName) => typeName === typeOrRegistry.typeName ? typeOrRegistry : void 0, "getMessage")
        } : typeOrRegistry;
        const details = [];
        for (const data of this.details) {
          if ("desc" in data) {
            if (registry.getMessage(data.desc.typeName)) {
              details.push((0, protobuf_1.create)(data.desc, data.value));
            }
            continue;
          }
          const desc = registry.getMessage(data.type);
          if (desc) {
            try {
              details.push((0, protobuf_1.fromBinary)(desc, data.value));
            } catch (_) {
            }
          }
        }
        return details;
      }
    };
    exports.ConnectError = ConnectError;
    function createMessage(message, code) {
      return message.length ? `[${(0, code_string_js_1.codeToString)(code)}] ${message}` : `[${(0, code_string_js_1.codeToString)(code)}]`;
    }
    __name(createMessage, "createMessage");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/http-headers.js
var require_http_headers = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/http-headers.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.encodeBinaryHeader = encodeBinaryHeader;
    exports.decodeBinaryHeader = decodeBinaryHeader;
    exports.appendHeaders = appendHeaders;
    var protobuf_1 = require_cjs();
    var wire_1 = require_wire();
    var connect_error_js_1 = require_connect_error();
    var code_js_1 = require_code();
    function encodeBinaryHeader(value, desc) {
      let bytes;
      if (desc !== void 0) {
        bytes = (0, protobuf_1.toBinary)(desc, value);
      } else if (typeof value == "string") {
        bytes = new TextEncoder().encode(value);
      } else {
        bytes = value instanceof Uint8Array ? value : new Uint8Array(value);
      }
      return (0, wire_1.base64Encode)(bytes, "std_raw");
    }
    __name(encodeBinaryHeader, "encodeBinaryHeader");
    function decodeBinaryHeader(value, desc, options) {
      try {
        const bytes = (0, wire_1.base64Decode)(value);
        if (desc) {
          return (0, protobuf_1.fromBinary)(desc, bytes, options);
        }
        return bytes;
      } catch (e) {
        throw connect_error_js_1.ConnectError.from(e, code_js_1.Code.DataLoss);
      }
    }
    __name(decodeBinaryHeader, "decodeBinaryHeader");
    function appendHeaders(...headers) {
      const h = new Headers();
      for (const e of headers) {
        e.forEach((value, key) => {
          h.append(key, value);
        });
      }
      return h;
    }
    __name(appendHeaders, "appendHeaders");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/any-client.js
var require_any_client = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/any-client.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.makeAnyClient = makeAnyClient;
    function makeAnyClient(service, createMethod) {
      const client = {};
      for (const desc of service.methods) {
        const method = createMethod(desc);
        if (method != null) {
          client[desc.localName] = method;
        }
      }
      return client;
    }
    __name(makeAnyClient, "makeAnyClient");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/compression.js
var require_compression = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/compression.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.compressedFlag = void 0;
    exports.compressionNegotiate = compressionNegotiate;
    var connect_error_js_1 = require_connect_error();
    var code_js_1 = require_code();
    exports.compressedFlag = 1;
    function compressionNegotiate(available, requested, accepted, headerNameAcceptEncoding) {
      let request = null;
      let response = null;
      let error = void 0;
      if (requested !== null && requested !== "identity") {
        const found = available.find((c) => c.name === requested);
        if (found) {
          request = found;
        } else {
          const acceptable = available.map((c) => c.name).join(",");
          error = new connect_error_js_1.ConnectError(`unknown compression "${requested}": supported encodings are ${acceptable}`, code_js_1.Code.Unimplemented, {
            [headerNameAcceptEncoding]: acceptable
          });
        }
      }
      if (accepted === null || accepted === "") {
        response = request;
      } else {
        const acceptNames = accepted.split(",").map((n) => n.trim());
        for (const name of acceptNames) {
          const found = available.find((c) => c.name === name);
          if (found) {
            response = found;
            break;
          }
        }
      }
      return { request, response, error };
    }
    __name(compressionNegotiate, "compressionNegotiate");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/envelope.js
var require_envelope = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/envelope.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createEnvelopeReadableStream = createEnvelopeReadableStream;
    exports.envelopeCompress = envelopeCompress;
    exports.envelopeDecompress = envelopeDecompress;
    exports.encodeEnvelope = encodeEnvelope;
    exports.encodeEnvelopes = encodeEnvelopes;
    var connect_error_js_1 = require_connect_error();
    var code_js_1 = require_code();
    var compression_js_1 = require_compression();
    function createEnvelopeReadableStream(stream) {
      let reader;
      let buffer = new Uint8Array(0);
      function append(chunk) {
        const n = new Uint8Array(buffer.length + chunk.length);
        n.set(buffer);
        n.set(chunk, buffer.length);
        buffer = n;
      }
      __name(append, "append");
      return new ReadableStream({
        start() {
          reader = stream.getReader();
        },
        async pull(controller) {
          let header = void 0;
          for (; ; ) {
            if (header === void 0 && buffer.byteLength >= 5) {
              let length = 0;
              for (let i = 1; i < 5; i++) {
                length = (length << 8) + buffer[i];
              }
              header = { flags: buffer[0], length };
            }
            if (header !== void 0 && buffer.byteLength >= header.length + 5) {
              break;
            }
            const result = await reader.read();
            if (result.done) {
              break;
            }
            append(result.value);
          }
          if (header === void 0) {
            if (buffer.byteLength == 0) {
              controller.close();
              return;
            }
            controller.error(new connect_error_js_1.ConnectError("premature end of stream", code_js_1.Code.DataLoss));
            return;
          }
          const data = buffer.subarray(5, 5 + header.length);
          buffer = buffer.subarray(5 + header.length);
          controller.enqueue({
            flags: header.flags,
            data
          });
        }
      });
    }
    __name(createEnvelopeReadableStream, "createEnvelopeReadableStream");
    async function envelopeCompress(envelope, compression, compressMinBytes) {
      let { flags, data } = envelope;
      if ((flags & compression_js_1.compressedFlag) === compression_js_1.compressedFlag) {
        throw new connect_error_js_1.ConnectError("invalid envelope, already compressed", code_js_1.Code.Internal);
      }
      if (compression && data.byteLength >= compressMinBytes) {
        data = await compression.compress(data);
        flags = flags | compression_js_1.compressedFlag;
      }
      return { data, flags };
    }
    __name(envelopeCompress, "envelopeCompress");
    async function envelopeDecompress(envelope, compression, readMaxBytes) {
      let { flags, data } = envelope;
      if ((flags & compression_js_1.compressedFlag) === compression_js_1.compressedFlag) {
        if (!compression) {
          throw new connect_error_js_1.ConnectError("received compressed envelope, but do not know how to decompress", code_js_1.Code.Internal);
        }
        data = await compression.decompress(data, readMaxBytes);
        flags = flags ^ compression_js_1.compressedFlag;
      }
      return { data, flags };
    }
    __name(envelopeDecompress, "envelopeDecompress");
    function encodeEnvelope(flags, data) {
      const bytes = new Uint8Array(data.length + 5);
      bytes.set(data, 5);
      const v = new DataView(bytes.buffer, bytes.byteOffset, bytes.byteLength);
      v.setUint8(0, flags);
      v.setUint32(1, data.length);
      return bytes;
    }
    __name(encodeEnvelope, "encodeEnvelope");
    function encodeEnvelopes(...envelopes) {
      const len = envelopes.reduce((previousValue, currentValue) => previousValue + currentValue.data.length + 5, 0);
      const bytes = new Uint8Array(len);
      const v = new DataView(bytes.buffer);
      let offset = 0;
      for (const e of envelopes) {
        v.setUint8(offset, e.flags);
        v.setUint32(offset + 1, e.data.length);
        bytes.set(e.data, offset + 5);
        offset += e.data.length + 5;
      }
      return bytes;
    }
    __name(encodeEnvelopes, "encodeEnvelopes");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/limit-io.js
var require_limit_io = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/limit-io.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.validateReadWriteMaxBytes = validateReadWriteMaxBytes;
    exports.assertWriteMaxBytes = assertWriteMaxBytes;
    exports.assertReadMaxBytes = assertReadMaxBytes;
    var connect_error_js_1 = require_connect_error();
    var code_js_1 = require_code();
    var maxReadMaxBytes = 4294967295;
    var maxWriteMaxBytes = maxReadMaxBytes;
    var defaultCompressMinBytes = 1024;
    function validateReadWriteMaxBytes(readMaxBytes, writeMaxBytes, compressMinBytes) {
      writeMaxBytes !== null && writeMaxBytes !== void 0 ? writeMaxBytes : writeMaxBytes = maxWriteMaxBytes;
      readMaxBytes !== null && readMaxBytes !== void 0 ? readMaxBytes : readMaxBytes = maxReadMaxBytes;
      compressMinBytes !== null && compressMinBytes !== void 0 ? compressMinBytes : compressMinBytes = defaultCompressMinBytes;
      if (writeMaxBytes < 1 || writeMaxBytes > maxWriteMaxBytes) {
        throw new connect_error_js_1.ConnectError(`writeMaxBytes ${writeMaxBytes} must be >= 1 and <= ${maxWriteMaxBytes}`, code_js_1.Code.Internal);
      }
      if (readMaxBytes < 1 || readMaxBytes > maxReadMaxBytes) {
        throw new connect_error_js_1.ConnectError(`readMaxBytes ${readMaxBytes} must be >= 1 and <= ${maxReadMaxBytes}`, code_js_1.Code.Internal);
      }
      return {
        readMaxBytes,
        writeMaxBytes,
        compressMinBytes
      };
    }
    __name(validateReadWriteMaxBytes, "validateReadWriteMaxBytes");
    function assertWriteMaxBytes(writeMaxBytes, bytesWritten) {
      if (bytesWritten > writeMaxBytes) {
        throw new connect_error_js_1.ConnectError(`message size ${bytesWritten} is larger than configured writeMaxBytes ${writeMaxBytes}`, code_js_1.Code.ResourceExhausted);
      }
    }
    __name(assertWriteMaxBytes, "assertWriteMaxBytes");
    function assertReadMaxBytes(readMaxBytes, bytesRead, totalSizeKnown = false) {
      if (bytesRead > readMaxBytes) {
        let message = `message size is larger than configured readMaxBytes ${readMaxBytes}`;
        if (totalSizeKnown) {
          message = `message size ${bytesRead} is larger than configured readMaxBytes ${readMaxBytes}`;
        }
        throw new connect_error_js_1.ConnectError(message, code_js_1.Code.ResourceExhausted);
      }
    }
    __name(assertReadMaxBytes, "assertReadMaxBytes");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/async-iterable.js
var require_async_iterable = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/async-iterable.js"(exports) {
    "use strict";
    init_esm();
    var __asyncValues = exports && exports.__asyncValues || function(o) {
      if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
      var m = o[Symbol.asyncIterator], i;
      return m ? m.call(o) : (o = typeof __values === "function" ? __values(o) : o[Symbol.iterator](), i = {}, verb("next"), verb("throw"), verb("return"), i[Symbol.asyncIterator] = function() {
        return this;
      }, i);
      function verb(n) {
        i[n] = o[n] && function(v) {
          return new Promise(function(resolve, reject) {
            v = o[n](v), settle(resolve, reject, v.done, v.value);
          });
        };
      }
      __name(verb, "verb");
      function settle(resolve, reject, d, v) {
        Promise.resolve(v).then(function(v2) {
          resolve({ value: v2, done: d });
        }, reject);
      }
      __name(settle, "settle");
    };
    var __await = exports && exports.__await || function(v) {
      return this instanceof __await ? (this.v = v, this) : new __await(v);
    };
    var __asyncGenerator = exports && exports.__asyncGenerator || function(thisArg, _arguments, generator) {
      if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
      var g = generator.apply(thisArg, _arguments || []), i, q = [];
      return i = Object.create((typeof AsyncIterator === "function" ? AsyncIterator : Object).prototype), verb("next"), verb("throw"), verb("return", awaitReturn), i[Symbol.asyncIterator] = function() {
        return this;
      }, i;
      function awaitReturn(f) {
        return function(v) {
          return Promise.resolve(v).then(f, reject);
        };
      }
      __name(awaitReturn, "awaitReturn");
      function verb(n, f) {
        if (g[n]) {
          i[n] = function(v) {
            return new Promise(function(a, b) {
              q.push([n, v, a, b]) > 1 || resume(n, v);
            });
          };
          if (f) i[n] = f(i[n]);
        }
      }
      __name(verb, "verb");
      function resume(n, v) {
        try {
          step(g[n](v));
        } catch (e) {
          settle(q[0][3], e);
        }
      }
      __name(resume, "resume");
      function step(r) {
        r.value instanceof __await ? Promise.resolve(r.value.v).then(fulfill, reject) : settle(q[0][2], r);
      }
      __name(step, "step");
      function fulfill(value) {
        resume("next", value);
      }
      __name(fulfill, "fulfill");
      function reject(value) {
        resume("throw", value);
      }
      __name(reject, "reject");
      function settle(f, v) {
        if (f(v), q.shift(), q.length) resume(q[0][0], q[0][1]);
      }
      __name(settle, "settle");
    };
    var __asyncDelegator = exports && exports.__asyncDelegator || function(o) {
      var i, p;
      return i = {}, verb("next"), verb("throw", function(e) {
        throw e;
      }), verb("return"), i[Symbol.iterator] = function() {
        return this;
      }, i;
      function verb(n, f) {
        i[n] = o[n] ? function(v) {
          return (p = !p) ? { value: __await(o[n](v)), done: false } : f ? f(v) : v;
        } : f;
      }
      __name(verb, "verb");
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.pipeTo = pipeTo;
    exports.sinkAll = sinkAll;
    exports.sinkAllBytes = sinkAllBytes;
    exports.pipe = pipe;
    exports.transformCatch = transformCatch;
    exports.transformCatchFinally = transformCatchFinally;
    exports.transformAppend = transformAppend;
    exports.transformPrepend = transformPrepend;
    exports.transformReadAllBytes = transformReadAllBytes;
    exports.transformSerializeEnvelope = transformSerializeEnvelope;
    exports.transformParseEnvelope = transformParseEnvelope;
    exports.transformCompressEnvelope = transformCompressEnvelope;
    exports.transformDecompressEnvelope = transformDecompressEnvelope;
    exports.transformJoinEnvelopes = transformJoinEnvelopes;
    exports.transformSplitEnvelope = transformSplitEnvelope;
    exports.readAllBytes = readAllBytes;
    exports.untilFirst = untilFirst;
    exports.makeIterableAbortable = makeIterableAbortable;
    exports.createWritableIterable = createWritableIterable;
    exports.createAsyncIterable = createAsyncIterable;
    var code_js_1 = require_code();
    var connect_error_js_1 = require_connect_error();
    var envelope_js_1 = require_envelope();
    var limit_io_js_1 = require_limit_io();
    function pipeTo(source, ...rest) {
      const [transforms, sink, opt] = pickTransformsAndSink(rest);
      let iterable = source;
      let abortable;
      if ((opt === null || opt === void 0 ? void 0 : opt.propagateDownStreamError) === true) {
        iterable = abortable = makeIterableAbortable(iterable);
      }
      iterable = pipe(iterable, ...transforms, { propagateDownStreamError: false });
      return sink(iterable).catch((reason) => {
        if (abortable) {
          return abortable.abort(reason).then(() => Promise.reject(reason));
        }
        return Promise.reject(reason);
      });
    }
    __name(pipeTo, "pipeTo");
    function pickTransformsAndSink(rest) {
      let opt;
      if (typeof rest[rest.length - 1] != "function") {
        opt = rest.pop();
      }
      const sink = rest.pop();
      return [rest, sink, opt];
    }
    __name(pickTransformsAndSink, "pickTransformsAndSink");
    function sinkAll() {
      return async function(iterable) {
        var _a, e_1, _b, _c;
        const all = [];
        try {
          for (var _d = true, iterable_1 = __asyncValues(iterable), iterable_1_1; iterable_1_1 = await iterable_1.next(), _a = iterable_1_1.done, !_a; _d = true) {
            _c = iterable_1_1.value;
            _d = false;
            const chunk = _c;
            all.push(chunk);
          }
        } catch (e_1_1) {
          e_1 = { error: e_1_1 };
        } finally {
          try {
            if (!_d && !_a && (_b = iterable_1.return)) await _b.call(iterable_1);
          } finally {
            if (e_1) throw e_1.error;
          }
        }
        return all;
      };
    }
    __name(sinkAll, "sinkAll");
    function sinkAllBytes(readMaxBytes, lengthHint) {
      return async function(iterable) {
        return await readAllBytes(iterable, readMaxBytes, lengthHint);
      };
    }
    __name(sinkAllBytes, "sinkAllBytes");
    function pipe(source, ...rest) {
      return __asyncGenerator(this, arguments, /* @__PURE__ */ __name(function* pipe_1() {
        var _a;
        const [transforms, opt] = pickTransforms(rest);
        let abortable;
        const sourceIt = source[Symbol.asyncIterator]();
        const cachedSource = {
          [Symbol.asyncIterator]() {
            return sourceIt;
          }
        };
        let iterable = cachedSource;
        if ((opt === null || opt === void 0 ? void 0 : opt.propagateDownStreamError) === true) {
          iterable = abortable = makeIterableAbortable(iterable);
        }
        for (const t of transforms) {
          iterable = t(iterable);
        }
        const it = iterable[Symbol.asyncIterator]();
        try {
          for (; ; ) {
            const r = yield __await(it.next());
            if (r.done === true) {
              break;
            }
            if (!abortable) {
              yield yield __await(r.value);
              continue;
            }
            try {
              yield yield __await(r.value);
            } catch (e) {
              yield __await(abortable.abort(e));
              throw e;
            }
          }
        } finally {
          if ((opt === null || opt === void 0 ? void 0 : opt.propagateDownStreamError) === true) {
            (_a = sourceIt.return) === null || _a === void 0 ? void 0 : _a.call(sourceIt).catch(() => {
            });
          }
        }
      }, "pipe_1"));
    }
    __name(pipe, "pipe");
    function pickTransforms(rest) {
      let opt;
      if (typeof rest[rest.length - 1] != "function") {
        opt = rest.pop();
      }
      return [rest, opt];
    }
    __name(pickTransforms, "pickTransforms");
    function transformCatch(catchError) {
      return function(iterable) {
        return __asyncGenerator(this, arguments, function* () {
          const it = iterable[Symbol.asyncIterator]();
          for (; ; ) {
            let r;
            try {
              r = yield __await(it.next());
            } catch (e) {
              const caught = yield __await(catchError(e));
              if (caught !== void 0) {
                yield yield __await(caught);
              }
              break;
            }
            if (r.done === true) {
              break;
            }
            yield yield __await(r.value);
          }
        });
      };
    }
    __name(transformCatch, "transformCatch");
    function transformCatchFinally(catchFinally) {
      return function(iterable) {
        return __asyncGenerator(this, arguments, function* () {
          let err;
          const it = iterable[Symbol.asyncIterator]();
          for (; ; ) {
            let r;
            try {
              r = yield __await(it.next());
            } catch (e) {
              err = e;
              break;
            }
            if (r.done === true) {
              break;
            }
            yield yield __await(r.value);
          }
          const caught = yield __await(catchFinally(err));
          if (caught !== void 0) {
            yield yield __await(caught);
          }
        });
      };
    }
    __name(transformCatchFinally, "transformCatchFinally");
    function transformAppend(provide) {
      return function(iterable) {
        return __asyncGenerator(this, arguments, function* () {
          var _a, e_2, _b, _c;
          try {
            for (var _d = true, iterable_2 = __asyncValues(iterable), iterable_2_1; iterable_2_1 = yield __await(iterable_2.next()), _a = iterable_2_1.done, !_a; _d = true) {
              _c = iterable_2_1.value;
              _d = false;
              const chunk = _c;
              yield yield __await(chunk);
            }
          } catch (e_2_1) {
            e_2 = { error: e_2_1 };
          } finally {
            try {
              if (!_d && !_a && (_b = iterable_2.return)) yield __await(_b.call(iterable_2));
            } finally {
              if (e_2) throw e_2.error;
            }
          }
          const append = yield __await(provide());
          if (append !== void 0) {
            yield yield __await(append);
          }
        });
      };
    }
    __name(transformAppend, "transformAppend");
    function transformPrepend(provide) {
      return function(iterable) {
        return __asyncGenerator(this, arguments, function* () {
          var _a, e_3, _b, _c;
          const prepend = yield __await(provide());
          if (prepend !== void 0) {
            yield yield __await(prepend);
          }
          try {
            for (var _d = true, iterable_3 = __asyncValues(iterable), iterable_3_1; iterable_3_1 = yield __await(iterable_3.next()), _a = iterable_3_1.done, !_a; _d = true) {
              _c = iterable_3_1.value;
              _d = false;
              const chunk = _c;
              yield yield __await(chunk);
            }
          } catch (e_3_1) {
            e_3 = { error: e_3_1 };
          } finally {
            try {
              if (!_d && !_a && (_b = iterable_3.return)) yield __await(_b.call(iterable_3));
            } finally {
              if (e_3) throw e_3.error;
            }
          }
        });
      };
    }
    __name(transformPrepend, "transformPrepend");
    function transformReadAllBytes(readMaxBytes, lengthHint) {
      return function(iterable) {
        return __asyncGenerator(this, arguments, function* () {
          yield yield __await(yield __await(readAllBytes(iterable, readMaxBytes, lengthHint)));
        });
      };
    }
    __name(transformReadAllBytes, "transformReadAllBytes");
    function transformSerializeEnvelope(serialization, endStreamFlag, endSerialization) {
      if (endStreamFlag === void 0 || endSerialization === void 0) {
        return function(iterable) {
          return __asyncGenerator(this, arguments, function* () {
            var _a, e_4, _b, _c;
            try {
              for (var _d = true, iterable_4 = __asyncValues(iterable), iterable_4_1; iterable_4_1 = yield __await(iterable_4.next()), _a = iterable_4_1.done, !_a; _d = true) {
                _c = iterable_4_1.value;
                _d = false;
                const chunk = _c;
                const data = serialization.serialize(chunk);
                yield yield __await({ flags: 0, data });
              }
            } catch (e_4_1) {
              e_4 = { error: e_4_1 };
            } finally {
              try {
                if (!_d && !_a && (_b = iterable_4.return)) yield __await(_b.call(iterable_4));
              } finally {
                if (e_4) throw e_4.error;
              }
            }
          });
        };
      }
      return function(iterable) {
        return __asyncGenerator(this, arguments, function* () {
          var _a, e_5, _b, _c;
          try {
            for (var _d = true, iterable_5 = __asyncValues(iterable), iterable_5_1; iterable_5_1 = yield __await(iterable_5.next()), _a = iterable_5_1.done, !_a; _d = true) {
              _c = iterable_5_1.value;
              _d = false;
              const chunk = _c;
              let data;
              let flags = 0;
              if (chunk.end) {
                flags = flags | endStreamFlag;
                data = endSerialization.serialize(chunk.value);
              } else {
                data = serialization.serialize(chunk.value);
              }
              yield yield __await({ flags, data });
            }
          } catch (e_5_1) {
            e_5 = { error: e_5_1 };
          } finally {
            try {
              if (!_d && !_a && (_b = iterable_5.return)) yield __await(_b.call(iterable_5));
            } finally {
              if (e_5) throw e_5.error;
            }
          }
        });
      };
    }
    __name(transformSerializeEnvelope, "transformSerializeEnvelope");
    function transformParseEnvelope(serialization, endStreamFlag, endSerialization) {
      if (endSerialization && endStreamFlag !== void 0) {
        return function(iterable) {
          return __asyncGenerator(this, arguments, function* () {
            var _a, e_6, _b, _c;
            try {
              for (var _d = true, iterable_6 = __asyncValues(iterable), iterable_6_1; iterable_6_1 = yield __await(iterable_6.next()), _a = iterable_6_1.done, !_a; _d = true) {
                _c = iterable_6_1.value;
                _d = false;
                const { flags, data } = _c;
                if ((flags & endStreamFlag) === endStreamFlag) {
                  yield yield __await({ value: endSerialization.parse(data), end: true });
                } else {
                  yield yield __await({ value: serialization.parse(data), end: false });
                }
              }
            } catch (e_6_1) {
              e_6 = { error: e_6_1 };
            } finally {
              try {
                if (!_d && !_a && (_b = iterable_6.return)) yield __await(_b.call(iterable_6));
              } finally {
                if (e_6) throw e_6.error;
              }
            }
          });
        };
      }
      return function(iterable) {
        return __asyncGenerator(this, arguments, function* () {
          var _a, e_7, _b, _c;
          try {
            for (var _d = true, iterable_7 = __asyncValues(iterable), iterable_7_1; iterable_7_1 = yield __await(iterable_7.next()), _a = iterable_7_1.done, !_a; _d = true) {
              _c = iterable_7_1.value;
              _d = false;
              const { flags, data } = _c;
              if (endStreamFlag !== void 0 && (flags & endStreamFlag) === endStreamFlag) {
                if (endSerialization === null) {
                  throw new connect_error_js_1.ConnectError("unexpected end flag", code_js_1.Code.InvalidArgument);
                }
                continue;
              }
              yield yield __await(serialization.parse(data));
            }
          } catch (e_7_1) {
            e_7 = { error: e_7_1 };
          } finally {
            try {
              if (!_d && !_a && (_b = iterable_7.return)) yield __await(_b.call(iterable_7));
            } finally {
              if (e_7) throw e_7.error;
            }
          }
        });
      };
    }
    __name(transformParseEnvelope, "transformParseEnvelope");
    function transformCompressEnvelope(compression, compressMinBytes) {
      return function(iterable) {
        return __asyncGenerator(this, arguments, function* () {
          var _a, e_8, _b, _c;
          try {
            for (var _d = true, iterable_8 = __asyncValues(iterable), iterable_8_1; iterable_8_1 = yield __await(iterable_8.next()), _a = iterable_8_1.done, !_a; _d = true) {
              _c = iterable_8_1.value;
              _d = false;
              const env2 = _c;
              yield yield __await(yield __await((0, envelope_js_1.envelopeCompress)(env2, compression, compressMinBytes)));
            }
          } catch (e_8_1) {
            e_8 = { error: e_8_1 };
          } finally {
            try {
              if (!_d && !_a && (_b = iterable_8.return)) yield __await(_b.call(iterable_8));
            } finally {
              if (e_8) throw e_8.error;
            }
          }
        });
      };
    }
    __name(transformCompressEnvelope, "transformCompressEnvelope");
    function transformDecompressEnvelope(compression, readMaxBytes) {
      return function(iterable) {
        return __asyncGenerator(this, arguments, function* () {
          var _a, e_9, _b, _c;
          try {
            for (var _d = true, iterable_9 = __asyncValues(iterable), iterable_9_1; iterable_9_1 = yield __await(iterable_9.next()), _a = iterable_9_1.done, !_a; _d = true) {
              _c = iterable_9_1.value;
              _d = false;
              const env2 = _c;
              yield yield __await(yield __await((0, envelope_js_1.envelopeDecompress)(env2, compression, readMaxBytes)));
            }
          } catch (e_9_1) {
            e_9 = { error: e_9_1 };
          } finally {
            try {
              if (!_d && !_a && (_b = iterable_9.return)) yield __await(_b.call(iterable_9));
            } finally {
              if (e_9) throw e_9.error;
            }
          }
        });
      };
    }
    __name(transformDecompressEnvelope, "transformDecompressEnvelope");
    function transformJoinEnvelopes() {
      return function(iterable) {
        return __asyncGenerator(this, arguments, function* () {
          var _a, e_10, _b, _c;
          try {
            for (var _d = true, iterable_10 = __asyncValues(iterable), iterable_10_1; iterable_10_1 = yield __await(iterable_10.next()), _a = iterable_10_1.done, !_a; _d = true) {
              _c = iterable_10_1.value;
              _d = false;
              const { flags, data } = _c;
              yield yield __await((0, envelope_js_1.encodeEnvelope)(flags, data));
            }
          } catch (e_10_1) {
            e_10 = { error: e_10_1 };
          } finally {
            try {
              if (!_d && !_a && (_b = iterable_10.return)) yield __await(_b.call(iterable_10));
            } finally {
              if (e_10) throw e_10.error;
            }
          }
        });
      };
    }
    __name(transformJoinEnvelopes, "transformJoinEnvelopes");
    function transformSplitEnvelope(readMaxBytes) {
      function append(buffer, chunk) {
        const n = new Uint8Array(buffer.byteLength + chunk.byteLength);
        n.set(buffer);
        n.set(chunk, buffer.length);
        return n;
      }
      __name(append, "append");
      function shiftEnvelope(buffer, header) {
        if (buffer.byteLength < 5 + header.length) {
          return [void 0, buffer];
        }
        return [
          { flags: header.flags, data: buffer.subarray(5, 5 + header.length) },
          buffer.subarray(5 + header.length)
        ];
      }
      __name(shiftEnvelope, "shiftEnvelope");
      function peekHeader(buffer) {
        if (buffer.byteLength < 5) {
          return void 0;
        }
        const view = new DataView(buffer.buffer, buffer.byteOffset, buffer.byteLength);
        const length = view.getUint32(1);
        const flags = view.getUint8(0);
        return { length, flags };
      }
      __name(peekHeader, "peekHeader");
      return function(iterable) {
        return __asyncGenerator(this, arguments, function* () {
          var _a, e_11, _b, _c;
          let buffer = new Uint8Array(0);
          try {
            for (var _d = true, iterable_11 = __asyncValues(iterable), iterable_11_1; iterable_11_1 = yield __await(iterable_11.next()), _a = iterable_11_1.done, !_a; _d = true) {
              _c = iterable_11_1.value;
              _d = false;
              const chunk = _c;
              buffer = append(buffer, chunk);
              for (; ; ) {
                const header = peekHeader(buffer);
                if (!header) {
                  break;
                }
                (0, limit_io_js_1.assertReadMaxBytes)(readMaxBytes, header.length, true);
                let env2;
                [env2, buffer] = shiftEnvelope(buffer, header);
                if (!env2) {
                  break;
                }
                yield yield __await(env2);
              }
            }
          } catch (e_11_1) {
            e_11 = { error: e_11_1 };
          } finally {
            try {
              if (!_d && !_a && (_b = iterable_11.return)) yield __await(_b.call(iterable_11));
            } finally {
              if (e_11) throw e_11.error;
            }
          }
          if (buffer.byteLength > 0) {
            const header = peekHeader(buffer);
            let message = "protocol error: incomplete envelope";
            if (header) {
              message = `protocol error: promised ${header.length} bytes in enveloped message, got ${buffer.byteLength - 5} bytes`;
            }
            throw new connect_error_js_1.ConnectError(message, code_js_1.Code.InvalidArgument);
          }
        });
      };
    }
    __name(transformSplitEnvelope, "transformSplitEnvelope");
    async function readAllBytes(iterable, readMaxBytes, lengthHint) {
      var _a, e_12, _b, _c, _d, e_13, _e, _f;
      const [ok, hint] = parseLengthHint(lengthHint);
      if (ok) {
        if (hint > readMaxBytes) {
          (0, limit_io_js_1.assertReadMaxBytes)(readMaxBytes, hint, true);
        }
        const buffer = new Uint8Array(hint);
        let offset2 = 0;
        try {
          for (var _g = true, iterable_12 = __asyncValues(iterable), iterable_12_1; iterable_12_1 = await iterable_12.next(), _a = iterable_12_1.done, !_a; _g = true) {
            _c = iterable_12_1.value;
            _g = false;
            const chunk = _c;
            if (offset2 + chunk.byteLength > hint) {
              throw new connect_error_js_1.ConnectError(`protocol error: promised ${hint} bytes, received ${offset2 + chunk.byteLength}`, code_js_1.Code.InvalidArgument);
            }
            buffer.set(chunk, offset2);
            offset2 += chunk.byteLength;
          }
        } catch (e_12_1) {
          e_12 = { error: e_12_1 };
        } finally {
          try {
            if (!_g && !_a && (_b = iterable_12.return)) await _b.call(iterable_12);
          } finally {
            if (e_12) throw e_12.error;
          }
        }
        if (offset2 < hint) {
          throw new connect_error_js_1.ConnectError(`protocol error: promised ${hint} bytes, received ${offset2}`, code_js_1.Code.InvalidArgument);
        }
        return buffer;
      }
      const chunks = [];
      let count = 0;
      try {
        for (var _h = true, iterable_13 = __asyncValues(iterable), iterable_13_1; iterable_13_1 = await iterable_13.next(), _d = iterable_13_1.done, !_d; _h = true) {
          _f = iterable_13_1.value;
          _h = false;
          const chunk = _f;
          count += chunk.byteLength;
          (0, limit_io_js_1.assertReadMaxBytes)(readMaxBytes, count);
          chunks.push(chunk);
        }
      } catch (e_13_1) {
        e_13 = { error: e_13_1 };
      } finally {
        try {
          if (!_h && !_d && (_e = iterable_13.return)) await _e.call(iterable_13);
        } finally {
          if (e_13) throw e_13.error;
        }
      }
      const all = new Uint8Array(count);
      let offset = 0;
      for (let chunk = chunks.shift(); chunk; chunk = chunks.shift()) {
        all.set(chunk, offset);
        offset += chunk.byteLength;
      }
      return all;
    }
    __name(readAllBytes, "readAllBytes");
    function parseLengthHint(lengthHint) {
      if (lengthHint === void 0 || lengthHint === null) {
        return [false, 0];
      }
      const n = typeof lengthHint == "string" ? parseInt(lengthHint, 10) : lengthHint;
      if (!Number.isSafeInteger(n) || n < 0) {
        return [false, n];
      }
      return [true, n];
    }
    __name(parseLengthHint, "parseLengthHint");
    async function untilFirst(iterable) {
      const it = iterable[Symbol.asyncIterator]();
      let first = await it.next();
      return {
        [Symbol.asyncIterator]() {
          const w = {
            async next() {
              if (first !== null) {
                const n = first;
                first = null;
                return n;
              }
              return await it.next();
            }
          };
          if (it.throw !== void 0) {
            w.throw = (e) => it.throw(e);
          }
          if (it.return !== void 0) {
            w.return = (value) => it.return(value);
          }
          return w;
        }
      };
    }
    __name(untilFirst, "untilFirst");
    function makeIterableAbortable(iterable) {
      const innerCandidate = iterable[Symbol.asyncIterator]();
      if (innerCandidate.throw === void 0) {
        throw new Error("AsyncIterable does not implement throw");
      }
      const inner = innerCandidate;
      let aborted;
      let resultPromise;
      let it = {
        next() {
          resultPromise = inner.next().finally(() => {
            resultPromise = void 0;
          });
          return resultPromise;
        },
        throw(e) {
          return inner.throw(e);
        }
      };
      if (innerCandidate.return !== void 0) {
        it = Object.assign(Object.assign({}, it), { return(value) {
          return inner.return(value);
        } });
      }
      let used = false;
      return {
        abort(reason) {
          if (aborted) {
            return aborted.state;
          }
          const f = /* @__PURE__ */ __name(() => {
            return inner.throw(reason).then((r) => r.done === true ? "completed" : "caught", () => "rethrown");
          }, "f");
          if (resultPromise) {
            aborted = { reason, state: resultPromise.then(f, f) };
            return aborted.state;
          }
          aborted = { reason, state: f() };
          return aborted.state;
        },
        [Symbol.asyncIterator]() {
          if (used) {
            throw new Error("AsyncIterable cannot be re-used");
          }
          used = true;
          return it;
        }
      };
    }
    __name(makeIterableAbortable, "makeIterableAbortable");
    function createWritableIterable() {
      const readQueue = [];
      const writeQueue = [];
      let err = void 0;
      let nextResolve;
      let nextReject;
      let nextPromise = new Promise((resolve, reject) => {
        nextResolve = resolve;
        nextReject = reject;
      });
      let closed = false;
      function drain() {
        for (const next of readQueue.splice(0, readQueue.length)) {
          next({ done: true, value: void 0 });
        }
      }
      __name(drain, "drain");
      return {
        close() {
          closed = true;
          drain();
        },
        async write(payload) {
          if (closed) {
            throw err !== null && err !== void 0 ? err : new Error("cannot write, WritableIterable already closed");
          }
          const read = readQueue.shift();
          if (read === void 0) {
            writeQueue.push(payload);
          } else {
            read({ done: false, value: payload });
            if (readQueue.length > 0) {
              return;
            }
          }
          const limit = writeQueue.length + 1;
          for (let i = 0; i < limit; i++) {
            await nextPromise;
          }
        },
        [Symbol.asyncIterator]() {
          return {
            next() {
              nextResolve();
              nextPromise = new Promise((resolve, reject) => {
                nextResolve = resolve;
                nextReject = reject;
              });
              const write = writeQueue.shift();
              if (write !== void 0) {
                return Promise.resolve({ done: false, value: write });
              }
              if (closed) {
                return Promise.resolve({ done: true, value: void 0 });
              }
              let readResolve;
              const readPromise = new Promise((resolve) => readResolve = resolve);
              readQueue.push(readResolve);
              return readPromise;
            },
            throw(throwErr) {
              err = throwErr;
              closed = true;
              writeQueue.splice(0, writeQueue.length);
              nextPromise.catch(() => {
              });
              nextReject(err);
              drain();
              return Promise.resolve({ done: true, value: void 0 });
            },
            return() {
              closed = true;
              writeQueue.splice(0, writeQueue.length);
              nextResolve();
              nextPromise = Promise.reject(new Error("cannot write, consumer called return"));
              nextPromise.catch(() => {
              });
              drain();
              return Promise.resolve({ done: true, value: void 0 });
            }
          };
        }
      };
    }
    __name(createWritableIterable, "createWritableIterable");
    function createAsyncIterable(items) {
      return __asyncGenerator(this, arguments, /* @__PURE__ */ __name(function* createAsyncIterable_1() {
        yield __await(yield* __asyncDelegator(__asyncValues(items)));
      }, "createAsyncIterable_1"));
    }
    __name(createAsyncIterable, "createAsyncIterable");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/callback-client.js
var require_callback_client = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/callback-client.js"(exports) {
    "use strict";
    init_esm();
    var __asyncValues = exports && exports.__asyncValues || function(o) {
      if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
      var m = o[Symbol.asyncIterator], i;
      return m ? m.call(o) : (o = typeof __values === "function" ? __values(o) : o[Symbol.iterator](), i = {}, verb("next"), verb("throw"), verb("return"), i[Symbol.asyncIterator] = function() {
        return this;
      }, i);
      function verb(n) {
        i[n] = o[n] && function(v) {
          return new Promise(function(resolve, reject) {
            v = o[n](v), settle(resolve, reject, v.done, v.value);
          });
        };
      }
      __name(verb, "verb");
      function settle(resolve, reject, d, v) {
        Promise.resolve(v).then(function(v2) {
          resolve({ value: v2, done: d });
        }, reject);
      }
      __name(settle, "settle");
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createCallbackClient = createCallbackClient;
    var protobuf_1 = require_cjs();
    var connect_error_js_1 = require_connect_error();
    var code_js_1 = require_code();
    var any_client_js_1 = require_any_client();
    var async_iterable_js_1 = require_async_iterable();
    function createCallbackClient(service, transport) {
      return (0, any_client_js_1.makeAnyClient)(service, (method) => {
        switch (method.methodKind) {
          case "unary":
            return createUnaryFn(transport, method);
          case "server_streaming":
            return createServerStreamingFn(transport, method);
          default:
            return null;
        }
      });
    }
    __name(createCallbackClient, "createCallbackClient");
    function createUnaryFn(transport, method) {
      return function(requestMessage, callback, options) {
        const abort = new AbortController();
        options = wrapSignal(abort, options);
        transport.unary(method, abort.signal, options.timeoutMs, options.headers, requestMessage, options.contextValues).then((response) => {
          var _a, _b;
          (_a = options.onHeader) === null || _a === void 0 ? void 0 : _a.call(options, response.header);
          (_b = options.onTrailer) === null || _b === void 0 ? void 0 : _b.call(options, response.trailer);
          callback(void 0, response.message);
        }, (reason) => {
          const err = connect_error_js_1.ConnectError.from(reason, code_js_1.Code.Internal);
          if (err.code === code_js_1.Code.Canceled && abort.signal.aborted) {
            return;
          }
          callback(err, (0, protobuf_1.create)(method.output));
        });
        return () => abort.abort();
      };
    }
    __name(createUnaryFn, "createUnaryFn");
    function createServerStreamingFn(transport, method) {
      return function(input, onResponse, onClose, options) {
        const abort = new AbortController();
        async function run() {
          var _a, e_1, _b, _c;
          var _d, _e;
          options = wrapSignal(abort, options);
          const response = await transport.stream(method, options.signal, options.timeoutMs, options.headers, (0, async_iterable_js_1.createAsyncIterable)([input]), options.contextValues);
          (_d = options.onHeader) === null || _d === void 0 ? void 0 : _d.call(options, response.header);
          try {
            for (var _f = true, _g = __asyncValues(response.message), _h; _h = await _g.next(), _a = _h.done, !_a; _f = true) {
              _c = _h.value;
              _f = false;
              const message = _c;
              onResponse(message);
            }
          } catch (e_1_1) {
            e_1 = { error: e_1_1 };
          } finally {
            try {
              if (!_f && !_a && (_b = _g.return)) await _b.call(_g);
            } finally {
              if (e_1) throw e_1.error;
            }
          }
          (_e = options.onTrailer) === null || _e === void 0 ? void 0 : _e.call(options, response.trailer);
          onClose(void 0);
        }
        __name(run, "run");
        run().catch((reason) => {
          const err = connect_error_js_1.ConnectError.from(reason, code_js_1.Code.Internal);
          if (err.code === code_js_1.Code.Canceled && abort.signal.aborted) {
            onClose(void 0);
          } else {
            onClose(err);
          }
        });
        return () => abort.abort();
      };
    }
    __name(createServerStreamingFn, "createServerStreamingFn");
    function wrapSignal(abort, options) {
      if (options === null || options === void 0 ? void 0 : options.signal) {
        options.signal.addEventListener("abort", () => abort.abort());
        if (options.signal.aborted) {
          abort.abort();
        }
      }
      return Object.assign(Object.assign({}, options), { signal: abort.signal });
    }
    __name(wrapSignal, "wrapSignal");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/promise-client.js
var require_promise_client = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/promise-client.js"(exports) {
    "use strict";
    init_esm();
    var __asyncValues = exports && exports.__asyncValues || function(o) {
      if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
      var m = o[Symbol.asyncIterator], i;
      return m ? m.call(o) : (o = typeof __values === "function" ? __values(o) : o[Symbol.iterator](), i = {}, verb("next"), verb("throw"), verb("return"), i[Symbol.asyncIterator] = function() {
        return this;
      }, i);
      function verb(n) {
        i[n] = o[n] && function(v) {
          return new Promise(function(resolve, reject) {
            v = o[n](v), settle(resolve, reject, v.done, v.value);
          });
        };
      }
      __name(verb, "verb");
      function settle(resolve, reject, d, v) {
        Promise.resolve(v).then(function(v2) {
          resolve({ value: v2, done: d });
        }, reject);
      }
      __name(settle, "settle");
    };
    var __await = exports && exports.__await || function(v) {
      return this instanceof __await ? (this.v = v, this) : new __await(v);
    };
    var __asyncDelegator = exports && exports.__asyncDelegator || function(o) {
      var i, p;
      return i = {}, verb("next"), verb("throw", function(e) {
        throw e;
      }), verb("return"), i[Symbol.iterator] = function() {
        return this;
      }, i;
      function verb(n, f) {
        i[n] = o[n] ? function(v) {
          return (p = !p) ? { value: __await(o[n](v)), done: false } : f ? f(v) : v;
        } : f;
      }
      __name(verb, "verb");
    };
    var __asyncGenerator = exports && exports.__asyncGenerator || function(thisArg, _arguments, generator) {
      if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
      var g = generator.apply(thisArg, _arguments || []), i, q = [];
      return i = Object.create((typeof AsyncIterator === "function" ? AsyncIterator : Object).prototype), verb("next"), verb("throw"), verb("return", awaitReturn), i[Symbol.asyncIterator] = function() {
        return this;
      }, i;
      function awaitReturn(f) {
        return function(v) {
          return Promise.resolve(v).then(f, reject);
        };
      }
      __name(awaitReturn, "awaitReturn");
      function verb(n, f) {
        if (g[n]) {
          i[n] = function(v) {
            return new Promise(function(a, b) {
              q.push([n, v, a, b]) > 1 || resume(n, v);
            });
          };
          if (f) i[n] = f(i[n]);
        }
      }
      __name(verb, "verb");
      function resume(n, v) {
        try {
          step(g[n](v));
        } catch (e) {
          settle(q[0][3], e);
        }
      }
      __name(resume, "resume");
      function step(r) {
        r.value instanceof __await ? Promise.resolve(r.value.v).then(fulfill, reject) : settle(q[0][2], r);
      }
      __name(step, "step");
      function fulfill(value) {
        resume("next", value);
      }
      __name(fulfill, "fulfill");
      function reject(value) {
        resume("throw", value);
      }
      __name(reject, "reject");
      function settle(f, v) {
        if (f(v), q.shift(), q.length) resume(q[0][0], q[0][1]);
      }
      __name(settle, "settle");
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createClient = createClient;
    exports.createUnaryFn = createUnaryFn;
    exports.createServerStreamingFn = createServerStreamingFn;
    exports.createClientStreamingFn = createClientStreamingFn;
    exports.createBiDiStreamingFn = createBiDiStreamingFn;
    var any_client_js_1 = require_any_client();
    var connect_error_js_1 = require_connect_error();
    var code_js_1 = require_code();
    var async_iterable_js_1 = require_async_iterable();
    function createClient(service, transport) {
      return (0, any_client_js_1.makeAnyClient)(service, (method) => {
        switch (method.methodKind) {
          case "unary":
            return createUnaryFn(transport, method);
          case "server_streaming":
            return createServerStreamingFn(transport, method);
          case "client_streaming":
            return createClientStreamingFn(transport, method);
          case "bidi_streaming":
            return createBiDiStreamingFn(transport, method);
          default:
            return null;
        }
      });
    }
    __name(createClient, "createClient");
    function createUnaryFn(transport, method) {
      return async function(input, options) {
        var _a, _b;
        const response = await transport.unary(method, options === null || options === void 0 ? void 0 : options.signal, options === null || options === void 0 ? void 0 : options.timeoutMs, options === null || options === void 0 ? void 0 : options.headers, input, options === null || options === void 0 ? void 0 : options.contextValues);
        (_a = options === null || options === void 0 ? void 0 : options.onHeader) === null || _a === void 0 ? void 0 : _a.call(options, response.header);
        (_b = options === null || options === void 0 ? void 0 : options.onTrailer) === null || _b === void 0 ? void 0 : _b.call(options, response.trailer);
        return response.message;
      };
    }
    __name(createUnaryFn, "createUnaryFn");
    function createServerStreamingFn(transport, method) {
      return function(input, options) {
        return handleStreamResponse(transport.stream(method, options === null || options === void 0 ? void 0 : options.signal, options === null || options === void 0 ? void 0 : options.timeoutMs, options === null || options === void 0 ? void 0 : options.headers, (0, async_iterable_js_1.createAsyncIterable)([input]), options === null || options === void 0 ? void 0 : options.contextValues), options);
      };
    }
    __name(createServerStreamingFn, "createServerStreamingFn");
    function createClientStreamingFn(transport, method) {
      return async function(request, options) {
        var _a, e_1, _b, _c;
        var _d, _e;
        const response = await transport.stream(method, options === null || options === void 0 ? void 0 : options.signal, options === null || options === void 0 ? void 0 : options.timeoutMs, options === null || options === void 0 ? void 0 : options.headers, request, options === null || options === void 0 ? void 0 : options.contextValues);
        (_d = options === null || options === void 0 ? void 0 : options.onHeader) === null || _d === void 0 ? void 0 : _d.call(options, response.header);
        let singleMessage;
        let count = 0;
        try {
          for (var _f = true, _g = __asyncValues(response.message), _h; _h = await _g.next(), _a = _h.done, !_a; _f = true) {
            _c = _h.value;
            _f = false;
            const message = _c;
            singleMessage = message;
            count++;
          }
        } catch (e_1_1) {
          e_1 = { error: e_1_1 };
        } finally {
          try {
            if (!_f && !_a && (_b = _g.return)) await _b.call(_g);
          } finally {
            if (e_1) throw e_1.error;
          }
        }
        if (!singleMessage) {
          throw new connect_error_js_1.ConnectError("protocol error: missing response message", code_js_1.Code.Unimplemented);
        }
        if (count > 1) {
          throw new connect_error_js_1.ConnectError("protocol error: received extra messages for client streaming method", code_js_1.Code.Unimplemented);
        }
        (_e = options === null || options === void 0 ? void 0 : options.onTrailer) === null || _e === void 0 ? void 0 : _e.call(options, response.trailer);
        return singleMessage;
      };
    }
    __name(createClientStreamingFn, "createClientStreamingFn");
    function createBiDiStreamingFn(transport, method) {
      return function(request, options) {
        return handleStreamResponse(transport.stream(method, options === null || options === void 0 ? void 0 : options.signal, options === null || options === void 0 ? void 0 : options.timeoutMs, options === null || options === void 0 ? void 0 : options.headers, request, options === null || options === void 0 ? void 0 : options.contextValues), options);
      };
    }
    __name(createBiDiStreamingFn, "createBiDiStreamingFn");
    function handleStreamResponse(stream, options) {
      const it = function() {
        return __asyncGenerator(this, arguments, function* () {
          var _a, _b;
          const response = yield __await(stream);
          (_a = options === null || options === void 0 ? void 0 : options.onHeader) === null || _a === void 0 ? void 0 : _a.call(options, response.header);
          yield __await(yield* __asyncDelegator(__asyncValues(response.message)));
          (_b = options === null || options === void 0 ? void 0 : options.onTrailer) === null || _b === void 0 ? void 0 : _b.call(options, response.trailer);
        });
      }()[Symbol.asyncIterator]();
      return {
        [Symbol.asyncIterator]: () => ({
          next: /* @__PURE__ */ __name(() => it.next(), "next")
        })
      };
    }
    __name(handleStreamResponse, "handleStreamResponse");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/signals.js
var require_signals = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/signals.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createLinkedAbortController = createLinkedAbortController;
    exports.createDeadlineSignal = createDeadlineSignal;
    exports.getAbortSignalReason = getAbortSignalReason;
    var connect_error_js_1 = require_connect_error();
    var code_js_1 = require_code();
    function createLinkedAbortController(...signals) {
      const controller = new AbortController();
      const sa = signals.filter((s) => s !== void 0).concat(controller.signal);
      for (const signal of sa) {
        if (signal.aborted) {
          onAbort.apply(signal);
          break;
        }
        signal.addEventListener("abort", onAbort);
      }
      function onAbort() {
        if (!controller.signal.aborted) {
          controller.abort(getAbortSignalReason(this));
        }
        for (const signal of sa) {
          signal.removeEventListener("abort", onAbort);
        }
      }
      __name(onAbort, "onAbort");
      return controller;
    }
    __name(createLinkedAbortController, "createLinkedAbortController");
    function createDeadlineSignal(timeoutMs) {
      const controller = new AbortController();
      const listener = /* @__PURE__ */ __name(() => {
        controller.abort(new connect_error_js_1.ConnectError("the operation timed out", code_js_1.Code.DeadlineExceeded));
      }, "listener");
      let timeoutId;
      if (timeoutMs !== void 0) {
        if (timeoutMs <= 0)
          listener();
        else
          timeoutId = setTimeout(listener, timeoutMs);
      }
      return {
        signal: controller.signal,
        cleanup: /* @__PURE__ */ __name(() => clearTimeout(timeoutId), "cleanup")
      };
    }
    __name(createDeadlineSignal, "createDeadlineSignal");
    function getAbortSignalReason(signal) {
      if (!signal.aborted) {
        return void 0;
      }
      if (signal.reason !== void 0) {
        return signal.reason;
      }
      const e = new Error("This operation was aborted");
      e.name = "AbortError";
      return e;
    }
    __name(getAbortSignalReason, "getAbortSignalReason");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/context-values.js
var require_context_values = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/context-values.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createContextValues = createContextValues;
    exports.createContextKey = createContextKey;
    function createContextValues() {
      return {
        get(key) {
          return key.id in this ? this[key.id] : key.defaultValue;
        },
        set(key, value) {
          this[key.id] = value;
          return this;
        },
        delete(key) {
          delete this[key.id];
          return this;
        }
      };
    }
    __name(createContextValues, "createContextValues");
    function createContextKey(defaultValue, options) {
      return { id: Symbol(options === null || options === void 0 ? void 0 : options.description), defaultValue };
    }
    __name(createContextKey, "createContextKey");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/implementation.js
var require_implementation = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/implementation.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createHandlerContext = createHandlerContext;
    exports.createMethodImplSpec = createMethodImplSpec;
    exports.createServiceImplSpec = createServiceImplSpec;
    var connect_error_js_1 = require_connect_error();
    var code_js_1 = require_code();
    var signals_js_1 = require_signals();
    var context_values_js_1 = require_context_values();
    function createHandlerContext(init) {
      var _a;
      let timeoutMs;
      if (init.timeoutMs !== void 0) {
        const date = new Date(Date.now() + init.timeoutMs);
        timeoutMs = /* @__PURE__ */ __name(() => date.getTime() - Date.now(), "timeoutMs");
      } else {
        timeoutMs = /* @__PURE__ */ __name(() => void 0, "timeoutMs");
      }
      const deadline = (0, signals_js_1.createDeadlineSignal)(init.timeoutMs);
      const abortController = (0, signals_js_1.createLinkedAbortController)(deadline.signal, init.requestSignal, init.shutdownSignal);
      return Object.assign(Object.assign({}, init), { signal: abortController.signal, timeoutMs, requestHeader: new Headers(init.requestHeader), responseHeader: new Headers(init.responseHeader), responseTrailer: new Headers(init.responseTrailer), abort(reason) {
        deadline.cleanup();
        abortController.abort(reason);
      }, values: (_a = init.contextValues) !== null && _a !== void 0 ? _a : (0, context_values_js_1.createContextValues)() });
    }
    __name(createHandlerContext, "createHandlerContext");
    function createMethodImplSpec(method, impl) {
      return {
        kind: method.methodKind,
        method,
        impl
      };
    }
    __name(createMethodImplSpec, "createMethodImplSpec");
    function createServiceImplSpec(service, impl) {
      const s = { service, methods: {} };
      for (const method of service.methods) {
        let fn = impl[method.localName];
        if (typeof fn == "function") {
          fn = fn.bind(impl);
        } else {
          const message = `${service.typeName}.${method.name} is not implemented`;
          fn = /* @__PURE__ */ __name(function unimplemented() {
            throw new connect_error_js_1.ConnectError(message, code_js_1.Code.Unimplemented);
          }, "unimplemented");
        }
        s.methods[method.localName] = createMethodImplSpec(method, fn);
      }
      return s;
    }
    __name(createServiceImplSpec, "createServiceImplSpec");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc-web/trailer.js
var require_trailer = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc-web/trailer.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.trailerFlag = void 0;
    exports.trailerParse = trailerParse;
    exports.trailerSerialize = trailerSerialize;
    exports.createTrailerSerialization = createTrailerSerialization;
    exports.trailerFlag = 128;
    function trailerParse(data) {
      const headers = new Headers();
      const lines = new TextDecoder().decode(data).split("\r\n");
      for (const line of lines) {
        if (line === "") {
          continue;
        }
        const i = line.indexOf(":");
        if (i > 0) {
          const name = line.substring(0, i).trim();
          const value = line.substring(i + 1).trim();
          headers.append(name, value);
        }
      }
      return headers;
    }
    __name(trailerParse, "trailerParse");
    function trailerSerialize(trailer) {
      const lines = [];
      trailer.forEach((value, key) => {
        lines.push(`${key}: ${value}\r
`);
      });
      return new TextEncoder().encode(lines.join(""));
    }
    __name(trailerSerialize, "trailerSerialize");
    function createTrailerSerialization() {
      return {
        serialize: trailerSerialize,
        parse: trailerParse
      };
    }
    __name(createTrailerSerialization, "createTrailerSerialization");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/headers.js
var require_headers = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/headers.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.headerUserAgent = exports.headerMessageType = exports.headerStatusDetailsBin = exports.headerGrpcMessage = exports.headerGrpcStatus = exports.headerTimeout = exports.headerAcceptEncoding = exports.headerEncoding = exports.headerContentType = void 0;
    exports.headerContentType = "Content-Type";
    exports.headerEncoding = "Grpc-Encoding";
    exports.headerAcceptEncoding = "Grpc-Accept-Encoding";
    exports.headerTimeout = "Grpc-Timeout";
    exports.headerGrpcStatus = "Grpc-Status";
    exports.headerGrpcMessage = "Grpc-Message";
    exports.headerStatusDetailsBin = "Grpc-Status-Details-Bin";
    exports.headerMessageType = "Grpc-Message-Type";
    exports.headerUserAgent = "User-Agent";
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc-web/headers.js
var require_headers2 = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc-web/headers.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.headerXGrpcWeb = exports.headerXUserAgent = exports.headerUserAgent = exports.headerStatusDetailsBin = exports.headerGrpcMessage = exports.headerGrpcStatus = exports.headerTimeout = exports.headerAcceptEncoding = exports.headerEncoding = exports.headerContentType = void 0;
    var headers_js_1 = require_headers();
    Object.defineProperty(exports, "headerContentType", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return headers_js_1.headerContentType;
    }, "get") });
    Object.defineProperty(exports, "headerEncoding", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return headers_js_1.headerEncoding;
    }, "get") });
    Object.defineProperty(exports, "headerAcceptEncoding", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return headers_js_1.headerAcceptEncoding;
    }, "get") });
    Object.defineProperty(exports, "headerTimeout", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return headers_js_1.headerTimeout;
    }, "get") });
    Object.defineProperty(exports, "headerGrpcStatus", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return headers_js_1.headerGrpcStatus;
    }, "get") });
    Object.defineProperty(exports, "headerGrpcMessage", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return headers_js_1.headerGrpcMessage;
    }, "get") });
    Object.defineProperty(exports, "headerStatusDetailsBin", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return headers_js_1.headerStatusDetailsBin;
    }, "get") });
    Object.defineProperty(exports, "headerUserAgent", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return headers_js_1.headerUserAgent;
    }, "get") });
    exports.headerXUserAgent = "X-User-Agent";
    exports.headerXGrpcWeb = "X-Grpc-Web";
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc-web/content-type.js
var require_content_type = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc-web/content-type.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.contentTypeJson = exports.contentTypeProto = exports.contentTypeRegExp = void 0;
    exports.parseContentType = parseContentType;
    exports.contentTypeRegExp = /^application\/grpc-web(-text)?(?:\+(?:(json)(?:; ?charset=utf-?8)?|proto))?$/i;
    exports.contentTypeProto = "application/grpc-web+proto";
    exports.contentTypeJson = "application/grpc-web+json";
    function parseContentType(contentType) {
      const match = contentType === null || contentType === void 0 ? void 0 : contentType.match(exports.contentTypeRegExp);
      if (!match) {
        return void 0;
      }
      const text = !!match[1];
      const binary = !match[2];
      return { text, binary };
    }
    __name(parseContentType, "parseContentType");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/parse-timeout.js
var require_parse_timeout = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/parse-timeout.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.parseTimeout = parseTimeout;
    var code_js_1 = require_code();
    var connect_error_js_1 = require_connect_error();
    function parseTimeout(value, maxTimeoutMs) {
      if (value === null) {
        return {};
      }
      const results = /^(\d{1,8})([HMSmun])$/.exec(value);
      if (results === null) {
        return {
          error: new connect_error_js_1.ConnectError(`protocol error: invalid grpc timeout value: ${value}`, code_js_1.Code.InvalidArgument)
        };
      }
      const unitToMultiplicand = {
        H: 60 * 60 * 1e3,
        // hour
        M: 60 * 1e3,
        // minute
        S: 1e3,
        // second
        m: 1,
        // millisecond
        u: 1e-3,
        // microsecond
        n: 1e-6
        // nanosecond
      };
      const timeoutMs = unitToMultiplicand[results[2]] * parseInt(results[1]);
      if (timeoutMs > maxTimeoutMs) {
        return {
          timeoutMs,
          error: new connect_error_js_1.ConnectError(`timeout ${timeoutMs}ms must be <= ${maxTimeoutMs}`, code_js_1.Code.InvalidArgument)
        };
      }
      return {
        timeoutMs
      };
    }
    __name(parseTimeout, "parseTimeout");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/embed.js
var require_embed = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/embed.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.embedFileDesc = embedFileDesc;
    exports.pathInFileDesc = pathInFileDesc;
    exports.createFileDescriptorProtoBoot = createFileDescriptorProtoBoot;
    var names_js_1 = require_names();
    var fields_js_1 = require_fields();
    var base64_encoding_js_1 = require_base64_encoding();
    var to_binary_js_1 = require_to_binary();
    var clone_js_1 = require_clone();
    var descriptor_pb_js_1 = require_descriptor_pb();
    function embedFileDesc(file) {
      const embed = {
        bootable: false,
        proto() {
          const stripped = (0, clone_js_1.clone)(descriptor_pb_js_1.FileDescriptorProtoSchema, file);
          (0, fields_js_1.clearField)(stripped, descriptor_pb_js_1.FileDescriptorProtoSchema.field.dependency);
          (0, fields_js_1.clearField)(stripped, descriptor_pb_js_1.FileDescriptorProtoSchema.field.sourceCodeInfo);
          stripped.messageType.map(stripJsonNames);
          return stripped;
        },
        base64() {
          const bytes = (0, to_binary_js_1.toBinary)(descriptor_pb_js_1.FileDescriptorProtoSchema, this.proto());
          return (0, base64_encoding_js_1.base64Encode)(bytes, "std_raw");
        }
      };
      return file.name == "google/protobuf/descriptor.proto" ? Object.assign(Object.assign({}, embed), { bootable: true, boot() {
        return createFileDescriptorProtoBoot(this.proto());
      } }) : embed;
    }
    __name(embedFileDesc, "embedFileDesc");
    function stripJsonNames(d) {
      for (const f of d.field) {
        if (f.jsonName === (0, names_js_1.protoCamelCase)(f.name)) {
          (0, fields_js_1.clearField)(f, descriptor_pb_js_1.FieldDescriptorProtoSchema.field.jsonName);
        }
      }
      for (const n of d.nestedType) {
        stripJsonNames(n);
      }
    }
    __name(stripJsonNames, "stripJsonNames");
    function pathInFileDesc(desc) {
      if (desc.kind == "service") {
        return [desc.file.services.indexOf(desc)];
      }
      const parent = desc.parent;
      if (parent == void 0) {
        switch (desc.kind) {
          case "enum":
            return [desc.file.enums.indexOf(desc)];
          case "message":
            return [desc.file.messages.indexOf(desc)];
          case "extension":
            return [desc.file.extensions.indexOf(desc)];
        }
      }
      function findPath(cur) {
        const nested = [];
        for (let parent2 = cur.parent; parent2; ) {
          const idx = parent2.nestedMessages.indexOf(cur);
          nested.unshift(idx);
          cur = parent2;
          parent2 = cur.parent;
        }
        nested.unshift(cur.file.messages.indexOf(cur));
        return nested;
      }
      __name(findPath, "findPath");
      const path = findPath(parent);
      switch (desc.kind) {
        case "extension":
          return [...path, parent.nestedExtensions.indexOf(desc)];
        case "message":
          return [...path, parent.nestedMessages.indexOf(desc)];
        case "enum":
          return [...path, parent.nestedEnums.indexOf(desc)];
      }
    }
    __name(pathInFileDesc, "pathInFileDesc");
    function createFileDescriptorProtoBoot(proto2) {
      var _a;
      assert(proto2.name == "google/protobuf/descriptor.proto");
      assert(proto2.package == "google.protobuf");
      assert(!proto2.dependency.length);
      assert(!proto2.publicDependency.length);
      assert(!proto2.weakDependency.length);
      assert(!proto2.optionDependency.length);
      assert(!proto2.service.length);
      assert(!proto2.extension.length);
      assert(proto2.sourceCodeInfo === void 0);
      assert(proto2.syntax == "" || proto2.syntax == "proto2");
      assert(!((_a = proto2.options) === null || _a === void 0 ? void 0 : _a.features));
      assert(proto2.edition === descriptor_pb_js_1.Edition.EDITION_UNKNOWN);
      return {
        name: proto2.name,
        package: proto2.package,
        messageType: proto2.messageType.map(createDescriptorBoot),
        enumType: proto2.enumType.map(createEnumDescriptorBoot)
      };
    }
    __name(createFileDescriptorProtoBoot, "createFileDescriptorProtoBoot");
    function createDescriptorBoot(proto2) {
      assert(proto2.extension.length == 0);
      assert(!proto2.oneofDecl.length);
      assert(!proto2.options);
      assert(!(0, fields_js_1.isFieldSet)(proto2, descriptor_pb_js_1.DescriptorProtoSchema.field.visibility));
      const b = {
        name: proto2.name
      };
      if (proto2.field.length) {
        b.field = proto2.field.map(createFieldDescriptorBoot);
      }
      if (proto2.nestedType.length) {
        b.nestedType = proto2.nestedType.map(createDescriptorBoot);
      }
      if (proto2.enumType.length) {
        b.enumType = proto2.enumType.map(createEnumDescriptorBoot);
      }
      if (proto2.extensionRange.length) {
        b.extensionRange = proto2.extensionRange.map((r) => {
          assert(!r.options);
          return { start: r.start, end: r.end };
        });
      }
      return b;
    }
    __name(createDescriptorBoot, "createDescriptorBoot");
    function createFieldDescriptorBoot(proto2) {
      assert((0, fields_js_1.isFieldSet)(proto2, descriptor_pb_js_1.FieldDescriptorProtoSchema.field.name));
      assert((0, fields_js_1.isFieldSet)(proto2, descriptor_pb_js_1.FieldDescriptorProtoSchema.field.number));
      assert((0, fields_js_1.isFieldSet)(proto2, descriptor_pb_js_1.FieldDescriptorProtoSchema.field.type));
      assert(!(0, fields_js_1.isFieldSet)(proto2, descriptor_pb_js_1.FieldDescriptorProtoSchema.field.oneofIndex));
      assert(!(0, fields_js_1.isFieldSet)(proto2, descriptor_pb_js_1.FieldDescriptorProtoSchema.field.jsonName) || proto2.jsonName === (0, names_js_1.protoCamelCase)(proto2.name));
      const b = {
        name: proto2.name,
        number: proto2.number,
        type: proto2.type
      };
      if ((0, fields_js_1.isFieldSet)(proto2, descriptor_pb_js_1.FieldDescriptorProtoSchema.field.label)) {
        b.label = proto2.label;
      }
      if ((0, fields_js_1.isFieldSet)(proto2, descriptor_pb_js_1.FieldDescriptorProtoSchema.field.typeName)) {
        b.typeName = proto2.typeName;
      }
      if ((0, fields_js_1.isFieldSet)(proto2, descriptor_pb_js_1.FieldDescriptorProtoSchema.field.extendee)) {
        b.extendee = proto2.extendee;
      }
      if ((0, fields_js_1.isFieldSet)(proto2, descriptor_pb_js_1.FieldDescriptorProtoSchema.field.defaultValue)) {
        b.defaultValue = proto2.defaultValue;
      }
      if (proto2.options) {
        b.options = createFieldOptionsBoot(proto2.options);
      }
      return b;
    }
    __name(createFieldDescriptorBoot, "createFieldDescriptorBoot");
    function createFieldOptionsBoot(proto2) {
      const b = {};
      assert(!(0, fields_js_1.isFieldSet)(proto2, descriptor_pb_js_1.FieldOptionsSchema.field.ctype));
      if ((0, fields_js_1.isFieldSet)(proto2, descriptor_pb_js_1.FieldOptionsSchema.field.packed)) {
        b.packed = proto2.packed;
      }
      assert(!(0, fields_js_1.isFieldSet)(proto2, descriptor_pb_js_1.FieldOptionsSchema.field.jstype));
      assert(!(0, fields_js_1.isFieldSet)(proto2, descriptor_pb_js_1.FieldOptionsSchema.field.lazy));
      assert(!(0, fields_js_1.isFieldSet)(proto2, descriptor_pb_js_1.FieldOptionsSchema.field.unverifiedLazy));
      if ((0, fields_js_1.isFieldSet)(proto2, descriptor_pb_js_1.FieldOptionsSchema.field.deprecated)) {
        b.deprecated = proto2.deprecated;
      }
      assert(!(0, fields_js_1.isFieldSet)(proto2, descriptor_pb_js_1.FieldOptionsSchema.field.weak));
      assert(!(0, fields_js_1.isFieldSet)(proto2, descriptor_pb_js_1.FieldOptionsSchema.field.debugRedact));
      if ((0, fields_js_1.isFieldSet)(proto2, descriptor_pb_js_1.FieldOptionsSchema.field.retention)) {
        b.retention = proto2.retention;
      }
      if (proto2.targets.length) {
        b.targets = proto2.targets;
      }
      if (proto2.editionDefaults.length) {
        b.editionDefaults = proto2.editionDefaults.map((d) => ({
          value: d.value,
          edition: d.edition
        }));
      }
      assert(!(0, fields_js_1.isFieldSet)(proto2, descriptor_pb_js_1.FieldOptionsSchema.field.features));
      assert(!(0, fields_js_1.isFieldSet)(proto2, descriptor_pb_js_1.FieldOptionsSchema.field.uninterpretedOption));
      return b;
    }
    __name(createFieldOptionsBoot, "createFieldOptionsBoot");
    function createEnumDescriptorBoot(proto2) {
      assert(!proto2.options);
      assert(!(0, fields_js_1.isFieldSet)(proto2, descriptor_pb_js_1.EnumDescriptorProtoSchema.field.visibility));
      return {
        name: proto2.name,
        value: proto2.value.map((v) => {
          assert(!v.options);
          return {
            name: v.name,
            number: v.number
          };
        })
      };
    }
    __name(createEnumDescriptorBoot, "createEnumDescriptorBoot");
    function assert(condition) {
      if (!condition) {
        throw new Error();
      }
    }
    __name(assert, "assert");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv1/enum.js
var require_enum2 = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv1/enum.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.tsEnum = void 0;
    exports.enumDesc = enumDesc;
    var enum_js_1 = require_enum();
    Object.defineProperty(exports, "tsEnum", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return enum_js_1.tsEnum;
    }, "get") });
    function enumDesc(file, path, ...paths) {
      if (paths.length == 0) {
        return file.enums[path];
      }
      const e = paths.pop();
      return paths.reduce((acc, cur) => acc.nestedMessages[cur], file.messages[path]).nestedEnums[e];
    }
    __name(enumDesc, "enumDesc");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv1/extension.js
var require_extension2 = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv1/extension.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.extDesc = extDesc;
    function extDesc(file, path, ...paths) {
      if (paths.length == 0) {
        return file.extensions[path];
      }
      const e = paths.pop();
      return paths.reduce((acc, cur) => acc.nestedMessages[cur], file.messages[path]).nestedExtensions[e];
    }
    __name(extDesc, "extDesc");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv1/file.js
var require_file2 = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv1/file.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.fileDesc = void 0;
    var file_js_1 = require_file();
    Object.defineProperty(exports, "fileDesc", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return file_js_1.fileDesc;
    }, "get") });
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv1/message.js
var require_message2 = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv1/message.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.messageDesc = messageDesc;
    function messageDesc(file, path, ...paths) {
      return paths.reduce((acc, cur) => acc.nestedMessages[cur], file.messages[path]);
    }
    __name(messageDesc, "messageDesc");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv1/service.js
var require_service = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv1/service.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.serviceDesc = serviceDesc;
    function serviceDesc(file, path, ...paths) {
      if (paths.length > 0) {
        throw new Error();
      }
      return file.services[path];
    }
    __name(serviceDesc, "serviceDesc");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/symbols.js
var require_symbols = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/symbols.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.symbols = exports.wktPublicImportPaths = exports.packageName = void 0;
    exports.packageName = "@bufbuild/protobuf";
    exports.wktPublicImportPaths = {
      "google/protobuf/compiler/plugin.proto": exports.packageName + "/wkt",
      "google/protobuf/any.proto": exports.packageName + "/wkt",
      "google/protobuf/api.proto": exports.packageName + "/wkt",
      "google/protobuf/cpp_features.proto": exports.packageName + "/wkt",
      "google/protobuf/descriptor.proto": exports.packageName + "/wkt",
      "google/protobuf/duration.proto": exports.packageName + "/wkt",
      "google/protobuf/empty.proto": exports.packageName + "/wkt",
      "google/protobuf/field_mask.proto": exports.packageName + "/wkt",
      "google/protobuf/go_features.proto": exports.packageName + "/wkt",
      "google/protobuf/java_features.proto": exports.packageName + "/wkt",
      "google/protobuf/source_context.proto": exports.packageName + "/wkt",
      "google/protobuf/struct.proto": exports.packageName + "/wkt",
      "google/protobuf/timestamp.proto": exports.packageName + "/wkt",
      "google/protobuf/type.proto": exports.packageName + "/wkt",
      "google/protobuf/wrappers.proto": exports.packageName + "/wkt"
    };
    exports.symbols = {
      isMessage: { typeOnly: false, bootstrapWktFrom: "../../is-message.js", from: exports.packageName },
      Message: { typeOnly: true, bootstrapWktFrom: "../../types.js", from: exports.packageName },
      create: { typeOnly: false, bootstrapWktFrom: "../../create.js", from: exports.packageName },
      fromJson: { typeOnly: false, bootstrapWktFrom: "../../from-json.js", from: exports.packageName },
      fromJsonString: { typeOnly: false, bootstrapWktFrom: "../../from-json.js", from: exports.packageName },
      fromBinary: { typeOnly: false, bootstrapWktFrom: "../../from-binary.js", from: exports.packageName },
      toBinary: { typeOnly: false, bootstrapWktFrom: "../../to-binary.js", from: exports.packageName },
      toJson: { typeOnly: false, bootstrapWktFrom: "../../to-json.js", from: exports.packageName },
      toJsonString: { typeOnly: false, bootstrapWktFrom: "../../to-json.js", from: exports.packageName },
      protoInt64: { typeOnly: false, bootstrapWktFrom: "../../proto-int64.js", from: exports.packageName },
      JsonValue: { typeOnly: true, bootstrapWktFrom: "../../json-value.js", from: exports.packageName },
      JsonObject: { typeOnly: true, bootstrapWktFrom: "../../json-value.js", from: exports.packageName },
      codegen: {
        boot: { typeOnly: false, bootstrapWktFrom: "../../codegenv2/boot.js", from: exports.packageName + "/codegenv2" },
        fileDesc: { typeOnly: false, bootstrapWktFrom: "../../codegenv2/file.js", from: exports.packageName + "/codegenv2" },
        enumDesc: { typeOnly: false, bootstrapWktFrom: "../../codegenv2/enum.js", from: exports.packageName + "/codegenv2" },
        extDesc: { typeOnly: false, bootstrapWktFrom: "../../codegenv2/extension.js", from: exports.packageName + "/codegenv2" },
        messageDesc: { typeOnly: false, bootstrapWktFrom: "../../codegenv2/message.js", from: exports.packageName + "/codegenv2" },
        serviceDesc: { typeOnly: false, bootstrapWktFrom: "../../codegenv2/service.js", from: exports.packageName + "/codegenv2" },
        tsEnum: { typeOnly: false, bootstrapWktFrom: "../../codegenv2/enum.js", from: exports.packageName + "/codegenv2" },
        GenFile: { typeOnly: true, bootstrapWktFrom: "../../codegenv2/types.js", from: exports.packageName + "/codegenv2" },
        GenEnum: { typeOnly: true, bootstrapWktFrom: "../../codegenv2/types.js", from: exports.packageName + "/codegenv2" },
        GenExtension: { typeOnly: true, bootstrapWktFrom: "../../codegenv2/types.js", from: exports.packageName + "/codegenv2" },
        GenMessage: { typeOnly: true, bootstrapWktFrom: "../../codegenv2/types.js", from: exports.packageName + "/codegenv2" },
        GenService: { typeOnly: true, bootstrapWktFrom: "../../codegenv2/types.js", from: exports.packageName + "/codegenv2" }
      }
    };
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv1/symbols.js
var require_symbols2 = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv1/symbols.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.symbols = exports.wktPublicImportPaths = exports.packageName = void 0;
    var symbols_js_1 = require_symbols();
    exports.packageName = symbols_js_1.packageName;
    exports.wktPublicImportPaths = symbols_js_1.wktPublicImportPaths;
    exports.symbols = Object.assign(Object.assign({}, symbols_js_1.symbols), { codegen: {
      boot: { typeOnly: false, bootstrapWktFrom: "../../codegenv1/boot.js", from: exports.packageName + "/codegenv1" },
      fileDesc: { typeOnly: false, bootstrapWktFrom: "../../codegenv1/file.js", from: exports.packageName + "/codegenv1" },
      enumDesc: { typeOnly: false, bootstrapWktFrom: "../../codegenv1/enum.js", from: exports.packageName + "/codegenv1" },
      extDesc: { typeOnly: false, bootstrapWktFrom: "../../codegenv1/extension.js", from: exports.packageName + "/codegenv1" },
      messageDesc: { typeOnly: false, bootstrapWktFrom: "../../codegenv1/message.js", from: exports.packageName + "/codegenv1" },
      serviceDesc: { typeOnly: false, bootstrapWktFrom: "../../codegenv1/service.js", from: exports.packageName + "/codegenv1" },
      tsEnum: { typeOnly: false, bootstrapWktFrom: "../../codegenv1/enum.js", from: exports.packageName + "/codegenv1" },
      GenFile: { typeOnly: true, bootstrapWktFrom: "../../codegenv1/types.js", from: exports.packageName + "/codegenv1" },
      GenEnum: { typeOnly: true, bootstrapWktFrom: "../../codegenv1/types.js", from: exports.packageName + "/codegenv1" },
      GenExtension: { typeOnly: true, bootstrapWktFrom: "../../codegenv1/types.js", from: exports.packageName + "/codegenv1" },
      GenMessage: { typeOnly: true, bootstrapWktFrom: "../../codegenv1/types.js", from: exports.packageName + "/codegenv1" },
      GenService: { typeOnly: true, bootstrapWktFrom: "../../codegenv1/types.js", from: exports.packageName + "/codegenv1" }
    } });
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/scalar.js
var require_scalar2 = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/scalar.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.scalarTypeScriptType = scalarTypeScriptType;
    exports.scalarJsonType = scalarJsonType;
    var descriptors_js_1 = require_descriptors();
    function scalarTypeScriptType(scalar, longAsString) {
      switch (scalar) {
        case descriptors_js_1.ScalarType.STRING:
          return "string";
        case descriptors_js_1.ScalarType.BOOL:
          return "boolean";
        case descriptors_js_1.ScalarType.UINT64:
        case descriptors_js_1.ScalarType.SFIXED64:
        case descriptors_js_1.ScalarType.FIXED64:
        case descriptors_js_1.ScalarType.SINT64:
        case descriptors_js_1.ScalarType.INT64:
          return longAsString ? "string" : "bigint";
        case descriptors_js_1.ScalarType.BYTES:
          return "Uint8Array";
        default:
          return "number";
      }
    }
    __name(scalarTypeScriptType, "scalarTypeScriptType");
    function scalarJsonType(scalar) {
      switch (scalar) {
        case descriptors_js_1.ScalarType.DOUBLE:
        case descriptors_js_1.ScalarType.FLOAT:
          return `number | "NaN" | "Infinity" | "-Infinity"`;
        case descriptors_js_1.ScalarType.UINT64:
        case descriptors_js_1.ScalarType.SFIXED64:
        case descriptors_js_1.ScalarType.FIXED64:
        case descriptors_js_1.ScalarType.SINT64:
        case descriptors_js_1.ScalarType.INT64:
          return "string";
        case descriptors_js_1.ScalarType.INT32:
        case descriptors_js_1.ScalarType.FIXED32:
        case descriptors_js_1.ScalarType.UINT32:
        case descriptors_js_1.ScalarType.SFIXED32:
        case descriptors_js_1.ScalarType.SINT32:
          return "number";
        case descriptors_js_1.ScalarType.STRING:
          return "string";
        case descriptors_js_1.ScalarType.BOOL:
          return "boolean";
        case descriptors_js_1.ScalarType.BYTES:
          return "string";
      }
    }
    __name(scalarJsonType, "scalarJsonType");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv1/types.js
var require_types2 = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv1/types.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv1/index.js
var require_codegenv1 = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv1/index.js"(exports) {
    "use strict";
    init_esm();
    var __createBinding = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: /* @__PURE__ */ __name(function() {
          return m[k];
        }, "get") };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    });
    var __exportStar = exports && exports.__exportStar || function(m, exports2) {
      for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p)) __createBinding(exports2, m, p);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    __exportStar(require_boot(), exports);
    __exportStar(require_embed(), exports);
    __exportStar(require_enum2(), exports);
    __exportStar(require_extension2(), exports);
    __exportStar(require_file2(), exports);
    __exportStar(require_message2(), exports);
    __exportStar(require_service(), exports);
    __exportStar(require_symbols2(), exports);
    __exportStar(require_scalar2(), exports);
    __exportStar(require_types2(), exports);
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/gen/status_pb.js
var require_status_pb = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/gen/status_pb.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.StatusSchema = exports.file_status = void 0;
    var codegenv1_1 = require_codegenv1();
    var wkt_1 = require_wkt();
    exports.file_status = (0, codegenv1_1.fileDesc)("", [wkt_1.file_google_protobuf_any]);
    exports.StatusSchema = (0, codegenv1_1.messageDesc)(exports.file_status, 0);
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/trailer-status.js
var require_trailer_status = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/trailer-status.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.grpcStatusOk = void 0;
    exports.setTrailerStatus = setTrailerStatus;
    exports.findTrailerError = findTrailerError;
    var status_pb_js_1 = require_status_pb();
    var connect_error_js_1 = require_connect_error();
    var http_headers_js_1 = require_http_headers();
    var code_js_1 = require_code();
    var wkt_1 = require_wkt();
    var headers_js_1 = require_headers();
    var protobuf_1 = require_cjs();
    exports.grpcStatusOk = "0";
    function setTrailerStatus(target, error) {
      if (error) {
        target.set(headers_js_1.headerGrpcStatus, error.code.toString(10));
        target.set(headers_js_1.headerGrpcMessage, encodeURIComponent(error.rawMessage));
        if (error.details.length > 0) {
          const status = (0, protobuf_1.create)(status_pb_js_1.StatusSchema, {
            code: error.code,
            message: error.rawMessage,
            details: error.details.map((detail) => "desc" in detail ? (0, wkt_1.anyPack)(detail.desc, (0, protobuf_1.create)(detail.desc, detail.value)) : {
              typeUrl: `type.googleapis.com/${detail.type}`,
              value: detail.value
            })
          });
          target.set(headers_js_1.headerStatusDetailsBin, (0, http_headers_js_1.encodeBinaryHeader)(status, status_pb_js_1.StatusSchema));
        }
      } else {
        target.set(headers_js_1.headerGrpcStatus, exports.grpcStatusOk.toString());
      }
      return target;
    }
    __name(setTrailerStatus, "setTrailerStatus");
    function findTrailerError(headerOrTrailer) {
      var _a;
      const statusBytes = headerOrTrailer.get(headers_js_1.headerStatusDetailsBin);
      if (statusBytes != null) {
        const status = (0, http_headers_js_1.decodeBinaryHeader)(statusBytes, status_pb_js_1.StatusSchema);
        if (status.code == 0) {
          return void 0;
        }
        const error = new connect_error_js_1.ConnectError(status.message, status.code, headerOrTrailer);
        error.details = status.details.map((any) => ({
          type: any.typeUrl.substring(any.typeUrl.lastIndexOf("/") + 1),
          value: any.value
        }));
        return error;
      }
      const grpcStatus = headerOrTrailer.get(headers_js_1.headerGrpcStatus);
      if (grpcStatus != null) {
        if (grpcStatus === exports.grpcStatusOk) {
          return void 0;
        }
        const code = parseInt(grpcStatus, 10);
        if (code in code_js_1.Code) {
          return new connect_error_js_1.ConnectError(decodeURIComponent((_a = headerOrTrailer.get(headers_js_1.headerGrpcMessage)) !== null && _a !== void 0 ? _a : ""), code, headerOrTrailer);
        }
        return new connect_error_js_1.ConnectError(`invalid grpc-status: ${grpcStatus}`, code_js_1.Code.Internal, headerOrTrailer);
      }
      return void 0;
    }
    __name(findTrailerError, "findTrailerError");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/content-type-matcher.js
var require_content_type_matcher = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/content-type-matcher.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.contentTypeMatcher = contentTypeMatcher;
    var contentTypeMatcherCacheSize = 1024;
    function contentTypeMatcher(...supported) {
      const cache = /* @__PURE__ */ new Map();
      const source = supported.reduce((previousValue, currentValue) => previousValue.concat("supported" in currentValue ? currentValue.supported : currentValue), []);
      function match(contentType) {
        if (contentType === null || contentType.length == 0) {
          return false;
        }
        const cached = cache.get(contentType);
        if (cached !== void 0) {
          return cached;
        }
        const ok = source.some((re) => re.test(contentType));
        if (cache.size < contentTypeMatcherCacheSize) {
          cache.set(contentType, ok);
        }
        return ok;
      }
      __name(match, "match");
      match.supported = source;
      return match;
    }
    __name(contentTypeMatcher, "contentTypeMatcher");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/create-method-url.js
var require_create_method_url = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/create-method-url.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createMethodUrl = createMethodUrl;
    function createMethodUrl(baseUrl, method) {
      return baseUrl.toString().replace(/\/?$/, `/${method.parent.typeName}/${method.name}`);
    }
    __name(createMethodUrl, "createMethodUrl");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/normalize.js
var require_normalize = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/normalize.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.normalize = normalize;
    exports.normalizeIterable = normalizeIterable;
    var protobuf_1 = require_cjs();
    function normalize(desc, message) {
      return (0, protobuf_1.create)(desc, message);
    }
    __name(normalize, "normalize");
    function normalizeIterable(desc, input) {
      function transform(result) {
        if (result.done === true) {
          return result;
        }
        return {
          done: result.done,
          value: normalize(desc, result.value)
        };
      }
      __name(transform, "transform");
      return {
        [Symbol.asyncIterator]() {
          const it = input[Symbol.asyncIterator]();
          const res = {
            next: /* @__PURE__ */ __name(() => it.next().then(transform), "next")
          };
          if (it.throw !== void 0) {
            res.throw = (e) => it.throw(e).then(transform);
          }
          if (it.return !== void 0) {
            res.return = (v) => it.return(v).then(transform);
          }
          return res;
        }
      };
    }
    __name(normalizeIterable, "normalizeIterable");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/interceptor.js
var require_interceptor = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/interceptor.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.applyInterceptors = applyInterceptors;
    function applyInterceptors(next, interceptors) {
      var _a;
      return (_a = interceptors === null || interceptors === void 0 ? void 0 : interceptors.concat().reverse().reduce(
        // eslint-disable-next-line @typescript-eslint/no-unsafe-argument
        (n, i) => i(n),
        next
      )) !== null && _a !== void 0 ? _a : next;
    }
    __name(applyInterceptors, "applyInterceptors");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/invoke-implementation.js
var require_invoke_implementation = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/invoke-implementation.js"(exports) {
    "use strict";
    init_esm();
    var __await = exports && exports.__await || function(v) {
      return this instanceof __await ? (this.v = v, this) : new __await(v);
    };
    var __asyncGenerator = exports && exports.__asyncGenerator || function(thisArg, _arguments, generator) {
      if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
      var g = generator.apply(thisArg, _arguments || []), i, q = [];
      return i = Object.create((typeof AsyncIterator === "function" ? AsyncIterator : Object).prototype), verb("next"), verb("throw"), verb("return", awaitReturn), i[Symbol.asyncIterator] = function() {
        return this;
      }, i;
      function awaitReturn(f) {
        return function(v) {
          return Promise.resolve(v).then(f, reject);
        };
      }
      __name(awaitReturn, "awaitReturn");
      function verb(n, f) {
        if (g[n]) {
          i[n] = function(v) {
            return new Promise(function(a, b) {
              q.push([n, v, a, b]) > 1 || resume(n, v);
            });
          };
          if (f) i[n] = f(i[n]);
        }
      }
      __name(verb, "verb");
      function resume(n, v) {
        try {
          step(g[n](v));
        } catch (e) {
          settle(q[0][3], e);
        }
      }
      __name(resume, "resume");
      function step(r) {
        r.value instanceof __await ? Promise.resolve(r.value.v).then(fulfill, reject) : settle(q[0][2], r);
      }
      __name(step, "step");
      function fulfill(value) {
        resume("next", value);
      }
      __name(fulfill, "fulfill");
      function reject(value) {
        resume("throw", value);
      }
      __name(reject, "reject");
      function settle(f, v) {
        if (f(v), q.shift(), q.length) resume(q[0][0], q[0][1]);
      }
      __name(settle, "settle");
    };
    var __asyncValues = exports && exports.__asyncValues || function(o) {
      if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
      var m = o[Symbol.asyncIterator], i;
      return m ? m.call(o) : (o = typeof __values === "function" ? __values(o) : o[Symbol.iterator](), i = {}, verb("next"), verb("throw"), verb("return"), i[Symbol.asyncIterator] = function() {
        return this;
      }, i);
      function verb(n) {
        i[n] = o[n] && function(v) {
          return new Promise(function(resolve, reject) {
            v = o[n](v), settle(resolve, reject, v.done, v.value);
          });
        };
      }
      __name(verb, "verb");
      function settle(resolve, reject, d, v) {
        Promise.resolve(v).then(function(v2) {
          resolve({ value: v2, done: d });
        }, reject);
      }
      __name(settle, "settle");
    };
    var __asyncDelegator = exports && exports.__asyncDelegator || function(o) {
      var i, p;
      return i = {}, verb("next"), verb("throw", function(e) {
        throw e;
      }), verb("return"), i[Symbol.iterator] = function() {
        return this;
      }, i;
      function verb(n, f) {
        i[n] = o[n] ? function(v) {
          return (p = !p) ? { value: __await(o[n](v)), done: false } : f ? f(v) : v;
        } : f;
      }
      __name(verb, "verb");
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.invokeUnaryImplementation = invokeUnaryImplementation;
    exports.transformInvokeImplementation = transformInvokeImplementation;
    var connect_error_js_1 = require_connect_error();
    var code_js_1 = require_code();
    var async_iterable_js_1 = require_async_iterable();
    var normalize_js_1 = require_normalize();
    var interceptor_js_1 = require_interceptor();
    async function invokeUnaryImplementation(spec, context, input, interceptors) {
      const anyFn = /* @__PURE__ */ __name(async (req) => {
        return Object.assign({ message: (0, normalize_js_1.normalize)(spec.method.output, await spec.impl(req.message, mergeRequest(context, req))), stream: false, method: spec.method }, responseCommon(context, spec));
      }, "anyFn");
      const next = (0, interceptor_js_1.applyInterceptors)(anyFn, interceptors);
      const { message, header, trailer } = await next(Object.assign({ stream: false, message: input, method: spec.method }, requestCommon(context, spec)));
      copyHeaders(header, context.responseHeader);
      copyHeaders(trailer, context.responseTrailer);
      return message;
    }
    __name(invokeUnaryImplementation, "invokeUnaryImplementation");
    function transformInvokeImplementation(spec, context, interceptors) {
      switch (spec.kind) {
        case "unary":
          return /* @__PURE__ */ __name(function unary(input) {
            return __asyncGenerator(this, arguments, /* @__PURE__ */ __name(function* unary_1() {
              yield yield __await(yield __await(invokeUnaryImplementation(spec, context, yield __await(ensureSingle(input, "unary")), interceptors)));
            }, "unary_1"));
          }, "unary");
        case "server_streaming": {
          return /* @__PURE__ */ __name(function serverStreaming(input) {
            return invokeStreamImplementation(spec, context, input, interceptors, async (req) => {
              const output = (0, normalize_js_1.normalizeIterable)(spec.method.output, spec.impl(await ensureSingle(input, "server-streaming"), mergeRequest(context, req)));
              return Object.assign({ stream: true, message: output, method: spec.method }, responseCommon(context, spec));
            });
          }, "serverStreaming");
        }
        case "client_streaming": {
          return /* @__PURE__ */ __name(function clientStreaming(input) {
            return invokeStreamImplementation(spec, context, input, interceptors, async (req) => {
              return Object.assign({ message: (0, async_iterable_js_1.createAsyncIterable)([
                (0, normalize_js_1.normalize)(spec.method.output, await spec.impl(req.message, mergeRequest(context, req)))
              ]), stream: true, method: spec.method }, responseCommon(context, spec));
            });
          }, "clientStreaming");
        }
        case "bidi_streaming":
          return /* @__PURE__ */ __name(function biDiStreaming(input) {
            return invokeStreamImplementation(spec, context, input, interceptors, (req) => {
              return Promise.resolve(Object.assign({ message: (0, normalize_js_1.normalizeIterable)(spec.method.output, spec.impl(req.message, mergeRequest(context, req))), stream: true, method: spec.method }, responseCommon(context, spec)));
            });
          }, "biDiStreaming");
      }
    }
    __name(transformInvokeImplementation, "transformInvokeImplementation");
    function invokeStreamImplementation(spec, context, input, interceptors, anyFn) {
      return __asyncGenerator(this, arguments, /* @__PURE__ */ __name(function* invokeStreamImplementation_1() {
        const next = (0, interceptor_js_1.applyInterceptors)(anyFn, interceptors);
        const { message, header, trailer } = yield __await(next(Object.assign({ stream: true, message: input, method: spec.method }, requestCommon(context, spec))));
        copyHeaders(header, context.responseHeader);
        yield __await(yield* __asyncDelegator(__asyncValues(message)));
        copyHeaders(trailer, context.responseTrailer);
      }, "invokeStreamImplementation_1"));
    }
    __name(invokeStreamImplementation, "invokeStreamImplementation");
    async function ensureSingle(iterable, method) {
      const it = iterable[Symbol.asyncIterator]();
      const first = await it.next();
      if (first.done === true) {
        throw new connect_error_js_1.ConnectError(`protocol error: missing input message for ${method} method`, code_js_1.Code.Unimplemented);
      }
      const second = await it.next();
      if (second.done !== true) {
        throw new connect_error_js_1.ConnectError(`protocol error: received extra input message for ${method} method`, code_js_1.Code.Unimplemented);
      }
      return first.value;
    }
    __name(ensureSingle, "ensureSingle");
    function requestCommon(context, spec) {
      return {
        requestMethod: context.requestMethod,
        url: context.url,
        signal: context.signal,
        header: context.requestHeader,
        service: spec.method.parent,
        contextValues: context.values
      };
    }
    __name(requestCommon, "requestCommon");
    function responseCommon(context, spec) {
      return {
        service: spec.method.parent,
        header: context.responseHeader,
        trailer: context.responseTrailer
      };
    }
    __name(responseCommon, "responseCommon");
    function mergeRequest(context, req) {
      return Object.assign(Object.assign({}, context), { service: req.service, requestHeader: req.header, signal: req.signal, values: req.contextValues });
    }
    __name(mergeRequest, "mergeRequest");
    function copyHeaders(from, to) {
      if (from === to) {
        return;
      }
      to.forEach((_, key) => {
        to.delete(key);
      });
      from.forEach((value, key) => {
        to.set(key, value);
      });
    }
    __name(copyHeaders, "copyHeaders");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/serialization.js
var require_serialization = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/serialization.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getJsonOptions = getJsonOptions;
    exports.createMethodSerializationLookup = createMethodSerializationLookup;
    exports.createClientMethodSerializers = createClientMethodSerializers;
    exports.limitSerialization = limitSerialization;
    exports.createBinarySerialization = createBinarySerialization;
    exports.createJsonSerialization = createJsonSerialization;
    var protobuf_1 = require_cjs();
    var connect_error_js_1 = require_connect_error();
    var code_js_1 = require_code();
    var limit_io_js_1 = require_limit_io();
    function getJsonOptions(options) {
      var _a;
      const o = Object.assign({}, options);
      (_a = o.ignoreUnknownFields) !== null && _a !== void 0 ? _a : o.ignoreUnknownFields = true;
      return o;
    }
    __name(getJsonOptions, "getJsonOptions");
    function createMethodSerializationLookup(method, binaryOptions, jsonOptions, limitOptions) {
      const inputBinary = limitSerialization(createBinarySerialization(method.input, binaryOptions), limitOptions);
      const inputJson = limitSerialization(createJsonSerialization(method.input, jsonOptions), limitOptions);
      const outputBinary = limitSerialization(createBinarySerialization(method.output, binaryOptions), limitOptions);
      const outputJson = limitSerialization(createJsonSerialization(method.output, jsonOptions), limitOptions);
      return {
        getI(useBinaryFormat) {
          return useBinaryFormat ? inputBinary : inputJson;
        },
        getO(useBinaryFormat) {
          return useBinaryFormat ? outputBinary : outputJson;
        }
      };
    }
    __name(createMethodSerializationLookup, "createMethodSerializationLookup");
    function createClientMethodSerializers(method, useBinaryFormat, jsonOptions, binaryOptions) {
      const input = useBinaryFormat ? createBinarySerialization(method.input, binaryOptions) : createJsonSerialization(method.input, jsonOptions);
      const output = useBinaryFormat ? createBinarySerialization(method.output, binaryOptions) : createJsonSerialization(method.output, jsonOptions);
      return { parse: output.parse, serialize: input.serialize };
    }
    __name(createClientMethodSerializers, "createClientMethodSerializers");
    function limitSerialization(serialization, limitOptions) {
      return {
        serialize(data) {
          const bytes = serialization.serialize(data);
          (0, limit_io_js_1.assertWriteMaxBytes)(limitOptions.writeMaxBytes, bytes.byteLength);
          return bytes;
        },
        parse(data) {
          (0, limit_io_js_1.assertReadMaxBytes)(limitOptions.readMaxBytes, data.byteLength, true);
          return serialization.parse(data);
        }
      };
    }
    __name(limitSerialization, "limitSerialization");
    function createBinarySerialization(desc, options) {
      return {
        parse(data) {
          try {
            return (0, protobuf_1.fromBinary)(desc, data, options);
          } catch (e) {
            const m = e instanceof Error ? e.message : String(e);
            throw new connect_error_js_1.ConnectError(`parse binary: ${m}`, code_js_1.Code.Internal);
          }
        },
        serialize(data) {
          try {
            return (0, protobuf_1.toBinary)(desc, data, options);
          } catch (e) {
            const m = e instanceof Error ? e.message : String(e);
            throw new connect_error_js_1.ConnectError(`serialize binary: ${m}`, code_js_1.Code.Internal);
          }
        }
      };
    }
    __name(createBinarySerialization, "createBinarySerialization");
    function createJsonSerialization(desc, options) {
      var _a, _b;
      const textEncoder = (_a = options === null || options === void 0 ? void 0 : options.textEncoder) !== null && _a !== void 0 ? _a : new TextEncoder();
      const textDecoder = (_b = options === null || options === void 0 ? void 0 : options.textDecoder) !== null && _b !== void 0 ? _b : new TextDecoder();
      const o = getJsonOptions(options);
      return {
        parse(data) {
          try {
            const json = textDecoder.decode(data);
            return (0, protobuf_1.fromJsonString)(desc, json, o);
          } catch (e) {
            throw connect_error_js_1.ConnectError.from(e, code_js_1.Code.InvalidArgument);
          }
        },
        serialize(data) {
          try {
            const json = (0, protobuf_1.toJsonString)(desc, data, o);
            return textEncoder.encode(json);
          } catch (e) {
            throw connect_error_js_1.ConnectError.from(e, code_js_1.Code.Internal);
          }
        }
      };
    }
    __name(createJsonSerialization, "createJsonSerialization");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/universal.js
var require_universal = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/universal.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.uResponseVersionNotSupported = exports.uResponseMethodNotAllowed = exports.uResponseUnsupportedMediaType = exports.uResponseNotFound = exports.uResponseOk = void 0;
    exports.assertByteStreamRequest = assertByteStreamRequest;
    function assertByteStreamRequest(req) {
      if (typeof req.body == "object" && req.body !== null && Symbol.asyncIterator in req.body) {
        return;
      }
      throw new Error("byte stream required, but received JSON");
    }
    __name(assertByteStreamRequest, "assertByteStreamRequest");
    exports.uResponseOk = {
      status: 200
    };
    exports.uResponseNotFound = {
      status: 404
    };
    exports.uResponseUnsupportedMediaType = {
      status: 415
    };
    exports.uResponseMethodNotAllowed = {
      status: 405
    };
    exports.uResponseVersionNotSupported = {
      status: 505
    };
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/universal-handler.js
var require_universal_handler = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/universal-handler.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.validateUniversalHandlerOptions = validateUniversalHandlerOptions;
    exports.createUniversalServiceHandlers = createUniversalServiceHandlers;
    exports.createUniversalMethodHandler = createUniversalMethodHandler;
    exports.negotiateProtocol = negotiateProtocol;
    var universal_js_1 = require_universal();
    var content_type_matcher_js_1 = require_content_type_matcher();
    var limit_io_js_1 = require_limit_io();
    var connect_error_js_1 = require_connect_error();
    var code_js_1 = require_code();
    function validateUniversalHandlerOptions(opt) {
      var _a, _b, _c;
      opt !== null && opt !== void 0 ? opt : opt = {};
      const acceptCompression = opt.acceptCompression ? [...opt.acceptCompression] : [];
      const requireConnectProtocolHeader = (_a = opt.requireConnectProtocolHeader) !== null && _a !== void 0 ? _a : false;
      const maxTimeoutMs = (_b = opt.maxTimeoutMs) !== null && _b !== void 0 ? _b : Number.MAX_SAFE_INTEGER;
      return Object.assign(Object.assign({ acceptCompression }, (0, limit_io_js_1.validateReadWriteMaxBytes)(opt.readMaxBytes, opt.writeMaxBytes, opt.compressMinBytes)), { jsonOptions: opt.jsonOptions, binaryOptions: opt.binaryOptions, maxTimeoutMs, shutdownSignal: opt.shutdownSignal, requireConnectProtocolHeader, interceptors: (_c = opt.interceptors) !== null && _c !== void 0 ? _c : [] });
    }
    __name(validateUniversalHandlerOptions, "validateUniversalHandlerOptions");
    function createUniversalServiceHandlers(spec, protocols) {
      return Object.entries(spec.methods).map(([, implSpec]) => createUniversalMethodHandler(implSpec, protocols));
    }
    __name(createUniversalServiceHandlers, "createUniversalServiceHandlers");
    function createUniversalMethodHandler(spec, protocols) {
      return negotiateProtocol(protocols.map((f) => f(spec)));
    }
    __name(createUniversalMethodHandler, "createUniversalMethodHandler");
    function negotiateProtocol(protocolHandlers) {
      if (protocolHandlers.length == 0) {
        throw new connect_error_js_1.ConnectError("at least one protocol is required", code_js_1.Code.Internal);
      }
      const service = protocolHandlers[0].service;
      const method = protocolHandlers[0].method;
      const requestPath = protocolHandlers[0].requestPath;
      if (protocolHandlers.some((h) => h.service !== service || h.method !== method)) {
        throw new connect_error_js_1.ConnectError("cannot negotiate protocol for different RPCs", code_js_1.Code.Internal);
      }
      if (protocolHandlers.some((h) => h.requestPath !== requestPath)) {
        throw new connect_error_js_1.ConnectError("cannot negotiate protocol for different requestPaths", code_js_1.Code.Internal);
      }
      async function protocolNegotiatingHandler(request) {
        var _a;
        if (method.methodKind == "bidi_streaming" && request.httpVersion.startsWith("1.")) {
          return Object.assign(Object.assign({}, universal_js_1.uResponseVersionNotSupported), {
            // Clients coded to expect full-duplex connections may hang if they've
            // mistakenly negotiated HTTP/1.1. To unblock them, we must close the
            // underlying TCP connection.
            header: new Headers({ Connection: "close" })
          });
        }
        const contentType = (_a = request.header.get("Content-Type")) !== null && _a !== void 0 ? _a : "";
        const matchingMethod = protocolHandlers.filter((h) => h.allowedMethods.includes(request.method));
        if (matchingMethod.length == 0) {
          return universal_js_1.uResponseMethodNotAllowed;
        }
        if (matchingMethod.length == 1 && contentType === "") {
          const onlyMatch = matchingMethod[0];
          return onlyMatch(request);
        }
        const matchingContentTypes = matchingMethod.filter((h) => h.supportedContentType(contentType));
        if (matchingContentTypes.length == 0) {
          return universal_js_1.uResponseUnsupportedMediaType;
        }
        const firstMatch = matchingContentTypes[0];
        return firstMatch(request);
      }
      __name(protocolNegotiatingHandler, "protocolNegotiatingHandler");
      return Object.assign(protocolNegotiatingHandler, {
        service,
        method,
        requestPath,
        supportedContentType: (0, content_type_matcher_js_1.contentTypeMatcher)(...protocolHandlers.map((h) => h.supportedContentType)),
        protocolNames: protocolHandlers.flatMap((h) => h.protocolNames).filter((value, index, array) => array.indexOf(value) === index),
        allowedMethods: protocolHandlers.flatMap((h) => h.allowedMethods).filter((value, index, array) => array.indexOf(value) === index)
      });
    }
    __name(negotiateProtocol, "negotiateProtocol");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc-web/handler-factory.js
var require_handler_factory = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc-web/handler-factory.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createHandlerFactory = createHandlerFactory;
    var connect_error_js_1 = require_connect_error();
    var code_js_1 = require_code();
    var implementation_js_1 = require_implementation();
    var trailer_js_1 = require_trailer();
    var headers_js_1 = require_headers2();
    var content_type_js_1 = require_content_type();
    var parse_timeout_js_1 = require_parse_timeout();
    var trailer_status_js_1 = require_trailer_status();
    var async_iterable_js_1 = require_async_iterable();
    var compression_js_1 = require_compression();
    var content_type_matcher_js_1 = require_content_type_matcher();
    var create_method_url_js_1 = require_create_method_url();
    var invoke_implementation_js_1 = require_invoke_implementation();
    var serialization_js_1 = require_serialization();
    var universal_handler_js_1 = require_universal_handler();
    var universal_js_1 = require_universal();
    var protocolName = "grpc-web";
    var methodPost = "POST";
    function createHandlerFactory(options) {
      const opt = (0, universal_handler_js_1.validateUniversalHandlerOptions)(options);
      const trailerSerialization = (0, trailer_js_1.createTrailerSerialization)();
      function fact(spec) {
        const h = createHandler(opt, trailerSerialization, spec);
        return Object.assign(h, {
          protocolNames: [protocolName],
          allowedMethods: [methodPost],
          supportedContentType: (0, content_type_matcher_js_1.contentTypeMatcher)(content_type_js_1.contentTypeRegExp),
          requestPath: (0, create_method_url_js_1.createMethodUrl)("/", spec.method),
          service: spec.method.parent,
          method: spec.method
        });
      }
      __name(fact, "fact");
      fact.protocolName = protocolName;
      return fact;
    }
    __name(createHandlerFactory, "createHandlerFactory");
    function createHandler(opt, trailerSerialization, spec) {
      const serialization = (0, serialization_js_1.createMethodSerializationLookup)(spec.method, opt.binaryOptions, opt.jsonOptions, opt);
      return /* @__PURE__ */ __name(async function handle(req) {
        (0, universal_js_1.assertByteStreamRequest)(req);
        const type = (0, content_type_js_1.parseContentType)(req.header.get(headers_js_1.headerContentType));
        if (type == void 0 || type.text) {
          return universal_js_1.uResponseUnsupportedMediaType;
        }
        if (req.method !== methodPost) {
          return universal_js_1.uResponseMethodNotAllowed;
        }
        const timeout = (0, parse_timeout_js_1.parseTimeout)(req.header.get(headers_js_1.headerTimeout), opt.maxTimeoutMs);
        const context = (0, implementation_js_1.createHandlerContext)(Object.assign(Object.assign({}, spec), { service: spec.method.parent, requestMethod: req.method, protocolName, timeoutMs: timeout.timeoutMs, shutdownSignal: opt.shutdownSignal, requestSignal: req.signal, requestHeader: req.header, url: req.url, responseHeader: {
          [headers_js_1.headerContentType]: type.binary ? content_type_js_1.contentTypeProto : content_type_js_1.contentTypeJson
        }, responseTrailer: {
          [headers_js_1.headerGrpcStatus]: trailer_status_js_1.grpcStatusOk
        }, contextValues: req.contextValues }));
        const compression = (0, compression_js_1.compressionNegotiate)(opt.acceptCompression, req.header.get(headers_js_1.headerEncoding), req.header.get(headers_js_1.headerAcceptEncoding), headers_js_1.headerAcceptEncoding);
        if (compression.response) {
          context.responseHeader.set(headers_js_1.headerEncoding, compression.response.name);
        }
        const inputIt = (0, async_iterable_js_1.pipe)(req.body, (0, async_iterable_js_1.transformPrepend)(() => {
          if (compression.error)
            throw compression.error;
          if (timeout.error)
            throw timeout.error;
          return void 0;
        }), (0, async_iterable_js_1.transformSplitEnvelope)(opt.readMaxBytes), (0, async_iterable_js_1.transformDecompressEnvelope)(compression.request, opt.readMaxBytes), (0, async_iterable_js_1.transformParseEnvelope)(serialization.getI(type.binary), trailer_js_1.trailerFlag));
        const it = (0, invoke_implementation_js_1.transformInvokeImplementation)(spec, context, opt.interceptors)(inputIt)[Symbol.asyncIterator]();
        const outputIt = (0, async_iterable_js_1.pipe)(
          // We wrap the iterator in an async iterator to ensure that the
          // abort signal is aborted when the iterator is done.
          {
            [Symbol.asyncIterator]() {
              return {
                next: /* @__PURE__ */ __name(() => it.next(), "next"),
                throw: /* @__PURE__ */ __name((e) => {
                  var _a, _b;
                  context.abort(e);
                  return (_b = (_a = it.throw) === null || _a === void 0 ? void 0 : _a.call(it, e)) !== null && _b !== void 0 ? _b : Promise.reject({ done: true });
                }, "throw"),
                return: /* @__PURE__ */ __name((v) => {
                  var _a, _b;
                  context.abort();
                  return (_b = (_a = it.return) === null || _a === void 0 ? void 0 : _a.call(it, v)) !== null && _b !== void 0 ? _b : Promise.resolve({ done: true, value: v });
                }, "return")
              };
            }
          },
          (0, async_iterable_js_1.transformSerializeEnvelope)(serialization.getO(type.binary)),
          (0, async_iterable_js_1.transformCatchFinally)((e) => {
            context.abort(e);
            if (e instanceof connect_error_js_1.ConnectError) {
              (0, trailer_status_js_1.setTrailerStatus)(context.responseTrailer, e);
            } else if (e !== void 0) {
              (0, trailer_status_js_1.setTrailerStatus)(context.responseTrailer, new connect_error_js_1.ConnectError("internal error", code_js_1.Code.Internal, void 0, void 0, e));
            }
            return {
              flags: trailer_js_1.trailerFlag,
              data: trailerSerialization.serialize(context.responseTrailer)
            };
          }),
          (0, async_iterable_js_1.transformCompressEnvelope)(compression.response, opt.compressMinBytes),
          (0, async_iterable_js_1.transformJoinEnvelopes)(),
          { propagateDownStreamError: true }
        );
        return Object.assign(Object.assign({}, universal_js_1.uResponseOk), {
          // We wait for the first response body bytes before resolving, so that
          // implementations have a chance to add headers before an adapter commits
          // them to the wire.
          body: await (0, async_iterable_js_1.untilFirst)(outputIt),
          header: context.responseHeader
        });
      }, "handle");
    }
    __name(createHandler, "createHandler");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/content-type.js
var require_content_type2 = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/content-type.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.contentTypeJson = exports.contentTypeProto = exports.contentTypeRegExp = void 0;
    exports.parseContentType = parseContentType;
    exports.contentTypeRegExp = /^application\/grpc(?:\+(?:(json)(?:; ?charset=utf-?8)?|proto))?$/i;
    exports.contentTypeProto = "application/grpc+proto";
    exports.contentTypeJson = "application/grpc+json";
    function parseContentType(contentType) {
      const match = contentType === null || contentType === void 0 ? void 0 : contentType.match(exports.contentTypeRegExp);
      if (!match) {
        return void 0;
      }
      const binary = !match[1];
      return { binary };
    }
    __name(parseContentType, "parseContentType");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/handler-factory.js
var require_handler_factory2 = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/handler-factory.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createHandlerFactory = createHandlerFactory;
    var implementation_js_1 = require_implementation();
    var connect_error_js_1 = require_connect_error();
    var code_js_1 = require_code();
    var content_type_js_1 = require_content_type2();
    var headers_js_1 = require_headers();
    var trailer_status_js_1 = require_trailer_status();
    var parse_timeout_js_1 = require_parse_timeout();
    var async_iterable_js_1 = require_async_iterable();
    var compression_js_1 = require_compression();
    var content_type_matcher_js_1 = require_content_type_matcher();
    var create_method_url_js_1 = require_create_method_url();
    var invoke_implementation_js_1 = require_invoke_implementation();
    var serialization_js_1 = require_serialization();
    var universal_handler_js_1 = require_universal_handler();
    var universal_js_1 = require_universal();
    var protocolName = "grpc";
    var methodPost = "POST";
    function createHandlerFactory(options) {
      const opt = (0, universal_handler_js_1.validateUniversalHandlerOptions)(options);
      function fact(spec) {
        const h = createHandler(opt, spec);
        return Object.assign(h, {
          protocolNames: [protocolName],
          allowedMethods: [methodPost],
          supportedContentType: (0, content_type_matcher_js_1.contentTypeMatcher)(content_type_js_1.contentTypeRegExp),
          requestPath: (0, create_method_url_js_1.createMethodUrl)("/", spec.method),
          service: spec.method.parent,
          method: spec.method
        });
      }
      __name(fact, "fact");
      fact.protocolName = protocolName;
      return fact;
    }
    __name(createHandlerFactory, "createHandlerFactory");
    function createHandler(opt, spec) {
      const serialization = (0, serialization_js_1.createMethodSerializationLookup)(spec.method, opt.binaryOptions, opt.jsonOptions, opt);
      return /* @__PURE__ */ __name(async function handle(req) {
        (0, universal_js_1.assertByteStreamRequest)(req);
        const type = (0, content_type_js_1.parseContentType)(req.header.get(headers_js_1.headerContentType));
        if (type == void 0) {
          return universal_js_1.uResponseUnsupportedMediaType;
        }
        if (req.method !== methodPost) {
          return universal_js_1.uResponseMethodNotAllowed;
        }
        const timeout = (0, parse_timeout_js_1.parseTimeout)(req.header.get(headers_js_1.headerTimeout), opt.maxTimeoutMs);
        const context = (0, implementation_js_1.createHandlerContext)(Object.assign(Object.assign({}, spec), { service: spec.method.parent, requestMethod: req.method, protocolName, timeoutMs: timeout.timeoutMs, shutdownSignal: opt.shutdownSignal, requestSignal: req.signal, requestHeader: req.header, url: req.url, responseHeader: {
          [headers_js_1.headerContentType]: type.binary ? content_type_js_1.contentTypeProto : content_type_js_1.contentTypeJson
        }, responseTrailer: {
          [headers_js_1.headerGrpcStatus]: trailer_status_js_1.grpcStatusOk
        }, contextValues: req.contextValues }));
        const compression = (0, compression_js_1.compressionNegotiate)(opt.acceptCompression, req.header.get(headers_js_1.headerEncoding), req.header.get(headers_js_1.headerAcceptEncoding), headers_js_1.headerAcceptEncoding);
        if (compression.response) {
          context.responseHeader.set(headers_js_1.headerEncoding, compression.response.name);
        }
        const inputIt = (0, async_iterable_js_1.pipe)(req.body, (0, async_iterable_js_1.transformPrepend)(() => {
          if (compression.error)
            throw compression.error;
          if (timeout.error)
            throw timeout.error;
          return void 0;
        }), (0, async_iterable_js_1.transformSplitEnvelope)(opt.readMaxBytes), (0, async_iterable_js_1.transformDecompressEnvelope)(compression.request, opt.readMaxBytes), (0, async_iterable_js_1.transformParseEnvelope)(serialization.getI(type.binary)));
        const it = (0, invoke_implementation_js_1.transformInvokeImplementation)(spec, context, opt.interceptors)(inputIt)[Symbol.asyncIterator]();
        const outputIt = (0, async_iterable_js_1.pipe)(
          // We wrap the iterator in an async iterator to ensure that the
          // abort signal is aborted when the iterator is done.
          {
            [Symbol.asyncIterator]() {
              return {
                next: /* @__PURE__ */ __name(() => it.next(), "next"),
                throw: /* @__PURE__ */ __name((e) => {
                  var _a, _b;
                  context.abort(e);
                  return (_b = (_a = it.throw) === null || _a === void 0 ? void 0 : _a.call(it, e)) !== null && _b !== void 0 ? _b : Promise.reject({ done: true });
                }, "throw"),
                return: /* @__PURE__ */ __name((v) => {
                  var _a, _b;
                  context.abort();
                  return (_b = (_a = it.return) === null || _a === void 0 ? void 0 : _a.call(it, v)) !== null && _b !== void 0 ? _b : Promise.resolve({ done: true, value: v });
                }, "return")
              };
            }
          },
          (0, async_iterable_js_1.transformSerializeEnvelope)(serialization.getO(type.binary)),
          (0, async_iterable_js_1.transformCompressEnvelope)(compression.response, opt.compressMinBytes),
          (0, async_iterable_js_1.transformJoinEnvelopes)(),
          (0, async_iterable_js_1.transformCatchFinally)((e) => {
            context.abort(e);
            if (e instanceof connect_error_js_1.ConnectError) {
              (0, trailer_status_js_1.setTrailerStatus)(context.responseTrailer, e);
            } else if (e !== void 0) {
              (0, trailer_status_js_1.setTrailerStatus)(context.responseTrailer, new connect_error_js_1.ConnectError("internal error", code_js_1.Code.Internal, void 0, void 0, e));
            }
          }),
          { propagateDownStreamError: true }
        );
        return Object.assign(Object.assign({}, universal_js_1.uResponseOk), {
          // We wait for the first response body bytes before resolving, so that
          // implementations have a chance to add headers before an adapter commits
          // them to the wire.
          body: await (0, async_iterable_js_1.untilFirst)(outputIt),
          header: context.responseHeader,
          trailer: context.responseTrailer
        });
      }, "handle");
    }
    __name(createHandler, "createHandler");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/content-type.js
var require_content_type3 = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/content-type.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.contentTypeStreamJson = exports.contentTypeStreamProto = exports.contentTypeUnaryJson = exports.contentTypeUnaryProto = exports.contentTypeStreamRegExp = exports.contentTypeUnaryRegExp = exports.contentTypeRegExp = void 0;
    exports.parseContentType = parseContentType;
    exports.parseEncodingQuery = parseEncodingQuery;
    exports.contentTypeRegExp = /^application\/(connect\+)?(?:(json)(?:; ?charset=utf-?8)?|(proto))$/i;
    exports.contentTypeUnaryRegExp = /^application\/(?:json(?:; ?charset=utf-?8)?|proto)$/i;
    exports.contentTypeStreamRegExp = /^application\/connect\+?(?:json(?:; ?charset=utf-?8)?|proto)$/i;
    exports.contentTypeUnaryProto = "application/proto";
    exports.contentTypeUnaryJson = "application/json";
    exports.contentTypeStreamProto = "application/connect+proto";
    exports.contentTypeStreamJson = "application/connect+json";
    var encodingProto = "proto";
    var encodingJson = "json";
    function parseContentType(contentType) {
      const match = contentType === null || contentType === void 0 ? void 0 : contentType.match(exports.contentTypeRegExp);
      if (!match) {
        return void 0;
      }
      const stream = !!match[1];
      const binary = !!match[3];
      return { stream, binary };
    }
    __name(parseContentType, "parseContentType");
    function parseEncodingQuery(encoding) {
      switch (encoding) {
        case encodingProto:
          return { stream: false, binary: true };
        case encodingJson:
          return { stream: false, binary: false };
        default:
          return void 0;
      }
    }
    __name(parseEncodingQuery, "parseEncodingQuery");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/error-json.js
var require_error_json = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/error-json.js"(exports) {
    "use strict";
    init_esm();
    var __rest = exports && exports.__rest || function(s, e) {
      var t = {};
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
      if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
          if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
            t[p[i]] = s[p[i]];
        }
      return t;
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.errorFromJson = errorFromJson;
    exports.errorFromJsonBytes = errorFromJsonBytes;
    exports.errorToJson = errorToJson;
    exports.errorToJsonBytes = errorToJsonBytes;
    var wire_1 = require_wire();
    var protobuf_1 = require_cjs();
    var code_js_1 = require_code();
    var connect_error_js_1 = require_connect_error();
    var code_string_js_1 = require_code_string();
    function errorFromJson(jsonValue, metadata, fallback) {
      var _a;
      if (metadata) {
        new Headers(metadata).forEach((value, key) => fallback.metadata.append(key, value));
      }
      if (typeof jsonValue !== "object" || jsonValue == null || Array.isArray(jsonValue)) {
        throw fallback;
      }
      let code = fallback.code;
      if ("code" in jsonValue && typeof jsonValue.code === "string") {
        code = (_a = (0, code_string_js_1.codeFromString)(jsonValue.code)) !== null && _a !== void 0 ? _a : code;
      }
      const message = jsonValue.message;
      if (message != null && typeof message !== "string") {
        throw fallback;
      }
      const error = new connect_error_js_1.ConnectError(message !== null && message !== void 0 ? message : "", code, metadata);
      if ("details" in jsonValue && Array.isArray(jsonValue.details)) {
        for (const detail of jsonValue.details) {
          if (detail === null || typeof detail != "object" || Array.isArray(detail) || typeof detail.type != "string" || typeof detail.value != "string") {
            throw fallback;
          }
          try {
            error.details.push({
              type: detail.type,
              value: (0, wire_1.base64Decode)(detail.value),
              debug: detail.debug
            });
          } catch (e) {
            throw fallback;
          }
        }
      }
      return error;
    }
    __name(errorFromJson, "errorFromJson");
    function errorFromJsonBytes(bytes, metadata, fallback) {
      let jsonValue;
      try {
        jsonValue = JSON.parse(new TextDecoder().decode(bytes));
      } catch (e) {
        throw fallback;
      }
      return errorFromJson(jsonValue, metadata, fallback);
    }
    __name(errorFromJsonBytes, "errorFromJsonBytes");
    function errorToJson(error, jsonWriteOptions) {
      const o = {
        code: (0, code_string_js_1.codeToString)(error.code)
      };
      if (error.rawMessage.length > 0) {
        o.message = error.rawMessage;
      }
      if (error.details.length > 0) {
        o.details = error.details.map((detail) => {
          if ("desc" in detail) {
            const msg = (0, protobuf_1.create)(detail.desc, detail.value);
            const i = {
              type: detail.desc.typeName,
              value: (0, protobuf_1.toBinary)(detail.desc, msg)
            };
            try {
              i.debug = (0, protobuf_1.toJson)(detail.desc, msg, jsonWriteOptions);
            } catch (e) {
            }
            return i;
          }
          return detail;
        }).map((_a) => {
          var { value } = _a, rest = __rest(_a, ["value"]);
          return Object.assign(Object.assign({}, rest), { value: (0, wire_1.base64Encode)(value, "std_raw") });
        });
      }
      return o;
    }
    __name(errorToJson, "errorToJson");
    function errorToJsonBytes(error, jsonWriteOptions) {
      const textEncoder = new TextEncoder();
      try {
        const jsonObject = errorToJson(error, jsonWriteOptions);
        const jsonString = JSON.stringify(jsonObject);
        return textEncoder.encode(jsonString);
      } catch (e) {
        const m = e instanceof Error ? e.message : String(e);
        throw new connect_error_js_1.ConnectError(`failed to serialize Connect Error: ${m}`, code_js_1.Code.Internal);
      }
    }
    __name(errorToJsonBytes, "errorToJsonBytes");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/end-stream.js
var require_end_stream = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/end-stream.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.endStreamFlag = void 0;
    exports.endStreamFromJson = endStreamFromJson;
    exports.endStreamToJson = endStreamToJson;
    exports.createEndStreamSerialization = createEndStreamSerialization;
    var error_json_js_1 = require_error_json();
    var http_headers_js_1 = require_http_headers();
    var connect_error_js_1 = require_connect_error();
    var code_js_1 = require_code();
    exports.endStreamFlag = 2;
    function endStreamFromJson(data) {
      const parseErr = new connect_error_js_1.ConnectError("invalid end stream", code_js_1.Code.Unknown);
      let jsonValue;
      try {
        jsonValue = JSON.parse(typeof data == "string" ? data : new TextDecoder().decode(data));
      } catch (e) {
        throw parseErr;
      }
      if (typeof jsonValue != "object" || jsonValue == null || Array.isArray(jsonValue)) {
        throw parseErr;
      }
      const metadata = new Headers();
      if ("metadata" in jsonValue) {
        if (typeof jsonValue.metadata != "object" || jsonValue.metadata == null || Array.isArray(jsonValue.metadata)) {
          throw parseErr;
        }
        for (const [key, values] of Object.entries(jsonValue.metadata)) {
          if (!Array.isArray(values) || values.some((value) => typeof value != "string")) {
            throw parseErr;
          }
          for (const value of values) {
            metadata.append(key, value);
          }
        }
      }
      const error = "error" in jsonValue && jsonValue.error != null ? (0, error_json_js_1.errorFromJson)(jsonValue.error, metadata, parseErr) : void 0;
      return { metadata, error };
    }
    __name(endStreamFromJson, "endStreamFromJson");
    function endStreamToJson(metadata, error, jsonWriteOptions) {
      const es = {};
      if (error !== void 0) {
        es.error = (0, error_json_js_1.errorToJson)(error, jsonWriteOptions);
        metadata = (0, http_headers_js_1.appendHeaders)(metadata, error.metadata);
      }
      let hasMetadata = false;
      const md = {};
      metadata.forEach((value, key) => {
        hasMetadata = true;
        md[key] = [value];
      });
      if (hasMetadata) {
        es.metadata = md;
      }
      return es;
    }
    __name(endStreamToJson, "endStreamToJson");
    function createEndStreamSerialization(options) {
      const textEncoder = new TextEncoder();
      return {
        serialize(data) {
          try {
            const jsonObject = endStreamToJson(data.metadata, data.error, options);
            const jsonString = JSON.stringify(jsonObject);
            return textEncoder.encode(jsonString);
          } catch (e) {
            const m = e instanceof Error ? e.message : String(e);
            throw new connect_error_js_1.ConnectError(`failed to serialize EndStreamResponse: ${m}`, code_js_1.Code.Internal);
          }
        },
        parse(data) {
          try {
            return endStreamFromJson(data);
          } catch (e) {
            const m = e instanceof Error ? e.message : String(e);
            throw new connect_error_js_1.ConnectError(`failed to parse EndStreamResponse: ${m}`, code_js_1.Code.InvalidArgument);
          }
        }
      };
    }
    __name(createEndStreamSerialization, "createEndStreamSerialization");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/headers.js
var require_headers3 = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/headers.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.headerUserAgent = exports.headerProtocolVersion = exports.headerTimeout = exports.headerStreamAcceptEncoding = exports.headerUnaryAcceptEncoding = exports.headerStreamEncoding = exports.headerUnaryEncoding = exports.headerUnaryContentLength = exports.headerContentType = void 0;
    exports.headerContentType = "Content-Type";
    exports.headerUnaryContentLength = "Content-Length";
    exports.headerUnaryEncoding = "Content-Encoding";
    exports.headerStreamEncoding = "Connect-Content-Encoding";
    exports.headerUnaryAcceptEncoding = "Accept-Encoding";
    exports.headerStreamAcceptEncoding = "Connect-Accept-Encoding";
    exports.headerTimeout = "Connect-Timeout-Ms";
    exports.headerProtocolVersion = "Connect-Protocol-Version";
    exports.headerUserAgent = "User-Agent";
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/http-status.js
var require_http_status = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/http-status.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.codeFromHttpStatus = codeFromHttpStatus;
    exports.codeToHttpStatus = codeToHttpStatus;
    var code_js_1 = require_code();
    function codeFromHttpStatus(httpStatus) {
      switch (httpStatus) {
        case 400:
          return code_js_1.Code.Internal;
        case 401:
          return code_js_1.Code.Unauthenticated;
        case 403:
          return code_js_1.Code.PermissionDenied;
        case 404:
          return code_js_1.Code.Unimplemented;
        case 429:
          return code_js_1.Code.Unavailable;
        case 502:
          return code_js_1.Code.Unavailable;
        case 503:
          return code_js_1.Code.Unavailable;
        case 504:
          return code_js_1.Code.Unavailable;
        default:
          return code_js_1.Code.Unknown;
      }
    }
    __name(codeFromHttpStatus, "codeFromHttpStatus");
    function codeToHttpStatus(code) {
      switch (code) {
        case code_js_1.Code.Canceled:
          return 499;
        // Client Closed Request
        case code_js_1.Code.Unknown:
          return 500;
        // Internal Server Error
        case code_js_1.Code.InvalidArgument:
          return 400;
        // Bad Request
        case code_js_1.Code.DeadlineExceeded:
          return 504;
        // Gateway Timeout
        case code_js_1.Code.NotFound:
          return 404;
        // Not Found
        case code_js_1.Code.AlreadyExists:
          return 409;
        // Conflict
        case code_js_1.Code.PermissionDenied:
          return 403;
        // Forbidden
        case code_js_1.Code.ResourceExhausted:
          return 429;
        // Too Many Requests
        case code_js_1.Code.FailedPrecondition:
          return 400;
        // Bad Request
        case code_js_1.Code.Aborted:
          return 409;
        // Conflict
        case code_js_1.Code.OutOfRange:
          return 400;
        // Bad Request
        case code_js_1.Code.Unimplemented:
          return 501;
        // Not Implemented
        case code_js_1.Code.Internal:
          return 500;
        // Internal Server Error
        case code_js_1.Code.Unavailable:
          return 503;
        // Service Unavailable
        case code_js_1.Code.DataLoss:
          return 500;
        // Internal Server Error
        case code_js_1.Code.Unauthenticated:
          return 401;
        // Unauthorized
        default:
          return 500;
      }
    }
    __name(codeToHttpStatus, "codeToHttpStatus");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/parse-timeout.js
var require_parse_timeout2 = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/parse-timeout.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.parseTimeout = parseTimeout;
    var code_js_1 = require_code();
    var connect_error_js_1 = require_connect_error();
    function parseTimeout(value, maxTimeoutMs) {
      if (value === null) {
        return {};
      }
      const results = /^\d{1,10}$/.exec(value);
      if (results === null) {
        return {
          error: new connect_error_js_1.ConnectError(`protocol error: invalid connect timeout value: ${value}`, code_js_1.Code.InvalidArgument)
        };
      }
      const timeoutMs = parseInt(results[0]);
      if (timeoutMs > maxTimeoutMs) {
        return {
          timeoutMs,
          error: new connect_error_js_1.ConnectError(`timeout ${timeoutMs}ms must be <= ${maxTimeoutMs}`, code_js_1.Code.InvalidArgument)
        };
      }
      return {
        timeoutMs: parseInt(results[0])
      };
    }
    __name(parseTimeout, "parseTimeout");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/query-params.js
var require_query_params = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/query-params.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.paramMessage = exports.paramBase64 = exports.paramCompression = exports.paramEncoding = exports.paramConnectVersion = void 0;
    exports.paramConnectVersion = "connect";
    exports.paramEncoding = "encoding";
    exports.paramCompression = "compression";
    exports.paramBase64 = "base64";
    exports.paramMessage = "message";
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/trailer-mux.js
var require_trailer_mux = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/trailer-mux.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.trailerDemux = trailerDemux;
    exports.trailerMux = trailerMux;
    function trailerDemux(header) {
      const h = new Headers(), t = new Headers();
      header.forEach((value, key) => {
        if (key.toLowerCase().startsWith("trailer-")) {
          t.append(key.substring(8), value);
        } else {
          h.append(key, value);
        }
      });
      return [h, t];
    }
    __name(trailerDemux, "trailerDemux");
    function trailerMux(header, trailer) {
      const h = new Headers(header);
      trailer.forEach((value, key) => {
        h.append(`trailer-${key}`, value);
      });
      return h;
    }
    __name(trailerMux, "trailerMux");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/version.js
var require_version = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/version.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.protocolVersion = void 0;
    exports.requireProtocolVersionHeader = requireProtocolVersionHeader;
    exports.requireProtocolVersionParam = requireProtocolVersionParam;
    var headers_js_1 = require_headers3();
    var query_params_js_1 = require_query_params();
    var connect_error_js_1 = require_connect_error();
    var code_js_1 = require_code();
    exports.protocolVersion = "1";
    function requireProtocolVersionHeader(requestHeader) {
      const v = requestHeader.get(headers_js_1.headerProtocolVersion);
      if (v === null) {
        throw new connect_error_js_1.ConnectError(`missing required header: set ${headers_js_1.headerProtocolVersion} to "${exports.protocolVersion}"`, code_js_1.Code.InvalidArgument);
      } else if (v !== exports.protocolVersion) {
        throw new connect_error_js_1.ConnectError(`${headers_js_1.headerProtocolVersion} must be "${exports.protocolVersion}": got "${v}"`, code_js_1.Code.InvalidArgument);
      }
    }
    __name(requireProtocolVersionHeader, "requireProtocolVersionHeader");
    function requireProtocolVersionParam(queryParams) {
      const v = queryParams.get(query_params_js_1.paramConnectVersion);
      if (v === null) {
        throw new connect_error_js_1.ConnectError(`missing required parameter: set ${query_params_js_1.paramConnectVersion} to "v${exports.protocolVersion}"`, code_js_1.Code.InvalidArgument);
      } else if (v !== `v${exports.protocolVersion}`) {
        throw new connect_error_js_1.ConnectError(`${query_params_js_1.paramConnectVersion} must be "v${exports.protocolVersion}": got "${v}"`, code_js_1.Code.InvalidArgument);
      }
    }
    __name(requireProtocolVersionParam, "requireProtocolVersionParam");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/handler-factory.js
var require_handler_factory3 = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/handler-factory.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createHandlerFactory = createHandlerFactory;
    var protobuf_1 = require_cjs();
    var wire_1 = require_wire();
    var code_js_1 = require_code();
    var connect_error_js_1 = require_connect_error();
    var implementation_js_1 = require_implementation();
    var content_type_js_1 = require_content_type3();
    var end_stream_js_1 = require_end_stream();
    var error_json_js_1 = require_error_json();
    var headers_js_1 = require_headers3();
    var http_status_js_1 = require_http_status();
    var parse_timeout_js_1 = require_parse_timeout2();
    var query_params_js_1 = require_query_params();
    var trailer_mux_js_1 = require_trailer_mux();
    var version_js_1 = require_version();
    var compression_js_1 = require_compression();
    var serialization_js_1 = require_serialization();
    var universal_handler_js_1 = require_universal_handler();
    var universal_js_1 = require_universal();
    var async_iterable_js_1 = require_async_iterable();
    var content_type_matcher_js_1 = require_content_type_matcher();
    var create_method_url_js_1 = require_create_method_url();
    var invoke_implementation_js_1 = require_invoke_implementation();
    var wkt_1 = require_wkt();
    var protocolName = "connect";
    var methodPost = "POST";
    var methodGet = "GET";
    function createHandlerFactory(options) {
      const opt = (0, universal_handler_js_1.validateUniversalHandlerOptions)(options);
      const endStreamSerialization = (0, end_stream_js_1.createEndStreamSerialization)(opt.jsonOptions);
      function fact(spec) {
        let h;
        let contentTypeRegExp;
        const serialization = (0, serialization_js_1.createMethodSerializationLookup)(spec.method, opt.binaryOptions, opt.jsonOptions, opt);
        switch (spec.kind) {
          case "unary":
            contentTypeRegExp = content_type_js_1.contentTypeUnaryRegExp;
            h = createUnaryHandler(opt, spec, serialization);
            break;
          default:
            contentTypeRegExp = content_type_js_1.contentTypeStreamRegExp;
            h = createStreamHandler(opt, spec, serialization, endStreamSerialization);
            break;
        }
        const allowedMethods = [methodPost];
        if (spec.method.idempotency === wkt_1.MethodOptions_IdempotencyLevel.NO_SIDE_EFFECTS) {
          allowedMethods.push(methodGet);
        }
        return Object.assign(h, {
          protocolNames: [protocolName],
          supportedContentType: (0, content_type_matcher_js_1.contentTypeMatcher)(contentTypeRegExp),
          allowedMethods,
          requestPath: (0, create_method_url_js_1.createMethodUrl)("/", spec.method),
          service: spec.method.parent,
          method: spec.method
        });
      }
      __name(fact, "fact");
      fact.protocolName = protocolName;
      return fact;
    }
    __name(createHandlerFactory, "createHandlerFactory");
    function createUnaryHandler(opt, spec, serialization) {
      return /* @__PURE__ */ __name(async function handle(req) {
        const isGet = req.method == methodGet;
        if (isGet && spec.method.idempotency != wkt_1.MethodOptions_IdempotencyLevel.NO_SIDE_EFFECTS) {
          return universal_js_1.uResponseMethodNotAllowed;
        }
        const queryParams = new URL(req.url).searchParams;
        const compressionRequested = isGet ? queryParams.get(query_params_js_1.paramCompression) : req.header.get(headers_js_1.headerUnaryEncoding);
        const type = isGet ? (0, content_type_js_1.parseEncodingQuery)(queryParams.get(query_params_js_1.paramEncoding)) : (0, content_type_js_1.parseContentType)(req.header.get(headers_js_1.headerContentType));
        if (type == void 0 || type.stream) {
          return universal_js_1.uResponseUnsupportedMediaType;
        }
        const timeout = (0, parse_timeout_js_1.parseTimeout)(req.header.get(headers_js_1.headerTimeout), opt.maxTimeoutMs);
        const context = (0, implementation_js_1.createHandlerContext)(Object.assign(Object.assign({}, spec), { service: spec.method.parent, requestMethod: req.method, protocolName, timeoutMs: timeout.timeoutMs, shutdownSignal: opt.shutdownSignal, requestSignal: req.signal, requestHeader: req.header, url: req.url, responseHeader: {
          [headers_js_1.headerContentType]: type.binary ? content_type_js_1.contentTypeUnaryProto : content_type_js_1.contentTypeUnaryJson
        }, contextValues: req.contextValues }));
        const compression = (0, compression_js_1.compressionNegotiate)(opt.acceptCompression, compressionRequested, req.header.get(headers_js_1.headerUnaryAcceptEncoding), headers_js_1.headerUnaryAcceptEncoding);
        let status = universal_js_1.uResponseOk.status;
        let body;
        try {
          if (opt.requireConnectProtocolHeader) {
            if (isGet) {
              (0, version_js_1.requireProtocolVersionParam)(queryParams);
            } else {
              (0, version_js_1.requireProtocolVersionHeader)(req.header);
            }
          }
          if (compression.error) {
            throw compression.error;
          }
          if (timeout.error) {
            throw timeout.error;
          }
          let reqBody;
          if (isGet) {
            reqBody = await readUnaryMessageFromQuery(opt.readMaxBytes, compression.request, queryParams);
          } else {
            reqBody = await readUnaryMessageFromBody(opt.readMaxBytes, compression.request, req);
          }
          const input = parseUnaryMessage(spec.method, type.binary, serialization, reqBody);
          const output = await (0, invoke_implementation_js_1.invokeUnaryImplementation)(spec, context, input, opt.interceptors);
          body = serialization.getO(type.binary).serialize(output);
        } catch (e) {
          context.abort(e);
          let error;
          if (e instanceof connect_error_js_1.ConnectError) {
            error = e;
          } else {
            error = new connect_error_js_1.ConnectError("internal error", code_js_1.Code.Internal, void 0, void 0, e);
          }
          status = (0, http_status_js_1.codeToHttpStatus)(error.code);
          context.responseHeader.set(headers_js_1.headerContentType, content_type_js_1.contentTypeUnaryJson);
          error.metadata.forEach((value, key) => {
            context.responseHeader.set(key, value);
          });
          body = (0, error_json_js_1.errorToJsonBytes)(error, opt.jsonOptions);
        } finally {
          context.abort();
        }
        if (compression.response && body.byteLength >= opt.compressMinBytes) {
          body = await compression.response.compress(body);
          context.responseHeader.set(headers_js_1.headerUnaryEncoding, compression.response.name);
        }
        const header = (0, trailer_mux_js_1.trailerMux)(context.responseHeader, context.responseTrailer);
        header.set(headers_js_1.headerUnaryContentLength, body.byteLength.toString(10));
        return {
          status,
          body: (0, async_iterable_js_1.createAsyncIterable)([body]),
          header
        };
      }, "handle");
    }
    __name(createUnaryHandler, "createUnaryHandler");
    async function readUnaryMessageFromBody(readMaxBytes, compression, request) {
      if (typeof request.body == "object" && request.body !== null && Symbol.asyncIterator in request.body) {
        let reqBytes = await (0, async_iterable_js_1.readAllBytes)(request.body, readMaxBytes, request.header.get(headers_js_1.headerUnaryContentLength));
        if (compression) {
          reqBytes = await compression.decompress(reqBytes, readMaxBytes);
        }
        return reqBytes;
      }
      return request.body;
    }
    __name(readUnaryMessageFromBody, "readUnaryMessageFromBody");
    async function readUnaryMessageFromQuery(readMaxBytes, compression, queryParams) {
      var _a;
      const base64 = queryParams.get(query_params_js_1.paramBase64);
      const message = (_a = queryParams.get(query_params_js_1.paramMessage)) !== null && _a !== void 0 ? _a : "";
      let decoded;
      if (base64 === "1") {
        decoded = (0, wire_1.base64Decode)(message);
      } else {
        decoded = new TextEncoder().encode(message);
      }
      if (compression) {
        decoded = await compression.decompress(decoded, readMaxBytes);
      }
      return decoded;
    }
    __name(readUnaryMessageFromQuery, "readUnaryMessageFromQuery");
    function parseUnaryMessage(method, useBinaryFormat, serialization, input) {
      if (input instanceof Uint8Array) {
        return serialization.getI(useBinaryFormat).parse(input);
      }
      if (useBinaryFormat) {
        throw new connect_error_js_1.ConnectError("received parsed JSON request body, but content-type indicates binary format", code_js_1.Code.Internal);
      }
      try {
        return (0, protobuf_1.fromJson)(method.input, input);
      } catch (e) {
        throw connect_error_js_1.ConnectError.from(e, code_js_1.Code.InvalidArgument);
      }
    }
    __name(parseUnaryMessage, "parseUnaryMessage");
    function createStreamHandler(opt, spec, serialization, endStreamSerialization) {
      return /* @__PURE__ */ __name(async function handle(req) {
        (0, universal_js_1.assertByteStreamRequest)(req);
        const type = (0, content_type_js_1.parseContentType)(req.header.get(headers_js_1.headerContentType));
        if (type == void 0 || !type.stream) {
          return universal_js_1.uResponseUnsupportedMediaType;
        }
        if (req.method !== methodPost) {
          return universal_js_1.uResponseMethodNotAllowed;
        }
        const timeout = (0, parse_timeout_js_1.parseTimeout)(req.header.get(headers_js_1.headerTimeout), opt.maxTimeoutMs);
        const context = (0, implementation_js_1.createHandlerContext)(Object.assign(Object.assign({}, spec), { service: spec.method.parent, requestMethod: req.method, protocolName, timeoutMs: timeout.timeoutMs, shutdownSignal: opt.shutdownSignal, requestSignal: req.signal, requestHeader: req.header, url: req.url, responseHeader: {
          [headers_js_1.headerContentType]: type.binary ? content_type_js_1.contentTypeStreamProto : content_type_js_1.contentTypeStreamJson
        }, contextValues: req.contextValues }));
        const compression = (0, compression_js_1.compressionNegotiate)(opt.acceptCompression, req.header.get(headers_js_1.headerStreamEncoding), req.header.get(headers_js_1.headerStreamAcceptEncoding), headers_js_1.headerStreamAcceptEncoding);
        if (compression.response) {
          context.responseHeader.set(headers_js_1.headerStreamEncoding, compression.response.name);
        }
        const inputIt = (0, async_iterable_js_1.pipe)(req.body, (0, async_iterable_js_1.transformPrepend)(() => {
          if (opt.requireConnectProtocolHeader) {
            (0, version_js_1.requireProtocolVersionHeader)(req.header);
          }
          if (compression.error)
            throw compression.error;
          if (timeout.error)
            throw timeout.error;
          return void 0;
        }), (0, async_iterable_js_1.transformSplitEnvelope)(opt.readMaxBytes), (0, async_iterable_js_1.transformDecompressEnvelope)(compression.request, opt.readMaxBytes), (0, async_iterable_js_1.transformParseEnvelope)(serialization.getI(type.binary), end_stream_js_1.endStreamFlag));
        const it = (0, invoke_implementation_js_1.transformInvokeImplementation)(spec, context, opt.interceptors)(inputIt)[Symbol.asyncIterator]();
        const outputIt = (0, async_iterable_js_1.pipe)(
          // We wrap the iterator in an async iterator to ensure that the
          // abort signal is aborted when the iterator is done.
          {
            [Symbol.asyncIterator]() {
              return {
                next: /* @__PURE__ */ __name(() => it.next(), "next"),
                throw: /* @__PURE__ */ __name((e) => {
                  var _a, _b;
                  context.abort(e);
                  return (_b = (_a = it.throw) === null || _a === void 0 ? void 0 : _a.call(it, e)) !== null && _b !== void 0 ? _b : Promise.reject({ done: true });
                }, "throw"),
                return: /* @__PURE__ */ __name((v) => {
                  var _a, _b;
                  context.abort();
                  return (_b = (_a = it.return) === null || _a === void 0 ? void 0 : _a.call(it, v)) !== null && _b !== void 0 ? _b : Promise.resolve({ done: true, value: v });
                }, "return")
              };
            }
          },
          (0, async_iterable_js_1.transformSerializeEnvelope)(serialization.getO(type.binary)),
          (0, async_iterable_js_1.transformCatchFinally)((e) => {
            context.abort(e);
            const end = {
              metadata: context.responseTrailer
            };
            if (e instanceof connect_error_js_1.ConnectError) {
              end.error = e;
            } else if (e !== void 0) {
              end.error = new connect_error_js_1.ConnectError("internal error", code_js_1.Code.Internal, void 0, void 0, e);
            }
            return {
              flags: end_stream_js_1.endStreamFlag,
              data: endStreamSerialization.serialize(end)
            };
          }),
          (0, async_iterable_js_1.transformCompressEnvelope)(compression.response, opt.compressMinBytes),
          (0, async_iterable_js_1.transformJoinEnvelopes)(),
          { propagateDownStreamError: true }
        );
        return Object.assign(Object.assign({}, universal_js_1.uResponseOk), {
          // We wait for the first response body bytes before resolving, so that
          // implementations have a chance to add headers before an adapter commits
          // them to the wire.
          body: await (0, async_iterable_js_1.untilFirst)(outputIt),
          header: context.responseHeader
        });
      }, "handle");
    }
    __name(createStreamHandler, "createStreamHandler");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/router.js
var require_router = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/router.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createConnectRouter = createConnectRouter;
    var connect_error_js_1 = require_connect_error();
    var code_js_1 = require_code();
    var implementation_js_1 = require_implementation();
    var handler_factory_js_1 = require_handler_factory();
    var handler_factory_js_2 = require_handler_factory2();
    var handler_factory_js_3 = require_handler_factory3();
    var universal_handler_js_1 = require_universal_handler();
    function createConnectRouter(routerOptions) {
      const base = whichProtocols(routerOptions);
      const handlers = [];
      const router = {
        handlers,
        service: /* @__PURE__ */ __name((service, implementation, options) => {
          const { protocols } = whichProtocols(options, base);
          handlers.push(...(0, universal_handler_js_1.createUniversalServiceHandlers)((0, implementation_js_1.createServiceImplSpec)(service, implementation), protocols));
          return router;
        }, "service"),
        rpc: /* @__PURE__ */ __name((method, impl, opt) => {
          const { protocols } = whichProtocols(opt, base);
          handlers.push((0, universal_handler_js_1.createUniversalMethodHandler)((0, implementation_js_1.createMethodImplSpec)(method, impl), protocols));
          return router;
        }, "rpc")
      };
      return router;
    }
    __name(createConnectRouter, "createConnectRouter");
    function whichProtocols(options, base) {
      if (base && !options) {
        return base;
      }
      const opt = base ? Object.assign(Object.assign({}, (0, universal_handler_js_1.validateUniversalHandlerOptions)(base.options)), options) : Object.assign(Object.assign({}, options), (0, universal_handler_js_1.validateUniversalHandlerOptions)(options !== null && options !== void 0 ? options : {}));
      const protocols = [];
      if ((options === null || options === void 0 ? void 0 : options.grpc) !== false) {
        protocols.push((0, handler_factory_js_2.createHandlerFactory)(opt));
      }
      if ((options === null || options === void 0 ? void 0 : options.grpcWeb) !== false) {
        protocols.push((0, handler_factory_js_1.createHandlerFactory)(opt));
      }
      if ((options === null || options === void 0 ? void 0 : options.connect) !== false) {
        protocols.push((0, handler_factory_js_3.createHandlerFactory)(opt));
      }
      if (protocols.length === 0) {
        throw new connect_error_js_1.ConnectError("cannot create handler, all protocols are disabled", code_js_1.Code.InvalidArgument);
      }
      return {
        options: opt,
        protocols
      };
    }
    __name(whichProtocols, "whichProtocols");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/cors.js
var require_cors = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/cors.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.cors = void 0;
    var connect = require_headers3();
    var grpc = require_headers();
    var grpcWeb = require_headers2();
    exports.cors = {
      /**
       * Request methods that scripts running in the browser are permitted to use.
       *
       * To support cross-domain requests with the protocols supported by Connect,
       * these headers fields must be included in the preflight response header
       * Access-Control-Allow-Methods.
       */
      allowedMethods: ["POST", "GET"],
      /**
       * Header fields that scripts running in the browser are permitted to send.
       *
       * To support cross-domain requests with the protocols supported by Connect,
       * these field names must be included in the preflight response header
       * Access-Control-Allow-Headers.
       *
       * Make sure to include any application-specific headers your browser client
       * may send.
       */
      allowedHeaders: [
        connect.headerContentType,
        connect.headerProtocolVersion,
        connect.headerTimeout,
        connect.headerStreamEncoding,
        // Unused in web browsers, but added for future-proofing
        connect.headerStreamAcceptEncoding,
        // Unused in web browsers, but added for future-proofing
        connect.headerUnaryEncoding,
        // Unused in web browsers, but added for future-proofing
        connect.headerUnaryAcceptEncoding,
        // Unused in web browsers, but added for future-proofing
        grpc.headerMessageType,
        // Unused in web browsers, but added for future-proofing
        grpcWeb.headerXGrpcWeb,
        grpcWeb.headerXUserAgent,
        grpcWeb.headerTimeout
      ],
      /**
       * Header fields that scripts running the browser are permitted to see.
       *
       * To support cross-domain requests with the protocols supported by Connect,
       * these field names must be included in header Access-Control-Expose-Headers
       * of the actual response.
       *
       * Make sure to include any application-specific headers your browser client
       * should see. If your application uses trailers, they will be sent as header
       * fields with a `Trailer-` prefix for Connect unary RPCs - make sure to
       * expose them as well if you want them to be visible in all supported
       * protocols.
       */
      exposedHeaders: [
        grpcWeb.headerGrpcStatus,
        // Crucial for gRPC-web
        grpcWeb.headerGrpcMessage,
        // Crucial for gRPC-web
        grpcWeb.headerStatusDetailsBin,
        // Error details in gRPC, gRPC-web
        connect.headerUnaryEncoding,
        // Unused in web browsers, but added for future-proofing
        connect.headerStreamEncoding
        // Unused in web browsers, but added for future-proofing
      ]
    };
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/request-header.js
var require_request_header = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/request-header.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.requestHeader = requestHeader;
    exports.requestHeaderWithCompression = requestHeaderWithCompression;
    var headers_js_1 = require_headers3();
    var version_js_1 = require_version();
    var content_type_js_1 = require_content_type3();
    function requestHeader(methodKind, useBinaryFormat, timeoutMs, userProvidedHeaders, setUserAgent) {
      const result = new Headers(userProvidedHeaders !== null && userProvidedHeaders !== void 0 ? userProvidedHeaders : {});
      if (timeoutMs !== void 0) {
        result.set(headers_js_1.headerTimeout, `${timeoutMs}`);
      }
      result.set(headers_js_1.headerContentType, methodKind == "unary" ? useBinaryFormat ? content_type_js_1.contentTypeUnaryProto : content_type_js_1.contentTypeUnaryJson : useBinaryFormat ? content_type_js_1.contentTypeStreamProto : content_type_js_1.contentTypeStreamJson);
      result.set(headers_js_1.headerProtocolVersion, version_js_1.protocolVersion);
      if (!result.has(headers_js_1.headerUserAgent) && setUserAgent) {
        result.set(headers_js_1.headerUserAgent, "connect-es/2.0.0-rc.3");
      }
      return result;
    }
    __name(requestHeader, "requestHeader");
    function requestHeaderWithCompression(methodKind, useBinaryFormat, timeoutMs, userProvidedHeaders, acceptCompression, sendCompression, setUserAgent) {
      const result = requestHeader(methodKind, useBinaryFormat, timeoutMs, userProvidedHeaders, setUserAgent);
      if (sendCompression != null) {
        const name = methodKind == "unary" ? headers_js_1.headerUnaryEncoding : headers_js_1.headerStreamEncoding;
        result.set(name, sendCompression.name);
      }
      if (acceptCompression.length > 0) {
        const name = methodKind == "unary" ? headers_js_1.headerUnaryAcceptEncoding : headers_js_1.headerStreamAcceptEncoding;
        result.set(name, acceptCompression.map((c) => c.name).join(","));
      }
      return result;
    }
    __name(requestHeaderWithCompression, "requestHeaderWithCompression");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/validate-response.js
var require_validate_response = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/validate-response.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.validateResponse = validateResponse;
    exports.validateResponseWithCompression = validateResponseWithCompression;
    var code_js_1 = require_code();
    var http_status_js_1 = require_http_status();
    var connect_error_js_1 = require_connect_error();
    var content_type_js_1 = require_content_type3();
    var headers_js_1 = require_headers3();
    function validateResponse(methodKind, useBinaryFormat, status, headers) {
      const mimeType = headers.get(headers_js_1.headerContentType);
      const parsedType = (0, content_type_js_1.parseContentType)(mimeType);
      if (status !== 200) {
        const errorFromStatus = new connect_error_js_1.ConnectError(`HTTP ${status}`, (0, http_status_js_1.codeFromHttpStatus)(status), headers);
        if (methodKind == "unary" && parsedType && !parsedType.binary) {
          return { isUnaryError: true, unaryError: errorFromStatus };
        }
        throw errorFromStatus;
      }
      const allowedContentType = {
        binary: useBinaryFormat,
        stream: methodKind !== "unary"
      };
      if ((parsedType === null || parsedType === void 0 ? void 0 : parsedType.binary) !== allowedContentType.binary || parsedType.stream !== allowedContentType.stream) {
        throw new connect_error_js_1.ConnectError(`unsupported content type ${mimeType}`, parsedType === void 0 ? code_js_1.Code.Unknown : code_js_1.Code.Internal, headers);
      }
      return { isUnaryError: false };
    }
    __name(validateResponse, "validateResponse");
    function validateResponseWithCompression(methodKind, acceptCompression, useBinaryFormat, status, headers) {
      let compression;
      const encoding = headers.get(methodKind == "unary" ? headers_js_1.headerUnaryEncoding : headers_js_1.headerStreamEncoding);
      if (encoding != null && encoding.toLowerCase() !== "identity") {
        compression = acceptCompression.find((c) => c.name === encoding);
        if (!compression) {
          throw new connect_error_js_1.ConnectError(`unsupported response encoding "${encoding}"`, code_js_1.Code.Internal, headers);
        }
      }
      return Object.assign({ compression }, validateResponse(methodKind, useBinaryFormat, status, headers));
    }
    __name(validateResponseWithCompression, "validateResponseWithCompression");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/get-request.js
var require_get_request = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/get-request.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.transformConnectPostToGetRequest = transformConnectPostToGetRequest;
    var wire_1 = require_wire();
    var headers_js_1 = require_headers3();
    var version_js_1 = require_version();
    var contentTypePrefix = "application/";
    function encodeMessageForUrl(message, useBase64) {
      if (useBase64) {
        return (0, wire_1.base64Encode)(message, "url");
      } else {
        return encodeURIComponent(new TextDecoder().decode(message));
      }
    }
    __name(encodeMessageForUrl, "encodeMessageForUrl");
    function transformConnectPostToGetRequest(request, message, useBase64) {
      let query = `?connect=v${version_js_1.protocolVersion}`;
      const contentType = request.header.get(headers_js_1.headerContentType);
      if ((contentType === null || contentType === void 0 ? void 0 : contentType.indexOf(contentTypePrefix)) === 0) {
        query += "&encoding=" + encodeURIComponent(contentType.slice(contentTypePrefix.length));
      }
      const compression = request.header.get(headers_js_1.headerUnaryEncoding);
      if (compression !== null && compression !== "identity") {
        query += "&compression=" + encodeURIComponent(compression);
        useBase64 = true;
      }
      if (useBase64) {
        query += "&base64=1";
      }
      query += "&message=" + encodeMessageForUrl(message, useBase64);
      const url = request.url + query;
      const header = new Headers(request.header);
      [
        headers_js_1.headerProtocolVersion,
        headers_js_1.headerContentType,
        headers_js_1.headerUnaryContentLength,
        headers_js_1.headerUnaryEncoding,
        headers_js_1.headerUnaryAcceptEncoding
      ].forEach((h) => header.delete(h));
      return Object.assign(Object.assign({}, request), {
        requestMethod: "GET",
        url,
        header
      });
    }
    __name(transformConnectPostToGetRequest, "transformConnectPostToGetRequest");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/run-call.js
var require_run_call = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/run-call.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.runUnaryCall = runUnaryCall;
    exports.runStreamingCall = runStreamingCall;
    var interceptor_js_1 = require_interceptor();
    var connect_error_js_1 = require_connect_error();
    var signals_js_1 = require_signals();
    var normalize_js_1 = require_normalize();
    function runUnaryCall(opt) {
      const next = (0, interceptor_js_1.applyInterceptors)(opt.next, opt.interceptors);
      const [signal, abort, done] = setupSignal(opt);
      const req = Object.assign(Object.assign({}, opt.req), { message: (0, normalize_js_1.normalize)(opt.req.method.input, opt.req.message), signal });
      return next(req).then((res) => {
        done();
        return res;
      }, abort);
    }
    __name(runUnaryCall, "runUnaryCall");
    function runStreamingCall(opt) {
      const next = (0, interceptor_js_1.applyInterceptors)(opt.next, opt.interceptors);
      const [signal, abort, done] = setupSignal(opt);
      const req = Object.assign(Object.assign({}, opt.req), { message: (0, normalize_js_1.normalizeIterable)(opt.req.method.input, opt.req.message), signal });
      let doneCalled = false;
      signal.addEventListener("abort", function() {
        var _a, _b;
        const it = opt.req.message[Symbol.asyncIterator]();
        if (!doneCalled) {
          (_a = it.throw) === null || _a === void 0 ? void 0 : _a.call(it, this.reason).catch(() => {
          });
        }
        (_b = it.return) === null || _b === void 0 ? void 0 : _b.call(it).catch(() => {
        });
      });
      return next(req).then((res) => {
        return Object.assign(Object.assign({}, res), { message: {
          [Symbol.asyncIterator]() {
            const it = res.message[Symbol.asyncIterator]();
            return {
              next() {
                return it.next().then((r) => {
                  if (r.done == true) {
                    doneCalled = true;
                    done();
                  }
                  return r;
                }, abort);
              }
              // We deliberately omit throw/return.
            };
          }
        } });
      }, abort);
    }
    __name(runStreamingCall, "runStreamingCall");
    function setupSignal(opt) {
      const { signal, cleanup } = (0, signals_js_1.createDeadlineSignal)(opt.timeoutMs);
      const controller = (0, signals_js_1.createLinkedAbortController)(opt.signal, signal);
      return [
        controller.signal,
        /* @__PURE__ */ __name(function abort(reason) {
          const e = connect_error_js_1.ConnectError.from(signal.aborted ? (0, signals_js_1.getAbortSignalReason)(signal) : reason);
          controller.abort(e);
          cleanup();
          return Promise.reject(e);
        }, "abort"),
        /* @__PURE__ */ __name(function done() {
          cleanup();
          controller.abort();
        }, "done")
      ];
    }
    __name(setupSignal, "setupSignal");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/transport.js
var require_transport = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/transport.js"(exports) {
    "use strict";
    init_esm();
    var __asyncValues = exports && exports.__asyncValues || function(o) {
      if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
      var m = o[Symbol.asyncIterator], i;
      return m ? m.call(o) : (o = typeof __values === "function" ? __values(o) : o[Symbol.iterator](), i = {}, verb("next"), verb("throw"), verb("return"), i[Symbol.asyncIterator] = function() {
        return this;
      }, i);
      function verb(n) {
        i[n] = o[n] && function(v) {
          return new Promise(function(resolve, reject) {
            v = o[n](v), settle(resolve, reject, v.done, v.value);
          });
        };
      }
      __name(verb, "verb");
      function settle(resolve, reject, d, v) {
        Promise.resolve(v).then(function(v2) {
          resolve({ value: v2, done: d });
        }, reject);
      }
      __name(settle, "settle");
    };
    var __await = exports && exports.__await || function(v) {
      return this instanceof __await ? (this.v = v, this) : new __await(v);
    };
    var __asyncGenerator = exports && exports.__asyncGenerator || function(thisArg, _arguments, generator) {
      if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
      var g = generator.apply(thisArg, _arguments || []), i, q = [];
      return i = Object.create((typeof AsyncIterator === "function" ? AsyncIterator : Object).prototype), verb("next"), verb("throw"), verb("return", awaitReturn), i[Symbol.asyncIterator] = function() {
        return this;
      }, i;
      function awaitReturn(f) {
        return function(v) {
          return Promise.resolve(v).then(f, reject);
        };
      }
      __name(awaitReturn, "awaitReturn");
      function verb(n, f) {
        if (g[n]) {
          i[n] = function(v) {
            return new Promise(function(a, b) {
              q.push([n, v, a, b]) > 1 || resume(n, v);
            });
          };
          if (f) i[n] = f(i[n]);
        }
      }
      __name(verb, "verb");
      function resume(n, v) {
        try {
          step(g[n](v));
        } catch (e) {
          settle(q[0][3], e);
        }
      }
      __name(resume, "resume");
      function step(r) {
        r.value instanceof __await ? Promise.resolve(r.value.v).then(fulfill, reject) : settle(q[0][2], r);
      }
      __name(step, "step");
      function fulfill(value) {
        resume("next", value);
      }
      __name(fulfill, "fulfill");
      function reject(value) {
        resume("throw", value);
      }
      __name(reject, "reject");
      function settle(f, v) {
        if (f(v), q.shift(), q.length) resume(q[0][0], q[0][1]);
      }
      __name(settle, "settle");
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createTransport = createTransport;
    var request_header_js_1 = require_request_header();
    var headers_js_1 = require_headers3();
    var validate_response_js_1 = require_validate_response();
    var trailer_mux_js_1 = require_trailer_mux();
    var error_json_js_1 = require_error_json();
    var end_stream_js_1 = require_end_stream();
    var get_request_js_1 = require_get_request();
    var code_js_1 = require_code();
    var connect_error_js_1 = require_connect_error();
    var http_headers_js_1 = require_http_headers();
    var async_iterable_js_1 = require_async_iterable();
    var create_method_url_js_1 = require_create_method_url();
    var run_call_js_1 = require_run_call();
    var serialization_js_1 = require_serialization();
    var context_values_js_1 = require_context_values();
    var wkt_1 = require_wkt();
    function createTransport(opt) {
      return {
        async unary(method, signal, timeoutMs, header, message, contextValues) {
          const serialization = (0, serialization_js_1.createMethodSerializationLookup)(method, opt.binaryOptions, opt.jsonOptions, opt);
          timeoutMs = timeoutMs === void 0 ? opt.defaultTimeoutMs : timeoutMs <= 0 ? void 0 : timeoutMs;
          return await (0, run_call_js_1.runUnaryCall)({
            interceptors: opt.interceptors,
            signal,
            timeoutMs,
            req: {
              stream: false,
              service: method.parent,
              method,
              requestMethod: "POST",
              url: (0, create_method_url_js_1.createMethodUrl)(opt.baseUrl, method),
              header: (0, request_header_js_1.requestHeaderWithCompression)(method.methodKind, opt.useBinaryFormat, timeoutMs, header, opt.acceptCompression, opt.sendCompression, true),
              contextValues: contextValues !== null && contextValues !== void 0 ? contextValues : (0, context_values_js_1.createContextValues)(),
              message
            },
            next: /* @__PURE__ */ __name(async (req) => {
              let requestBody = serialization.getI(opt.useBinaryFormat).serialize(req.message);
              if (opt.sendCompression && requestBody.byteLength > opt.compressMinBytes) {
                requestBody = await opt.sendCompression.compress(requestBody);
                req.header.set(headers_js_1.headerUnaryEncoding, opt.sendCompression.name);
              } else {
                req.header.delete(headers_js_1.headerUnaryEncoding);
              }
              const useGet = opt.useHttpGet === true && method.idempotency === wkt_1.MethodOptions_IdempotencyLevel.NO_SIDE_EFFECTS;
              let body;
              if (useGet) {
                req = (0, get_request_js_1.transformConnectPostToGetRequest)(req, requestBody, opt.useBinaryFormat);
              } else {
                body = (0, async_iterable_js_1.createAsyncIterable)([requestBody]);
              }
              const universalResponse = await opt.httpClient({
                url: req.url,
                method: req.requestMethod,
                header: req.header,
                signal: req.signal,
                body
              });
              const { compression, isUnaryError, unaryError } = (0, validate_response_js_1.validateResponseWithCompression)(method.methodKind, opt.acceptCompression, opt.useBinaryFormat, universalResponse.status, universalResponse.header);
              const [header2, trailer] = (0, trailer_mux_js_1.trailerDemux)(universalResponse.header);
              let responseBody = await (0, async_iterable_js_1.pipeTo)(universalResponse.body, (0, async_iterable_js_1.sinkAllBytes)(opt.readMaxBytes, universalResponse.header.get(headers_js_1.headerUnaryContentLength)), { propagateDownStreamError: false });
              if (compression) {
                responseBody = await compression.decompress(responseBody, opt.readMaxBytes);
              }
              if (isUnaryError) {
                throw (0, error_json_js_1.errorFromJsonBytes)(responseBody, (0, http_headers_js_1.appendHeaders)(header2, trailer), unaryError);
              }
              return {
                stream: false,
                service: method.parent,
                method,
                header: header2,
                message: serialization.getO(opt.useBinaryFormat).parse(responseBody),
                trailer
              };
            }, "next")
          });
        },
        async stream(method, signal, timeoutMs, header, input, contextValues) {
          const serialization = (0, serialization_js_1.createMethodSerializationLookup)(method, opt.binaryOptions, opt.jsonOptions, opt);
          const endStreamSerialization = (0, end_stream_js_1.createEndStreamSerialization)(opt.jsonOptions);
          timeoutMs = timeoutMs === void 0 ? opt.defaultTimeoutMs : timeoutMs <= 0 ? void 0 : timeoutMs;
          return (0, run_call_js_1.runStreamingCall)({
            interceptors: opt.interceptors,
            signal,
            timeoutMs,
            req: {
              stream: true,
              service: method.parent,
              method,
              requestMethod: "POST",
              url: (0, create_method_url_js_1.createMethodUrl)(opt.baseUrl, method),
              header: (0, request_header_js_1.requestHeaderWithCompression)(method.methodKind, opt.useBinaryFormat, timeoutMs, header, opt.acceptCompression, opt.sendCompression, true),
              contextValues: contextValues !== null && contextValues !== void 0 ? contextValues : (0, context_values_js_1.createContextValues)(),
              message: input
            },
            next: /* @__PURE__ */ __name(async (req) => {
              const uRes = await opt.httpClient({
                url: req.url,
                method: "POST",
                header: req.header,
                signal: req.signal,
                body: (0, async_iterable_js_1.pipe)(req.message, (0, async_iterable_js_1.transformSerializeEnvelope)(serialization.getI(opt.useBinaryFormat)), (0, async_iterable_js_1.transformCompressEnvelope)(opt.sendCompression, opt.compressMinBytes), (0, async_iterable_js_1.transformJoinEnvelopes)(), { propagateDownStreamError: true })
              });
              const { compression } = (0, validate_response_js_1.validateResponseWithCompression)(method.methodKind, opt.acceptCompression, opt.useBinaryFormat, uRes.status, uRes.header);
              const res = Object.assign(Object.assign({}, req), { header: uRes.header, trailer: new Headers(), message: (0, async_iterable_js_1.pipe)(uRes.body, (0, async_iterable_js_1.transformSplitEnvelope)(opt.readMaxBytes), (0, async_iterable_js_1.transformDecompressEnvelope)(compression !== null && compression !== void 0 ? compression : null, opt.readMaxBytes), (0, async_iterable_js_1.transformParseEnvelope)(serialization.getO(opt.useBinaryFormat), end_stream_js_1.endStreamFlag, endStreamSerialization), function(iterable) {
                return __asyncGenerator(this, arguments, function* () {
                  var _a, e_1, _b, _c;
                  let endStreamReceived = false;
                  try {
                    for (var _d = true, iterable_1 = __asyncValues(iterable), iterable_1_1; iterable_1_1 = yield __await(iterable_1.next()), _a = iterable_1_1.done, !_a; _d = true) {
                      _c = iterable_1_1.value;
                      _d = false;
                      const chunk = _c;
                      if (chunk.end) {
                        if (endStreamReceived) {
                          throw new connect_error_js_1.ConnectError("protocol error: received extra EndStreamResponse", code_js_1.Code.InvalidArgument);
                        }
                        endStreamReceived = true;
                        if (chunk.value.error) {
                          const error = chunk.value.error;
                          uRes.header.forEach((value, key) => {
                            error.metadata.append(key, value);
                          });
                          throw error;
                        }
                        chunk.value.metadata.forEach((value, key) => res.trailer.set(key, value));
                        continue;
                      }
                      if (endStreamReceived) {
                        throw new connect_error_js_1.ConnectError("protocol error: received extra message after EndStreamResponse", code_js_1.Code.InvalidArgument);
                      }
                      yield yield __await(chunk.value);
                    }
                  } catch (e_1_1) {
                    e_1 = { error: e_1_1 };
                  } finally {
                    try {
                      if (!_d && !_a && (_b = iterable_1.return)) yield __await(_b.call(iterable_1));
                    } finally {
                      if (e_1) throw e_1.error;
                    }
                  }
                  if (!endStreamReceived) {
                    throw new connect_error_js_1.ConnectError("protocol error: missing EndStreamResponse", code_js_1.Code.InvalidArgument);
                  }
                });
              }, { propagateDownStreamError: true }) });
              return res;
            }, "next")
          });
        }
      };
    }
    __name(createTransport, "createTransport");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/universal-handler-client.js
var require_universal_handler_client = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/universal-handler-client.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createUniversalHandlerClient = createUniversalHandlerClient;
    var code_js_1 = require_code();
    var connect_error_js_1 = require_connect_error();
    var async_iterable_js_1 = require_async_iterable();
    var signals_js_1 = require_signals();
    function createUniversalHandlerClient(uHandlers) {
      const handlerMap = /* @__PURE__ */ new Map();
      for (const handler of uHandlers) {
        handlerMap.set(handler.requestPath, handler);
      }
      return async (uClientReq) => {
        var _a, _b, _c;
        const pathname = new URL(uClientReq.url).pathname;
        const handler = handlerMap.get(pathname);
        if (!handler) {
          throw new connect_error_js_1.ConnectError(`RouterHttpClient: no handler registered for ${pathname}`, code_js_1.Code.Unimplemented);
        }
        const reqSignal = (_a = uClientReq.signal) !== null && _a !== void 0 ? _a : new AbortController().signal;
        const uServerRes = await raceSignal(reqSignal, handler({
          body: (_b = uClientReq.body) !== null && _b !== void 0 ? _b : (0, async_iterable_js_1.createAsyncIterable)([]),
          httpVersion: "2.0",
          method: uClientReq.method,
          url: uClientReq.url,
          header: uClientReq.header,
          signal: reqSignal
        }));
        const body = (_c = uServerRes.body) !== null && _c !== void 0 ? _c : (0, async_iterable_js_1.createAsyncIterable)([]);
        return {
          body: (0, async_iterable_js_1.pipe)(body, (iterable) => {
            return {
              [Symbol.asyncIterator]() {
                const it = iterable[Symbol.asyncIterator]();
                const w = {
                  next() {
                    return raceSignal(reqSignal, it.next());
                  }
                };
                if (it.throw !== void 0) {
                  w.throw = (e) => it.throw(e);
                }
                if (it.return !== void 0) {
                  w.return = (value) => it.return(value);
                }
                return w;
              }
            };
          }),
          header: new Headers(uServerRes.header),
          status: uServerRes.status,
          trailer: new Headers(uServerRes.trailer)
        };
      };
    }
    __name(createUniversalHandlerClient, "createUniversalHandlerClient");
    function raceSignal(signal, promise) {
      let cleanup;
      const signalPromise = new Promise((_, reject) => {
        const onAbort = /* @__PURE__ */ __name(() => reject((0, signals_js_1.getAbortSignalReason)(signal)), "onAbort");
        if (signal.aborted) {
          return onAbort();
        }
        signal.addEventListener("abort", onAbort);
        cleanup = /* @__PURE__ */ __name(() => signal.removeEventListener("abort", onAbort), "cleanup");
      });
      return Promise.race([signalPromise, promise]).finally(cleanup);
    }
    __name(raceSignal, "raceSignal");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/router-transport.js
var require_router_transport = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/router-transport.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createRouterTransport = createRouterTransport;
    var transport_js_1 = require_transport();
    var universal_handler_client_js_1 = require_universal_handler_client();
    var router_js_1 = require_router();
    function createRouterTransport(routes, options) {
      var _a, _b;
      const router = (0, router_js_1.createConnectRouter)(Object.assign(Object.assign({}, (_a = options === null || options === void 0 ? void 0 : options.router) !== null && _a !== void 0 ? _a : {}), { connect: true }));
      routes(router);
      return (0, transport_js_1.createTransport)(Object.assign({ httpClient: (0, universal_handler_client_js_1.createUniversalHandlerClient)(router.handlers), baseUrl: "https://in-memory", useBinaryFormat: true, interceptors: [], acceptCompression: [], sendCompression: null, compressMinBytes: Number.MAX_SAFE_INTEGER, readMaxBytes: Number.MAX_SAFE_INTEGER, writeMaxBytes: Number.MAX_SAFE_INTEGER }, (_b = options === null || options === void 0 ? void 0 : options.transport) !== null && _b !== void 0 ? _b : {}));
    }
    __name(createRouterTransport, "createRouterTransport");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/index.js
var require_cjs2 = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createRouterTransport = exports.createMethodImplSpec = exports.createServiceImplSpec = exports.makeAnyClient = exports.createContextValues = exports.createContextKey = exports.cors = exports.createHandlerContext = exports.createConnectRouter = exports.createClient = exports.createCallbackClient = exports.appendHeaders = exports.decodeBinaryHeader = exports.encodeBinaryHeader = exports.Code = exports.ConnectError = void 0;
    var connect_error_js_1 = require_connect_error();
    Object.defineProperty(exports, "ConnectError", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return connect_error_js_1.ConnectError;
    }, "get") });
    var code_js_1 = require_code();
    Object.defineProperty(exports, "Code", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return code_js_1.Code;
    }, "get") });
    var http_headers_js_1 = require_http_headers();
    Object.defineProperty(exports, "encodeBinaryHeader", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return http_headers_js_1.encodeBinaryHeader;
    }, "get") });
    Object.defineProperty(exports, "decodeBinaryHeader", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return http_headers_js_1.decodeBinaryHeader;
    }, "get") });
    Object.defineProperty(exports, "appendHeaders", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return http_headers_js_1.appendHeaders;
    }, "get") });
    var callback_client_js_1 = require_callback_client();
    Object.defineProperty(exports, "createCallbackClient", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return callback_client_js_1.createCallbackClient;
    }, "get") });
    var promise_client_js_1 = require_promise_client();
    Object.defineProperty(exports, "createClient", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return promise_client_js_1.createClient;
    }, "get") });
    var router_js_1 = require_router();
    Object.defineProperty(exports, "createConnectRouter", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return router_js_1.createConnectRouter;
    }, "get") });
    var implementation_js_1 = require_implementation();
    Object.defineProperty(exports, "createHandlerContext", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return implementation_js_1.createHandlerContext;
    }, "get") });
    var cors_js_1 = require_cors();
    Object.defineProperty(exports, "cors", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return cors_js_1.cors;
    }, "get") });
    var context_values_js_1 = require_context_values();
    Object.defineProperty(exports, "createContextKey", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return context_values_js_1.createContextKey;
    }, "get") });
    Object.defineProperty(exports, "createContextValues", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return context_values_js_1.createContextValues;
    }, "get") });
    var any_client_js_1 = require_any_client();
    Object.defineProperty(exports, "makeAnyClient", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return any_client_js_1.makeAnyClient;
    }, "get") });
    var implementation_js_2 = require_implementation();
    Object.defineProperty(exports, "createServiceImplSpec", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return implementation_js_2.createServiceImplSpec;
    }, "get") });
    Object.defineProperty(exports, "createMethodImplSpec", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return implementation_js_2.createMethodImplSpec;
    }, "get") });
    var router_transport_js_1 = require_router_transport();
    Object.defineProperty(exports, "createRouterTransport", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return router_transport_js_1.createRouterTransport;
    }, "get") });
  }
});

// ../../node_modules/.pnpm/compare-versions@6.1.1/node_modules/compare-versions/lib/umd/index.js
var require_umd = __commonJS({
  "../../node_modules/.pnpm/compare-versions@6.1.1/node_modules/compare-versions/lib/umd/index.js"(exports, module) {
    init_esm();
    (function(global2, factory) {
      typeof exports === "object" && typeof module !== "undefined" ? factory(exports) : typeof define === "function" && define.amd ? define(["exports"], factory) : (global2 = typeof globalThis !== "undefined" ? globalThis : global2 || self, factory(global2.compareVersions = {}));
    })(exports, function(exports2) {
      "use strict";
      const semver = /^[v^~<>=]*?(\d+)(?:\.([x*]|\d+)(?:\.([x*]|\d+)(?:\.([x*]|\d+))?(?:-([\da-z\-]+(?:\.[\da-z\-]+)*))?(?:\+[\da-z\-]+(?:\.[\da-z\-]+)*)?)?)?$/i;
      const validateAndParse = /* @__PURE__ */ __name((version) => {
        if (typeof version !== "string") {
          throw new TypeError("Invalid argument expected string");
        }
        const match = version.match(semver);
        if (!match) {
          throw new Error(`Invalid argument not valid semver ('${version}' received)`);
        }
        match.shift();
        return match;
      }, "validateAndParse");
      const isWildcard = /* @__PURE__ */ __name((s) => s === "*" || s === "x" || s === "X", "isWildcard");
      const tryParse = /* @__PURE__ */ __name((v) => {
        const n = parseInt(v, 10);
        return isNaN(n) ? v : n;
      }, "tryParse");
      const forceType = /* @__PURE__ */ __name((a, b) => typeof a !== typeof b ? [String(a), String(b)] : [a, b], "forceType");
      const compareStrings = /* @__PURE__ */ __name((a, b) => {
        if (isWildcard(a) || isWildcard(b))
          return 0;
        const [ap, bp] = forceType(tryParse(a), tryParse(b));
        if (ap > bp)
          return 1;
        if (ap < bp)
          return -1;
        return 0;
      }, "compareStrings");
      const compareSegments = /* @__PURE__ */ __name((a, b) => {
        for (let i = 0; i < Math.max(a.length, b.length); i++) {
          const r = compareStrings(a[i] || "0", b[i] || "0");
          if (r !== 0)
            return r;
        }
        return 0;
      }, "compareSegments");
      const compareVersions = /* @__PURE__ */ __name((v1, v2) => {
        const n1 = validateAndParse(v1);
        const n2 = validateAndParse(v2);
        const p1 = n1.pop();
        const p2 = n2.pop();
        const r = compareSegments(n1, n2);
        if (r !== 0)
          return r;
        if (p1 && p2) {
          return compareSegments(p1.split("."), p2.split("."));
        } else if (p1 || p2) {
          return p1 ? -1 : 1;
        }
        return 0;
      }, "compareVersions");
      const compare = /* @__PURE__ */ __name((v1, v2, operator) => {
        assertValidOperator(operator);
        const res = compareVersions(v1, v2);
        return operatorResMap[operator].includes(res);
      }, "compare");
      const operatorResMap = {
        ">": [1],
        ">=": [0, 1],
        "=": [0],
        "<=": [-1, 0],
        "<": [-1],
        "!=": [-1, 1]
      };
      const allowedOperators = Object.keys(operatorResMap);
      const assertValidOperator = /* @__PURE__ */ __name((op) => {
        if (typeof op !== "string") {
          throw new TypeError(`Invalid operator type, expected string but got ${typeof op}`);
        }
        if (allowedOperators.indexOf(op) === -1) {
          throw new Error(`Invalid operator, expected one of ${allowedOperators.join("|")}`);
        }
      }, "assertValidOperator");
      const satisfies = /* @__PURE__ */ __name((version, range) => {
        range = range.replace(/([><=]+)\s+/g, "$1");
        if (range.includes("||")) {
          return range.split("||").some((r4) => satisfies(version, r4));
        } else if (range.includes(" - ")) {
          const [a, b] = range.split(" - ", 2);
          return satisfies(version, `>=${a} <=${b}`);
        } else if (range.includes(" ")) {
          return range.trim().replace(/\s{2,}/g, " ").split(" ").every((r4) => satisfies(version, r4));
        }
        const m = range.match(/^([<>=~^]+)/);
        const op = m ? m[1] : "=";
        if (op !== "^" && op !== "~")
          return compare(version, range, op);
        const [v1, v2, v3, , vp] = validateAndParse(version);
        const [r1, r2, r3, , rp] = validateAndParse(range);
        const v = [v1, v2, v3];
        const r = [r1, r2 !== null && r2 !== void 0 ? r2 : "x", r3 !== null && r3 !== void 0 ? r3 : "x"];
        if (rp) {
          if (!vp)
            return false;
          if (compareSegments(v, r) !== 0)
            return false;
          if (compareSegments(vp.split("."), rp.split(".")) === -1)
            return false;
        }
        const nonZero = r.findIndex((v4) => v4 !== "0") + 1;
        const i = op === "~" ? 2 : nonZero > 1 ? nonZero : 1;
        if (compareSegments(v.slice(0, i), r.slice(0, i)) !== 0)
          return false;
        if (compareSegments(v.slice(i), r.slice(i)) === -1)
          return false;
        return true;
      }, "satisfies");
      const validate = /* @__PURE__ */ __name((version) => typeof version === "string" && /^[v\d]/.test(version) && semver.test(version), "validate");
      const validateStrict = /* @__PURE__ */ __name((version) => typeof version === "string" && /^(0|[1-9]\d*)\.(0|[1-9]\d*)\.(0|[1-9]\d*)(?:-((?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\.(?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\+([0-9a-zA-Z-]+(?:\.[0-9a-zA-Z-]+)*))?$/.test(version), "validateStrict");
      exports2.compare = compare;
      exports2.compareVersions = compareVersions;
      exports2.satisfies = satisfies;
      exports2.validate = validate;
      exports2.validateStrict = validateStrict;
    });
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/service.js
var require_service2 = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/service.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.serviceDesc = serviceDesc;
    function serviceDesc(file, path, ...paths) {
      if (paths.length > 0) {
        throw new Error();
      }
      return file.services[path];
    }
    __name(serviceDesc, "serviceDesc");
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/types.js
var require_types3 = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/types.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
  }
});

// ../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/index.js
var require_codegenv2 = __commonJS({
  "../../node_modules/.pnpm/@bufbuild+protobuf@2.12.0/node_modules/@bufbuild/protobuf/dist/cjs/codegenv2/index.js"(exports) {
    "use strict";
    init_esm();
    var __createBinding = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: /* @__PURE__ */ __name(function() {
          return m[k];
        }, "get") };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    });
    var __exportStar = exports && exports.__exportStar || function(m, exports2) {
      for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p)) __createBinding(exports2, m, p);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    __exportStar(require_boot(), exports);
    __exportStar(require_embed(), exports);
    __exportStar(require_enum(), exports);
    __exportStar(require_extension(), exports);
    __exportStar(require_file(), exports);
    __exportStar(require_message(), exports);
    __exportStar(require_service2(), exports);
    __exportStar(require_symbols(), exports);
    __exportStar(require_scalar2(), exports);
    __exportStar(require_types3(), exports);
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/universal-fetch.js
var require_universal_fetch = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/universal-fetch.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createFetchClient = createFetchClient;
    exports.createFetchHandler = createFetchHandler;
    exports.universalClientRequestToFetch = universalClientRequestToFetch;
    exports.universalClientResponseFromFetch = universalClientResponseFromFetch;
    exports.universalServerRequestFromFetch = universalServerRequestFromFetch;
    exports.universalServerResponseToFetch = universalServerResponseToFetch;
    function createFetchClient(fetchFn) {
      return /* @__PURE__ */ __name(async function fetchClient(request) {
        const res = await fetchFn(universalClientRequestToFetch(request));
        return universalClientResponseFromFetch(res);
      }, "fetchClient");
    }
    __name(createFetchClient, "createFetchClient");
    function createFetchHandler(uHandler, options) {
      async function handleFetch(req) {
        const uReq = universalServerRequestFromFetch(req, options !== null && options !== void 0 ? options : {});
        const uRes = await uHandler(uReq);
        return universalServerResponseToFetch(uRes);
      }
      __name(handleFetch, "handleFetch");
      return Object.assign(handleFetch, uHandler);
    }
    __name(createFetchHandler, "createFetchHandler");
    function universalClientRequestToFetch(req) {
      const body = req.body === void 0 ? null : iterableToReadableStream(req.body);
      return new Request(req.url, {
        method: req.method,
        headers: req.header,
        signal: req.signal,
        body
      });
    }
    __name(universalClientRequestToFetch, "universalClientRequestToFetch");
    function universalClientResponseFromFetch(res) {
      return {
        status: res.status,
        header: res.headers,
        body: iterableFromReadableStream(res.body),
        trailer: new Headers()
      };
    }
    __name(universalClientResponseFromFetch, "universalClientResponseFromFetch");
    function universalServerRequestFromFetch(req, options) {
      var _a;
      return {
        httpVersion: (_a = options.httpVersion) !== null && _a !== void 0 ? _a : "",
        method: req.method,
        url: req.url,
        header: req.headers,
        body: iterableFromReadableStream(req.body),
        signal: req.signal
      };
    }
    __name(universalServerRequestFromFetch, "universalServerRequestFromFetch");
    function universalServerResponseToFetch(res) {
      let body = null;
      if (res.body !== void 0) {
        body = iterableToReadableStream(res.body);
      }
      return new Response(body, {
        status: res.status,
        headers: res.header
      });
    }
    __name(universalServerResponseToFetch, "universalServerResponseToFetch");
    function iterableToReadableStream(iterable) {
      const it = iterable[Symbol.asyncIterator]();
      return new ReadableStream({
        async pull(controller) {
          const r = await it.next();
          if (r.done === true) {
            controller.close();
            return;
          }
          controller.enqueue(r.value);
        },
        async cancel(reason) {
          if (it.throw) {
            try {
              await it.throw(reason);
            } catch (_a) {
            }
          }
        }
      });
    }
    __name(iterableToReadableStream, "iterableToReadableStream");
    function iterableFromReadableStream(body) {
      const reader = body === null || body === void 0 ? void 0 : body.getReader();
      return {
        [Symbol.asyncIterator]() {
          return {
            async next() {
              if (reader !== void 0) {
                const r = await reader.read();
                if (r.done) {
                  return {
                    done: true,
                    value: void 0
                  };
                }
                return r;
              }
              return {
                done: true,
                value: void 0
              };
            },
            async throw(e) {
              if (reader !== void 0) {
                await reader.cancel(e);
              }
              return {
                done: true,
                value: void 0
              };
            }
          };
        }
      };
    }
    __name(iterableFromReadableStream, "iterableFromReadableStream");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/index.js
var require_protocol = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.uResponseOk = exports.assertByteStreamRequest = exports.createDeadlineSignal = exports.getAbortSignalReason = exports.createLinkedAbortController = exports.transformInvokeImplementation = exports.invokeUnaryImplementation = exports.contentTypeMatcher = exports.untilFirst = exports.readAllBytes = exports.createAsyncIterable = exports.createWritableIterable = exports.makeIterableAbortable = exports.sinkAllBytes = exports.sinkAll = exports.pipeTo = exports.transformParseEnvelope = exports.transformSerializeEnvelope = exports.transformSplitEnvelope = exports.transformJoinEnvelopes = exports.transformDecompressEnvelope = exports.transformCompressEnvelope = exports.transformReadAllBytes = exports.transformPrepend = exports.transformAppend = exports.transformCatchFinally = exports.transformCatch = exports.pipe = exports.compressionNegotiate = exports.compressedFlag = exports.createEnvelopeReadableStream = exports.envelopeCompress = exports.envelopeDecompress = exports.encodeEnvelopes = exports.encodeEnvelope = exports.validateReadWriteMaxBytes = exports.limitSerialization = exports.getJsonOptions = exports.createClientMethodSerializers = exports.createMethodSerializationLookup = exports.runStreamingCall = exports.runUnaryCall = exports.universalServerResponseToFetch = exports.universalServerRequestFromFetch = exports.universalClientResponseFromFetch = exports.universalClientRequestToFetch = exports.createFetchHandler = exports.createFetchClient = exports.createUniversalHandlerClient = exports.createMethodUrl = void 0;
    exports.createUniversalMethodHandler = exports.createUniversalServiceHandlers = exports.validateUniversalHandlerOptions = exports.uResponseVersionNotSupported = exports.uResponseMethodNotAllowed = exports.uResponseUnsupportedMediaType = exports.uResponseNotFound = void 0;
    var create_method_url_js_1 = require_create_method_url();
    Object.defineProperty(exports, "createMethodUrl", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return create_method_url_js_1.createMethodUrl;
    }, "get") });
    var universal_handler_client_js_1 = require_universal_handler_client();
    Object.defineProperty(exports, "createUniversalHandlerClient", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return universal_handler_client_js_1.createUniversalHandlerClient;
    }, "get") });
    var universal_fetch_js_1 = require_universal_fetch();
    Object.defineProperty(exports, "createFetchClient", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return universal_fetch_js_1.createFetchClient;
    }, "get") });
    Object.defineProperty(exports, "createFetchHandler", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return universal_fetch_js_1.createFetchHandler;
    }, "get") });
    Object.defineProperty(exports, "universalClientRequestToFetch", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return universal_fetch_js_1.universalClientRequestToFetch;
    }, "get") });
    Object.defineProperty(exports, "universalClientResponseFromFetch", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return universal_fetch_js_1.universalClientResponseFromFetch;
    }, "get") });
    Object.defineProperty(exports, "universalServerRequestFromFetch", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return universal_fetch_js_1.universalServerRequestFromFetch;
    }, "get") });
    Object.defineProperty(exports, "universalServerResponseToFetch", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return universal_fetch_js_1.universalServerResponseToFetch;
    }, "get") });
    var run_call_js_1 = require_run_call();
    Object.defineProperty(exports, "runUnaryCall", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return run_call_js_1.runUnaryCall;
    }, "get") });
    Object.defineProperty(exports, "runStreamingCall", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return run_call_js_1.runStreamingCall;
    }, "get") });
    var serialization_js_1 = require_serialization();
    Object.defineProperty(exports, "createMethodSerializationLookup", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return serialization_js_1.createMethodSerializationLookup;
    }, "get") });
    Object.defineProperty(exports, "createClientMethodSerializers", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return serialization_js_1.createClientMethodSerializers;
    }, "get") });
    Object.defineProperty(exports, "getJsonOptions", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return serialization_js_1.getJsonOptions;
    }, "get") });
    Object.defineProperty(exports, "limitSerialization", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return serialization_js_1.limitSerialization;
    }, "get") });
    var limit_io_js_1 = require_limit_io();
    Object.defineProperty(exports, "validateReadWriteMaxBytes", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return limit_io_js_1.validateReadWriteMaxBytes;
    }, "get") });
    var envelope_js_1 = require_envelope();
    Object.defineProperty(exports, "encodeEnvelope", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return envelope_js_1.encodeEnvelope;
    }, "get") });
    Object.defineProperty(exports, "encodeEnvelopes", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return envelope_js_1.encodeEnvelopes;
    }, "get") });
    Object.defineProperty(exports, "envelopeDecompress", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return envelope_js_1.envelopeDecompress;
    }, "get") });
    Object.defineProperty(exports, "envelopeCompress", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return envelope_js_1.envelopeCompress;
    }, "get") });
    Object.defineProperty(exports, "createEnvelopeReadableStream", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return envelope_js_1.createEnvelopeReadableStream;
    }, "get") });
    var compression_js_1 = require_compression();
    Object.defineProperty(exports, "compressedFlag", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return compression_js_1.compressedFlag;
    }, "get") });
    Object.defineProperty(exports, "compressionNegotiate", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return compression_js_1.compressionNegotiate;
    }, "get") });
    var async_iterable_js_1 = require_async_iterable();
    Object.defineProperty(exports, "pipe", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return async_iterable_js_1.pipe;
    }, "get") });
    Object.defineProperty(exports, "transformCatch", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return async_iterable_js_1.transformCatch;
    }, "get") });
    Object.defineProperty(exports, "transformCatchFinally", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return async_iterable_js_1.transformCatchFinally;
    }, "get") });
    Object.defineProperty(exports, "transformAppend", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return async_iterable_js_1.transformAppend;
    }, "get") });
    Object.defineProperty(exports, "transformPrepend", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return async_iterable_js_1.transformPrepend;
    }, "get") });
    Object.defineProperty(exports, "transformReadAllBytes", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return async_iterable_js_1.transformReadAllBytes;
    }, "get") });
    Object.defineProperty(exports, "transformCompressEnvelope", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return async_iterable_js_1.transformCompressEnvelope;
    }, "get") });
    Object.defineProperty(exports, "transformDecompressEnvelope", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return async_iterable_js_1.transformDecompressEnvelope;
    }, "get") });
    Object.defineProperty(exports, "transformJoinEnvelopes", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return async_iterable_js_1.transformJoinEnvelopes;
    }, "get") });
    Object.defineProperty(exports, "transformSplitEnvelope", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return async_iterable_js_1.transformSplitEnvelope;
    }, "get") });
    Object.defineProperty(exports, "transformSerializeEnvelope", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return async_iterable_js_1.transformSerializeEnvelope;
    }, "get") });
    Object.defineProperty(exports, "transformParseEnvelope", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return async_iterable_js_1.transformParseEnvelope;
    }, "get") });
    Object.defineProperty(exports, "pipeTo", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return async_iterable_js_1.pipeTo;
    }, "get") });
    Object.defineProperty(exports, "sinkAll", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return async_iterable_js_1.sinkAll;
    }, "get") });
    Object.defineProperty(exports, "sinkAllBytes", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return async_iterable_js_1.sinkAllBytes;
    }, "get") });
    Object.defineProperty(exports, "makeIterableAbortable", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return async_iterable_js_1.makeIterableAbortable;
    }, "get") });
    Object.defineProperty(exports, "createWritableIterable", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return async_iterable_js_1.createWritableIterable;
    }, "get") });
    Object.defineProperty(exports, "createAsyncIterable", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return async_iterable_js_1.createAsyncIterable;
    }, "get") });
    Object.defineProperty(exports, "readAllBytes", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return async_iterable_js_1.readAllBytes;
    }, "get") });
    Object.defineProperty(exports, "untilFirst", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return async_iterable_js_1.untilFirst;
    }, "get") });
    var content_type_matcher_js_1 = require_content_type_matcher();
    Object.defineProperty(exports, "contentTypeMatcher", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return content_type_matcher_js_1.contentTypeMatcher;
    }, "get") });
    var invoke_implementation_js_1 = require_invoke_implementation();
    Object.defineProperty(exports, "invokeUnaryImplementation", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return invoke_implementation_js_1.invokeUnaryImplementation;
    }, "get") });
    Object.defineProperty(exports, "transformInvokeImplementation", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return invoke_implementation_js_1.transformInvokeImplementation;
    }, "get") });
    var signals_js_1 = require_signals();
    Object.defineProperty(exports, "createLinkedAbortController", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return signals_js_1.createLinkedAbortController;
    }, "get") });
    Object.defineProperty(exports, "getAbortSignalReason", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return signals_js_1.getAbortSignalReason;
    }, "get") });
    Object.defineProperty(exports, "createDeadlineSignal", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return signals_js_1.createDeadlineSignal;
    }, "get") });
    var universal_js_1 = require_universal();
    Object.defineProperty(exports, "assertByteStreamRequest", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return universal_js_1.assertByteStreamRequest;
    }, "get") });
    Object.defineProperty(exports, "uResponseOk", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return universal_js_1.uResponseOk;
    }, "get") });
    Object.defineProperty(exports, "uResponseNotFound", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return universal_js_1.uResponseNotFound;
    }, "get") });
    Object.defineProperty(exports, "uResponseUnsupportedMediaType", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return universal_js_1.uResponseUnsupportedMediaType;
    }, "get") });
    Object.defineProperty(exports, "uResponseMethodNotAllowed", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return universal_js_1.uResponseMethodNotAllowed;
    }, "get") });
    Object.defineProperty(exports, "uResponseVersionNotSupported", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return universal_js_1.uResponseVersionNotSupported;
    }, "get") });
    var universal_handler_js_1 = require_universal_handler();
    Object.defineProperty(exports, "validateUniversalHandlerOptions", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return universal_handler_js_1.validateUniversalHandlerOptions;
    }, "get") });
    Object.defineProperty(exports, "createUniversalServiceHandlers", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return universal_handler_js_1.createUniversalServiceHandlers;
    }, "get") });
    Object.defineProperty(exports, "createUniversalMethodHandler", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return universal_handler_js_1.createUniversalMethodHandler;
    }, "get") });
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/index.js
var require_protocol_connect = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-connect/index.js"(exports) {
    "use strict";
    init_esm();
    var __createBinding = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: /* @__PURE__ */ __name(function() {
          return m[k];
        }, "get") };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    });
    var __exportStar = exports && exports.__exportStar || function(m, exports2) {
      for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p)) __createBinding(exports2, m, p);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.codeToString = exports.codeFromString = exports.protocolVersion = exports.transformConnectPostToGetRequest = exports.trailerDemux = exports.trailerMux = exports.validateResponseWithCompression = exports.validateResponse = exports.parseTimeout = exports.contentTypeStreamRegExp = exports.contentTypeUnaryRegExp = exports.contentTypeRegExp = exports.contentTypeStreamJson = exports.contentTypeStreamProto = exports.contentTypeUnaryJson = exports.contentTypeUnaryProto = exports.parseContentType = exports.errorToJsonBytes = exports.errorToJson = exports.errorFromJsonBytes = exports.errorFromJson = exports.createEndStreamSerialization = exports.endStreamFlag = exports.endStreamFromJson = exports.endStreamToJson = exports.requestHeaderWithCompression = exports.requestHeader = exports.codeToHttpStatus = exports.codeFromHttpStatus = exports.createTransport = exports.createHandlerFactory = void 0;
    var handler_factory_js_1 = require_handler_factory3();
    Object.defineProperty(exports, "createHandlerFactory", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return handler_factory_js_1.createHandlerFactory;
    }, "get") });
    var transport_js_1 = require_transport();
    Object.defineProperty(exports, "createTransport", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return transport_js_1.createTransport;
    }, "get") });
    var http_status_js_1 = require_http_status();
    Object.defineProperty(exports, "codeFromHttpStatus", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return http_status_js_1.codeFromHttpStatus;
    }, "get") });
    Object.defineProperty(exports, "codeToHttpStatus", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return http_status_js_1.codeToHttpStatus;
    }, "get") });
    var request_header_js_1 = require_request_header();
    Object.defineProperty(exports, "requestHeader", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return request_header_js_1.requestHeader;
    }, "get") });
    Object.defineProperty(exports, "requestHeaderWithCompression", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return request_header_js_1.requestHeaderWithCompression;
    }, "get") });
    var end_stream_js_1 = require_end_stream();
    Object.defineProperty(exports, "endStreamToJson", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return end_stream_js_1.endStreamToJson;
    }, "get") });
    Object.defineProperty(exports, "endStreamFromJson", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return end_stream_js_1.endStreamFromJson;
    }, "get") });
    Object.defineProperty(exports, "endStreamFlag", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return end_stream_js_1.endStreamFlag;
    }, "get") });
    Object.defineProperty(exports, "createEndStreamSerialization", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return end_stream_js_1.createEndStreamSerialization;
    }, "get") });
    var error_json_js_1 = require_error_json();
    Object.defineProperty(exports, "errorFromJson", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return error_json_js_1.errorFromJson;
    }, "get") });
    Object.defineProperty(exports, "errorFromJsonBytes", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return error_json_js_1.errorFromJsonBytes;
    }, "get") });
    Object.defineProperty(exports, "errorToJson", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return error_json_js_1.errorToJson;
    }, "get") });
    Object.defineProperty(exports, "errorToJsonBytes", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return error_json_js_1.errorToJsonBytes;
    }, "get") });
    var content_type_js_1 = require_content_type3();
    Object.defineProperty(exports, "parseContentType", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return content_type_js_1.parseContentType;
    }, "get") });
    Object.defineProperty(exports, "contentTypeUnaryProto", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return content_type_js_1.contentTypeUnaryProto;
    }, "get") });
    Object.defineProperty(exports, "contentTypeUnaryJson", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return content_type_js_1.contentTypeUnaryJson;
    }, "get") });
    Object.defineProperty(exports, "contentTypeStreamProto", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return content_type_js_1.contentTypeStreamProto;
    }, "get") });
    Object.defineProperty(exports, "contentTypeStreamJson", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return content_type_js_1.contentTypeStreamJson;
    }, "get") });
    Object.defineProperty(exports, "contentTypeRegExp", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return content_type_js_1.contentTypeRegExp;
    }, "get") });
    Object.defineProperty(exports, "contentTypeUnaryRegExp", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return content_type_js_1.contentTypeUnaryRegExp;
    }, "get") });
    Object.defineProperty(exports, "contentTypeStreamRegExp", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return content_type_js_1.contentTypeStreamRegExp;
    }, "get") });
    var parse_timeout_js_1 = require_parse_timeout2();
    Object.defineProperty(exports, "parseTimeout", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return parse_timeout_js_1.parseTimeout;
    }, "get") });
    var validate_response_js_1 = require_validate_response();
    Object.defineProperty(exports, "validateResponse", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return validate_response_js_1.validateResponse;
    }, "get") });
    Object.defineProperty(exports, "validateResponseWithCompression", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return validate_response_js_1.validateResponseWithCompression;
    }, "get") });
    var trailer_mux_js_1 = require_trailer_mux();
    Object.defineProperty(exports, "trailerMux", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return trailer_mux_js_1.trailerMux;
    }, "get") });
    Object.defineProperty(exports, "trailerDemux", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return trailer_mux_js_1.trailerDemux;
    }, "get") });
    __exportStar(require_headers3(), exports);
    var get_request_js_1 = require_get_request();
    Object.defineProperty(exports, "transformConnectPostToGetRequest", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return get_request_js_1.transformConnectPostToGetRequest;
    }, "get") });
    var version_js_1 = require_version();
    Object.defineProperty(exports, "protocolVersion", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return version_js_1.protocolVersion;
    }, "get") });
    var code_string_js_1 = require_code_string();
    Object.defineProperty(exports, "codeFromString", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return code_string_js_1.codeFromString;
    }, "get") });
    var code_string_js_2 = require_code_string();
    Object.defineProperty(exports, "codeToString", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return code_string_js_2.codeToString;
    }, "get") });
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect-web@2.0.0-rc.3_@bufbuild+protobuf@2.12.0_@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0_/node_modules/@connectrpc/connect-web/dist/cjs/assert-fetch-api.js
var require_assert_fetch_api = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect-web@2.0.0-rc.3_@bufbuild+protobuf@2.12.0_@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0_/node_modules/@connectrpc/connect-web/dist/cjs/assert-fetch-api.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.assertFetchApi = assertFetchApi;
    function assertFetchApi() {
      try {
        new Headers();
      } catch (_) {
        throw new Error("connect-web requires the fetch API. Are you running on an old version of Node.js? Node.js is not supported in Connect for Web - please stay tuned for Connect for Node.");
      }
    }
    __name(assertFetchApi, "assertFetchApi");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect-web@2.0.0-rc.3_@bufbuild+protobuf@2.12.0_@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0_/node_modules/@connectrpc/connect-web/dist/cjs/connect-transport.js
var require_connect_transport = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect-web@2.0.0-rc.3_@bufbuild+protobuf@2.12.0_@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0_/node_modules/@connectrpc/connect-web/dist/cjs/connect-transport.js"(exports) {
    "use strict";
    init_esm();
    var __await = exports && exports.__await || function(v) {
      return this instanceof __await ? (this.v = v, this) : new __await(v);
    };
    var __asyncGenerator = exports && exports.__asyncGenerator || function(thisArg, _arguments, generator) {
      if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
      var g = generator.apply(thisArg, _arguments || []), i, q = [];
      return i = Object.create((typeof AsyncIterator === "function" ? AsyncIterator : Object).prototype), verb("next"), verb("throw"), verb("return", awaitReturn), i[Symbol.asyncIterator] = function() {
        return this;
      }, i;
      function awaitReturn(f) {
        return function(v) {
          return Promise.resolve(v).then(f, reject);
        };
      }
      __name(awaitReturn, "awaitReturn");
      function verb(n, f) {
        if (g[n]) {
          i[n] = function(v) {
            return new Promise(function(a, b) {
              q.push([n, v, a, b]) > 1 || resume(n, v);
            });
          };
          if (f) i[n] = f(i[n]);
        }
      }
      __name(verb, "verb");
      function resume(n, v) {
        try {
          step(g[n](v));
        } catch (e) {
          settle(q[0][3], e);
        }
      }
      __name(resume, "resume");
      function step(r) {
        r.value instanceof __await ? Promise.resolve(r.value.v).then(fulfill, reject) : settle(q[0][2], r);
      }
      __name(step, "step");
      function fulfill(value) {
        resume("next", value);
      }
      __name(fulfill, "fulfill");
      function reject(value) {
        resume("throw", value);
      }
      __name(reject, "reject");
      function settle(f, v) {
        if (f(v), q.shift(), q.length) resume(q[0][0], q[0][1]);
      }
      __name(settle, "settle");
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createConnectTransport = createConnectTransport;
    var protobuf_1 = require_cjs();
    var connect_1 = require_cjs2();
    var protocol_1 = require_protocol();
    var protocol_connect_1 = require_protocol_connect();
    var assert_fetch_api_js_1 = require_assert_fetch_api();
    var wkt_1 = require_wkt();
    var fetchOptions = {
      redirect: "error"
    };
    function createConnectTransport(options) {
      var _a;
      (0, assert_fetch_api_js_1.assertFetchApi)();
      const useBinaryFormat = (_a = options.useBinaryFormat) !== null && _a !== void 0 ? _a : false;
      return {
        async unary(method, signal, timeoutMs, header, message, contextValues) {
          const { serialize, parse } = (0, protocol_1.createClientMethodSerializers)(method, useBinaryFormat, options.jsonOptions, options.binaryOptions);
          timeoutMs = timeoutMs === void 0 ? options.defaultTimeoutMs : timeoutMs <= 0 ? void 0 : timeoutMs;
          return await (0, protocol_1.runUnaryCall)({
            interceptors: options.interceptors,
            signal,
            timeoutMs,
            req: {
              stream: false,
              service: method.parent,
              method,
              requestMethod: "POST",
              url: (0, protocol_1.createMethodUrl)(options.baseUrl, method),
              header: (0, protocol_connect_1.requestHeader)(method.methodKind, useBinaryFormat, timeoutMs, header, false),
              contextValues: contextValues !== null && contextValues !== void 0 ? contextValues : (0, connect_1.createContextValues)(),
              message
            },
            next: /* @__PURE__ */ __name(async (req) => {
              var _a2;
              const useGet = options.useHttpGet === true && method.idempotency === wkt_1.MethodOptions_IdempotencyLevel.NO_SIDE_EFFECTS;
              let body = null;
              if (useGet) {
                req = (0, protocol_connect_1.transformConnectPostToGetRequest)(req, serialize(req.message), useBinaryFormat);
              } else {
                body = serialize(req.message);
              }
              const fetch2 = (_a2 = options.fetch) !== null && _a2 !== void 0 ? _a2 : globalThis.fetch;
              const response = await fetch2(req.url, Object.assign(Object.assign({}, fetchOptions), { method: req.requestMethod, headers: req.header, signal: req.signal, body }));
              const { isUnaryError, unaryError } = (0, protocol_connect_1.validateResponse)(method.methodKind, useBinaryFormat, response.status, response.headers);
              if (isUnaryError) {
                throw (0, protocol_connect_1.errorFromJson)(await response.json(), (0, connect_1.appendHeaders)(...(0, protocol_connect_1.trailerDemux)(response.headers)), unaryError);
              }
              const [demuxedHeader, demuxedTrailer] = (0, protocol_connect_1.trailerDemux)(response.headers);
              return {
                stream: false,
                service: method.parent,
                method,
                header: demuxedHeader,
                message: useBinaryFormat ? parse(new Uint8Array(await response.arrayBuffer())) : (0, protobuf_1.fromJson)(method.output, await response.json(), (0, protocol_1.getJsonOptions)(options.jsonOptions)),
                trailer: demuxedTrailer
              };
            }, "next")
          });
        },
        async stream(method, signal, timeoutMs, header, input, contextValues) {
          const { serialize, parse } = (0, protocol_1.createClientMethodSerializers)(method, useBinaryFormat, options.jsonOptions, options.binaryOptions);
          function parseResponseBody(body, trailerTarget, header2, signal2) {
            return __asyncGenerator(this, arguments, /* @__PURE__ */ __name(function* parseResponseBody_1() {
              const reader = (0, protocol_1.createEnvelopeReadableStream)(body).getReader();
              let endStreamReceived = false;
              for (; ; ) {
                const result = yield __await(reader.read());
                if (result.done) {
                  break;
                }
                const { flags, data } = result.value;
                if ((flags & protocol_1.compressedFlag) === protocol_1.compressedFlag) {
                  throw new connect_1.ConnectError(`protocol error: received unsupported compressed output`, connect_1.Code.Internal);
                }
                if ((flags & protocol_connect_1.endStreamFlag) === protocol_connect_1.endStreamFlag) {
                  endStreamReceived = true;
                  const endStream = (0, protocol_connect_1.endStreamFromJson)(data);
                  if (endStream.error) {
                    const error = endStream.error;
                    header2.forEach((value, key) => {
                      error.metadata.append(key, value);
                    });
                    throw error;
                  }
                  endStream.metadata.forEach((value, key) => trailerTarget.set(key, value));
                  continue;
                }
                yield yield __await(parse(data));
              }
              if ("throwIfAborted" in signal2) {
                signal2.throwIfAborted();
              }
              if (!endStreamReceived) {
                throw "missing EndStreamResponse";
              }
            }, "parseResponseBody_1"));
          }
          __name(parseResponseBody, "parseResponseBody");
          async function createRequestBody(input2) {
            if (method.methodKind != "server_streaming") {
              throw "The fetch API does not support streaming request bodies";
            }
            const r = await input2[Symbol.asyncIterator]().next();
            if (r.done == true) {
              throw "missing request message";
            }
            return (0, protocol_1.encodeEnvelope)(0, serialize(r.value));
          }
          __name(createRequestBody, "createRequestBody");
          timeoutMs = timeoutMs === void 0 ? options.defaultTimeoutMs : timeoutMs <= 0 ? void 0 : timeoutMs;
          return await (0, protocol_1.runStreamingCall)({
            interceptors: options.interceptors,
            timeoutMs,
            signal,
            req: {
              stream: true,
              service: method.parent,
              method,
              requestMethod: "POST",
              url: (0, protocol_1.createMethodUrl)(options.baseUrl, method),
              header: (0, protocol_connect_1.requestHeader)(method.methodKind, useBinaryFormat, timeoutMs, header, false),
              contextValues: contextValues !== null && contextValues !== void 0 ? contextValues : (0, connect_1.createContextValues)(),
              message: input
            },
            next: /* @__PURE__ */ __name(async (req) => {
              var _a2;
              const fetch2 = (_a2 = options.fetch) !== null && _a2 !== void 0 ? _a2 : globalThis.fetch;
              const fRes = await fetch2(req.url, Object.assign(Object.assign({}, fetchOptions), { method: req.requestMethod, headers: req.header, signal: req.signal, body: await createRequestBody(req.message) }));
              (0, protocol_connect_1.validateResponse)(method.methodKind, useBinaryFormat, fRes.status, fRes.headers);
              if (fRes.body === null) {
                throw "missing response body";
              }
              const trailer = new Headers();
              const res = Object.assign(Object.assign({}, req), { header: fRes.headers, trailer, message: parseResponseBody(fRes.body, trailer, fRes.headers, req.signal) });
              return res;
            }, "next")
          });
        }
      };
    }
    __name(createConnectTransport, "createConnectTransport");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/validate-trailer.js
var require_validate_trailer = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/validate-trailer.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.validateTrailer = validateTrailer;
    var code_js_1 = require_code();
    var connect_error_js_1 = require_connect_error();
    var headers_js_1 = require_headers();
    var trailer_status_js_1 = require_trailer_status();
    function validateTrailer(trailer, header) {
      const err = (0, trailer_status_js_1.findTrailerError)(trailer);
      if (err) {
        header.forEach((value, key) => {
          err.metadata.append(key, value);
        });
        throw err;
      }
      if (!header.has(headers_js_1.headerGrpcStatus) && !trailer.has(headers_js_1.headerGrpcStatus)) {
        throw new connect_error_js_1.ConnectError("protocol error: missing status", code_js_1.Code.Internal);
      }
    }
    __name(validateTrailer, "validateTrailer");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc-web/request-header.js
var require_request_header2 = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc-web/request-header.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.requestHeader = requestHeader;
    exports.requestHeaderWithCompression = requestHeaderWithCompression;
    var headers_js_1 = require_headers2();
    var content_type_js_1 = require_content_type();
    function requestHeader(useBinaryFormat, timeoutMs, userProvidedHeaders, setUserAgent) {
      const result = new Headers(userProvidedHeaders !== null && userProvidedHeaders !== void 0 ? userProvidedHeaders : {});
      result.set(headers_js_1.headerContentType, useBinaryFormat ? content_type_js_1.contentTypeProto : content_type_js_1.contentTypeJson);
      result.set(headers_js_1.headerXGrpcWeb, "1");
      let userAgent = "connect-es/2.0.0-rc.3";
      userAgent = result.has(headers_js_1.headerUserAgent) ? result.get(headers_js_1.headerUserAgent) : result.has(headers_js_1.headerXUserAgent) ? result.get(headers_js_1.headerXUserAgent) : userAgent;
      result.set(headers_js_1.headerXUserAgent, userAgent);
      if (setUserAgent) {
        result.set(headers_js_1.headerUserAgent, userAgent);
      }
      if (timeoutMs !== void 0) {
        result.set(headers_js_1.headerTimeout, `${timeoutMs}m`);
      }
      return result;
    }
    __name(requestHeader, "requestHeader");
    function requestHeaderWithCompression(useBinaryFormat, timeoutMs, userProvidedHeaders, acceptCompression, sendCompression, setUserAgent) {
      const result = requestHeader(useBinaryFormat, timeoutMs, userProvidedHeaders, setUserAgent);
      if (sendCompression != null) {
        result.set(headers_js_1.headerEncoding, sendCompression.name);
      }
      if (acceptCompression.length > 0) {
        result.set(headers_js_1.headerAcceptEncoding, acceptCompression.map((c) => c.name).join(","));
      }
      return result;
    }
    __name(requestHeaderWithCompression, "requestHeaderWithCompression");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/http-status.js
var require_http_status2 = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/http-status.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.codeFromHttpStatus = codeFromHttpStatus;
    var code_js_1 = require_code();
    function codeFromHttpStatus(httpStatus) {
      switch (httpStatus) {
        case 400:
          return code_js_1.Code.Internal;
        case 401:
          return code_js_1.Code.Unauthenticated;
        case 403:
          return code_js_1.Code.PermissionDenied;
        case 404:
          return code_js_1.Code.Unimplemented;
        case 429:
          return code_js_1.Code.Unavailable;
        case 502:
          return code_js_1.Code.Unavailable;
        case 503:
          return code_js_1.Code.Unavailable;
        case 504:
          return code_js_1.Code.Unavailable;
        default:
          return code_js_1.Code.Unknown;
      }
    }
    __name(codeFromHttpStatus, "codeFromHttpStatus");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc-web/validate-response.js
var require_validate_response2 = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc-web/validate-response.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.validateResponse = validateResponse;
    exports.validateResponseWithCompression = validateResponseWithCompression;
    var connect_error_js_1 = require_connect_error();
    var code_js_1 = require_code();
    var headers_js_1 = require_headers2();
    var http_status_js_1 = require_http_status2();
    var trailer_status_js_1 = require_trailer_status();
    function validateResponse(status, headers) {
      var _a;
      if (status >= 200 && status < 300) {
        return {
          foundStatus: headers.has(headers_js_1.headerGrpcStatus),
          headerError: (0, trailer_status_js_1.findTrailerError)(headers)
        };
      }
      throw new connect_error_js_1.ConnectError(decodeURIComponent((_a = headers.get(headers_js_1.headerGrpcMessage)) !== null && _a !== void 0 ? _a : `HTTP ${status}`), (0, http_status_js_1.codeFromHttpStatus)(status), headers);
    }
    __name(validateResponse, "validateResponse");
    function validateResponseWithCompression(acceptCompression, status, headers) {
      const { foundStatus, headerError } = validateResponse(status, headers);
      let compression;
      const encoding = headers.get(headers_js_1.headerEncoding);
      if (encoding !== null && encoding.toLowerCase() !== "identity") {
        compression = acceptCompression.find((c) => c.name === encoding);
        if (!compression) {
          throw new connect_error_js_1.ConnectError(`unsupported response encoding "${encoding}"`, code_js_1.Code.Internal, headers);
        }
      }
      return {
        foundStatus,
        compression,
        headerError
      };
    }
    __name(validateResponseWithCompression, "validateResponseWithCompression");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc-web/transport.js
var require_transport2 = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc-web/transport.js"(exports) {
    "use strict";
    init_esm();
    var __asyncValues = exports && exports.__asyncValues || function(o) {
      if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
      var m = o[Symbol.asyncIterator], i;
      return m ? m.call(o) : (o = typeof __values === "function" ? __values(o) : o[Symbol.iterator](), i = {}, verb("next"), verb("throw"), verb("return"), i[Symbol.asyncIterator] = function() {
        return this;
      }, i);
      function verb(n) {
        i[n] = o[n] && function(v) {
          return new Promise(function(resolve, reject) {
            v = o[n](v), settle(resolve, reject, v.done, v.value);
          });
        };
      }
      __name(verb, "verb");
      function settle(resolve, reject, d, v) {
        Promise.resolve(v).then(function(v2) {
          resolve({ value: v2, done: d });
        }, reject);
      }
      __name(settle, "settle");
    };
    var __await = exports && exports.__await || function(v) {
      return this instanceof __await ? (this.v = v, this) : new __await(v);
    };
    var __asyncGenerator = exports && exports.__asyncGenerator || function(thisArg, _arguments, generator) {
      if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
      var g = generator.apply(thisArg, _arguments || []), i, q = [];
      return i = Object.create((typeof AsyncIterator === "function" ? AsyncIterator : Object).prototype), verb("next"), verb("throw"), verb("return", awaitReturn), i[Symbol.asyncIterator] = function() {
        return this;
      }, i;
      function awaitReturn(f) {
        return function(v) {
          return Promise.resolve(v).then(f, reject);
        };
      }
      __name(awaitReturn, "awaitReturn");
      function verb(n, f) {
        if (g[n]) {
          i[n] = function(v) {
            return new Promise(function(a, b) {
              q.push([n, v, a, b]) > 1 || resume(n, v);
            });
          };
          if (f) i[n] = f(i[n]);
        }
      }
      __name(verb, "verb");
      function resume(n, v) {
        try {
          step(g[n](v));
        } catch (e) {
          settle(q[0][3], e);
        }
      }
      __name(resume, "resume");
      function step(r) {
        r.value instanceof __await ? Promise.resolve(r.value.v).then(fulfill, reject) : settle(q[0][2], r);
      }
      __name(step, "step");
      function fulfill(value) {
        resume("next", value);
      }
      __name(fulfill, "fulfill");
      function reject(value) {
        resume("throw", value);
      }
      __name(reject, "reject");
      function settle(f, v) {
        if (f(v), q.shift(), q.length) resume(q[0][0], q[0][1]);
      }
      __name(settle, "settle");
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createTransport = createTransport;
    var validate_trailer_js_1 = require_validate_trailer();
    var request_header_js_1 = require_request_header2();
    var validate_response_js_1 = require_validate_response2();
    var trailer_js_1 = require_trailer();
    var code_js_1 = require_code();
    var connect_error_js_1 = require_connect_error();
    var async_iterable_js_1 = require_async_iterable();
    var create_method_url_js_1 = require_create_method_url();
    var run_call_js_1 = require_run_call();
    var serialization_js_1 = require_serialization();
    var context_values_js_1 = require_context_values();
    var headers_js_1 = require_headers2();
    function createTransport(opt) {
      return {
        async unary(method, signal, timeoutMs, header, message, contextValues) {
          const serialization = (0, serialization_js_1.createMethodSerializationLookup)(method, opt.binaryOptions, opt.jsonOptions, opt);
          timeoutMs = timeoutMs === void 0 ? opt.defaultTimeoutMs : timeoutMs <= 0 ? void 0 : timeoutMs;
          return await (0, run_call_js_1.runUnaryCall)({
            interceptors: opt.interceptors,
            signal,
            timeoutMs,
            req: {
              stream: false,
              service: method.parent,
              method,
              requestMethod: "POST",
              url: (0, create_method_url_js_1.createMethodUrl)(opt.baseUrl, method),
              header: (0, request_header_js_1.requestHeaderWithCompression)(opt.useBinaryFormat, timeoutMs, header, opt.acceptCompression, opt.sendCompression, true),
              contextValues: contextValues !== null && contextValues !== void 0 ? contextValues : (0, context_values_js_1.createContextValues)(),
              message
            },
            next: /* @__PURE__ */ __name(async (req) => {
              const uRes = await opt.httpClient({
                url: req.url,
                method: req.requestMethod,
                header: req.header,
                signal: req.signal,
                body: (0, async_iterable_js_1.pipe)((0, async_iterable_js_1.createAsyncIterable)([req.message]), (0, async_iterable_js_1.transformSerializeEnvelope)(serialization.getI(opt.useBinaryFormat)), (0, async_iterable_js_1.transformCompressEnvelope)(opt.sendCompression, opt.compressMinBytes), (0, async_iterable_js_1.transformJoinEnvelopes)(), {
                  propagateDownStreamError: true
                })
              });
              const { compression, headerError } = (0, validate_response_js_1.validateResponseWithCompression)(opt.acceptCompression, uRes.status, uRes.header);
              const { trailer, message: message2 } = await (0, async_iterable_js_1.pipeTo)(uRes.body, (0, async_iterable_js_1.transformSplitEnvelope)(opt.readMaxBytes), (0, async_iterable_js_1.transformDecompressEnvelope)(compression !== null && compression !== void 0 ? compression : null, opt.readMaxBytes), (0, async_iterable_js_1.transformParseEnvelope)(serialization.getO(opt.useBinaryFormat), trailer_js_1.trailerFlag, (0, trailer_js_1.createTrailerSerialization)()), async (iterable) => {
                var _a, e_1, _b, _c;
                let message3;
                let trailer2;
                try {
                  for (var _d = true, iterable_1 = __asyncValues(iterable), iterable_1_1; iterable_1_1 = await iterable_1.next(), _a = iterable_1_1.done, !_a; _d = true) {
                    _c = iterable_1_1.value;
                    _d = false;
                    const env2 = _c;
                    if (env2.end) {
                      if (trailer2 !== void 0) {
                        throw new connect_error_js_1.ConnectError("protocol error: received extra trailer", code_js_1.Code.Unimplemented);
                      }
                      trailer2 = env2.value;
                    } else {
                      if (message3 !== void 0) {
                        throw new connect_error_js_1.ConnectError("protocol error: received extra output message for unary method", code_js_1.Code.Unimplemented);
                      }
                      message3 = env2.value;
                    }
                  }
                } catch (e_1_1) {
                  e_1 = { error: e_1_1 };
                } finally {
                  try {
                    if (!_d && !_a && (_b = iterable_1.return)) await _b.call(iterable_1);
                  } finally {
                    if (e_1) throw e_1.error;
                  }
                }
                return { trailer: trailer2, message: message3 };
              }, {
                propagateDownStreamError: false
              });
              if (trailer === void 0) {
                if (headerError != void 0) {
                  throw headerError;
                }
                throw new connect_error_js_1.ConnectError("protocol error: missing trailer", uRes.header.has(headers_js_1.headerGrpcStatus) ? code_js_1.Code.Unimplemented : code_js_1.Code.Unknown);
              }
              (0, validate_trailer_js_1.validateTrailer)(trailer, uRes.header);
              if (message2 === void 0) {
                throw new connect_error_js_1.ConnectError("protocol error: missing output message for unary method", trailer.has(headers_js_1.headerGrpcStatus) ? code_js_1.Code.Unimplemented : code_js_1.Code.Unknown);
              }
              return {
                stream: false,
                service: method.parent,
                method,
                header: uRes.header,
                message: message2,
                trailer
              };
            }, "next")
          });
        },
        async stream(method, signal, timeoutMs, header, input, contextValues) {
          const serialization = (0, serialization_js_1.createMethodSerializationLookup)(method, opt.binaryOptions, opt.jsonOptions, opt);
          timeoutMs = timeoutMs === void 0 ? opt.defaultTimeoutMs : timeoutMs <= 0 ? void 0 : timeoutMs;
          return (0, run_call_js_1.runStreamingCall)({
            interceptors: opt.interceptors,
            signal,
            timeoutMs,
            req: {
              stream: true,
              service: method.parent,
              method,
              requestMethod: "POST",
              url: (0, create_method_url_js_1.createMethodUrl)(opt.baseUrl, method),
              header: (0, request_header_js_1.requestHeaderWithCompression)(opt.useBinaryFormat, timeoutMs, header, opt.acceptCompression, opt.sendCompression, true),
              contextValues: contextValues !== null && contextValues !== void 0 ? contextValues : (0, context_values_js_1.createContextValues)(),
              message: input
            },
            next: /* @__PURE__ */ __name(async (req) => {
              const uRes = await opt.httpClient({
                url: req.url,
                method: req.requestMethod,
                header: req.header,
                signal: req.signal,
                body: (0, async_iterable_js_1.pipe)(req.message, (0, async_iterable_js_1.transformSerializeEnvelope)(serialization.getI(opt.useBinaryFormat)), (0, async_iterable_js_1.transformCompressEnvelope)(opt.sendCompression, opt.compressMinBytes), (0, async_iterable_js_1.transformJoinEnvelopes)(), { propagateDownStreamError: true })
              });
              const { compression, foundStatus, headerError } = (0, validate_response_js_1.validateResponseWithCompression)(opt.acceptCompression, uRes.status, uRes.header);
              if (headerError) {
                throw headerError;
              }
              const res = Object.assign(Object.assign({}, req), { header: uRes.header, trailer: new Headers(), message: (0, async_iterable_js_1.pipe)(uRes.body, (0, async_iterable_js_1.transformSplitEnvelope)(opt.readMaxBytes), (0, async_iterable_js_1.transformDecompressEnvelope)(compression !== null && compression !== void 0 ? compression : null, opt.readMaxBytes), (0, async_iterable_js_1.transformParseEnvelope)(serialization.getO(opt.useBinaryFormat), trailer_js_1.trailerFlag, (0, trailer_js_1.createTrailerSerialization)()), function(iterable) {
                return __asyncGenerator(this, arguments, function* () {
                  var _a, e_2, _b, _c;
                  if (foundStatus) {
                    const r = yield __await(iterable[Symbol.asyncIterator]().next());
                    if (r.done !== true) {
                      throw new connect_error_js_1.ConnectError("protocol error: extra data for trailers-only", code_js_1.Code.InvalidArgument);
                    }
                    return yield __await(void 0);
                  }
                  let trailerReceived = false;
                  try {
                    for (var _d = true, iterable_2 = __asyncValues(iterable), iterable_2_1; iterable_2_1 = yield __await(iterable_2.next()), _a = iterable_2_1.done, !_a; _d = true) {
                      _c = iterable_2_1.value;
                      _d = false;
                      const chunk = _c;
                      if (chunk.end) {
                        if (trailerReceived) {
                          throw new connect_error_js_1.ConnectError("protocol error: received extra trailer", code_js_1.Code.InvalidArgument);
                        }
                        trailerReceived = true;
                        (0, validate_trailer_js_1.validateTrailer)(chunk.value, uRes.header);
                        chunk.value.forEach((value, key) => res.trailer.set(key, value));
                        continue;
                      }
                      if (trailerReceived) {
                        throw new connect_error_js_1.ConnectError("protocol error: received extra message after trailer", code_js_1.Code.InvalidArgument);
                      }
                      yield yield __await(chunk.value);
                    }
                  } catch (e_2_1) {
                    e_2 = { error: e_2_1 };
                  } finally {
                    try {
                      if (!_d && !_a && (_b = iterable_2.return)) yield __await(_b.call(iterable_2));
                    } finally {
                      if (e_2) throw e_2.error;
                    }
                  }
                  if (!trailerReceived) {
                    throw new connect_error_js_1.ConnectError("protocol error: missing trailer", code_js_1.Code.Internal);
                  }
                });
              }, { propagateDownStreamError: true }) });
              return res;
            }, "next")
          });
        }
      };
    }
    __name(createTransport, "createTransport");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/request-header.js
var require_request_header3 = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/request-header.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.requestHeader = requestHeader;
    exports.requestHeaderWithCompression = requestHeaderWithCompression;
    var headers_js_1 = require_headers();
    var content_type_js_1 = require_content_type2();
    function requestHeader(useBinaryFormat, timeoutMs, userProvidedHeaders) {
      const result = new Headers(userProvidedHeaders !== null && userProvidedHeaders !== void 0 ? userProvidedHeaders : {});
      result.set(headers_js_1.headerContentType, useBinaryFormat ? content_type_js_1.contentTypeProto : content_type_js_1.contentTypeJson);
      if (!result.has(headers_js_1.headerUserAgent)) {
        result.set(headers_js_1.headerUserAgent, "connect-es/2.0.0-rc.3");
      }
      if (timeoutMs !== void 0) {
        result.set(headers_js_1.headerTimeout, `${timeoutMs}m`);
      }
      result.set("Te", "trailers");
      return result;
    }
    __name(requestHeader, "requestHeader");
    function requestHeaderWithCompression(useBinaryFormat, timeoutMs, userProvidedHeaders, acceptCompression, sendCompression) {
      const result = requestHeader(useBinaryFormat, timeoutMs, userProvidedHeaders);
      if (sendCompression != null) {
        result.set(headers_js_1.headerEncoding, sendCompression.name);
      }
      if (acceptCompression.length > 0) {
        result.set(headers_js_1.headerAcceptEncoding, acceptCompression.map((c) => c.name).join(","));
      }
      return result;
    }
    __name(requestHeaderWithCompression, "requestHeaderWithCompression");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/validate-response.js
var require_validate_response3 = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/validate-response.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.validateResponse = validateResponse;
    exports.validateResponseWithCompression = validateResponseWithCompression;
    var http_status_js_1 = require_http_status2();
    var connect_error_js_1 = require_connect_error();
    var trailer_status_js_1 = require_trailer_status();
    var code_js_1 = require_code();
    var headers_js_1 = require_headers();
    var content_type_js_1 = require_content_type2();
    function validateResponse(status, headers) {
      if (status != 200) {
        throw new connect_error_js_1.ConnectError(`HTTP ${status}`, (0, http_status_js_1.codeFromHttpStatus)(status), headers);
      }
      const mimeType = headers.get(headers_js_1.headerContentType);
      const parsedType = (0, content_type_js_1.parseContentType)(mimeType);
      if (parsedType == void 0) {
        throw new connect_error_js_1.ConnectError(`unsupported content type ${mimeType}`, code_js_1.Code.Unknown);
      }
      return {
        foundStatus: headers.has(headers_js_1.headerGrpcStatus),
        headerError: (0, trailer_status_js_1.findTrailerError)(headers)
      };
    }
    __name(validateResponse, "validateResponse");
    function validateResponseWithCompression(acceptCompression, status, headers) {
      const { foundStatus, headerError } = validateResponse(status, headers);
      let compression;
      const encoding = headers.get(headers_js_1.headerEncoding);
      if (encoding !== null && encoding.toLowerCase() !== "identity") {
        compression = acceptCompression.find((c) => c.name === encoding);
        if (!compression) {
          throw new connect_error_js_1.ConnectError(`unsupported response encoding "${encoding}"`, code_js_1.Code.Internal, headers);
        }
      }
      return {
        foundStatus,
        compression,
        headerError
      };
    }
    __name(validateResponseWithCompression, "validateResponseWithCompression");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/transport.js
var require_transport3 = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/transport.js"(exports) {
    "use strict";
    init_esm();
    var __asyncValues = exports && exports.__asyncValues || function(o) {
      if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
      var m = o[Symbol.asyncIterator], i;
      return m ? m.call(o) : (o = typeof __values === "function" ? __values(o) : o[Symbol.iterator](), i = {}, verb("next"), verb("throw"), verb("return"), i[Symbol.asyncIterator] = function() {
        return this;
      }, i);
      function verb(n) {
        i[n] = o[n] && function(v) {
          return new Promise(function(resolve, reject) {
            v = o[n](v), settle(resolve, reject, v.done, v.value);
          });
        };
      }
      __name(verb, "verb");
      function settle(resolve, reject, d, v) {
        Promise.resolve(v).then(function(v2) {
          resolve({ value: v2, done: d });
        }, reject);
      }
      __name(settle, "settle");
    };
    var __await = exports && exports.__await || function(v) {
      return this instanceof __await ? (this.v = v, this) : new __await(v);
    };
    var __asyncDelegator = exports && exports.__asyncDelegator || function(o) {
      var i, p;
      return i = {}, verb("next"), verb("throw", function(e) {
        throw e;
      }), verb("return"), i[Symbol.iterator] = function() {
        return this;
      }, i;
      function verb(n, f) {
        i[n] = o[n] ? function(v) {
          return (p = !p) ? { value: __await(o[n](v)), done: false } : f ? f(v) : v;
        } : f;
      }
      __name(verb, "verb");
    };
    var __asyncGenerator = exports && exports.__asyncGenerator || function(thisArg, _arguments, generator) {
      if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
      var g = generator.apply(thisArg, _arguments || []), i, q = [];
      return i = Object.create((typeof AsyncIterator === "function" ? AsyncIterator : Object).prototype), verb("next"), verb("throw"), verb("return", awaitReturn), i[Symbol.asyncIterator] = function() {
        return this;
      }, i;
      function awaitReturn(f) {
        return function(v) {
          return Promise.resolve(v).then(f, reject);
        };
      }
      __name(awaitReturn, "awaitReturn");
      function verb(n, f) {
        if (g[n]) {
          i[n] = function(v) {
            return new Promise(function(a, b) {
              q.push([n, v, a, b]) > 1 || resume(n, v);
            });
          };
          if (f) i[n] = f(i[n]);
        }
      }
      __name(verb, "verb");
      function resume(n, v) {
        try {
          step(g[n](v));
        } catch (e) {
          settle(q[0][3], e);
        }
      }
      __name(resume, "resume");
      function step(r) {
        r.value instanceof __await ? Promise.resolve(r.value.v).then(fulfill, reject) : settle(q[0][2], r);
      }
      __name(step, "step");
      function fulfill(value) {
        resume("next", value);
      }
      __name(fulfill, "fulfill");
      function reject(value) {
        resume("throw", value);
      }
      __name(reject, "reject");
      function settle(f, v) {
        if (f(v), q.shift(), q.length) resume(q[0][0], q[0][1]);
      }
      __name(settle, "settle");
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createTransport = createTransport;
    var request_header_js_1 = require_request_header3();
    var validate_response_js_1 = require_validate_response3();
    var validate_trailer_js_1 = require_validate_trailer();
    var code_js_1 = require_code();
    var connect_error_js_1 = require_connect_error();
    var async_iterable_js_1 = require_async_iterable();
    var create_method_url_js_1 = require_create_method_url();
    var run_call_js_1 = require_run_call();
    var serialization_js_1 = require_serialization();
    var context_values_js_1 = require_context_values();
    var headers_js_1 = require_headers();
    function createTransport(opt) {
      return {
        async unary(method, signal, timeoutMs, header, message, contextValues) {
          const serialization = (0, serialization_js_1.createMethodSerializationLookup)(method, opt.binaryOptions, opt.jsonOptions, opt);
          timeoutMs = timeoutMs === void 0 ? opt.defaultTimeoutMs : timeoutMs <= 0 ? void 0 : timeoutMs;
          return await (0, run_call_js_1.runUnaryCall)({
            interceptors: opt.interceptors,
            signal,
            timeoutMs,
            req: {
              stream: false,
              service: method.parent,
              method,
              requestMethod: "POST",
              url: (0, create_method_url_js_1.createMethodUrl)(opt.baseUrl, method),
              header: (0, request_header_js_1.requestHeaderWithCompression)(opt.useBinaryFormat, timeoutMs, header, opt.acceptCompression, opt.sendCompression),
              contextValues: contextValues !== null && contextValues !== void 0 ? contextValues : (0, context_values_js_1.createContextValues)(),
              message
            },
            next: /* @__PURE__ */ __name(async (req) => {
              const uRes = await opt.httpClient({
                url: req.url,
                method: "POST",
                header: req.header,
                signal: req.signal,
                body: (0, async_iterable_js_1.pipe)((0, async_iterable_js_1.createAsyncIterable)([req.message]), (0, async_iterable_js_1.transformSerializeEnvelope)(serialization.getI(opt.useBinaryFormat)), (0, async_iterable_js_1.transformCompressEnvelope)(opt.sendCompression, opt.compressMinBytes), (0, async_iterable_js_1.transformJoinEnvelopes)(), {
                  propagateDownStreamError: true
                })
              });
              const { compression, headerError } = (0, validate_response_js_1.validateResponseWithCompression)(opt.acceptCompression, uRes.status, uRes.header);
              const message2 = await (0, async_iterable_js_1.pipeTo)(uRes.body, (0, async_iterable_js_1.transformSplitEnvelope)(opt.readMaxBytes), (0, async_iterable_js_1.transformDecompressEnvelope)(compression !== null && compression !== void 0 ? compression : null, opt.readMaxBytes), (0, async_iterable_js_1.transformParseEnvelope)(serialization.getO(opt.useBinaryFormat)), async (iterable) => {
                var _a, e_1, _b, _c;
                let message3;
                try {
                  for (var _d = true, iterable_1 = __asyncValues(iterable), iterable_1_1; iterable_1_1 = await iterable_1.next(), _a = iterable_1_1.done, !_a; _d = true) {
                    _c = iterable_1_1.value;
                    _d = false;
                    const chunk = _c;
                    if (message3 !== void 0) {
                      throw new connect_error_js_1.ConnectError("protocol error: received extra output message for unary method", code_js_1.Code.Unimplemented);
                    }
                    message3 = chunk;
                  }
                } catch (e_1_1) {
                  e_1 = { error: e_1_1 };
                } finally {
                  try {
                    if (!_d && !_a && (_b = iterable_1.return)) await _b.call(iterable_1);
                  } finally {
                    if (e_1) throw e_1.error;
                  }
                }
                return message3;
              }, { propagateDownStreamError: false });
              (0, validate_trailer_js_1.validateTrailer)(uRes.trailer, uRes.header);
              if (message2 === void 0) {
                if (headerError) {
                  throw headerError;
                }
                throw new connect_error_js_1.ConnectError("protocol error: missing output message for unary method", uRes.trailer.has(headers_js_1.headerGrpcStatus) ? code_js_1.Code.Unimplemented : code_js_1.Code.Unknown);
              }
              if (headerError) {
                throw new connect_error_js_1.ConnectError("protocol error: received output message for unary method with error status", code_js_1.Code.Unknown);
              }
              return {
                stream: false,
                service: method.parent,
                method,
                header: uRes.header,
                message: message2,
                trailer: uRes.trailer
              };
            }, "next")
          });
        },
        async stream(method, signal, timeoutMs, header, input, contextValues) {
          const serialization = (0, serialization_js_1.createMethodSerializationLookup)(method, opt.binaryOptions, opt.jsonOptions, opt);
          timeoutMs = timeoutMs === void 0 ? opt.defaultTimeoutMs : timeoutMs <= 0 ? void 0 : timeoutMs;
          return (0, run_call_js_1.runStreamingCall)({
            interceptors: opt.interceptors,
            signal,
            timeoutMs,
            req: {
              stream: true,
              service: method.parent,
              method,
              requestMethod: "POST",
              url: (0, create_method_url_js_1.createMethodUrl)(opt.baseUrl, method),
              header: (0, request_header_js_1.requestHeaderWithCompression)(opt.useBinaryFormat, timeoutMs, header, opt.acceptCompression, opt.sendCompression),
              contextValues: contextValues !== null && contextValues !== void 0 ? contextValues : (0, context_values_js_1.createContextValues)(),
              message: input
            },
            next: /* @__PURE__ */ __name(async (req) => {
              const uRes = await opt.httpClient({
                url: req.url,
                method: "POST",
                header: req.header,
                signal: req.signal,
                body: (0, async_iterable_js_1.pipe)(req.message, (0, async_iterable_js_1.transformSerializeEnvelope)(serialization.getI(opt.useBinaryFormat)), (0, async_iterable_js_1.transformCompressEnvelope)(opt.sendCompression, opt.compressMinBytes), (0, async_iterable_js_1.transformJoinEnvelopes)(), { propagateDownStreamError: true })
              });
              const { compression, foundStatus, headerError } = (0, validate_response_js_1.validateResponseWithCompression)(opt.acceptCompression, uRes.status, uRes.header);
              if (headerError) {
                throw headerError;
              }
              const res = Object.assign(Object.assign({}, req), { header: uRes.header, trailer: uRes.trailer, message: (0, async_iterable_js_1.pipe)(uRes.body, (0, async_iterable_js_1.transformSplitEnvelope)(opt.readMaxBytes), (0, async_iterable_js_1.transformDecompressEnvelope)(compression !== null && compression !== void 0 ? compression : null, opt.readMaxBytes), (0, async_iterable_js_1.transformParseEnvelope)(serialization.getO(opt.useBinaryFormat)), function(iterable) {
                return __asyncGenerator(this, arguments, function* () {
                  yield __await(yield* __asyncDelegator(__asyncValues(iterable)));
                  if (!foundStatus) {
                    (0, validate_trailer_js_1.validateTrailer)(uRes.trailer, uRes.header);
                  }
                });
              }, { propagateDownStreamError: true }) });
              return res;
            }, "next")
          });
        }
      };
    }
    __name(createTransport, "createTransport");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/index.js
var require_protocol_grpc = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc/index.js"(exports) {
    "use strict";
    init_esm();
    var __createBinding = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: /* @__PURE__ */ __name(function() {
          return m[k];
        }, "get") };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    });
    var __exportStar = exports && exports.__exportStar || function(m, exports2) {
      for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p)) __createBinding(exports2, m, p);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.validateTrailer = exports.validateResponseWithCompression = exports.validateResponse = exports.grpcStatusOk = exports.setTrailerStatus = exports.findTrailerError = exports.parseTimeout = exports.contentTypeProto = exports.contentTypeJson = exports.contentTypeRegExp = exports.parseContentType = exports.requestHeaderWithCompression = exports.requestHeader = exports.codeFromHttpStatus = exports.createTransport = exports.createHandlerFactory = void 0;
    var handler_factory_js_1 = require_handler_factory2();
    Object.defineProperty(exports, "createHandlerFactory", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return handler_factory_js_1.createHandlerFactory;
    }, "get") });
    var transport_js_1 = require_transport3();
    Object.defineProperty(exports, "createTransport", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return transport_js_1.createTransport;
    }, "get") });
    var http_status_js_1 = require_http_status2();
    Object.defineProperty(exports, "codeFromHttpStatus", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return http_status_js_1.codeFromHttpStatus;
    }, "get") });
    var request_header_js_1 = require_request_header3();
    Object.defineProperty(exports, "requestHeader", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return request_header_js_1.requestHeader;
    }, "get") });
    Object.defineProperty(exports, "requestHeaderWithCompression", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return request_header_js_1.requestHeaderWithCompression;
    }, "get") });
    var content_type_js_1 = require_content_type2();
    Object.defineProperty(exports, "parseContentType", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return content_type_js_1.parseContentType;
    }, "get") });
    Object.defineProperty(exports, "contentTypeRegExp", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return content_type_js_1.contentTypeRegExp;
    }, "get") });
    Object.defineProperty(exports, "contentTypeJson", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return content_type_js_1.contentTypeJson;
    }, "get") });
    Object.defineProperty(exports, "contentTypeProto", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return content_type_js_1.contentTypeProto;
    }, "get") });
    var parse_timeout_js_1 = require_parse_timeout();
    Object.defineProperty(exports, "parseTimeout", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return parse_timeout_js_1.parseTimeout;
    }, "get") });
    var trailer_status_js_1 = require_trailer_status();
    Object.defineProperty(exports, "findTrailerError", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return trailer_status_js_1.findTrailerError;
    }, "get") });
    Object.defineProperty(exports, "setTrailerStatus", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return trailer_status_js_1.setTrailerStatus;
    }, "get") });
    Object.defineProperty(exports, "grpcStatusOk", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return trailer_status_js_1.grpcStatusOk;
    }, "get") });
    var validate_response_js_1 = require_validate_response3();
    Object.defineProperty(exports, "validateResponse", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return validate_response_js_1.validateResponse;
    }, "get") });
    Object.defineProperty(exports, "validateResponseWithCompression", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return validate_response_js_1.validateResponseWithCompression;
    }, "get") });
    var validate_trailer_js_1 = require_validate_trailer();
    Object.defineProperty(exports, "validateTrailer", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return validate_trailer_js_1.validateTrailer;
    }, "get") });
    __exportStar(require_headers(), exports);
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc-web/index.js
var require_protocol_grpc_web = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0/node_modules/@connectrpc/connect/dist/cjs/protocol-grpc-web/index.js"(exports) {
    "use strict";
    init_esm();
    var __createBinding = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: /* @__PURE__ */ __name(function() {
          return m[k];
        }, "get") };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0) k2 = k;
      o[k2] = m[k];
    });
    var __exportStar = exports && exports.__exportStar || function(m, exports2) {
      for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p)) __createBinding(exports2, m, p);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.grpcStatusOk = exports.validateTrailer = exports.setTrailerStatus = exports.parseTimeout = exports.createTrailerSerialization = exports.trailerSerialize = exports.trailerParse = exports.trailerFlag = exports.validateResponseWithCompression = exports.validateResponse = exports.contentTypeJson = exports.contentTypeProto = exports.contentTypeRegExp = exports.parseContentType = exports.requestHeaderWithCompression = exports.requestHeader = exports.createTransport = exports.createHandlerFactory = void 0;
    var handler_factory_js_1 = require_handler_factory();
    Object.defineProperty(exports, "createHandlerFactory", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return handler_factory_js_1.createHandlerFactory;
    }, "get") });
    var transport_js_1 = require_transport2();
    Object.defineProperty(exports, "createTransport", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return transport_js_1.createTransport;
    }, "get") });
    var request_header_js_1 = require_request_header2();
    Object.defineProperty(exports, "requestHeader", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return request_header_js_1.requestHeader;
    }, "get") });
    Object.defineProperty(exports, "requestHeaderWithCompression", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return request_header_js_1.requestHeaderWithCompression;
    }, "get") });
    var content_type_js_1 = require_content_type();
    Object.defineProperty(exports, "parseContentType", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return content_type_js_1.parseContentType;
    }, "get") });
    Object.defineProperty(exports, "contentTypeRegExp", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return content_type_js_1.contentTypeRegExp;
    }, "get") });
    Object.defineProperty(exports, "contentTypeProto", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return content_type_js_1.contentTypeProto;
    }, "get") });
    Object.defineProperty(exports, "contentTypeJson", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return content_type_js_1.contentTypeJson;
    }, "get") });
    var validate_response_js_1 = require_validate_response2();
    Object.defineProperty(exports, "validateResponse", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return validate_response_js_1.validateResponse;
    }, "get") });
    Object.defineProperty(exports, "validateResponseWithCompression", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return validate_response_js_1.validateResponseWithCompression;
    }, "get") });
    var trailer_js_1 = require_trailer();
    Object.defineProperty(exports, "trailerFlag", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return trailer_js_1.trailerFlag;
    }, "get") });
    Object.defineProperty(exports, "trailerParse", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return trailer_js_1.trailerParse;
    }, "get") });
    Object.defineProperty(exports, "trailerSerialize", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return trailer_js_1.trailerSerialize;
    }, "get") });
    Object.defineProperty(exports, "createTrailerSerialization", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return trailer_js_1.createTrailerSerialization;
    }, "get") });
    var index_js_1 = require_protocol_grpc();
    Object.defineProperty(exports, "parseTimeout", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return index_js_1.parseTimeout;
    }, "get") });
    Object.defineProperty(exports, "setTrailerStatus", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return index_js_1.setTrailerStatus;
    }, "get") });
    Object.defineProperty(exports, "validateTrailer", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return index_js_1.validateTrailer;
    }, "get") });
    Object.defineProperty(exports, "grpcStatusOk", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return index_js_1.grpcStatusOk;
    }, "get") });
    __exportStar(require_headers2(), exports);
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect-web@2.0.0-rc.3_@bufbuild+protobuf@2.12.0_@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0_/node_modules/@connectrpc/connect-web/dist/cjs/grpc-web-transport.js
var require_grpc_web_transport = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect-web@2.0.0-rc.3_@bufbuild+protobuf@2.12.0_@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0_/node_modules/@connectrpc/connect-web/dist/cjs/grpc-web-transport.js"(exports) {
    "use strict";
    init_esm();
    var __await = exports && exports.__await || function(v) {
      return this instanceof __await ? (this.v = v, this) : new __await(v);
    };
    var __asyncGenerator = exports && exports.__asyncGenerator || function(thisArg, _arguments, generator) {
      if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
      var g = generator.apply(thisArg, _arguments || []), i, q = [];
      return i = Object.create((typeof AsyncIterator === "function" ? AsyncIterator : Object).prototype), verb("next"), verb("throw"), verb("return", awaitReturn), i[Symbol.asyncIterator] = function() {
        return this;
      }, i;
      function awaitReturn(f) {
        return function(v) {
          return Promise.resolve(v).then(f, reject);
        };
      }
      __name(awaitReturn, "awaitReturn");
      function verb(n, f) {
        if (g[n]) {
          i[n] = function(v) {
            return new Promise(function(a, b) {
              q.push([n, v, a, b]) > 1 || resume(n, v);
            });
          };
          if (f) i[n] = f(i[n]);
        }
      }
      __name(verb, "verb");
      function resume(n, v) {
        try {
          step(g[n](v));
        } catch (e) {
          settle(q[0][3], e);
        }
      }
      __name(resume, "resume");
      function step(r) {
        r.value instanceof __await ? Promise.resolve(r.value.v).then(fulfill, reject) : settle(q[0][2], r);
      }
      __name(step, "step");
      function fulfill(value) {
        resume("next", value);
      }
      __name(fulfill, "fulfill");
      function reject(value) {
        resume("throw", value);
      }
      __name(reject, "reject");
      function settle(f, v) {
        if (f(v), q.shift(), q.length) resume(q[0][0], q[0][1]);
      }
      __name(settle, "settle");
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createGrpcWebTransport = createGrpcWebTransport;
    var connect_1 = require_cjs2();
    var protocol_1 = require_protocol();
    var protocol_grpc_web_1 = require_protocol_grpc_web();
    var assert_fetch_api_js_1 = require_assert_fetch_api();
    var fetchOptions = {
      redirect: "error"
    };
    function createGrpcWebTransport(options) {
      var _a;
      (0, assert_fetch_api_js_1.assertFetchApi)();
      const useBinaryFormat = (_a = options.useBinaryFormat) !== null && _a !== void 0 ? _a : true;
      return {
        async unary(method, signal, timeoutMs, header, message, contextValues) {
          const { serialize, parse } = (0, protocol_1.createClientMethodSerializers)(method, useBinaryFormat, options.jsonOptions, options.binaryOptions);
          timeoutMs = timeoutMs === void 0 ? options.defaultTimeoutMs : timeoutMs <= 0 ? void 0 : timeoutMs;
          return await (0, protocol_1.runUnaryCall)({
            interceptors: options.interceptors,
            signal,
            timeoutMs,
            req: {
              stream: false,
              service: method.parent,
              method,
              requestMethod: "POST",
              url: (0, protocol_1.createMethodUrl)(options.baseUrl, method),
              header: (0, protocol_grpc_web_1.requestHeader)(useBinaryFormat, timeoutMs, header, false),
              contextValues: contextValues !== null && contextValues !== void 0 ? contextValues : (0, connect_1.createContextValues)(),
              message
            },
            next: /* @__PURE__ */ __name(async (req) => {
              var _a2;
              const fetch2 = (_a2 = options.fetch) !== null && _a2 !== void 0 ? _a2 : globalThis.fetch;
              const response = await fetch2(req.url, Object.assign(Object.assign({}, fetchOptions), { method: req.requestMethod, headers: req.header, signal: req.signal, body: (0, protocol_1.encodeEnvelope)(0, serialize(req.message)) }));
              const { headerError } = (0, protocol_grpc_web_1.validateResponse)(response.status, response.headers);
              if (!response.body) {
                if (headerError !== void 0)
                  throw headerError;
                throw "missing response body";
              }
              const reader = (0, protocol_1.createEnvelopeReadableStream)(response.body).getReader();
              let trailer;
              let message2;
              for (; ; ) {
                const r = await reader.read();
                if (r.done) {
                  break;
                }
                const { flags, data } = r.value;
                if ((flags & protocol_1.compressedFlag) === protocol_1.compressedFlag) {
                  throw new connect_1.ConnectError(`protocol error: received unsupported compressed output`, connect_1.Code.Internal);
                }
                if (flags === protocol_grpc_web_1.trailerFlag) {
                  if (trailer !== void 0) {
                    throw "extra trailer";
                  }
                  trailer = (0, protocol_grpc_web_1.trailerParse)(data);
                  continue;
                }
                if (message2 !== void 0) {
                  throw new connect_1.ConnectError("extra message", connect_1.Code.Unimplemented);
                }
                message2 = parse(data);
              }
              if (trailer === void 0) {
                if (headerError !== void 0)
                  throw headerError;
                throw new connect_1.ConnectError("missing trailer", response.headers.has(protocol_grpc_web_1.headerGrpcStatus) ? connect_1.Code.Unimplemented : connect_1.Code.Unknown);
              }
              (0, protocol_grpc_web_1.validateTrailer)(trailer, response.headers);
              if (message2 === void 0) {
                throw new connect_1.ConnectError("missing message", trailer.has(protocol_grpc_web_1.headerGrpcStatus) ? connect_1.Code.Unimplemented : connect_1.Code.Unknown);
              }
              return {
                stream: false,
                service: method.parent,
                method,
                header: response.headers,
                message: message2,
                trailer
              };
            }, "next")
          });
        },
        async stream(method, signal, timeoutMs, header, input, contextValues) {
          const { serialize, parse } = (0, protocol_1.createClientMethodSerializers)(method, useBinaryFormat, options.jsonOptions, options.binaryOptions);
          function parseResponseBody(body, foundStatus, trailerTarget, header2, signal2) {
            return __asyncGenerator(this, arguments, /* @__PURE__ */ __name(function* parseResponseBody_1() {
              const reader = (0, protocol_1.createEnvelopeReadableStream)(body).getReader();
              if (foundStatus) {
                if (!(yield __await(reader.read())).done) {
                  throw "extra data for trailers-only";
                }
                return yield __await(void 0);
              }
              let trailerReceived = false;
              for (; ; ) {
                const result = yield __await(reader.read());
                if (result.done) {
                  break;
                }
                const { flags, data } = result.value;
                if ((flags & protocol_grpc_web_1.trailerFlag) === protocol_grpc_web_1.trailerFlag) {
                  if (trailerReceived) {
                    throw "extra trailer";
                  }
                  trailerReceived = true;
                  const trailer = (0, protocol_grpc_web_1.trailerParse)(data);
                  (0, protocol_grpc_web_1.validateTrailer)(trailer, header2);
                  trailer.forEach((value, key) => trailerTarget.set(key, value));
                  continue;
                }
                if (trailerReceived) {
                  throw "extra message";
                }
                yield yield __await(parse(data));
              }
              if ("throwIfAborted" in signal2) {
                signal2.throwIfAborted();
              }
              if (!trailerReceived) {
                throw "missing trailer";
              }
            }, "parseResponseBody_1"));
          }
          __name(parseResponseBody, "parseResponseBody");
          async function createRequestBody(input2) {
            if (method.methodKind != "server_streaming") {
              throw "The fetch API does not support streaming request bodies";
            }
            const r = await input2[Symbol.asyncIterator]().next();
            if (r.done == true) {
              throw "missing request message";
            }
            return (0, protocol_1.encodeEnvelope)(0, serialize(r.value));
          }
          __name(createRequestBody, "createRequestBody");
          timeoutMs = timeoutMs === void 0 ? options.defaultTimeoutMs : timeoutMs <= 0 ? void 0 : timeoutMs;
          return (0, protocol_1.runStreamingCall)({
            interceptors: options.interceptors,
            signal,
            timeoutMs,
            req: {
              stream: true,
              service: method.parent,
              method,
              requestMethod: "POST",
              url: (0, protocol_1.createMethodUrl)(options.baseUrl, method),
              header: (0, protocol_grpc_web_1.requestHeader)(useBinaryFormat, timeoutMs, header, false),
              contextValues: contextValues !== null && contextValues !== void 0 ? contextValues : (0, connect_1.createContextValues)(),
              message: input
            },
            next: /* @__PURE__ */ __name(async (req) => {
              var _a2;
              const fetch2 = (_a2 = options.fetch) !== null && _a2 !== void 0 ? _a2 : globalThis.fetch;
              const fRes = await fetch2(req.url, Object.assign(Object.assign({}, fetchOptions), { method: req.requestMethod, headers: req.header, signal: req.signal, body: await createRequestBody(req.message) }));
              const { foundStatus, headerError } = (0, protocol_grpc_web_1.validateResponse)(fRes.status, fRes.headers);
              if (headerError != void 0) {
                throw headerError;
              }
              if (!fRes.body) {
                throw "missing response body";
              }
              const trailer = new Headers();
              const res = Object.assign(Object.assign({}, req), { header: fRes.headers, trailer, message: parseResponseBody(fRes.body, foundStatus, trailer, fRes.headers, req.signal) });
              return res;
            }, "next")
          });
        }
      };
    }
    __name(createGrpcWebTransport, "createGrpcWebTransport");
  }
});

// ../../node_modules/.pnpm/@connectrpc+connect-web@2.0.0-rc.3_@bufbuild+protobuf@2.12.0_@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0_/node_modules/@connectrpc/connect-web/dist/cjs/index.js
var require_cjs3 = __commonJS({
  "../../node_modules/.pnpm/@connectrpc+connect-web@2.0.0-rc.3_@bufbuild+protobuf@2.12.0_@connectrpc+connect@2.0.0-rc.3_@bufbuild+protobuf@2.12.0_/node_modules/@connectrpc/connect-web/dist/cjs/index.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createGrpcWebTransport = exports.createConnectTransport = void 0;
    var connect_transport_js_1 = require_connect_transport();
    Object.defineProperty(exports, "createConnectTransport", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return connect_transport_js_1.createConnectTransport;
    }, "get") });
    var grpc_web_transport_js_1 = require_grpc_web_transport();
    Object.defineProperty(exports, "createGrpcWebTransport", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return grpc_web_transport_js_1.createGrpcWebTransport;
    }, "get") });
  }
});

// ../../node_modules/.pnpm/chalk@5.6.2/node_modules/chalk/source/vendor/ansi-styles/index.js
function assembleStyles() {
  const codes = /* @__PURE__ */ new Map();
  for (const [groupName, group] of Object.entries(styles)) {
    for (const [styleName, style] of Object.entries(group)) {
      styles[styleName] = {
        open: `\x1B[${style[0]}m`,
        close: `\x1B[${style[1]}m`
      };
      group[styleName] = styles[styleName];
      codes.set(style[0], style[1]);
    }
    Object.defineProperty(styles, groupName, {
      value: group,
      enumerable: false
    });
  }
  Object.defineProperty(styles, "codes", {
    value: codes,
    enumerable: false
  });
  styles.color.close = "\x1B[39m";
  styles.bgColor.close = "\x1B[49m";
  styles.color.ansi = wrapAnsi16();
  styles.color.ansi256 = wrapAnsi256();
  styles.color.ansi16m = wrapAnsi16m();
  styles.bgColor.ansi = wrapAnsi16(ANSI_BACKGROUND_OFFSET);
  styles.bgColor.ansi256 = wrapAnsi256(ANSI_BACKGROUND_OFFSET);
  styles.bgColor.ansi16m = wrapAnsi16m(ANSI_BACKGROUND_OFFSET);
  Object.defineProperties(styles, {
    rgbToAnsi256: {
      value(red, green, blue) {
        if (red === green && green === blue) {
          if (red < 8) {
            return 16;
          }
          if (red > 248) {
            return 231;
          }
          return Math.round((red - 8) / 247 * 24) + 232;
        }
        return 16 + 36 * Math.round(red / 255 * 5) + 6 * Math.round(green / 255 * 5) + Math.round(blue / 255 * 5);
      },
      enumerable: false
    },
    hexToRgb: {
      value(hex) {
        const matches = /[a-f\d]{6}|[a-f\d]{3}/i.exec(hex.toString(16));
        if (!matches) {
          return [0, 0, 0];
        }
        let [colorString] = matches;
        if (colorString.length === 3) {
          colorString = [...colorString].map((character) => character + character).join("");
        }
        const integer2 = Number.parseInt(colorString, 16);
        return [
          /* eslint-disable no-bitwise */
          integer2 >> 16 & 255,
          integer2 >> 8 & 255,
          integer2 & 255
          /* eslint-enable no-bitwise */
        ];
      },
      enumerable: false
    },
    hexToAnsi256: {
      value: /* @__PURE__ */ __name((hex) => styles.rgbToAnsi256(...styles.hexToRgb(hex)), "value"),
      enumerable: false
    },
    ansi256ToAnsi: {
      value(code) {
        if (code < 8) {
          return 30 + code;
        }
        if (code < 16) {
          return 90 + (code - 8);
        }
        let red;
        let green;
        let blue;
        if (code >= 232) {
          red = ((code - 232) * 10 + 8) / 255;
          green = red;
          blue = red;
        } else {
          code -= 16;
          const remainder = code % 36;
          red = Math.floor(code / 36) / 5;
          green = Math.floor(remainder / 6) / 5;
          blue = remainder % 6 / 5;
        }
        const value = Math.max(red, green, blue) * 2;
        if (value === 0) {
          return 30;
        }
        let result = 30 + (Math.round(blue) << 2 | Math.round(green) << 1 | Math.round(red));
        if (value === 2) {
          result += 60;
        }
        return result;
      },
      enumerable: false
    },
    rgbToAnsi: {
      value: /* @__PURE__ */ __name((red, green, blue) => styles.ansi256ToAnsi(styles.rgbToAnsi256(red, green, blue)), "value"),
      enumerable: false
    },
    hexToAnsi: {
      value: /* @__PURE__ */ __name((hex) => styles.ansi256ToAnsi(styles.hexToAnsi256(hex)), "value"),
      enumerable: false
    }
  });
  return styles;
}
var ANSI_BACKGROUND_OFFSET, wrapAnsi16, wrapAnsi256, wrapAnsi16m, styles, modifierNames, foregroundColorNames, backgroundColorNames, colorNames, ansiStyles, ansi_styles_default;
var init_ansi_styles = __esm({
  "../../node_modules/.pnpm/chalk@5.6.2/node_modules/chalk/source/vendor/ansi-styles/index.js"() {
    init_esm();
    ANSI_BACKGROUND_OFFSET = 10;
    wrapAnsi16 = /* @__PURE__ */ __name((offset = 0) => (code) => `\x1B[${code + offset}m`, "wrapAnsi16");
    wrapAnsi256 = /* @__PURE__ */ __name((offset = 0) => (code) => `\x1B[${38 + offset};5;${code}m`, "wrapAnsi256");
    wrapAnsi16m = /* @__PURE__ */ __name((offset = 0) => (red, green, blue) => `\x1B[${38 + offset};2;${red};${green};${blue}m`, "wrapAnsi16m");
    styles = {
      modifier: {
        reset: [0, 0],
        // 21 isn't widely supported and 22 does the same thing
        bold: [1, 22],
        dim: [2, 22],
        italic: [3, 23],
        underline: [4, 24],
        overline: [53, 55],
        inverse: [7, 27],
        hidden: [8, 28],
        strikethrough: [9, 29]
      },
      color: {
        black: [30, 39],
        red: [31, 39],
        green: [32, 39],
        yellow: [33, 39],
        blue: [34, 39],
        magenta: [35, 39],
        cyan: [36, 39],
        white: [37, 39],
        // Bright color
        blackBright: [90, 39],
        gray: [90, 39],
        // Alias of `blackBright`
        grey: [90, 39],
        // Alias of `blackBright`
        redBright: [91, 39],
        greenBright: [92, 39],
        yellowBright: [93, 39],
        blueBright: [94, 39],
        magentaBright: [95, 39],
        cyanBright: [96, 39],
        whiteBright: [97, 39]
      },
      bgColor: {
        bgBlack: [40, 49],
        bgRed: [41, 49],
        bgGreen: [42, 49],
        bgYellow: [43, 49],
        bgBlue: [44, 49],
        bgMagenta: [45, 49],
        bgCyan: [46, 49],
        bgWhite: [47, 49],
        // Bright color
        bgBlackBright: [100, 49],
        bgGray: [100, 49],
        // Alias of `bgBlackBright`
        bgGrey: [100, 49],
        // Alias of `bgBlackBright`
        bgRedBright: [101, 49],
        bgGreenBright: [102, 49],
        bgYellowBright: [103, 49],
        bgBlueBright: [104, 49],
        bgMagentaBright: [105, 49],
        bgCyanBright: [106, 49],
        bgWhiteBright: [107, 49]
      }
    };
    modifierNames = Object.keys(styles.modifier);
    foregroundColorNames = Object.keys(styles.color);
    backgroundColorNames = Object.keys(styles.bgColor);
    colorNames = [...foregroundColorNames, ...backgroundColorNames];
    __name(assembleStyles, "assembleStyles");
    ansiStyles = assembleStyles();
    ansi_styles_default = ansiStyles;
  }
});

// ../../node_modules/.pnpm/chalk@5.6.2/node_modules/chalk/source/vendor/supports-color/index.js
import process2 from "node:process";
import os from "node:os";
import tty from "node:tty";
function hasFlag(flag, argv = globalThis.Deno ? globalThis.Deno.args : process2.argv) {
  const prefix = flag.startsWith("-") ? "" : flag.length === 1 ? "-" : "--";
  const position = argv.indexOf(prefix + flag);
  const terminatorPosition = argv.indexOf("--");
  return position !== -1 && (terminatorPosition === -1 || position < terminatorPosition);
}
function envForceColor() {
  if ("FORCE_COLOR" in env) {
    if (env.FORCE_COLOR === "true") {
      return 1;
    }
    if (env.FORCE_COLOR === "false") {
      return 0;
    }
    return env.FORCE_COLOR.length === 0 ? 1 : Math.min(Number.parseInt(env.FORCE_COLOR, 10), 3);
  }
}
function translateLevel(level) {
  if (level === 0) {
    return false;
  }
  return {
    level,
    hasBasic: true,
    has256: level >= 2,
    has16m: level >= 3
  };
}
function _supportsColor(haveStream, { streamIsTTY, sniffFlags = true } = {}) {
  const noFlagForceColor = envForceColor();
  if (noFlagForceColor !== void 0) {
    flagForceColor = noFlagForceColor;
  }
  const forceColor = sniffFlags ? flagForceColor : noFlagForceColor;
  if (forceColor === 0) {
    return 0;
  }
  if (sniffFlags) {
    if (hasFlag("color=16m") || hasFlag("color=full") || hasFlag("color=truecolor")) {
      return 3;
    }
    if (hasFlag("color=256")) {
      return 2;
    }
  }
  if ("TF_BUILD" in env && "AGENT_NAME" in env) {
    return 1;
  }
  if (haveStream && !streamIsTTY && forceColor === void 0) {
    return 0;
  }
  const min = forceColor || 0;
  if (env.TERM === "dumb") {
    return min;
  }
  if (process2.platform === "win32") {
    const osRelease = os.release().split(".");
    if (Number(osRelease[0]) >= 10 && Number(osRelease[2]) >= 10586) {
      return Number(osRelease[2]) >= 14931 ? 3 : 2;
    }
    return 1;
  }
  if ("CI" in env) {
    if (["GITHUB_ACTIONS", "GITEA_ACTIONS", "CIRCLECI"].some((key) => key in env)) {
      return 3;
    }
    if (["TRAVIS", "APPVEYOR", "GITLAB_CI", "BUILDKITE", "DRONE"].some((sign) => sign in env) || env.CI_NAME === "codeship") {
      return 1;
    }
    return min;
  }
  if ("TEAMCITY_VERSION" in env) {
    return /^(9\.(0*[1-9]\d*)\.|\d{2,}\.)/.test(env.TEAMCITY_VERSION) ? 1 : 0;
  }
  if (env.COLORTERM === "truecolor") {
    return 3;
  }
  if (env.TERM === "xterm-kitty") {
    return 3;
  }
  if (env.TERM === "xterm-ghostty") {
    return 3;
  }
  if (env.TERM === "wezterm") {
    return 3;
  }
  if ("TERM_PROGRAM" in env) {
    const version = Number.parseInt((env.TERM_PROGRAM_VERSION || "").split(".")[0], 10);
    switch (env.TERM_PROGRAM) {
      case "iTerm.app": {
        return version >= 3 ? 3 : 2;
      }
      case "Apple_Terminal": {
        return 2;
      }
    }
  }
  if (/-256(color)?$/i.test(env.TERM)) {
    return 2;
  }
  if (/^screen|^xterm|^vt100|^vt220|^rxvt|color|ansi|cygwin|linux/i.test(env.TERM)) {
    return 1;
  }
  if ("COLORTERM" in env) {
    return 1;
  }
  return min;
}
function createSupportsColor(stream, options = {}) {
  const level = _supportsColor(stream, {
    streamIsTTY: stream && stream.isTTY,
    ...options
  });
  return translateLevel(level);
}
var env, flagForceColor, supportsColor, supports_color_default;
var init_supports_color = __esm({
  "../../node_modules/.pnpm/chalk@5.6.2/node_modules/chalk/source/vendor/supports-color/index.js"() {
    init_esm();
    __name(hasFlag, "hasFlag");
    ({ env } = process2);
    if (hasFlag("no-color") || hasFlag("no-colors") || hasFlag("color=false") || hasFlag("color=never")) {
      flagForceColor = 0;
    } else if (hasFlag("color") || hasFlag("colors") || hasFlag("color=true") || hasFlag("color=always")) {
      flagForceColor = 1;
    }
    __name(envForceColor, "envForceColor");
    __name(translateLevel, "translateLevel");
    __name(_supportsColor, "_supportsColor");
    __name(createSupportsColor, "createSupportsColor");
    supportsColor = {
      stdout: createSupportsColor({ isTTY: tty.isatty(1) }),
      stderr: createSupportsColor({ isTTY: tty.isatty(2) })
    };
    supports_color_default = supportsColor;
  }
});

// ../../node_modules/.pnpm/chalk@5.6.2/node_modules/chalk/source/utilities.js
function stringReplaceAll(string, substring, replacer) {
  let index = string.indexOf(substring);
  if (index === -1) {
    return string;
  }
  const substringLength = substring.length;
  let endIndex = 0;
  let returnValue = "";
  do {
    returnValue += string.slice(endIndex, index) + substring + replacer;
    endIndex = index + substringLength;
    index = string.indexOf(substring, endIndex);
  } while (index !== -1);
  returnValue += string.slice(endIndex);
  return returnValue;
}
function stringEncaseCRLFWithFirstIndex(string, prefix, postfix, index) {
  let endIndex = 0;
  let returnValue = "";
  do {
    const gotCR = string[index - 1] === "\r";
    returnValue += string.slice(endIndex, gotCR ? index - 1 : index) + prefix + (gotCR ? "\r\n" : "\n") + postfix;
    endIndex = index + 1;
    index = string.indexOf("\n", endIndex);
  } while (index !== -1);
  returnValue += string.slice(endIndex);
  return returnValue;
}
var init_utilities = __esm({
  "../../node_modules/.pnpm/chalk@5.6.2/node_modules/chalk/source/utilities.js"() {
    init_esm();
    __name(stringReplaceAll, "stringReplaceAll");
    __name(stringEncaseCRLFWithFirstIndex, "stringEncaseCRLFWithFirstIndex");
  }
});

// ../../node_modules/.pnpm/chalk@5.6.2/node_modules/chalk/source/index.js
var source_exports = {};
__export(source_exports, {
  Chalk: () => Chalk,
  backgroundColorNames: () => backgroundColorNames,
  backgroundColors: () => backgroundColorNames,
  chalkStderr: () => chalkStderr,
  colorNames: () => colorNames,
  colors: () => colorNames,
  default: () => source_default,
  foregroundColorNames: () => foregroundColorNames,
  foregroundColors: () => foregroundColorNames,
  modifierNames: () => modifierNames,
  modifiers: () => modifierNames,
  supportsColor: () => stdoutColor,
  supportsColorStderr: () => stderrColor
});
function createChalk(options) {
  return chalkFactory(options);
}
var stdoutColor, stderrColor, GENERATOR, STYLER, IS_EMPTY, levelMapping, styles2, applyOptions, Chalk, chalkFactory, getModelAnsi, usedModels, proto, createStyler, createBuilder, applyStyle, chalk, chalkStderr, source_default;
var init_source = __esm({
  "../../node_modules/.pnpm/chalk@5.6.2/node_modules/chalk/source/index.js"() {
    init_esm();
    init_ansi_styles();
    init_supports_color();
    init_utilities();
    init_ansi_styles();
    ({ stdout: stdoutColor, stderr: stderrColor } = supports_color_default);
    GENERATOR = Symbol("GENERATOR");
    STYLER = Symbol("STYLER");
    IS_EMPTY = Symbol("IS_EMPTY");
    levelMapping = [
      "ansi",
      "ansi",
      "ansi256",
      "ansi16m"
    ];
    styles2 = /* @__PURE__ */ Object.create(null);
    applyOptions = /* @__PURE__ */ __name((object, options = {}) => {
      if (options.level && !(Number.isInteger(options.level) && options.level >= 0 && options.level <= 3)) {
        throw new Error("The `level` option should be an integer from 0 to 3");
      }
      const colorLevel = stdoutColor ? stdoutColor.level : 0;
      object.level = options.level === void 0 ? colorLevel : options.level;
    }, "applyOptions");
    Chalk = class {
      static {
        __name(this, "Chalk");
      }
      constructor(options) {
        return chalkFactory(options);
      }
    };
    chalkFactory = /* @__PURE__ */ __name((options) => {
      const chalk2 = /* @__PURE__ */ __name((...strings) => strings.join(" "), "chalk");
      applyOptions(chalk2, options);
      Object.setPrototypeOf(chalk2, createChalk.prototype);
      return chalk2;
    }, "chalkFactory");
    __name(createChalk, "createChalk");
    Object.setPrototypeOf(createChalk.prototype, Function.prototype);
    for (const [styleName, style] of Object.entries(ansi_styles_default)) {
      styles2[styleName] = {
        get() {
          const builder = createBuilder(this, createStyler(style.open, style.close, this[STYLER]), this[IS_EMPTY]);
          Object.defineProperty(this, styleName, { value: builder });
          return builder;
        }
      };
    }
    styles2.visible = {
      get() {
        const builder = createBuilder(this, this[STYLER], true);
        Object.defineProperty(this, "visible", { value: builder });
        return builder;
      }
    };
    getModelAnsi = /* @__PURE__ */ __name((model, level, type, ...arguments_) => {
      if (model === "rgb") {
        if (level === "ansi16m") {
          return ansi_styles_default[type].ansi16m(...arguments_);
        }
        if (level === "ansi256") {
          return ansi_styles_default[type].ansi256(ansi_styles_default.rgbToAnsi256(...arguments_));
        }
        return ansi_styles_default[type].ansi(ansi_styles_default.rgbToAnsi(...arguments_));
      }
      if (model === "hex") {
        return getModelAnsi("rgb", level, type, ...ansi_styles_default.hexToRgb(...arguments_));
      }
      return ansi_styles_default[type][model](...arguments_);
    }, "getModelAnsi");
    usedModels = ["rgb", "hex", "ansi256"];
    for (const model of usedModels) {
      styles2[model] = {
        get() {
          const { level } = this;
          return function(...arguments_) {
            const styler = createStyler(getModelAnsi(model, levelMapping[level], "color", ...arguments_), ansi_styles_default.color.close, this[STYLER]);
            return createBuilder(this, styler, this[IS_EMPTY]);
          };
        }
      };
      const bgModel = "bg" + model[0].toUpperCase() + model.slice(1);
      styles2[bgModel] = {
        get() {
          const { level } = this;
          return function(...arguments_) {
            const styler = createStyler(getModelAnsi(model, levelMapping[level], "bgColor", ...arguments_), ansi_styles_default.bgColor.close, this[STYLER]);
            return createBuilder(this, styler, this[IS_EMPTY]);
          };
        }
      };
    }
    proto = Object.defineProperties(() => {
    }, {
      ...styles2,
      level: {
        enumerable: true,
        get() {
          return this[GENERATOR].level;
        },
        set(level) {
          this[GENERATOR].level = level;
        }
      }
    });
    createStyler = /* @__PURE__ */ __name((open, close, parent) => {
      let openAll;
      let closeAll;
      if (parent === void 0) {
        openAll = open;
        closeAll = close;
      } else {
        openAll = parent.openAll + open;
        closeAll = close + parent.closeAll;
      }
      return {
        open,
        close,
        openAll,
        closeAll,
        parent
      };
    }, "createStyler");
    createBuilder = /* @__PURE__ */ __name((self2, _styler, _isEmpty) => {
      const builder = /* @__PURE__ */ __name((...arguments_) => applyStyle(builder, arguments_.length === 1 ? "" + arguments_[0] : arguments_.join(" ")), "builder");
      Object.setPrototypeOf(builder, proto);
      builder[GENERATOR] = self2;
      builder[STYLER] = _styler;
      builder[IS_EMPTY] = _isEmpty;
      return builder;
    }, "createBuilder");
    applyStyle = /* @__PURE__ */ __name((self2, string) => {
      if (self2.level <= 0 || !string) {
        return self2[IS_EMPTY] ? "" : string;
      }
      let styler = self2[STYLER];
      if (styler === void 0) {
        return string;
      }
      const { openAll, closeAll } = styler;
      if (string.includes("\x1B")) {
        while (styler !== void 0) {
          string = stringReplaceAll(string, styler.close, styler.open);
          styler = styler.parent;
        }
      }
      const lfIndex = string.indexOf("\n");
      if (lfIndex !== -1) {
        string = stringEncaseCRLFWithFirstIndex(string, closeAll, openAll, lfIndex);
      }
      return openAll + string + closeAll;
    }, "applyStyle");
    Object.defineProperties(createChalk.prototype, styles2);
    chalk = createChalk();
    chalkStderr = createChalk({ level: stderrColor ? stderrColor.level : 0 });
    source_default = chalk;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/argument.js
var require_argument = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/argument.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Argument = void 0;
    var Argument = class {
      static {
        __name(this, "Argument");
      }
      constructor(value, range) {
        this.value = value;
        this.range = range;
      }
      toString() {
        return this.value;
      }
      getRange() {
        return this.range;
      }
      getValue() {
        return this.value;
      }
      isAfter(position) {
        if (this.range.end.line < position.line) {
          return false;
        }
        return this.range.start.line > position.line ? true : this.range.start.character > position.character;
      }
      isBefore(position) {
        if (this.range.start.line < position.line) {
          return true;
        }
        return this.range.end.line > position.line ? false : this.range.end.character < position.character;
      }
    };
    exports.Argument = Argument;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/jsonArgument.js
var require_jsonArgument = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/jsonArgument.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.JSONArgument = void 0;
    var argument_1 = require_argument();
    var JSONArgument = class extends argument_1.Argument {
      static {
        __name(this, "JSONArgument");
      }
      constructor(value, range, jsonRange) {
        super(value, range);
        this.jsonRange = jsonRange;
      }
      getJSONRange() {
        return this.jsonRange;
      }
      getJSONValue() {
        let value = super.getValue();
        value = value.substring(1, value.length - 1);
        return value;
      }
    };
    exports.JSONArgument = JSONArgument;
  }
});

// ../../node_modules/.pnpm/vscode-languageserver-types@3.18.0/node_modules/vscode-languageserver-types/lib/esm/main.js
var main_exports = {};
__export(main_exports, {
  AnnotatedTextEdit: () => AnnotatedTextEdit,
  ApplyKind: () => ApplyKind,
  ChangeAnnotation: () => ChangeAnnotation,
  ChangeAnnotationIdentifier: () => ChangeAnnotationIdentifier,
  CodeAction: () => CodeAction,
  CodeActionContext: () => CodeActionContext,
  CodeActionKind: () => CodeActionKind,
  CodeActionTag: () => CodeActionTag,
  CodeActionTriggerKind: () => CodeActionTriggerKind,
  CodeDescription: () => CodeDescription,
  CodeLens: () => CodeLens,
  Color: () => Color,
  ColorInformation: () => ColorInformation,
  ColorPresentation: () => ColorPresentation,
  Command: () => Command,
  CompletionItem: () => CompletionItem,
  CompletionItemKind: () => CompletionItemKind,
  CompletionItemLabelDetails: () => CompletionItemLabelDetails,
  CompletionItemTag: () => CompletionItemTag,
  CompletionList: () => CompletionList,
  CreateFile: () => CreateFile,
  DeleteFile: () => DeleteFile,
  Diagnostic: () => Diagnostic,
  DiagnosticRelatedInformation: () => DiagnosticRelatedInformation,
  DiagnosticSeverity: () => DiagnosticSeverity,
  DiagnosticTag: () => DiagnosticTag,
  DocumentHighlight: () => DocumentHighlight,
  DocumentHighlightKind: () => DocumentHighlightKind,
  DocumentLink: () => DocumentLink,
  DocumentSymbol: () => DocumentSymbol,
  DocumentUri: () => DocumentUri,
  EOL: () => EOL,
  FoldingRange: () => FoldingRange,
  FoldingRangeKind: () => FoldingRangeKind,
  FormattingOptions: () => FormattingOptions,
  Hover: () => Hover,
  InlayHint: () => InlayHint,
  InlayHintKind: () => InlayHintKind,
  InlayHintLabelPart: () => InlayHintLabelPart,
  InlineCompletionContext: () => InlineCompletionContext,
  InlineCompletionItem: () => InlineCompletionItem,
  InlineCompletionList: () => InlineCompletionList,
  InlineCompletionTriggerKind: () => InlineCompletionTriggerKind,
  InlineValueContext: () => InlineValueContext,
  InlineValueEvaluatableExpression: () => InlineValueEvaluatableExpression,
  InlineValueText: () => InlineValueText,
  InlineValueVariableLookup: () => InlineValueVariableLookup,
  InsertReplaceEdit: () => InsertReplaceEdit,
  InsertTextFormat: () => InsertTextFormat,
  InsertTextMode: () => InsertTextMode,
  LanguageKind: () => LanguageKind,
  Location: () => Location,
  LocationLink: () => LocationLink,
  MarkedString: () => MarkedString,
  MarkupContent: () => MarkupContent,
  MarkupKind: () => MarkupKind,
  OptionalVersionedTextDocumentIdentifier: () => OptionalVersionedTextDocumentIdentifier,
  ParameterInformation: () => ParameterInformation,
  Position: () => Position,
  Range: () => Range,
  RenameFile: () => RenameFile,
  SelectedCompletionInfo: () => SelectedCompletionInfo,
  SelectionRange: () => SelectionRange,
  SemanticTokenModifiers: () => SemanticTokenModifiers,
  SemanticTokenTypes: () => SemanticTokenTypes,
  SemanticTokens: () => SemanticTokens,
  SignatureInformation: () => SignatureInformation,
  SnippetTextEdit: () => SnippetTextEdit,
  StringValue: () => StringValue,
  SymbolInformation: () => SymbolInformation,
  SymbolKind: () => SymbolKind,
  SymbolTag: () => SymbolTag,
  TextDocument: () => TextDocument,
  TextDocumentEdit: () => TextDocumentEdit,
  TextDocumentIdentifier: () => TextDocumentIdentifier,
  TextDocumentItem: () => TextDocumentItem,
  TextEdit: () => TextEdit,
  URI: () => URI,
  VersionedTextDocumentIdentifier: () => VersionedTextDocumentIdentifier,
  WorkspaceChange: () => WorkspaceChange,
  WorkspaceEdit: () => WorkspaceEdit,
  WorkspaceFolder: () => WorkspaceFolder,
  WorkspaceSymbol: () => WorkspaceSymbol,
  integer: () => integer,
  uinteger: () => uinteger
});
var DocumentUri, URI, integer, uinteger, Position, Range, Location, LocationLink, Color, ColorInformation, ColorPresentation, FoldingRangeKind, FoldingRange, DiagnosticRelatedInformation, DiagnosticSeverity, DiagnosticTag, CodeDescription, Diagnostic, Command, TextEdit, ChangeAnnotation, ChangeAnnotationIdentifier, AnnotatedTextEdit, TextDocumentEdit, CreateFile, RenameFile, DeleteFile, WorkspaceEdit, TextEditChangeImpl, SnippetTextEdit, ChangeAnnotations, WorkspaceChange, TextDocumentIdentifier, VersionedTextDocumentIdentifier, OptionalVersionedTextDocumentIdentifier, LanguageKind, TextDocumentItem, MarkupKind, MarkupContent, CompletionItemKind, InsertTextFormat, CompletionItemTag, InsertReplaceEdit, InsertTextMode, ApplyKind, CompletionItemLabelDetails, CompletionItem, CompletionList, MarkedString, Hover, ParameterInformation, SignatureInformation, DocumentHighlightKind, DocumentHighlight, SymbolKind, SymbolTag, SymbolInformation, WorkspaceSymbol, DocumentSymbol, CodeActionKind, CodeActionTriggerKind, CodeActionContext, CodeActionTag, CodeAction, CodeLens, FormattingOptions, DocumentLink, SelectionRange, SemanticTokenTypes, SemanticTokenModifiers, SemanticTokens, InlineValueText, InlineValueVariableLookup, InlineValueEvaluatableExpression, InlineValueContext, InlayHintKind, InlayHintLabelPart, InlayHint, StringValue, InlineCompletionItem, InlineCompletionList, InlineCompletionTriggerKind, SelectedCompletionInfo, InlineCompletionContext, WorkspaceFolder, EOL, TextDocument, FullTextDocument, Is;
var init_main = __esm({
  "../../node_modules/.pnpm/vscode-languageserver-types@3.18.0/node_modules/vscode-languageserver-types/lib/esm/main.js"() {
    "use strict";
    init_esm();
    (function(DocumentUri2) {
      function is(value) {
        return typeof value === "string";
      }
      __name(is, "is");
      DocumentUri2.is = is;
    })(DocumentUri || (DocumentUri = {}));
    (function(URI2) {
      function is(value) {
        return typeof value === "string";
      }
      __name(is, "is");
      URI2.is = is;
    })(URI || (URI = {}));
    (function(integer2) {
      integer2.MIN_VALUE = -2147483648;
      integer2.MAX_VALUE = 2147483647;
      function is(value) {
        return typeof value === "number" && integer2.MIN_VALUE <= value && value <= integer2.MAX_VALUE;
      }
      __name(is, "is");
      integer2.is = is;
    })(integer || (integer = {}));
    (function(uinteger2) {
      uinteger2.MIN_VALUE = 0;
      uinteger2.MAX_VALUE = 2147483647;
      function is(value) {
        return typeof value === "number" && uinteger2.MIN_VALUE <= value && value <= uinteger2.MAX_VALUE;
      }
      __name(is, "is");
      uinteger2.is = is;
    })(uinteger || (uinteger = {}));
    (function(Position2) {
      function create(line, character) {
        if (line === Number.MAX_VALUE) {
          line = uinteger.MAX_VALUE;
        }
        if (character === Number.MAX_VALUE) {
          character = uinteger.MAX_VALUE;
        }
        return { line, character };
      }
      __name(create, "create");
      Position2.create = create;
      function is(value) {
        const candidate = value;
        return Is.objectLiteral(candidate) && Is.uinteger(candidate.line) && Is.uinteger(candidate.character);
      }
      __name(is, "is");
      Position2.is = is;
    })(Position || (Position = {}));
    (function(Range2) {
      function create(one, two, three, four) {
        if (Is.uinteger(one) && Is.uinteger(two) && Is.uinteger(three) && Is.uinteger(four)) {
          return { start: Position.create(one, two), end: Position.create(three, four) };
        } else if (Position.is(one) && Position.is(two)) {
          return { start: one, end: two };
        } else {
          throw new Error(`Range#create called with invalid arguments[${one}, ${two}, ${three}, ${four}]`);
        }
      }
      __name(create, "create");
      Range2.create = create;
      function is(value) {
        const candidate = value;
        return Is.objectLiteral(candidate) && Position.is(candidate.start) && Position.is(candidate.end);
      }
      __name(is, "is");
      Range2.is = is;
    })(Range || (Range = {}));
    (function(Location2) {
      function create(uri, range) {
        return { uri, range };
      }
      __name(create, "create");
      Location2.create = create;
      function is(value) {
        const candidate = value;
        return Is.objectLiteral(candidate) && Range.is(candidate.range) && (Is.string(candidate.uri) || Is.undefined(candidate.uri));
      }
      __name(is, "is");
      Location2.is = is;
    })(Location || (Location = {}));
    (function(LocationLink2) {
      function create(targetUri, targetRange, targetSelectionRange, originSelectionRange) {
        return { targetUri, targetRange, targetSelectionRange, originSelectionRange };
      }
      __name(create, "create");
      LocationLink2.create = create;
      function is(value) {
        const candidate = value;
        return Is.objectLiteral(candidate) && Range.is(candidate.targetRange) && Is.string(candidate.targetUri) && Range.is(candidate.targetSelectionRange) && (Range.is(candidate.originSelectionRange) || Is.undefined(candidate.originSelectionRange));
      }
      __name(is, "is");
      LocationLink2.is = is;
    })(LocationLink || (LocationLink = {}));
    (function(Color2) {
      function create(red, green, blue, alpha) {
        return {
          red,
          green,
          blue,
          alpha
        };
      }
      __name(create, "create");
      Color2.create = create;
      function is(value) {
        const candidate = value;
        return Is.objectLiteral(candidate) && Is.numberRange(candidate.red, 0, 1) && Is.numberRange(candidate.green, 0, 1) && Is.numberRange(candidate.blue, 0, 1) && Is.numberRange(candidate.alpha, 0, 1);
      }
      __name(is, "is");
      Color2.is = is;
    })(Color || (Color = {}));
    (function(ColorInformation2) {
      function create(range, color) {
        return {
          range,
          color
        };
      }
      __name(create, "create");
      ColorInformation2.create = create;
      function is(value) {
        const candidate = value;
        return Is.objectLiteral(candidate) && Range.is(candidate.range) && Color.is(candidate.color);
      }
      __name(is, "is");
      ColorInformation2.is = is;
    })(ColorInformation || (ColorInformation = {}));
    (function(ColorPresentation2) {
      function create(label, textEdit, additionalTextEdits) {
        return {
          label,
          textEdit,
          additionalTextEdits
        };
      }
      __name(create, "create");
      ColorPresentation2.create = create;
      function is(value) {
        const candidate = value;
        return Is.objectLiteral(candidate) && Is.string(candidate.label) && (Is.undefined(candidate.textEdit) || TextEdit.is(candidate)) && (Is.undefined(candidate.additionalTextEdits) || Is.typedArray(candidate.additionalTextEdits, TextEdit.is));
      }
      __name(is, "is");
      ColorPresentation2.is = is;
    })(ColorPresentation || (ColorPresentation = {}));
    (function(FoldingRangeKind2) {
      FoldingRangeKind2.Comment = "comment";
      FoldingRangeKind2.Imports = "imports";
      FoldingRangeKind2.Region = "region";
    })(FoldingRangeKind || (FoldingRangeKind = {}));
    (function(FoldingRange2) {
      function create(startLine, endLine, startCharacter, endCharacter, kind, collapsedText) {
        const result = {
          startLine,
          endLine
        };
        if (Is.defined(startCharacter)) {
          result.startCharacter = startCharacter;
        }
        if (Is.defined(endCharacter)) {
          result.endCharacter = endCharacter;
        }
        if (Is.defined(kind)) {
          result.kind = kind;
        }
        if (Is.defined(collapsedText)) {
          result.collapsedText = collapsedText;
        }
        return result;
      }
      __name(create, "create");
      FoldingRange2.create = create;
      function is(value) {
        const candidate = value;
        return Is.objectLiteral(candidate) && Is.uinteger(candidate.startLine) && Is.uinteger(candidate.startLine) && (Is.undefined(candidate.startCharacter) || Is.uinteger(candidate.startCharacter)) && (Is.undefined(candidate.endCharacter) || Is.uinteger(candidate.endCharacter)) && (Is.undefined(candidate.kind) || Is.string(candidate.kind));
      }
      __name(is, "is");
      FoldingRange2.is = is;
    })(FoldingRange || (FoldingRange = {}));
    (function(DiagnosticRelatedInformation2) {
      function create(location, message) {
        return {
          location,
          message
        };
      }
      __name(create, "create");
      DiagnosticRelatedInformation2.create = create;
      function is(value) {
        const candidate = value;
        return Is.defined(candidate) && Location.is(candidate.location) && Is.string(candidate.message);
      }
      __name(is, "is");
      DiagnosticRelatedInformation2.is = is;
    })(DiagnosticRelatedInformation || (DiagnosticRelatedInformation = {}));
    (function(DiagnosticSeverity2) {
      DiagnosticSeverity2.Error = 1;
      DiagnosticSeverity2.Warning = 2;
      DiagnosticSeverity2.Information = 3;
      DiagnosticSeverity2.Hint = 4;
    })(DiagnosticSeverity || (DiagnosticSeverity = {}));
    (function(DiagnosticTag2) {
      DiagnosticTag2.Unnecessary = 1;
      DiagnosticTag2.Deprecated = 2;
    })(DiagnosticTag || (DiagnosticTag = {}));
    (function(CodeDescription2) {
      function is(value) {
        const candidate = value;
        return Is.objectLiteral(candidate) && Is.string(candidate.href);
      }
      __name(is, "is");
      CodeDescription2.is = is;
    })(CodeDescription || (CodeDescription = {}));
    (function(Diagnostic2) {
      function create(range, message, severity, code, source, relatedInformation) {
        const result = { range, message };
        if (Is.defined(severity)) {
          result.severity = severity;
        }
        if (Is.defined(code)) {
          result.code = code;
        }
        if (Is.defined(source)) {
          result.source = source;
        }
        if (Is.defined(relatedInformation)) {
          result.relatedInformation = relatedInformation;
        }
        return result;
      }
      __name(create, "create");
      Diagnostic2.create = create;
      function is(value) {
        var _a;
        const candidate = value;
        return Is.defined(candidate) && Range.is(candidate.range) && (Is.string(candidate.message) || MarkupContent.is(candidate.message)) && (Is.number(candidate.severity) || Is.undefined(candidate.severity)) && (Is.integer(candidate.code) || Is.string(candidate.code) || Is.undefined(candidate.code)) && (Is.undefined(candidate.codeDescription) || Is.string((_a = candidate.codeDescription) === null || _a === void 0 ? void 0 : _a.href)) && (Is.string(candidate.source) || Is.undefined(candidate.source)) && (Is.undefined(candidate.relatedInformation) || Is.typedArray(candidate.relatedInformation, DiagnosticRelatedInformation.is));
      }
      __name(is, "is");
      Diagnostic2.is = is;
      function is3_17(value) {
        return Is.string(value.message);
      }
      __name(is3_17, "is3_17");
      Diagnostic2.is3_17 = is3_17;
      function getMessageString(diagnostic) {
        if (Is.string(diagnostic.message)) {
          return diagnostic.message;
        } else if (MarkupContent.is(diagnostic.message)) {
          return diagnostic.message.value;
        } else {
          throw new Error(`Unknown message type ${typeof diagnostic.message}`);
        }
      }
      __name(getMessageString, "getMessageString");
      Diagnostic2.getMessageString = getMessageString;
    })(Diagnostic || (Diagnostic = {}));
    (function(Command2) {
      function create(title, command, ...args) {
        const result = { title, command };
        if (Is.defined(args) && args.length > 0) {
          result.arguments = args;
        }
        return result;
      }
      __name(create, "create");
      Command2.create = create;
      function is(value) {
        const candidate = value;
        return Is.defined(candidate) && Is.string(candidate.title) && (candidate.tooltip === void 0 || Is.string(candidate.tooltip)) && Is.string(candidate.command);
      }
      __name(is, "is");
      Command2.is = is;
    })(Command || (Command = {}));
    (function(TextEdit2) {
      function replace(range, newText) {
        return { range, newText };
      }
      __name(replace, "replace");
      TextEdit2.replace = replace;
      function insert(position, newText) {
        return { range: { start: position, end: position }, newText };
      }
      __name(insert, "insert");
      TextEdit2.insert = insert;
      function del(range) {
        return { range, newText: "" };
      }
      __name(del, "del");
      TextEdit2.del = del;
      function is(value) {
        const candidate = value;
        return Is.objectLiteral(candidate) && Is.string(candidate.newText) && Range.is(candidate.range);
      }
      __name(is, "is");
      TextEdit2.is = is;
    })(TextEdit || (TextEdit = {}));
    (function(ChangeAnnotation2) {
      function create(label, needsConfirmation, description) {
        const result = { label };
        if (needsConfirmation !== void 0) {
          result.needsConfirmation = needsConfirmation;
        }
        if (description !== void 0) {
          result.description = description;
        }
        return result;
      }
      __name(create, "create");
      ChangeAnnotation2.create = create;
      function is(value) {
        const candidate = value;
        return Is.objectLiteral(candidate) && Is.string(candidate.label) && (Is.boolean(candidate.needsConfirmation) || candidate.needsConfirmation === void 0) && (Is.string(candidate.description) || candidate.description === void 0);
      }
      __name(is, "is");
      ChangeAnnotation2.is = is;
    })(ChangeAnnotation || (ChangeAnnotation = {}));
    (function(ChangeAnnotationIdentifier2) {
      function is(value) {
        const candidate = value;
        return Is.string(candidate);
      }
      __name(is, "is");
      ChangeAnnotationIdentifier2.is = is;
    })(ChangeAnnotationIdentifier || (ChangeAnnotationIdentifier = {}));
    (function(AnnotatedTextEdit2) {
      function replace(range, newText, annotation) {
        return { range, newText, annotationId: annotation };
      }
      __name(replace, "replace");
      AnnotatedTextEdit2.replace = replace;
      function insert(position, newText, annotation) {
        return { range: { start: position, end: position }, newText, annotationId: annotation };
      }
      __name(insert, "insert");
      AnnotatedTextEdit2.insert = insert;
      function del(range, annotation) {
        return { range, newText: "", annotationId: annotation };
      }
      __name(del, "del");
      AnnotatedTextEdit2.del = del;
      function is(value) {
        const candidate = value;
        return TextEdit.is(candidate) && (ChangeAnnotation.is(candidate.annotationId) || ChangeAnnotationIdentifier.is(candidate.annotationId));
      }
      __name(is, "is");
      AnnotatedTextEdit2.is = is;
    })(AnnotatedTextEdit || (AnnotatedTextEdit = {}));
    (function(TextDocumentEdit2) {
      function create(textDocument, edits) {
        return { textDocument, edits };
      }
      __name(create, "create");
      TextDocumentEdit2.create = create;
      function is(value) {
        const candidate = value;
        return Is.defined(candidate) && OptionalVersionedTextDocumentIdentifier.is(candidate.textDocument) && Array.isArray(candidate.edits);
      }
      __name(is, "is");
      TextDocumentEdit2.is = is;
    })(TextDocumentEdit || (TextDocumentEdit = {}));
    (function(CreateFile2) {
      function create(uri, options, annotation) {
        const result = {
          kind: "create",
          uri
        };
        if (options !== void 0 && (options.overwrite !== void 0 || options.ignoreIfExists !== void 0)) {
          result.options = options;
        }
        if (annotation !== void 0) {
          result.annotationId = annotation;
        }
        return result;
      }
      __name(create, "create");
      CreateFile2.create = create;
      function is(value) {
        const candidate = value;
        return candidate && candidate.kind === "create" && Is.string(candidate.uri) && (candidate.options === void 0 || (candidate.options.overwrite === void 0 || Is.boolean(candidate.options.overwrite)) && (candidate.options.ignoreIfExists === void 0 || Is.boolean(candidate.options.ignoreIfExists))) && (candidate.annotationId === void 0 || ChangeAnnotationIdentifier.is(candidate.annotationId));
      }
      __name(is, "is");
      CreateFile2.is = is;
    })(CreateFile || (CreateFile = {}));
    (function(RenameFile2) {
      function create(oldUri, newUri, options, annotation) {
        const result = {
          kind: "rename",
          oldUri,
          newUri
        };
        if (options !== void 0 && (options.overwrite !== void 0 || options.ignoreIfExists !== void 0)) {
          result.options = options;
        }
        if (annotation !== void 0) {
          result.annotationId = annotation;
        }
        return result;
      }
      __name(create, "create");
      RenameFile2.create = create;
      function is(value) {
        const candidate = value;
        return candidate && candidate.kind === "rename" && Is.string(candidate.oldUri) && Is.string(candidate.newUri) && (candidate.options === void 0 || (candidate.options.overwrite === void 0 || Is.boolean(candidate.options.overwrite)) && (candidate.options.ignoreIfExists === void 0 || Is.boolean(candidate.options.ignoreIfExists))) && (candidate.annotationId === void 0 || ChangeAnnotationIdentifier.is(candidate.annotationId));
      }
      __name(is, "is");
      RenameFile2.is = is;
    })(RenameFile || (RenameFile = {}));
    (function(DeleteFile2) {
      function create(uri, options, annotation) {
        const result = {
          kind: "delete",
          uri
        };
        if (options !== void 0 && (options.recursive !== void 0 || options.ignoreIfNotExists !== void 0)) {
          result.options = options;
        }
        if (annotation !== void 0) {
          result.annotationId = annotation;
        }
        return result;
      }
      __name(create, "create");
      DeleteFile2.create = create;
      function is(value) {
        const candidate = value;
        return candidate && candidate.kind === "delete" && Is.string(candidate.uri) && (candidate.options === void 0 || (candidate.options.recursive === void 0 || Is.boolean(candidate.options.recursive)) && (candidate.options.ignoreIfNotExists === void 0 || Is.boolean(candidate.options.ignoreIfNotExists))) && (candidate.annotationId === void 0 || ChangeAnnotationIdentifier.is(candidate.annotationId));
      }
      __name(is, "is");
      DeleteFile2.is = is;
    })(DeleteFile || (DeleteFile = {}));
    (function(WorkspaceEdit2) {
      function is(value) {
        const candidate = value;
        return candidate && (candidate.changes !== void 0 || candidate.documentChanges !== void 0) && (candidate.documentChanges === void 0 || candidate.documentChanges.every((change) => {
          if (Is.string(change.kind)) {
            return CreateFile.is(change) || RenameFile.is(change) || DeleteFile.is(change);
          } else {
            return TextDocumentEdit.is(change);
          }
        }));
      }
      __name(is, "is");
      WorkspaceEdit2.is = is;
    })(WorkspaceEdit || (WorkspaceEdit = {}));
    TextEditChangeImpl = class {
      static {
        __name(this, "TextEditChangeImpl");
      }
      constructor(edits, changeAnnotations) {
        this.edits = edits;
        this.changeAnnotations = changeAnnotations;
      }
      insert(position, newText, annotation) {
        let edit;
        let id;
        if (annotation === void 0) {
          edit = TextEdit.insert(position, newText);
        } else if (ChangeAnnotationIdentifier.is(annotation)) {
          id = annotation;
          edit = AnnotatedTextEdit.insert(position, newText, annotation);
        } else {
          this.assertChangeAnnotations(this.changeAnnotations);
          id = this.changeAnnotations.manage(annotation);
          edit = AnnotatedTextEdit.insert(position, newText, id);
        }
        this.edits.push(edit);
        if (id !== void 0) {
          return id;
        }
      }
      replace(range, newText, annotation) {
        let edit;
        let id;
        if (annotation === void 0) {
          edit = TextEdit.replace(range, newText);
        } else if (ChangeAnnotationIdentifier.is(annotation)) {
          id = annotation;
          edit = AnnotatedTextEdit.replace(range, newText, annotation);
        } else {
          this.assertChangeAnnotations(this.changeAnnotations);
          id = this.changeAnnotations.manage(annotation);
          edit = AnnotatedTextEdit.replace(range, newText, id);
        }
        this.edits.push(edit);
        if (id !== void 0) {
          return id;
        }
      }
      delete(range, annotation) {
        let edit;
        let id;
        if (annotation === void 0) {
          edit = TextEdit.del(range);
        } else if (ChangeAnnotationIdentifier.is(annotation)) {
          id = annotation;
          edit = AnnotatedTextEdit.del(range, annotation);
        } else {
          this.assertChangeAnnotations(this.changeAnnotations);
          id = this.changeAnnotations.manage(annotation);
          edit = AnnotatedTextEdit.del(range, id);
        }
        this.edits.push(edit);
        if (id !== void 0) {
          return id;
        }
      }
      add(edit) {
        this.edits.push(edit);
      }
      all() {
        return this.edits;
      }
      clear() {
        this.edits.splice(0, this.edits.length);
      }
      assertChangeAnnotations(value) {
        if (value === void 0) {
          throw new Error(`Text edit change is not configured to manage change annotations.`);
        }
      }
    };
    (function(SnippetTextEdit2) {
      function is(value) {
        const candidate = value;
        return Is.objectLiteral(candidate) && Range.is(candidate.range) && StringValue.isSnippet(candidate.snippet) && (candidate.annotationId === void 0 || (ChangeAnnotation.is(candidate.annotationId) || ChangeAnnotationIdentifier.is(candidate.annotationId)));
      }
      __name(is, "is");
      SnippetTextEdit2.is = is;
    })(SnippetTextEdit || (SnippetTextEdit = {}));
    ChangeAnnotations = class {
      static {
        __name(this, "ChangeAnnotations");
      }
      constructor(annotations) {
        this._annotations = annotations === void 0 ? /* @__PURE__ */ Object.create(null) : annotations;
        this._counter = 0;
        this._size = 0;
      }
      all() {
        return this._annotations;
      }
      get size() {
        return this._size;
      }
      manage(idOrAnnotation, annotation) {
        let id;
        if (ChangeAnnotationIdentifier.is(idOrAnnotation)) {
          id = idOrAnnotation;
        } else {
          id = this.nextId();
          annotation = idOrAnnotation;
        }
        if (this._annotations[id] !== void 0) {
          throw new Error(`Id ${id} is already in use.`);
        }
        if (annotation === void 0) {
          throw new Error(`No annotation provided for id ${id}`);
        }
        this._annotations[id] = annotation;
        this._size++;
        return id;
      }
      nextId() {
        this._counter++;
        return this._counter.toString();
      }
    };
    WorkspaceChange = class {
      static {
        __name(this, "WorkspaceChange");
      }
      constructor(workspaceEdit) {
        this._textEditChanges = /* @__PURE__ */ Object.create(null);
        if (workspaceEdit !== void 0) {
          this._workspaceEdit = workspaceEdit;
          if (workspaceEdit.documentChanges) {
            this._changeAnnotations = new ChangeAnnotations(workspaceEdit.changeAnnotations);
            workspaceEdit.changeAnnotations = this._changeAnnotations.all();
            workspaceEdit.documentChanges.forEach((change) => {
              if (TextDocumentEdit.is(change)) {
                const textEditChange = new TextEditChangeImpl(change.edits, this._changeAnnotations);
                this._textEditChanges[change.textDocument.uri] = textEditChange;
              }
            });
          } else if (workspaceEdit.changes) {
            Object.keys(workspaceEdit.changes).forEach((key) => {
              const textEditChange = new TextEditChangeImpl(workspaceEdit.changes[key]);
              this._textEditChanges[key] = textEditChange;
            });
          }
        } else {
          this._workspaceEdit = {};
        }
      }
      /**
       * Returns the underlying {@link WorkspaceEdit} literal
       * use to be returned from a workspace edit operation like rename.
       */
      get edit() {
        this.initDocumentChanges();
        if (this._changeAnnotations !== void 0) {
          if (this._changeAnnotations.size === 0) {
            this._workspaceEdit.changeAnnotations = void 0;
          } else {
            this._workspaceEdit.changeAnnotations = this._changeAnnotations.all();
          }
        }
        return this._workspaceEdit;
      }
      getTextEditChange(key) {
        if (OptionalVersionedTextDocumentIdentifier.is(key)) {
          this.initDocumentChanges();
          if (this._workspaceEdit.documentChanges === void 0) {
            throw new Error("Workspace edit is not configured for document changes.");
          }
          const textDocument = { uri: key.uri, version: key.version };
          let result = this._textEditChanges[textDocument.uri];
          if (!result) {
            const edits = [];
            const textDocumentEdit = {
              textDocument,
              edits
            };
            this._workspaceEdit.documentChanges.push(textDocumentEdit);
            result = new TextEditChangeImpl(edits, this._changeAnnotations);
            this._textEditChanges[textDocument.uri] = result;
          }
          return result;
        } else {
          this.initChanges();
          if (this._workspaceEdit.changes === void 0) {
            throw new Error("Workspace edit is not configured for normal text edit changes.");
          }
          let result = this._textEditChanges[key];
          if (!result) {
            const edits = [];
            this._workspaceEdit.changes[key] = edits;
            result = new TextEditChangeImpl(edits);
            this._textEditChanges[key] = result;
          }
          return result;
        }
      }
      initDocumentChanges() {
        if (this._workspaceEdit.documentChanges === void 0 && this._workspaceEdit.changes === void 0) {
          this._changeAnnotations = new ChangeAnnotations();
          this._workspaceEdit.documentChanges = [];
          this._workspaceEdit.changeAnnotations = this._changeAnnotations.all();
        }
      }
      initChanges() {
        if (this._workspaceEdit.documentChanges === void 0 && this._workspaceEdit.changes === void 0) {
          this._workspaceEdit.changes = /* @__PURE__ */ Object.create(null);
        }
      }
      createFile(uri, optionsOrAnnotation, options) {
        this.initDocumentChanges();
        if (this._workspaceEdit.documentChanges === void 0) {
          throw new Error("Workspace edit is not configured for document changes.");
        }
        let annotation;
        if (ChangeAnnotation.is(optionsOrAnnotation) || ChangeAnnotationIdentifier.is(optionsOrAnnotation)) {
          annotation = optionsOrAnnotation;
        } else {
          options = optionsOrAnnotation;
        }
        let operation;
        let id;
        if (annotation === void 0) {
          operation = CreateFile.create(uri, options);
        } else {
          id = ChangeAnnotationIdentifier.is(annotation) ? annotation : this._changeAnnotations.manage(annotation);
          operation = CreateFile.create(uri, options, id);
        }
        this._workspaceEdit.documentChanges.push(operation);
        if (id !== void 0) {
          return id;
        }
      }
      renameFile(oldUri, newUri, optionsOrAnnotation, options) {
        this.initDocumentChanges();
        if (this._workspaceEdit.documentChanges === void 0) {
          throw new Error("Workspace edit is not configured for document changes.");
        }
        let annotation;
        if (ChangeAnnotation.is(optionsOrAnnotation) || ChangeAnnotationIdentifier.is(optionsOrAnnotation)) {
          annotation = optionsOrAnnotation;
        } else {
          options = optionsOrAnnotation;
        }
        let operation;
        let id;
        if (annotation === void 0) {
          operation = RenameFile.create(oldUri, newUri, options);
        } else {
          id = ChangeAnnotationIdentifier.is(annotation) ? annotation : this._changeAnnotations.manage(annotation);
          operation = RenameFile.create(oldUri, newUri, options, id);
        }
        this._workspaceEdit.documentChanges.push(operation);
        if (id !== void 0) {
          return id;
        }
      }
      deleteFile(uri, optionsOrAnnotation, options) {
        this.initDocumentChanges();
        if (this._workspaceEdit.documentChanges === void 0) {
          throw new Error("Workspace edit is not configured for document changes.");
        }
        let annotation;
        if (ChangeAnnotation.is(optionsOrAnnotation) || ChangeAnnotationIdentifier.is(optionsOrAnnotation)) {
          annotation = optionsOrAnnotation;
        } else {
          options = optionsOrAnnotation;
        }
        let operation;
        let id;
        if (annotation === void 0) {
          operation = DeleteFile.create(uri, options);
        } else {
          id = ChangeAnnotationIdentifier.is(annotation) ? annotation : this._changeAnnotations.manage(annotation);
          operation = DeleteFile.create(uri, options, id);
        }
        this._workspaceEdit.documentChanges.push(operation);
        if (id !== void 0) {
          return id;
        }
      }
    };
    (function(TextDocumentIdentifier2) {
      function create(uri) {
        return { uri };
      }
      __name(create, "create");
      TextDocumentIdentifier2.create = create;
      function is(value) {
        const candidate = value;
        return Is.defined(candidate) && Is.string(candidate.uri);
      }
      __name(is, "is");
      TextDocumentIdentifier2.is = is;
    })(TextDocumentIdentifier || (TextDocumentIdentifier = {}));
    (function(VersionedTextDocumentIdentifier2) {
      function create(uri, version) {
        return { uri, version };
      }
      __name(create, "create");
      VersionedTextDocumentIdentifier2.create = create;
      function is(value) {
        const candidate = value;
        return Is.defined(candidate) && Is.string(candidate.uri) && Is.integer(candidate.version);
      }
      __name(is, "is");
      VersionedTextDocumentIdentifier2.is = is;
    })(VersionedTextDocumentIdentifier || (VersionedTextDocumentIdentifier = {}));
    (function(OptionalVersionedTextDocumentIdentifier2) {
      function create(uri, version) {
        return { uri, version };
      }
      __name(create, "create");
      OptionalVersionedTextDocumentIdentifier2.create = create;
      function is(value) {
        const candidate = value;
        return Is.defined(candidate) && Is.string(candidate.uri) && (candidate.version === null || Is.integer(candidate.version));
      }
      __name(is, "is");
      OptionalVersionedTextDocumentIdentifier2.is = is;
    })(OptionalVersionedTextDocumentIdentifier || (OptionalVersionedTextDocumentIdentifier = {}));
    (function(LanguageKind2) {
      LanguageKind2.ABAP = "abap";
      LanguageKind2.WindowsBat = "bat";
      LanguageKind2.BibTeX = "bibtex";
      LanguageKind2.Clojure = "clojure";
      LanguageKind2.Coffeescript = "coffeescript";
      LanguageKind2.C = "c";
      LanguageKind2.CPP = "cpp";
      LanguageKind2.CSharp = "csharp";
      LanguageKind2.CSS = "css";
      LanguageKind2.D = "d";
      LanguageKind2.Delphi = "pascal";
      LanguageKind2.Diff = "diff";
      LanguageKind2.Dart = "dart";
      LanguageKind2.Dockerfile = "dockerfile";
      LanguageKind2.Elixir = "elixir";
      LanguageKind2.Erlang = "erlang";
      LanguageKind2.FSharp = "fsharp";
      LanguageKind2.GitCommit = "git-commit";
      LanguageKind2.GitRebase = "git-rebase";
      LanguageKind2.Go = "go";
      LanguageKind2.Groovy = "groovy";
      LanguageKind2.Handlebars = "handlebars";
      LanguageKind2.Haskell = "haskell";
      LanguageKind2.HTML = "html";
      LanguageKind2.Ini = "ini";
      LanguageKind2.Java = "java";
      LanguageKind2.JavaScript = "javascript";
      LanguageKind2.JavaScriptReact = "javascriptreact";
      LanguageKind2.JSON = "json";
      LanguageKind2.LaTeX = "latex";
      LanguageKind2.Less = "less";
      LanguageKind2.Lua = "lua";
      LanguageKind2.Makefile = "makefile";
      LanguageKind2.Markdown = "markdown";
      LanguageKind2.ObjectiveC = "objective-c";
      LanguageKind2.ObjectiveCPP = "objective-cpp";
      LanguageKind2.Pascal = "pascal";
      LanguageKind2.Perl = "perl";
      LanguageKind2.Perl6 = "perl6";
      LanguageKind2.PHP = "php";
      LanguageKind2.Plaintext = "plaintext";
      LanguageKind2.Powershell = "powershell";
      LanguageKind2.Pug = "jade";
      LanguageKind2.Python = "python";
      LanguageKind2.R = "r";
      LanguageKind2.Razor = "razor";
      LanguageKind2.Ruby = "ruby";
      LanguageKind2.Rust = "rust";
      LanguageKind2.SCSS = "scss";
      LanguageKind2.SASS = "sass";
      LanguageKind2.Scala = "scala";
      LanguageKind2.ShaderLab = "shaderlab";
      LanguageKind2.ShellScript = "shellscript";
      LanguageKind2.SQL = "sql";
      LanguageKind2.Swift = "swift";
      LanguageKind2.TypeScript = "typescript";
      LanguageKind2.TypeScriptReact = "typescriptreact";
      LanguageKind2.TeX = "tex";
      LanguageKind2.VisualBasic = "vb";
      LanguageKind2.XML = "xml";
      LanguageKind2.XSL = "xsl";
      LanguageKind2.YAML = "yaml";
    })(LanguageKind || (LanguageKind = {}));
    (function(TextDocumentItem2) {
      function create(uri, languageId, version, text) {
        return { uri, languageId, version, text };
      }
      __name(create, "create");
      TextDocumentItem2.create = create;
      function is(value) {
        const candidate = value;
        return Is.defined(candidate) && Is.string(candidate.uri) && Is.string(candidate.languageId) && Is.integer(candidate.version) && Is.string(candidate.text);
      }
      __name(is, "is");
      TextDocumentItem2.is = is;
    })(TextDocumentItem || (TextDocumentItem = {}));
    (function(MarkupKind2) {
      MarkupKind2.PlainText = "plaintext";
      MarkupKind2.Markdown = "markdown";
      function is(value) {
        const candidate = value;
        return candidate === MarkupKind2.PlainText || candidate === MarkupKind2.Markdown;
      }
      __name(is, "is");
      MarkupKind2.is = is;
    })(MarkupKind || (MarkupKind = {}));
    (function(MarkupContent2) {
      function is(value) {
        const candidate = value;
        return Is.objectLiteral(value) && MarkupKind.is(candidate.kind) && Is.string(candidate.value);
      }
      __name(is, "is");
      MarkupContent2.is = is;
    })(MarkupContent || (MarkupContent = {}));
    (function(CompletionItemKind2) {
      CompletionItemKind2.Text = 1;
      CompletionItemKind2.Method = 2;
      CompletionItemKind2.Function = 3;
      CompletionItemKind2.Constructor = 4;
      CompletionItemKind2.Field = 5;
      CompletionItemKind2.Variable = 6;
      CompletionItemKind2.Class = 7;
      CompletionItemKind2.Interface = 8;
      CompletionItemKind2.Module = 9;
      CompletionItemKind2.Property = 10;
      CompletionItemKind2.Unit = 11;
      CompletionItemKind2.Value = 12;
      CompletionItemKind2.Enum = 13;
      CompletionItemKind2.Keyword = 14;
      CompletionItemKind2.Snippet = 15;
      CompletionItemKind2.Color = 16;
      CompletionItemKind2.File = 17;
      CompletionItemKind2.Reference = 18;
      CompletionItemKind2.Folder = 19;
      CompletionItemKind2.EnumMember = 20;
      CompletionItemKind2.Constant = 21;
      CompletionItemKind2.Struct = 22;
      CompletionItemKind2.Event = 23;
      CompletionItemKind2.Operator = 24;
      CompletionItemKind2.TypeParameter = 25;
    })(CompletionItemKind || (CompletionItemKind = {}));
    (function(InsertTextFormat2) {
      InsertTextFormat2.PlainText = 1;
      InsertTextFormat2.Snippet = 2;
    })(InsertTextFormat || (InsertTextFormat = {}));
    (function(CompletionItemTag2) {
      CompletionItemTag2.Deprecated = 1;
    })(CompletionItemTag || (CompletionItemTag = {}));
    (function(InsertReplaceEdit2) {
      function create(newText, insert, replace) {
        return { newText, insert, replace };
      }
      __name(create, "create");
      InsertReplaceEdit2.create = create;
      function is(value) {
        const candidate = value;
        return candidate && Is.string(candidate.newText) && Range.is(candidate.insert) && Range.is(candidate.replace);
      }
      __name(is, "is");
      InsertReplaceEdit2.is = is;
    })(InsertReplaceEdit || (InsertReplaceEdit = {}));
    (function(InsertTextMode2) {
      InsertTextMode2.asIs = 1;
      InsertTextMode2.adjustIndentation = 2;
    })(InsertTextMode || (InsertTextMode = {}));
    (function(ApplyKind2) {
      ApplyKind2.Replace = 1;
      ApplyKind2.Merge = 2;
    })(ApplyKind || (ApplyKind = {}));
    (function(CompletionItemLabelDetails2) {
      function is(value) {
        const candidate = value;
        return candidate && (Is.string(candidate.detail) || candidate.detail === void 0) && (Is.string(candidate.description) || candidate.description === void 0);
      }
      __name(is, "is");
      CompletionItemLabelDetails2.is = is;
    })(CompletionItemLabelDetails || (CompletionItemLabelDetails = {}));
    (function(CompletionItem2) {
      function create(label) {
        return { label };
      }
      __name(create, "create");
      CompletionItem2.create = create;
    })(CompletionItem || (CompletionItem = {}));
    (function(CompletionList2) {
      function create(items, isIncomplete) {
        return { items: items ? items : [], isIncomplete: !!isIncomplete };
      }
      __name(create, "create");
      CompletionList2.create = create;
    })(CompletionList || (CompletionList = {}));
    (function(MarkedString2) {
      function fromPlainText(plainText) {
        return plainText.replace(/[\\`*_{}[\]()#+\-.!]/g, "\\$&");
      }
      __name(fromPlainText, "fromPlainText");
      MarkedString2.fromPlainText = fromPlainText;
      function is(value) {
        const candidate = value;
        return Is.string(candidate) || Is.objectLiteral(candidate) && Is.string(candidate.language) && Is.string(candidate.value);
      }
      __name(is, "is");
      MarkedString2.is = is;
    })(MarkedString || (MarkedString = {}));
    (function(Hover2) {
      function is(value) {
        const candidate = value;
        return !!candidate && Is.objectLiteral(candidate) && (MarkupContent.is(candidate.contents) || MarkedString.is(candidate.contents) || Is.typedArray(candidate.contents, MarkedString.is)) && (value.range === void 0 || Range.is(value.range));
      }
      __name(is, "is");
      Hover2.is = is;
    })(Hover || (Hover = {}));
    (function(ParameterInformation2) {
      function create(label, documentation) {
        return documentation ? { label, documentation } : { label };
      }
      __name(create, "create");
      ParameterInformation2.create = create;
    })(ParameterInformation || (ParameterInformation = {}));
    (function(SignatureInformation2) {
      function create(label, documentation, ...parameters) {
        const result = { label };
        if (Is.defined(documentation)) {
          result.documentation = documentation;
        }
        if (Is.defined(parameters)) {
          result.parameters = parameters;
        } else {
          result.parameters = [];
        }
        return result;
      }
      __name(create, "create");
      SignatureInformation2.create = create;
    })(SignatureInformation || (SignatureInformation = {}));
    (function(DocumentHighlightKind2) {
      DocumentHighlightKind2.Text = 1;
      DocumentHighlightKind2.Read = 2;
      DocumentHighlightKind2.Write = 3;
    })(DocumentHighlightKind || (DocumentHighlightKind = {}));
    (function(DocumentHighlight2) {
      function create(range, kind) {
        const result = { range };
        if (Is.number(kind)) {
          result.kind = kind;
        }
        return result;
      }
      __name(create, "create");
      DocumentHighlight2.create = create;
    })(DocumentHighlight || (DocumentHighlight = {}));
    (function(SymbolKind2) {
      SymbolKind2.File = 1;
      SymbolKind2.Module = 2;
      SymbolKind2.Namespace = 3;
      SymbolKind2.Package = 4;
      SymbolKind2.Class = 5;
      SymbolKind2.Method = 6;
      SymbolKind2.Property = 7;
      SymbolKind2.Field = 8;
      SymbolKind2.Constructor = 9;
      SymbolKind2.Enum = 10;
      SymbolKind2.Interface = 11;
      SymbolKind2.Function = 12;
      SymbolKind2.Variable = 13;
      SymbolKind2.Constant = 14;
      SymbolKind2.String = 15;
      SymbolKind2.Number = 16;
      SymbolKind2.Boolean = 17;
      SymbolKind2.Array = 18;
      SymbolKind2.Object = 19;
      SymbolKind2.Key = 20;
      SymbolKind2.Null = 21;
      SymbolKind2.EnumMember = 22;
      SymbolKind2.Struct = 23;
      SymbolKind2.Event = 24;
      SymbolKind2.Operator = 25;
      SymbolKind2.TypeParameter = 26;
    })(SymbolKind || (SymbolKind = {}));
    (function(SymbolTag2) {
      SymbolTag2.Deprecated = 1;
    })(SymbolTag || (SymbolTag = {}));
    (function(SymbolInformation2) {
      function create(name, kind, range, uri, containerName) {
        const result = {
          name,
          kind,
          location: { uri, range }
        };
        if (containerName) {
          result.containerName = containerName;
        }
        return result;
      }
      __name(create, "create");
      SymbolInformation2.create = create;
    })(SymbolInformation || (SymbolInformation = {}));
    (function(WorkspaceSymbol2) {
      function create(name, kind, uri, range) {
        return range !== void 0 ? { name, kind, location: { uri, range } } : { name, kind, location: { uri } };
      }
      __name(create, "create");
      WorkspaceSymbol2.create = create;
    })(WorkspaceSymbol || (WorkspaceSymbol = {}));
    (function(DocumentSymbol2) {
      function create(name, detail, kind, range, selectionRange, children) {
        const result = {
          name,
          detail,
          kind,
          range,
          selectionRange
        };
        if (children !== void 0) {
          result.children = children;
        }
        return result;
      }
      __name(create, "create");
      DocumentSymbol2.create = create;
      function is(value) {
        const candidate = value;
        return candidate && Is.string(candidate.name) && Is.number(candidate.kind) && Range.is(candidate.range) && Range.is(candidate.selectionRange) && (candidate.detail === void 0 || Is.string(candidate.detail)) && (candidate.deprecated === void 0 || Is.boolean(candidate.deprecated)) && (candidate.children === void 0 || Array.isArray(candidate.children)) && (candidate.tags === void 0 || Array.isArray(candidate.tags));
      }
      __name(is, "is");
      DocumentSymbol2.is = is;
    })(DocumentSymbol || (DocumentSymbol = {}));
    (function(CodeActionKind2) {
      CodeActionKind2.Empty = "";
      CodeActionKind2.QuickFix = "quickfix";
      CodeActionKind2.Refactor = "refactor";
      CodeActionKind2.RefactorExtract = "refactor.extract";
      CodeActionKind2.RefactorInline = "refactor.inline";
      CodeActionKind2.RefactorMove = "refactor.move";
      CodeActionKind2.RefactorRewrite = "refactor.rewrite";
      CodeActionKind2.Source = "source";
      CodeActionKind2.SourceOrganizeImports = "source.organizeImports";
      CodeActionKind2.SourceFixAll = "source.fixAll";
      CodeActionKind2.Notebook = "notebook";
    })(CodeActionKind || (CodeActionKind = {}));
    (function(CodeActionTriggerKind2) {
      CodeActionTriggerKind2.Invoked = 1;
      CodeActionTriggerKind2.Automatic = 2;
    })(CodeActionTriggerKind || (CodeActionTriggerKind = {}));
    (function(CodeActionContext2) {
      function create(diagnostics, only, triggerKind) {
        const result = { diagnostics };
        if (only !== void 0 && only !== null) {
          result.only = only;
        }
        if (triggerKind !== void 0 && triggerKind !== null) {
          result.triggerKind = triggerKind;
        }
        return result;
      }
      __name(create, "create");
      CodeActionContext2.create = create;
      function is(value) {
        const candidate = value;
        return Is.defined(candidate) && Is.typedArray(candidate.diagnostics, Diagnostic.is) && (candidate.only === void 0 || Is.typedArray(candidate.only, Is.string)) && (candidate.triggerKind === void 0 || candidate.triggerKind === CodeActionTriggerKind.Invoked || candidate.triggerKind === CodeActionTriggerKind.Automatic);
      }
      __name(is, "is");
      CodeActionContext2.is = is;
    })(CodeActionContext || (CodeActionContext = {}));
    (function(CodeActionTag2) {
      CodeActionTag2.LLMGenerated = 1;
      function is(value) {
        return Is.defined(value) && value === CodeActionTag2.LLMGenerated;
      }
      __name(is, "is");
      CodeActionTag2.is = is;
    })(CodeActionTag || (CodeActionTag = {}));
    (function(CodeAction2) {
      function create(title, kindOrCommandOrEdit, kind) {
        const result = { title };
        let checkKind = true;
        if (typeof kindOrCommandOrEdit === "string") {
          checkKind = false;
          result.kind = kindOrCommandOrEdit;
        } else if (Command.is(kindOrCommandOrEdit)) {
          result.command = kindOrCommandOrEdit;
        } else {
          result.edit = kindOrCommandOrEdit;
        }
        if (checkKind && kind !== void 0) {
          result.kind = kind;
        }
        return result;
      }
      __name(create, "create");
      CodeAction2.create = create;
      function is(value) {
        const candidate = value;
        return candidate && Is.string(candidate.title) && (candidate.diagnostics === void 0 || Is.typedArray(candidate.diagnostics, Diagnostic.is)) && (candidate.kind === void 0 || Is.string(candidate.kind)) && (candidate.edit !== void 0 || candidate.command !== void 0) && (candidate.command === void 0 || Command.is(candidate.command)) && (candidate.isPreferred === void 0 || Is.boolean(candidate.isPreferred)) && (candidate.edit === void 0 || WorkspaceEdit.is(candidate.edit)) && (candidate.tags === void 0 || Is.typedArray(candidate.tags, CodeActionTag.is));
      }
      __name(is, "is");
      CodeAction2.is = is;
    })(CodeAction || (CodeAction = {}));
    (function(CodeLens2) {
      function create(range, data) {
        const result = { range };
        if (Is.defined(data)) {
          result.data = data;
        }
        return result;
      }
      __name(create, "create");
      CodeLens2.create = create;
      function is(value) {
        const candidate = value;
        return Is.defined(candidate) && Range.is(candidate.range) && (Is.undefined(candidate.command) || Command.is(candidate.command));
      }
      __name(is, "is");
      CodeLens2.is = is;
    })(CodeLens || (CodeLens = {}));
    (function(FormattingOptions2) {
      function create(tabSize, insertSpaces) {
        return { tabSize, insertSpaces };
      }
      __name(create, "create");
      FormattingOptions2.create = create;
      function is(value) {
        const candidate = value;
        return Is.defined(candidate) && Is.uinteger(candidate.tabSize) && Is.boolean(candidate.insertSpaces);
      }
      __name(is, "is");
      FormattingOptions2.is = is;
    })(FormattingOptions || (FormattingOptions = {}));
    (function(DocumentLink2) {
      function create(range, target, data) {
        return { range, target, data };
      }
      __name(create, "create");
      DocumentLink2.create = create;
      function is(value) {
        const candidate = value;
        return Is.defined(candidate) && Range.is(candidate.range) && (Is.undefined(candidate.target) || Is.string(candidate.target));
      }
      __name(is, "is");
      DocumentLink2.is = is;
    })(DocumentLink || (DocumentLink = {}));
    (function(SelectionRange2) {
      function create(range, parent) {
        return { range, parent };
      }
      __name(create, "create");
      SelectionRange2.create = create;
      function is(value) {
        const candidate = value;
        return Is.objectLiteral(candidate) && Range.is(candidate.range) && (candidate.parent === void 0 || SelectionRange2.is(candidate.parent));
      }
      __name(is, "is");
      SelectionRange2.is = is;
    })(SelectionRange || (SelectionRange = {}));
    (function(SemanticTokenTypes2) {
      SemanticTokenTypes2["namespace"] = "namespace";
      SemanticTokenTypes2["type"] = "type";
      SemanticTokenTypes2["class"] = "class";
      SemanticTokenTypes2["enum"] = "enum";
      SemanticTokenTypes2["interface"] = "interface";
      SemanticTokenTypes2["struct"] = "struct";
      SemanticTokenTypes2["typeParameter"] = "typeParameter";
      SemanticTokenTypes2["parameter"] = "parameter";
      SemanticTokenTypes2["variable"] = "variable";
      SemanticTokenTypes2["property"] = "property";
      SemanticTokenTypes2["enumMember"] = "enumMember";
      SemanticTokenTypes2["event"] = "event";
      SemanticTokenTypes2["function"] = "function";
      SemanticTokenTypes2["method"] = "method";
      SemanticTokenTypes2["macro"] = "macro";
      SemanticTokenTypes2["keyword"] = "keyword";
      SemanticTokenTypes2["modifier"] = "modifier";
      SemanticTokenTypes2["comment"] = "comment";
      SemanticTokenTypes2["string"] = "string";
      SemanticTokenTypes2["number"] = "number";
      SemanticTokenTypes2["regexp"] = "regexp";
      SemanticTokenTypes2["operator"] = "operator";
      SemanticTokenTypes2["decorator"] = "decorator";
      SemanticTokenTypes2["label"] = "label";
    })(SemanticTokenTypes || (SemanticTokenTypes = {}));
    (function(SemanticTokenModifiers2) {
      SemanticTokenModifiers2["declaration"] = "declaration";
      SemanticTokenModifiers2["definition"] = "definition";
      SemanticTokenModifiers2["readonly"] = "readonly";
      SemanticTokenModifiers2["static"] = "static";
      SemanticTokenModifiers2["deprecated"] = "deprecated";
      SemanticTokenModifiers2["abstract"] = "abstract";
      SemanticTokenModifiers2["async"] = "async";
      SemanticTokenModifiers2["modification"] = "modification";
      SemanticTokenModifiers2["documentation"] = "documentation";
      SemanticTokenModifiers2["defaultLibrary"] = "defaultLibrary";
    })(SemanticTokenModifiers || (SemanticTokenModifiers = {}));
    (function(SemanticTokens2) {
      function is(value) {
        const candidate = value;
        return Is.objectLiteral(candidate) && (candidate.resultId === void 0 || typeof candidate.resultId === "string") && Array.isArray(candidate.data) && (candidate.data.length === 0 || typeof candidate.data[0] === "number");
      }
      __name(is, "is");
      SemanticTokens2.is = is;
    })(SemanticTokens || (SemanticTokens = {}));
    (function(InlineValueText2) {
      function create(range, text) {
        return { range, text };
      }
      __name(create, "create");
      InlineValueText2.create = create;
      function is(value) {
        const candidate = value;
        return candidate !== void 0 && candidate !== null && Range.is(candidate.range) && Is.string(candidate.text);
      }
      __name(is, "is");
      InlineValueText2.is = is;
    })(InlineValueText || (InlineValueText = {}));
    (function(InlineValueVariableLookup2) {
      function create(range, variableName, caseSensitiveLookup) {
        return { range, variableName, caseSensitiveLookup };
      }
      __name(create, "create");
      InlineValueVariableLookup2.create = create;
      function is(value) {
        const candidate = value;
        return candidate !== void 0 && candidate !== null && Range.is(candidate.range) && Is.boolean(candidate.caseSensitiveLookup) && (Is.string(candidate.variableName) || candidate.variableName === void 0);
      }
      __name(is, "is");
      InlineValueVariableLookup2.is = is;
    })(InlineValueVariableLookup || (InlineValueVariableLookup = {}));
    (function(InlineValueEvaluatableExpression2) {
      function create(range, expression) {
        return { range, expression };
      }
      __name(create, "create");
      InlineValueEvaluatableExpression2.create = create;
      function is(value) {
        const candidate = value;
        return candidate !== void 0 && candidate !== null && Range.is(candidate.range) && (Is.string(candidate.expression) || candidate.expression === void 0);
      }
      __name(is, "is");
      InlineValueEvaluatableExpression2.is = is;
    })(InlineValueEvaluatableExpression || (InlineValueEvaluatableExpression = {}));
    (function(InlineValueContext2) {
      function create(frameId, stoppedLocation) {
        return { frameId, stoppedLocation };
      }
      __name(create, "create");
      InlineValueContext2.create = create;
      function is(value) {
        const candidate = value;
        return Is.defined(candidate) && Range.is(value.stoppedLocation);
      }
      __name(is, "is");
      InlineValueContext2.is = is;
    })(InlineValueContext || (InlineValueContext = {}));
    (function(InlayHintKind2) {
      InlayHintKind2.Type = 1;
      InlayHintKind2.Parameter = 2;
      function is(value) {
        return value === 1 || value === 2;
      }
      __name(is, "is");
      InlayHintKind2.is = is;
    })(InlayHintKind || (InlayHintKind = {}));
    (function(InlayHintLabelPart2) {
      function create(value) {
        return { value };
      }
      __name(create, "create");
      InlayHintLabelPart2.create = create;
      function is(value) {
        const candidate = value;
        return Is.objectLiteral(candidate) && (candidate.tooltip === void 0 || Is.string(candidate.tooltip) || MarkupContent.is(candidate.tooltip)) && (candidate.location === void 0 || Location.is(candidate.location)) && (candidate.command === void 0 || Command.is(candidate.command));
      }
      __name(is, "is");
      InlayHintLabelPart2.is = is;
    })(InlayHintLabelPart || (InlayHintLabelPart = {}));
    (function(InlayHint2) {
      function create(position, label, kind) {
        const result = { position, label };
        if (kind !== void 0) {
          result.kind = kind;
        }
        return result;
      }
      __name(create, "create");
      InlayHint2.create = create;
      function is(value) {
        const candidate = value;
        return Is.objectLiteral(candidate) && Position.is(candidate.position) && (Is.string(candidate.label) || Is.typedArray(candidate.label, InlayHintLabelPart.is)) && (candidate.kind === void 0 || InlayHintKind.is(candidate.kind)) && candidate.textEdits === void 0 || Is.typedArray(candidate.textEdits, TextEdit.is) && (candidate.tooltip === void 0 || Is.string(candidate.tooltip) || MarkupContent.is(candidate.tooltip)) && (candidate.paddingLeft === void 0 || Is.boolean(candidate.paddingLeft)) && (candidate.paddingRight === void 0 || Is.boolean(candidate.paddingRight));
      }
      __name(is, "is");
      InlayHint2.is = is;
    })(InlayHint || (InlayHint = {}));
    (function(StringValue2) {
      function createSnippet(value) {
        return { kind: "snippet", value };
      }
      __name(createSnippet, "createSnippet");
      StringValue2.createSnippet = createSnippet;
      function isSnippet(value) {
        const candidate = value;
        return Is.objectLiteral(candidate) && candidate.kind === "snippet" && Is.string(candidate.value);
      }
      __name(isSnippet, "isSnippet");
      StringValue2.isSnippet = isSnippet;
    })(StringValue || (StringValue = {}));
    (function(InlineCompletionItem2) {
      function create(insertText, filterText, range, command) {
        return { insertText, filterText, range, command };
      }
      __name(create, "create");
      InlineCompletionItem2.create = create;
    })(InlineCompletionItem || (InlineCompletionItem = {}));
    (function(InlineCompletionList2) {
      function create(items) {
        return { items };
      }
      __name(create, "create");
      InlineCompletionList2.create = create;
    })(InlineCompletionList || (InlineCompletionList = {}));
    (function(InlineCompletionTriggerKind2) {
      InlineCompletionTriggerKind2.Invoked = 1;
      InlineCompletionTriggerKind2.Automatic = 2;
    })(InlineCompletionTriggerKind || (InlineCompletionTriggerKind = {}));
    (function(SelectedCompletionInfo2) {
      function create(range, text) {
        return { range, text };
      }
      __name(create, "create");
      SelectedCompletionInfo2.create = create;
    })(SelectedCompletionInfo || (SelectedCompletionInfo = {}));
    (function(InlineCompletionContext2) {
      function create(triggerKind, selectedCompletionInfo) {
        return { triggerKind, selectedCompletionInfo };
      }
      __name(create, "create");
      InlineCompletionContext2.create = create;
    })(InlineCompletionContext || (InlineCompletionContext = {}));
    (function(WorkspaceFolder2) {
      function is(value) {
        const candidate = value;
        return Is.objectLiteral(candidate) && URI.is(candidate.uri) && Is.string(candidate.name);
      }
      __name(is, "is");
      WorkspaceFolder2.is = is;
    })(WorkspaceFolder || (WorkspaceFolder = {}));
    EOL = ["\n", "\r\n", "\r"];
    (function(TextDocument3) {
      function create(uri, languageId, version, content) {
        return new FullTextDocument(uri, languageId, version, content);
      }
      __name(create, "create");
      TextDocument3.create = create;
      function is(value) {
        const candidate = value;
        return Is.defined(candidate) && Is.string(candidate.uri) && (Is.undefined(candidate.languageId) || Is.string(candidate.languageId)) && Is.uinteger(candidate.lineCount) && Is.func(candidate.getText) && Is.func(candidate.positionAt) && Is.func(candidate.offsetAt) ? true : false;
      }
      __name(is, "is");
      TextDocument3.is = is;
      function applyEdits(document, edits) {
        let text = document.getText();
        const sortedEdits = mergeSort2(edits, (a, b) => {
          const diff = a.range.start.line - b.range.start.line;
          if (diff === 0) {
            return a.range.start.character - b.range.start.character;
          }
          return diff;
        });
        let lastModifiedOffset = text.length;
        for (let i = sortedEdits.length - 1; i >= 0; i--) {
          const e = sortedEdits[i];
          const startOffset = document.offsetAt(e.range.start);
          const endOffset = document.offsetAt(e.range.end);
          if (endOffset <= lastModifiedOffset) {
            text = text.substring(0, startOffset) + e.newText + text.substring(endOffset, text.length);
          } else {
            throw new Error("Overlapping edit");
          }
          lastModifiedOffset = startOffset;
        }
        return text;
      }
      __name(applyEdits, "applyEdits");
      TextDocument3.applyEdits = applyEdits;
      function mergeSort2(data, compare) {
        if (data.length <= 1) {
          return data;
        }
        const p = data.length / 2 | 0;
        const left = data.slice(0, p);
        const right = data.slice(p);
        mergeSort2(left, compare);
        mergeSort2(right, compare);
        let leftIdx = 0;
        let rightIdx = 0;
        let i = 0;
        while (leftIdx < left.length && rightIdx < right.length) {
          const ret = compare(left[leftIdx], right[rightIdx]);
          if (ret <= 0) {
            data[i++] = left[leftIdx++];
          } else {
            data[i++] = right[rightIdx++];
          }
        }
        while (leftIdx < left.length) {
          data[i++] = left[leftIdx++];
        }
        while (rightIdx < right.length) {
          data[i++] = right[rightIdx++];
        }
        return data;
      }
      __name(mergeSort2, "mergeSort");
    })(TextDocument || (TextDocument = {}));
    FullTextDocument = class {
      static {
        __name(this, "FullTextDocument");
      }
      constructor(uri, languageId, version, content) {
        this._uri = uri;
        this._languageId = languageId;
        this._version = version;
        this._content = content;
        this._lineOffsets = void 0;
      }
      get uri() {
        return this._uri;
      }
      get languageId() {
        return this._languageId;
      }
      get version() {
        return this._version;
      }
      getText(range) {
        if (range) {
          const start = this.offsetAt(range.start);
          const end = this.offsetAt(range.end);
          return this._content.substring(start, end);
        }
        return this._content;
      }
      update(event, version) {
        this._content = event.text;
        this._version = version;
        this._lineOffsets = void 0;
      }
      getLineOffsets() {
        if (this._lineOffsets === void 0) {
          const lineOffsets = [];
          const text = this._content;
          let isLineStart = true;
          for (let i = 0; i < text.length; i++) {
            if (isLineStart) {
              lineOffsets.push(i);
              isLineStart = false;
            }
            const ch = text.charAt(i);
            isLineStart = ch === "\r" || ch === "\n";
            if (ch === "\r" && i + 1 < text.length && text.charAt(i + 1) === "\n") {
              i++;
            }
          }
          if (isLineStart && text.length > 0) {
            lineOffsets.push(text.length);
          }
          this._lineOffsets = lineOffsets;
        }
        return this._lineOffsets;
      }
      positionAt(offset) {
        offset = Math.max(Math.min(offset, this._content.length), 0);
        const lineOffsets = this.getLineOffsets();
        let low = 0, high = lineOffsets.length;
        if (high === 0) {
          return Position.create(0, offset);
        }
        while (low < high) {
          const mid = Math.floor((low + high) / 2);
          if (lineOffsets[mid] > offset) {
            high = mid;
          } else {
            low = mid + 1;
          }
        }
        const line = low - 1;
        return Position.create(line, offset - lineOffsets[line]);
      }
      offsetAt(position) {
        const lineOffsets = this.getLineOffsets();
        if (position.line >= lineOffsets.length) {
          return this._content.length;
        } else if (position.line < 0) {
          return 0;
        }
        const lineOffset = lineOffsets[position.line];
        const nextLineOffset = position.line + 1 < lineOffsets.length ? lineOffsets[position.line + 1] : this._content.length;
        return Math.max(Math.min(lineOffset + position.character, nextLineOffset), lineOffset);
      }
      get lineCount() {
        return this.getLineOffsets().length;
      }
    };
    (function(Is2) {
      const toString = Object.prototype.toString;
      function defined(value) {
        return typeof value !== "undefined";
      }
      __name(defined, "defined");
      Is2.defined = defined;
      function undefined2(value) {
        return typeof value === "undefined";
      }
      __name(undefined2, "undefined");
      Is2.undefined = undefined2;
      function boolean(value) {
        return value === true || value === false;
      }
      __name(boolean, "boolean");
      Is2.boolean = boolean;
      function string(value) {
        return toString.call(value) === "[object String]";
      }
      __name(string, "string");
      Is2.string = string;
      function number(value) {
        return toString.call(value) === "[object Number]";
      }
      __name(number, "number");
      Is2.number = number;
      function numberRange(value, min, max) {
        return toString.call(value) === "[object Number]" && min <= value && value <= max;
      }
      __name(numberRange, "numberRange");
      Is2.numberRange = numberRange;
      function integer2(value) {
        return toString.call(value) === "[object Number]" && -2147483648 <= value && value <= 2147483647;
      }
      __name(integer2, "integer");
      Is2.integer = integer2;
      function uinteger2(value) {
        return toString.call(value) === "[object Number]" && 0 <= value && value <= 2147483647;
      }
      __name(uinteger2, "uinteger");
      Is2.uinteger = uinteger2;
      function func(value) {
        return toString.call(value) === "[object Function]";
      }
      __name(func, "func");
      Is2.func = func;
      function objectLiteral(value) {
        return value !== null && typeof value === "object";
      }
      __name(objectLiteral, "objectLiteral");
      Is2.objectLiteral = objectLiteral;
      function typedArray(value, check) {
        return Array.isArray(value) && value.every(check);
      }
      __name(typedArray, "typedArray");
      Is2.typedArray = typedArray;
    })(Is || (Is = {}));
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/line.js
var require_line = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/line.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Line = void 0;
    var Line = class {
      static {
        __name(this, "Line");
      }
      constructor(document, range) {
        this.document = document;
        this.range = range;
      }
      getRange() {
        return this.range;
      }
      getTextContent() {
        return this.document.getText().substring(this.document.offsetAt(this.range.start), this.document.offsetAt(this.range.end));
      }
      isAfter(line) {
        return this.range.start.line > line.range.start.line;
      }
      isBefore(line) {
        return this.range.start.line < line;
      }
    };
    exports.Line = Line;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/util.js
var require_util = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/util.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Util = void 0;
    var Util = class _Util {
      static {
        __name(this, "Util");
      }
      static isUTF8BOM(char) {
        const uintArray = Uint8Array.from(Buffer.from(char, "UTF-8"));
        return uintArray[0] === 239 && uintArray[1] == 187 && uintArray[2] == 191;
      }
      static isWhitespace(char) {
        return char === " " || char === "	" || _Util.isNewline(char);
      }
      static isNewline(char) {
        return char === "\r" || char === "\n";
      }
      static findLeadingNonWhitespace(content, escapeChar) {
        whitespaceCheck: for (let i = 0; i < content.length; i++) {
          switch (content.charAt(i)) {
            case " ":
            case "	":
              continue;
            case escapeChar:
              escapeCheck: for (let j = i + 1; j < content.length; j++) {
                switch (content.charAt(j)) {
                  case " ":
                  case "	":
                    continue;
                  case "\r":
                    i = j + 1;
                    continue whitespaceCheck;
                  case "\n":
                    i = j;
                    continue whitespaceCheck;
                  default:
                    break escapeCheck;
                }
              }
              return -1;
            default:
              return i;
          }
        }
        return -1;
      }
      /**
       * Determines if the given position is contained within the given range.
       *
       * @param position the position to check
       * @param range the range to see if the position is inside of
       */
      static isInsideRange(position, range) {
        if (range.start.line === range.end.line) {
          return range.start.line === position.line && range.start.character <= position.character && position.character <= range.end.character;
        } else if (range.start.line === position.line) {
          return range.start.character <= position.character;
        } else if (range.end.line === position.line) {
          return position.character <= range.end.character;
        }
        return range.start.line < position.line && position.line < range.end.line;
      }
      static parseHeredocName(value) {
        value = value.substring(2);
        if (value.charAt(0) === "-") {
          value = value.substring(1);
        }
        if (value.charAt(0) === '"') {
          if (value.charAt(value.length - 1) !== '"') {
            return null;
          }
          value = value.substring(1, value.length - 1);
        }
        if (value.charAt(0) === "'") {
          if (value.charAt(value.length - 1) !== "'") {
            return null;
          }
          value = value.substring(1, value.length - 1);
        }
        if (value.charAt(0) === "<") {
          return null;
        }
        return value;
      }
    };
    exports.Util = Util;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/comment.js
var require_comment = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/comment.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Comment = void 0;
    var vscode_languageserver_types_1 = (init_main(), __toCommonJS(main_exports));
    var line_1 = require_line();
    var util_1 = require_util();
    var Comment = class extends line_1.Line {
      static {
        __name(this, "Comment");
      }
      constructor(document, range) {
        super(document, range);
      }
      toString() {
        const content = this.getContent();
        if (content) {
          return "# " + content;
        }
        return "#";
      }
      /**
       * Returns the content of this comment. This excludes leading and
       * trailing whitespace as well as the # symbol. If the comment only
       * consists of whitespace, the empty string will be returned.
       */
      getContent() {
        let range = this.getContentRange();
        if (range === null) {
          return "";
        }
        return this.document.getText().substring(this.document.offsetAt(range.start), this.document.offsetAt(range.end));
      }
      /**
       * Returns a range that includes the content of the comment
       * excluding any leading and trailing whitespace as well as the #
       * symbol. May return null if the comment only consists of whitespace
       * characters.
       */
      getContentRange() {
        let range = this.getRange();
        const startOffset = this.document.offsetAt(range.start);
        let raw = this.document.getText().substring(startOffset, this.document.offsetAt(range.end));
        let start = -1;
        let end = -1;
        for (let i = 1; i < raw.length; i++) {
          if (!util_1.Util.isWhitespace(raw.charAt(i))) {
            start = i;
            break;
          }
        }
        if (start === -1) {
          return null;
        }
        for (let i = raw.length - 1; i >= 1; i--) {
          if (!util_1.Util.isWhitespace(raw.charAt(i))) {
            end = i + 1;
            break;
          }
        }
        return vscode_languageserver_types_1.Range.create(this.document.positionAt(startOffset + start), this.document.positionAt(startOffset + end));
      }
    };
    exports.Comment = Comment;
  }
});

// ../../node_modules/.pnpm/vscode-languageserver-textdocument@1.0.12/node_modules/vscode-languageserver-textdocument/lib/esm/main.js
var main_exports2 = {};
__export(main_exports2, {
  TextDocument: () => TextDocument2
});
function mergeSort(data, compare) {
  if (data.length <= 1) {
    return data;
  }
  const p = data.length / 2 | 0;
  const left = data.slice(0, p);
  const right = data.slice(p);
  mergeSort(left, compare);
  mergeSort(right, compare);
  let leftIdx = 0;
  let rightIdx = 0;
  let i = 0;
  while (leftIdx < left.length && rightIdx < right.length) {
    const ret = compare(left[leftIdx], right[rightIdx]);
    if (ret <= 0) {
      data[i++] = left[leftIdx++];
    } else {
      data[i++] = right[rightIdx++];
    }
  }
  while (leftIdx < left.length) {
    data[i++] = left[leftIdx++];
  }
  while (rightIdx < right.length) {
    data[i++] = right[rightIdx++];
  }
  return data;
}
function computeLineOffsets(text, isAtLineStart, textOffset = 0) {
  const result = isAtLineStart ? [textOffset] : [];
  for (let i = 0; i < text.length; i++) {
    const ch = text.charCodeAt(i);
    if (isEOL(ch)) {
      if (ch === 13 && i + 1 < text.length && text.charCodeAt(i + 1) === 10) {
        i++;
      }
      result.push(textOffset + i + 1);
    }
  }
  return result;
}
function isEOL(char) {
  return char === 13 || char === 10;
}
function getWellformedRange(range) {
  const start = range.start;
  const end = range.end;
  if (start.line > end.line || start.line === end.line && start.character > end.character) {
    return { start: end, end: start };
  }
  return range;
}
function getWellformedEdit(textEdit) {
  const range = getWellformedRange(textEdit.range);
  if (range !== textEdit.range) {
    return { newText: textEdit.newText, range };
  }
  return textEdit;
}
var FullTextDocument2, TextDocument2;
var init_main2 = __esm({
  "../../node_modules/.pnpm/vscode-languageserver-textdocument@1.0.12/node_modules/vscode-languageserver-textdocument/lib/esm/main.js"() {
    "use strict";
    init_esm();
    FullTextDocument2 = class _FullTextDocument {
      static {
        __name(this, "FullTextDocument");
      }
      constructor(uri, languageId, version, content) {
        this._uri = uri;
        this._languageId = languageId;
        this._version = version;
        this._content = content;
        this._lineOffsets = void 0;
      }
      get uri() {
        return this._uri;
      }
      get languageId() {
        return this._languageId;
      }
      get version() {
        return this._version;
      }
      getText(range) {
        if (range) {
          const start = this.offsetAt(range.start);
          const end = this.offsetAt(range.end);
          return this._content.substring(start, end);
        }
        return this._content;
      }
      update(changes, version) {
        for (const change of changes) {
          if (_FullTextDocument.isIncremental(change)) {
            const range = getWellformedRange(change.range);
            const startOffset = this.offsetAt(range.start);
            const endOffset = this.offsetAt(range.end);
            this._content = this._content.substring(0, startOffset) + change.text + this._content.substring(endOffset, this._content.length);
            const startLine = Math.max(range.start.line, 0);
            const endLine = Math.max(range.end.line, 0);
            let lineOffsets = this._lineOffsets;
            const addedLineOffsets = computeLineOffsets(change.text, false, startOffset);
            if (endLine - startLine === addedLineOffsets.length) {
              for (let i = 0, len = addedLineOffsets.length; i < len; i++) {
                lineOffsets[i + startLine + 1] = addedLineOffsets[i];
              }
            } else {
              if (addedLineOffsets.length < 1e4) {
                lineOffsets.splice(startLine + 1, endLine - startLine, ...addedLineOffsets);
              } else {
                this._lineOffsets = lineOffsets = lineOffsets.slice(0, startLine + 1).concat(addedLineOffsets, lineOffsets.slice(endLine + 1));
              }
            }
            const diff = change.text.length - (endOffset - startOffset);
            if (diff !== 0) {
              for (let i = startLine + 1 + addedLineOffsets.length, len = lineOffsets.length; i < len; i++) {
                lineOffsets[i] = lineOffsets[i] + diff;
              }
            }
          } else if (_FullTextDocument.isFull(change)) {
            this._content = change.text;
            this._lineOffsets = void 0;
          } else {
            throw new Error("Unknown change event received");
          }
        }
        this._version = version;
      }
      getLineOffsets() {
        if (this._lineOffsets === void 0) {
          this._lineOffsets = computeLineOffsets(this._content, true);
        }
        return this._lineOffsets;
      }
      positionAt(offset) {
        offset = Math.max(Math.min(offset, this._content.length), 0);
        const lineOffsets = this.getLineOffsets();
        let low = 0, high = lineOffsets.length;
        if (high === 0) {
          return { line: 0, character: offset };
        }
        while (low < high) {
          const mid = Math.floor((low + high) / 2);
          if (lineOffsets[mid] > offset) {
            high = mid;
          } else {
            low = mid + 1;
          }
        }
        const line = low - 1;
        offset = this.ensureBeforeEOL(offset, lineOffsets[line]);
        return { line, character: offset - lineOffsets[line] };
      }
      offsetAt(position) {
        const lineOffsets = this.getLineOffsets();
        if (position.line >= lineOffsets.length) {
          return this._content.length;
        } else if (position.line < 0) {
          return 0;
        }
        const lineOffset = lineOffsets[position.line];
        if (position.character <= 0) {
          return lineOffset;
        }
        const nextLineOffset = position.line + 1 < lineOffsets.length ? lineOffsets[position.line + 1] : this._content.length;
        const offset = Math.min(lineOffset + position.character, nextLineOffset);
        return this.ensureBeforeEOL(offset, lineOffset);
      }
      ensureBeforeEOL(offset, lineOffset) {
        while (offset > lineOffset && isEOL(this._content.charCodeAt(offset - 1))) {
          offset--;
        }
        return offset;
      }
      get lineCount() {
        return this.getLineOffsets().length;
      }
      static isIncremental(event) {
        const candidate = event;
        return candidate !== void 0 && candidate !== null && typeof candidate.text === "string" && candidate.range !== void 0 && (candidate.rangeLength === void 0 || typeof candidate.rangeLength === "number");
      }
      static isFull(event) {
        const candidate = event;
        return candidate !== void 0 && candidate !== null && typeof candidate.text === "string" && candidate.range === void 0 && candidate.rangeLength === void 0;
      }
    };
    (function(TextDocument3) {
      function create(uri, languageId, version, content) {
        return new FullTextDocument2(uri, languageId, version, content);
      }
      __name(create, "create");
      TextDocument3.create = create;
      function update(document, changes, version) {
        if (document instanceof FullTextDocument2) {
          document.update(changes, version);
          return document;
        } else {
          throw new Error("TextDocument.update: document must be created by TextDocument.create");
        }
      }
      __name(update, "update");
      TextDocument3.update = update;
      function applyEdits(document, edits) {
        const text = document.getText();
        const sortedEdits = mergeSort(edits.map(getWellformedEdit), (a, b) => {
          const diff = a.range.start.line - b.range.start.line;
          if (diff === 0) {
            return a.range.start.character - b.range.start.character;
          }
          return diff;
        });
        let lastModifiedOffset = 0;
        const spans = [];
        for (const e of sortedEdits) {
          const startOffset = document.offsetAt(e.range.start);
          if (startOffset < lastModifiedOffset) {
            throw new Error("Overlapping edit");
          } else if (startOffset > lastModifiedOffset) {
            spans.push(text.substring(lastModifiedOffset, startOffset));
          }
          if (e.newText.length) {
            spans.push(e.newText);
          }
          lastModifiedOffset = document.offsetAt(e.range.end);
        }
        spans.push(text.substr(lastModifiedOffset));
        return spans.join("");
      }
      __name(applyEdits, "applyEdits");
      TextDocument3.applyEdits = applyEdits;
    })(TextDocument2 || (TextDocument2 = {}));
    __name(mergeSort, "mergeSort");
    __name(computeLineOffsets, "computeLineOffsets");
    __name(isEOL, "isEOL");
    __name(getWellformedRange, "getWellformedRange");
    __name(getWellformedEdit, "getWellformedEdit");
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/parserDirective.js
var require_parserDirective = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/parserDirective.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ParserDirective = void 0;
    var main_1 = require_main();
    var line_1 = require_line();
    var ParserDirective = class extends line_1.Line {
      static {
        __name(this, "ParserDirective");
      }
      constructor(document, range, nameRange, valueRange) {
        super(document, range);
        this.nameRange = nameRange;
        this.valueRange = valueRange;
      }
      toString() {
        return "# " + this.getName() + "=" + this.getValue();
      }
      getNameRange() {
        return this.nameRange;
      }
      getValueRange() {
        return this.valueRange;
      }
      getName() {
        return this.document.getText().substring(this.document.offsetAt(this.nameRange.start), this.document.offsetAt(this.nameRange.end));
      }
      getValue() {
        return this.document.getText().substring(this.document.offsetAt(this.valueRange.start), this.document.offsetAt(this.valueRange.end));
      }
      getDirective() {
        const directive = main_1.Directive[this.getName().toLowerCase()];
        return directive === void 0 ? null : directive;
      }
    };
    exports.ParserDirective = ParserDirective;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/heredoc.js
var require_heredoc = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/heredoc.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Heredoc = void 0;
    var Heredoc = class {
      static {
        __name(this, "Heredoc");
      }
      constructor(startRange, name, nameRange, contentRange, endRange) {
        this.startRange = startRange;
        this.name = name;
        this.nameRange = nameRange;
        this.contentRange = contentRange;
        this.endRange = endRange;
      }
      /**
       * Returns the name of the here-document.
       *
       * This API is experimental and subject to change.
       */
      getName() {
        return this.name;
      }
      /**
       * Returns the range of the start operator and the name. If the
       * here-document is initialized with <<EOT then the start range would
       * encompass all five characters.
       *
       * This API is experimental and subject to change.
       */
      getStartRange() {
        return this.startRange;
      }
      /**
       * Returns the range of this here-document's name that is declared at
       * the beginning of the here-document with the operator. If the
       * here-document is initialized with <<EOT then the name range would
       * encompass the latter three "EOT" characters.
       *
       * This API is experimental and subject to change.
       */
      getNameRange() {
        return this.nameRange;
      }
      /**
       * Returns the range of the content of this here-document. This may
       * be null if the here-document has no content because:
       * - the start range is the only thing that was declared
       * - the end range was declared immediately and there is no content
       *
       * This API is experimental and subject to change.
       */
      getContentRange() {
        return this.contentRange;
      }
      /**
       * Returns the range of the here-document's name on a line that
       * represents the end of the here-document.
       *
       * This API is experimental and subject to change.
       */
      getDelimiterRange() {
        return this.endRange;
      }
    };
    exports.Heredoc = Heredoc;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/variable.js
var require_variable = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/variable.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Variable = void 0;
    var Variable = class {
      static {
        __name(this, "Variable");
      }
      constructor(name, nameRange, range, modifier, modifierRange, substitutionParameter, substitutionRange, defined, buildVariable, stringValue) {
        this.name = name;
        this.nameRange = nameRange;
        this.range = range;
        this.modifier = modifier;
        this.modifierRange = modifierRange;
        this.substitutionParameter = substitutionParameter;
        this.substitutionRange = substitutionRange;
        this.defined = defined;
        this.buildVariable = buildVariable;
        this.stringValue = stringValue;
      }
      toString() {
        return this.stringValue;
      }
      getName() {
        return this.name;
      }
      getNameRange() {
        return this.nameRange;
      }
      /**
       * Returns the range of the entire variable. This includes the symbols for
       * the declaration of the variable such as the $, {, and } symbols.
       *
       * @return the range in the document that this variable encompasses in its
       *         entirety
       */
      getRange() {
        return this.range;
      }
      /**
       * Returns the modifier character that has been set for
       * specifying how this variable should be expanded and resolved.
       * If this variable is ${variable:+value} then the modifier
       * character is '+'. Will be the empty string if the variable is
       * declared as ${variable:}. Otherwise, will be null if this
       * variable will not use variable substitution at all (such as
       * ${variable} or $variable).
       *
       * @return this variable's modifier character, or the empty
       *         string if it does not have one, or null if this
       *         variable will not use variable substitution
       */
      getModifier() {
        return this.modifier;
      }
      getModifierRange() {
        return this.modifierRange;
      }
      /**
       * Returns the parameter that will be used for substitution if
       * this variable uses modifiers to define how its value should be
       * resolved. If this variable is ${variable:+value} then the
       * substitution value will be 'value'. Will be the empty string
       * if the variable is declared as ${variable:+} or some other
       * variant where the only thing that follows the modifier
       * character (excluding considerations of escape characters and
       * so on) is the variable's closing bracket. May be null if this
       * variable does not have a modifier character defined (such as
       * ${variable} or $variable).
       *
       * @return this variable's substitution parameter, or the empty
       *         string if it does not have one, or null if there is
       *         not one defined
       */
      getSubstitutionParameter() {
        return this.substitutionParameter;
      }
      getSubstitutionRange() {
        return this.substitutionRange;
      }
      /**
       * Returns whether this variable has been defined or not.
       *
       * @return true if this variable has been defined, false otherwise
       */
      isDefined() {
        return this.defined;
      }
      isBuildVariable() {
        return this.buildVariable === true;
      }
      isEnvironmentVariable() {
        return this.buildVariable === false;
      }
    };
    exports.Variable = Variable;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instruction.js
var require_instruction = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instruction.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Instruction = void 0;
    var vscode_languageserver_types_1 = (init_main(), __toCommonJS(main_exports));
    var util_1 = require_util();
    var line_1 = require_line();
    var argument_1 = require_argument();
    var heredoc_1 = require_heredoc();
    var variable_1 = require_variable();
    var main_1 = require_main();
    var Instruction = class extends line_1.Line {
      static {
        __name(this, "Instruction");
      }
      constructor(document, range, dockerfile, escapeChar, instruction, instructionRange) {
        super(document, range);
        this.dockerfile = dockerfile;
        this.escapeChar = escapeChar;
        this.instruction = instruction;
        this.instructionRange = instructionRange;
      }
      toString() {
        let value = this.getKeyword();
        for (let arg of this.getRawArguments()) {
          value += " ";
          value += arg.getValue();
        }
        return value;
      }
      getRangeContent(range) {
        if (range === null) {
          return null;
        }
        return this.document.getText().substring(this.document.offsetAt(range.start), this.document.offsetAt(range.end));
      }
      getInstructionRange() {
        return this.instructionRange;
      }
      getInstruction() {
        return this.instruction;
      }
      getKeyword() {
        return this.getInstruction().toUpperCase();
      }
      getArgumentsRange() {
        let args = this.getArguments();
        if (args.length === 0) {
          return null;
        }
        return vscode_languageserver_types_1.Range.create(args[0].getRange().start, args[args.length - 1].getRange().end);
      }
      getArgumentsRanges() {
        let args = this.getArguments();
        if (args.length === 0) {
          return [];
        }
        if (args[0].getRange().start.line === args[args.length - 1].getRange().end.line) {
          return [vscode_languageserver_types_1.Range.create(args[0].getRange().start, args[args.length - 1].getRange().end)];
        }
        let ranges = [];
        let end = -1;
        let startPosition = args[0].getRange().start;
        let range = this.getInstructionRange();
        let extra = this.document.offsetAt(startPosition) - this.document.offsetAt(range.start);
        let content = this.getTextContent();
        let fullArgs = content.substring(extra, this.document.offsetAt(args[args.length - 1].getRange().end) - this.document.offsetAt(range.start));
        let offset = this.document.offsetAt(range.start) + extra;
        let start = false;
        let comment = false;
        for (let i = 0; i < fullArgs.length; i++) {
          let char = fullArgs.charAt(i);
          if (char === this.escapeChar) {
            let next = fullArgs.charAt(i + 1);
            if (next === " " || next === "	") {
              whitespaceCheck: for (let j = i + 2; j < fullArgs.length; j++) {
                switch (fullArgs.charAt(j)) {
                  case " ":
                  case "	":
                    continue;
                  case "\r":
                    j++;
                  case "\n":
                    if (startPosition !== null) {
                      ranges.push(vscode_languageserver_types_1.Range.create(startPosition, this.document.positionAt(offset + end + 1)));
                    }
                    startPosition = null;
                    start = true;
                    comment = false;
                    i = j;
                    break whitespaceCheck;
                  default:
                    break whitespaceCheck;
                }
              }
            } else if (next === "\r") {
              if (startPosition !== null) {
                ranges.push(vscode_languageserver_types_1.Range.create(startPosition, this.document.positionAt(offset + end + 1)));
                startPosition = null;
              }
              start = true;
              comment = false;
              i += 2;
            } else if (next === "\n") {
              if (startPosition !== null) {
                ranges.push(vscode_languageserver_types_1.Range.create(startPosition, this.document.positionAt(offset + end + 1)));
              }
              startPosition = null;
              start = true;
              comment = false;
              i++;
            } else {
              i++;
            }
          } else if (util_1.Util.isNewline(char)) {
            if (comment) {
              startPosition = null;
              start = true;
              comment = false;
            }
          } else {
            if (!comment) {
              if (startPosition === null) {
                if (char === "#") {
                  comment = true;
                  continue;
                }
                let position = this.document.positionAt(offset + i);
                if (position.character !== 0) {
                  startPosition = vscode_languageserver_types_1.Position.create(position.line, 0);
                }
              }
              end = i;
            }
          }
        }
        if (startPosition === null) {
          ranges.push(vscode_languageserver_types_1.Range.create(this.document.positionAt(offset + end), this.document.positionAt(offset + end + 1)));
        } else {
          ranges.push(vscode_languageserver_types_1.Range.create(startPosition, this.document.positionAt(offset + end + 1)));
        }
        return ranges;
      }
      getRawArgumentsContent() {
        let args = this.getArguments();
        if (args.length === 0) {
          return null;
        }
        return this.getRangeContent(vscode_languageserver_types_1.Range.create(args[0].getRange().start, args[args.length - 1].getRange().end));
      }
      getArgumentsContent() {
        let args = this.getArguments();
        if (args.length === 0) {
          return null;
        }
        let content = "";
        let ranges = this.getArgumentsRanges();
        let documentText = this.document.getText();
        for (let range of ranges) {
          content += documentText.substring(this.document.offsetAt(range.start), this.document.offsetAt(range.end));
        }
        return content;
      }
      getArguments() {
        return this.getRawArguments();
      }
      getRawArguments() {
        let args = [];
        let range = this.getInstructionRange();
        let extra = this.document.offsetAt(range.end) - this.document.offsetAt(range.start);
        let content = this.getTextContent();
        let fullArgs = content.substring(extra);
        let offset = this.document.offsetAt(range.start) + extra;
        let start = false;
        let comment = false;
        let found = -1;
        let escapedWhitespaceDetected = false;
        let escaping = false;
        let escapeMarker = -1;
        let escapedArg = "";
        for (let i = 0; i < fullArgs.length; i++) {
          let char = fullArgs.charAt(i);
          if (util_1.Util.isWhitespace(char)) {
            if (escaping) {
              escapedWhitespaceDetected = true;
              if (util_1.Util.isNewline(char)) {
                escapedWhitespaceDetected = false;
                if (comment) {
                  comment = false;
                  start = true;
                }
              }
              continue;
            } else if (found !== -1) {
              if (escapeMarker === -1) {
                args.push(new argument_1.Argument(escapedArg, vscode_languageserver_types_1.Range.create(this.document.positionAt(offset + found), this.document.positionAt(offset + i))));
              } else {
                args.push(new argument_1.Argument(escapedArg, vscode_languageserver_types_1.Range.create(this.document.positionAt(offset + found), this.document.positionAt(offset + escapeMarker))));
              }
              escapeMarker = -1;
              escapedArg = "";
              found = -1;
            }
          } else if (char === this.escapeChar) {
            let next = fullArgs.charAt(i + 1);
            if (next === " " || next === "	") {
              whitespaceCheck: for (let j = i + 2; j < fullArgs.length; j++) {
                let newlineCheck = fullArgs.charAt(j);
                switch (newlineCheck) {
                  case " ":
                  case "	":
                    continue;
                  case "\r":
                    j++;
                  case "\n":
                    comment = false;
                    escaping = true;
                    start = true;
                    if (found !== -1) {
                      escapeMarker = i;
                    }
                    i = j;
                    break whitespaceCheck;
                  default:
                    escapeMarker = i;
                    if (found === -1) {
                      i = j - 1;
                    }
                    break whitespaceCheck;
                }
              }
            } else if (next === "\r") {
              comment = false;
              escaping = true;
              start = true;
              if (found !== -1 && escapeMarker === -1) {
                escapeMarker = i;
              }
              i += 2;
            } else if (next === "\n") {
              comment = false;
              escaping = true;
              start = true;
              if (found !== -1 && escapeMarker === -1) {
                escapeMarker = i;
              }
              i++;
            } else {
              if (escapedWhitespaceDetected && escapeMarker !== -1) {
                args.push(new argument_1.Argument(escapedArg, vscode_languageserver_types_1.Range.create(this.document.positionAt(offset + found), this.document.positionAt(offset + escapeMarker))));
                escapedArg = "";
                found = -1;
              }
              escapeMarker = -1;
              escapedWhitespaceDetected = false;
              escaping = false;
              if (next === "$") {
                escapedArg = escapedArg + char + next;
              } else if (next === "") {
                break;
              } else {
                escapedArg = escapedArg + next;
              }
              if (found === -1) {
                found = i;
              }
              i++;
            }
          } else if (!comment) {
            if (start && char === "#") {
              comment = true;
            } else {
              if (escapedWhitespaceDetected && escapeMarker !== -1) {
                args.push(new argument_1.Argument(escapedArg, vscode_languageserver_types_1.Range.create(this.document.positionAt(offset + found), this.document.positionAt(offset + escapeMarker))));
                escapedArg = "";
                found = -1;
              }
              escapedWhitespaceDetected = false;
              escaping = false;
              escapeMarker = -1;
              escapedArg = escapedArg + char;
              if (found === -1) {
                found = i;
              }
            }
            start = false;
          }
        }
        if (found !== -1) {
          if (escapeMarker === -1) {
            args.push(new argument_1.Argument(escapedArg, vscode_languageserver_types_1.Range.create(this.document.positionAt(offset + found), this.document.positionAt(offset + fullArgs.length))));
          } else {
            args.push(new argument_1.Argument(escapedArg, vscode_languageserver_types_1.Range.create(this.document.positionAt(offset + found), this.document.positionAt(offset + escapeMarker))));
          }
        }
        return args;
      }
      getExpandedArguments() {
        let args = this.getArguments();
        for (let i = 0; i < args.length; i++) {
          const argRange = args[i].getRange();
          let offset = this.document.offsetAt(argRange.start);
          const variables = this.parseVariables(offset, args[i].getValue());
          const swaps = [];
          let requiresExpansion = false;
          for (let variable of variables) {
            const value = this.dockerfile.resolveVariable(variable.getName(), variable.getNameRange().start.line);
            swaps.push(value);
            requiresExpansion = requiresExpansion || value !== void 0;
          }
          if (requiresExpansion) {
            let expanded = "";
            for (let j = 0; j < swaps.length; j++) {
              const variableRange = variables[j].getRange();
              const start = this.document.offsetAt(variableRange.start);
              const end = this.document.offsetAt(variableRange.end);
              if (swaps[j]) {
                expanded += this.document.getText().substring(offset, start);
                expanded += swaps[j];
                offset = end;
              } else {
                expanded += this.document.getText().substring(offset, end);
                offset = end;
              }
            }
            const argEnd = this.document.offsetAt(argRange.end);
            if (argEnd !== offset) {
              expanded += this.document.getText().substring(offset, argEnd);
            }
            args[i] = new argument_1.Argument(expanded, argRange);
          }
        }
        return args;
      }
      getVariables() {
        const variables = [];
        const args = this.getRawArguments();
        for (const arg of args) {
          let range = arg.getRange();
          let rawValue = this.document.getText().substring(this.document.offsetAt(range.start), this.document.offsetAt(range.end));
          const parsedVariables = this.parseVariables(this.document.offsetAt(arg.getRange().start), rawValue);
          for (const parsedVariable of parsedVariables) {
            variables.push(parsedVariable);
          }
        }
        return variables;
      }
      parseVariables(offset, arg) {
        let variables = [];
        variableLoop: for (let i = 0; i < arg.length; i++) {
          switch (arg.charAt(i)) {
            case this.escapeChar:
              if (arg.charAt(i + 1) === "$") {
                i++;
              }
              break;
            case "$":
              if (arg.charAt(i + 1) === "{") {
                let escapedString = "${";
                let escapedName = "";
                let nameEnd = -1;
                let escapedSubstitutionParameter = "";
                let substitutionStart = -1;
                let substitutionEnd = -1;
                let modifierRead = -1;
                nameLoop: for (let j = i + 2; j < arg.length; j++) {
                  let char = arg.charAt(j);
                  switch (char) {
                    case this.escapeChar:
                      for (let k = j + 1; k < arg.length; k++) {
                        switch (arg.charAt(k)) {
                          case " ":
                          case "	":
                          case "\r":
                            continue;
                          case "\n":
                            j = k;
                            continue nameLoop;
                        }
                      }
                      break;
                    case "}":
                      escapedString += "}";
                      let modifier = null;
                      let modifierRange = null;
                      let substitutionParameter = modifierRead !== -1 ? escapedSubstitutionParameter : null;
                      let substitutionRange = null;
                      if (nameEnd === -1) {
                        nameEnd = j;
                      } else if (nameEnd + 1 === j) {
                        modifier = "";
                        modifierRange = vscode_languageserver_types_1.Range.create(this.document.positionAt(offset + nameEnd + 1), this.document.positionAt(offset + nameEnd + 1));
                      } else {
                        if (substitutionStart === -1) {
                          substitutionStart = modifierRead + 1;
                          substitutionEnd = modifierRead + 1;
                        } else {
                          substitutionEnd = substitutionEnd + 1;
                        }
                        modifier = arg.substring(modifierRead, modifierRead + 1);
                        modifierRange = vscode_languageserver_types_1.Range.create(this.document.positionAt(offset + modifierRead), this.document.positionAt(offset + modifierRead + 1));
                        substitutionRange = vscode_languageserver_types_1.Range.create(this.document.positionAt(offset + substitutionStart), this.document.positionAt(offset + substitutionEnd));
                      }
                      let start = this.document.positionAt(offset + i);
                      variables.push(new variable_1.Variable(escapedName, vscode_languageserver_types_1.Range.create(this.document.positionAt(offset + i + 2), this.document.positionAt(offset + nameEnd)), vscode_languageserver_types_1.Range.create(start, this.document.positionAt(offset + j + 1)), modifier, modifierRange, substitutionParameter, substitutionRange, this.dockerfile.resolveVariable(escapedName, start.line) !== void 0, this.isBuildVariable(escapedName, start.line), escapedString));
                      i = j;
                      continue variableLoop;
                    case ":":
                      if (nameEnd === -1) {
                        nameEnd = j;
                      } else if (modifierRead !== -1) {
                        if (substitutionStart === -1) {
                          substitutionStart = j;
                          substitutionEnd = j;
                        } else {
                          substitutionEnd = j;
                        }
                        escapedSubstitutionParameter += ":";
                      } else {
                        modifierRead = j;
                      }
                      escapedString += ":";
                      break;
                    case "\n":
                    case "\r":
                    case " ":
                    case "	":
                      break;
                    default:
                      if (nameEnd === -1) {
                        escapedName += char;
                      } else if (modifierRead !== -1) {
                        if (substitutionStart === -1) {
                          substitutionStart = j;
                          substitutionEnd = j;
                        } else {
                          substitutionEnd = j;
                        }
                        escapedSubstitutionParameter += char;
                      } else {
                        modifierRead = j;
                      }
                      escapedString += char;
                      break;
                  }
                }
                break variableLoop;
              } else if (util_1.Util.isWhitespace(arg.charAt(i + 1)) || i === arg.length - 1) {
                continue;
              } else {
                let escapedName = "";
                nameLoop: for (let j = i + 1; j < arg.length; j++) {
                  let char = arg.charAt(j);
                  switch (char) {
                    case "\r":
                    case "\n":
                    case " ":
                    case "	":
                      continue;
                    case "$":
                    case "'":
                    case '"':
                      let varStart = this.document.positionAt(offset + i);
                      variables.push(new variable_1.Variable(escapedName, vscode_languageserver_types_1.Range.create(this.document.positionAt(offset + i + 1), this.document.positionAt(offset + j)), vscode_languageserver_types_1.Range.create(varStart, this.document.positionAt(offset + j)), null, null, null, null, this.dockerfile.resolveVariable(escapedName, varStart.line) !== void 0, this.isBuildVariable(escapedName, varStart.line), "$" + escapedName));
                      i = j - 1;
                      continue variableLoop;
                    case this.escapeChar:
                      for (let k = j + 1; k < arg.length; k++) {
                        switch (arg.charAt(k)) {
                          case " ":
                          case "	":
                          case "\r":
                            continue;
                          case "\n":
                            j = k;
                            continue nameLoop;
                        }
                      }
                      let start2 = this.document.positionAt(offset + i);
                      variables.push(new variable_1.Variable(escapedName, vscode_languageserver_types_1.Range.create(this.document.positionAt(offset + i + 1), this.document.positionAt(offset + j)), vscode_languageserver_types_1.Range.create(start2, this.document.positionAt(offset + j)), null, null, null, null, this.dockerfile.resolveVariable(escapedName, start2.line) !== void 0, this.isBuildVariable(escapedName, start2.line), "$" + escapedName));
                      break variableLoop;
                  }
                  if (char.match(/^[a-z0-9_]+$/i) === null) {
                    let varStart = this.document.positionAt(offset + i);
                    variables.push(new variable_1.Variable(escapedName, vscode_languageserver_types_1.Range.create(this.document.positionAt(offset + i + 1), this.document.positionAt(offset + j)), vscode_languageserver_types_1.Range.create(varStart, this.document.positionAt(offset + j)), null, null, null, null, this.dockerfile.resolveVariable(escapedName, varStart.line) !== void 0, this.isBuildVariable(escapedName, varStart.line), "$" + escapedName));
                    i = j - 1;
                    continue variableLoop;
                  }
                  escapedName += char;
                }
                let start = this.document.positionAt(offset + i);
                variables.push(new variable_1.Variable(escapedName, vscode_languageserver_types_1.Range.create(this.document.positionAt(offset + i + 1), this.document.positionAt(offset + arg.length)), vscode_languageserver_types_1.Range.create(start, this.document.positionAt(offset + arg.length)), null, null, null, null, this.dockerfile.resolveVariable(escapedName, start.line) !== void 0, this.isBuildVariable(escapedName, start.line), "$" + escapedName));
              }
              break variableLoop;
          }
        }
        return variables;
      }
      isBuildVariable(variable, line) {
        if (this.getKeyword() === main_1.Keyword.FROM) {
          for (const initialArg of this.dockerfile.getInitialARGs()) {
            const arg = initialArg;
            const property = arg.getProperty();
            if (property && variable === property.getName()) {
              return true;
            }
          }
          return void 0;
        }
        let image = this.dockerfile.getContainingImage(vscode_languageserver_types_1.Position.create(line, 0));
        let envs = image.getENVs();
        for (let i = envs.length - 1; i >= 0; i--) {
          if (envs[i].isBefore(line)) {
            for (let property of envs[i].getProperties()) {
              if (property.getName() === variable) {
                return false;
              }
            }
          }
        }
        let args = image.getARGs();
        for (let i = args.length - 1; i >= 0; i--) {
          if (args[i].isBefore(line)) {
            let property = args[i].getProperty();
            if (property && property.getName() === variable) {
              return true;
            }
          }
        }
        return void 0;
      }
      createSingleLineHeredocs(args) {
        const heredocs = [];
        for (const arg of args) {
          const value = arg.getValue();
          if (value.startsWith("<<") && util_1.Util.parseHeredocName(value) !== null) {
            const startRange = arg.getRange();
            const nameRange = this.getNameRange(startRange);
            const name = this.getName(nameRange);
            heredocs.push(new heredoc_1.Heredoc(startRange, name, nameRange, null, null));
          }
        }
        return heredocs;
      }
      getName(nameRange) {
        const content = this.document.getText(nameRange);
        let escaping = false;
        let name = "";
        nameLoop: for (let i = 0; i < content.length; i++) {
          const ch = content.charAt(i);
          switch (ch) {
            case this.escapeChar:
              escaping = true;
              for (let j = i + 1; j < content.length; j++) {
                switch (content.charAt(j)) {
                  case " ":
                  case "	":
                    break;
                  case "\r":
                    i = j + 1;
                    continue nameLoop;
                  case "\n":
                    i = j;
                    continue nameLoop;
                  default:
                    name += content.charAt(j);
                    i = j;
                    continue nameLoop;
                }
              }
              break;
            case "#":
              if (escaping) {
                for (let j = i + 1; j < content.length; j++) {
                  switch (content.charAt(j)) {
                    case "\n":
                      i = j;
                      continue nameLoop;
                  }
                }
              }
            case " ":
            case "	":
            case "\r":
            case "\n":
              if (escaping) {
                break;
              }
            default:
              name += ch;
              break;
          }
        }
        return name;
      }
      getNameRange(startRange) {
        const content = this.document.getText(startRange);
        let endFound = false;
        let searchHyphen = false;
        let start = -1;
        let end = -1;
        let escaping = false;
        let quote = null;
        contentLoop: for (let i = 0; i < content.length; i++) {
          const ch = content.charAt(i);
          switch (ch) {
            case '"':
            case "'":
              if (quote === ch) {
                break contentLoop;
              }
              quote = ch;
              continue;
            case this.escapeChar:
              for (let j = i + 1; j < content.length; j++) {
                switch (content.charAt(j)) {
                  case "\n":
                    escaping = true;
                    j = i;
                    continue contentLoop;
                }
              }
              break;
            case " ":
            case "	":
            case "\r":
            case "\n":
              break;
            case "<":
              if (endFound) {
                searchHyphen = true;
              } else {
                endFound = true;
              }
              break;
            case "-":
              if (searchHyphen) {
                searchHyphen = false;
                break;
              }
            case "#":
              if (escaping) {
                for (let j = i + 1; j < content.length; j++) {
                  switch (content.charAt(j)) {
                    case "\n":
                      i = j;
                      continue contentLoop;
                  }
                }
              }
            default:
              if (start === -1) {
                start = i;
              }
              if (quote !== null) {
                end = i + 1;
                break;
              }
              break contentLoop;
          }
        }
        if (start === -1) {
          return vscode_languageserver_types_1.Range.create(startRange.end, startRange.end);
        }
        const nameStart = this.document.positionAt(this.document.offsetAt(startRange.start) + start);
        const nameEnd = quote !== null ? this.document.positionAt(this.document.offsetAt(startRange.start) + end) : startRange.end;
        return vscode_languageserver_types_1.Range.create(nameStart, nameEnd);
      }
      getHeredocs() {
        const args = this.getArguments();
        if (args.length === 0) {
          return [];
        }
        const heredocs = [];
        const range = this.getRange();
        if (range.start.line === range.end.line) {
          return this.createSingleLineHeredocs(args);
        }
        const heredocDefinitions = [];
        let heredocsProcessed = false;
        let escaping = false;
        let contentStart = -1;
        let contentEnd = -1;
        let lineStart = -1;
        let currentHeredoc = 0;
        const startOffset = this.document.offsetAt(args[0].getRange().start);
        const content = this.getRangeContent(vscode_languageserver_types_1.Range.create(args[0].getRange().start, this.getRange().end));
        contentLoop: for (let i = 0; i < content.length; i++) {
          switch (content.charAt(i)) {
            case this.escapeChar:
              escaping = true;
              for (let j = i + 1; j < content.length; j++) {
                switch (content.charAt(j)) {
                  case " ":
                  case "	":
                    break;
                  case "\r":
                    j++;
                  case "\n":
                    i = j;
                    continue contentLoop;
                  default:
                    i = j;
                    continue contentLoop;
                }
              }
              break;
            case "\r":
              break;
            case "\n":
              if (escaping) {
                break;
              }
              if (heredocsProcessed) {
                if (contentStart === -1) {
                  contentStart = i;
                }
                contentEnd = i;
                const arg = heredocDefinitions[currentHeredoc];
                const startRange = arg.getRange();
                const nameRange = this.getNameRange(startRange);
                const name = this.getName(nameRange);
                const delimiterRange = this.getDelimiterRange(arg, name, vscode_languageserver_types_1.Range.create(this.document.positionAt(startOffset + lineStart), this.document.positionAt(startOffset + i)));
                if (delimiterRange !== null) {
                  const contentRange = vscode_languageserver_types_1.Range.create(this.document.positionAt(startOffset + contentStart), this.document.positionAt(startOffset + lineStart - 1));
                  heredocs.push(new heredoc_1.Heredoc(startRange, name, nameRange, contentRange, delimiterRange));
                  contentStart = -1;
                  currentHeredoc++;
                }
                lineStart = -1;
              } else {
                const offsetLimit = startOffset + i;
                for (const arg of args) {
                  if (this.document.offsetAt(arg.getRange().start) < offsetLimit) {
                    if (arg.getValue().startsWith("<<")) {
                      heredocDefinitions.push(arg);
                    }
                  } else {
                    break;
                  }
                }
                heredocsProcessed = true;
                lineStart = -1;
                continue contentLoop;
              }
              break;
            case " ":
            case "	":
              if (escaping) {
                break;
              }
            case "#":
              if (escaping) {
                for (let j = i + 1; j < content.length; j++) {
                  switch (content.charAt(j)) {
                    case "\n":
                      i = j;
                      continue contentLoop;
                  }
                }
              }
            default:
              if (escaping) {
                escaping = false;
              }
              if (heredocsProcessed) {
                if (contentStart === -1) {
                  contentStart = i;
                }
                if (lineStart === -1) {
                  lineStart = i;
                }
              }
              break;
          }
        }
        if (heredocsProcessed) {
          const arg = heredocDefinitions[currentHeredoc];
          const startRange = arg.getRange();
          const nameRange = this.getNameRange(startRange);
          const name = this.getName(nameRange);
          let contentRange = null;
          const delimiterRange = this.getDelimiterRange(arg, name, vscode_languageserver_types_1.Range.create(this.document.positionAt(startOffset + lineStart), range.end));
          if (delimiterRange === null) {
            contentRange = vscode_languageserver_types_1.Range.create(this.document.positionAt(startOffset + contentStart), range.end);
          } else if (contentEnd !== -1) {
            contentRange = vscode_languageserver_types_1.Range.create(this.document.positionAt(startOffset + contentStart), this.document.positionAt(startOffset + contentEnd));
          }
          heredocs.push(new heredoc_1.Heredoc(startRange, name, nameRange, contentRange, delimiterRange));
          currentHeredoc++;
          for (let i = currentHeredoc; i < heredocDefinitions.length; i++) {
            const arg2 = heredocDefinitions[currentHeredoc];
            const startRange2 = arg2.getRange();
            const nameRange2 = this.getNameRange(startRange2);
            const name2 = this.getName(nameRange2);
            heredocs.push(new heredoc_1.Heredoc(startRange2, name2, nameRange2, null, null));
            currentHeredoc++;
          }
        } else {
          return this.createSingleLineHeredocs(args);
        }
        return heredocs;
      }
      getDelimiterRange(startArg, name, candidateRange) {
        const text = this.document.getText(candidateRange);
        if (startArg.getValue().startsWith("<<-")) {
          let index = 0;
          while (text.charAt(index) === "	") {
            index++;
          }
          if (text.substring(index) === name) {
            return vscode_languageserver_types_1.Range.create(vscode_languageserver_types_1.Position.create(candidateRange.start.line, index), candidateRange.end);
          }
          return null;
        }
        return text === name ? candidateRange : null;
      }
    };
    exports.Instruction = Instruction;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/flagOption.js
var require_flagOption = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/flagOption.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.FlagOption = void 0;
    var FlagOption = class {
      static {
        __name(this, "FlagOption");
      }
      constructor(range, name, nameRange, value, valueRange) {
        this.range = range;
        this.name = name;
        this.nameRange = nameRange;
        this.value = value;
        this.valueRange = valueRange;
      }
      toString() {
        if (this.valueRange !== null) {
          return this.name + "=" + this.value;
        }
        return this.name;
      }
      getRange() {
        return this.range;
      }
      getName() {
        return this.name;
      }
      getNameRange() {
        return this.nameRange;
      }
      getValue() {
        return this.value;
      }
      getValueRange() {
        return this.valueRange;
      }
    };
    exports.FlagOption = FlagOption;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/flag.js
var require_flag = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/flag.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Flag = void 0;
    var vscode_languageserver_types_1 = (init_main(), __toCommonJS(main_exports));
    var flagOption_1 = require_flagOption();
    var Flag = class {
      static {
        __name(this, "Flag");
      }
      constructor(document, range, name, nameRange, value, valueRange) {
        this.options = [];
        this.range = range;
        this.name = name;
        this.nameRange = nameRange;
        this.value = value;
        this.valueRange = valueRange;
        if (this.value !== null) {
          let offset = document.offsetAt(valueRange.start);
          let nameStart = -1;
          let valueStart = -1;
          let hasOptions = false;
          for (let i = 0; i < value.length; i++) {
            switch (value.charAt(i)) {
              case "=":
                hasOptions = true;
                if (valueStart === -1) {
                  valueStart = i + 1;
                  break;
                }
                break;
              case ",":
                this.options.push(this.createFlagOption(document, value, offset, nameStart, valueStart, i));
                nameStart = -1;
                valueStart = -1;
                break;
              default:
                if (nameStart === -1) {
                  nameStart = i;
                }
                break;
            }
          }
          if (hasOptions && nameStart !== -1) {
            this.options.push(this.createFlagOption(document, value, offset, nameStart, valueStart, value.length));
          }
        }
      }
      createFlagOption(document, content, documentOffset, nameStart, valueStart, valueEnd) {
        const optionRange = vscode_languageserver_types_1.Range.create(document.positionAt(documentOffset + nameStart), document.positionAt(documentOffset + valueEnd));
        if (valueStart === -1) {
          return new flagOption_1.FlagOption(optionRange, content.substring(nameStart, valueEnd), optionRange, null, null);
        }
        return new flagOption_1.FlagOption(optionRange, content.substring(nameStart, valueStart - 1), vscode_languageserver_types_1.Range.create(document.positionAt(documentOffset + nameStart), document.positionAt(documentOffset + valueStart - 1)), content.substring(valueStart, valueEnd), vscode_languageserver_types_1.Range.create(document.positionAt(documentOffset + valueStart), document.positionAt(documentOffset + valueEnd)));
      }
      toString() {
        if (this.valueRange) {
          return "--" + this.name + "=" + this.value;
        }
        return "--" + this.name;
      }
      /**
       * Returns the range that encompasses this entire flag. This includes the
       * -- prefix in the beginning to the last character of the flag's value (if
       * it has been defined).
       *
       * @return the entire range of this flag
       */
      getRange() {
        return this.range;
      }
      /**
       * Returns the name of this flag. The name does not include the -- prefix.
       * Thus, for HEALTHCHECK's --interval flag, interval is the flag's name and
       * not --interval.
       *
       * @return this flag's name
       */
      getName() {
        return this.name;
      }
      /**
       * Returns the range that encompasses the flag's name
       *
       * @return the range containing the flag's name
       */
      getNameRange() {
        return this.nameRange;
      }
      /**
       * Returns the value that has been set to this flag. May be null if the
       * flag is invalid and has no value set like a --start-period. If the flag
       * is instead a --start-period= with an equals sign then the flag's value
       * is the empty string.
       *
       * @return this flag's value if it has been defined, null otherwise
       */
      getValue() {
        return this.value;
      }
      /**
       * Returns the range that encompasses this flag's value. If no value has
       * been set then null will be returned.
       *
       * @return the range containing this flag's value, or null if the flag
       *         has no value defined
       */
      getValueRange() {
        return this.valueRange;
      }
      getOption(name) {
        for (const option of this.options) {
          if (option.getName() === name) {
            return option;
          }
        }
        return null;
      }
      getOptions() {
        return this.options;
      }
      hasOptions() {
        return this.options.length > 0;
      }
    };
    exports.Flag = Flag;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/modifiableInstruction.js
var require_modifiableInstruction = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/modifiableInstruction.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ModifiableInstruction = void 0;
    var vscode_languageserver_types_1 = (init_main(), __toCommonJS(main_exports));
    var flag_1 = require_flag();
    var instruction_1 = require_instruction();
    var ModifiableInstruction = class extends instruction_1.Instruction {
      static {
        __name(this, "ModifiableInstruction");
      }
      constructor(document, range, dockerfile, escapeChar, instruction, instructionRange) {
        super(document, range, dockerfile, escapeChar, instruction, instructionRange);
      }
      getFlags() {
        if (!this.flags) {
          this.flags = [];
          for (let arg of this.getArguments()) {
            let value = arg.getValue();
            if (this.stopSearchingForFlags(value)) {
              return this.flags;
            } else if (value.indexOf("--") === 0) {
              let range = arg.getRange();
              let rawValue = this.document.getText().substring(this.document.offsetAt(range.start), this.document.offsetAt(range.end));
              let nameIndex = value.indexOf("=");
              let index = rawValue.indexOf("=");
              let firstMatch = false;
              let secondMatch = false;
              let startIndex = -1;
              nameSearchLoop: for (let i = 0; i < rawValue.length; i++) {
                switch (rawValue.charAt(i)) {
                  case "\\":
                  case " ":
                  case "	":
                  case "\r":
                  case "\n":
                    break;
                  case "-":
                    if (secondMatch) {
                      startIndex = i;
                      break nameSearchLoop;
                    } else if (firstMatch) {
                      secondMatch = true;
                    } else {
                      firstMatch = true;
                    }
                    break;
                  default:
                    startIndex = i;
                    break nameSearchLoop;
                }
              }
              let nameStart = this.document.positionAt(this.document.offsetAt(range.start) + startIndex);
              if (index === -1) {
                this.flags.push(new flag_1.Flag(this.document, range, value.substring(2), vscode_languageserver_types_1.Range.create(nameStart, range.end), null, null));
              } else if (index === value.length - 1) {
                let nameEnd = this.document.positionAt(this.document.offsetAt(range.start) + index);
                this.flags.push(new flag_1.Flag(this.document, range, value.substring(2, index), vscode_languageserver_types_1.Range.create(nameStart, nameEnd), "", vscode_languageserver_types_1.Range.create(range.end, range.end)));
              } else {
                let nameEnd = this.document.positionAt(this.document.offsetAt(range.start) + index);
                this.flags.push(new flag_1.Flag(this.document, range, value.substring(2, nameIndex), vscode_languageserver_types_1.Range.create(nameStart, nameEnd), value.substring(nameIndex + 1), vscode_languageserver_types_1.Range.create(this.document.positionAt(this.document.offsetAt(range.start) + index + 1), range.end)));
              }
            }
          }
        }
        return this.flags;
      }
      getArguments() {
        const args = super.getArguments();
        const flags = this.getFlags();
        if (flags.length === 0) {
          return args;
        }
        for (let i = 0; i < flags.length; i++) {
          args.shift();
        }
        return args;
      }
    };
    exports.ModifiableInstruction = ModifiableInstruction;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/jsonInstruction.js
var require_jsonInstruction = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/jsonInstruction.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.JSONInstruction = void 0;
    var vscode_languageserver_types_1 = (init_main(), __toCommonJS(main_exports));
    var argument_1 = require_argument();
    var jsonArgument_1 = require_jsonArgument();
    var modifiableInstruction_1 = require_modifiableInstruction();
    var JSONInstruction = class extends modifiableInstruction_1.ModifiableInstruction {
      static {
        __name(this, "JSONInstruction");
      }
      constructor(document, range, dockerfile, escapeChar, instruction, instructionRange) {
        super(document, range, dockerfile, escapeChar, instruction, instructionRange);
        this.openingBracket = null;
        this.closingBracket = null;
        this.jsonStrings = [];
        const argsContent = this.getRawArgumentsContent();
        if (argsContent === null) {
          return;
        }
        const args = this.getArguments();
        if (args.length === 1 && args[0].getValue() === "[]") {
          let argRange = args[0].getRange();
          this.openingBracket = new argument_1.Argument("[", vscode_languageserver_types_1.Range.create(argRange.start.line, argRange.start.character, argRange.start.line, argRange.start.character + 1));
          this.closingBracket = new argument_1.Argument("]", vscode_languageserver_types_1.Range.create(argRange.start.line, argRange.start.character + 1, argRange.end.line, argRange.end.character));
          return;
        } else if (args.length === 2 && args[0].getValue() === "[" && args[1].getValue() === "]") {
          this.openingBracket = args[0];
          this.closingBracket = args[1];
          return;
        }
        const argsOffset = document.offsetAt(this.getArgumentsRange().start);
        let start = -1;
        let last = "";
        let quoted = false;
        let escapedArg = "";
        argsCheck: for (let i = 0; i < argsContent.length; i++) {
          let char = argsContent.charAt(i);
          switch (char) {
            case "[":
              if (last === "") {
                this.openingBracket = new argument_1.Argument("[", vscode_languageserver_types_1.Range.create(document.positionAt(argsOffset + i), document.positionAt(argsOffset + i + 1)));
                last = "[";
              } else if (quoted) {
                escapedArg = escapedArg + char;
              } else {
                break argsCheck;
              }
              break;
            case '"':
              if (last === "[" || last === ",") {
                start = i;
                quoted = true;
                last = '"';
                escapedArg = escapedArg + char;
                continue;
              } else if (last === '"') {
                if (quoted) {
                  escapedArg = escapedArg + char;
                  quoted = false;
                  this.jsonStrings.push(new jsonArgument_1.JSONArgument(escapedArg, vscode_languageserver_types_1.Range.create(document.positionAt(argsOffset + start), document.positionAt(argsOffset + i + 1)), vscode_languageserver_types_1.Range.create(document.positionAt(argsOffset + start + 1), document.positionAt(argsOffset + i))));
                  escapedArg = "";
                } else {
                  break argsCheck;
                }
              } else {
                break argsCheck;
              }
              break;
            case ",":
              if (quoted) {
                escapedArg = escapedArg + char;
              } else {
                if (last === '"') {
                  last = ",";
                } else {
                  break argsCheck;
                }
              }
              break;
            case "]":
              if (quoted) {
                escapedArg = escapedArg + char;
              } else if (last !== "") {
                this.closingBracket = new argument_1.Argument("]", vscode_languageserver_types_1.Range.create(document.positionAt(argsOffset + i), document.positionAt(argsOffset + i + 1)));
                break argsCheck;
              }
              break;
            case " ":
            case "	":
              break;
            case "\\":
              if (quoted) {
                switch (argsContent.charAt(i + 1)) {
                  case '"':
                  case "\\":
                    escapedArg = escapedArg + argsContent.charAt(i + 1);
                    i++;
                    continue;
                  case " ":
                  case "	":
                    escapeCheck: for (let j = i + 2; j < argsContent.length; j++) {
                      switch (argsContent.charAt(j)) {
                        case "\r":
                          j++;
                        case "\n":
                          i = j;
                          continue argsCheck;
                        case " ":
                        case "	":
                          break;
                        default:
                          break escapeCheck;
                      }
                    }
                    break;
                  case "\r":
                    i++;
                  default:
                    i++;
                    continue;
                }
              } else {
                escapeCheck: for (let j = i + 1; j < argsContent.length; j++) {
                  switch (argsContent.charAt(j)) {
                    case "\r":
                      j++;
                    case "\n":
                      i = j;
                      continue argsCheck;
                    case " ":
                    case "	":
                      break;
                    default:
                      break escapeCheck;
                  }
                }
              }
              break argsCheck;
            default:
              if (!quoted) {
                break argsCheck;
              }
              escapedArg = escapedArg + char;
              break;
          }
        }
      }
      stopSearchingForFlags(_value) {
        return true;
      }
      getOpeningBracket() {
        return this.openingBracket;
      }
      getJSONStrings() {
        return this.jsonStrings;
      }
      getClosingBracket() {
        return this.closingBracket;
      }
    };
    exports.JSONInstruction = JSONInstruction;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/add.js
var require_add = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/add.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Add = void 0;
    var jsonInstruction_1 = require_jsonInstruction();
    var Add = class extends jsonInstruction_1.JSONInstruction {
      static {
        __name(this, "Add");
      }
      constructor(document, range, dockerfile, escapeChar, instruction, instructionRange) {
        super(document, range, dockerfile, escapeChar, instruction, instructionRange);
      }
      stopSearchingForFlags(argument) {
        return argument.indexOf("--") === -1;
      }
    };
    exports.Add = Add;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/property.js
var require_property = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/property.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Property = void 0;
    var vscode_languageserver_types_1 = (init_main(), __toCommonJS(main_exports));
    var util_1 = require_util();
    var Property = class _Property {
      static {
        __name(this, "Property");
      }
      constructor(document, escapeChar, arg, arg2) {
        this.assignmentOperatorRange = null;
        this.assignmentOperator = null;
        this.valueRange = null;
        this.value = null;
        this.document = document;
        this.escapeChar = escapeChar;
        this.nameRange = _Property.getNameRange(document, arg);
        let value = document.getText().substring(document.offsetAt(this.nameRange.start), document.offsetAt(this.nameRange.end));
        this.name = _Property.getValue(value, escapeChar);
        if (arg2) {
          this.valueRange = arg2.getRange();
          value = document.getText().substring(document.offsetAt(this.valueRange.start), document.offsetAt(this.valueRange.end));
          this.value = _Property.getValue(value, escapeChar);
          this.range = vscode_languageserver_types_1.Range.create(this.nameRange.start, this.valueRange.end);
        } else {
          let argRange = arg.getRange();
          if (this.nameRange.start.line === argRange.start.line && this.nameRange.start.character === argRange.start.character && this.nameRange.end.line === argRange.end.line && this.nameRange.end.character === argRange.end.character) {
          } else {
            this.valueRange = _Property.getValueRange(document, arg);
            value = document.getText().substring(document.offsetAt(this.valueRange.start), document.offsetAt(this.valueRange.end));
            this.value = _Property.getValue(value, escapeChar);
            this.assignmentOperatorRange = vscode_languageserver_types_1.Range.create(this.nameRange.end, this.valueRange.start);
            this.assignmentOperator = "=";
          }
          this.range = argRange;
        }
      }
      getRange() {
        return this.range;
      }
      getName() {
        return this.name;
      }
      getNameRange() {
        return this.nameRange;
      }
      getValue() {
        return this.value;
      }
      getValueRange() {
        return this.valueRange;
      }
      /**
       * Retrieves the operator used for delimiting between the name and
       * value of this property. This will either be the "=" character
       * or null if a character was not used or if this property has no
       * value defined.
       */
      getAssignmentOperator() {
        return this.assignmentOperator;
      }
      getAssignmentOperatorRange() {
        return this.assignmentOperatorRange;
      }
      /**
       * Returns the value of this property including any enclosing
       * single or double quotes and relevant escape characters.
       * Escaped newlines and its associated contiguous whitespace
       * characters however will not be returned as they are deemed to
       * be uninteresting to clients trying to return a Dockerfile.
       *
       * @return the unescaped value of this property or null if this
       *         property has no associated value
       */
      getUnescapedValue() {
        if (this.valueRange === null) {
          return null;
        }
        let escaped = false;
        let rawValue = "";
        let value = this.document.getText().substring(this.document.offsetAt(this.valueRange.start), this.document.offsetAt(this.valueRange.end));
        rawLoop: for (let i = 0; i < value.length; i++) {
          let char = value.charAt(i);
          switch (char) {
            case this.escapeChar:
              for (let j = i + 1; j < value.length; j++) {
                switch (value.charAt(j)) {
                  case "\r":
                    j++;
                  case "\n":
                    escaped = true;
                    i = j;
                    continue rawLoop;
                  case " ":
                  case "	":
                    break;
                  default:
                    rawValue = rawValue + char;
                    continue rawLoop;
                }
              }
              rawValue = rawValue + char;
              break;
            case "\r":
            case "\n":
              break;
            case " ":
            case "	":
              if (!escaped) {
                rawValue = rawValue + char;
              }
              break;
            case "#":
              if (escaped) {
                for (let j = i + 1; j < value.length; j++) {
                  switch (value.charAt(j)) {
                    case "\r":
                      j++;
                    case "\n":
                      i = j;
                      continue rawLoop;
                  }
                }
              } else {
                rawValue = rawValue + char;
              }
              break;
            default:
              rawValue = rawValue + char;
              escaped = false;
              break;
          }
        }
        return rawValue;
      }
      static getNameRange(document, arg) {
        let value = arg.getValue();
        let index = value.indexOf("=");
        if (index !== -1) {
          let initial = value.charAt(0);
          let before = value.charAt(index - 1);
          if (initial === '"' && before === '"' || initial === "'" && before === "'" || initial !== '"' && initial !== "'") {
            return vscode_languageserver_types_1.Range.create(arg.getRange().start, document.positionAt(document.offsetAt(arg.getRange().start) + index));
          }
        }
        return arg.getRange();
      }
      static getValueRange(document, arg) {
        return vscode_languageserver_types_1.Range.create(document.positionAt(document.offsetAt(arg.getRange().start) + arg.getValue().indexOf("=") + 1), document.positionAt(document.offsetAt(arg.getRange().end)));
      }
      /**
       * Returns the actual value of this key-value pair. The value will
       * have its escape characters removed if applicable. If the value
       * spans multiple lines and there are comments nested within the
       * lines, they too will be removed.
       *
       * @return the value that this key-value pair will actually be, may
       *         be null if no value is defined, may be the empty string
       *         if the value only consists of whitespace
       */
      static getValue(value, escapeChar) {
        let escaped = false;
        const skip = util_1.Util.findLeadingNonWhitespace(value, escapeChar);
        if (skip !== 0 && value.charAt(skip) === "#") {
          escaped = true;
        }
        value = value.substring(skip);
        let first = value.charAt(0);
        let last = value.charAt(value.length - 1);
        let literal = first === "'" || first === '"';
        let inSingle = first === "'" && last === "'";
        let inDouble = false;
        if (first === '"') {
          for (let i = 1; i < value.length; i++) {
            if (value.charAt(i) === escapeChar) {
              i++;
            } else if (value.charAt(i) === '"' && i === value.length - 1) {
              inDouble = true;
            }
          }
        }
        if (inSingle || inDouble) {
          value = value.substring(1, value.length - 1);
        }
        let commentCheck = -1;
        let escapedValue = "";
        let start = 0;
        parseValue: for (let i = 0; i < value.length; i++) {
          let char = value.charAt(i);
          switch (char) {
            case escapeChar:
              if (i + 1 === value.length) {
                escapedValue = escapedValue + escapeChar;
                break parseValue;
              }
              char = value.charAt(i + 1);
              if (char === " " || char === "	") {
                whitespaceCheck: for (let j = i + 2; j < value.length; j++) {
                  let char2 = value.charAt(j);
                  switch (char2) {
                    case " ":
                    case "	":
                      break;
                    case "\r":
                      j++;
                    case "\n":
                      escaped = true;
                      i = j;
                      continue parseValue;
                    default:
                      if (!inDouble && !inSingle && !literal) {
                        if (char2 === escapeChar) {
                          escapedValue = escapedValue + char;
                          i = i + 1;
                        } else {
                          escapedValue = escapedValue + char + char2;
                          i = j;
                        }
                        continue parseValue;
                      }
                      break whitespaceCheck;
                  }
                }
              }
              if (inDouble) {
                if (char === "\r") {
                  escaped = true;
                  i = i + 2;
                } else if (char === "\n") {
                  escaped = true;
                  i++;
                } else if (char !== '"') {
                  if (char === escapeChar) {
                    i++;
                  }
                  escapedValue = escapedValue + escapeChar;
                }
                continue parseValue;
              } else if (inSingle || literal) {
                if (char === "\r") {
                  escaped = true;
                  i = i + 2;
                } else if (char === "\n") {
                  escaped = true;
                  i++;
                } else {
                  escapedValue = escapedValue + escapeChar;
                }
                continue parseValue;
              } else if (char === escapeChar) {
                escapedValue = escapedValue + escapeChar;
                i++;
              } else if (char === "\r") {
                escaped = true;
                i = i + 2;
              } else if (char === "\n") {
                escaped = true;
                i++;
                start = i;
              } else {
                escapedValue = escapedValue + char;
                i++;
              }
              break;
            case " ":
            case "	":
              if (escaped && commentCheck === -1) {
                commentCheck = i;
              }
              escapedValue = escapedValue + char;
              break;
            case "\r":
              i++;
            case "\n":
              if (escaped && commentCheck !== -1) {
                escapedValue = escapedValue.substring(0, escapedValue.length - (i - commentCheck - 1));
                commentCheck = -1;
              }
              break;
            case "#":
              if (escaped) {
                if (commentCheck !== -1) {
                  escapedValue = escapedValue.substring(0, escapedValue.length - (i - commentCheck));
                  commentCheck = -1;
                }
                newlineCheck: for (let j = i + 1; j < value.length; j++) {
                  switch (value.charAt(j)) {
                    case "\r":
                      j++;
                    case "\n":
                      i = j;
                      break newlineCheck;
                  }
                }
                continue parseValue;
              }
            default:
              if (escaped) {
                escaped = false;
                commentCheck = -1;
              }
              escapedValue = escapedValue + char;
              break;
          }
        }
        return escapedValue;
      }
    };
    exports.Property = Property;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/propertyInstruction.js
var require_propertyInstruction = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/propertyInstruction.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.PropertyInstruction = void 0;
    var vscode_languageserver_types_1 = (init_main(), __toCommonJS(main_exports));
    var instruction_1 = require_instruction();
    var property_1 = require_property();
    var argument_1 = require_argument();
    var util_1 = require_util();
    var PropertyInstruction = class extends instruction_1.Instruction {
      static {
        __name(this, "PropertyInstruction");
      }
      constructor(document, range, dockerfile, escapeChar, instruction, instructionRange) {
        super(document, range, dockerfile, escapeChar, instruction, instructionRange);
        this.properties = void 0;
      }
      getProperties() {
        if (this.properties === void 0) {
          let args = this.getPropertyArguments();
          if (args.length === 0) {
            this.properties = [];
          } else if (args.length === 1) {
            this.properties = [new property_1.Property(this.document, this.escapeChar, args[0])];
          } else if (args.length === 2) {
            if (args[0].getValue().indexOf("=") === -1) {
              this.properties = [new property_1.Property(this.document, this.escapeChar, args[0], args[1])];
            } else {
              this.properties = [
                new property_1.Property(this.document, this.escapeChar, args[0]),
                new property_1.Property(this.document, this.escapeChar, args[1])
              ];
            }
          } else if (args[0].getValue().indexOf("=") === -1) {
            let text = this.document.getText();
            let start = args[1].getRange().start;
            let end = args[args.length - 1].getRange().end;
            text = text.substring(this.document.offsetAt(start), this.document.offsetAt(end));
            this.properties = [new property_1.Property(this.document, this.escapeChar, args[0], new argument_1.Argument(text, vscode_languageserver_types_1.Range.create(args[1].getRange().start, args[args.length - 1].getRange().end)))];
          } else {
            this.properties = [];
            for (let i = 0; i < args.length; i++) {
              this.properties.push(new property_1.Property(this.document, this.escapeChar, args[i]));
            }
          }
        }
        return this.properties;
      }
      /**
       * Goes from the back of the string and returns the first
       * non-whitespace character that is found. If an escape character
       * is found with newline characters, the escape character will
       * not be considered a non-whitespace character and its index in
       * the string will not be returned.
       *
       * @param content the string to search through
       * @return the index in the string for the first non-whitespace
       *         character when searching from the end of the string
       */
      findTrailingNonWhitespace(content) {
        let index = content.length;
        whitespaceCheck: for (let i = content.length - 1; i >= 0; i--) {
          switch (content.charAt(i)) {
            case " ":
            case "	":
              continue;
            case "\n":
              if (content.charAt(i - 1) === "\r") {
                i = i - 1;
              }
            case "\r":
              newlineCheck: for (let j = i - 1; j >= 0; j--) {
                switch (content.charAt(j)) {
                  case " ":
                  case "	":
                  case "\r":
                  case "\n":
                  case this.escapeChar:
                    continue;
                  default:
                    index = j;
                    break newlineCheck;
                }
              }
              break whitespaceCheck;
            default:
              index = i;
              break whitespaceCheck;
          }
        }
        return index;
      }
      getPropertyArguments() {
        const args = [];
        let range = this.getInstructionRange();
        let instructionNameEndOffset = this.document.offsetAt(range.end);
        let extra = instructionNameEndOffset - this.document.offsetAt(range.start);
        let content = this.getTextContent();
        let fullArgs = content.substring(extra);
        let start = util_1.Util.findLeadingNonWhitespace(fullArgs, this.escapeChar);
        if (start === -1) {
          return [];
        }
        const startPosition = this.document.positionAt(instructionNameEndOffset + start);
        let escaped = range.start.line !== startPosition.line;
        let endingEscape = false;
        let mark = -1;
        let end = this.findTrailingNonWhitespace(fullArgs);
        content = fullArgs.substring(start, end + 1);
        let argStart = escaped ? -1 : 0;
        let spaced = false;
        argumentLoop: for (let i = 0; i < content.length; i++) {
          let char = content.charAt(i);
          switch (char) {
            case this.escapeChar:
              if (i + 1 === content.length) {
                endingEscape = true;
                break argumentLoop;
              }
              if (!escaped) {
                mark = i;
              }
              switch (content.charAt(i + 1)) {
                case " ":
                case "	":
                  if (!util_1.Util.isWhitespace(content.charAt(i + 2))) {
                    i = i + 1;
                    continue argumentLoop;
                  }
                  whitespaceCheck: for (let j = i + 2; j < content.length; j++) {
                    switch (content.charAt(j)) {
                      case "\r":
                        j++;
                      case "\n":
                        escaped = true;
                        i = j;
                        continue argumentLoop;
                      case " ":
                      case "	":
                        break;
                      default:
                        args.push(new argument_1.Argument(content.substring(argStart, i), vscode_languageserver_types_1.Range.create(this.document.positionAt(instructionNameEndOffset + start + argStart), this.document.positionAt(instructionNameEndOffset + start + i + 2))));
                        argStart = j;
                        break whitespaceCheck;
                    }
                  }
                  i = argStart - 1;
                  continue argumentLoop;
                case "\r":
                  i++;
                case "\n":
                  escaped = true;
                  i = i + 1;
                  continue argumentLoop;
                case this.escapeChar:
                  if (argStart === -1) {
                    argStart = i;
                  }
                  i = i + 1;
                  continue argumentLoop;
                default:
                  if (argStart === -1) {
                    argStart = i;
                  }
                  continue argumentLoop;
              }
            case "'":
            case '"':
              if (spaced) {
                this.createSpacedArgument(argStart, args, content, mark, instructionNameEndOffset, start);
                argStart = i;
                spaced = false;
              }
              if (argStart === -1) {
                argStart = i;
              }
              for (let j = i + 1; j < content.length; j++) {
                switch (content.charAt(j)) {
                  case char:
                    if (content.charAt(j + 1) !== " " && content.charAt(j + 1) !== "") {
                      i = j;
                      continue argumentLoop;
                    }
                    args.push(new argument_1.Argument(content.substring(argStart, j + 1), vscode_languageserver_types_1.Range.create(this.document.positionAt(instructionNameEndOffset + start + argStart), this.document.positionAt(instructionNameEndOffset + start + j + 1))));
                    i = j;
                    argStart = -1;
                    continue argumentLoop;
                  case this.escapeChar:
                    j++;
                    break;
                }
              }
              break argumentLoop;
            case " ":
            case "	":
              if (escaped) {
                if (argStart !== -1) {
                  spaced = true;
                }
              } else if (argStart !== -1) {
                args.push(new argument_1.Argument(content.substring(argStart, i), vscode_languageserver_types_1.Range.create(this.document.positionAt(instructionNameEndOffset + start + argStart), this.document.positionAt(instructionNameEndOffset + start + i))));
                argStart = -1;
              }
              break;
            case "\r":
              i++;
            case "\n":
              spaced = false;
              break;
            case "#":
              if (escaped) {
                for (let j = i + 1; j < content.length; j++) {
                  switch (content.charAt(j)) {
                    case "\r":
                      j++;
                    case "\n":
                      i = j;
                      spaced = false;
                      continue argumentLoop;
                  }
                }
                if (argStart !== -1) {
                  let value = content.substring(argStart, mark);
                  args.push(new argument_1.Argument(value, vscode_languageserver_types_1.Range.create(this.document.positionAt(instructionNameEndOffset + start + argStart), this.document.positionAt(instructionNameEndOffset + start + mark))));
                  argStart = -1;
                }
                break argumentLoop;
              } else if (argStart === -1) {
                argStart = i;
              }
              break;
            default:
              if (spaced) {
                this.createSpacedArgument(argStart, args, content, mark, instructionNameEndOffset, start);
                argStart = i;
                spaced = false;
              }
              escaped = false;
              if (argStart === -1) {
                argStart = i;
              }
              if (char === "$" && content.charAt(i + 1) === "{") {
                let singleQuotes = false;
                let doubleQuotes = false;
                let escaped2 = false;
                for (let j = i + 1; j < content.length; j++) {
                  switch (content.charAt(j)) {
                    case this.escapeChar:
                      escaped2 = true;
                      break;
                    case "\r":
                    case "\n":
                      break;
                    case "'":
                      singleQuotes = !singleQuotes;
                      escaped2 = false;
                      break;
                    case '"':
                      doubleQuotes = !doubleQuotes;
                      escaped2 = false;
                      break;
                    case " ":
                    case "	":
                      if (escaped2 || singleQuotes || doubleQuotes) {
                        break;
                      }
                      i = j - 1;
                      continue argumentLoop;
                    case "}":
                      i = j;
                      continue argumentLoop;
                    default:
                      escaped2 = false;
                      break;
                  }
                }
                break argumentLoop;
              }
              break;
          }
        }
        if (argStart !== -1 && argStart !== content.length) {
          let end2 = endingEscape ? content.length - 1 : content.length;
          let value = content.substring(argStart, end2);
          args.push(new argument_1.Argument(value, vscode_languageserver_types_1.Range.create(this.document.positionAt(instructionNameEndOffset + start + argStart), this.document.positionAt(instructionNameEndOffset + start + end2))));
        }
        return args;
      }
      createSpacedArgument(argStart, args, content, mark, instructionNameEndOffset, start) {
        if (argStart !== -1) {
          args.push(new argument_1.Argument(content.substring(argStart, mark), vscode_languageserver_types_1.Range.create(this.document.positionAt(instructionNameEndOffset + start + argStart), this.document.positionAt(instructionNameEndOffset + start + mark))));
        }
      }
    };
    exports.PropertyInstruction = PropertyInstruction;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/arg.js
var require_arg = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/arg.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Arg = void 0;
    var property_1 = require_property();
    var propertyInstruction_1 = require_propertyInstruction();
    var Arg = class extends propertyInstruction_1.PropertyInstruction {
      static {
        __name(this, "Arg");
      }
      constructor(document, range, dockerfile, escapeChar, instruction, instructionRange) {
        super(document, range, dockerfile, escapeChar, instruction, instructionRange);
        this.property = null;
        const args = this.getPropertyArguments();
        if (args.length === 1) {
          this.property = new property_1.Property(this.document, this.escapeChar, args[0]);
        } else {
          this.property = null;
        }
      }
      /**
       * Returns the variable defined by this ARG. This may be null if
       * this ARG instruction is malformed and has no variable
       * declaration.
       */
      getProperty() {
        return this.property;
      }
    };
    exports.Arg = Arg;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/cmd.js
var require_cmd = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/cmd.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Cmd = void 0;
    var jsonInstruction_1 = require_jsonInstruction();
    var Cmd = class extends jsonInstruction_1.JSONInstruction {
      static {
        __name(this, "Cmd");
      }
      constructor(document, range, dockerfile, escapeChar, instruction, instructionRange) {
        super(document, range, dockerfile, escapeChar, instruction, instructionRange);
      }
    };
    exports.Cmd = Cmd;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/copy.js
var require_copy = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/copy.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Copy = void 0;
    var jsonInstruction_1 = require_jsonInstruction();
    var Copy = class extends jsonInstruction_1.JSONInstruction {
      static {
        __name(this, "Copy");
      }
      constructor(document, range, dockerfile, escapeChar, instruction, instructionRange) {
        super(document, range, dockerfile, escapeChar, instruction, instructionRange);
      }
      stopSearchingForFlags(argument) {
        return argument.indexOf("--") === -1;
      }
      getFromFlag() {
        let flags = super.getFlags();
        return flags.length === 1 && flags[0].getName() === "from" ? flags[0] : null;
      }
      /**
       * Returns there here-documents that are defined in this RUN
       * instruction.
       *
       * This API is experimental and subject to change.
       */
      getHeredocs() {
        return super.getHeredocs();
      }
    };
    exports.Copy = Copy;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/env.js
var require_env = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/env.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Env = void 0;
    var propertyInstruction_1 = require_propertyInstruction();
    var Env = class extends propertyInstruction_1.PropertyInstruction {
      static {
        __name(this, "Env");
      }
      constructor(document, range, dockerfile, escapeChar, instruction, instructionRange) {
        super(document, range, dockerfile, escapeChar, instruction, instructionRange);
      }
      getProperties() {
        return super.getProperties();
      }
    };
    exports.Env = Env;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/entrypoint.js
var require_entrypoint = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/entrypoint.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Entrypoint = void 0;
    var jsonInstruction_1 = require_jsonInstruction();
    var Entrypoint = class extends jsonInstruction_1.JSONInstruction {
      static {
        __name(this, "Entrypoint");
      }
      constructor(document, range, dockerfile, escapeChar, instruction, instructionRange) {
        super(document, range, dockerfile, escapeChar, instruction, instructionRange);
      }
    };
    exports.Entrypoint = Entrypoint;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/from.js
var require_from = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/from.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.From = void 0;
    var vscode_languageserver_types_1 = (init_main(), __toCommonJS(main_exports));
    var modifiableInstruction_1 = require_modifiableInstruction();
    var From = class extends modifiableInstruction_1.ModifiableInstruction {
      static {
        __name(this, "From");
      }
      constructor(document, range, dockerfile, escapeChar, instruction, instructionRange) {
        super(document, range, dockerfile, escapeChar, instruction, instructionRange);
      }
      stopSearchingForFlags(argument) {
        return argument.indexOf("--") === -1;
      }
      getImage() {
        const args = this.getArguments();
        return args.length > 0 ? args[0].getValue() : null;
      }
      /**
       * Returns the name of the image that will be used as the base image.
       *
       * @return the base image's name, or null if unspecified
       */
      getImageName() {
        const imageName = this.getRangeContent(this.getImageNameRange());
        if (imageName === null) {
          return null;
        }
        let commented = false;
        let escaped = false;
        let name = "";
        for (let i = 0; i < imageName.length; i++) {
          const ch = imageName.charAt(i);
          switch (ch) {
            case this.escapeChar:
              escaped = true;
              break;
            case "\r":
              continue;
            case "\n":
              commented = false;
              break;
            case " ":
            case "	":
              break;
            case "#":
              if (escaped) {
                commented = true;
              } else {
                name = name + ch;
                escaped = false;
              }
              break;
            default:
              if (!commented) {
                name = name + ch;
                escaped = false;
              }
              break;
          }
        }
        return name;
      }
      /**
       * Returns the range that covers the name of the image used by
       * this instruction.
       *
       * @return the range of the name of this instruction's argument,
       *         or null if no image has been specified
       */
      getImageNameRange() {
        let range = this.getImageRange();
        if (range) {
          let registryRange = this.getRegistryRange();
          if (registryRange) {
            range.start = this.document.positionAt(this.document.offsetAt(registryRange.end) + 1);
          }
          let tagRange = this.getImageTagRange();
          let digestRange = this.getImageDigestRange();
          if (tagRange === null) {
            if (digestRange !== null) {
              range.end = this.document.positionAt(this.document.offsetAt(digestRange.start) - 1);
            }
          } else {
            range.end = this.document.positionAt(this.document.offsetAt(tagRange.start) - 1);
          }
          return range;
        }
        return null;
      }
      /**
       * Returns the range that covers the image argument of this
       * instruction. This includes the tag or digest of the image if
       * it has been specified by the instruction.
       *
       * @return the range of the image argument, or null if no image
       *         has been specified
       */
      getImageRange() {
        let args = this.getArguments();
        return args.length !== 0 ? args[0].getRange() : null;
      }
      getImageTag() {
        return this.getRangeContent(this.getImageTagRange());
      }
      /**
       * Returns the range in the document that the tag of the base
       * image encompasses.
       *
       * @return the base image's tag's range in the document, or null
       *         if no tag has been specified
       */
      getImageTagRange() {
        const range = this.getImageRange();
        if (range) {
          const rangeStartOffset = this.document.offsetAt(range.start);
          const content = this.getRangeContent(range);
          const atIndex = this.indexOf(rangeStartOffset, content, "@");
          const slashIndex = content.indexOf("/");
          if (atIndex === -1) {
            const colonIndex = this.lastIndexOf(rangeStartOffset, content, ":");
            if (colonIndex > slashIndex) {
              return vscode_languageserver_types_1.Range.create(this.document.positionAt(rangeStartOffset + colonIndex + 1), range.end);
            }
          }
          const subcontent = content.substring(0, atIndex);
          const subcolonIndex = subcontent.indexOf(":");
          if (subcolonIndex === -1) {
            return null;
          }
          if (slashIndex === -1) {
            return vscode_languageserver_types_1.Range.create(this.document.positionAt(rangeStartOffset + subcolonIndex + 1), this.document.positionAt(rangeStartOffset + atIndex));
          }
          if (subcolonIndex < slashIndex) {
            return null;
          }
          return vscode_languageserver_types_1.Range.create(this.document.positionAt(rangeStartOffset + subcolonIndex + 1), this.document.positionAt(rangeStartOffset + subcontent.length));
        }
        return null;
      }
      getImageDigest() {
        return this.getRangeContent(this.getImageDigestRange());
      }
      /**
       * Returns the range in the document that the digest of the base
       * image encompasses.
       *
       * @return the base image's digest's range in the document, or null
       *         if no digest has been specified
       */
      getImageDigestRange() {
        let range = this.getImageRange();
        if (range) {
          let content = this.getRangeContent(range);
          let index = this.lastIndexOf(this.document.offsetAt(range.start), content, "@");
          if (index !== -1) {
            return vscode_languageserver_types_1.Range.create(range.start.line, range.start.character + index + 1, range.end.line, range.end.character);
          }
        }
        return null;
      }
      indexOf(documentOffset, content, searchString) {
        let index = content.indexOf(searchString);
        const variables = this.getVariables();
        for (let i = 0; i < variables.length; i++) {
          const position = documentOffset + index;
          const variableRange = variables[i].getRange();
          if (this.document.offsetAt(variableRange.start) < position && position < this.document.offsetAt(variableRange.end)) {
            const offset = this.document.offsetAt(variableRange.end) - documentOffset;
            const substring = content.substring(offset);
            const subIndex = substring.indexOf(searchString);
            if (subIndex === -1) {
              return -1;
            }
            index = subIndex + offset;
            i = -1;
            continue;
          }
        }
        return index;
      }
      lastIndexOf(documentOffset, content, searchString) {
        let index = content.lastIndexOf(searchString);
        const variables = this.getVariables();
        for (let i = 0; i < variables.length; i++) {
          const position = documentOffset + index;
          const variableRange = variables[i].getRange();
          if (this.document.offsetAt(variableRange.start) < position && position < this.document.offsetAt(variableRange.end)) {
            index = content.substring(0, index).lastIndexOf(searchString);
            if (index === -1) {
              return -1;
            }
            i = -1;
            continue;
          }
        }
        return index;
      }
      getRegistry() {
        return this.getRangeContent(this.getRegistryRange());
      }
      getRegistryRange() {
        const range = this.getImageRange();
        if (range) {
          const tagRange = this.getImageTagRange();
          const digestRange = this.getImageDigestRange();
          if (tagRange === null) {
            if (digestRange !== null) {
              range.end = this.document.positionAt(this.document.offsetAt(digestRange.start) - 1);
            }
          } else {
            range.end = this.document.positionAt(this.document.offsetAt(tagRange.start) - 1);
          }
          const content = this.getRangeContent(range);
          const rangeStart = this.document.offsetAt(range.start);
          const startingSlashIndex = this.indexOf(rangeStart, content, "/");
          if (startingSlashIndex === -1) {
            return null;
          }
          const portIndex = this.indexOf(rangeStart, content, ":");
          const dotIndex = this.indexOf(rangeStart, content, ".");
          if (portIndex !== -1 || dotIndex !== -1) {
            return vscode_languageserver_types_1.Range.create(range.start, this.document.positionAt(rangeStart + startingSlashIndex));
          }
          const registry = content.substring(0, startingSlashIndex);
          if (registry === "localhost") {
            return vscode_languageserver_types_1.Range.create(range.start, this.document.positionAt(rangeStart + startingSlashIndex));
          }
        }
        return null;
      }
      getBuildStage() {
        let range = this.getBuildStageRange();
        return range === null ? null : this.getRangeContent(range);
      }
      getBuildStageRange() {
        let args = this.getArguments();
        if (args.length > 2 && args[1].getValue().toUpperCase() === "AS") {
          return args[2].getRange();
        }
        return null;
      }
      getPlatformFlag() {
        let flags = super.getFlags();
        return flags.length === 1 && flags[0].getName() === "platform" ? flags[0] : null;
      }
    };
    exports.From = From;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/healthcheck.js
var require_healthcheck = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/healthcheck.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Healthcheck = void 0;
    var modifiableInstruction_1 = require_modifiableInstruction();
    var Healthcheck = class extends modifiableInstruction_1.ModifiableInstruction {
      static {
        __name(this, "Healthcheck");
      }
      constructor(document, range, dockerfile, escapeChar, instruction, instructionRange) {
        super(document, range, dockerfile, escapeChar, instruction, instructionRange);
      }
      stopSearchingForFlags(argument) {
        argument = argument.toUpperCase();
        return argument === "CMD" || argument === "NONE";
      }
      getSubcommand() {
        let args = this.getArguments();
        return args.length !== 0 ? args[0] : null;
      }
    };
    exports.Healthcheck = Healthcheck;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/label.js
var require_label = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/label.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Label = void 0;
    var propertyInstruction_1 = require_propertyInstruction();
    var util_1 = require_util();
    var Label = class extends propertyInstruction_1.PropertyInstruction {
      static {
        __name(this, "Label");
      }
      constructor(document, range, dockerfile, escapeChar, instruction, instructionRange) {
        super(document, range, dockerfile, escapeChar, instruction, instructionRange);
      }
      getVariables() {
        const variables = super.getVariables();
        const properties = this.getProperties();
        for (const property of properties) {
          const value = property.getUnescapedValue();
          if (value !== null && value.length > 2 && value.charAt(0) === "'" && value.charAt(value.length - 1) === "'") {
            const range = property.getValueRange();
            for (let i = 0; i < variables.length; i++) {
              if (util_1.Util.isInsideRange(variables[i].getRange().start, range)) {
                variables.splice(i, 1);
                i--;
              }
            }
          }
        }
        return variables;
      }
      getProperties() {
        return super.getProperties();
      }
    };
    exports.Label = Label;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/onbuild.js
var require_onbuild = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/onbuild.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Onbuild = void 0;
    var vscode_languageserver_types_1 = (init_main(), __toCommonJS(main_exports));
    var parser_1 = require_parser();
    var instruction_1 = require_instruction();
    var Onbuild = class extends instruction_1.Instruction {
      static {
        __name(this, "Onbuild");
      }
      constructor(document, range, dockerfile, escapeChar, instruction, instructionRange) {
        super(document, range, dockerfile, escapeChar, instruction, instructionRange);
      }
      getTrigger() {
        let trigger = this.getTriggerWord();
        return trigger === null ? null : trigger.toUpperCase();
      }
      getTriggerWord() {
        return this.getRangeContent(this.getTriggerRange());
      }
      getTriggerRange() {
        let args = this.getArguments();
        return args.length > 0 ? args[0].getRange() : null;
      }
      getTriggerInstruction() {
        let triggerRange = this.getTriggerRange();
        if (triggerRange === null) {
          return null;
        }
        let args = this.getArguments();
        return parser_1.Parser.createInstruction(this.document, this.dockerfile, this.escapeChar, vscode_languageserver_types_1.Range.create(args[0].getRange().start, this.getRange().end), this.getTriggerWord(), triggerRange);
      }
    };
    exports.Onbuild = Onbuild;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/run.js
var require_run = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/run.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Run = void 0;
    var jsonInstruction_1 = require_jsonInstruction();
    var Run = class extends jsonInstruction_1.JSONInstruction {
      static {
        __name(this, "Run");
      }
      constructor(document, range, dockerfile, escapeChar, instruction, instructionRange) {
        super(document, range, dockerfile, escapeChar, instruction, instructionRange);
      }
      stopSearchingForFlags(argument) {
        return argument.indexOf("--") === -1;
      }
      /**
       * Returns there here-documents that are defined in this RUN
       * instruction.
       *
       * This API is experimental and subject to change.
       */
      getHeredocs() {
        return super.getHeredocs();
      }
    };
    exports.Run = Run;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/shell.js
var require_shell = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/shell.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Shell = void 0;
    var jsonInstruction_1 = require_jsonInstruction();
    var Shell = class extends jsonInstruction_1.JSONInstruction {
      static {
        __name(this, "Shell");
      }
      constructor(document, range, dockerfile, escapeChar, instruction, instructionRange) {
        super(document, range, dockerfile, escapeChar, instruction, instructionRange);
      }
    };
    exports.Shell = Shell;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/stopsignal.js
var require_stopsignal = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/stopsignal.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Stopsignal = void 0;
    var instruction_1 = require_instruction();
    var Stopsignal = class extends instruction_1.Instruction {
      static {
        __name(this, "Stopsignal");
      }
      constructor(document, range, dockerfile, escapeChar, instruction, instructionRange) {
        super(document, range, dockerfile, escapeChar, instruction, instructionRange);
      }
    };
    exports.Stopsignal = Stopsignal;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/workdir.js
var require_workdir = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/workdir.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Workdir = void 0;
    var instruction_1 = require_instruction();
    var Workdir = class extends instruction_1.Instruction {
      static {
        __name(this, "Workdir");
      }
      constructor(document, range, dockerfile, escapeChar, instruction, instructionRange) {
        super(document, range, dockerfile, escapeChar, instruction, instructionRange);
      }
      /**
       * Returns the path that has been defined. Note that this path may
       * be absolute or relative depending on what was written in the
       * instruction.
       *
       * @return the working directory's path, or null if this
       *         instruction has no arguments
       */
      getPath() {
        return this.getArgumentsContent();
      }
      /**
       * Returns the absolute path that this instruction resolves to. The
       * function will inspect prior WORKDIR instructions in the current
       * image or another build stage in the Dockerfile to try to
       * determine this.
       *
       * @return the absolute path of the working directory, or null if
       *         this instruction has no arguments, or undefined if it
       *         cannot be determined because only relative paths could be
       *         found
       */
      getAbsolutePath() {
        const path = this.getPath();
        if (path === null || path.startsWith("/")) {
          return path;
        }
        const startLine = this.getRange().start.line;
        const hierarchy = this.dockerfile.getStageHierarchy(startLine);
        for (let i = hierarchy.length - 1; i >= 0; i--) {
          const workdirs = hierarchy[i].getWORKDIRs();
          for (let j = workdirs.length - 1; j >= 0; j--) {
            if (workdirs[j].getRange().start.line < startLine) {
              const parent = workdirs[j].getAbsolutePath();
              if (parent === void 0 || parent === null) {
                return void 0;
              }
              return parent.endsWith("/") ? parent + path : parent + "/" + path;
            }
          }
        }
        return void 0;
      }
    };
    exports.Workdir = Workdir;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/user.js
var require_user = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/user.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.User = void 0;
    var instruction_1 = require_instruction();
    var User = class extends instruction_1.Instruction {
      static {
        __name(this, "User");
      }
      constructor(document, range, dockerfile, escapeChar, instruction, instructionRange) {
        super(document, range, dockerfile, escapeChar, instruction, instructionRange);
      }
    };
    exports.User = User;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/volume.js
var require_volume = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/instructions/volume.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Volume = void 0;
    var jsonInstruction_1 = require_jsonInstruction();
    var Volume = class extends jsonInstruction_1.JSONInstruction {
      static {
        __name(this, "Volume");
      }
      constructor(document, range, dockerfile, escapeChar, instruction, instructionRange) {
        super(document, range, dockerfile, escapeChar, instruction, instructionRange);
      }
    };
    exports.Volume = Volume;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/imageTemplate.js
var require_imageTemplate = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/imageTemplate.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ImageTemplate = void 0;
    var vscode_languageserver_types_1 = (init_main(), __toCommonJS(main_exports));
    var arg_1 = require_arg();
    var cmd_1 = require_cmd();
    var copy_1 = require_copy();
    var env_1 = require_env();
    var entrypoint_1 = require_entrypoint();
    var from_1 = require_from();
    var healthcheck_1 = require_healthcheck();
    var onbuild_1 = require_onbuild();
    var util_1 = require_util();
    var workdir_1 = require_workdir();
    var ImageTemplate = class {
      static {
        __name(this, "ImageTemplate");
      }
      constructor() {
        this.comments = [];
        this.instructions = [];
      }
      addComment(comment) {
        this.comments.push(comment);
      }
      getComments() {
        return this.comments;
      }
      addInstruction(instruction) {
        this.instructions.push(instruction);
      }
      getInstructions() {
        return this.instructions;
      }
      getInstructionAt(line) {
        for (let instruction of this.instructions) {
          if (util_1.Util.isInsideRange(vscode_languageserver_types_1.Position.create(line, 0), instruction.getRange())) {
            return instruction;
          }
        }
        return null;
      }
      /**
       * Gets all the ARG instructions that are defined in this image.
       */
      getARGs() {
        let args = [];
        for (let instruction of this.instructions) {
          if (instruction instanceof arg_1.Arg) {
            args.push(instruction);
          }
        }
        return args;
      }
      /**
       * Gets all the CMD instructions that are defined in this image.
       */
      getCMDs() {
        let cmds = [];
        for (let instruction of this.instructions) {
          if (instruction instanceof cmd_1.Cmd) {
            cmds.push(instruction);
          }
        }
        return cmds;
      }
      /**
       * Gets all the COPY instructions that are defined in this image.
       */
      getCOPYs() {
        let copies = [];
        for (let instruction of this.instructions) {
          if (instruction instanceof copy_1.Copy) {
            copies.push(instruction);
          }
        }
        return copies;
      }
      /**
       * Gets all the ENTRYPOINT instructions that are defined in this image.
       */
      getENTRYPOINTs() {
        let froms = [];
        for (let instruction of this.instructions) {
          if (instruction instanceof entrypoint_1.Entrypoint) {
            froms.push(instruction);
          }
        }
        return froms;
      }
      /**
       * Gets all the ENV instructions that are defined in this image.
       */
      getENVs() {
        let args = [];
        for (let instruction of this.instructions) {
          if (instruction instanceof env_1.Env) {
            args.push(instruction);
          }
        }
        return args;
      }
      getFROM() {
        for (const instruction of this.instructions) {
          if (instruction instanceof from_1.From) {
            return instruction;
          }
        }
        return null;
      }
      /**
       * Gets all the FROM instructions that are defined in this image.
       */
      getFROMs() {
        let froms = [];
        for (let instruction of this.instructions) {
          if (instruction instanceof from_1.From) {
            froms.push(instruction);
          }
        }
        return froms;
      }
      /**
       * Gets all the HEALTHCHECK instructions that are defined in this image.
       */
      getHEALTHCHECKs() {
        let froms = [];
        for (let instruction of this.instructions) {
          if (instruction instanceof healthcheck_1.Healthcheck) {
            froms.push(instruction);
          }
        }
        return froms;
      }
      getWORKDIRs() {
        const workdirs = [];
        for (const instruction of this.instructions) {
          if (instruction instanceof workdir_1.Workdir) {
            workdirs.push(instruction);
          }
        }
        return workdirs;
      }
      getOnbuildTriggers() {
        let triggers = [];
        for (let instruction of this.instructions) {
          if (instruction instanceof onbuild_1.Onbuild) {
            let trigger = instruction.getTriggerInstruction();
            if (trigger) {
              triggers.push(trigger);
            }
          }
        }
        return triggers;
      }
      getAvailableVariables(currentLine) {
        const variables = [];
        for (const arg of this.getARGs()) {
          if (arg.isBefore(currentLine)) {
            const property = arg.getProperty();
            if (property) {
              const variable = property.getName();
              if (variables.indexOf(variable) === -1) {
                variables.push(variable);
              }
            }
          }
        }
        for (const env2 of this.getENVs()) {
          if (env2.isBefore(currentLine)) {
            for (const property of env2.getProperties()) {
              const variable = property.getName();
              if (variables.indexOf(variable) === -1) {
                variables.push(variable);
              }
            }
          }
        }
        return variables;
      }
      /**
       * Resolves a variable with the given name at the specified line
       * to its value. If null is returned, then the variable has been
       * defined but no value was given. If undefined is returned, then
       * a variable with the given name has not been defined yet as of
       * the given line.
       *
       * @param variable the name of the variable to resolve
       * @param line the line number that the variable is on, zero-based
       * @return the value of the variable as defined by an ARG or ENV
       *         instruction, or null if no value has been specified, or
       *         undefined if a variable with the given name has not
       *         been defined
       */
      resolveVariable(variable, line) {
        let envs = this.getENVs();
        for (let i = envs.length - 1; i >= 0; i--) {
          if (envs[i].isBefore(line)) {
            for (let property of envs[i].getProperties()) {
              if (property.getName() === variable) {
                return property.getValue();
              }
            }
          }
        }
        let args = this.getARGs();
        for (let i = args.length - 1; i >= 0; i--) {
          if (args[i].isBefore(line)) {
            let property = args[i].getProperty();
            if (property && property.getName() === variable) {
              return property.getValue();
            }
          }
        }
        return void 0;
      }
      getRange() {
        const instructions = this.getInstructions();
        if (instructions.length === 0) {
          return vscode_languageserver_types_1.Range.create(0, 0, 0, 0);
        }
        const instructionStart = instructions[0].getRange().start;
        const instructionEnd = instructions[instructions.length - 1].getRange().end;
        return vscode_languageserver_types_1.Range.create(instructionStart, instructionEnd);
      }
      contains(position) {
        const range = this.getRange();
        if (range === null) {
          return false;
        }
        return util_1.Util.isInsideRange(position, range);
      }
    };
    exports.ImageTemplate = ImageTemplate;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/dockerfile.js
var require_dockerfile = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/dockerfile.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Dockerfile = void 0;
    var vscode_languageserver_types_1 = (init_main(), __toCommonJS(main_exports));
    var ast = require_main();
    var imageTemplate_1 = require_imageTemplate();
    var from_1 = require_from();
    var util_1 = require_util();
    var main_1 = require_main();
    var Dockerfile = class extends imageTemplate_1.ImageTemplate {
      static {
        __name(this, "Dockerfile");
      }
      constructor(document) {
        super();
        this.initialInstructions = new imageTemplate_1.ImageTemplate();
        this.buildStages = [];
        this.directives = [];
        this.foundFrom = false;
        this.document = document;
      }
      getEscapeCharacter() {
        for (const directive of this.directives) {
          if (directive.getDirective() === ast.Directive.escape) {
            const value = directive.getValue();
            if (value === "\\" || value === "`") {
              return value;
            }
          }
        }
        return "\\";
      }
      getInitialARGs() {
        return this.initialInstructions.getARGs();
      }
      getContainingImage(position) {
        let range = vscode_languageserver_types_1.Range.create(vscode_languageserver_types_1.Position.create(0, 0), this.document.positionAt(this.document.getText().length));
        if (!util_1.Util.isInsideRange(position, range)) {
          return null;
        }
        if (this.initialInstructions.getComments().length > 0 || this.initialInstructions.getInstructions().length > 0) {
          if (util_1.Util.isInsideRange(position, this.initialInstructions.getRange())) {
            return this.initialInstructions;
          }
        }
        for (const buildStage of this.buildStages) {
          if (util_1.Util.isInsideRange(position, buildStage.getRange())) {
            return buildStage;
          }
        }
        return this;
      }
      addInstruction(instruction) {
        if (instruction.getKeyword() === main_1.Keyword.FROM) {
          this.currentBuildStage = new imageTemplate_1.ImageTemplate();
          this.buildStages.push(this.currentBuildStage);
          this.foundFrom = true;
        } else if (!this.foundFrom) {
          this.initialInstructions.addInstruction(instruction);
        }
        if (this.foundFrom) {
          this.currentBuildStage.addInstruction(instruction);
        }
        super.addInstruction(instruction);
      }
      setDirectives(directives) {
        this.directives = directives;
      }
      getDirective() {
        return this.directives.length === 0 ? null : this.directives[0];
      }
      getDirectives() {
        return this.directives;
      }
      resolveVariable(variable, line) {
        for (let from of this.getFROMs()) {
          let range = from.getRange();
          if (range.start.line <= line && line <= range.end.line) {
            let initialARGs = new imageTemplate_1.ImageTemplate();
            for (let instruction of this.initialInstructions.getARGs()) {
              initialARGs.addInstruction(instruction);
            }
            return initialARGs.resolveVariable(variable, line);
          }
        }
        let image = this.getContainingImage(vscode_languageserver_types_1.Position.create(line, 0));
        if (image === null) {
          return void 0;
        }
        let resolvedVariable = image.resolveVariable(variable, line);
        if (resolvedVariable === null) {
          let initialARGs = new imageTemplate_1.ImageTemplate();
          for (let instruction of this.initialInstructions.getARGs()) {
            initialARGs.addInstruction(instruction);
          }
          return initialARGs.resolveVariable(variable, line);
        }
        return resolvedVariable;
      }
      getAvailableVariables(currentLine) {
        if (this.getInstructionAt(currentLine) instanceof from_1.From) {
          let variables = [];
          for (let arg of this.getInitialARGs()) {
            let property = arg.getProperty();
            if (property) {
              variables.push(property.getName());
            }
          }
          return variables;
        }
        let image = this.getContainingImage(vscode_languageserver_types_1.Position.create(currentLine, 0));
        return image ? image.getAvailableVariables(currentLine) : [];
      }
      getParentStage(image) {
        const templateFrom = image.getFROM();
        const imageName = templateFrom === null ? null : templateFrom.getImageName();
        if (imageName === null) {
          return null;
        }
        for (const from of this.getFROMs()) {
          if (from.getBuildStage() === imageName) {
            const range = from.getRange();
            if (range.start.line === templateFrom.getRange().start.line) {
              return null;
            }
            return this.getContainingImage(range.start);
          }
        }
        return null;
      }
      getStageHierarchy(line) {
        const image = this.getContainingImage(vscode_languageserver_types_1.Position.create(line, 0));
        if (image === null) {
          return [];
        }
        const stages = [image];
        let stage = this.getParentStage(image);
        while (stage !== null) {
          stages.splice(0, 0, stage);
          stage = this.getParentStage(stage);
        }
        return stages;
      }
      getAvailableWorkingDirectories(line) {
        const availableDirectories = /* @__PURE__ */ new Set();
        for (const image of this.getStageHierarchy(line)) {
          for (const workdir of image.getWORKDIRs()) {
            if (workdir.getRange().end.line < line) {
              let directory = workdir.getAbsolutePath();
              if (directory !== void 0 && directory !== null) {
                if (!directory.endsWith("/")) {
                  directory += "/";
                }
                availableDirectories.add(directory);
              }
            }
          }
        }
        return Array.from(availableDirectories);
      }
      /**
       * Internally reorganize the comments in the Dockerfile and allocate
       * them to the relevant build stages that they belong to.
       */
      organizeComments() {
        const comments = this.getComments();
        for (let i = 0; i < comments.length; i++) {
          if (util_1.Util.isInsideRange(comments[i].getRange().end, this.initialInstructions.getRange())) {
            this.initialInstructions.addComment(comments[i]);
          } else {
            for (const buildStage of this.buildStages) {
              if (util_1.Util.isInsideRange(comments[i].getRange().start, buildStage.getRange())) {
                buildStage.addComment(comments[i]);
              }
            }
          }
        }
      }
      getRange() {
        const comments = this.getComments();
        const instructions = this.getInstructions();
        let range = null;
        if (comments.length === 0) {
          if (instructions.length > 0) {
            range = vscode_languageserver_types_1.Range.create(instructions[0].getRange().start, instructions[instructions.length - 1].getRange().end);
          }
        } else if (instructions.length === 0) {
          range = vscode_languageserver_types_1.Range.create(comments[0].getRange().start, comments[comments.length - 1].getRange().end);
        } else {
          const commentStart = comments[0].getRange().start;
          const commentEnd = comments[comments.length - 1].getRange().end;
          const instructionStart = instructions[0].getRange().start;
          const instructionEnd = instructions[instructions.length - 1].getRange().end;
          if (commentStart.line < instructionStart.line) {
            if (commentEnd.line < instructionEnd.line) {
              range = vscode_languageserver_types_1.Range.create(commentStart, instructionEnd);
            }
            range = vscode_languageserver_types_1.Range.create(commentStart, commentEnd);
          } else if (commentEnd.line < instructionEnd.line) {
            range = vscode_languageserver_types_1.Range.create(instructionStart, instructionEnd);
          } else {
            range = vscode_languageserver_types_1.Range.create(instructionStart, commentEnd);
          }
        }
        if (range === null) {
          if (this.directives.length === 0) {
            return null;
          }
          return this.directives[0].getRange();
        } else if (this.directives.length === 0) {
          return range;
        }
        return vscode_languageserver_types_1.Range.create(this.directives[0].getRange().start, range.end);
      }
    };
    exports.Dockerfile = Dockerfile;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/parser.js
var require_parser = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/parser.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Parser = void 0;
    var vscode_languageserver_textdocument_1 = (init_main2(), __toCommonJS(main_exports2));
    var vscode_languageserver_types_1 = (init_main(), __toCommonJS(main_exports));
    var comment_1 = require_comment();
    var parserDirective_1 = require_parserDirective();
    var instruction_1 = require_instruction();
    var add_1 = require_add();
    var arg_1 = require_arg();
    var cmd_1 = require_cmd();
    var copy_1 = require_copy();
    var env_1 = require_env();
    var entrypoint_1 = require_entrypoint();
    var from_1 = require_from();
    var healthcheck_1 = require_healthcheck();
    var label_1 = require_label();
    var onbuild_1 = require_onbuild();
    var run_1 = require_run();
    var shell_1 = require_shell();
    var stopsignal_1 = require_stopsignal();
    var workdir_1 = require_workdir();
    var user_1 = require_user();
    var volume_1 = require_volume();
    var dockerfile_1 = require_dockerfile();
    var util_1 = require_util();
    var main_1 = require_main();
    var Parser = class _Parser {
      static {
        __name(this, "Parser");
      }
      constructor() {
        this.escapeChar = null;
      }
      static createInstruction(document, dockerfile, escapeChar, lineRange, instruction, instructionRange) {
        switch (instruction.toUpperCase()) {
          case "ADD":
            return new add_1.Add(document, lineRange, dockerfile, escapeChar, instruction, instructionRange);
          case "ARG":
            return new arg_1.Arg(document, lineRange, dockerfile, escapeChar, instruction, instructionRange);
          case "CMD":
            return new cmd_1.Cmd(document, lineRange, dockerfile, escapeChar, instruction, instructionRange);
          case "COPY":
            return new copy_1.Copy(document, lineRange, dockerfile, escapeChar, instruction, instructionRange);
          case "ENTRYPOINT":
            return new entrypoint_1.Entrypoint(document, lineRange, dockerfile, escapeChar, instruction, instructionRange);
          case "ENV":
            return new env_1.Env(document, lineRange, dockerfile, escapeChar, instruction, instructionRange);
          case "FROM":
            return new from_1.From(document, lineRange, dockerfile, escapeChar, instruction, instructionRange);
          case "HEALTHCHECK":
            return new healthcheck_1.Healthcheck(document, lineRange, dockerfile, escapeChar, instruction, instructionRange);
          case "LABEL":
            return new label_1.Label(document, lineRange, dockerfile, escapeChar, instruction, instructionRange);
          case "ONBUILD":
            return new onbuild_1.Onbuild(document, lineRange, dockerfile, escapeChar, instruction, instructionRange);
          case "RUN":
            return new run_1.Run(document, lineRange, dockerfile, escapeChar, instruction, instructionRange);
          case "SHELL":
            return new shell_1.Shell(document, lineRange, dockerfile, escapeChar, instruction, instructionRange);
          case "STOPSIGNAL":
            return new stopsignal_1.Stopsignal(document, lineRange, dockerfile, escapeChar, instruction, instructionRange);
          case "WORKDIR":
            return new workdir_1.Workdir(document, lineRange, dockerfile, escapeChar, instruction, instructionRange);
          case "USER":
            return new user_1.User(document, lineRange, dockerfile, escapeChar, instruction, instructionRange);
          case "VOLUME":
            return new volume_1.Volume(document, lineRange, dockerfile, escapeChar, instruction, instructionRange);
        }
        return new instruction_1.Instruction(document, lineRange, dockerfile, escapeChar, instruction, instructionRange);
      }
      getParserDirectives(document, buffer) {
        const directives = [];
        this.escapeChar = "";
        const offset = util_1.Util.isUTF8BOM(buffer.substring(0, 1)) ? 1 : 0;
        directiveCheck: for (let i = offset; i < buffer.length; i++) {
          switch (buffer.charAt(i)) {
            case " ":
            case "	":
              break;
            case "\r":
            case "\n":
              break directiveCheck;
            case "#":
              let directiveStart = -1;
              let directiveEnd = -1;
              for (let j = i + 1; j < buffer.length; j++) {
                let char = buffer.charAt(j);
                switch (char) {
                  case " ":
                  case "	":
                    if (directiveStart !== -1 && directiveEnd === -1) {
                      directiveEnd = j;
                    }
                    break;
                  case "\r":
                  case "\n":
                    break directiveCheck;
                  case "=":
                    let valueStart = -1;
                    let valueEnd = -1;
                    if (directiveEnd === -1) {
                      directiveEnd = j;
                    }
                    let lineEnd = buffer.length;
                    directiveValue: for (let k = j + 1; k < buffer.length; k++) {
                      char = buffer.charAt(k);
                      switch (char) {
                        case "\r":
                        case "\n":
                          if (valueStart !== -1 && valueEnd === -1) {
                            valueEnd = k;
                          }
                          lineEnd = k;
                          break directiveValue;
                        case "	":
                        case " ":
                          if (valueStart !== -1 && valueEnd === -1) {
                            valueEnd = k;
                          }
                          continue;
                        default:
                          if (valueStart === -1) {
                            valueStart = k;
                          }
                          break;
                      }
                    }
                    if (directiveStart === -1) {
                      break directiveCheck;
                    }
                    if (valueStart === -1) {
                      valueStart = j + 1;
                      valueEnd = lineEnd;
                    } else if (valueEnd === -1) {
                      valueEnd = buffer.length;
                    }
                    const lineRange = vscode_languageserver_types_1.Range.create(document.positionAt(i), document.positionAt(lineEnd));
                    const nameRange = vscode_languageserver_types_1.Range.create(document.positionAt(directiveStart), document.positionAt(directiveEnd));
                    const valueRange = vscode_languageserver_types_1.Range.create(document.positionAt(valueStart), document.positionAt(valueEnd));
                    directives.push(new parserDirective_1.ParserDirective(document, lineRange, nameRange, valueRange));
                    directiveStart = -1;
                    if (buffer.charAt(valueEnd) === "\r") {
                      i = valueEnd + 1;
                    } else {
                      i = valueEnd;
                    }
                    continue directiveCheck;
                  default:
                    if (directiveStart === -1) {
                      directiveStart = j;
                    }
                    break;
                }
              }
              break;
            default:
              break directiveCheck;
          }
        }
        return directives;
      }
      parse(buffer) {
        this.document = vscode_languageserver_textdocument_1.TextDocument.create("", "", 0, buffer);
        this.buffer = buffer;
        let dockerfile = new dockerfile_1.Dockerfile(this.document);
        let directives = this.getParserDirectives(this.document, this.buffer);
        let offset = 0;
        this.escapeChar = "\\";
        if (directives.length > 0) {
          dockerfile.setDirectives(directives);
          this.escapeChar = dockerfile.getEscapeCharacter();
          offset = this.document.offsetAt(vscode_languageserver_types_1.Position.create(directives.length, 0));
        } else if (util_1.Util.isUTF8BOM(buffer.substring(0, 1))) {
          offset = 1;
        }
        for (let i = offset; i < this.buffer.length; i++) {
          const char = this.buffer.charAt(i);
          switch (char) {
            case " ":
            case "	":
            case "\r":
            case "\n":
              break;
            case "#":
              i = this.processComment(dockerfile, i);
              break;
            default:
              i = this.processInstruction(dockerfile, char, i);
              break;
          }
        }
        dockerfile.organizeComments();
        return dockerfile;
      }
      processInstruction(dockerfile, char, start) {
        let instruction = char;
        let instructionEnd = -1;
        let escapedInstruction = false;
        instructionCheck: for (let i = start + 1; i < this.buffer.length; i++) {
          char = this.buffer.charAt(i);
          switch (char) {
            case this.escapeChar:
              escapedInstruction = true;
              char = this.buffer.charAt(i + 1);
              if (char === "\r" || char === "\n") {
                if (instructionEnd === -1) {
                  instructionEnd = i;
                }
                i++;
              } else if (char === " " || char === "	") {
                for (let j = i + 2; j < this.buffer.length; j++) {
                  switch (this.buffer.charAt(j)) {
                    case " ":
                    case "	":
                      break;
                    case "\r":
                    case "\n":
                      i = j;
                      continue instructionCheck;
                    default:
                      instructionEnd = i + 1;
                      instruction = instruction + this.escapeChar;
                      i = j - 2;
                      continue instructionCheck;
                  }
                }
                instructionEnd = i + 1;
                instruction = instruction + this.escapeChar;
                break instructionCheck;
              } else {
                instructionEnd = i + 1;
                instruction = instruction + this.escapeChar;
                escapedInstruction = false;
              }
              break;
            case " ":
            case "	":
              if (escapedInstruction) {
                escapeCheck: for (let j = i + 1; j < this.buffer.length; j++) {
                  switch (this.buffer.charAt(j)) {
                    case " ":
                    case "	":
                      break;
                    case "\r":
                    case "\n":
                      i = j;
                      continue instructionCheck;
                    default:
                      break escapeCheck;
                  }
                }
                escapedInstruction = false;
              }
              if (instructionEnd === -1) {
                instructionEnd = i;
              }
              i = this.processArguments(dockerfile, instruction, instructionEnd, start, i);
              dockerfile.addInstruction(this.createInstruction(dockerfile, instruction, start, instructionEnd, i));
              return i;
            case "\r":
            case "\n":
              if (escapedInstruction) {
                continue;
              }
              if (instructionEnd === -1) {
                instructionEnd = i;
              }
              dockerfile.addInstruction(this.createInstruction(dockerfile, instruction, start, i, i));
              return i;
            case "#":
              if (escapedInstruction) {
                continue;
              }
            default:
              instructionEnd = i + 1;
              instruction = instruction + char;
              escapedInstruction = false;
              break;
          }
        }
        if (instructionEnd === -1) {
          instructionEnd = this.buffer.length;
        }
        dockerfile.addInstruction(this.createInstruction(dockerfile, instruction, start, instructionEnd, this.buffer.length));
        return this.buffer.length;
      }
      processHeredocs(instruction, offset) {
        let keyword = instruction.getKeyword();
        if (keyword === main_1.Keyword.ONBUILD) {
          instruction = instruction.getTriggerInstruction();
          if (instruction === null) {
            return offset;
          }
          keyword = instruction.getKeyword();
        }
        if (keyword !== main_1.Keyword.ADD && keyword !== main_1.Keyword.COPY && keyword !== main_1.Keyword.RUN) {
          return offset;
        }
        const heredocs = [];
        let tabbed = false;
        for (const arg of instruction.getArguments()) {
          const value = arg.getValue();
          if (value.startsWith("<<") && value.length > 2) {
            if (value.startsWith("<<-")) {
              tabbed = true;
            }
            const name = util_1.Util.parseHeredocName(value);
            if (name !== null) {
              heredocs.push(name);
            }
          }
        }
        if (heredocs.length > 0) {
          for (const heredoc of heredocs) {
            offset = this.parseHeredoc(heredoc, offset, tabbed);
          }
        }
        return offset;
      }
      processArguments(dockerfile, instruction, instructionEnd, start, offset) {
        let escaped = false;
        argumentsCheck: for (let i = offset + 1; i < this.buffer.length; i++) {
          switch (this.buffer.charAt(i)) {
            case "\r":
            case "\n":
              if (escaped) {
                continue;
              }
              return this.processHeredocs(this.createInstruction(dockerfile, instruction, start, instructionEnd, i), i);
            case this.escapeChar:
              const next = this.buffer.charAt(i + 1);
              if (next === "\n" || next === "\r") {
                escaped = true;
                i++;
              } else if (next === " " || next === "	") {
                for (let j = i + 2; j < this.buffer.length; j++) {
                  switch (this.buffer.charAt(j)) {
                    case " ":
                    case "	":
                      break;
                    case "\r":
                    case "\n":
                      escaped = true;
                    default:
                      i = j;
                      continue argumentsCheck;
                  }
                }
                return this.buffer.length;
              }
              continue;
            case "#":
              if (escaped) {
                i = this.processComment(dockerfile, i);
                continue argumentsCheck;
              }
              break;
            case " ":
            case "	":
              break;
            default:
              if (escaped) {
                escaped = false;
              }
              break;
          }
        }
        return this.buffer.length;
      }
      processComment(dockerfile, start) {
        let end = this.buffer.length;
        commentLoop: for (let i = start + 1; i < this.buffer.length; i++) {
          switch (this.buffer.charAt(i)) {
            case "\r":
            case "\n":
              end = i;
              break commentLoop;
          }
        }
        const range = vscode_languageserver_types_1.Range.create(this.document.positionAt(start), this.document.positionAt(end));
        dockerfile.addComment(new comment_1.Comment(this.document, range));
        return end;
      }
      parseHeredoc(heredocName, offset, tabbed) {
        let startWord = -1;
        let lineStart = true;
        for (let i = offset; i < this.buffer.length; i++) {
          switch (this.buffer.charAt(i)) {
            case " ":
              lineStart = false;
              break;
            case "	":
              if (!tabbed) {
                lineStart = false;
              }
              break;
            case "\r":
            case "\n":
              if (startWord !== -1 && heredocName === this.buffer.substring(startWord, i)) {
                return i;
              }
              startWord = -1;
              lineStart = true;
              break;
            default:
              if (lineStart) {
                startWord = i;
                lineStart = false;
              }
              break;
          }
        }
        return this.buffer.length;
      }
      createInstruction(dockerfile, instruction, start, instructionEnd, end) {
        const startPosition = this.document.positionAt(start);
        const instructionRange = vscode_languageserver_types_1.Range.create(startPosition, this.document.positionAt(instructionEnd));
        const lineRange = vscode_languageserver_types_1.Range.create(startPosition, this.document.positionAt(end));
        return _Parser.createInstruction(this.document, dockerfile, this.escapeChar, lineRange, instruction, instructionRange);
      }
    };
    exports.Parser = Parser;
  }
});

// ../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/main.js
var require_main = __commonJS({
  "../../node_modules/.pnpm/dockerfile-ast@0.7.1/node_modules/dockerfile-ast/lib/main.js"(exports) {
    "use strict";
    init_esm();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.DockerfileParser = exports.DefaultVariables = exports.Directive = exports.Keyword = exports.Workdir = exports.Volume = exports.User = exports.Stopsignal = exports.Shell = exports.Run = exports.PropertyInstruction = exports.Onbuild = exports.ModifiableInstruction = exports.Label = exports.JSONInstruction = exports.Heredoc = exports.Healthcheck = exports.From = exports.Env = exports.Entrypoint = exports.Copy = exports.Cmd = exports.Arg = exports.Add = exports.Variable = exports.Property = exports.ParserDirective = exports.Line = exports.Instruction = exports.Flag = exports.Comment = exports.JSONArgument = exports.Argument = void 0;
    var argument_1 = require_argument();
    Object.defineProperty(exports, "Argument", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return argument_1.Argument;
    }, "get") });
    var jsonArgument_1 = require_jsonArgument();
    Object.defineProperty(exports, "JSONArgument", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return jsonArgument_1.JSONArgument;
    }, "get") });
    var comment_1 = require_comment();
    Object.defineProperty(exports, "Comment", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return comment_1.Comment;
    }, "get") });
    var parser_1 = require_parser();
    var flag_1 = require_flag();
    Object.defineProperty(exports, "Flag", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return flag_1.Flag;
    }, "get") });
    var instruction_1 = require_instruction();
    Object.defineProperty(exports, "Instruction", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return instruction_1.Instruction;
    }, "get") });
    var line_1 = require_line();
    Object.defineProperty(exports, "Line", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return line_1.Line;
    }, "get") });
    var parserDirective_1 = require_parserDirective();
    Object.defineProperty(exports, "ParserDirective", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return parserDirective_1.ParserDirective;
    }, "get") });
    var property_1 = require_property();
    Object.defineProperty(exports, "Property", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return property_1.Property;
    }, "get") });
    var variable_1 = require_variable();
    Object.defineProperty(exports, "Variable", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return variable_1.Variable;
    }, "get") });
    var add_1 = require_add();
    Object.defineProperty(exports, "Add", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return add_1.Add;
    }, "get") });
    var arg_1 = require_arg();
    Object.defineProperty(exports, "Arg", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return arg_1.Arg;
    }, "get") });
    var cmd_1 = require_cmd();
    Object.defineProperty(exports, "Cmd", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return cmd_1.Cmd;
    }, "get") });
    var copy_1 = require_copy();
    Object.defineProperty(exports, "Copy", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return copy_1.Copy;
    }, "get") });
    var entrypoint_1 = require_entrypoint();
    Object.defineProperty(exports, "Entrypoint", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return entrypoint_1.Entrypoint;
    }, "get") });
    var env_1 = require_env();
    Object.defineProperty(exports, "Env", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return env_1.Env;
    }, "get") });
    var from_1 = require_from();
    Object.defineProperty(exports, "From", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return from_1.From;
    }, "get") });
    var healthcheck_1 = require_healthcheck();
    Object.defineProperty(exports, "Healthcheck", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return healthcheck_1.Healthcheck;
    }, "get") });
    var heredoc_1 = require_heredoc();
    Object.defineProperty(exports, "Heredoc", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return heredoc_1.Heredoc;
    }, "get") });
    var jsonInstruction_1 = require_jsonInstruction();
    Object.defineProperty(exports, "JSONInstruction", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return jsonInstruction_1.JSONInstruction;
    }, "get") });
    var label_1 = require_label();
    Object.defineProperty(exports, "Label", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return label_1.Label;
    }, "get") });
    var modifiableInstruction_1 = require_modifiableInstruction();
    Object.defineProperty(exports, "ModifiableInstruction", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return modifiableInstruction_1.ModifiableInstruction;
    }, "get") });
    var onbuild_1 = require_onbuild();
    Object.defineProperty(exports, "Onbuild", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return onbuild_1.Onbuild;
    }, "get") });
    var propertyInstruction_1 = require_propertyInstruction();
    Object.defineProperty(exports, "PropertyInstruction", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return propertyInstruction_1.PropertyInstruction;
    }, "get") });
    var run_1 = require_run();
    Object.defineProperty(exports, "Run", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return run_1.Run;
    }, "get") });
    var shell_1 = require_shell();
    Object.defineProperty(exports, "Shell", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return shell_1.Shell;
    }, "get") });
    var stopsignal_1 = require_stopsignal();
    Object.defineProperty(exports, "Stopsignal", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return stopsignal_1.Stopsignal;
    }, "get") });
    var user_1 = require_user();
    Object.defineProperty(exports, "User", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return user_1.User;
    }, "get") });
    var volume_1 = require_volume();
    Object.defineProperty(exports, "Volume", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return volume_1.Volume;
    }, "get") });
    var workdir_1 = require_workdir();
    Object.defineProperty(exports, "Workdir", { enumerable: true, get: /* @__PURE__ */ __name(function() {
      return workdir_1.Workdir;
    }, "get") });
    var Keyword;
    (function(Keyword2) {
      Keyword2["ADD"] = "ADD";
      Keyword2["ARG"] = "ARG";
      Keyword2["CMD"] = "CMD";
      Keyword2["COPY"] = "COPY";
      Keyword2["ENTRYPOINT"] = "ENTRYPOINT";
      Keyword2["ENV"] = "ENV";
      Keyword2["EXPOSE"] = "EXPOSE";
      Keyword2["FROM"] = "FROM";
      Keyword2["HEALTHCHECK"] = "HEALTHCHECK";
      Keyword2["LABEL"] = "LABEL";
      Keyword2["MAINTAINER"] = "MAINTAINER";
      Keyword2["ONBUILD"] = "ONBUILD";
      Keyword2["RUN"] = "RUN";
      Keyword2["SHELL"] = "SHELL";
      Keyword2["STOPSIGNAL"] = "STOPSIGNAL";
      Keyword2["USER"] = "USER";
      Keyword2["VOLUME"] = "VOLUME";
      Keyword2["WORKDIR"] = "WORKDIR";
    })(Keyword || (exports.Keyword = Keyword = {}));
    var Directive;
    (function(Directive2) {
      Directive2["escape"] = "escape";
      Directive2["syntax"] = "syntax";
    })(Directive || (exports.Directive = Directive = {}));
    exports.DefaultVariables = [
      "ALL_PROXY",
      "all_proxy",
      "FTP_PROXY",
      "ftp_proxy",
      "HTTP_PROXY",
      "http_proxy",
      "HTTPS_PROXY",
      "https_proxy",
      "NO_PROXY",
      "no_proxy"
    ];
    var DockerfileParser;
    (function(DockerfileParser2) {
      function parse(content) {
        let parser = new parser_1.Parser();
        return parser.parse(content);
      }
      __name(parse, "parse");
      DockerfileParser2.parse = parse;
    })(DockerfileParser || (exports.DockerfileParser = DockerfileParser = {}));
  }
});

// ../../node_modules/.pnpm/e2b@2.27.1/node_modules/e2b/dist/index.js
var require_dist2 = __commonJS({
  "../../node_modules/.pnpm/e2b@2.27.1/node_modules/e2b/dist/index.js"(exports, module) {
    "use strict";
    init_esm();
    var __create = Object.create;
    var __defProp = Object.defineProperty;
    var __defProps = Object.defineProperties;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
    var __getOwnPropNames = Object.getOwnPropertyNames;
    var __getOwnPropSymbols = Object.getOwnPropertySymbols;
    var __getProtoOf = Object.getPrototypeOf;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __propIsEnum = Object.prototype.propertyIsEnumerable;
    var __knownSymbol = /* @__PURE__ */ __name((name, symbol) => (symbol = Symbol[name]) ? symbol : Symbol.for("Symbol." + name), "__knownSymbol");
    var __defNormalProp = /* @__PURE__ */ __name((obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value, "__defNormalProp");
    var __spreadValues = /* @__PURE__ */ __name((a, b) => {
      for (var prop in b || (b = {}))
        if (__hasOwnProp.call(b, prop))
          __defNormalProp(a, prop, b[prop]);
      if (__getOwnPropSymbols)
        for (var prop of __getOwnPropSymbols(b)) {
          if (__propIsEnum.call(b, prop))
            __defNormalProp(a, prop, b[prop]);
        }
      return a;
    }, "__spreadValues");
    var __spreadProps = /* @__PURE__ */ __name((a, b) => __defProps(a, __getOwnPropDescs(b)), "__spreadProps");
    var __objRest = /* @__PURE__ */ __name((source, exclude) => {
      var target = {};
      for (var prop in source)
        if (__hasOwnProp.call(source, prop) && exclude.indexOf(prop) < 0)
          target[prop] = source[prop];
      if (source != null && __getOwnPropSymbols)
        for (var prop of __getOwnPropSymbols(source)) {
          if (exclude.indexOf(prop) < 0 && __propIsEnum.call(source, prop))
            target[prop] = source[prop];
        }
      return target;
    }, "__objRest");
    var __export2 = /* @__PURE__ */ __name((target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    }, "__export");
    var __copyProps = /* @__PURE__ */ __name((to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: /* @__PURE__ */ __name(() => from[key], "get"), enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    }, "__copyProps");
    var __toESM = /* @__PURE__ */ __name((mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
      // If the importer is in node compatibility mode or this is not an ESM
      // file that has been converted to a CommonJS file using a Babel-
      // compatible transform (i.e. "__esModule" has not been set), then set
      // "default" to the CommonJS "module.exports" for node compatibility.
      isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
      mod
    )), "__toESM");
    var __toCommonJS2 = /* @__PURE__ */ __name((mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod), "__toCommonJS");
    var __await = /* @__PURE__ */ __name(function(promise, isYieldStar) {
      this[0] = promise;
      this[1] = isYieldStar;
    }, "__await");
    var __asyncGenerator = /* @__PURE__ */ __name((__this, __arguments, generator) => {
      var resume = /* @__PURE__ */ __name((k, v, yes, no) => {
        try {
          var x = generator[k](v), isAwait = (v = x.value) instanceof __await, done = x.done;
          Promise.resolve(isAwait ? v[0] : v).then((y) => isAwait ? resume(k === "return" ? k : "next", v[1] ? { done: y.done, value: y.value } : y, yes, no) : yes({ value: y, done })).catch((e) => resume("throw", e, yes, no));
        } catch (e) {
          no(e);
        }
      }, "resume"), method = /* @__PURE__ */ __name((k) => it[k] = (x) => new Promise((yes, no) => resume(k, x, yes, no)), "method"), it = {};
      return generator = generator.apply(__this, __arguments), it[__knownSymbol("asyncIterator")] = () => it, method("next"), method("throw"), method("return"), it;
    }, "__asyncGenerator");
    var __forAwait = /* @__PURE__ */ __name((obj, it, method) => (it = obj[__knownSymbol("asyncIterator")]) ? it.call(obj) : (obj = obj[__knownSymbol("iterator")](), it = {}, method = /* @__PURE__ */ __name((key, fn) => (fn = obj[key]) && (it[key] = (arg) => new Promise((yes, no, done) => (arg = fn.call(obj, arg), done = arg.done, Promise.resolve(arg.value).then((value) => yes({ value, done }), no)))), "method"), method("next"), method("return"), it), "__forAwait");
    var src_exports = {};
    __export2(src_exports, {
      ALL_TRAFFIC: /* @__PURE__ */ __name(() => ALL_TRAFFIC, "ALL_TRAFFIC"),
      ApiClient: /* @__PURE__ */ __name(() => ApiClient, "ApiClient"),
      AuthenticationError: /* @__PURE__ */ __name(() => AuthenticationError, "AuthenticationError"),
      BuildError: /* @__PURE__ */ __name(() => BuildError, "BuildError"),
      CommandExitError: /* @__PURE__ */ __name(() => CommandExitError, "CommandExitError"),
      ConnectionConfig: /* @__PURE__ */ __name(() => ConnectionConfig, "ConnectionConfig"),
      FileNotFoundError: /* @__PURE__ */ __name(() => FileNotFoundError, "FileNotFoundError"),
      FileType: /* @__PURE__ */ __name(() => FileType2, "FileType"),
      FileUploadError: /* @__PURE__ */ __name(() => FileUploadError, "FileUploadError"),
      FilesystemEventType: /* @__PURE__ */ __name(() => FilesystemEventType, "FilesystemEventType"),
      Git: /* @__PURE__ */ __name(() => Git, "Git"),
      GitAuthError: /* @__PURE__ */ __name(() => GitAuthError, "GitAuthError"),
      GitUpstreamError: /* @__PURE__ */ __name(() => GitUpstreamError, "GitUpstreamError"),
      InvalidArgumentError: /* @__PURE__ */ __name(() => InvalidArgumentError, "InvalidArgumentError"),
      LogEntry: /* @__PURE__ */ __name(() => LogEntry, "LogEntry"),
      LogEntryEnd: /* @__PURE__ */ __name(() => LogEntryEnd, "LogEntryEnd"),
      LogEntryStart: /* @__PURE__ */ __name(() => LogEntryStart, "LogEntryStart"),
      NotEnoughSpaceError: /* @__PURE__ */ __name(() => NotEnoughSpaceError, "NotEnoughSpaceError"),
      NotFoundError: /* @__PURE__ */ __name(() => NotFoundError, "NotFoundError"),
      RateLimitError: /* @__PURE__ */ __name(() => RateLimitError, "RateLimitError"),
      ReadyCmd: /* @__PURE__ */ __name(() => ReadyCmd, "ReadyCmd"),
      Sandbox: /* @__PURE__ */ __name(() => Sandbox, "Sandbox"),
      SandboxError: /* @__PURE__ */ __name(() => SandboxError, "SandboxError"),
      SandboxNotFoundError: /* @__PURE__ */ __name(() => SandboxNotFoundError, "SandboxNotFoundError"),
      Template: /* @__PURE__ */ __name(() => Template, "Template"),
      TemplateBase: /* @__PURE__ */ __name(() => TemplateBase, "TemplateBase"),
      TemplateError: /* @__PURE__ */ __name(() => TemplateError, "TemplateError"),
      TimeoutError: /* @__PURE__ */ __name(() => TimeoutError, "TimeoutError"),
      Volume: /* @__PURE__ */ __name(() => Volume, "Volume"),
      VolumeError: /* @__PURE__ */ __name(() => VolumeError, "VolumeError"),
      VolumeFileType: /* @__PURE__ */ __name(() => VolumeFileType, "VolumeFileType"),
      default: /* @__PURE__ */ __name(() => src_default, "default"),
      defaultBuildLogger: /* @__PURE__ */ __name(() => defaultBuildLogger, "defaultBuildLogger"),
      getSignature: /* @__PURE__ */ __name(() => getSignature, "getSignature"),
      waitForFile: /* @__PURE__ */ __name(() => waitForFile, "waitForFile"),
      waitForPort: /* @__PURE__ */ __name(() => waitForPort, "waitForPort"),
      waitForProcess: /* @__PURE__ */ __name(() => waitForProcess, "waitForProcess"),
      waitForTimeout: /* @__PURE__ */ __name(() => waitForTimeout, "waitForTimeout"),
      waitForURL: /* @__PURE__ */ __name(() => waitForURL, "waitForURL")
    });
    module.exports = __toCommonJS2(src_exports);
    var import_openapi_fetch = __toESM(require_dist());
    var import_platform2 = __toESM(require_platform());
    var version = "2.27.1";
    var import_platform = __toESM(require_platform());
    function getRuntime() {
      var _a3, _b, _c;
      if (globalThis.Bun) {
        return { runtime: "bun", version: globalThis.Bun.version };
      }
      if (globalThis.Deno) {
        return { runtime: "deno", version: globalThis.Deno.version.deno };
      }
      if (((_b = (_a3 = globalThis.process) == null ? void 0 : _a3.release) == null ? void 0 : _b.name) === "node") {
        return { runtime: "node", version: import_platform.default.version || "unknown" };
      }
      if (typeof EdgeRuntime === "string") {
        return { runtime: "vercel-edge", version: "unknown" };
      }
      if (((_c = globalThis.navigator) == null ? void 0 : _c.userAgent) === "Cloudflare-Workers") {
        return { runtime: "cloudflare-worker", version: "unknown" };
      }
      if (typeof window !== "undefined") {
        return { runtime: "browser", version: import_platform.default.version || "unknown" };
      }
      return { runtime: "unknown", version: "unknown" };
    }
    __name(getRuntime, "getRuntime");
    var { runtime, version: runtimeVersion } = getRuntime();
    async function sha256(data) {
      if (typeof crypto !== "undefined") {
        const encoder = new TextEncoder();
        const dataBuffer = encoder.encode(data);
        const hashBuffer = await crypto.subtle.digest("SHA-256", dataBuffer);
        const hashArray = new Uint8Array(hashBuffer);
        return btoa(String.fromCharCode(...hashArray));
      }
      const { createHash } = __require("crypto");
      const hash = createHash("sha256").update(data, "utf8").digest();
      return hash.toString("base64");
    }
    __name(sha256, "sha256");
    function timeoutToSeconds(timeout) {
      return Math.ceil(timeout / 1e3);
    }
    __name(timeoutToSeconds, "timeoutToSeconds");
    function dynamicRequire(module2) {
      if (runtime === "browser") {
        throw new Error("Browser runtime is not supported for require");
      }
      return __require(module2);
    }
    __name(dynamicRequire, "dynamicRequire");
    async function dynamicImport(module2) {
      if (runtime === "browser") {
        throw new Error("Browser runtime is not supported for dynamic import");
      }
      return await import(module2);
    }
    __name(dynamicImport, "dynamicImport");
    function ansiRegex({ onlyFirst = false } = {}) {
      const ST = "(?:\\u0007|\\u001B\\u005C|\\u009C)";
      const osc = `(?:\\u001B\\][\\s\\S]*?${ST})`;
      const csi = "[\\u001B\\u009B][[\\]()#;?]*(?:\\d{1,4}(?:[;:]\\d{0,4})*)?[\\dA-PR-TZcf-nq-uy=><~]";
      const pattern = `${osc}|${csi}`;
      return new RegExp(pattern, onlyFirst ? void 0 : "g");
    }
    __name(ansiRegex, "ansiRegex");
    function stripAnsi(text) {
      return text.replace(ansiRegex(), "");
    }
    __name(stripAnsi, "stripAnsi");
    function toBlob(data) {
      if (data instanceof Blob) {
        return data;
      }
      if (typeof data === "string" || data instanceof ArrayBuffer) {
        return new Blob([data]);
      }
      return new Response(data).blob();
    }
    __name(toBlob, "toBlob");
    function shellQuote(s) {
      return "'" + s.replace(/'/g, "'\\''") + "'";
    }
    __name(shellQuote, "shellQuote");
    async function toUploadBody(data, gzip) {
      if (gzip) {
        const stream = data instanceof ReadableStream ? data : data instanceof Blob ? data.stream() : new Blob([data]).stream();
        const compressed = stream.pipeThrough(new CompressionStream("gzip"));
        return new Response(compressed).blob();
      }
      return toBlob(data);
    }
    __name(toUploadBody, "toUploadBody");
    var _a;
    var defaultHeaders = {
      browser: typeof window !== "undefined" && import_platform2.default.name || "unknown",
      lang: "js",
      lang_version: runtimeVersion,
      package_version: version,
      publisher: "e2b",
      sdk_runtime: runtime,
      system: ((_a = import_platform2.default.os) == null ? void 0 : _a.family) || "unknown"
    };
    function getEnvVar(name) {
      if (runtime === "deno") {
        return Deno.env.get(name);
      }
      if (typeof process === "undefined") {
        return "";
      }
      return process.env[name];
    }
    __name(getEnvVar, "getEnvVar");
    function parseIntEnv(name, defaultValue) {
      const raw = getEnvVar(name);
      if (!raw) return defaultValue;
      const parsed = Number.parseInt(raw, 10);
      if (!Number.isFinite(parsed)) {
        throw new Error(
          `Invalid ${name}=${JSON.stringify(raw)}: expected an integer.`
        );
      }
      return parsed;
    }
    __name(parseIntEnv, "parseIntEnv");
    function parsePositiveIntEnv(name, defaultValue) {
      const parsed = parseIntEnv(name, defaultValue);
      if (parsed < 1) {
        throw new Error(`Invalid ${name}=${parsed}: expected a positive integer.`);
      }
      return parsed;
    }
    __name(parsePositiveIntEnv, "parsePositiveIntEnv");
    function parseInflightLimitEnv(name, defaultValue) {
      const parsed = parseIntEnv(name, defaultValue);
      if (parsed < 0) {
        throw new Error(
          `Invalid ${name}=${parsed}: expected a non-negative integer (use 0 to disable the cap).`
        );
      }
      return parsed;
    }
    __name(parseInflightLimitEnv, "parseInflightLimitEnv");
    var Semaphore = class {
      static {
        __name(this, "Semaphore");
      }
      constructor(max) {
        this.max = max;
        this.active = 0;
        this.queue = [];
      }
      async acquire(signal) {
        if (signal == null ? void 0 : signal.aborted) throw abortReason(signal);
        if (this.active < this.max) {
          this.active++;
          return () => this.release();
        }
        return new Promise((resolve, reject) => {
          const onAcquire = /* @__PURE__ */ __name(() => {
            signal == null ? void 0 : signal.removeEventListener("abort", onAbort);
            this.active++;
            resolve(() => this.release());
          }, "onAcquire");
          const onAbort = /* @__PURE__ */ __name(() => {
            const i = this.queue.indexOf(onAcquire);
            if (i >= 0) this.queue.splice(i, 1);
            reject(abortReason(signal));
          }, "onAbort");
          this.queue.push(onAcquire);
          signal == null ? void 0 : signal.addEventListener("abort", onAbort, { once: true });
        });
      }
      release() {
        this.active--;
        const next = this.queue.shift();
        if (next) next();
      }
    };
    function abortReason(signal) {
      var _a3;
      return (_a3 = signal == null ? void 0 : signal.reason) != null ? _a3 : new DOMException("Aborted", "AbortError");
    }
    __name(abortReason, "abortReason");
    function limitConcurrency(fetcher, max) {
      if (!Number.isFinite(max) || max <= 0) {
        return fetcher;
      }
      const sem = new Semaphore(max);
      return async (input, init) => {
        var _a3;
        const signal = (_a3 = init == null ? void 0 : init.signal) != null ? _a3 : input instanceof Request ? input.signal : void 0;
        const release = await sem.acquire(signal);
        try {
          return await fetcher(input, init);
        } finally {
          release();
        }
      };
    }
    __name(limitConcurrency, "limitConcurrency");
    async function loadUndici() {
      try {
        const importModule = new Function(
          "moduleName",
          "return import(moduleName)"
        );
        return await importModule("undici");
      } catch (e) {
        return void 0;
      }
    }
    __name(loadUndici, "loadUndici");
    function toUndiciRequestInput(input, init) {
      if (!(input instanceof Request)) {
        return { input, init };
      }
      const requestInit = __spreadValues({
        body: input.body,
        cache: input.cache,
        credentials: input.credentials,
        headers: input.headers,
        integrity: input.integrity,
        keepalive: input.keepalive,
        method: input.method,
        mode: input.mode,
        redirect: input.redirect,
        referrer: input.referrer,
        referrerPolicy: input.referrerPolicy,
        signal: input.signal
      }, init);
      if (requestInit.body) {
        requestInit.duplex = "half";
      }
      return {
        input: input.url,
        init: requestInit
      };
    }
    __name(toUndiciRequestInput, "toUndiciRequestInput");
    var DEFAULT_API_CONNECTION_LIMIT = 100;
    var DEFAULT_API_INFLIGHT_LIMIT = 1e3;
    var apiFetch;
    function createApiFetch() {
      if (apiFetch) {
        return apiFetch;
      }
      apiFetch = createApiFetchForRuntime(runtime);
      return apiFetch;
    }
    __name(createApiFetch, "createApiFetch");
    function createApiFetchForRuntime(currentRuntime = runtime, options = {}) {
      if (currentRuntime !== "node") {
        return fetch;
      }
      let fetcherPromise;
      return async (input, init) => {
        fetcherPromise != null ? fetcherPromise : fetcherPromise = buildApiFetcher(options);
        const fetcher = await fetcherPromise;
        return fetcher(input, init);
      };
    }
    __name(createApiFetchForRuntime, "createApiFetchForRuntime");
    async function buildApiFetcher(options) {
      var _a3, _b, _c;
      const undici = await ((_a3 = options.loadUndici) != null ? _a3 : loadUndici)();
      const inflightLimit = (_b = options.inflightLimit) != null ? _b : getApiInflightLimit();
      if (!undici) {
        return limitConcurrency(fetch, inflightLimit);
      }
      const { Agent, fetch: undiciFetch } = undici;
      const dispatcher = new Agent({
        allowH2: true,
        connections: (_c = options.connectionLimit) != null ? _c : getApiConnectionLimit()
      });
      const fetchWithDispatcher = undiciFetch;
      const wrapped = /* @__PURE__ */ __name((input, init) => {
        const request = toUndiciRequestInput(input, init);
        return fetchWithDispatcher(request.input, __spreadProps(__spreadValues({}, request.init), {
          dispatcher
        }));
      }, "wrapped");
      return limitConcurrency(wrapped, inflightLimit);
    }
    __name(buildApiFetcher, "buildApiFetcher");
    function getApiConnectionLimit() {
      return parsePositiveIntEnv(
        "E2B_API_CONNECTIONS",
        DEFAULT_API_CONNECTION_LIMIT
      );
    }
    __name(getApiConnectionLimit, "getApiConnectionLimit");
    function getApiInflightLimit() {
      return parseInflightLimitEnv(
        "E2B_API_INFLIGHT_REQUESTS",
        DEFAULT_API_INFLIGHT_LIMIT
      );
    }
    __name(getApiInflightLimit, "getApiInflightLimit");
    function formatSandboxTimeoutError(message) {
      return new TimeoutError(
        `${message}: This error is likely due to sandbox timeout. You can modify the sandbox timeout by passing 'timeoutMs' when starting the sandbox or calling '.setTimeout' on the sandbox with the desired timeout.`
      );
    }
    __name(formatSandboxTimeoutError, "formatSandboxTimeoutError");
    var SandboxError = class extends Error {
      static {
        __name(this, "SandboxError");
      }
      constructor(message, stackTrace) {
        super(message);
        this.name = "SandboxError";
        if (stackTrace) {
          this.stack = stackTrace;
        }
      }
    };
    var TimeoutError = class extends SandboxError {
      static {
        __name(this, "TimeoutError");
      }
      constructor(message, stackTrace) {
        super(message, stackTrace);
        this.name = "TimeoutError";
      }
    };
    var InvalidArgumentError = class extends SandboxError {
      static {
        __name(this, "InvalidArgumentError");
      }
      constructor(message, stackTrace) {
        super(message, stackTrace);
        this.name = "InvalidArgumentError";
      }
    };
    var NotEnoughSpaceError = class extends SandboxError {
      static {
        __name(this, "NotEnoughSpaceError");
      }
      constructor(message, stackTrace) {
        super(message, stackTrace);
        this.name = "NotEnoughSpaceError";
      }
    };
    var NotFoundError = class extends SandboxError {
      static {
        __name(this, "NotFoundError");
      }
      constructor(message, stackTrace) {
        super(message, stackTrace);
        this.name = "NotFoundError";
      }
    };
    var FileNotFoundError = class extends NotFoundError {
      static {
        __name(this, "FileNotFoundError");
      }
      constructor(message, stackTrace) {
        super(message, stackTrace);
        this.name = "FileNotFoundError";
      }
    };
    var SandboxNotFoundError = class extends NotFoundError {
      static {
        __name(this, "SandboxNotFoundError");
      }
      constructor(message, stackTrace) {
        super(message, stackTrace);
        this.name = "SandboxNotFoundError";
      }
    };
    var AuthenticationError = class extends Error {
      static {
        __name(this, "AuthenticationError");
      }
      constructor(message) {
        super(message);
        this.name = "AuthenticationError";
      }
    };
    var GitAuthError = class extends AuthenticationError {
      static {
        __name(this, "GitAuthError");
      }
      constructor(message) {
        super(message);
        this.name = "GitAuthError";
      }
    };
    var GitUpstreamError = class extends SandboxError {
      static {
        __name(this, "GitUpstreamError");
      }
      constructor(message, stackTrace) {
        super(message, stackTrace);
        this.name = "GitUpstreamError";
      }
    };
    var TemplateError = class extends SandboxError {
      static {
        __name(this, "TemplateError");
      }
      constructor(message, stackTrace) {
        super(message, stackTrace);
        this.name = "TemplateError";
      }
    };
    var RateLimitError = class extends SandboxError {
      static {
        __name(this, "RateLimitError");
      }
      constructor(message) {
        super(message);
        this.name = "RateLimitError";
      }
    };
    var BuildError = class extends Error {
      static {
        __name(this, "BuildError");
      }
      constructor(message, stackTrace) {
        super(message);
        this.name = "BuildError";
        if (stackTrace) {
          this.stack = stackTrace;
        }
      }
    };
    var FileUploadError = class extends BuildError {
      static {
        __name(this, "FileUploadError");
      }
      constructor(message, stackTrace) {
        super(message, stackTrace);
        this.name = "FileUploadError";
      }
    };
    var VolumeError = class extends Error {
      static {
        __name(this, "VolumeError");
      }
      constructor(message) {
        super(message);
        this.name = "VolumeError";
      }
    };
    function formatLog(log) {
      return JSON.parse(JSON.stringify(log));
    }
    __name(formatLog, "formatLog");
    function createRpcLogger(logger) {
      function logEach(stream) {
        return __asyncGenerator(this, null, function* () {
          var _a3;
          try {
            for (var iter = __forAwait(stream), more, temp, error; more = !(temp = yield new __await(iter.next())).done; more = false) {
              const m = temp.value;
              (_a3 = logger.debug) == null ? void 0 : _a3.call(logger, "Response stream:", formatLog(m));
              yield m;
            }
          } catch (temp2) {
            error = [temp2];
          } finally {
            try {
              more && (temp = iter.return) && (yield new __await(temp.call(iter)));
            } finally {
              if (error)
                throw error[0];
            }
          }
        });
      }
      __name(logEach, "logEach");
      return (next) => async (req) => {
        var _a3, _b;
        (_a3 = logger.info) == null ? void 0 : _a3.call(logger, `Request: POST ${req.url}`);
        const res = await next(req);
        if (res.stream) {
          return __spreadProps(__spreadValues({}, res), {
            message: logEach(res.message)
          });
        } else {
          (_b = logger.info) == null ? void 0 : _b.call(logger, "Response:", formatLog(res.message));
        }
        return res;
      };
    }
    __name(createRpcLogger, "createRpcLogger");
    function createApiLogger(logger) {
      return {
        async onRequest({ request }) {
          var _a3;
          (_a3 = logger.info) == null ? void 0 : _a3.call(logger, `Request ${request.method} ${request.url}`);
          return request;
        },
        async onResponse({ response }) {
          var _a3, _b;
          if (response.status >= 400) {
            (_a3 = logger.error) == null ? void 0 : _a3.call(logger, "Response:", response.status, response.statusText);
          } else {
            (_b = logger.info) == null ? void 0 : _b.call(logger, "Response:", response.status, response.statusText);
          }
          return response;
        }
      };
    }
    __name(createApiLogger, "createApiLogger");
    var API_KEY_PATTERN = /^e2b_[0-9a-f]+$/;
    var API_KEY_EXAMPLE = `e2b_${"0".repeat(40)}`;
    function validateApiKey(apiKey) {
      if (!API_KEY_PATTERN.test(apiKey)) {
        throw new AuthenticationError(
          `Invalid API key format: expected "e2b_" followed by hex characters (e.g. "${API_KEY_EXAMPLE}"). Visit the API Keys tab at https://e2b.dev/dashboard?tab=keys to get your API key.`
        );
      }
    }
    __name(validateApiKey, "validateApiKey");
    function handleApiError(response, errorClass = SandboxError, stackTrace) {
      var _a3, _b, _c, _d, _e, _f;
      if (response.error === void 0) {
        return;
      }
      if (response.response.status === 401) {
        const message2 = "Unauthorized, please check your credentials.";
        const content = (_b = (_a3 = response.error) == null ? void 0 : _a3.message) != null ? _b : response.error;
        if (content) {
          return new AuthenticationError(`${message2} - ${content}`);
        }
        return new AuthenticationError(message2);
      }
      if (response.response.status === 429) {
        const message2 = "Rate limit exceeded, please try again later";
        const content = (_d = (_c = response.error) == null ? void 0 : _c.message) != null ? _d : response.error;
        if (content) {
          return new RateLimitError(`${message2} - ${content}`);
        }
        return new RateLimitError(message2);
      }
      const message = (_f = (_e = response.error) == null ? void 0 : _e.message) != null ? _f : response.error;
      return new errorClass(`${response.response.status}: ${message}`, stackTrace);
    }
    __name(handleApiError, "handleApiError");
    var ApiClient = class {
      static {
        __name(this, "ApiClient");
      }
      constructor(config, opts = { requireAccessToken: false, requireApiKey: false }) {
        if ((opts == null ? void 0 : opts.requireApiKey) && !config.apiKey) {
          throw new AuthenticationError(
            "API key is required, please visit the Team tab at https://e2b.dev/dashboard to get your API key. You can either set the environment variable `E2B_API_KEY` or you can pass it directly to the sandbox like Sandbox.create({ apiKey: 'e2b_...' })"
          );
        }
        if (config.apiKey) {
          validateApiKey(config.apiKey);
        }
        if ((opts == null ? void 0 : opts.requireAccessToken) && !config.accessToken) {
          throw new AuthenticationError(
            "Access token is required, please visit the Personal tab at https://e2b.dev/dashboard to get your access token. You can set the environment variable `E2B_ACCESS_TOKEN` or pass the `accessToken` in options."
          );
        }
        this.api = (0, import_openapi_fetch.default)({
          baseUrl: config.apiUrl,
          fetch: createApiFetch(),
          // In HTTP 1.1, all connections are considered persistent unless declared otherwise
          // keepalive: true,
          headers: __spreadValues(__spreadValues(__spreadValues(__spreadValues({}, defaultHeaders), config.apiKey && { "X-API-KEY": config.apiKey }), config.accessToken && {
            Authorization: `Bearer ${config.accessToken}`
          }), config.headers),
          querySerializer: {
            array: {
              style: "form",
              explode: false
            }
          }
        });
        if (config.logger) {
          this.api.use(createApiLogger(config.logger));
        }
      }
    };
    var supportedDomains = ["e2b.app", "e2b.dev", "e2b.pro", "e2b-staging.dev"];
    var REQUEST_TIMEOUT_MS = 6e4;
    var DEFAULT_SANDBOX_TIMEOUT_MS = 3e5;
    var KEEPALIVE_PING_INTERVAL_SEC = 50;
    var KEEPALIVE_PING_HEADER = "Keepalive-Ping-Interval";
    function buildRequestSignal(requestTimeoutMs, userSignal) {
      const timeoutSignal = requestTimeoutMs ? AbortSignal.timeout(requestTimeoutMs) : void 0;
      if (timeoutSignal && userSignal) {
        return AbortSignal.any([timeoutSignal, userSignal]);
      }
      return timeoutSignal != null ? timeoutSignal : userSignal;
    }
    __name(buildRequestSignal, "buildRequestSignal");
    function setupRequestController(requestTimeoutMs, userSignal) {
      const controller = new AbortController();
      const onUserAbort = /* @__PURE__ */ __name(() => controller.abort(userSignal == null ? void 0 : userSignal.reason), "onUserAbort");
      if (userSignal) {
        if (userSignal.aborted) {
          controller.abort(userSignal.reason);
        } else {
          userSignal.addEventListener("abort", onUserAbort, { once: true });
        }
      }
      let reqTimeout = requestTimeoutMs ? setTimeout(
        () => controller.abort(
          new DOMException(
            `Request handshake timed out after ${requestTimeoutMs}ms`,
            "TimeoutError"
          )
        ),
        requestTimeoutMs
      ) : void 0;
      const clearStartTimeout = /* @__PURE__ */ __name(() => {
        if (reqTimeout) {
          clearTimeout(reqTimeout);
          reqTimeout = void 0;
        }
      }, "clearStartTimeout");
      let cleaned = false;
      const cleanup = /* @__PURE__ */ __name(() => {
        if (cleaned) return;
        cleaned = true;
        userSignal == null ? void 0 : userSignal.removeEventListener("abort", onUserAbort);
        clearStartTimeout();
        controller.abort();
      }, "cleanup");
      return { controller, clearStartTimeout, cleanup };
    }
    __name(setupRequestController, "setupRequestController");
    var _ConnectionConfig = class _ConnectionConfig2 {
      static {
        __name(this, "_ConnectionConfig");
      }
      constructor(opts) {
        var _a3;
        this.apiKey = (opts == null ? void 0 : opts.apiKey) || _ConnectionConfig2.apiKey;
        this.debug = (opts == null ? void 0 : opts.debug) || _ConnectionConfig2.debug;
        this.domain = (opts == null ? void 0 : opts.domain) || _ConnectionConfig2.domain;
        this.accessToken = (opts == null ? void 0 : opts.accessToken) || _ConnectionConfig2.accessToken;
        this.requestTimeoutMs = (_a3 = opts == null ? void 0 : opts.requestTimeoutMs) != null ? _a3 : REQUEST_TIMEOUT_MS;
        this.logger = opts == null ? void 0 : opts.logger;
        this.headers = (opts == null ? void 0 : opts.headers) || {};
        this.headers["User-Agent"] = `e2b-js-sdk/${version}`;
        this.apiUrl = (opts == null ? void 0 : opts.apiUrl) || _ConnectionConfig2.apiUrl || (this.debug ? "http://localhost:3000" : `https://api.${this.domain}`);
        this.sandboxUrl = (opts == null ? void 0 : opts.sandboxUrl) || _ConnectionConfig2.sandboxUrl;
      }
      static get domain() {
        return getEnvVar("E2B_DOMAIN") || "e2b.app";
      }
      static get apiUrl() {
        return getEnvVar("E2B_API_URL");
      }
      static get sandboxUrl() {
        return getEnvVar("E2B_SANDBOX_URL");
      }
      static get debug() {
        return (getEnvVar("E2B_DEBUG") || "false").toLowerCase() === "true";
      }
      static get apiKey() {
        return getEnvVar("E2B_API_KEY");
      }
      static get accessToken() {
        return getEnvVar("E2B_ACCESS_TOKEN");
      }
      getSignal(requestTimeoutMs, signal) {
        return buildRequestSignal(requestTimeoutMs != null ? requestTimeoutMs : this.requestTimeoutMs, signal);
      }
      getSandboxUrl(sandboxId, opts) {
        var _a3;
        if (this.sandboxUrl) {
          return this.sandboxUrl;
        }
        if (this.debug) {
          return `http://${this.getHost(sandboxId, opts.envdPort, opts.sandboxDomain)}`;
        }
        const sandboxDomain = (_a3 = opts.sandboxDomain) != null ? _a3 : this.domain;
        if (runtime !== "browser" && supportedDomains.includes(sandboxDomain)) {
          return `https://sandbox.${sandboxDomain}`;
        }
        return `https://${this.getHost(sandboxId, opts.envdPort, sandboxDomain)}`;
      }
      getSandboxDirectUrl(sandboxId, opts) {
        if (this.sandboxUrl) {
          return this.sandboxUrl;
        }
        if (this.debug) {
          return `http://${this.getHost(sandboxId, opts.envdPort, opts.sandboxDomain)}`;
        }
        return `https://${this.getHost(sandboxId, opts.envdPort, opts.sandboxDomain)}`;
      }
      getHost(sandboxId, port, sandboxDomain) {
        if (this.debug) {
          return `localhost:${port}`;
        }
        return `${port}-${sandboxId}.${sandboxDomain != null ? sandboxDomain : this.domain}`;
      }
    };
    _ConnectionConfig.envdPort = 49983;
    var ConnectionConfig = _ConnectionConfig;
    var defaultUsername = "user";
    async function getSignature({
      path: path2,
      operation,
      user,
      expirationInSeconds,
      envdAccessToken
    }) {
      if (!envdAccessToken) {
        throw new Error(
          "Access token is not set and signature cannot be generated!"
        );
      }
      const signatureExpiration = expirationInSeconds ? Math.floor(Date.now() / 1e3) + expirationInSeconds : null;
      let signatureRaw;
      if (user == void 0) {
        user = "";
      }
      if (signatureExpiration === null) {
        signatureRaw = `${path2}:${operation}:${user}:${envdAccessToken}`;
      } else {
        signatureRaw = `${path2}:${operation}:${user}:${envdAccessToken}:${signatureExpiration.toString()}`;
      }
      const hashBase64 = await sha256(signatureRaw);
      const signature = "v1_" + hashBase64.replace(/=+$/, "");
      return {
        signature,
        expiration: signatureExpiration
      };
    }
    __name(getSignature, "getSignature");
    var import_connect3 = require_cjs2();
    var import_openapi_fetch2 = __toESM(require_dist());
    var import_connect = require_cjs2();
    var DEFAULT_ERROR_MAP = {
      400: (message) => new InvalidArgumentError(message),
      401: (message) => new AuthenticationError(message),
      404: (message) => new NotFoundError(message),
      429: (message) => new SandboxError(`${message}: The requests are being rate limited.`),
      502: formatSandboxTimeoutError,
      507: (message) => new NotEnoughSpaceError(message)
    };
    async function handleEnvdApiError(res, errorMap) {
      var _a3, _b, _c;
      if (!res.error) {
        return;
      }
      const message = typeof res.error == "string" ? res.error : ((_a3 = res.error) == null ? void 0 : _a3.message) || await res.response.text();
      if (errorMap && res.response.status in errorMap) {
        return (_b = errorMap[res.response.status]) == null ? void 0 : _b.call(errorMap, message);
      }
      if (res.response.status in DEFAULT_ERROR_MAP) {
        return (_c = DEFAULT_ERROR_MAP[res.response.status]) == null ? void 0 : _c.call(DEFAULT_ERROR_MAP, message);
      }
      return new SandboxError(`${res.response.status}: ${message}`);
    }
    __name(handleEnvdApiError, "handleEnvdApiError");
    async function handleProcessStartEvent(events) {
      var _a3;
      let startEvent;
      try {
        startEvent = (await events[Symbol.asyncIterator]().next()).value;
      } catch (err) {
        if (err instanceof import_connect.ConnectError) {
          if (err.code === import_connect.Code.Unavailable) {
            throw new SandboxNotFoundError(
              "Sandbox is probably not running anymore"
            );
          }
        }
        throw err;
      }
      if (((_a3 = startEvent.event) == null ? void 0 : _a3.event.case) !== "start") {
        throw new Error("Expected start event");
      }
      return startEvent.event.event.value.pid;
    }
    __name(handleProcessStartEvent, "handleProcessStartEvent");
    async function handleWatchDirStartEvent(events) {
      var _a3;
      let startEvent;
      try {
        startEvent = (await events[Symbol.asyncIterator]().next()).value;
      } catch (err) {
        if (err instanceof import_connect.ConnectError) {
          if (err.code === import_connect.Code.Unavailable) {
            throw new SandboxNotFoundError(
              "Sandbox is probably not running anymore"
            );
          }
        }
        throw err;
      }
      if (((_a3 = startEvent.event) == null ? void 0 : _a3.case) !== "start") {
        throw new Error("Expected start event");
      }
      return startEvent.event.value;
    }
    __name(handleWatchDirStartEvent, "handleWatchDirStartEvent");
    var EnvdApiClient = class {
      static {
        __name(this, "EnvdApiClient");
      }
      constructor(config, metadata) {
        this.api = (0, import_openapi_fetch2.default)({
          baseUrl: config.apiUrl,
          fetch: config == null ? void 0 : config.fetch,
          headers: config == null ? void 0 : config.headers
          // In HTTP 1.1, all connections are considered persistent unless declared otherwise
          // keepalive: true,
        });
        this.version = metadata.version;
        if (config.logger) {
          this.api.use(createApiLogger(config.logger));
        }
      }
    };
    var import_connect2 = require_cjs2();
    var import_compare_versions = require_umd();
    var ENVD_VERSION_RECURSIVE_WATCH = "0.1.4";
    var ENVD_DEBUG_FALLBACK = "99.99.99";
    var ENVD_COMMANDS_STDIN = "0.3.0";
    var ENVD_DEFAULT_USER = "0.4.0";
    var ENVD_ENVD_CLOSE = "0.5.2";
    var ENVD_OCTET_STREAM_UPLOAD = "0.5.7";
    var DEFAULT_ERROR_MAP2 = {
      [import_connect2.Code.InvalidArgument]: (message) => new InvalidArgumentError(message),
      [import_connect2.Code.Unauthenticated]: (message) => new AuthenticationError(message),
      [import_connect2.Code.NotFound]: (message) => new NotFoundError(message),
      [import_connect2.Code.Unavailable]: formatSandboxTimeoutError,
      [import_connect2.Code.Canceled]: (message) => new TimeoutError(
        `${message}: This error is likely due to exceeding 'requestTimeoutMs'. You can pass the request timeout value as an option when making the request.`
      ),
      [import_connect2.Code.DeadlineExceeded]: (message) => new TimeoutError(
        `${message}: This error is likely due to exceeding 'timeoutMs' — the total time a long running request (like command execution or directory watch) can be active. It can be modified by passing 'timeoutMs' when making the request. Use '0' to disable the timeout.`
      )
    };
    function handleRpcError(err, errorMap) {
      if (err instanceof import_connect2.ConnectError) {
        if (errorMap && err.code in errorMap) {
          return errorMap[err.code](err.message);
        }
        if (err.code in DEFAULT_ERROR_MAP2) {
          return DEFAULT_ERROR_MAP2[err.code](err.message);
        }
        return new SandboxError(`${err.code}: ${err.message}`);
      }
      return err;
    }
    __name(handleRpcError, "handleRpcError");
    function encode64(value) {
      switch (runtime) {
        case "deno":
          return btoa(value);
        case "node":
          return Buffer.from(value).toString("base64");
        case "bun":
          return Buffer.from(value).toString("base64");
        default:
          return btoa(value);
      }
    }
    __name(encode64, "encode64");
    function authenticationHeader(envdVersion, username) {
      if (username == void 0 && (0, import_compare_versions.compareVersions)(envdVersion, ENVD_DEFAULT_USER) < 0) {
        username = defaultUsername;
      }
      if (!username) {
        return {};
      }
      const value = `${username}:`;
      const encoded = encode64(value);
      return { Authorization: `Basic ${encoded}` };
    }
    __name(authenticationHeader, "authenticationHeader");
    var import_codegenv2 = require_codegenv2();
    var import_wkt = require_wkt();
    var file_filesystem_filesystem = /* @__PURE__ */ (0, import_codegenv2.fileDesc)(
      "",
      [import_wkt.file_google_protobuf_timestamp]
    );
    var Filesystem = /* @__PURE__ */ (0, import_codegenv2.serviceDesc)(file_filesystem_filesystem, 0);
    var FilesystemEventType = /* @__PURE__ */ ((FilesystemEventType2) => {
      FilesystemEventType2["CHMOD"] = "chmod";
      FilesystemEventType2["CREATE"] = "create";
      FilesystemEventType2["REMOVE"] = "remove";
      FilesystemEventType2["RENAME"] = "rename";
      FilesystemEventType2["WRITE"] = "write";
      return FilesystemEventType2;
    })(FilesystemEventType || {});
    function mapEventType(type) {
      switch (type) {
        case 5:
          return "chmod";
        case 1:
          return "create";
        case 3:
          return "remove";
        case 4:
          return "rename";
        case 2:
          return "write";
      }
    }
    __name(mapEventType, "mapEventType");
    var WatchHandle = class {
      static {
        __name(this, "WatchHandle");
      }
      constructor(handleStop, events, onEvent, onExit) {
        this.handleStop = handleStop;
        this.events = events;
        this.onEvent = onEvent;
        this.onExit = onExit;
        this.handleEvents();
      }
      /**
       * Stop watching the directory.
       */
      async stop() {
        this.handleStop();
      }
      iterateEvents() {
        return __asyncGenerator(this, null, function* () {
          try {
            try {
              for (var iter = __forAwait(this.events), more, temp, error; more = !(temp = yield new __await(iter.next())).done; more = false) {
                const event = temp.value;
                switch (event.event.case) {
                  case "filesystem":
                    yield event.event;
                    break;
                }
              }
            } catch (temp2) {
              error = [temp2];
            } finally {
              try {
                more && (temp = iter.return) && (yield new __await(temp.call(iter)));
              } finally {
                if (error)
                  throw error[0];
              }
            }
          } catch (err) {
            throw handleRpcError(err);
          }
        });
      }
      async handleEvents() {
        var _a3, _b, _c;
        try {
          try {
            for (var iter = __forAwait(this.iterateEvents()), more, temp, error; more = !(temp = await iter.next()).done; more = false) {
              const event = temp.value;
              const eventType = mapEventType(event.value.type);
              if (eventType === void 0) {
                continue;
              }
              (_a3 = this.onEvent) == null ? void 0 : _a3.call(this, {
                name: event.value.name,
                type: eventType
              });
            }
          } catch (temp2) {
            error = [temp2];
          } finally {
            try {
              more && (temp = iter.return) && await temp.call(iter);
            } finally {
              if (error)
                throw error[0];
            }
          }
          (_b = this.onExit) == null ? void 0 : _b.call(this);
        } catch (err) {
          (_c = this.onExit) == null ? void 0 : _c.call(this, err);
        } finally {
          this.handleStop();
        }
      }
    };
    var import_compare_versions2 = require_umd();
    var FILESYSTEM_HTTP_ERROR_MAP = {
      404: (message) => new FileNotFoundError(message)
    };
    var FILESYSTEM_RPC_ERROR_MAP = {
      [import_connect3.Code.NotFound]: (message) => new FileNotFoundError(message)
    };
    function handleFilesystemRpcError(err) {
      return handleRpcError(err, FILESYSTEM_RPC_ERROR_MAP);
    }
    __name(handleFilesystemRpcError, "handleFilesystemRpcError");
    function handleFilesystemEnvdApiError(res) {
      return handleEnvdApiError(res, FILESYSTEM_HTTP_ERROR_MAP);
    }
    __name(handleFilesystemEnvdApiError, "handleFilesystemEnvdApiError");
    var FileType2 = /* @__PURE__ */ ((FileType3) => {
      FileType3["FILE"] = "file";
      FileType3["DIR"] = "dir";
      return FileType3;
    })(FileType2 || {});
    function mapFileType(fileType) {
      switch (fileType) {
        case 2:
          return "dir";
        case 1:
          return "file";
      }
    }
    __name(mapFileType, "mapFileType");
    function mapModifiedTime(modifiedTime) {
      if (!modifiedTime) return void 0;
      return new Date(
        Number(modifiedTime.seconds) * 1e3 + Math.floor(modifiedTime.nanos / 1e6)
      );
    }
    __name(mapModifiedTime, "mapModifiedTime");
    var Filesystem2 = class {
      static {
        __name(this, "Filesystem2");
      }
      constructor(transport, envdApi, connectionConfig) {
        this.envdApi = envdApi;
        this.connectionConfig = connectionConfig;
        this.defaultWatchTimeout = 6e4;
        this.defaultWatchRecursive = false;
        this.rpc = (0, import_connect3.createClient)(Filesystem, transport);
      }
      async read(path2, opts) {
        var _a3;
        const format = (_a3 = opts == null ? void 0 : opts.format) != null ? _a3 : "text";
        let user = opts == null ? void 0 : opts.user;
        if (user == void 0 && (0, import_compare_versions2.compareVersions)(this.envdApi.version, ENVD_DEFAULT_USER) < 0) {
          user = defaultUsername;
        }
        const headers = {};
        if (opts == null ? void 0 : opts.gzip) {
          headers["Accept-Encoding"] = "gzip";
        }
        const res = await this.envdApi.api.GET("/files", {
          params: {
            query: {
              path: path2,
              username: user
            }
          },
          parseAs: format === "bytes" ? "arrayBuffer" : format,
          signal: this.connectionConfig.getSignal(
            opts == null ? void 0 : opts.requestTimeoutMs,
            opts == null ? void 0 : opts.signal
          ),
          headers
        });
        const err = await handleFilesystemEnvdApiError(res);
        if (err) {
          throw err;
        }
        if (format === "bytes") {
          return new Uint8Array(res.data);
        }
        if (res.response.headers.get("content-length") === "0") {
          return "";
        }
        return res.data;
      }
      async write(pathOrFiles, dataOrOpts, opts) {
        var _a3, _b;
        if (typeof pathOrFiles !== "string" && !Array.isArray(pathOrFiles)) {
          throw new Error("Path or files are required");
        }
        if (typeof pathOrFiles === "string" && Array.isArray(dataOrOpts)) {
          throw new Error(
            "Cannot specify both path and array of files. You have to specify either path and data for a single file or an array for multiple files."
          );
        }
        const { path: path2, writeOpts, writeFiles } = typeof pathOrFiles === "string" ? {
          path: pathOrFiles,
          writeOpts: opts,
          writeFiles: [
            {
              data: dataOrOpts
            }
          ]
        } : {
          path: void 0,
          writeOpts: dataOrOpts,
          writeFiles: pathOrFiles
        };
        if (writeFiles.length === 0) return [];
        let user = writeOpts == null ? void 0 : writeOpts.user;
        if (user == void 0 && (0, import_compare_versions2.compareVersions)(this.envdApi.version, ENVD_DEFAULT_USER) < 0) {
          user = defaultUsername;
        }
        const supportsOctetStream = (0, import_compare_versions2.compareVersions)(this.envdApi.version, ENVD_OCTET_STREAM_UPLOAD) >= 0;
        const useOctetStream = ((_a3 = writeOpts == null ? void 0 : writeOpts.useOctetStream) != null ? _a3 : false) && supportsOctetStream;
        const results = [];
        const useGzip = (writeOpts == null ? void 0 : writeOpts.gzip) === true;
        if (useOctetStream) {
          const headers = {
            "Content-Type": "application/octet-stream"
          };
          if (useGzip) {
            headers["Content-Encoding"] = "gzip";
          }
          const uploadResults = await Promise.all(
            writeFiles.map(async (file) => {
              const filePath = path2 != null ? path2 : file.path;
              const body = await toUploadBody(file.data, useGzip);
              const res = await this.envdApi.api.POST("/files", {
                params: {
                  query: {
                    path: filePath,
                    username: user
                  }
                },
                bodySerializer: /* @__PURE__ */ __name(() => body, "bodySerializer"),
                headers,
                signal: this.connectionConfig.getSignal(
                  writeOpts == null ? void 0 : writeOpts.requestTimeoutMs,
                  writeOpts == null ? void 0 : writeOpts.signal
                ),
                body: {}
              });
              const err = await handleFilesystemEnvdApiError(res);
              if (err) {
                throw err;
              }
              const files = res.data;
              if (!files || files.length === 0) {
                throw new Error(
                  "Expected to receive information about written file"
                );
              }
              return files;
            })
          );
          for (const files of uploadResults) {
            results.push(...files);
          }
        } else {
          const formData = new FormData();
          for (const file of writeFiles) {
            formData.append(
              "file",
              await toBlob(file.data),
              (_b = file.path) != null ? _b : path2
            );
          }
          const res = await this.envdApi.api.POST("/files", {
            params: {
              query: {
                path: path2,
                username: user
              }
            },
            bodySerializer: /* @__PURE__ */ __name(() => formData, "bodySerializer"),
            signal: this.connectionConfig.getSignal(
              writeOpts == null ? void 0 : writeOpts.requestTimeoutMs,
              writeOpts == null ? void 0 : writeOpts.signal
            ),
            body: {}
          });
          const err = await handleFilesystemEnvdApiError(res);
          if (err) {
            throw err;
          }
          const files = res.data;
          if (!files || files.length === 0) {
            throw new Error("Expected to receive information about written file");
          }
          results.push(...files);
        }
        return results.length === 1 && path2 ? results[0] : results;
      }
      /**
       * Write multiple files.
       *
       *
       * Writing to a file that doesn't exist creates the file.
       *
       * Writing to a file that already exists overwrites the file.
       *
       * Writing to a file at path that doesn't exist creates the necessary directories.
       *
       * @param files list of files to write as `WriteEntry` objects, each containing `path` and `data`.
       * @param opts connection options.
       *
       * @returns information about the written files
       */
      async writeFiles(files, opts) {
        return this.write(files, opts);
      }
      /**
       * List entries in a directory.
       *
       * @param path path to the directory.
       * @param opts connection options.
       *
       * @returns list of entries in the sandbox filesystem directory.
       */
      async list(path2, opts) {
        var _a3;
        if (typeof (opts == null ? void 0 : opts.depth) === "number" && opts.depth < 1) {
          throw new InvalidArgumentError("depth should be at least one");
        }
        try {
          const res = await this.rpc.listDir(
            {
              path: path2,
              depth: (_a3 = opts == null ? void 0 : opts.depth) != null ? _a3 : 1
            },
            {
              headers: authenticationHeader(this.envdApi.version, opts == null ? void 0 : opts.user),
              signal: this.connectionConfig.getSignal(
                opts == null ? void 0 : opts.requestTimeoutMs,
                opts == null ? void 0 : opts.signal
              )
            }
          );
          const entries = [];
          for (const e of res.entries) {
            const type = mapFileType(e.type);
            if (type) {
              entries.push({
                name: e.name,
                type,
                path: e.path,
                size: Number(e.size),
                mode: e.mode,
                permissions: e.permissions,
                owner: e.owner,
                group: e.group,
                modifiedTime: mapModifiedTime(e.modifiedTime),
                symlinkTarget: e.symlinkTarget
              });
            }
          }
          return entries;
        } catch (err) {
          throw handleFilesystemRpcError(err);
        }
      }
      /**
       * Create a new directory and all directories along the way if needed on the specified path.
       *
       * @param path path to a new directory. For example '/dirA/dirB' when creating 'dirB'.
       * @param opts connection options.
       *
       * @returns `true` if the directory was created, `false` if it already exists.
       */
      async makeDir(path2, opts) {
        try {
          await this.rpc.makeDir(
            { path: path2 },
            {
              headers: authenticationHeader(this.envdApi.version, opts == null ? void 0 : opts.user),
              signal: this.connectionConfig.getSignal(
                opts == null ? void 0 : opts.requestTimeoutMs,
                opts == null ? void 0 : opts.signal
              )
            }
          );
          return true;
        } catch (err) {
          if (err instanceof import_connect3.ConnectError) {
            if (err.code === import_connect3.Code.AlreadyExists) {
              return false;
            }
          }
          throw handleFilesystemRpcError(err);
        }
      }
      /**
       * Rename a file or directory.
       *
       * @param oldPath path to the file or directory to rename.
       * @param newPath new path for the file or directory.
       * @param opts connection options.
       *
       * @returns information about renamed file or directory.
       */
      async rename(oldPath, newPath, opts) {
        try {
          const res = await this.rpc.move(
            {
              source: oldPath,
              destination: newPath
            },
            {
              headers: authenticationHeader(this.envdApi.version, opts == null ? void 0 : opts.user),
              signal: this.connectionConfig.getSignal(
                opts == null ? void 0 : opts.requestTimeoutMs,
                opts == null ? void 0 : opts.signal
              )
            }
          );
          const entry = res.entry;
          if (!entry) {
            throw new Error("Expected to receive information about moved object");
          }
          return {
            name: entry.name,
            type: mapFileType(entry.type),
            path: entry.path,
            size: Number(entry.size),
            mode: entry.mode,
            permissions: entry.permissions,
            owner: entry.owner,
            group: entry.group,
            modifiedTime: mapModifiedTime(entry.modifiedTime),
            symlinkTarget: entry.symlinkTarget
          };
        } catch (err) {
          throw handleFilesystemRpcError(err);
        }
      }
      /**
       * Remove a file or directory.
       *
       * @param path path to a file or directory.
       * @param opts connection options.
       */
      async remove(path2, opts) {
        try {
          await this.rpc.remove(
            { path: path2 },
            {
              headers: authenticationHeader(this.envdApi.version, opts == null ? void 0 : opts.user),
              signal: this.connectionConfig.getSignal(
                opts == null ? void 0 : opts.requestTimeoutMs,
                opts == null ? void 0 : opts.signal
              )
            }
          );
        } catch (err) {
          throw handleFilesystemRpcError(err);
        }
      }
      /**
       * Check if a file or a directory exists.
       *
       * @param path path to a file or a directory
       * @param opts connection options.
       *
       * @returns `true` if the file or directory exists, `false` otherwise
       */
      async exists(path2, opts) {
        try {
          await this.rpc.stat(
            { path: path2 },
            {
              headers: authenticationHeader(this.envdApi.version, opts == null ? void 0 : opts.user),
              signal: this.connectionConfig.getSignal(
                opts == null ? void 0 : opts.requestTimeoutMs,
                opts == null ? void 0 : opts.signal
              )
            }
          );
          return true;
        } catch (err) {
          if (err instanceof import_connect3.ConnectError) {
            if (err.code === import_connect3.Code.NotFound) {
              return false;
            }
          }
          throw handleFilesystemRpcError(err);
        }
      }
      /**
       * Get information about a file or directory.
       *
       * @param path path to a file or directory.
       * @param opts connection options.
       *
       * @returns information about the file or directory like name, type, and path.
       */
      async getInfo(path2, opts) {
        try {
          const res = await this.rpc.stat(
            { path: path2 },
            {
              headers: authenticationHeader(this.envdApi.version, opts == null ? void 0 : opts.user),
              signal: this.connectionConfig.getSignal(
                opts == null ? void 0 : opts.requestTimeoutMs,
                opts == null ? void 0 : opts.signal
              )
            }
          );
          if (!res.entry) {
            throw new Error(
              "Expected to receive information about the file or directory"
            );
          }
          return {
            name: res.entry.name,
            type: mapFileType(res.entry.type),
            path: res.entry.path,
            size: Number(res.entry.size),
            mode: res.entry.mode,
            permissions: res.entry.permissions,
            owner: res.entry.owner,
            group: res.entry.group,
            modifiedTime: mapModifiedTime(res.entry.modifiedTime),
            symlinkTarget: res.entry.symlinkTarget
          };
        } catch (err) {
          throw handleFilesystemRpcError(err);
        }
      }
      /**
       * Start watching a directory for filesystem events.
       *
       * @param path path to directory to watch.
       * @param onEvent callback to call when an event in the directory occurs.
       * @param opts connection options.
       *
       * @returns `WatchHandle` object for stopping watching directory.
       */
      async watchDir(path2, onEvent, opts) {
        var _a3, _b, _c;
        if ((opts == null ? void 0 : opts.recursive) && this.envdApi.version && (0, import_compare_versions2.compareVersions)(this.envdApi.version, ENVD_VERSION_RECURSIVE_WATCH) < 0) {
          throw new TemplateError(
            "You need to update the template to use recursive watching. You can do this by running `e2b template build` in the directory with the template."
          );
        }
        const requestTimeoutMs = (_a3 = opts == null ? void 0 : opts.requestTimeoutMs) != null ? _a3 : this.connectionConfig.requestTimeoutMs;
        const { controller, clearStartTimeout, cleanup } = setupRequestController(
          requestTimeoutMs,
          opts == null ? void 0 : opts.signal
        );
        const events = this.rpc.watchDir(
          {
            path: path2,
            recursive: (_b = opts == null ? void 0 : opts.recursive) != null ? _b : this.defaultWatchRecursive
          },
          {
            headers: __spreadProps(__spreadValues({}, authenticationHeader(this.envdApi.version, opts == null ? void 0 : opts.user)), {
              [KEEPALIVE_PING_HEADER]: KEEPALIVE_PING_INTERVAL_SEC.toString()
            }),
            signal: controller.signal,
            timeoutMs: (_c = opts == null ? void 0 : opts.timeoutMs) != null ? _c : this.defaultWatchTimeout
          }
        );
        try {
          await handleWatchDirStartEvent(events);
          clearStartTimeout();
          return new WatchHandle(cleanup, events, onEvent, opts == null ? void 0 : opts.onExit);
        } catch (err) {
          cleanup();
          throw handleFilesystemRpcError(err);
        }
      }
    };
    var CommandExitError = class extends SandboxError {
      static {
        __name(this, "CommandExitError");
      }
      constructor(result) {
        super(result.error);
        this.result = result;
        this.name = "CommandExitError";
      }
      /**
       * Command execution exit code.
       * `0` if the command finished successfully.
       */
      get exitCode() {
        return this.result.exitCode;
      }
      /**
       * Error message from command execution.
       */
      get error() {
        return this.result.error;
      }
      /**
       * Command execution stdout output.
       */
      get stdout() {
        return this.result.stdout;
      }
      /**
       * Command execution stderr output.
       */
      get stderr() {
        return this.result.stderr;
      }
    };
    var CommandHandle = class {
      static {
        __name(this, "CommandHandle");
      }
      /**
       * @hidden
       * @internal
       * @access protected
       */
      constructor(pid, handleDisconnect, handleKill, events, onStdout, onStderr, onPty) {
        this.pid = pid;
        this.handleDisconnect = handleDisconnect;
        this.handleKill = handleKill;
        this.events = events;
        this.onStdout = onStdout;
        this.onStderr = onStderr;
        this.onPty = onPty;
        this._stdout = "";
        this._stderr = "";
        this._wait = this.handleEvents();
      }
      /**
       * Command execution exit code.
       * `0` if the command finished successfully.
       *
       * It is `undefined` if the command is still running.
       */
      get exitCode() {
        var _a3;
        return (_a3 = this.result) == null ? void 0 : _a3.exitCode;
      }
      /**
       * Error message from command execution.
       */
      get error() {
        var _a3;
        return (_a3 = this.result) == null ? void 0 : _a3.error;
      }
      /**
       * Command execution stderr output.
       */
      get stderr() {
        return this._stderr;
      }
      /**
       * Command execution stdout output.
       */
      get stdout() {
        return this._stdout;
      }
      /**
       * Wait for the command to finish and return the result.
       * If the command exits with a non-zero exit code, it throws a `CommandExitError`.
       *
       * @returns `CommandResult` result of command execution.
       */
      async wait() {
        await this._wait;
        if (this.iterationError) {
          throw this.iterationError;
        }
        if (!this.result) {
          throw new SandboxError("Process exited without a result");
        }
        if (this.result.exitCode !== 0) {
          throw new CommandExitError(this.result);
        }
        return this.result;
      }
      /**
       * Disconnect from the command.
       *
       * The command is not killed, but SDK stops receiving events from the command.
       * You can reconnect to the command using {@link Commands.connect}.
       */
      async disconnect() {
        this.handleDisconnect();
      }
      /**
       * Kill the command.
       * It uses `SIGKILL` signal to kill the command.
       *
       * @returns `true` if the command was killed successfully, `false` if the command was not found.
       */
      async kill() {
        return await this.handleKill();
      }
      iterateEvents() {
        return __asyncGenerator(this, null, function* () {
          var _a3;
          try {
            for (var iter = __forAwait(this.events), more, temp, error; more = !(temp = yield new __await(iter.next())).done; more = false) {
              const event = temp.value;
              const e = (_a3 = event == null ? void 0 : event.event) == null ? void 0 : _a3.event;
              let out;
              switch (e == null ? void 0 : e.case) {
                case "data":
                  switch (e.value.output.case) {
                    case "stdout":
                      out = new TextDecoder().decode(e.value.output.value);
                      this._stdout += out;
                      yield [out, null, null];
                      break;
                    case "stderr":
                      out = new TextDecoder().decode(e.value.output.value);
                      this._stderr += out;
                      yield [null, out, null];
                      break;
                    case "pty":
                      yield [null, null, e.value.output.value];
                      break;
                  }
                  break;
                case "end":
                  this.result = {
                    exitCode: e.value.exitCode,
                    error: e.value.error,
                    stdout: this.stdout,
                    stderr: this.stderr
                  };
                  break;
              }
            }
          } catch (temp2) {
            error = [temp2];
          } finally {
            try {
              more && (temp = iter.return) && (yield new __await(temp.call(iter)));
            } finally {
              if (error)
                throw error[0];
            }
          }
        });
      }
      async handleEvents() {
        var _a3, _b, _c;
        try {
          try {
            for (var iter = __forAwait(this.iterateEvents()), more, temp, error; more = !(temp = await iter.next()).done; more = false) {
              const [stdout, stderr, pty] = temp.value;
              if (stdout !== null) {
                await ((_a3 = this.onStdout) == null ? void 0 : _a3.call(this, stdout));
              } else if (stderr !== null) {
                await ((_b = this.onStderr) == null ? void 0 : _b.call(this, stderr));
              } else if (pty) {
                await ((_c = this.onPty) == null ? void 0 : _c.call(this, pty));
              }
            }
          } catch (temp2) {
            error = [temp2];
          } finally {
            try {
              more && (temp = iter.return) && await temp.call(iter);
            } finally {
              if (error)
                throw error[0];
            }
          }
        } catch (e) {
          this.iterationError = handleRpcError(e);
        } finally {
          this.handleDisconnect();
        }
      }
    };
    var ALL_TRAFFIC = "0.0.0.0/0";
    function shellEscape(value) {
      return `'${value.replace(/'/g, `'"'"'`)}'`;
    }
    __name(shellEscape, "shellEscape");
    function withCredentials(url, username, password) {
      if (!username && !password) {
        return url;
      }
      if (!username || !password) {
        throw new InvalidArgumentError(
          "Both username and password are required when using Git credentials."
        );
      }
      let parsed;
      try {
        parsed = new URL(url);
      } catch (e) {
        throw new InvalidArgumentError(`Invalid Git URL: ${url}`);
      }
      if (parsed.protocol !== "http:" && parsed.protocol !== "https:") {
        throw new InvalidArgumentError(
          "Only http(s) Git URLs support username/password credentials."
        );
      }
      parsed.username = username;
      parsed.password = password;
      return parsed.toString();
    }
    __name(withCredentials, "withCredentials");
    function stripCredentials(url) {
      let parsed;
      try {
        parsed = new URL(url);
      } catch (e) {
        return url;
      }
      if (parsed.protocol !== "http:" && parsed.protocol !== "https:") {
        return url;
      }
      if (!parsed.username && !parsed.password) {
        return url;
      }
      parsed.username = "";
      parsed.password = "";
      return parsed.toString();
    }
    __name(stripCredentials, "stripCredentials");
    function deriveRepoDirFromUrl(url) {
      let parsed;
      try {
        parsed = new URL(url);
      } catch (e) {
        return void 0;
      }
      const trimmedPath = parsed.pathname.replace(/\/+$/, "");
      const lastSegment = trimmedPath.split("/").pop();
      if (!lastSegment) {
        return void 0;
      }
      return lastSegment.endsWith(".git") ? lastSegment.slice(0, -4) : lastSegment;
    }
    __name(deriveRepoDirFromUrl, "deriveRepoDirFromUrl");
    function buildGitCommand(args, repoPath) {
      const parts = ["git"];
      if (repoPath) {
        parts.push("-C", repoPath);
      }
      parts.push(...args);
      return parts.map((part) => shellEscape(part)).join(" ");
    }
    __name(buildGitCommand, "buildGitCommand");
    function buildPushArgs(remoteName, opts) {
      const { remote, branch, setUpstream } = opts;
      const args = ["push"];
      const targetRemote = remoteName != null ? remoteName : remote;
      if (setUpstream && targetRemote) {
        args.push("--set-upstream");
      }
      if (targetRemote) {
        args.push(targetRemote);
      }
      if (branch) {
        args.push(branch);
      }
      return args;
    }
    __name(buildPushArgs, "buildPushArgs");
    function parseAheadBehind(segment) {
      if (!segment) {
        return { ahead: 0, behind: 0 };
      }
      let ahead = 0;
      let behind = 0;
      if (segment.includes("ahead")) {
        try {
          ahead = Number.parseInt(
            segment.split("ahead")[1].split(",")[0].trim(),
            10
          );
        } catch (e) {
          ahead = 0;
        }
      }
      if (segment.includes("behind")) {
        try {
          behind = Number.parseInt(
            segment.split("behind")[1].split(",")[0].trim(),
            10
          );
        } catch (e) {
          behind = 0;
        }
      }
      return { ahead, behind };
    }
    __name(parseAheadBehind, "parseAheadBehind");
    function normalizeBranchName(name) {
      if (name.startsWith("HEAD (detached at ")) {
        return name.replace("HEAD (detached at ", "").replace(/\)$/, "");
      }
      return name.replace("HEAD (no branch)", "HEAD").replace("No commits yet on ", "").replace("Initial commit on ", "");
    }
    __name(normalizeBranchName, "normalizeBranchName");
    function deriveStatus(indexStatus, workingStatus) {
      const statuses = /* @__PURE__ */ new Set([indexStatus, workingStatus]);
      if (statuses.has("U")) return "conflict";
      if (statuses.has("R")) return "renamed";
      if (statuses.has("C")) return "copied";
      if (statuses.has("D")) return "deleted";
      if (statuses.has("A")) return "added";
      if (statuses.has("M")) return "modified";
      if (statuses.has("T")) return "typechange";
      if (statuses.has("?")) return "untracked";
      return "unknown";
    }
    __name(deriveStatus, "deriveStatus");
    function parseGitStatus(output) {
      const lines = output.split("\n").map((line) => line.replace(/\r$/, "")).filter((line) => line.trim().length > 0);
      let currentBranch;
      let upstream;
      let ahead = 0;
      let behind = 0;
      let detached = false;
      const fileStatus = [];
      if (lines.length === 0) {
        return {
          currentBranch,
          upstream,
          ahead,
          behind,
          detached,
          fileStatus,
          isClean: true,
          hasChanges: false,
          hasStaged: false,
          hasUntracked: false,
          hasConflicts: false,
          totalCount: 0,
          stagedCount: 0,
          unstagedCount: 0,
          untrackedCount: 0,
          conflictCount: 0
        };
      }
      const branchLine = lines[0];
      if (branchLine.startsWith("## ")) {
        const branchInfo = branchLine.slice(3);
        const aheadStart = branchInfo.indexOf(" [");
        const branchPart = aheadStart === -1 ? branchInfo : branchInfo.slice(0, aheadStart);
        const aheadPart = aheadStart === -1 ? void 0 : branchInfo.slice(aheadStart + 2, -1);
        const normalizedBranch = normalizeBranchName(branchPart);
        const rawBranch = branchPart;
        const isDetached = rawBranch.startsWith("HEAD (detached at ") || rawBranch.includes("detached");
        if (isDetached || normalizedBranch.startsWith("HEAD")) {
          detached = true;
        } else if (normalizedBranch.includes("...")) {
          const [branch, upstreamBranch] = normalizedBranch.split("...");
          currentBranch = branch || void 0;
          upstream = upstreamBranch || void 0;
        } else {
          currentBranch = normalizedBranch || void 0;
        }
        const aheadBehind = parseAheadBehind(aheadPart);
        ahead = aheadBehind.ahead;
        behind = aheadBehind.behind;
      }
      for (const line of lines.slice(1)) {
        if (line.startsWith("?? ")) {
          const name2 = line.slice(3);
          fileStatus.push({
            name: name2,
            status: "untracked",
            indexStatus: "?",
            workingTreeStatus: "?",
            staged: false
          });
          continue;
        }
        if (line.length < 3) {
          continue;
        }
        const indexStatus = line[0];
        const workingTreeStatus = line[1];
        const path2 = line.slice(3);
        let renamedFrom;
        let name = path2;
        if (path2.includes(" -> ")) {
          const parts = path2.split(" -> ");
          renamedFrom = parts[0];
          name = parts.slice(1).join(" -> ");
        }
        fileStatus.push(__spreadValues({
          name,
          status: deriveStatus(indexStatus, workingTreeStatus),
          indexStatus,
          workingTreeStatus,
          staged: indexStatus !== " " && indexStatus !== "?"
        }, renamedFrom ? { renamedFrom } : {}));
      }
      const totalCount = fileStatus.length;
      const stagedCount = fileStatus.filter((item) => item.staged).length;
      const untrackedCount = fileStatus.filter(
        (item) => item.status === "untracked"
      ).length;
      const conflictCount = fileStatus.filter(
        (item) => item.status === "conflict"
      ).length;
      const unstagedCount = totalCount - stagedCount;
      return {
        currentBranch,
        upstream,
        ahead,
        behind,
        detached,
        fileStatus,
        isClean: totalCount === 0,
        hasChanges: totalCount > 0,
        hasStaged: stagedCount > 0,
        hasUntracked: untrackedCount > 0,
        hasConflicts: conflictCount > 0,
        totalCount,
        stagedCount,
        unstagedCount,
        untrackedCount,
        conflictCount
      };
    }
    __name(parseGitStatus, "parseGitStatus");
    function parseGitBranches(output) {
      const branches = [];
      let currentBranch;
      const lines = output.split("\n").map((line) => line.trim()).filter((line) => line.length > 0);
      for (const line of lines) {
        const parts = line.split("	");
        const name = parts[0];
        branches.push(name);
        if (parts.length > 1 && parts[1] === "*") {
          currentBranch = name;
        }
      }
      return { branches, currentBranch };
    }
    __name(parseGitBranches, "parseGitBranches");
    function isAuthFailure(err) {
      if (!(err instanceof CommandExitError)) {
        return false;
      }
      const message = `${err.stderr}
${err.stdout}`.toLowerCase();
      const authSnippets = [
        "authentication failed",
        "terminal prompts disabled",
        "could not read username",
        "invalid username or password",
        "access denied",
        "permission denied",
        "not authorized"
      ];
      return authSnippets.some((snippet) => message.includes(snippet));
    }
    __name(isAuthFailure, "isAuthFailure");
    function getScopeFlag(scope) {
      if (scope !== "global" && scope !== "local" && scope !== "system") {
        throw new InvalidArgumentError(
          "Git config scope must be one of: global, local, system."
        );
      }
      return `--${scope}`;
    }
    __name(getScopeFlag, "getScopeFlag");
    function isMissingUpstream(err) {
      if (!(err instanceof CommandExitError)) {
        return false;
      }
      const message = `${err.stderr}
${err.stdout}`.toLowerCase();
      const upstreamSnippets = [
        "has no upstream branch",
        "no upstream branch",
        "no upstream configured",
        "no tracking information for the current branch",
        "no tracking information",
        "set the remote as upstream",
        "set the upstream branch",
        "please specify which branch you want to merge with"
      ];
      return upstreamSnippets.some((snippet) => message.includes(snippet));
    }
    __name(isMissingUpstream, "isMissingUpstream");
    function buildAuthErrorMessage(action, missingPassword) {
      if (missingPassword) {
        return `Git ${action} requires a password/token for private repositories.`;
      }
      return `Git ${action} requires credentials for private repositories.`;
    }
    __name(buildAuthErrorMessage, "buildAuthErrorMessage");
    function buildUpstreamErrorMessage(action) {
      if (action === "push") {
        return "Git push failed because no upstream branch is configured. Set upstream once with { setUpstream: true } (and optional remote/branch), or pass remote and branch explicitly.";
      }
      return "Git pull failed because no upstream branch is configured. Pass remote and branch explicitly, or set upstream once (push with { setUpstream: true } or run: git branch --set-upstream-to=origin/<branch> <branch>).";
    }
    __name(buildUpstreamErrorMessage, "buildUpstreamErrorMessage");
    function getRepoPathForScope(scope, path2) {
      if (scope !== "local") {
        return void 0;
      }
      if (!path2) {
        throw new InvalidArgumentError(
          'A repository path is required when using scope "local".'
        );
      }
      return path2;
    }
    __name(getRepoPathForScope, "getRepoPathForScope");
    var DEFAULT_GIT_ENV = {
      GIT_TERMINAL_PROMPT: "0"
    };
    var Git = class {
      static {
        __name(this, "Git");
      }
      constructor(commands) {
        this.commands = commands;
      }
      /**
       * Clone a git repository into the sandbox.
       *
       * @param url Git repository URL.
       * @param opts Clone options.
       * @returns Command result from the command runner.
       */
      async clone(url, opts) {
        const _a3 = opts != null ? opts : {}, {
          username,
          password,
          branch,
          depth,
          path: path2,
          dangerouslyStoreCredentials
        } = _a3, rest = __objRest(_a3, [
          "username",
          "password",
          "branch",
          "depth",
          "path",
          "dangerouslyStoreCredentials"
        ]);
        if (password && !username) {
          throw new InvalidArgumentError(
            "Username is required when using a password or token for git clone."
          );
        }
        const attemptClone = /* @__PURE__ */ __name(async (authUsername, authPassword) => {
          const urlWithCreds = authUsername && authPassword ? withCredentials(url, authUsername, authPassword) : url;
          const sanitizedUrl = stripCredentials(urlWithCreds);
          const stripInlineCreds = !dangerouslyStoreCredentials && sanitizedUrl !== urlWithCreds;
          const repoPath = stripInlineCreds ? path2 != null ? path2 : deriveRepoDirFromUrl(url) : path2;
          if (stripInlineCreds && !repoPath) {
            throw new InvalidArgumentError(
              "A destination path is required when using credentials without storing them."
            );
          }
          const args = ["clone", urlWithCreds];
          if (branch) args.push("--branch", branch, "--single-branch");
          if (depth) args.push("--depth", depth.toString());
          if (repoPath) args.push(repoPath);
          const result = await this.runGit(args, void 0, rest);
          if (stripInlineCreds) {
            await this.runGit(
              ["remote", "set-url", "origin", sanitizedUrl],
              repoPath,
              rest
            );
          }
          return result;
        }, "attemptClone");
        try {
          return await attemptClone(username, password);
        } catch (err) {
          if (isAuthFailure(err)) {
            throw new GitAuthError(
              buildAuthErrorMessage("clone", Boolean(username) && !password)
            );
          }
          throw err;
        }
      }
      /**
       * Initialize a new git repository.
       *
       * @param path Destination path for the repository.
       * @param opts Init options.
       * @returns Command result from the command runner.
       */
      async init(path2, opts) {
        const _a3 = opts != null ? opts : {}, { bare, initialBranch } = _a3, rest = __objRest(_a3, ["bare", "initialBranch"]);
        const args = ["init"];
        if (initialBranch) {
          args.push("--initial-branch", initialBranch);
        }
        if (bare) {
          args.push("--bare");
        }
        args.push(path2);
        return this.runGit(args, void 0, rest);
      }
      /**
       * Add (or update) a remote for a repository.
       *
       * @param path Repository path.
       * @param name Remote name (for example, `"origin"`).
       * @param url Remote URL.
       * @param opts Remote add options.
       * @returns Command result from the command runner.
       */
      async remoteAdd(path2, name, url, opts) {
        if (!name || !url) {
          throw new InvalidArgumentError(
            "Both remote name and URL are required to add a git remote."
          );
        }
        const _a3 = opts != null ? opts : {}, { fetch: fetch2, overwrite } = _a3, rest = __objRest(_a3, ["fetch", "overwrite"]);
        const addArgs = ["remote", "add"];
        if (fetch2) {
          addArgs.push("-f");
        }
        addArgs.push(name, url);
        if (!overwrite) {
          return this.runGit(addArgs, path2, rest);
        }
        const addCmd = buildGitCommand(addArgs, path2);
        const setUrlCmd = buildGitCommand(["remote", "set-url", name, url], path2);
        let cmd = `${addCmd} || ${setUrlCmd}`;
        if (fetch2) {
          const fetchCmd = buildGitCommand(["fetch", name], path2);
          cmd = `(${cmd}) && ${fetchCmd}`;
        }
        return this.runShell(cmd, rest);
      }
      /**
       * Get the URL for a git remote.
       *
       * Returns `undefined` when the remote does not exist.
       *
       * @param path Repository path.
       * @param name Remote name (for example, `"origin"`).
       * @param opts Command execution options.
       * @returns Remote URL if present.
       */
      async remoteGet(path2, name, opts) {
        if (!name) {
          throw new InvalidArgumentError("Remote name is required.");
        }
        const cmd = `${buildGitCommand(["remote", "get-url", name], path2)} || true`;
        const result = await this.runShell(cmd, opts);
        const trimmed = result.stdout.trim();
        return trimmed.length > 0 ? trimmed : void 0;
      }
      /**
       * Get repository status information.
       *
       * @param path Repository path.
       * @param opts Command execution options.
       * @returns Parsed git status.
       */
      async status(path2, opts) {
        const result = await this.runGit(
          ["status", "--porcelain=1", "-b"],
          path2,
          opts
        );
        return parseGitStatus(result.stdout);
      }
      /**
       * List branches in a repository.
       *
       * @param path Repository path.
       * @param opts Command execution options.
       * @returns Parsed branch list.
       */
      async branches(path2, opts) {
        const result = await this.runGit(
          ["branch", "--format=%(refname:short)	%(HEAD)"],
          path2,
          opts
        );
        return parseGitBranches(result.stdout);
      }
      /**
       * Create and check out a new branch.
       *
       * @param path Repository path.
       * @param branch Branch name to create.
       * @param opts Command execution options.
       * @returns Command result from the command runner.
       */
      async createBranch(path2, branch, opts) {
        return this.runGit(["checkout", "-b", branch], path2, opts);
      }
      /**
       * Check out an existing branch.
       *
       * @param path Repository path.
       * @param branch Branch name to check out.
       * @param opts Command execution options.
       * @returns Command result from the command runner.
       */
      async checkoutBranch(path2, branch, opts) {
        return this.runGit(["checkout", branch], path2, opts);
      }
      /**
       * Delete a branch.
       *
       * @param path Repository path.
       * @param branch Branch name to delete.
       * @param opts Delete options.
       * @returns Command result from the command runner.
       */
      async deleteBranch(path2, branch, opts) {
        const _a3 = opts != null ? opts : {}, { force } = _a3, rest = __objRest(_a3, ["force"]);
        const args = ["branch", force ? "-D" : "-d", branch];
        return this.runGit(args, path2, rest);
      }
      /**
       * Stage files for commit.
       *
       * @param path Repository path.
       * @param opts Add options.
       * @returns Command result from the command runner.
       */
      async add(path2, opts) {
        const _a3 = opts != null ? opts : {}, { files, all = true } = _a3, rest = __objRest(_a3, ["files", "all"]);
        const args = ["add"];
        if (!files || files.length === 0) {
          args.push(all ? "-A" : ".");
        } else {
          args.push("--", ...files);
        }
        return this.runGit(args, path2, rest);
      }
      /**
       * Create a commit in the repository.
       *
       * @param path Repository path.
       * @param message Commit message.
       * @param opts Commit options.
       * @returns Command result from the command runner.
       */
      async commit(path2, message, opts) {
        const _a3 = opts != null ? opts : {}, { authorName, authorEmail, allowEmpty } = _a3, rest = __objRest(_a3, ["authorName", "authorEmail", "allowEmpty"]);
        const args = ["commit", "-m", message];
        if (allowEmpty) {
          args.push("--allow-empty");
        }
        const authorArgs = [];
        if (authorName) {
          authorArgs.push("-c", `user.name=${authorName}`);
        }
        if (authorEmail) {
          authorArgs.push("-c", `user.email=${authorEmail}`);
        }
        return this.runGit([...authorArgs, ...args], path2, rest);
      }
      /**
       * Reset the current HEAD to a specified state.
       *
       * @param path Repository path.
       * @param opts Reset options.
       * @returns Command result from the command runner.
       */
      async reset(path2, opts) {
        const _a3 = opts != null ? opts : {}, { mode, target, paths: paths2 } = _a3, rest = __objRest(_a3, ["mode", "target", "paths"]);
        const allowedModes = [
          "soft",
          "mixed",
          "hard",
          "merge",
          "keep"
        ];
        if (mode && !allowedModes.includes(mode)) {
          throw new InvalidArgumentError(
            `Reset mode must be one of ${allowedModes.join(", ")}.`
          );
        }
        const args = ["reset"];
        if (mode) {
          args.push(`--${mode}`);
        }
        if (target) {
          args.push(target);
        }
        if (paths2 && paths2.length > 0) {
          args.push("--", ...paths2);
        }
        return this.runGit(args, path2, rest);
      }
      /**
       * Restore working tree files or unstage changes.
       *
       * @param path Repository path.
       * @param opts Restore options.
       * @returns Command result from the command runner.
       */
      async restore(path2, opts) {
        const _a3 = opts, { paths: paths2, staged, worktree, source } = _a3, rest = __objRest(_a3, ["paths", "staged", "worktree", "source"]);
        if (!paths2 || paths2.length === 0) {
          throw new InvalidArgumentError("At least one path is required.");
        }
        let resolvedStaged = staged;
        let resolvedWorktree = worktree;
        if (staged === void 0 && worktree === void 0) {
          resolvedWorktree = true;
        } else if (staged === true && worktree === void 0) {
          resolvedWorktree = false;
        } else if (staged === void 0 && worktree !== void 0) {
          resolvedStaged = false;
        }
        if (resolvedStaged === false && resolvedWorktree === false) {
          throw new InvalidArgumentError(
            "At least one of staged or worktree must be true."
          );
        }
        const args = ["restore"];
        if (resolvedWorktree) {
          args.push("--worktree");
        }
        if (resolvedStaged) {
          args.push("--staged");
        }
        if (source) {
          args.push("--source", source);
        }
        args.push("--", ...paths2);
        return this.runGit(args, path2, rest);
      }
      /**
       * Push commits to a remote.
       *
       * @param path Repository path.
       * @param opts Push options.
       * @returns Command result from the command runner.
       */
      async push(path2, opts) {
        const _a3 = opts != null ? opts : {}, {
          remote,
          branch,
          setUpstream = true,
          username,
          password
        } = _a3, rest = __objRest(_a3, [
          "remote",
          "branch",
          "setUpstream",
          "username",
          "password"
        ]);
        if (password && !username) {
          throw new InvalidArgumentError(
            "Username is required when using a password or token for git push."
          );
        }
        if (username && password) {
          const remoteName = await this.resolveRemoteName(path2, remote, rest);
          return this.withRemoteCredentials(
            path2,
            remoteName,
            username,
            password,
            rest,
            () => this.runGit(
              buildPushArgs(remoteName, { remote, branch, setUpstream }),
              path2,
              rest
            )
          );
        }
        try {
          return await this.runGit(
            buildPushArgs(void 0, { remote, branch, setUpstream }),
            path2,
            rest
          );
        } catch (err) {
          if (isAuthFailure(err)) {
            throw new GitAuthError(
              buildAuthErrorMessage("push", Boolean(username) && !password)
            );
          }
          if (isMissingUpstream(err)) {
            throw new GitUpstreamError(buildUpstreamErrorMessage("push"));
          }
          throw err;
        }
      }
      /**
       * Pull changes from a remote.
       *
       * @param path Repository path.
       * @param opts Pull options.
       * @returns Command result from the command runner.
       */
      async pull(path2, opts) {
        const _a3 = opts != null ? opts : {}, { remote, branch, username, password } = _a3, rest = __objRest(_a3, ["remote", "branch", "username", "password"]);
        if (password && !username) {
          throw new InvalidArgumentError(
            "Username is required when using a password or token for git pull."
          );
        }
        if (!remote && !branch) {
          const hasUpstream = await this.hasUpstream(path2, rest);
          if (!hasUpstream) {
            throw new GitUpstreamError(buildUpstreamErrorMessage("pull"));
          }
        }
        const buildArgs = /* @__PURE__ */ __name((remoteName) => {
          const args = ["pull"];
          const targetRemote = remoteName != null ? remoteName : remote;
          if (targetRemote) {
            args.push(targetRemote);
          }
          if (branch) {
            args.push(branch);
          }
          return args;
        }, "buildArgs");
        if (username && password) {
          const remoteName = await this.resolveRemoteName(path2, remote, rest);
          return this.withRemoteCredentials(
            path2,
            remoteName,
            username,
            password,
            rest,
            () => this.runGit(buildArgs(remoteName), path2, rest)
          );
        }
        try {
          return await this.runGit(buildArgs(), path2, rest);
        } catch (err) {
          if (isAuthFailure(err)) {
            throw new GitAuthError(
              buildAuthErrorMessage("pull", Boolean(username) && !password)
            );
          }
          if (isMissingUpstream(err)) {
            throw new GitUpstreamError(buildUpstreamErrorMessage("pull"));
          }
          throw err;
        }
      }
      /**
       * Set a git config value.
       *
       * Use `scope: "local"` together with `path` to configure a specific repository.
       *
       * @param key Git config key (for example, `"pull.rebase"`).
       * @param value Git config value.
       * @param opts Config options.
       * @returns Command result from the command runner.
       */
      async setConfig(key, value, opts) {
        var _a3;
        if (!key) {
          throw new InvalidArgumentError("Git config key is required.");
        }
        const scope = (_a3 = opts == null ? void 0 : opts.scope) != null ? _a3 : "global";
        const scopeFlag = getScopeFlag(scope);
        const repoPath = getRepoPathForScope(scope, opts == null ? void 0 : opts.path);
        return this.runGit(["config", scopeFlag, key, value], repoPath, opts);
      }
      /**
       * Get a git config value.
       *
       * Returns `undefined` when the key is not set in the requested scope.
       *
       * @param key Git config key (for example, `"pull.rebase"`).
       * @param opts Config options.
       * @returns The config value if present.
       */
      async getConfig(key, opts) {
        var _a3;
        if (!key) {
          throw new InvalidArgumentError("Git config key is required.");
        }
        const scope = (_a3 = opts == null ? void 0 : opts.scope) != null ? _a3 : "global";
        const scopeFlag = getScopeFlag(scope);
        const repoPath = getRepoPathForScope(scope, opts == null ? void 0 : opts.path);
        const cmd = `${buildGitCommand(["config", scopeFlag, "--get", key], repoPath)} || true`;
        const result = await this.runShell(cmd, opts);
        const trimmed = result.stdout.trim();
        return trimmed.length > 0 ? trimmed : void 0;
      }
      /**
       * Dangerously authenticate git globally via the credential helper.
       *
       * This persists credentials in the credential store.
       * Prefer short-lived credentials when possible.
       *
       * @param opts Authentication options.
       * @returns Command result from the command runner.
       */
      async dangerouslyAuthenticate(opts) {
        const _a3 = opts, { username, password, host, protocol } = _a3, rest = __objRest(_a3, ["username", "password", "host", "protocol"]);
        if (!username || !password) {
          throw new InvalidArgumentError(
            "Both username and password are required to authenticate git."
          );
        }
        const targetHost = (host != null ? host : "github.com").trim();
        const targetProtocol = (protocol != null ? protocol : "https").trim();
        const credentialInput = [
          `protocol=${targetProtocol}`,
          `host=${targetHost}`,
          `username=${username}`,
          `password=${password}`,
          "",
          ""
        ].join("\n");
        await this.runGit(
          ["config", "--global", "credential.helper", "store"],
          void 0,
          rest
        );
        const approveCmd = `printf %s ${shellEscape(credentialInput)} | ${buildGitCommand(
          ["credential", "approve"]
        )}`;
        return this.runShell(approveCmd, rest);
      }
      /**
       * Configure git user name and email.
       *
       * @param name Git user name.
       * @param email Git user email.
       * @param opts Config options.
       * @returns Command result from the command runner.
       */
      async configureUser(name, email, opts) {
        var _a3;
        if (!name || !email) {
          throw new InvalidArgumentError("Both name and email are required.");
        }
        const scope = (_a3 = opts == null ? void 0 : opts.scope) != null ? _a3 : "global";
        const configOpts = __spreadProps(__spreadValues({}, opts), { scope });
        await this.setConfig("user.name", name, configOpts);
        return this.setConfig("user.email", email, configOpts);
      }
      /**
       * Build and execute a git command inside the sandbox.
       *
       * @param args Git arguments to pass to the git binary.
       * @param repoPath Repository path used with `git -C`, if provided.
       * @param opts Command execution options.
       * @returns Command result from the command runner.
       */
      async runGit(args, repoPath, opts) {
        const _a3 = opts != null ? opts : {}, { envs } = _a3, rest = __objRest(_a3, ["envs"]);
        const cmd = buildGitCommand(args, repoPath);
        const mergedEnvs = __spreadValues(__spreadValues({}, DEFAULT_GIT_ENV), envs != null ? envs : {});
        return this.commands.run(cmd, __spreadProps(__spreadValues({}, rest), {
          envs: mergedEnvs
        }));
      }
      /**
       * Execute a raw shell command while applying default git environment variables.
       * 
       Note: We can likely just modify runGit later to allow appending commands to the git but for now it's separate.
       */
      async runShell(cmd, opts) {
        const _a3 = opts != null ? opts : {}, { envs } = _a3, rest = __objRest(_a3, ["envs"]);
        const mergedEnvs = __spreadValues(__spreadValues({}, DEFAULT_GIT_ENV), envs != null ? envs : {});
        return this.commands.run(cmd, __spreadProps(__spreadValues({}, rest), {
          envs: mergedEnvs
        }));
      }
      async getRemoteUrl(path2, remote, opts) {
        const result = await this.runGit(["remote", "get-url", remote], path2, opts);
        const url = result.stdout.trim();
        if (!url) {
          throw new InvalidArgumentError(
            `Remote "${remote}" URL not found in repository.`
          );
        }
        return url;
      }
      async resolveRemoteName(path2, remote, opts) {
        if (remote) {
          return remote;
        }
        const result = await this.runGit(["remote"], path2, opts);
        const remotes = result.stdout.split("\n").map((line) => line.trim()).filter(Boolean);
        if (remotes.length === 1) {
          return remotes[0];
        }
        throw new InvalidArgumentError(
          "Remote is required when using username/password and the repository has multiple remotes."
        );
      }
      async withRemoteCredentials(path2, remote, username, password, opts, operation) {
        const originalUrl = await this.getRemoteUrl(path2, remote, opts);
        const credentialUrl = withCredentials(originalUrl, username, password);
        await this.runGit(["remote", "set-url", remote, credentialUrl], path2, opts);
        let result;
        let operationError;
        try {
          result = await operation();
        } catch (err) {
          operationError = err;
        }
        let restoreError;
        try {
          await this.runGit(["remote", "set-url", remote, originalUrl], path2, opts);
        } catch (err) {
          restoreError = err;
        }
        if (operationError) {
          throw operationError;
        }
        if (restoreError) {
          throw restoreError;
        }
        return result;
      }
      async hasUpstream(path2, opts) {
        try {
          const result = await this.runGit(
            ["rev-parse", "--abbrev-ref", "--symbolic-full-name", "@{u}"],
            path2,
            opts
          );
          return result.stdout.trim().length > 0;
        } catch (e) {
          return false;
        }
      }
    };
    var import_openapi_fetch3 = __toESM(require_dist());
    var FILE_TIMEOUT_MS = 36e5;
    var VolumeConnectionConfig = class _VolumeConnectionConfig {
      static {
        __name(this, "_VolumeConnectionConfig");
      }
      constructor(volume, opts) {
        var _a3, _b;
        this.domain = (opts == null ? void 0 : opts.domain) || volume.domain || _VolumeConnectionConfig.domain;
        this.debug = (_b = (_a3 = opts == null ? void 0 : opts.debug) != null ? _a3 : volume.debug) != null ? _b : _VolumeConnectionConfig.debug;
        this.apiUrl = (opts == null ? void 0 : opts.apiUrl) || _VolumeConnectionConfig.volumeApiUrl || (this.debug ? "http://localhost:8080" : `https://api.${this.domain}`);
        this.token = (opts == null ? void 0 : opts.token) || volume.token;
        this.headers = opts == null ? void 0 : opts.headers;
        this.logger = opts == null ? void 0 : opts.logger;
        this.requestTimeoutMs = opts == null ? void 0 : opts.requestTimeoutMs;
        this.signal = opts == null ? void 0 : opts.signal;
      }
      static get domain() {
        return getEnvVar("E2B_DOMAIN") || "e2b.app";
      }
      static get debug() {
        return (getEnvVar("E2B_DEBUG") || "false").toLowerCase() === "true";
      }
      static get volumeApiUrl() {
        return getEnvVar("E2B_VOLUME_API_URL");
      }
      getSignal(requestTimeoutMs, signal) {
        return buildRequestSignal(
          requestTimeoutMs != null ? requestTimeoutMs : this.requestTimeoutMs,
          signal != null ? signal : this.signal
        );
      }
    };
    var VolumeApiClient = class {
      static {
        __name(this, "VolumeApiClient");
      }
      constructor(config) {
        this.api = (0, import_openapi_fetch3.default)({
          baseUrl: config.apiUrl,
          headers: __spreadValues(__spreadValues(__spreadValues({}, defaultHeaders), config.token && { Authorization: `Bearer ${config.token}` }), config.headers)
        });
        if (config.logger) {
          this.api.use(createApiLogger(config.logger));
        }
      }
    };
    var VolumeFileType = /* @__PURE__ */ ((VolumeFileType2) => {
      VolumeFileType2["UNKNOWN"] = "unknown";
      VolumeFileType2["FILE"] = "file";
      VolumeFileType2["DIRECTORY"] = "directory";
      VolumeFileType2["SYMLINK"] = "symlink";
      return VolumeFileType2;
    })(VolumeFileType || {});
    function convertVolumeEntryStat(entry) {
      return __spreadProps(__spreadValues({}, entry), {
        type: entry.type,
        atime: new Date(entry.atime),
        mtime: new Date(entry.mtime),
        ctime: new Date(entry.ctime)
      });
    }
    __name(convertVolumeEntryStat, "convertVolumeEntryStat");
    var Volume = class _Volume {
      static {
        __name(this, "_Volume");
      }
      /**
       * Create a local Volume instance with no API call.
       *
       * @param volumeId volume ID.
       * @param name volume name.
       * @param token volume auth token.
       * @param domain domain for the volume API.
       * @param debug whether to use debug mode.
       */
      constructor(volumeId, name, token, domain, debug) {
        this.volumeId = volumeId;
        this.name = name;
        this.token = token;
        this.domain = domain;
        this.debug = debug;
      }
      /**
       * Create a new volume.
       *
       * @param name name of the volume.
       * @param opts connection options.
       *
       * @returns new Volume instance.
       */
      static async create(name, opts) {
        const config = new ConnectionConfig(opts);
        const client = new ApiClient(config);
        const res = await client.api.POST("/volumes", {
          body: {
            name
          },
          signal: config.getSignal(opts == null ? void 0 : opts.requestTimeoutMs, opts == null ? void 0 : opts.signal)
        });
        const err = handleApiError(res, VolumeError);
        if (err) {
          throw err;
        }
        if (!res.data) {
          throw new Error("Response data is missing");
        }
        return new _Volume(
          res.data.volumeID,
          res.data.name,
          res.data.token,
          config.domain,
          config.debug
        );
      }
      /**
       * Connect to an existing volume by ID.
       *
       * @param volumeId volume ID.
       * @param opts connection options.
       *
       * @returns Volume instance.
       */
      static async connect(volumeId, opts) {
        const config = new ConnectionConfig(opts);
        const { name, token } = await _Volume.getInfo(volumeId, opts);
        return new _Volume(volumeId, name, token, config.domain, config.debug);
      }
      /**
       * Get volume information.
       *
       * @param volumeId volume ID.
       * @param opts connection options.
       *
       * @returns volume information.
       */
      static async getInfo(volumeId, opts) {
        const config = new ConnectionConfig(opts);
        const client = new ApiClient(config);
        const res = await client.api.GET("/volumes/{volumeID}", {
          params: {
            path: {
              volumeID: volumeId
            }
          },
          signal: config.getSignal(opts == null ? void 0 : opts.requestTimeoutMs, opts == null ? void 0 : opts.signal)
        });
        if (res.response.status === 404) {
          throw new NotFoundError(`Volume ${volumeId} not found`);
        }
        const err = handleApiError(res, VolumeError);
        if (err) {
          throw err;
        }
        return {
          volumeId: res.data.volumeID,
          name: res.data.name,
          token: res.data.token
        };
      }
      /**
       * List all volumes.
       *
       * @param opts connection options.
       *
       * @returns list of volume information.
       */
      static async list(opts) {
        var _a3;
        const config = new ConnectionConfig(opts);
        const client = new ApiClient(config);
        const res = await client.api.GET("/volumes", {
          signal: config.getSignal(opts == null ? void 0 : opts.requestTimeoutMs, opts == null ? void 0 : opts.signal)
        });
        const err = handleApiError(res, VolumeError);
        if (err) {
          throw err;
        }
        return ((_a3 = res.data) != null ? _a3 : []).map((vol) => ({
          volumeId: vol.volumeID,
          name: vol.name
        }));
      }
      /**
       * Destroy a volume.
       *
       * @param volumeId volume ID.
       * @param opts connection options.
       */
      static async destroy(volumeId, opts) {
        const config = new ConnectionConfig(opts);
        const client = new ApiClient(config);
        const res = await client.api.DELETE("/volumes/{volumeID}", {
          params: {
            path: {
              volumeID: volumeId
            }
          },
          signal: config.getSignal(opts == null ? void 0 : opts.requestTimeoutMs, opts == null ? void 0 : opts.signal)
        });
        if (res.response.status === 404) {
          return false;
        }
        const err = handleApiError(res, VolumeError);
        if (err) {
          throw err;
        }
        return true;
      }
      /**
       * List directory contents.
       *
       * @param path path to the directory.
       * @param opts connection options.
       * @param [opts.depth] number of layers deep to recurse into the directory (default: 1).
       *
       * @returns list of entries in the directory.
       */
      async list(path2, opts) {
        const config = new VolumeConnectionConfig(this, opts);
        const client = new VolumeApiClient(config);
        const res = await client.api.GET("/volumecontent/{volumeID}/dir", {
          params: {
            path: {
              volumeID: this.volumeId
            },
            query: {
              path: path2,
              depth: opts == null ? void 0 : opts.depth
            }
          },
          signal: config.getSignal()
        });
        if (res.response.status === 404) {
          throw new NotFoundError(`Path ${path2} not found`);
        }
        const err = handleApiError(res, VolumeError);
        if (err) {
          throw err;
        }
        const entries = Array.isArray(res.data) ? res.data : [];
        return entries.map(convertVolumeEntryStat);
      }
      /**
       * Create a directory.
       *
       * @param path path to the directory to create.
       * @param options directory creation options.
       * @param opts connection options.
       */
      async makeDir(path2, opts) {
        const config = new VolumeConnectionConfig(this, opts);
        const client = new VolumeApiClient(config);
        const res = await client.api.POST("/volumecontent/{volumeID}/dir", {
          params: {
            path: {
              volumeID: this.volumeId
            },
            query: {
              path: path2,
              uid: opts == null ? void 0 : opts.uid,
              gid: opts == null ? void 0 : opts.gid,
              mode: opts == null ? void 0 : opts.mode,
              force: opts == null ? void 0 : opts.force
            }
          },
          signal: config.getSignal()
        });
        if (res.response.status === 404) {
          throw new NotFoundError(`Path ${path2} not found`);
        }
        const err = handleApiError(res, VolumeError);
        if (err) {
          throw err;
        }
        if (!res.data) {
          throw new Error("Response data is missing");
        }
        return convertVolumeEntryStat(
          res.data
        );
      }
      /**
       * Get information about a file or directory.
       *
       * @param path path to the file or directory.
       * @param opts connection options.
       *
       * @returns information about the entry.
       */
      async getInfo(path2, opts) {
        const config = new VolumeConnectionConfig(this, opts);
        const client = new VolumeApiClient(config);
        const res = await client.api.GET("/volumecontent/{volumeID}/path", {
          params: {
            path: {
              volumeID: this.volumeId
            },
            query: {
              path: path2
            }
          },
          signal: config.getSignal()
        });
        if (res.response.status === 404) {
          throw new NotFoundError(`Path ${path2} not found`);
        }
        const err = handleApiError(res, VolumeError);
        if (err) {
          throw err;
        }
        if (!res.data) {
          throw new Error("Response data is missing");
        }
        return convertVolumeEntryStat(
          res.data
        );
      }
      /**
       * Check whether a file or directory exists.
       *
       * Uses {@link getInfo} under the hood. Returns `true` if the path exists,
       * `false` if it does not (404). Other errors are rethrown.
       *
       * @param path path to the file or directory.
       * @param opts connection options.
       *
       * @returns `true` if the path exists, `false` otherwise.
       */
      async exists(path2, opts) {
        try {
          await this.getInfo(path2, opts);
          return true;
        } catch (err) {
          if (err instanceof NotFoundError) {
            return false;
          }
          throw err;
        }
      }
      /**
       * Update file or directory metadata.
       *
       * @param path path to the file or directory.
       * @param metadata metadata to update (uid, gid, mode).
       * @param opts connection options.
       *
       * @returns updated entry information.
       */
      async updateMetadata(path2, metadata, opts) {
        const config = new VolumeConnectionConfig(this, opts);
        const client = new VolumeApiClient(config);
        const res = await client.api.PATCH("/volumecontent/{volumeID}/path", {
          params: {
            path: {
              volumeID: this.volumeId
            },
            query: {
              path: path2
            }
          },
          body: {
            uid: metadata.uid,
            gid: metadata.gid,
            mode: metadata.mode
          },
          signal: config.getSignal()
        });
        if (res.response.status === 404) {
          throw new NotFoundError(`Path ${path2} not found`);
        }
        const err = handleApiError(res, VolumeError);
        if (err) {
          throw err;
        }
        if (!res.data) {
          throw new Error("Response data is missing");
        }
        return convertVolumeEntryStat(
          res.data
        );
      }
      async readFile(path2, opts) {
        var _a3, _b;
        const format = (_a3 = opts == null ? void 0 : opts.format) != null ? _a3 : "text";
        const config = new VolumeConnectionConfig(this, __spreadProps(__spreadValues({}, opts), {
          requestTimeoutMs: (_b = opts == null ? void 0 : opts.requestTimeoutMs) != null ? _b : FILE_TIMEOUT_MS
        }));
        const client = new VolumeApiClient(config);
        const res = await client.api.GET("/volumecontent/{volumeID}/file", {
          params: {
            path: {
              volumeID: this.volumeId
            },
            query: {
              path: path2
            }
          },
          parseAs: format === "bytes" ? "arrayBuffer" : format,
          signal: config.getSignal()
        });
        if (res.response.status === 404) {
          throw new NotFoundError(`Path ${path2} not found`);
        }
        const err = handleApiError(res, VolumeError);
        if (err) {
          throw err;
        }
        if (format === "bytes") {
          return new Uint8Array(res.data);
        }
        if (format === "text") {
          if (res.response.headers.get("content-length") === "0") {
            return "";
          }
          return typeof res.data === "string" ? res.data : "";
        }
        return res.data;
      }
      /**
       * Write content to a file.
       *
       * Writing to a file that doesn't exist creates the file.
       *
       * Writing to a file that already exists overwrites the file.
       *
       * @param path path to the file.
       * @param data data to write to the file. Data can be a string, `ArrayBuffer`, `Blob`, or `ReadableStream`.
       * @param options file creation options.
       * @param opts connection options.
       *
       * @returns information about the written file
       */
      async writeFile(path2, data, opts) {
        var _a3;
        const config = new VolumeConnectionConfig(this, __spreadProps(__spreadValues({}, opts), {
          requestTimeoutMs: (_a3 = opts == null ? void 0 : opts.requestTimeoutMs) != null ? _a3 : FILE_TIMEOUT_MS
        }));
        const client = new VolumeApiClient(config);
        const blob = await toBlob(data);
        const res = await client.api.PUT("/volumecontent/{volumeID}/file", {
          params: {
            path: {
              volumeID: this.volumeId
            },
            query: {
              path: path2,
              uid: opts == null ? void 0 : opts.uid,
              gid: opts == null ? void 0 : opts.gid,
              mode: opts == null ? void 0 : opts.mode,
              force: opts == null ? void 0 : opts.force
            }
          },
          bodySerializer: /* @__PURE__ */ __name(() => blob, "bodySerializer"),
          body: {},
          headers: {
            "Content-Type": "application/octet-stream"
          },
          signal: config.getSignal()
        });
        if (res.response.status === 404) {
          throw new NotFoundError(`Path ${path2} not found`);
        }
        const err = handleApiError(res, VolumeError);
        if (err) {
          throw err;
        }
        if (!res.data) {
          throw new Error("Response data is missing");
        }
        return convertVolumeEntryStat(
          res.data
        );
      }
      /**
       * Remove a file or directory.
       *
       * @param path path to the file or directory to remove.
       * @param opts connection options.
       */
      async remove(path2, opts) {
        const config = new VolumeConnectionConfig(this, opts);
        const client = new VolumeApiClient(config);
        const res = await client.api.DELETE("/volumecontent/{volumeID}/path", {
          params: {
            path: {
              volumeID: this.volumeId
            },
            query: {
              path: path2
            }
          },
          signal: config.getSignal()
        });
        if (res.response.status === 404) {
          throw new NotFoundError(`Path ${path2} not found`);
        }
        const err = handleApiError(res, VolumeError);
        if (err) {
          throw err;
        }
      }
    };
    var import_connect_web = require_cjs3();
    var envdFetch;
    var envdRpcFetch;
    var DEFAULT_ENVD_CONNECTION_LIMIT = 10;
    var DEFAULT_ENVD_RPC_CONNECTION_LIMIT = 200;
    var DEFAULT_ENVD_INFLIGHT_LIMIT = 2e3;
    var DEFAULT_ENVD_RPC_INFLIGHT_LIMIT = 2e3;
    function createEnvdFetchForRuntime(currentRuntime = runtime, options = {}) {
      if (currentRuntime !== "node") {
        return fetch;
      }
      let fetcherPromise;
      return async (input, init) => {
        fetcherPromise != null ? fetcherPromise : fetcherPromise = buildEnvdFetcher(options);
        const fetcher = await fetcherPromise;
        return fetcher(input, init);
      };
    }
    __name(createEnvdFetchForRuntime, "createEnvdFetchForRuntime");
    async function buildEnvdFetcher(options) {
      var _a3, _b, _c;
      const undici = await ((_a3 = options.loadUndici) != null ? _a3 : loadUndici)();
      const inflightLimit = (_b = options.inflightLimit) != null ? _b : 0;
      if (!undici) {
        return limitConcurrency(fetch, inflightLimit);
      }
      const { Agent, fetch: undiciFetch } = undici;
      const dispatcherOptions = {
        allowH2: true,
        connections: (_c = options.connectionLimit) != null ? _c : DEFAULT_ENVD_CONNECTION_LIMIT
      };
      const dispatcher = new Agent(dispatcherOptions);
      const fetchWithDispatcher = undiciFetch;
      const wrapped = /* @__PURE__ */ __name((input, init) => {
        const request = toUndiciRequestInput(input, init);
        return fetchWithDispatcher(request.input, __spreadProps(__spreadValues({}, request.init), {
          dispatcher
        }));
      }, "wrapped");
      return limitConcurrency(wrapped, inflightLimit);
    }
    __name(buildEnvdFetcher, "buildEnvdFetcher");
    function createEnvdFetch() {
      if (envdFetch) {
        return envdFetch;
      }
      envdFetch = createEnvdFetchForRuntime(runtime, {
        inflightLimit: getEnvdInflightLimit()
      });
      return envdFetch;
    }
    __name(createEnvdFetch, "createEnvdFetch");
    function createEnvdRpcFetch() {
      if (envdRpcFetch) {
        return envdRpcFetch;
      }
      envdRpcFetch = createEnvdFetchForRuntime(runtime, {
        connectionLimit: getEnvdRpcConnectionLimit(),
        inflightLimit: getEnvdRpcInflightLimit()
      });
      return envdRpcFetch;
    }
    __name(createEnvdRpcFetch, "createEnvdRpcFetch");
    function getEnvdRpcConnectionLimit() {
      return parsePositiveIntEnv(
        "E2B_ENVD_RPC_CONNECTIONS",
        DEFAULT_ENVD_RPC_CONNECTION_LIMIT
      );
    }
    __name(getEnvdRpcConnectionLimit, "getEnvdRpcConnectionLimit");
    function getEnvdInflightLimit() {
      return parseInflightLimitEnv(
        "E2B_ENVD_INFLIGHT_REQUESTS",
        DEFAULT_ENVD_INFLIGHT_LIMIT
      );
    }
    __name(getEnvdInflightLimit, "getEnvdInflightLimit");
    function getEnvdRpcInflightLimit() {
      return parseInflightLimitEnv(
        "E2B_ENVD_RPC_INFLIGHT_REQUESTS",
        DEFAULT_ENVD_RPC_INFLIGHT_LIMIT
      );
    }
    __name(getEnvdRpcInflightLimit, "getEnvdRpcInflightLimit");
    var import_connect5 = require_cjs2();
    var import_compare_versions3 = require_umd();
    var import_codegenv22 = require_codegenv2();
    var file_process_process = /* @__PURE__ */ (0, import_codegenv22.fileDesc)(
      ""
    );
    var Process = /* @__PURE__ */ (0, import_codegenv22.serviceDesc)(file_process_process, 0);
    var import_connect4 = require_cjs2();
    var Pty = class {
      static {
        __name(this, "Pty");
      }
      // 60 seconds
      constructor(transport, connectionConfig, metadata) {
        this.transport = transport;
        this.connectionConfig = connectionConfig;
        this.defaultPtyConnectionTimeout = 6e4;
        this.rpc = (0, import_connect4.createClient)(Process, this.transport);
        this.envdVersion = metadata.version;
      }
      /**
       * Create a new PTY (pseudo-terminal).
       *
       * @param opts options for creating the PTY.
       *
       * @returns handle to interact with the PTY.
       */
      async create(opts) {
        var _a3, _b, _c, _d, _e, _f;
        const requestTimeoutMs = (_a3 = opts == null ? void 0 : opts.requestTimeoutMs) != null ? _a3 : this.connectionConfig.requestTimeoutMs;
        const envs = (_b = opts == null ? void 0 : opts.envs) != null ? _b : {};
        envs.TERM = (_c = envs.TERM) != null ? _c : "xterm-256color";
        envs.LANG = (_d = envs.LANG) != null ? _d : "C.UTF-8";
        envs.LC_ALL = (_e = envs.LC_ALL) != null ? _e : "C.UTF-8";
        const { controller, clearStartTimeout, cleanup } = setupRequestController(
          requestTimeoutMs,
          opts == null ? void 0 : opts.signal
        );
        const events = this.rpc.start(
          {
            process: {
              cmd: "/bin/bash",
              args: ["-i", "-l"],
              envs,
              cwd: opts == null ? void 0 : opts.cwd
            },
            pty: {
              size: {
                cols: opts.cols,
                rows: opts.rows
              }
            }
          },
          {
            headers: __spreadProps(__spreadValues({}, authenticationHeader(this.envdVersion, opts == null ? void 0 : opts.user)), {
              [KEEPALIVE_PING_HEADER]: KEEPALIVE_PING_INTERVAL_SEC.toString()
            }),
            signal: controller.signal,
            timeoutMs: (_f = opts == null ? void 0 : opts.timeoutMs) != null ? _f : this.defaultPtyConnectionTimeout
          }
        );
        try {
          const pid = await handleProcessStartEvent(events);
          clearStartTimeout();
          return new CommandHandle(
            pid,
            cleanup,
            () => this.kill(pid),
            events,
            void 0,
            void 0,
            opts.onData
          );
        } catch (err) {
          cleanup();
          throw handleRpcError(err);
        }
      }
      /**
       * Connect to a running PTY.
       *
       * @param pid process ID of the PTY to connect to. You can get the list of running PTYs using {@link Commands.list}.
       * @param opts connection options.
       *
       * @returns handle to interact with the PTY.
       */
      async connect(pid, opts) {
        var _a3, _b;
        const requestTimeoutMs = (_a3 = opts == null ? void 0 : opts.requestTimeoutMs) != null ? _a3 : this.connectionConfig.requestTimeoutMs;
        const { controller, clearStartTimeout, cleanup } = setupRequestController(
          requestTimeoutMs,
          opts == null ? void 0 : opts.signal
        );
        const events = this.rpc.connect(
          {
            process: {
              selector: {
                case: "pid",
                value: pid
              }
            }
          },
          {
            signal: controller.signal,
            headers: {
              [KEEPALIVE_PING_HEADER]: KEEPALIVE_PING_INTERVAL_SEC.toString()
            },
            timeoutMs: (_b = opts == null ? void 0 : opts.timeoutMs) != null ? _b : this.defaultPtyConnectionTimeout
          }
        );
        try {
          const pid2 = await handleProcessStartEvent(events);
          clearStartTimeout();
          return new CommandHandle(
            pid2,
            cleanup,
            () => this.kill(pid2),
            events,
            void 0,
            void 0,
            opts == null ? void 0 : opts.onData
          );
        } catch (err) {
          cleanup();
          throw handleRpcError(err);
        }
      }
      /**
       * Send input to a PTY.
       *
       * @param pid process ID of the PTY.
       * @param data input data to send to the PTY.
       * @param opts connection options.
       */
      async sendInput(pid, data, opts) {
        try {
          await this.rpc.sendInput(
            {
              input: {
                input: {
                  case: "pty",
                  value: data
                }
              },
              process: {
                selector: {
                  case: "pid",
                  value: pid
                }
              }
            },
            {
              signal: this.connectionConfig.getSignal(
                opts == null ? void 0 : opts.requestTimeoutMs,
                opts == null ? void 0 : opts.signal
              )
            }
          );
        } catch (err) {
          throw handleRpcError(err);
        }
      }
      /**
       * Resize PTY.
       * Call this when the terminal window is resized and the number of columns and rows has changed.
       *
       * @param pid process ID of the PTY.
       * @param size new size of the PTY.
       * @param opts connection options.
       */
      async resize(pid, size, opts) {
        try {
          await this.rpc.update(
            {
              process: {
                selector: {
                  case: "pid",
                  value: pid
                }
              },
              pty: {
                size
              }
            },
            {
              signal: this.connectionConfig.getSignal(
                opts == null ? void 0 : opts.requestTimeoutMs,
                opts == null ? void 0 : opts.signal
              )
            }
          );
        } catch (err) {
          throw handleRpcError(err);
        }
      }
      /**
       * Kill a running PTY specified by process ID.
       * It uses `SIGKILL` signal to kill the PTY.
       *
       * @param pid process ID of the PTY.
       * @param opts connection options.
       *
       * @returns `true` if the PTY was killed, `false` if the PTY was not found.
       */
      async kill(pid, opts) {
        try {
          await this.rpc.sendSignal(
            {
              process: {
                selector: {
                  case: "pid",
                  value: pid
                }
              },
              signal: 9
              /* SIGKILL */
            },
            {
              signal: this.connectionConfig.getSignal(
                opts == null ? void 0 : opts.requestTimeoutMs,
                opts == null ? void 0 : opts.signal
              )
            }
          );
          return true;
        } catch (err) {
          if (err instanceof import_connect4.ConnectError) {
            if (err.code === import_connect4.Code.NotFound) {
              return false;
            }
          }
          throw handleRpcError(err);
        }
      }
    };
    var Commands = class {
      static {
        __name(this, "Commands");
      }
      constructor(transport, connectionConfig, metadata) {
        this.connectionConfig = connectionConfig;
        this.defaultProcessConnectionTimeout = 6e4;
        this.rpc = (0, import_connect5.createClient)(Process, transport);
        this.envdVersion = metadata.version;
      }
      /**
       * @hidden
       * @internal
       */
      get supportsStdinClose() {
        return (0, import_compare_versions3.compareVersions)(this.envdVersion, ENVD_ENVD_CLOSE) >= 0;
      }
      /**
       * List all running commands and PTY sessions.
       *
       * @param opts connection options.
       *
       * @returns list of running commands and PTY sessions.
       */
      async list(opts) {
        try {
          const res = await this.rpc.list(
            {},
            {
              signal: this.connectionConfig.getSignal(
                opts == null ? void 0 : opts.requestTimeoutMs,
                opts == null ? void 0 : opts.signal
              )
            }
          );
          return res.processes.map((p) => __spreadValues(__spreadProps(__spreadValues({
            pid: p.pid
          }, p.tag && { tag: p.tag }), {
            args: p.config.args,
            envs: p.config.envs,
            cmd: p.config.cmd
          }), p.config.cwd && { cwd: p.config.cwd }));
        } catch (err) {
          throw handleRpcError(err);
        }
      }
      /**
       * Send data to command stdin.
       *
       * @param pid process ID of the command. You can get the list of running commands using {@link Commands.list}.
       * @param data data to send to the command.
       * @param opts connection options.
       */
      async sendStdin(pid, data, opts) {
        try {
          const payload = typeof data === "string" ? new TextEncoder().encode(data) : data;
          await this.rpc.sendInput(
            {
              process: {
                selector: {
                  case: "pid",
                  value: pid
                }
              },
              input: {
                input: {
                  case: "stdin",
                  value: payload
                }
              }
            },
            {
              signal: this.connectionConfig.getSignal(
                opts == null ? void 0 : opts.requestTimeoutMs,
                opts == null ? void 0 : opts.signal
              )
            }
          );
        } catch (err) {
          throw handleRpcError(err);
        }
      }
      /**
       * @hidden
       * @internal
       */
      async closeStdin(pid, opts) {
        if (!this.supportsStdinClose) {
          throw new SandboxError(
            `Sandbox envd version ${this.envdVersion} doesn't support closeStdin. Please rebuild your template to pick up the latest sandbox version.`
          );
        }
        try {
          await this.rpc.closeStdin(
            {
              process: {
                selector: {
                  case: "pid",
                  value: pid
                }
              }
            },
            {
              signal: this.connectionConfig.getSignal(
                opts == null ? void 0 : opts.requestTimeoutMs,
                opts == null ? void 0 : opts.signal
              )
            }
          );
        } catch (err) {
          throw handleRpcError(err);
        }
      }
      /**
       * Kill a running command specified by its process ID.
       * It uses `SIGKILL` signal to kill the command.
       *
       * @param pid process ID of the command. You can get the list of running commands using {@link Commands.list}.
       * @param opts connection options.
       *
       * @returns `true` if the command was killed, `false` if the command was not found.
       */
      async kill(pid, opts) {
        try {
          await this.rpc.sendSignal(
            {
              process: {
                selector: {
                  case: "pid",
                  value: pid
                }
              },
              signal: 9
              /* SIGKILL */
            },
            {
              signal: this.connectionConfig.getSignal(
                opts == null ? void 0 : opts.requestTimeoutMs,
                opts == null ? void 0 : opts.signal
              )
            }
          );
          return true;
        } catch (err) {
          if (err instanceof import_connect5.ConnectError) {
            if (err.code === import_connect5.Code.NotFound) {
              return false;
            }
          }
          throw handleRpcError(err);
        }
      }
      /**
       * Connect to a running command.
       * You can use {@link CommandHandle.wait} to wait for the command to finish and get execution results.
       *
       * @param pid process ID of the command to connect to. You can get the list of running commands using {@link Commands.list}.
       * @param opts connection options.
       *
       * @returns `CommandHandle` handle to interact with the running command.
       */
      async connect(pid, opts) {
        var _a3, _b;
        const requestTimeoutMs = (_a3 = opts == null ? void 0 : opts.requestTimeoutMs) != null ? _a3 : this.connectionConfig.requestTimeoutMs;
        const { controller, clearStartTimeout, cleanup } = setupRequestController(
          requestTimeoutMs,
          opts == null ? void 0 : opts.signal
        );
        const events = this.rpc.connect(
          {
            process: {
              selector: {
                case: "pid",
                value: pid
              }
            }
          },
          {
            signal: controller.signal,
            headers: {
              [KEEPALIVE_PING_HEADER]: KEEPALIVE_PING_INTERVAL_SEC.toString()
            },
            timeoutMs: (_b = opts == null ? void 0 : opts.timeoutMs) != null ? _b : this.defaultProcessConnectionTimeout
          }
        );
        try {
          const pid2 = await handleProcessStartEvent(events);
          clearStartTimeout();
          return new CommandHandle(
            pid2,
            cleanup,
            () => this.kill(pid2),
            events,
            opts == null ? void 0 : opts.onStdout,
            opts == null ? void 0 : opts.onStderr,
            void 0
          );
        } catch (err) {
          cleanup();
          throw handleRpcError(err);
        }
      }
      async run(cmd, opts) {
        const proc = await this.start(cmd, opts);
        return (opts == null ? void 0 : opts.background) ? proc : proc.wait();
      }
      async start(cmd, opts) {
        var _a3, _b;
        if ((opts == null ? void 0 : opts.stdin) === false && (0, import_compare_versions3.compareVersions)(this.envdVersion, ENVD_COMMANDS_STDIN) < 0) {
          throw new SandboxError(
            `Sandbox envd version ${this.envdVersion} can't specify stdin, it's always turned on. Please rebuild your template if you need this feature.`
          );
        }
        const requestTimeoutMs = (_a3 = opts == null ? void 0 : opts.requestTimeoutMs) != null ? _a3 : this.connectionConfig.requestTimeoutMs;
        const { controller, clearStartTimeout, cleanup } = setupRequestController(
          requestTimeoutMs,
          opts == null ? void 0 : opts.signal
        );
        const events = this.rpc.start(
          {
            process: {
              cmd: "/bin/bash",
              cwd: opts == null ? void 0 : opts.cwd,
              envs: opts == null ? void 0 : opts.envs,
              args: ["-l", "-c", cmd]
            },
            stdin: (opts == null ? void 0 : opts.stdin) || false
          },
          {
            headers: __spreadProps(__spreadValues({}, authenticationHeader(this.envdVersion, opts == null ? void 0 : opts.user)), {
              [KEEPALIVE_PING_HEADER]: KEEPALIVE_PING_INTERVAL_SEC.toString()
            }),
            signal: controller.signal,
            timeoutMs: (_b = opts == null ? void 0 : opts.timeoutMs) != null ? _b : this.defaultProcessConnectionTimeout
          }
        );
        try {
          const pid = await handleProcessStartEvent(events);
          clearStartTimeout();
          return new CommandHandle(
            pid,
            cleanup,
            () => this.kill(pid),
            events,
            opts == null ? void 0 : opts.onStdout,
            opts == null ? void 0 : opts.onStderr,
            void 0
          );
        } catch (err) {
          cleanup();
          throw handleRpcError(err);
        }
      }
    };
    var import_compare_versions4 = require_umd();
    function resolveNetworkSelector(selector, rules) {
      if (selector === void 0) {
        return void 0;
      }
      if (typeof selector === "function") {
        return selector({ allTraffic: ALL_TRAFFIC, rules });
      }
      return selector;
    }
    __name(resolveNetworkSelector, "resolveNetworkSelector");
    function resolveRulesForBody(rules) {
      const out = {};
      for (const [host, hostRules] of rules) {
        out[host] = hostRules.map(
          (rule) => rule.transform === void 0 ? {} : { transform: rule.transform }
        );
      }
      return out;
    }
    __name(resolveRulesForBody, "resolveRulesForBody");
    function buildNetworkEgress(network) {
      var _a3;
      const rules = network.rules instanceof Map ? network.rules : new Map(Object.entries((_a3 = network.rules) != null ? _a3 : {}));
      const allowOut = resolveNetworkSelector(network.allowOut, rules);
      const denyOut = resolveNetworkSelector(network.denyOut, rules);
      return __spreadValues(__spreadValues(__spreadValues({}, allowOut !== void 0 ? { allowOut } : {}), denyOut !== void 0 ? { denyOut } : {}), network.rules !== void 0 ? { rules: resolveRulesForBody(rules) } : {});
    }
    __name(buildNetworkEgress, "buildNetworkEgress");
    function buildNetworkBody(network) {
      if (!network) {
        return void 0;
      }
      return __spreadValues(__spreadValues(__spreadValues({}, buildNetworkEgress(network)), network.allowPublicTraffic !== void 0 ? { allowPublicTraffic: network.allowPublicTraffic } : {}), network.maskRequestHost !== void 0 ? { maskRequestHost: network.maskRequestHost } : {});
    }
    __name(buildNetworkBody, "buildNetworkBody");
    function buildNetworkUpdateBody(network) {
      return __spreadValues(__spreadValues({}, buildNetworkEgress(network)), network.allowInternetAccess !== void 0 ? { allow_internet_access: network.allowInternetAccess } : {});
    }
    __name(buildNetworkUpdateBody, "buildNetworkUpdateBody");
    var SandboxApi = class {
      static {
        __name(this, "SandboxApi");
      }
      constructor() {
      }
      /**
       * Kill the sandbox specified by sandbox ID.
       *
       * @param sandboxId sandbox ID.
       * @param opts connection options.
       *
       * @returns `true` if the sandbox was found and killed, `false` otherwise.
       */
      static async kill(sandboxId, opts) {
        var _a3;
        const config = new ConnectionConfig(opts);
        const client = new ApiClient(config);
        const res = await client.api.DELETE("/sandboxes/{sandboxID}", {
          params: {
            path: {
              sandboxID: sandboxId
            }
          },
          signal: config.getSignal(opts == null ? void 0 : opts.requestTimeoutMs, opts == null ? void 0 : opts.signal)
        });
        if (((_a3 = res.error) == null ? void 0 : _a3.code) === 404) {
          return false;
        }
        const err = handleApiError(res);
        if (err) {
          throw err;
        }
        return true;
      }
      /**
       * Get sandbox information like sandbox ID, template, metadata, started at/end at date.
       *
       * @param sandboxId sandbox ID.
       * @param opts connection options.
       *
       * @returns sandbox information.
       */
      static async getInfo(sandboxId, opts) {
        const fullInfo = await this.getFullInfo(sandboxId, opts);
        delete fullInfo.envdAccessToken;
        delete fullInfo.sandboxDomain;
        return fullInfo;
      }
      /**
       * Get the metrics of the sandbox.
       *
       * @param sandboxId sandbox ID.
       * @param opts sandbox metrics options.
       *
       * @returns  List of sandbox metrics containing CPU, memory and disk usage information.
       */
      static async getMetrics(sandboxId, opts) {
        var _a3, _b;
        const config = new ConnectionConfig(opts);
        const client = new ApiClient(config);
        const start = (opts == null ? void 0 : opts.start) ? Math.round(opts.start.getTime() / 1e3) : void 0;
        const end = (opts == null ? void 0 : opts.end) ? Math.round(opts.end.getTime() / 1e3) : void 0;
        const res = await client.api.GET("/sandboxes/{sandboxID}/metrics", {
          params: {
            path: {
              sandboxID: sandboxId,
              start,
              end
            }
          },
          signal: config.getSignal(opts == null ? void 0 : opts.requestTimeoutMs, opts == null ? void 0 : opts.signal)
        });
        const err = handleApiError(res);
        if (err) {
          throw err;
        }
        return (_b = (_a3 = res.data) == null ? void 0 : _a3.map((metric) => ({
          timestamp: new Date(metric.timestamp),
          cpuUsedPct: metric.cpuUsedPct,
          cpuCount: metric.cpuCount,
          memUsed: metric.memUsed,
          memTotal: metric.memTotal,
          memCache: metric.memCache,
          diskUsed: metric.diskUsed,
          diskTotal: metric.diskTotal
        }))) != null ? _b : [];
      }
      /**
       * Set the timeout of the specified sandbox.
       * After the timeout expires the sandbox will be automatically killed.
       *
       * This method can extend or reduce the sandbox timeout set when creating the sandbox or from the last call to {@link Sandbox.setTimeout}.
       *
       * Maximum time a sandbox can be kept alive is 24 hours (86_400_000 milliseconds) for Pro users and 1 hour (3_600_000 milliseconds) for Hobby users.
       *
       * @param sandboxId sandbox ID.
       * @param timeoutMs timeout in **milliseconds**.
       * @param opts connection options.
       */
      static async setTimeout(sandboxId, timeoutMs, opts) {
        var _a3;
        const config = new ConnectionConfig(opts);
        const client = new ApiClient(config);
        const res = await client.api.POST("/sandboxes/{sandboxID}/timeout", {
          params: {
            path: {
              sandboxID: sandboxId
            }
          },
          body: {
            timeout: timeoutToSeconds(timeoutMs)
          },
          signal: config.getSignal(opts == null ? void 0 : opts.requestTimeoutMs, opts == null ? void 0 : opts.signal)
        });
        if (((_a3 = res.error) == null ? void 0 : _a3.code) === 404) {
          throw new SandboxNotFoundError(`Sandbox ${sandboxId} not found`);
        }
        const err = handleApiError(res);
        if (err) {
          throw err;
        }
      }
      /**
       * Update the network configuration of a running sandbox.
       *
       * Replaces the current egress configuration atomically — fields that are
       * omitted are cleared on the server.
       *
       * @param sandboxId sandbox ID.
       * @param network new network configuration.
       * @param opts connection options.
       */
      static async updateNetwork(sandboxId, network, opts) {
        var _a3;
        const config = new ConnectionConfig(opts);
        const client = new ApiClient(config);
        const res = await client.api.PUT("/sandboxes/{sandboxID}/network", {
          params: {
            path: {
              sandboxID: sandboxId
            }
          },
          body: buildNetworkUpdateBody(network),
          signal: config.getSignal(opts == null ? void 0 : opts.requestTimeoutMs, opts == null ? void 0 : opts.signal)
        });
        if (((_a3 = res.error) == null ? void 0 : _a3.code) === 404) {
          throw new SandboxNotFoundError(`Sandbox ${sandboxId} not found`);
        }
        const err = handleApiError(res);
        if (err) {
          throw err;
        }
      }
      static async getFullInfo(sandboxId, opts) {
        var _a3, _b, _c, _d, _e;
        const config = new ConnectionConfig(opts);
        const client = new ApiClient(config);
        const res = await client.api.GET("/sandboxes/{sandboxID}", {
          params: {
            path: {
              sandboxID: sandboxId
            }
          },
          signal: config.getSignal(opts == null ? void 0 : opts.requestTimeoutMs, opts == null ? void 0 : opts.signal)
        });
        if (((_a3 = res.error) == null ? void 0 : _a3.code) === 404) {
          throw new SandboxNotFoundError(`Sandbox ${sandboxId} not found`);
        }
        const err = handleApiError(res);
        if (err) {
          throw err;
        }
        if (!res.data) {
          throw new Error("Sandbox not found");
        }
        return __spreadProps(__spreadValues({
          sandboxId: res.data.sandboxID,
          templateId: res.data.templateID
        }, res.data.alias && { name: res.data.alias }), {
          metadata: (_b = res.data.metadata) != null ? _b : {},
          allowInternetAccess: (_c = res.data.allowInternetAccess) != null ? _c : void 0,
          envdVersion: res.data.envdVersion,
          envdAccessToken: res.data.envdAccessToken,
          startedAt: new Date(res.data.startedAt),
          endAt: new Date(res.data.endAt),
          state: res.data.state,
          cpuCount: res.data.cpuCount,
          memoryMB: res.data.memoryMB,
          network: res.data.network ? {
            allowOut: res.data.network.allowOut,
            denyOut: res.data.network.denyOut,
            rules: (_d = res.data.network.rules) != null ? _d : void 0,
            allowPublicTraffic: res.data.network.allowPublicTraffic,
            maskRequestHost: res.data.network.maskRequestHost
          } : void 0,
          lifecycle: res.data.lifecycle ? {
            onTimeout: res.data.lifecycle.onTimeout,
            autoResume: res.data.lifecycle.autoResume
          } : void 0,
          sandboxDomain: res.data.domain || void 0,
          volumeMounts: (_e = res.data.volumeMounts) != null ? _e : []
        });
      }
      /**
       * Pause the sandbox specified by sandbox ID.
       *
       * @param sandboxId sandbox ID.
       * @param opts connection options.
       *
       * @returns `true` if the sandbox got paused, `false` if the sandbox was already paused.
       */
      static async pause(sandboxId, opts) {
        var _a3, _b;
        const config = new ConnectionConfig(opts);
        const client = new ApiClient(config);
        const res = await client.api.POST("/sandboxes/{sandboxID}/pause", {
          params: {
            path: {
              sandboxID: sandboxId
            }
          },
          signal: config.getSignal(opts == null ? void 0 : opts.requestTimeoutMs, opts == null ? void 0 : opts.signal)
        });
        if (((_a3 = res.error) == null ? void 0 : _a3.code) === 404) {
          throw new SandboxNotFoundError(`Sandbox ${sandboxId} not found`);
        }
        if (((_b = res.error) == null ? void 0 : _b.code) === 409) {
          return false;
        }
        const err = handleApiError(res);
        if (err) {
          throw err;
        }
        return true;
      }
      /**
       * @deprecated Use {@link SandboxApi.pause} instead.
       */
      static async betaPause(sandboxId, opts) {
        return this.pause(sandboxId, opts);
      }
      /**
       * Create a snapshot from a sandbox.
       *
       * The sandbox will be paused while the snapshot is being created.
       * The snapshot can be used to create new sandboxes with the same state.
       * The snapshot is a persistent image that survives sandbox deletion.
       *
       * @param sandboxId sandbox ID to create snapshot from.
       * @param opts snapshot creation options including optional name and connection options.
       *
       * @returns snapshot information including the snapshot name that can be used with Sandbox.create().
       */
      static async createSnapshot(sandboxId, opts) {
        var _a3, _b;
        const config = new ConnectionConfig(opts);
        const client = new ApiClient(config);
        const res = await client.api.POST("/sandboxes/{sandboxID}/snapshots", {
          params: {
            path: {
              sandboxID: sandboxId
            }
          },
          body: (opts == null ? void 0 : opts.name) ? { name: opts.name } : {},
          signal: config.getSignal(opts == null ? void 0 : opts.requestTimeoutMs, opts == null ? void 0 : opts.signal)
        });
        if (((_a3 = res.error) == null ? void 0 : _a3.code) === 404) {
          throw new SandboxNotFoundError(`Sandbox ${sandboxId} not found`);
        }
        const err = handleApiError(res);
        if (err) {
          throw err;
        }
        return {
          snapshotId: res.data.snapshotID,
          names: (_b = res.data.names) != null ? _b : []
        };
      }
      /**
       * List all snapshots.
       *
       * @param opts list options including filters and pagination.
       *
       * @returns paginator for listing snapshots.
       */
      static listSnapshots(opts) {
        return new SnapshotPaginator(opts);
      }
      /**
       * Delete a snapshot.
       *
       * @param snapshotId snapshot ID.
       * @param opts connection options.
       *
       * @returns `true` if the snapshot was deleted, `false` if it was not found.
       */
      static async deleteSnapshot(snapshotId, opts) {
        var _a3;
        const config = new ConnectionConfig(opts);
        const client = new ApiClient(config);
        const res = await client.api.DELETE("/templates/{templateID}", {
          params: {
            path: {
              templateID: snapshotId
            }
          },
          signal: config.getSignal(opts == null ? void 0 : opts.requestTimeoutMs, opts == null ? void 0 : opts.signal)
        });
        if (((_a3 = res.error) == null ? void 0 : _a3.code) === 404) {
          return false;
        }
        const err = handleApiError(res);
        if (err) {
          throw err;
        }
        return true;
      }
      static async createSandbox(template, timeoutMs, opts) {
        var _a3, _b, _c, _d, _e, _f;
        const config = new ConnectionConfig(opts);
        const client = new ApiClient(config);
        const onTimeout = (_b = (_a3 = opts == null ? void 0 : opts.lifecycle) == null ? void 0 : _a3.onTimeout) != null ? _b : "kill";
        const autoResume = (_d = (_c = opts == null ? void 0 : opts.lifecycle) == null ? void 0 : _c.autoResume) != null ? _d : false;
        if (autoResume && onTimeout !== "pause") {
          throw new InvalidArgumentError(
            "autoResume can only be true when the resolved onTimeout is 'pause'."
          );
        }
        const body = {
          templateID: template,
          metadata: opts == null ? void 0 : opts.metadata,
          mcp: opts == null ? void 0 : opts.mcp,
          envVars: opts == null ? void 0 : opts.envs,
          timeout: timeoutToSeconds(timeoutMs),
          secure: (_e = opts == null ? void 0 : opts.secure) != null ? _e : true,
          allow_internet_access: (_f = opts == null ? void 0 : opts.allowInternetAccess) != null ? _f : true,
          network: buildNetworkBody(opts == null ? void 0 : opts.network),
          autoPause: onTimeout === "pause",
          autoResume: { enabled: autoResume }
        };
        if (opts == null ? void 0 : opts.volumeMounts) {
          body.volumeMounts = Object.entries(opts.volumeMounts).map(
            ([mountPath, vol]) => ({
              name: typeof vol === "string" ? vol : vol.name,
              path: mountPath
            })
          );
        }
        const res = await client.api.POST("/sandboxes", {
          body,
          signal: config.getSignal(opts == null ? void 0 : opts.requestTimeoutMs, opts == null ? void 0 : opts.signal)
        });
        const err = handleApiError(res);
        if (err) {
          throw err;
        }
        if ((0, import_compare_versions4.compareVersions)(res.data.envdVersion, "0.1.0") < 0) {
          await this.kill(res.data.sandboxID, opts);
          throw new TemplateError(
            "You need to update the template to use the new SDK. You can do this by running `e2b template build` in the directory with the template."
          );
        }
        return {
          sandboxId: res.data.sandboxID,
          sandboxDomain: res.data.domain || void 0,
          envdVersion: res.data.envdVersion,
          envdAccessToken: res.data.envdAccessToken,
          trafficAccessToken: res.data.trafficAccessToken || void 0
        };
      }
      static async connectSandbox(sandboxId, opts) {
        var _a3, _b;
        const timeoutMs = (_a3 = opts == null ? void 0 : opts.timeoutMs) != null ? _a3 : DEFAULT_SANDBOX_TIMEOUT_MS;
        const config = new ConnectionConfig(opts);
        const client = new ApiClient(config);
        const res = await client.api.POST("/sandboxes/{sandboxID}/connect", {
          params: {
            path: {
              sandboxID: sandboxId
            }
          },
          body: {
            timeout: timeoutToSeconds(timeoutMs)
          },
          signal: config.getSignal(opts == null ? void 0 : opts.requestTimeoutMs, opts == null ? void 0 : opts.signal)
        });
        if (((_b = res.error) == null ? void 0 : _b.code) === 404) {
          throw new SandboxNotFoundError(`Paused sandbox ${sandboxId} not found`);
        }
        const err = handleApiError(res);
        if (err) {
          throw err;
        }
        return {
          sandboxId: res.data.sandboxID,
          sandboxDomain: res.data.domain || void 0,
          envdVersion: res.data.envdVersion,
          envdAccessToken: res.data.envdAccessToken,
          trafficAccessToken: res.data.trafficAccessToken || void 0
        };
      }
    };
    var BasePaginator = class {
      static {
        __name(this, "BasePaginator");
      }
      constructor(opts, limit, nextToken) {
        this.opts = opts;
        this.limit = limit;
        this._hasNext = true;
        this._nextToken = nextToken;
      }
      /**
       * Returns true if there are more items to fetch.
       */
      get hasNext() {
        return this._hasNext;
      }
      /**
       * Returns the next token to use for pagination.
       */
      get nextToken() {
        return this._nextToken;
      }
      updatePagination(response) {
        this._nextToken = response.headers.get("x-next-token") || void 0;
        this._hasNext = !!this._nextToken;
      }
    };
    var SandboxPaginator = class extends BasePaginator {
      static {
        __name(this, "SandboxPaginator");
      }
      constructor(opts) {
        super(opts, opts == null ? void 0 : opts.limit, opts == null ? void 0 : opts.nextToken);
        this.query = opts == null ? void 0 : opts.query;
      }
      async nextItems(opts) {
        var _a3, _b, _c;
        if (!this.hasNext) {
          throw new Error("No more items to fetch");
        }
        let metadata = void 0;
        if ((_a3 = this.query) == null ? void 0 : _a3.metadata) {
          const encodedPairs = Object.fromEntries(
            Object.entries(this.query.metadata).map(([key, value]) => [
              encodeURIComponent(key),
              encodeURIComponent(value)
            ])
          );
          metadata = new URLSearchParams(encodedPairs).toString();
        }
        const config = new ConnectionConfig(__spreadValues(__spreadValues({}, this.opts), opts));
        const client = new ApiClient(config);
        const res = await client.api.GET("/v2/sandboxes", {
          params: {
            query: {
              metadata,
              state: (_b = this.query) == null ? void 0 : _b.state,
              limit: this.limit,
              nextToken: this.nextToken
            }
          },
          signal: config.getSignal(opts == null ? void 0 : opts.requestTimeoutMs, opts == null ? void 0 : opts.signal)
        });
        const err = handleApiError(res);
        if (err) {
          throw err;
        }
        this.updatePagination(res.response);
        return ((_c = res.data) != null ? _c : []).map(
          (sandbox) => {
            var _a4, _b2;
            return __spreadProps(__spreadValues({
              sandboxId: sandbox.sandboxID,
              templateId: sandbox.templateID
            }, sandbox.alias && { name: sandbox.alias }), {
              metadata: (_a4 = sandbox.metadata) != null ? _a4 : {},
              startedAt: new Date(sandbox.startedAt),
              endAt: new Date(sandbox.endAt),
              state: sandbox.state,
              cpuCount: sandbox.cpuCount,
              memoryMB: sandbox.memoryMB,
              envdVersion: sandbox.envdVersion,
              volumeMounts: (_b2 = sandbox.volumeMounts) != null ? _b2 : []
            });
          }
        );
      }
    };
    var SnapshotPaginator = class extends BasePaginator {
      static {
        __name(this, "SnapshotPaginator");
      }
      constructor(opts) {
        super(opts, opts == null ? void 0 : opts.limit, opts == null ? void 0 : opts.nextToken);
        this.sandboxId = opts == null ? void 0 : opts.sandboxId;
      }
      async nextItems(opts) {
        var _a3;
        if (!this.hasNext) {
          throw new Error("No more items to fetch");
        }
        const config = new ConnectionConfig(__spreadValues(__spreadValues({}, this.opts), opts));
        const client = new ApiClient(config);
        const res = await client.api.GET("/snapshots", {
          params: {
            query: {
              sandboxID: this.sandboxId,
              limit: this.limit,
              nextToken: this.nextToken
            }
          },
          signal: config.getSignal(opts == null ? void 0 : opts.requestTimeoutMs, opts == null ? void 0 : opts.signal)
        });
        const err = handleApiError(res);
        if (err) {
          throw err;
        }
        this.updatePagination(res.response);
        return ((_a3 = res.data) != null ? _a3 : []).map(
          (snapshot) => {
            var _a4;
            return {
              snapshotId: snapshot.snapshotID,
              names: (_a4 = snapshot.names) != null ? _a4 : []
            };
          }
        );
      }
    };
    var import_compare_versions5 = require_umd();
    var Sandbox = class extends SandboxApi {
      static {
        __name(this, "Sandbox");
      }
      /**
       * Use {@link Sandbox.create} to create a new Sandbox instead.
       *
       * @hidden
       * @hide
       * @internal
       * @access protected
       */
      constructor(opts) {
        var _a3, _b, _c;
        super();
        this.envdPort = 49983;
        this.mcpPort = 50005;
        this.connectionConfig = new ConnectionConfig(opts);
        this.sandboxId = opts.sandboxId;
        this.sandboxDomain = (_a3 = opts.sandboxDomain) != null ? _a3 : this.connectionConfig.domain;
        this.envdAccessToken = opts.envdAccessToken;
        this.trafficAccessToken = opts.trafficAccessToken;
        this.envdApiUrl = this.connectionConfig.getSandboxUrl(this.sandboxId, {
          sandboxDomain: this.sandboxDomain,
          envdPort: this.envdPort
        });
        this.envdDirectUrl = this.connectionConfig.getSandboxDirectUrl(
          this.sandboxId,
          {
            sandboxDomain: this.sandboxDomain,
            envdPort: this.envdPort
          }
        );
        const sandboxHeaders = {
          "E2b-Sandbox-Id": this.sandboxId,
          "E2b-Sandbox-Port": this.envdPort.toString()
        };
        const envdFetch2 = createEnvdFetch();
        const envdRpcFetch2 = createEnvdRpcFetch();
        const rpcTransport = (0, import_connect_web.createConnectTransport)({
          baseUrl: this.envdApiUrl,
          useBinaryFormat: false,
          interceptors: (opts == null ? void 0 : opts.logger) ? [createRpcLogger(opts.logger)] : void 0,
          fetch: /* @__PURE__ */ __name((url, options) => {
            const headers = new Headers(this.connectionConfig.headers);
            new Headers(options == null ? void 0 : options.headers).forEach(
              (value, key) => headers.append(key, value)
            );
            new Headers(sandboxHeaders).forEach(
              (value, key) => headers.append(key, value)
            );
            if (this.envdAccessToken) {
              headers.append("X-Access-Token", this.envdAccessToken);
            }
            options = __spreadProps(__spreadValues({}, options != null ? options : {}), {
              headers,
              redirect: "follow"
            });
            return envdRpcFetch2(url, options);
          }, "fetch")
        });
        this.envdApi = new EnvdApiClient(
          {
            apiUrl: this.envdApiUrl,
            logger: opts == null ? void 0 : opts.logger,
            accessToken: this.envdAccessToken,
            headers: __spreadValues(__spreadValues({
              "User-Agent": (_c = (_b = this.connectionConfig.headers) == null ? void 0 : _b["User-Agent"]) != null ? _c : ""
            }, sandboxHeaders), this.envdAccessToken ? { "X-Access-Token": this.envdAccessToken } : {}),
            fetch: /* @__PURE__ */ __name((request) => envdFetch2(request), "fetch")
          },
          {
            version: opts.envdVersion
          }
        );
        this.files = new Filesystem2(
          rpcTransport,
          this.envdApi,
          this.connectionConfig
        );
        this.commands = new Commands(rpcTransport, this.connectionConfig, {
          version: opts.envdVersion
        });
        this.pty = new Pty(rpcTransport, this.connectionConfig, {
          version: opts.envdVersion
        });
        this.git = new Git(this.commands);
      }
      /**
       * List all sandboxes.
       *
       * @param opts connection options.
       *
       * @returns paginator for listing sandboxes.
       */
      static list(opts) {
        return new SandboxPaginator(opts);
      }
      static async create(templateOrOpts, opts) {
        var _a3, _b, _c;
        const { template, sandboxOpts } = typeof templateOrOpts === "string" ? {
          template: templateOrOpts,
          sandboxOpts: opts
        } : {
          template: (_a3 = templateOrOpts == null ? void 0 : templateOrOpts.template) != null ? _a3 : (templateOrOpts == null ? void 0 : templateOrOpts.mcp) ? this.defaultMcpTemplate : this.defaultTemplate,
          sandboxOpts: templateOrOpts
        };
        const config = new ConnectionConfig(sandboxOpts);
        if (config.debug) {
          return new this(__spreadValues({
            sandboxId: "debug_sandbox_id",
            envdVersion: ENVD_DEBUG_FALLBACK
          }, config));
        }
        const sandboxInfo = await SandboxApi.createSandbox(
          template,
          (_b = sandboxOpts == null ? void 0 : sandboxOpts.timeoutMs) != null ? _b : this.defaultSandboxTimeoutMs,
          sandboxOpts
        );
        const sandbox = new this(__spreadValues(__spreadValues({}, sandboxInfo), config));
        if (sandboxOpts == null ? void 0 : sandboxOpts.mcp) {
          sandbox.mcpToken = crypto.randomUUID();
          const res = await sandbox.commands.run(
            `mcp-gateway --config ${shellQuote(JSON.stringify(sandboxOpts.mcp))}`,
            {
              user: "root",
              envs: {
                GATEWAY_ACCESS_TOKEN: (_c = sandbox.mcpToken) != null ? _c : ""
              }
            }
          );
          if (res.exitCode !== 0) {
            throw new Error(`Failed to start MCP gateway: ${res.stderr}`);
          }
        }
        return sandbox;
      }
      /**
       * Connect to a sandbox. If the sandbox is paused, it will be automatically resumed.
       * Sandbox must be either running or be paused.
       *
       * With sandbox ID you can connect to the same sandbox from different places or environments (serverless functions, etc).
       *
       * @param sandboxId sandbox ID.
       * @param opts connection options.
       *
       * @returns A running sandbox instance
       *
       * @example
       * ```ts
       * const sandbox = await Sandbox.create()
       * const sandboxId = sandbox.sandboxId
       *
       * // Connect to the same sandbox.
       * const sameSandbox = await Sandbox.connect(sandboxId)
       * ```
       */
      static async connect(sandboxId, opts) {
        const sandbox = await SandboxApi.connectSandbox(sandboxId, opts);
        const config = new ConnectionConfig(opts);
        return new this(__spreadValues({
          sandboxId,
          sandboxDomain: sandbox.sandboxDomain,
          envdAccessToken: sandbox.envdAccessToken,
          trafficAccessToken: sandbox.trafficAccessToken,
          envdVersion: sandbox.envdVersion
        }, config));
      }
      /**
       * Connect to a sandbox. If the sandbox is paused, it will be automatically resumed.
       * Sandbox must be either running or be paused.
       *
       * With sandbox ID you can connect to the same sandbox from different places or environments (serverless functions, etc).
       *
       * @param opts connection options.
       *
       * @returns A running sandbox instance
       *
       * @example
       * ```ts
       * const sandbox = await Sandbox.create()
       * await sandbox.betaPause()
       *
       * // Connect to the same sandbox.
       * const sameSandbox = await sandbox.connect()
       * ```
       */
      async connect(opts) {
        await SandboxApi.connectSandbox(this.sandboxId, this.resolveApiOpts(opts));
        return this;
      }
      /**
       * Get the host address for the specified sandbox port.
       * You can then use this address to connect to the sandbox port from outside the sandbox via HTTP or WebSocket.
       *
       * @param port number of the port in the sandbox.
       *
       * @returns host address of the sandbox port.
       *
       * @example
       * ```ts
       * const sandbox = await Sandbox.create()
       * // Start an HTTP server
       * await sandbox.commands.exec('python3 -m http.server 3000')
       * // Get the hostname of the HTTP server
       * const serverURL = sandbox.getHost(3000)
       * ```
       */
      getHost(port) {
        return this.connectionConfig.getHost(
          this.sandboxId,
          port,
          this.sandboxDomain
        );
      }
      /**
       * Check if the sandbox is running.
       *
       * @returns `true` if the sandbox is running, `false` otherwise.
       *
       * @example
       * ```ts
       * const sandbox = await Sandbox.create()
       * await sandbox.isRunning() // Returns true
       *
       * await sandbox.kill()
       * await sandbox.isRunning() // Returns false
       * ```
       */
      async isRunning(opts) {
        const signal = this.connectionConfig.getSignal(
          opts == null ? void 0 : opts.requestTimeoutMs,
          opts == null ? void 0 : opts.signal
        );
        const res = await this.envdApi.api.GET("/health", {
          signal
        });
        if (res.response.status == 502) {
          return false;
        }
        const err = await handleEnvdApiError(res);
        if (err) {
          throw err;
        }
        return true;
      }
      /**
       * Set the timeout of the sandbox.
       *
       * This method can extend or reduce the sandbox timeout set when creating the sandbox or from the last call to `.setTimeout`.
       * Maximum time a sandbox can be kept alive is 24 hours (86_400_000 milliseconds) for Pro users and 1 hour (3_600_000 milliseconds) for Hobby users.
       *
       * @param timeoutMs timeout in **milliseconds**.
       * @param opts connection options.
       */
      async setTimeout(timeoutMs, opts) {
        if (this.connectionConfig.debug) {
          return;
        }
        await SandboxApi.setTimeout(
          this.sandboxId,
          timeoutMs,
          this.resolveApiOpts(opts)
        );
      }
      /**
       * Update the network configuration of the sandbox.
       *
       * Replaces the current egress configuration atomically — fields that are
       * omitted are cleared on the server.
       *
       * @param network new network configuration.
       * @param opts connection options.
       */
      async updateNetwork(network, opts) {
        await SandboxApi.updateNetwork(
          this.sandboxId,
          network,
          this.resolveApiOpts(opts)
        );
      }
      /**
       * Kill the sandbox.
       *
       * @param opts connection options.
       */
      async kill(opts) {
        if (this.connectionConfig.debug) {
          return;
        }
        await SandboxApi.kill(this.sandboxId, this.resolveApiOpts(opts));
      }
      /**
       * Pause a sandbox by its ID.
       *
       * @param opts connection options.
       *
       * @returns sandbox ID that can be used to resume the sandbox.
       *
       * @example
       * ```ts
       * const sandbox = await Sandbox.create()
       * await sandbox.pause()
       * ```
       */
      async pause(opts) {
        return await SandboxApi.pause(this.sandboxId, this.resolveApiOpts(opts));
      }
      /**
       * @deprecated Use {@link Sandbox.pause} instead.
       */
      async betaPause(opts) {
        return await SandboxApi.betaPause(this.sandboxId, this.resolveApiOpts(opts));
      }
      /**
       * Create a snapshot of the sandbox's current state.
       *
       * The sandbox will be paused while the snapshot is being created.
       * The snapshot can be used to create new sandboxes with the same filesystem and state.
       * Snapshots are persistent and survive sandbox deletion.
       *
       * Use the returned `snapshotId` with `Sandbox.create(snapshotId)` to create a new sandbox from the snapshot.
       *
       * @param opts snapshot creation options including optional name and connection options.
       *
       * @returns snapshot information including the snapshot ID.
       *
       * @example
       * ```ts
       * const sandbox = await Sandbox.create()
       * await sandbox.files.write('/app/state.json', '{"step": 1}')
       *
       * // Create a snapshot
       * const snapshot = await sandbox.createSnapshot({ name: 'my-snapshot' })
       *
       * // Create a new sandbox from the snapshot
       * const newSandbox = await Sandbox.create(snapshot.snapshotId)
       * ```
       */
      async createSnapshot(opts) {
        return await SandboxApi.createSnapshot(this.sandboxId, __spreadProps(__spreadValues({}, this.resolveApiOpts(opts)), {
          name: opts == null ? void 0 : opts.name
        }));
      }
      /**
       * List all snapshots created from this sandbox.
       *
       * @param opts list options.
       *
       * @returns paginator for listing snapshots from this sandbox.
       */
      listSnapshots(opts) {
        return SandboxApi.listSnapshots(__spreadProps(__spreadValues({}, this.resolveApiOpts(opts)), {
          sandboxId: this.sandboxId
        }));
      }
      /**
       *
       * Get the MCP URL for the sandbox.
       *
       * @returns MCP URL for the sandbox.
       */
      getMcpUrl() {
        return `https://${this.getHost(this.mcpPort)}/mcp`;
      }
      /**
       * Get the MCP token for the sandbox.
       *
       * @returns MCP token for the sandbox, or undefined if MCP is not enabled.
       */
      async getMcpToken() {
        if (!this.mcpToken) {
          this.mcpToken = await this.files.read("/etc/mcp-gateway/.token", {
            user: "root"
          });
        }
        return this.mcpToken;
      }
      /**
       * Get the URL to upload a file to the sandbox.
       *
       * You have to send a POST request to this URL with the file as multipart/form-data.
       *
       * @param path path to the file in the sandbox.
       *
       * @param opts download url options.
       *
       * @returns URL for uploading file.
       */
      async uploadUrl(path2, opts) {
        opts = opts != null ? opts : {};
        const useSignature = !!this.envdAccessToken;
        if (!useSignature && opts.useSignatureExpiration != void 0) {
          throw new Error(
            "Signature expiration can be used only when sandbox is created as secured."
          );
        }
        let username = opts.user;
        if (username == void 0 && (0, import_compare_versions5.compareVersions)(this.envdApi.version, ENVD_DEFAULT_USER) < 0) {
          username = defaultUsername;
        }
        const filePath = path2 != null ? path2 : "";
        const fileUrl = this.fileUrl(filePath, username);
        if (useSignature) {
          const url = new URL(fileUrl);
          const sig = await getSignature({
            path: filePath,
            operation: "write",
            user: username,
            expirationInSeconds: opts.useSignatureExpiration,
            envdAccessToken: this.envdAccessToken
          });
          url.searchParams.set("signature", sig.signature);
          if (sig.expiration) {
            url.searchParams.set("signature_expiration", sig.expiration.toString());
          }
          return url.toString();
        }
        return fileUrl;
      }
      /**
       * Get the URL to download a file from the sandbox.
       *
       * @param path path to the file in the sandbox.
       *
       * @param opts download url options.
       *
       * @returns URL for downloading file.
       */
      async downloadUrl(path2, opts) {
        opts = opts != null ? opts : {};
        const useSignature = !!this.envdAccessToken;
        if (!useSignature && opts.useSignatureExpiration != void 0) {
          throw new Error(
            "Signature expiration can be used only when sandbox is created as secured."
          );
        }
        let username = opts.user;
        if (username == void 0 && (0, import_compare_versions5.compareVersions)(this.envdApi.version, ENVD_DEFAULT_USER) < 0) {
          username = defaultUsername;
        }
        const fileUrl = this.fileUrl(path2, username);
        if (useSignature) {
          const url = new URL(fileUrl);
          const sig = await getSignature({
            path: path2,
            operation: "read",
            user: username,
            expirationInSeconds: opts.useSignatureExpiration,
            envdAccessToken: this.envdAccessToken
          });
          url.searchParams.set("signature", sig.signature);
          if (sig.expiration) {
            url.searchParams.set("signature_expiration", sig.expiration.toString());
          }
          return url.toString();
        }
        return fileUrl;
      }
      /**
       * Get sandbox information like sandbox ID, template, metadata, started at/end at date.
       *
       * @param opts connection options.
       *
       * @returns information about the sandbox
       */
      async getInfo(opts) {
        return await SandboxApi.getInfo(this.sandboxId, this.resolveApiOpts(opts));
      }
      /**
       * Get the metrics of the sandbox.
       *
       * @param opts connection options.
       *
       * @returns  List of sandbox metrics containing CPU, memory and disk usage information.
       */
      async getMetrics(opts) {
        var _a3, _b;
        if (this.envdApi.version) {
          if ((0, import_compare_versions5.compareVersions)(this.envdApi.version, "0.1.5") < 0) {
            throw new SandboxError(
              "You need to update the template to use the new SDK. You can do this by running `e2b template build` in the directory with the template."
            );
          }
          if ((0, import_compare_versions5.compareVersions)(this.envdApi.version, "0.2.4") < 0) {
            (_b = (_a3 = this.connectionConfig.logger) == null ? void 0 : _a3.warn) == null ? void 0 : _b.call(
              _a3,
              "Disk metrics are not supported in this version of the sandbox, please rebuild the template to get disk metrics."
            );
          }
        }
        return await SandboxApi.getMetrics(
          this.sandboxId,
          this.resolveApiOpts(opts)
        );
      }
      resolveApiOpts(opts) {
        return __spreadValues(__spreadValues({}, this.connectionConfig), opts != null ? opts : {});
      }
      fileUrl(path2, username) {
        const url = new URL("/files", this.envdDirectUrl);
        if (username) {
          url.searchParams.set("username", username);
        }
        if (path2) {
          url.searchParams.set("path", path2);
        }
        return url.toString();
      }
    };
    Sandbox.defaultTemplate = "base";
    Sandbox.defaultMcpTemplate = "mcp-gateway";
    Sandbox.defaultSandboxTimeoutMs = DEFAULT_SANDBOX_TIMEOUT_MS;
    var FINALIZE_STEP_NAME = "finalize";
    var BASE_STEP_NAME = "base";
    var STACK_TRACE_DEPTH = 3;
    var RESOLVE_SYMLINKS = false;
    var FILE_UPLOAD_TIMEOUT_MS = 36e5;
    var import_chalk = __toESM((init_source(), __toCommonJS(source_exports)));
    var LogEntry = class {
      static {
        __name(this, "LogEntry");
      }
      constructor(timestamp, level, message) {
        this.timestamp = timestamp;
        this.level = level;
        this.message = message;
      }
      toString() {
        return `[${this.timestamp.toISOString()}] [${this.level}] ${stripAnsi(
          this.message
        )}`;
      }
    };
    var LogEntryStart = class extends LogEntry {
      static {
        __name(this, "LogEntryStart");
      }
      constructor(timestamp, message) {
        super(timestamp, "debug", message);
      }
    };
    var LogEntryEnd = class extends LogEntry {
      static {
        __name(this, "LogEntryEnd");
      }
      constructor(timestamp, message) {
        super(timestamp, "debug", message);
      }
    };
    var TIMER_UPDATE_INTERVAL_MS = 150;
    var DEFAULT_LEVEL = "info";
    var levels = {
      error: import_chalk.default.red("ERROR"),
      warn: import_chalk.default.hex("#FF4400")("WARN "),
      info: import_chalk.default.hex("#FF8800")("INFO "),
      debug: import_chalk.default.gray("DEBUG")
    };
    var level_order = {
      debug: 0,
      info: 1,
      warn: 2,
      error: 3
    };
    var DefaultBuildLogger = class {
      static {
        __name(this, "DefaultBuildLogger");
      }
      constructor(minLevel) {
        this.minLevel = minLevel != null ? minLevel : DEFAULT_LEVEL;
        this.state = this.getInitialState();
      }
      logger(logEntry) {
        if (logEntry instanceof LogEntryStart) {
          this.startTimer();
          return;
        }
        if (logEntry instanceof LogEntryEnd) {
          clearInterval(this.state.timerInterval);
          return;
        }
        if (level_order[logEntry.level] < level_order[this.minLevel]) {
          return;
        }
        const formattedLine = this.formatLogLine(logEntry);
        process.stdout.write(`${formattedLine}
`);
        this.updateTimer();
      }
      getInitialState(timerInterval) {
        return {
          startTime: Date.now(),
          animationFrame: 0,
          timerInterval
        };
      }
      formatTimerLine() {
        const elapsedSeconds = ((Date.now() - this.state.startTime) / 1e3).toFixed(
          1
        );
        return `${elapsedSeconds}s`;
      }
      animateStatus() {
        const frames = ["⣾", "⣽", "⣻", "⢿", "⡿", "⣟", "⣯", "⣷"];
        const idx = this.state.animationFrame % frames.length;
        return `${frames[idx]}`;
      }
      formatLogLine(line) {
        const timer = this.formatTimerLine().padEnd(5);
        const timestamp = import_chalk.default.dim(
          line.timestamp.toLocaleTimeString(void 0, {
            hour: "2-digit",
            minute: "2-digit",
            second: "2-digit"
          })
        );
        const level = levels[line.level] || levels[DEFAULT_LEVEL];
        const msg = line.message;
        return `${timer} | ${timestamp} ${level} ${msg}`;
      }
      startTimer() {
        if (!process.stdout.isTTY) {
          return;
        }
        const timerInterval = setInterval(
          this.updateTimer.bind(this),
          TIMER_UPDATE_INTERVAL_MS
        );
        this.state = this.getInitialState(timerInterval);
        this.updateTimer();
      }
      updateTimer() {
        if (!process.stdout.isTTY) {
          return;
        }
        this.state.animationFrame++;
        const jumpingSquares = this.animateStatus();
        process.stdout.write(
          `${jumpingSquares} Building ${this.formatTimerLine()}\r`
        );
      }
    };
    function defaultBuildLogger(options) {
      const buildLogger = new DefaultBuildLogger(options == null ? void 0 : options.minLevel);
      return buildLogger.logger.bind(buildLogger);
    }
    __name(defaultBuildLogger, "defaultBuildLogger");
    var import_node_crypto = __toESM(__require("crypto"));
    var import_node_fs = __toESM(__require("fs"));
    var import_node_path = __toESM(__require("path"));
    function validateRelativePath(src, stackTrace) {
      if (import_node_path.default.isAbsolute(src)) {
        const error = new TemplateError(
          `Invalid source path "${src}": absolute paths are not allowed. Use a relative path within the context directory.`,
          stackTrace
        );
        throw error;
      }
      const normalized = import_node_path.default.normalize(src);
      const escapes = normalized === ".." || normalized.startsWith(".." + import_node_path.default.sep);
      if (escapes) {
        const error = new TemplateError(
          `Invalid source path "${src}": path escapes the context directory. The path must stay within the context directory.`,
          stackTrace
        );
        throw error;
      }
    }
    __name(validateRelativePath, "validateRelativePath");
    function normalizeBuildArguments(nameOrOptions, options) {
      let name;
      let buildOptions;
      if (typeof nameOrOptions === "string") {
        name = nameOrOptions;
        buildOptions = options != null ? options : {};
      } else {
        const _a3 = nameOrOptions, { alias } = _a3, restOpts = __objRest(_a3, ["alias"]);
        name = alias;
        buildOptions = restOpts;
      }
      if (!name || name.length === 0) {
        throw new TemplateError("Name must be provided");
      }
      return { name, buildOptions };
    }
    __name(normalizeBuildArguments, "normalizeBuildArguments");
    function readDockerignore(contextPath) {
      const dockerignorePath = import_node_path.default.join(contextPath, ".dockerignore");
      if (!import_node_fs.default.existsSync(dockerignorePath)) {
        return [];
      }
      const content = import_node_fs.default.readFileSync(dockerignorePath, "utf-8");
      return content.split("\n").map((line) => line.trim()).filter((line) => line && !line.startsWith("#"));
    }
    __name(readDockerignore, "readDockerignore");
    function normalizePath(path2) {
      return path2.replace(/\\/g, "/");
    }
    __name(normalizePath, "normalizePath");
    async function getAllFilesInPath(src, contextPath, ignorePatterns, includeDirectories = true) {
      const { glob } = await dynamicImport("glob");
      const files = /* @__PURE__ */ new Map();
      const globFiles = await glob(src, {
        ignore: ignorePatterns,
        withFileTypes: true,
        dot: true,
        // this is required so that the ignore pattern is relative to the file path
        cwd: contextPath
      });
      for (const file of globFiles) {
        if (file.isDirectory()) {
          if (includeDirectories) {
            files.set(file.fullpath(), file);
          }
          const dirPattern = normalizePath(
            // When the matched directory is '.', `file.relative()` can be an empty string.
            // In that case, we want to match all files under the current directory instead of
            // creating an absolute glob like '/**/*' which would traverse the entire filesystem.
            import_node_path.default.join(file.relative() || ".", "**/*")
          );
          const dirFiles = await glob(dirPattern, {
            ignore: ignorePatterns,
            withFileTypes: true,
            dot: true,
            cwd: contextPath
          });
          dirFiles.forEach((f) => files.set(f.fullpath(), f));
        } else {
          files.set(file.fullpath(), file);
        }
      }
      return Array.from(files.values()).sort();
    }
    __name(getAllFilesInPath, "getAllFilesInPath");
    async function calculateFilesHash(src, dest, contextPath, ignorePatterns, resolveSymlinks, stackTrace) {
      const srcPath = import_node_path.default.join(contextPath, src);
      const hash = import_node_crypto.default.createHash("sha256");
      const content = `COPY ${src} ${dest}`;
      hash.update(content);
      const files = await getAllFilesInPath(src, contextPath, ignorePatterns, true);
      if (files.length === 0) {
        const error = new Error(`No files found in ${srcPath}`);
        if (stackTrace) {
          error.stack = stackTrace;
        }
        throw error;
      }
      const hashStats = /* @__PURE__ */ __name((stats) => {
        hash.update(stats.mode.toString());
        hash.update(stats.size.toString());
      }, "hashStats");
      for (const file of files) {
        const relativePath = file.relativePosix();
        hash.update(relativePath);
        if (file.isSymbolicLink()) {
          const stats2 = import_node_fs.default.statSync(file.fullpath(), { throwIfNoEntry: false });
          const shouldFollow = resolveSymlinks && ((stats2 == null ? void 0 : stats2.isFile()) || (stats2 == null ? void 0 : stats2.isDirectory()));
          if (!shouldFollow) {
            const stats3 = import_node_fs.default.lstatSync(file.fullpath());
            hashStats(stats3);
            const content2 = import_node_fs.default.readlinkSync(file.fullpath());
            hash.update(content2);
            continue;
          }
        }
        const stats = import_node_fs.default.statSync(file.fullpath());
        hashStats(stats);
        if (stats.isFile()) {
          const content2 = import_node_fs.default.readFileSync(file.fullpath());
          hash.update(new Uint8Array(content2));
        }
      }
      return hash.digest("hex");
    }
    __name(calculateFilesHash, "calculateFilesHash");
    function getCallerFrame(depth) {
      const stackTrace = new Error().stack;
      if (!stackTrace) {
        return;
      }
      const lines = stackTrace.split("\n").slice(1);
      if (lines.length < depth + 1) {
        return;
      }
      return lines.slice(depth).join("\n");
    }
    __name(getCallerFrame, "getCallerFrame");
    function callsites(depth) {
      const _originalPrepareStackTrace = Error.prepareStackTrace;
      try {
        let result = [];
        Error.prepareStackTrace = (_, callSites) => {
          const callSitesWithoutCurrent = callSites.slice(depth);
          result = callSitesWithoutCurrent;
          return callSitesWithoutCurrent;
        };
        new Error().stack;
        return result;
      } finally {
        Error.prepareStackTrace = _originalPrepareStackTrace;
      }
    }
    __name(callsites, "callsites");
    function getCallerDirectory(depth) {
      const callSites = callsites(depth + 1);
      if (callSites.length === 0) {
        return void 0;
      }
      let fileName = callSites[0].getFileName();
      if (!fileName) {
        return void 0;
      }
      if (fileName.startsWith("file:")) {
        const { fileURLToPath } = dynamicRequire("node:url");
        fileName = fileURLToPath(fileName);
      }
      return import_node_path.default.dirname(fileName);
    }
    __name(getCallerDirectory, "getCallerDirectory");
    function padOctal(mode) {
      return mode.toString(8).padStart(4, "0");
    }
    __name(padOctal, "padOctal");
    async function tarFileStream(fileName, fileContextPath, ignorePatterns, resolveSymlinks) {
      const { create } = await dynamicImport("tar");
      const allFiles = await getAllFilesInPath(
        fileName,
        fileContextPath,
        ignorePatterns,
        true
      );
      const filePaths = allFiles.map((file) => file.relativePosix());
      return create(
        {
          gzip: true,
          cwd: fileContextPath,
          follow: resolveSymlinks,
          noDirRecurse: true
        },
        filePaths
      );
    }
    __name(tarFileStream, "tarFileStream");
    async function tarFileStreamUpload(fileName, fileContextPath, ignorePatterns, resolveSymlinks) {
      return tarFileStream(
        fileName,
        fileContextPath,
        ignorePatterns,
        resolveSymlinks
      );
    }
    __name(tarFileStreamUpload, "tarFileStreamUpload");
    function getBuildStepIndex(step, stackTracesLength) {
      if (step === BASE_STEP_NAME) {
        return 0;
      }
      if (step === FINALIZE_STEP_NAME) {
        return stackTracesLength - 1;
      }
      return Number(step);
    }
    __name(getBuildStepIndex, "getBuildStepIndex");
    function readGCPServiceAccountJSON(contextPath, pathOrContent) {
      if (typeof pathOrContent === "string") {
        return import_node_fs.default.readFileSync(import_node_path.default.join(contextPath, pathOrContent), "utf-8");
      }
      return JSON.stringify(pathOrContent);
    }
    __name(readGCPServiceAccountJSON, "readGCPServiceAccountJSON");
    async function requestBuild(client, { name, tags, cpuCount, memoryMB }, signal) {
      const requestBuildRes = await client.api.POST("/v3/templates", {
        body: {
          name,
          tags,
          cpuCount,
          memoryMB
        },
        signal
      });
      const error = handleApiError(requestBuildRes, BuildError);
      if (error) {
        throw error;
      }
      if (!requestBuildRes.data) {
        throw new BuildError("Failed to request build");
      }
      return requestBuildRes.data;
    }
    __name(requestBuild, "requestBuild");
    async function getFileUploadLink(client, { templateID, filesHash }, stackTrace, signal) {
      const fileUploadLinkRes = await client.api.GET(
        "/templates/{templateID}/files/{hash}",
        {
          params: {
            path: {
              templateID,
              hash: filesHash
            }
          },
          signal
        }
      );
      const error = handleApiError(fileUploadLinkRes, FileUploadError, stackTrace);
      if (error) {
        throw error;
      }
      if (!fileUploadLinkRes.data) {
        throw new FileUploadError("Failed to get file upload link", stackTrace);
      }
      return fileUploadLinkRes.data;
    }
    __name(getFileUploadLink, "getFileUploadLink");
    async function uploadFile(options, stackTrace, abortOpts) {
      var _a3;
      const { fileName, url, fileContextPath, ignorePatterns, resolveSymlinks } = options;
      try {
        const uploadStream = await tarFileStreamUpload(
          fileName,
          fileContextPath,
          ignorePatterns,
          resolveSymlinks
        );
        const { buffer } = await dynamicImport("node:stream/consumers");
        const uploadBody = await buffer(
          uploadStream
        );
        const res = await fetch(url, {
          method: "PUT",
          body: uploadBody,
          signal: buildRequestSignal(
            (_a3 = abortOpts == null ? void 0 : abortOpts.requestTimeoutMs) != null ? _a3 : FILE_UPLOAD_TIMEOUT_MS,
            abortOpts == null ? void 0 : abortOpts.signal
          )
        });
        if (!res.ok) {
          throw new FileUploadError(
            `Failed to upload file: ${res.statusText}`,
            stackTrace
          );
        }
      } catch (error) {
        if (error instanceof FileUploadError) {
          throw error;
        }
        throw new FileUploadError(`Failed to upload file: ${error}`, stackTrace);
      }
    }
    __name(uploadFile, "uploadFile");
    async function triggerBuild(client, { templateID, buildID, template }, signal) {
      const triggerBuildRes = await client.api.POST(
        "/v2/templates/{templateID}/builds/{buildID}",
        {
          params: {
            path: {
              templateID,
              buildID
            }
          },
          body: template,
          signal
        }
      );
      const error = handleApiError(triggerBuildRes, BuildError);
      if (error) {
        throw error;
      }
    }
    __name(triggerBuild, "triggerBuild");
    function mapLogEntry(entry) {
      return new LogEntry(new Date(entry.timestamp), entry.level, entry.message);
    }
    __name(mapLogEntry, "mapLogEntry");
    function mapBuildStatusReason(reason) {
      var _a3;
      if (!reason) {
        return void 0;
      }
      return {
        message: reason.message,
        step: reason.step,
        logEntries: ((_a3 = reason.logEntries) != null ? _a3 : []).map(mapLogEntry)
      };
    }
    __name(mapBuildStatusReason, "mapBuildStatusReason");
    async function getBuildStatus(client, { templateID, buildID, logsOffset }, signal) {
      const buildStatusRes = await client.api.GET(
        "/templates/{templateID}/builds/{buildID}/status",
        {
          params: {
            path: {
              templateID,
              buildID
            },
            query: {
              logsOffset
            }
          },
          signal
        }
      );
      const error = handleApiError(buildStatusRes, BuildError);
      if (error) {
        throw error;
      }
      if (!buildStatusRes.data) {
        throw new BuildError("Failed to get build status");
      }
      return {
        buildID: buildStatusRes.data.buildID,
        templateID: buildStatusRes.data.templateID,
        status: buildStatusRes.data.status,
        logEntries: buildStatusRes.data.logEntries.map(mapLogEntry),
        logs: buildStatusRes.data.logs,
        reason: mapBuildStatusReason(buildStatusRes.data.reason)
      };
    }
    __name(getBuildStatus, "getBuildStatus");
    async function checkAliasExists(client, { alias }, signal) {
      const aliasRes = await client.api.GET("/templates/aliases/{alias}", {
        params: {
          path: {
            alias
          }
        },
        signal
      });
      if (aliasRes.response.status === 404) {
        return false;
      }
      if (aliasRes.response.status === 403) {
        return true;
      }
      const error = handleApiError(aliasRes, TemplateError);
      if (error) {
        throw error;
      }
      return aliasRes.data !== void 0;
    }
    __name(checkAliasExists, "checkAliasExists");
    async function waitForBuildFinish(client, {
      templateID,
      buildID,
      onBuildLogs,
      logsRefreshFrequency,
      stackTraces,
      signal,
      requestTimeoutMs
    }) {
      var _a3, _b, _c;
      let logsOffset = 0;
      let status = "building";
      while (status === "building" || status === "waiting") {
        signal == null ? void 0 : signal.throwIfAborted();
        const buildStatus = await getBuildStatus(
          client,
          {
            templateID,
            buildID,
            logsOffset
          },
          buildRequestSignal(requestTimeoutMs, signal)
        );
        logsOffset += buildStatus.logEntries.length;
        buildStatus.logEntries.forEach(
          (logEntry) => onBuildLogs == null ? void 0 : onBuildLogs(
            new LogEntry(
              logEntry.timestamp,
              logEntry.level,
              stripAnsi(logEntry.message)
            )
          )
        );
        status = buildStatus.status;
        switch (status) {
          case "ready": {
            return;
          }
          case "waiting": {
            break;
          }
          case "error": {
            let stackError;
            if (((_a3 = buildStatus.reason) == null ? void 0 : _a3.step) !== void 0) {
              const step = getBuildStepIndex(
                buildStatus.reason.step,
                stackTraces.length
              );
              stackError = stackTraces[step];
            }
            throw new BuildError(
              (_c = (_b = buildStatus == null ? void 0 : buildStatus.reason) == null ? void 0 : _b.message) != null ? _c : "Unknown error",
              stackError
            );
          }
        }
        await new Promise((resolve) => setTimeout(resolve, logsRefreshFrequency));
      }
      throw new BuildError("Unknown build error occurred.");
    }
    __name(waitForBuildFinish, "waitForBuildFinish");
    async function assignTags(client, { targetName, tags }, signal) {
      const res = await client.api.POST("/templates/tags", {
        body: { target: targetName, tags },
        signal
      });
      const error = handleApiError(res, TemplateError);
      if (error) {
        throw error;
      }
      if (!res.data) {
        throw new TemplateError("Failed to assign tags");
      }
      return {
        buildId: res.data.buildID,
        tags: res.data.tags
      };
    }
    __name(assignTags, "assignTags");
    async function removeTags(client, { name, tags }, signal) {
      const res = await client.api.DELETE("/templates/tags", {
        body: { name, tags },
        signal
      });
      const error = handleApiError(res, TemplateError);
      if (error) {
        throw error;
      }
    }
    __name(removeTags, "removeTags");
    async function getTemplateTags(client, { templateID }, signal) {
      const res = await client.api.GET("/templates/{templateID}/tags", {
        params: {
          path: {
            templateID
          }
        },
        signal
      });
      const error = handleApiError(res, TemplateError);
      if (error) {
        throw error;
      }
      if (!res.data) {
        throw new TemplateError("Failed to get template tags");
      }
      return res.data.map((item) => ({
        tag: item.tag,
        buildId: item.buildID,
        createdAt: new Date(item.createdAt)
      }));
    }
    __name(getTemplateTags, "getTemplateTags");
    var import_dockerfile_ast = require_main();
    var import_node_fs2 = __toESM(__require("fs"));
    var ReadyCmd = class {
      static {
        __name(this, "ReadyCmd");
      }
      constructor(cmd) {
        this.cmd = cmd;
      }
      getCmd() {
        return this.cmd;
      }
    };
    function waitForPort(port) {
      const cmd = `ss -tuln | grep :${port}`;
      return new ReadyCmd(cmd);
    }
    __name(waitForPort, "waitForPort");
    function waitForURL(url, statusCode = 200) {
      const cmd = `curl -s -o /dev/null -w "%{http_code}" ${url} | grep -q "${statusCode}"`;
      return new ReadyCmd(cmd);
    }
    __name(waitForURL, "waitForURL");
    function waitForProcess(processName) {
      const cmd = `pgrep ${processName} > /dev/null`;
      return new ReadyCmd(cmd);
    }
    __name(waitForProcess, "waitForProcess");
    function waitForFile(filename) {
      const cmd = `[ -f ${filename} ]`;
      return new ReadyCmd(cmd);
    }
    __name(waitForFile, "waitForFile");
    function waitForTimeout(timeout) {
      const seconds = Math.max(1, Math.floor(timeout / 1e3));
      const cmd = `sleep ${seconds}`;
      return new ReadyCmd(cmd);
    }
    __name(waitForTimeout, "waitForTimeout");
    function parseDockerfile(dockerfileContentOrPath, templateBuilder) {
      let dockerfileContent;
      try {
        if (import_node_fs2.default.existsSync(dockerfileContentOrPath) && import_node_fs2.default.statSync(dockerfileContentOrPath).isFile()) {
          dockerfileContent = import_node_fs2.default.readFileSync(dockerfileContentOrPath, "utf-8");
        } else {
          dockerfileContent = dockerfileContentOrPath;
        }
      } catch (e) {
        dockerfileContent = dockerfileContentOrPath;
      }
      const dockerfile = import_dockerfile_ast.DockerfileParser.parse(dockerfileContent);
      const instructions = dockerfile.getInstructions();
      const fromInstructions = instructions.filter(
        (instruction) => instruction.getKeyword() === "FROM"
      );
      if (fromInstructions.length > 1) {
        throw new Error("Multi-stage Dockerfiles are not supported");
      }
      if (fromInstructions.length === 0) {
        throw new Error("Dockerfile must contain a FROM instruction");
      }
      const fromInstruction = fromInstructions[0];
      const argumentsData = fromInstruction.getArguments();
      let baseImage = "e2bdev/base";
      let userChanged = false;
      let workdirChanged = false;
      if (argumentsData && argumentsData.length > 0) {
        baseImage = argumentsData[0].getValue();
      }
      templateBuilder.setUser("root");
      templateBuilder.setWorkdir("/");
      for (const instruction of instructions) {
        const keyword = instruction.getKeyword();
        switch (keyword) {
          case "FROM":
            break;
          case "RUN":
            handleRunInstruction(instruction, templateBuilder);
            break;
          case "COPY":
          case "ADD":
            handleCopyInstruction(
              instruction,
              templateBuilder
            );
            break;
          case "WORKDIR":
            handleWorkdirInstruction(instruction, templateBuilder);
            workdirChanged = true;
            break;
          case "USER":
            handleUserInstruction(instruction, templateBuilder);
            userChanged = true;
            break;
          case "ENV":
          case "ARG":
            handleEnvInstruction(instruction, templateBuilder);
            break;
          case "EXPOSE":
            break;
          case "VOLUME":
            break;
          case "CMD":
          case "ENTRYPOINT":
            handleCmdEntrypointInstruction(instruction, templateBuilder);
            break;
          default:
            console.warn(`Unsupported instruction: ${keyword}`);
            break;
        }
      }
      if (!userChanged) {
        templateBuilder.setUser("user");
      }
      if (!workdirChanged) {
        templateBuilder.setWorkdir("/home/user");
      }
      return {
        baseImage
      };
    }
    __name(parseDockerfile, "parseDockerfile");
    function handleRunInstruction(instruction, templateBuilder) {
      const argumentsData = instruction.getArguments();
      if (argumentsData && argumentsData.length > 0) {
        const command = argumentsData.map((arg) => arg.getValue()).join(" ");
        templateBuilder.runCmd(command);
      }
    }
    __name(handleRunInstruction, "handleRunInstruction");
    function handleCopyInstruction(instruction, templateBuilder) {
      var _a3;
      const argumentsData = instruction.getArguments();
      if (argumentsData && argumentsData.length >= 2) {
        const dest = argumentsData[argumentsData.length - 1].getValue();
        const sources = argumentsData.slice(0, -1).map((arg) => arg.getValue());
        let user;
        const flags = instruction.getFlags();
        const chownFlag = flags.find((flag) => flag.getName() === "chown");
        if (chownFlag) {
          user = (_a3 = chownFlag.getValue()) != null ? _a3 : void 0;
        }
        for (const src of sources) {
          templateBuilder.copy(src, dest, { user });
        }
      }
    }
    __name(handleCopyInstruction, "handleCopyInstruction");
    function handleWorkdirInstruction(instruction, templateBuilder) {
      const argumentsData = instruction.getArguments();
      if (argumentsData && argumentsData.length > 0) {
        const workdir = argumentsData[0].getValue();
        templateBuilder.setWorkdir(workdir);
      }
    }
    __name(handleWorkdirInstruction, "handleWorkdirInstruction");
    function handleUserInstruction(instruction, templateBuilder) {
      const argumentsData = instruction.getArguments();
      if (argumentsData && argumentsData.length > 0) {
        const user = argumentsData[0].getValue();
        templateBuilder.setUser(user);
      }
    }
    __name(handleUserInstruction, "handleUserInstruction");
    function handleEnvInstruction(instruction, templateBuilder) {
      const argumentsData = instruction.getArguments();
      const keyword = instruction.getKeyword();
      if (argumentsData && argumentsData.length >= 1) {
        const envVars = {};
        if (argumentsData.length === 2) {
          const firstArg = argumentsData[0].getValue();
          const secondArg = argumentsData[1].getValue();
          if (firstArg.includes("=") && secondArg.includes("=")) {
            for (const arg of argumentsData) {
              const envString = arg.getValue();
              const equalIndex = envString.indexOf("=");
              if (equalIndex > 0) {
                const key = envString.substring(0, equalIndex);
                const value = envString.substring(equalIndex + 1);
                envVars[key] = value;
              }
            }
          } else {
            envVars[firstArg] = secondArg;
          }
        } else if (argumentsData.length === 1) {
          const envString = argumentsData[0].getValue();
          const equalIndex = envString.indexOf("=");
          if (equalIndex > 0) {
            const key = envString.substring(0, equalIndex);
            const value = envString.substring(equalIndex + 1);
            envVars[key] = value;
          } else if (keyword === "ARG" && envString.trim()) {
            const key = envString.trim();
            envVars[key] = "";
          }
        } else {
          for (const arg of argumentsData) {
            const envString = arg.getValue();
            const equalIndex = envString.indexOf("=");
            if (equalIndex > 0) {
              const key = envString.substring(0, equalIndex);
              const value = envString.substring(equalIndex + 1);
              envVars[key] = value;
            } else if (keyword === "ARG") {
              const key = envString;
              envVars[key] = "";
            }
          }
        }
        if (Object.keys(envVars).length > 0) {
          templateBuilder.setEnvs(envVars);
        }
      }
    }
    __name(handleEnvInstruction, "handleEnvInstruction");
    function handleCmdEntrypointInstruction(instruction, templateBuilder) {
      const argumentsData = instruction.getArguments();
      if (argumentsData && argumentsData.length > 0) {
        let command = argumentsData.map((arg) => arg.getValue()).join(" ");
        try {
          const parsedCommand = JSON.parse(command);
          if (Array.isArray(parsedCommand)) {
            command = parsedCommand.join(" ");
          }
        } catch (e) {
        }
        templateBuilder.setStartCmd(command, waitForTimeout(2e4));
      }
    }
    __name(handleCmdEntrypointInstruction, "handleCmdEntrypointInstruction");
    var _a2;
    var TemplateBase = class _TemplateBase {
      static {
        __name(this, "_TemplateBase");
      }
      constructor(options) {
        this.defaultBaseImage = "e2bdev/base";
        this.baseImage = this.defaultBaseImage;
        this.baseTemplate = void 0;
        this.registryConfig = void 0;
        this.startCmd = void 0;
        this.readyCmd = void 0;
        this.force = false;
        this.forceNextLayer = false;
        this.instructions = [];
        this.fileContextPath = runtime === "browser" ? "." : (_a2 = getCallerDirectory(STACK_TRACE_DEPTH)) != null ? _a2 : ".";
        this.fileIgnorePatterns = [];
        this.logsRefreshFrequency = 200;
        this.stackTraces = [];
        this.stackTracesEnabled = true;
        this.stackTracesOverride = void 0;
        var _a3, _b;
        this.fileContextPath = (_a3 = options == null ? void 0 : options.fileContextPath) != null ? _a3 : this.fileContextPath;
        this.fileIgnorePatterns = (_b = options == null ? void 0 : options.fileIgnorePatterns) != null ? _b : this.fileIgnorePatterns;
      }
      /**
       * Convert a template to JSON representation.
       *
       * @param template The template to convert
       * @param computeHashes Whether to compute file hashes for cache invalidation
       * @returns JSON string representation of the template
       */
      static toJSON(template, computeHashes = true) {
        return template.toJSON(computeHashes);
      }
      /**
       * Convert a template to Dockerfile format.
       * Note: Templates based on other E2B templates cannot be converted to Dockerfile.
       *
       * @param template The template to convert
       * @returns Dockerfile string representation
       * @throws Error if the template is based on another E2B template
       */
      static toDockerfile(template) {
        return template.toDockerfile();
      }
      static async build(template, nameOrOptions, options) {
        var _a3, _b, _c;
        const { name, buildOptions } = normalizeBuildArguments(
          nameOrOptions,
          options
        );
        try {
          (_a3 = buildOptions.onBuildLogs) == null ? void 0 : _a3.call(buildOptions, new LogEntryStart(/* @__PURE__ */ new Date(), "Build started"));
          const baseTemplate = template;
          const config = new ConnectionConfig(buildOptions);
          const client = new ApiClient(config);
          const data = await baseTemplate.build(client, config, name, buildOptions);
          (_b = buildOptions.onBuildLogs) == null ? void 0 : _b.call(
            buildOptions,
            new LogEntry(/* @__PURE__ */ new Date(), "info", "Waiting for logs...")
          );
          await waitForBuildFinish(client, {
            templateID: data.templateId,
            buildID: data.buildId,
            onBuildLogs: buildOptions.onBuildLogs,
            logsRefreshFrequency: baseTemplate.logsRefreshFrequency,
            stackTraces: baseTemplate.stackTraces,
            signal: buildOptions.signal,
            requestTimeoutMs: config.requestTimeoutMs
          });
          return data;
        } finally {
          (_c = buildOptions.onBuildLogs) == null ? void 0 : _c.call(buildOptions, new LogEntryEnd(/* @__PURE__ */ new Date(), "Build finished"));
        }
      }
      static async buildInBackground(template, nameOrOptions, options) {
        const { name, buildOptions } = normalizeBuildArguments(
          nameOrOptions,
          options
        );
        const config = new ConnectionConfig(buildOptions);
        const client = new ApiClient(config);
        return template.build(client, config, name, buildOptions);
      }
      /**
       * Get the status of a build.
       *
       * @param data Build identifiers
       * @param options Authentication options
       *
       * @example
       * ```ts
       * const status = await Template.getBuildStatus(data, { logsOffset: 0 })
       * ```
       */
      static async getBuildStatus(data, options) {
        const config = new ConnectionConfig(options);
        const client = new ApiClient(config);
        return await getBuildStatus(
          client,
          {
            templateID: data.templateId,
            buildID: data.buildId,
            logsOffset: options == null ? void 0 : options.logsOffset
          },
          config.getSignal(void 0, options == null ? void 0 : options.signal)
        );
      }
      /**
       * Check if a template with the given name exists.
       *
       * @param name Template name to check
       * @param options Authentication options
       * @returns True if the name exists, false otherwise
       *
       * @example
       * ```ts
       * const exists = await Template.exists('my-python-env')
       * if (exists) {
       *   console.log('Template exists!')
       * }
       * ```
       */
      static async exists(name, options) {
        return _TemplateBase.aliasExists(name, options);
      }
      /**
       * Check if a template with the given alias exists.
       *
       * @param alias Template alias to check
       * @param options Authentication options
       * @returns True if the alias exists, false otherwise
       *
       * @deprecated Use `exists` instead.
       * @example
       * ```ts
       * const exists = await Template.aliasExists('my-python-env')
       * if (exists) {
       *   console.log('Template exists!')
       * }
       * ```
       */
      static async aliasExists(alias, options) {
        const config = new ConnectionConfig(options);
        const client = new ApiClient(config);
        return checkAliasExists(
          client,
          { alias },
          config.getSignal(void 0, options == null ? void 0 : options.signal)
        );
      }
      /**
       * Assign tag(s) to an existing template build.
       *
       * @param targetName Template name in 'name:tag' format (the source build to tag from)
       * @param tags Tag or tags to assign
       * @param options Authentication options
       * @returns Tag info with buildId and assigned tags
       *
       * @example
       * ```ts
       * // Assign a single tag
       * await Template.assignTags('my-template:v1.0', 'production')
       *
       * // Assign multiple tags
       * await Template.assignTags('my-template:v1.0', ['production', 'stable'])
       * ```
       */
      static async assignTags(targetName, tags, options) {
        const config = new ConnectionConfig(options);
        const client = new ApiClient(config);
        const normalizedTags = Array.isArray(tags) ? tags : [tags];
        return assignTags(
          client,
          { targetName, tags: normalizedTags },
          config.getSignal(void 0, options == null ? void 0 : options.signal)
        );
      }
      /**
       * Remove tag(s) from a template.
       *
       * @param name Template name
       * @param tags Tag or tags to remove
       * @param options Authentication options
       *
       * @example
       * ```ts
       * // Remove a single tag
       * await Template.removeTags('my-template', 'production')
       *
       * // Remove multiple tags from a template
       * await Template.removeTags('my-template', ['production', 'staging'])
       * ```
       */
      static async removeTags(name, tags, options) {
        const config = new ConnectionConfig(options);
        const client = new ApiClient(config);
        const normalizedTags = Array.isArray(tags) ? tags : [tags];
        return removeTags(
          client,
          { name, tags: normalizedTags },
          config.getSignal(void 0, options == null ? void 0 : options.signal)
        );
      }
      /**
       * Get all tags for a template.
       *
       * @param templateId Template ID or name
       * @param options Authentication options
       * @returns Array of tag details including tag name, buildId, and creation date
       *
       * @example
       * ```ts
       * const tags = await Template.getTags('my-template')
       * for (const tag of tags) {
       *   console.log(`Tag: ${tag.tag}, Build: ${tag.buildId}, Created: ${tag.createdAt}`)
       * }
       * ```
       */
      static async getTags(templateId, options) {
        const config = new ConnectionConfig(options);
        const client = new ApiClient(config);
        return getTemplateTags(
          client,
          { templateID: templateId },
          config.getSignal(void 0, options == null ? void 0 : options.signal)
        );
      }
      fromDebianImage(variant = "stable") {
        return this.fromImage(`debian:${variant}`);
      }
      fromUbuntuImage(variant = "latest") {
        return this.fromImage(`ubuntu:${variant}`);
      }
      fromPythonImage(version2 = "3") {
        return this.fromImage(`python:${version2}`);
      }
      fromNodeImage(variant = "lts") {
        return this.fromImage(`node:${variant}`);
      }
      fromBunImage(variant = "latest") {
        return this.fromImage(`oven/bun:${variant}`);
      }
      fromBaseImage() {
        return this.fromImage(this.defaultBaseImage);
      }
      fromImage(baseImage, credentials) {
        this.baseImage = baseImage;
        this.baseTemplate = void 0;
        if (credentials) {
          this.registryConfig = {
            type: "registry",
            username: credentials.username,
            password: credentials.password
          };
        }
        if (this.forceNextLayer) {
          this.force = true;
        }
        this.collectStackTrace();
        return this;
      }
      fromTemplate(template) {
        this.baseTemplate = template;
        this.baseImage = void 0;
        if (this.forceNextLayer) {
          this.force = true;
        }
        this.collectStackTrace();
        return this;
      }
      fromDockerfile(dockerfileContentOrPath) {
        const { baseImage } = this.runInStackTraceOverrideContext(
          () => parseDockerfile(dockerfileContentOrPath, this),
          // -1 as we're going up the call stack from the parseDockerfile function
          getCallerFrame(STACK_TRACE_DEPTH - 1)
        );
        this.baseImage = baseImage;
        this.baseTemplate = void 0;
        if (this.forceNextLayer) {
          this.force = true;
        }
        this.collectStackTrace();
        return this;
      }
      fromAWSRegistry(image, credentials) {
        this.baseImage = image;
        this.baseTemplate = void 0;
        this.registryConfig = {
          type: "aws",
          awsAccessKeyId: credentials.accessKeyId,
          awsSecretAccessKey: credentials.secretAccessKey,
          awsRegion: credentials.region
        };
        if (this.forceNextLayer) {
          this.force = true;
        }
        this.collectStackTrace();
        return this;
      }
      fromGCPRegistry(image, credentials) {
        this.baseImage = image;
        this.baseTemplate = void 0;
        this.registryConfig = {
          type: "gcp",
          serviceAccountJson: readGCPServiceAccountJSON(
            this.fileContextPath.toString(),
            credentials.serviceAccountJSON
          )
        };
        if (this.forceNextLayer) {
          this.force = true;
        }
        this.collectStackTrace();
        return this;
      }
      copy(src, dest, options) {
        var _a3;
        if (runtime === "browser") {
          throw new Error("Browser runtime is not supported for copy");
        }
        const srcs = Array.isArray(src) ? src : [src];
        const stackTrace = getCallerFrame(STACK_TRACE_DEPTH - 1);
        for (const src2 of srcs) {
          const srcString = src2.toString();
          validateRelativePath(srcString, stackTrace);
          const args = [
            srcString,
            dest.toString(),
            (_a3 = options == null ? void 0 : options.user) != null ? _a3 : "",
            (options == null ? void 0 : options.mode) ? padOctal(options.mode) : ""
          ];
          this.instructions.push({
            type: "COPY",
            args,
            force: (options == null ? void 0 : options.forceUpload) || this.forceNextLayer,
            forceUpload: options == null ? void 0 : options.forceUpload,
            resolveSymlinks: options == null ? void 0 : options.resolveSymlinks
          });
        }
        this.collectStackTrace();
        return this;
      }
      copyItems(items) {
        if (runtime === "browser") {
          throw new Error("Browser runtime is not supported for copyItems");
        }
        const stackTrace = getCallerFrame(STACK_TRACE_DEPTH - 1);
        this.runInNewStackTraceContext(() => {
          for (const item of items) {
            try {
              this.copy(item.src, item.dest, {
                forceUpload: item.forceUpload,
                user: item.user,
                mode: item.mode,
                resolveSymlinks: item.resolveSymlinks
              });
            } catch (error) {
              const copyError = error;
              copyError.stack = stackTrace;
              throw copyError;
            }
          }
        });
        return this;
      }
      remove(path2, options) {
        const paths2 = Array.isArray(path2) ? path2 : [path2];
        const args = ["rm"];
        if (options == null ? void 0 : options.recursive) {
          args.push("-r");
        }
        if (options == null ? void 0 : options.force) {
          args.push("-f");
        }
        args.push(...paths2.map((p) => p.toString()));
        return this.runInNewStackTraceContext(
          () => this.runCmd(args.join(" "), { user: options == null ? void 0 : options.user })
        );
      }
      rename(src, dest, options) {
        const args = ["mv", src.toString(), dest.toString()];
        if (options == null ? void 0 : options.force) {
          args.push("-f");
        }
        return this.runInNewStackTraceContext(
          () => this.runCmd(args.join(" "), { user: options == null ? void 0 : options.user })
        );
      }
      makeDir(path2, options) {
        const paths2 = Array.isArray(path2) ? path2 : [path2];
        const args = ["mkdir", "-p"];
        if (options == null ? void 0 : options.mode) {
          args.push(`-m ${padOctal(options.mode)}`);
        }
        args.push(...paths2.map((p) => p.toString()));
        return this.runInNewStackTraceContext(
          () => this.runCmd(args.join(" "), { user: options == null ? void 0 : options.user })
        );
      }
      makeSymlink(src, dest, options) {
        const args = ["ln", "-s"];
        if (options == null ? void 0 : options.force) {
          args.push("-f");
        }
        args.push(src.toString(), dest.toString());
        return this.runInNewStackTraceContext(
          () => this.runCmd(args.join(" "), { user: options == null ? void 0 : options.user })
        );
      }
      runCmd(commandOrCommands, options) {
        const cmds = Array.isArray(commandOrCommands) ? commandOrCommands : [commandOrCommands];
        const args = [cmds.join(" && ")];
        if (options == null ? void 0 : options.user) {
          args.push(options.user);
        }
        this.instructions.push({
          type: "RUN",
          args,
          force: this.forceNextLayer
        });
        this.collectStackTrace();
        return this;
      }
      setWorkdir(workdir) {
        this.instructions.push({
          type: "WORKDIR",
          args: [workdir.toString()],
          force: this.forceNextLayer
        });
        this.collectStackTrace();
        return this;
      }
      setUser(user) {
        this.instructions.push({
          type: "USER",
          args: [user],
          force: this.forceNextLayer
        });
        this.collectStackTrace();
        return this;
      }
      pipInstall(packages, options) {
        var _a3;
        const g = (_a3 = options == null ? void 0 : options.g) != null ? _a3 : true;
        const args = ["pip", "install"];
        const packageList = packages ? Array.isArray(packages) ? packages : [packages] : void 0;
        if (g === false) {
          args.push("--user");
        }
        if (packageList) {
          args.push(...packageList);
        } else {
          args.push(".");
        }
        return this.runInNewStackTraceContext(
          () => this.runCmd(args.join(" "), {
            user: g ? "root" : void 0
          })
        );
      }
      npmInstall(packages, options) {
        const args = ["npm", "install"];
        const packageList = packages ? Array.isArray(packages) ? packages : [packages] : void 0;
        if (options == null ? void 0 : options.g) {
          args.push("-g");
        }
        if (options == null ? void 0 : options.dev) {
          args.push("--save-dev");
        }
        if (packageList) {
          args.push(...packageList);
        }
        return this.runInNewStackTraceContext(
          () => this.runCmd(args.join(" "), {
            user: (options == null ? void 0 : options.g) ? "root" : void 0
          })
        );
      }
      bunInstall(packages, options) {
        const args = ["bun", "install"];
        const packageList = packages ? Array.isArray(packages) ? packages : [packages] : void 0;
        if (options == null ? void 0 : options.g) {
          args.push("-g");
        }
        if (options == null ? void 0 : options.dev) {
          args.push("--dev");
        }
        if (packageList) {
          args.push(...packageList);
        }
        return this.runInNewStackTraceContext(
          () => this.runCmd(args.join(" "), {
            user: (options == null ? void 0 : options.g) ? "root" : void 0
          })
        );
      }
      aptInstall(packages, options) {
        const packageList = Array.isArray(packages) ? packages : [packages];
        return this.runInNewStackTraceContext(
          () => this.runCmd(
            [
              "apt-get update",
              `DEBIAN_FRONTEND=noninteractive DEBCONF_NOWARNINGS=yes apt-get install -y ${(options == null ? void 0 : options.noInstallRecommends) ? "--no-install-recommends " : ""}${(options == null ? void 0 : options.fixMissing) ? "--fix-missing " : ""}${packageList.join(
                " "
              )}`
            ],
            { user: "root" }
          )
        );
      }
      addMcpServer(servers) {
        if (this.baseTemplate !== "mcp-gateway") {
          throw new BuildError(
            "MCP servers can only be added to mcp-gateway template",
            getCallerFrame(STACK_TRACE_DEPTH - 1)
          );
        }
        const serverList = Array.isArray(servers) ? servers : [servers];
        return this.runInNewStackTraceContext(
          () => this.runCmd(`mcp-gateway pull ${serverList.join(" ")}`, {
            user: "root"
          })
        );
      }
      gitClone(url, path2, options) {
        const args = ["git", "clone", url];
        if (options == null ? void 0 : options.branch) {
          args.push(`--branch ${options.branch}`);
          args.push("--single-branch");
        }
        if (options == null ? void 0 : options.depth) {
          args.push(`--depth ${options.depth}`);
        }
        if (path2) {
          args.push(path2.toString());
        }
        return this.runInNewStackTraceContext(
          () => this.runCmd(args.join(" "), { user: options == null ? void 0 : options.user })
        );
      }
      setStartCmd(startCommand, readyCommand) {
        this.startCmd = startCommand;
        if (readyCommand instanceof ReadyCmd) {
          this.readyCmd = readyCommand.getCmd();
        } else {
          this.readyCmd = readyCommand;
        }
        this.collectStackTrace();
        return this;
      }
      setReadyCmd(readyCommand) {
        if (readyCommand instanceof ReadyCmd) {
          this.readyCmd = readyCommand.getCmd();
        } else {
          this.readyCmd = readyCommand;
        }
        this.collectStackTrace();
        return this;
      }
      setEnvs(envs) {
        if (Object.keys(envs).length === 0) {
          return this;
        }
        this.instructions.push({
          type: "ENV",
          args: Object.entries(envs).flatMap(([key, value]) => [key, value]),
          force: this.forceNextLayer
        });
        this.collectStackTrace();
        return this;
      }
      skipCache() {
        this.forceNextLayer = true;
        return this;
      }
      betaDevContainerPrebuild(devcontainerDirectory) {
        if (this.baseTemplate !== "devcontainer") {
          throw new BuildError(
            "Devcontainers can only used in the devcontainer template",
            getCallerFrame(STACK_TRACE_DEPTH - 1)
          );
        }
        return this.runInNewStackTraceContext(() => {
          return this.runCmd(
            `devcontainer build --workspace-folder ${devcontainerDirectory}`,
            { user: "root" }
          );
        });
      }
      betaSetDevContainerStart(devcontainerDirectory) {
        if (this.baseTemplate !== "devcontainer") {
          throw new BuildError(
            "Devcontainers can only used in the devcontainer template",
            getCallerFrame(STACK_TRACE_DEPTH - 1)
          );
        }
        return this.runInNewStackTraceContext(() => {
          return this.setStartCmd(
            `sudo devcontainer up --workspace-folder ${devcontainerDirectory} && sudo /prepare-exec.sh ${devcontainerDirectory} | sudo tee /devcontainer.sh > /dev/null && sudo chmod +x /devcontainer.sh && sudo touch /devcontainer.up`,
            waitForFile("/devcontainer.up")
          );
        });
      }
      /**
       * Collect the current stack trace for debugging purposes.
       *
       * @param stackTracesDepth Depth to traverse in the call stack
       * @returns this for method chaining
       */
      collectStackTrace(stackTracesDepth = STACK_TRACE_DEPTH) {
        if (!this.stackTracesEnabled) {
          return this;
        }
        if (this.stackTracesOverride) {
          this.stackTraces.push(this.stackTracesOverride);
          return this;
        }
        this.stackTraces.push(getCallerFrame(stackTracesDepth));
        return this;
      }
      /**
       * Temporarily disable stack trace collection.
       *
       * @returns this for method chaining
       */
      disableStackTrace() {
        this.stackTracesEnabled = false;
        return this;
      }
      /**
       * Re-enable stack trace collection.
       *
       * @returns this for method chaining
       */
      enableStackTrace() {
        this.stackTracesEnabled = true;
        return this;
      }
      /**
       * Execute a function in a clean stack trace context.
       *
       * @param fn Function to execute
       * @returns The result of the function
       */
      runInNewStackTraceContext(fn) {
        this.disableStackTrace();
        const result = fn();
        this.enableStackTrace();
        this.collectStackTrace(STACK_TRACE_DEPTH + 1);
        return result;
      }
      runInStackTraceOverrideContext(fn, stackTraceOverride) {
        this.stackTracesOverride = stackTraceOverride;
        const result = fn();
        this.stackTracesOverride = void 0;
        return result;
      }
      /**
       * Convert the template to JSON representation.
       *
       * @param computeHashes Whether to compute file hashes for COPY instructions
       * @returns JSON string representation of the template
       */
      async toJSON(computeHashes) {
        let instructions = this.instructions;
        if (computeHashes) {
          instructions = await this.instructionsWithHashes();
        }
        return JSON.stringify(this.serialize(instructions), void 0, 2);
      }
      /**
       * Convert the template to Dockerfile format.
       *
       * Note: Only templates based on Docker images can be converted to Dockerfile.
       * Templates based on other E2B templates cannot be converted because they
       * may use features not available in standard Dockerfiles.
       *
       * @returns Dockerfile string representation
       * @throws Error if template is based on another E2B template or has no base image
       */
      toDockerfile() {
        if (this.baseTemplate !== void 0) {
          throw new Error(
            "Cannot convert template built from another template to Dockerfile. Templates based on other templates can only be built using the E2B API."
          );
        }
        if (this.baseImage === void 0) {
          throw new Error("No base image specified for template");
        }
        let dockerfile = `FROM ${this.baseImage}
`;
        for (const instruction of this.instructions) {
          if (instruction.type === "RUN") {
            dockerfile += `RUN ${instruction.args[0]}
`;
            continue;
          }
          if (instruction.type === "COPY") {
            dockerfile += `COPY ${instruction.args[0]} ${instruction.args[1]}
`;
            continue;
          }
          if (instruction.type === "ENV") {
            const values = [];
            for (let i = 0; i < instruction.args.length; i += 2) {
              values.push(`${instruction.args[i]}=${instruction.args[i + 1]}`);
            }
            dockerfile += `ENV ${values.join(" ")}
`;
            continue;
          }
          dockerfile += `${instruction.type} ${instruction.args.join(" ")}
`;
        }
        if (this.startCmd) {
          dockerfile += `ENTRYPOINT ${this.startCmd}
`;
        }
        return dockerfile;
      }
      /**
       * Internal implementation of the template build process.
       *
       * @param client API client for communicating with E2B backend
       * @param name Template name in 'name' or 'name:tag' format
       * @param tags Additional tags to assign to the build
       * @param options Build configuration options
       * @throws BuildError if the build fails
       */
      async build(client, config, name, options) {
        var _a3, _b, _c, _d, _e, _f;
        if (options.skipCache) {
          this.force = true;
        }
        (_a3 = options.onBuildLogs) == null ? void 0 : _a3.call(
          options,
          new LogEntry(
            /* @__PURE__ */ new Date(),
            "info",
            `Requesting build for template: ${name}${options.tags && options.tags.length > 0 ? ` with tags ${options.tags.join(", ")}` : ""}`
          )
        );
        const {
          templateID,
          buildID,
          tags: responseTags
        } = await requestBuild(
          client,
          {
            name,
            tags: options.tags,
            cpuCount: (_b = options.cpuCount) != null ? _b : 2,
            memoryMB: (_c = options.memoryMB) != null ? _c : 1024
          },
          config.getSignal(void 0, options.signal)
        );
        (_d = options.onBuildLogs) == null ? void 0 : _d.call(
          options,
          new LogEntry(
            /* @__PURE__ */ new Date(),
            "info",
            `Template created with ID: ${templateID}, Build ID: ${buildID}`
          )
        );
        const instructionsWithHashes = await this.instructionsWithHashes();
        const uploadPromises = instructionsWithHashes.map(
          async (instruction, index) => {
            var _a4, _b2, _c2, _d2;
            if (instruction.type !== "COPY") {
              return;
            }
            const src = instruction.args.length > 0 ? instruction.args[0] : null;
            const filesHash = (_a4 = instruction.filesHash) != null ? _a4 : null;
            if (src === null || filesHash === null) {
              throw new Error("Source path and files hash are required");
            }
            const forceUpload = instruction.forceUpload;
            let stackTrace = void 0;
            if (index + 1 >= 0 && index + 1 < this.stackTraces.length) {
              stackTrace = this.stackTraces[index + 1];
            }
            const { present, url } = await getFileUploadLink(
              client,
              {
                templateID,
                filesHash
              },
              stackTrace,
              config.getSignal(void 0, options.signal)
            );
            if (forceUpload && url != null || present === false && url != null) {
              await uploadFile(
                {
                  fileName: src,
                  fileContextPath: this.fileContextPath.toString(),
                  url,
                  ignorePatterns: [
                    ...this.fileIgnorePatterns,
                    ...readDockerignore(this.fileContextPath.toString())
                  ],
                  resolveSymlinks: (_b2 = instruction.resolveSymlinks) != null ? _b2 : RESOLVE_SYMLINKS
                },
                stackTrace,
                // Forward `requestTimeoutMs` only when the caller set it — we
                // never want to slap the 60s default on a multi-hundred-MB S3
                // upload, but a user-set per-build timeout should govern the
                // whole operation, including uploads.
                {
                  signal: options.signal,
                  requestTimeoutMs: options.requestTimeoutMs
                }
              );
              (_c2 = options.onBuildLogs) == null ? void 0 : _c2.call(
                options,
                new LogEntry(/* @__PURE__ */ new Date(), "info", `Uploaded '${src}'`)
              );
            } else {
              (_d2 = options.onBuildLogs) == null ? void 0 : _d2.call(
                options,
                new LogEntry(
                  /* @__PURE__ */ new Date(),
                  "info",
                  `Skipping upload of '${src}', already cached`
                )
              );
            }
          }
        );
        await Promise.all(uploadPromises);
        (_e = options.onBuildLogs) == null ? void 0 : _e.call(
          options,
          new LogEntry(/* @__PURE__ */ new Date(), "info", "All file uploads completed")
        );
        (_f = options.onBuildLogs) == null ? void 0 : _f.call(
          options,
          new LogEntry(/* @__PURE__ */ new Date(), "info", "Starting building...")
        );
        await triggerBuild(
          client,
          {
            templateID,
            buildID,
            template: this.serialize(instructionsWithHashes)
          },
          config.getSignal(void 0, options.signal)
        );
        return {
          alias: name,
          name,
          tags: responseTags,
          templateId: templateID,
          buildId: buildID
        };
      }
      /**
       * Add file hashes to COPY instructions for cache invalidation.
       *
       * @returns Copy of instructions array with filesHash added to COPY instructions
       */
      async instructionsWithHashes() {
        return Promise.all(
          this.instructions.map(async (instruction, index) => {
            var _a3;
            if (instruction.type !== "COPY") {
              return instruction;
            }
            const src = instruction.args.length > 0 ? instruction.args[0] : null;
            const dest = instruction.args.length > 1 ? instruction.args[1] : null;
            if (src === null || dest === null) {
              throw new Error("Source path and destination path are required");
            }
            let stackTrace = void 0;
            if (index + 1 >= 0 && index + 1 < this.stackTraces.length) {
              stackTrace = this.stackTraces[index + 1];
            }
            return __spreadProps(__spreadValues({}, instruction), {
              filesHash: await calculateFilesHash(
                src,
                dest,
                this.fileContextPath.toString(),
                [
                  ...this.fileIgnorePatterns,
                  ...runtime === "browser" ? [] : readDockerignore(this.fileContextPath.toString())
                ],
                (_a3 = instruction.resolveSymlinks) != null ? _a3 : RESOLVE_SYMLINKS,
                stackTrace
              )
            });
          })
        );
      }
      /**
       * Serialize the template to the API request format.
       *
       * @param steps Array of build instructions with file hashes
       * @returns Template data formatted for the API
       */
      serialize(steps) {
        const templateData = {
          startCmd: this.startCmd,
          readyCmd: this.readyCmd,
          steps,
          force: this.force
        };
        if (this.baseImage !== void 0) {
          templateData.fromImage = this.baseImage;
        }
        if (this.baseTemplate !== void 0) {
          templateData.fromTemplate = this.baseTemplate;
        }
        if (this.registryConfig !== void 0) {
          templateData.fromImageRegistry = this.registryConfig;
        }
        return templateData;
      }
    };
    function Template(options) {
      return new TemplateBase(options);
    }
    __name(Template, "Template");
    Template.build = TemplateBase.build;
    Template.buildInBackground = TemplateBase.buildInBackground;
    Template.getBuildStatus = TemplateBase.getBuildStatus;
    Template.exists = TemplateBase.exists;
    Template.aliasExists = TemplateBase.aliasExists;
    Template.assignTags = TemplateBase.assignTags;
    Template.removeTags = TemplateBase.removeTags;
    Template.getTags = TemplateBase.getTags;
    Template.toJSON = TemplateBase.toJSON;
    Template.toDockerfile = TemplateBase.toDockerfile;
    var src_default = Sandbox;
  }
});

export {
  require_dist2 as require_dist
};
/*! Bundled license information:

platform/platform.js:
  (*!
   * Platform.js v1.3.6
   * Copyright 2014-2020 Benjamin Tan
   * Copyright 2011-2013 John-David Dalton
   * Available under MIT license
   *)
*/
//# sourceMappingURL=chunk-IJP64K7S.mjs.map
